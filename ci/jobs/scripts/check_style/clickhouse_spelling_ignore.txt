# Exceptions for the `clickhouse_spelling` check in ci/jobs/check_style.py, which requires the
# product name to be spelled `ClickHouse`; it also accepts the conventional token spellings
# `clickhouse` and `CLICKHOUSE`.
#
# One entry per line. A line holding only a path exempts the whole file; a path followed by a
# literal string exempts only the lines of that file which contain that string. Lines starting
# with `#` are comments.
#
# An exception is for text that is not ours to spell: a name defined by another project, a
# verbatim quotation, or a misspelling a test deliberately feeds to the server. Prose, comments,
# identifiers and messages we write ourselves are never exempt - fix the spelling instead.

# The check, this list and the rule in the style guide have to spell out the misspellings they
# are about.
ci/jobs/scripts/check_style/clickhouse_spelling_ignore.txt
ci/jobs/scripts/check_style/clickhouse_spelling.py
# The changelog editing skill spells out the misspellings this check rejects.
.claude/skills/edit-changelog/SKILL.md	`Clickhouse`
ci/jobs/check_style.py	`Clickhouse`
ci/jobs/check_style.py	`click_house`
docs/resources/develop-contribute/contribute/style.mdx	Not `Clickhouse`

# Published documentation fragments are public URLs. Preserve these legacy anchors while
# enforcing the product spelling in their visible headings and everywhere else.
docs/concepts/core-concepts/primary-indexes.mdx	{#how-does-the-sparse-primary-index-work-in-clickHouse}
docs/concepts/core-concepts/query-parallelism.mdx	{#step-by-step-how-clickHouse-parallelizes-an-aggregation-query}

# These private-only paths are checked after this public change is synchronized. Their source
# files cannot be corrected in this repository; keep the exceptions narrow until the private
# sources are updated independently.
ci/jobs/scripts/cloud_utils/dataplane_api.py	Clickhouse is not available
tests/integration/test_shared_merge_tree_lost_blobs_complex_columns/test.py	Clickhouse does not remove blobs

# `ClickhouseClientOptions` / `ClickhouseOptions` are declared by our fork of the Azure SDK
# (contrib/azure, blob_options.hpp); renaming them belongs to that repository.
src/Disks/DiskObjectStorage/ObjectStorages/AzureBlobStorage/AzureBlobStorageCommon.cpp	ClickhouseOptions

# Question titles from the Stack Overflow dataset, quoted verbatim as query output.
docs/get-started/sample-datasets/stackoverflow.mdx	Clickhouse
docs/concepts/features/dictionaries/index.mdx	Clickhouse select last record

# `clickHouse*` are the parameter names of Google's Dataflow templates, defined by that product.
docs/integrations/connectors/data-ingestion/GCP/google-dataflow/templates/pubsub-to-clickhouse.mdx	clickHouse
docs/integrations/connectors/data-ingestion/GCP/google-dataflow/templates/bigquery-to-clickhouse.mdx	clickHouse

# The name of a third-party client library, as published by its author.
docs/integrations/language-clients/third-party/client-libraries.mdx	php-click-house-client

# The C# client documentation is synced from `ClickHouse/clickhouse-cs`; preserve its public
# `clickHouseType` parameter name.
docs/integrations/language-clients/csharp/overview.mdx	clickHouseType

# Tests that feed misspellings to the server on purpose: case variants for `LIKE` / `ILIKE`, a
# two-word phrase that must not match the one-word name, a token separator, an unknown URI scheme.
tests/queries/0_stateless/02346_text_index_function_like.sql	ClickhouseSQL
tests/queries/0_stateless/02346_text_index_function_ilike.sql	ClickhouseSQL
tests/queries/0_stateless/02346_text_index_function_hasPhrase.sql	'Click House'
tests/queries/0_stateless/02346_text_index_function_hasPhrase_positions.sql	'Click House'
tests/queries/0_stateless/02346_text_index_prewhere_support.sh	click-house,fast/io;merge.tree
tests/queries/0_stateless/02784_connection_string.sh	runClient "click_house:"

# A crawled web page kept verbatim as a test fixture.
tests/queries/0_stateless/03578_parallel_replicas_minicrawl.sql
