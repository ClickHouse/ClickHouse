1
This file is part of the TEMP experiment commit (run perf comparison 5x).
The praktika success-cache keys perf jobs by (name, digest-of-perf-paths) with
no sha: a push that does not touch a perf digest path would cache-skip all perf
jobs and re-report the previous results. Bump the number above in every push
that should actually re-measure. Delete this file together with the TEMP commit.
