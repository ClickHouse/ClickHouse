<test>

    <type>once</type>

    <tags>
        <tag>long</tag>
    </tags>

    <stop_conditions>
        <any_of>
            <average_speed_not_changing_for_ms>1000</average_speed_not_changing_for_ms>
            <total_time_ms>10000</total_time_ms>
        </any_of>
    </stop_conditions>

    <!-- lon [-180; 180], lat [-90; 90] -->
    <query>SELECT count() FROM system.numbers WHERE NOT ignore(greatCircleDistance((rand() % 360) * 1. - 180, (number % 150) * 1.2 - 90, (number % 359) + (1 / rand()) - 180, (rand() % 180) * 1. - 90))</query>
</test>