name: Documentation Team Review Requests

on:
  pull_request:
    branches: [master]
    types: [opened, reopened, synchronize]
  pull_request_target:
    branches: [master]
    types: [opened, reopened, synchronize, labeled]

concurrency:
  group: documentation-team-review-requests-${{ github.event_name }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: read

jobs:
  request-reviews-internal:
    if: >-
      github.event_name == 'pull_request' &&
      github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - name: Check out internal PR revision
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          persist-credentials: false

      - name: Request documentation team reviews
        env:
          GH_TOKEN: ${{ secrets.ROBOT_CLICKHOUSE_COMMIT_TOKEN }}
        run: python3 -m ci.jobs.scripts.team_review_requests

  request-reviews-fork:
    if: >-
      github.event_name == 'pull_request_target' &&
      github.event.pull_request.head.repo.full_name != github.repository &&
      contains(github.event.pull_request.labels.*.name, 'can be tested')
    runs-on: ubuntu-latest
    steps:
      - name: Check out trusted base revision
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          persist-credentials: false

      - name: Request documentation team reviews
        env:
          GH_TOKEN: ${{ secrets.ROBOT_CLICKHOUSE_COMMIT_TOKEN }}
        run: python3 -m ci.jobs.scripts.team_review_requests
