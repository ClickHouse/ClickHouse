---
title: Função Template
---



## functionName {#functionname-in-lower-case}

Descrição breve.

**Sintaxe** (sem SELECT)

```sql
<function syntax>
```

Alias: `<alias name>`. (Opcional)

Mais texto (Opcional).

**Argumentos** (Opcional)

-   `x` — Descrição. Opcional (somente para argumentos opcionais). Valores possíveis: <values list>. Valor padrão: <value>. [Type name](relative/path/to/type/dscr.md#type).
-   `y` — Descrição. Opcional (somente para argumentos opcionais). Valores possíveis: <values list>. Valor padrão: <value>. [Type name](relative/path/to/type/dscr.md#type).

**Parâmetros** (Opcional, somente para funções de agregação paramétricas)

-   `z` — Descrição. Opcional (somente para parâmetros opcionais). Valores possíveis: <values list>. Valor padrão: <value>. [Type name](relative/path/to/type/dscr.md#type).

**Valor(es) retornado(s)**

-   Lista de valores retornados.

Tipo: [Type name](relative/path/to/type/dscr.md#type).

**Exemplo**

O exemplo deve demonstrar o uso e/ou casos de uso. O texto a seguir contém as partes recomendadas de um exemplo.

Tabela de entrada (Opcional):

```text
```

```sql title="Query"
```

```text title="Response"
```

**Veja Também** (Opcional)

-   [vincular](#)