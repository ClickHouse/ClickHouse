---
description: 'Documentação de Funções de Tabela'
sidebar_label: 'Funções de Tabela'
sidebar_position: 1
slug: /sql-reference/table-functions/
title: 'Funções de Tabela'
doc_type: 'reference'
---

As funções de tabela são métodos de construção de tabelas.

{/*AUTOGENERATED_START*/ }

{/*AUTOGENERATED_END*/ }

<div id="usage">
  ## Uso
</div>

As funções de tabela podem ser usadas na cláusula [`FROM`](../../sql-reference/statements/select/from.md)
de uma consulta `SELECT`. Por exemplo, você pode usar `SELECT` para consultar dados de um arquivo na sua máquina local
usando a função de tabela `file`.

```bash title="Query"
echo "1, 2, 3" > example.csv
```

```text title="Response"
./clickhouse client
:) SELECT * FROM file('example.csv')
┌─c1─┬─c2─┬─c3─┐
│  1 │  2 │  3 │
└────┴────┴────┘
```

Você também pode usar funções de tabela para criar uma tabela temporária disponível
apenas na consulta atual. Por exemplo:

```sql title="Query"
SELECT * FROM generateSeries(1,5);
```

```response title="Response"
┌─generate_series─┐
│               1 │
│               2 │
│               3 │
│               4 │
│               5 │
└─────────────────┘
```

A tabela é excluída quando a consulta termina.

As funções de tabela podem ser usadas para criar tabelas usando a seguinte sintaxe:

```sql title="Query"
CREATE TABLE [IF NOT EXISTS] [db.]table_name AS table_function()
```

Por exemplo:

```sql title="Query"
CREATE TABLE series AS generateSeries(1, 5);
SELECT * FROM series;
```

```response title="Response"
┌─generate_series─┐
│               1 │
│               2 │
│               3 │
│               4 │
│               5 │
└─────────────────┘
```

Por fim, funções de tabela podem ser usadas para inserir dados em uma tabela. Por exemplo,
poderíamos gravar o conteúdo da tabela que criamos no exemplo anterior
em um arquivo em disco usando novamente a função de tabela `file`:

```sql title="Query"
INSERT INTO FUNCTION file('numbers.csv', 'CSV') SELECT * FROM series;
```

```bash title="Query"
cat numbers.csv
1
2
3
4
5
```

:::note
Você não pode usar funções de tabela se a configuração [allow&#95;ddl](/pt-BR/operations/settings/settings#allow_ddl) estiver desativada.
:::