---
slug: /sql-reference/functions/geo/s2
sidebar_label: 'Geometria S2'
title: 'Funções para trabalhar com índice S2'
description: 'Documentação das funções para trabalhar com índices S2'
doc_type: 'reference'
---

<div id="s2index">
  ## S2Index
</div>

[S2](https://s2geometry.io/) é um sistema de indexação geográfica em que todos os dados geográficos são representados em uma esfera (semelhante a um globo terrestre).

Na biblioteca S2, os pontos são representados como o índice S2 — um número específico que codifica internamente um ponto na superfície de uma esfera unitária, em vez de usar os pares tradicionais (latitude, longitude). Para obter o índice de ponto S2 de um determinado ponto especificado no formato (latitude, longitude), use a função [geoToS2](#geotos2). Além disso, você pode usar a função [s2ToGeo](#s2togeo) para obter as coordenadas geográficas correspondentes ao índice de ponto S2 especificado.

<div id="geotos2">
  ## geoToS2
</div>

Retorna o índice do ponto [S2](#s2index) correspondente às coordenadas fornecidas `(longitude, latitude)`.

**Sintaxe**

```sql
geoToS2(lon, lat)
```

**Argumentos**

* `lon` — Longitude. [Float64](../../data-types/float.md).
* `lat` — Latitude. [Float64](../../data-types/float.md).

**Valores retornados**

* índice de ponto S2. [UInt64](../../data-types/int-uint.md).

**Exemplo**

```sql title="Query"
SELECT geoToS2(37.79506683, 55.71290588) AS s2Index;
```

```text title="Response"
┌─────────────s2Index─┐
│ 4704772434919038107 │
└─────────────────────┘
```

<div id="s2togeo">
  ## s2ToGeo
</div>

Retorna as coordenadas geográficas `(longitude, latitude)` correspondentes ao índice de ponto [S2](#s2index) fornecido.

**Sintaxe**

```sql
s2ToGeo(s2index)
```

**Argumentos**

* `s2index` — índice S2. [UInt64](../../data-types/int-uint.md).

**Valores retornados**

* Uma [tupla](../../data-types/tuple.md) composta de dois valores:
  * `lon`. [Float64](../../data-types/float.md).
  * `lat`. [Float64](../../data-types/float.md).

**Exemplo**

```sql title="Query"
SELECT s2ToGeo(4704772434919038107) AS s2Coodrinates;
```

```text title="Response"
┌─s2Coodrinates────────────────────────┐
│ (37.79506681471008,55.7129059052841) │
└──────────────────────────────────────┘
```

<div id="s2getneighbors">
  ## s2GetNeighbors
</div>

Retorna os índices vizinhos do S2 correspondentes ao [S2](#s2index) informado. Cada célula do sistema S2 é um quadrilátero delimitado por quatro geodésicas. Portanto, cada célula tem 4 vizinhos.

**Sintaxe**

```sql
s2GetNeighbors(s2index)
```

**Argumentos**

* `s2index` — Índice S2. [UInt64](../../data-types/int-uint.md).

**Valor retornado**

* Um array formado por 4 índices vizinhos: `array[s2index1, s2index3, s2index2, s2index4]`. [Array](../../data-types/array.md)([UInt64](../../data-types/int-uint.md)).

**Exemplo**

```sql title="Query"
SELECT s2GetNeighbors(5074766849661468672) AS s2Neighbors;
```

```text title="Response"
┌─s2Neighbors───────────────────────────────────────────────────────────────────────┐
│ [5074766987100422144,5074766712222515200,5074767536856236032,5074767261978329088] │
└───────────────────────────────────────────────────────────────────────────────────┘
```

<div id="s2cellsintersect">
  ## s2CellsIntersect
</div>

Determina se as duas células [S2](#s2index) fornecidas se intersectam ou não.

**Sintaxe**

```sql
s2CellsIntersect(s2index1, s2index2)
```

**Argumentos**

* `siIndex1`, `s2index2` — índice S2. [UInt64](../../data-types/int-uint.md).

**Valor retornado**

* `1` — Se as células se intersectarem. [UInt8](../../data-types/int-uint.md).
* `0` — Se as células não se intersectarem. [UInt8](../../data-types/int-uint.md).

**Exemplo**

```sql title="Query"
SELECT s2CellsIntersect(9926595209846587392, 9926594385212866560) AS intersect;
```

```text title="Response"
┌─intersect─┐
│         1 │
└───────────┘
```

<div id="s2capcontains">
  ## s2CapContains
</div>

Determina se uma calota contém um ponto S2. Uma calota representa uma parte da esfera cortada por um plano. Ela é definida por um ponto sobre a esfera e um raio em graus.

**Sintaxe**

```sql
s2CapContains(center, degrees, point)
```

**Argumentos**

* `center` — Índice de ponto S2 correspondente à calota. [UInt64](../../data-types/int-uint.md).
* `degrees` — Raio da calota, em graus. [Float64](../../data-types/float.md).
* `point` — Índice de ponto S2. [UInt64](../../data-types/int-uint.md).

**Valor retornado**

* `1` — Se a calota contiver o índice de ponto S2. [UInt8](../../data-types/int-uint.md).
* `0` — Se a calota não contiver o índice de ponto S2. [UInt8](../../data-types/int-uint.md).

**Exemplo**

```sql title="Query"
SELECT s2CapContains(1157339245694594829, 1.0, 1157347770437378819) AS capContains;
```

```text title="Response"
┌─capContains─┐
│           1 │
└─────────────┘
```

<div id="s2capunion">
  ## s2CapUnion
</div>

Determina a menor calota que contém as duas calotas de entrada fornecidas. Uma calota representa uma porção da esfera delimitada por um plano. Ela é definida por um ponto sobre a esfera e um raio em graus.

**Sintaxe**

```sql
s2CapUnion(center1, radius1, center2, radius2)
```

**Argumentos**

* `center1`, `center2` — índices de ponto S2 correspondentes às duas calotas de entrada. [UInt64](../../data-types/int-uint.md).
* `radius1`, `radius2` — raio das duas calotas de entrada, em graus. [Float64](../../data-types/float.md).

**Valores retornados**

* `center` — índice de ponto S2 correspondente ao centro da menor calota que contém as duas calotas de entrada. [UInt64](../../data-types/int-uint.md).
* `radius` — raio da menor calota que contém as duas calotas de entrada. [Float64](../../data-types/float.md).

**Exemplo**

```sql title="Query"
SELECT s2CapUnion(3814912406305146967, 1.0, 1157347770437378819, 1.0) AS capUnion;
```

```text title="Response"
┌─capUnion───────────────────────────────┐
│ (4534655147792050737,60.2088283994957) │
└────────────────────────────────────────┘
```

<div id="s2rectadd">
  ## s2RectAdd
</div>

Aumenta o tamanho do retângulo delimitador para incluir o ponto S2 especificado. No sistema S2, um retângulo é representado por um tipo de S2Region chamado `S2LatLngRect`, que representa um retângulo no espaço de latitude e longitude.

**Sintaxe**

```sql
s2RectAdd(s2pointLow, s2pointHigh, s2Point)
```

**Argumentos**

* `s2PointLow` — índice de ponto S2 inferior correspondente ao retângulo. [UInt64](../../data-types/int-uint.md).
* `s2PointHigh` — índice de ponto S2 superior correspondente ao retângulo. [UInt64](../../data-types/int-uint.md).
* `s2Point` — índice de ponto S2 de destino que o retângulo delimitador deve ser expandido para incluir. [UInt64](../../data-types/int-uint.md).

**Valores retornados**

* `s2PointLow` — id de célula S2 inferior correspondente ao retângulo expandido. [UInt64](../../data-types/int-uint.md).
* `s2PointHigh` — id de célula S2 superior correspondente ao retângulo expandido. [UInt64](../../data-types/float.md).

**Exemplo**

```sql title="Query"
SELECT s2RectAdd(5178914411069187297, 5177056748191934217, 5179056748191934217) AS rectAdd;
```

```text title="Response"
┌─rectAdd───────────────────────────────────┐
│ (5179062030687166815,5177056748191934217) │
└───────────────────────────────────────────┘
```

<div id="s2rectcontains">
  ## s2RectContains
</div>

Determina se um determinado retângulo contém um ponto S2. No sistema S2, um retângulo é representado por um tipo de S2Region chamado `S2LatLngRect`, que representa um retângulo no espaço de latitude e longitude.

**Sintaxe**

```sql
s2RectContains(s2PointLow, s2PointHi, s2Point)
```

**Argumentos**

* `s2PointLow` — Menor índice de ponto S2 correspondente ao retângulo. [UInt64](../../data-types/int-uint.md).
* `s2PointHigh` — Maior índice de ponto S2 correspondente ao retângulo. [UInt64](../../data-types/int-uint.md).
* `s2Point` — Índice de ponto S2 de destino. [UInt64](../../data-types/int-uint.md).

**Valor retornado**

* `1` — Se o retângulo contiver o índice de ponto S2 especificado.
* `0` — Se o retângulo não contiver o índice de ponto S2 especificado.

**Exemplo**

```sql title="Query"
SELECT s2RectContains(5179062030687166815, 5177056748191934217, 5177914411069187297) AS rectContains;
```

```text title="Response"
┌─rectContains─┐
│            0 │
└──────────────┘
```

<div id="s2rectunion">
  ## s2RectUnion
</div>

Retorna o menor retângulo que contém a união entre este retângulo e o retângulo fornecido. No sistema S2, um retângulo é representado por um tipo de S2Region chamado `S2LatLngRect`, que representa um retângulo no espaço de latitude e longitude.

**Sintaxe**

```sql
s2RectUnion(s2Rect1PointLow, s2Rect1PointHi, s2Rect2PointLow, s2Rect2PointHi)
```

**Argumentos**

* `s2Rect1PointLow`, `s2Rect1PointHi` — índices de ponto S2 baixo e alto correspondentes ao primeiro retângulo. [UInt64](../../data-types/int-uint.md).
* `s2Rect2PointLow`, `s2Rect2PointHi` — índices de ponto S2 baixo e alto correspondentes ao segundo retângulo. [UInt64](../../data-types/int-uint.md).

**Valores retornados**

* `s2UnionRect2PointLow` — ID de célula S2 baixo correspondente ao retângulo de união. [UInt64](../../data-types/int-uint.md).
* `s2UnionRect2PointHi` — ID de célula S2 alto correspondente ao retângulo de união. [UInt64](../../data-types/int-uint.md).

**Exemplo**

```sql title="Query"
SELECT s2RectUnion(5178914411069187297, 5177056748191934217, 5179062030687166815, 5177056748191934217) AS rectUnion;
```

```text title="Response"
┌─rectUnion─────────────────────────────────┐
│ (5179062030687166815,5177056748191934217) │
└───────────────────────────────────────────┘
```

<div id="s2rectintersection">
  ## s2RectIntersection
</div>

Retorna o menor retângulo que contém a interseção entre este retângulo e o retângulo fornecido. No sistema S2, um retângulo é representado por um tipo de S2Region chamado `S2LatLngRect`, que representa um retângulo no espaço de latitude e longitude.

**Sintaxe**

```sql
s2RectIntersection(s2Rect1PointLow, s2Rect1PointHi, s2Rect2PointLow, s2Rect2PointHi)
```

**Argumentos**

* `s2Rect1PointLow`, `s2Rect1PointHi` — Índices de ponto S2 inferior e superior correspondentes ao primeiro retângulo. [UInt64](../../data-types/int-uint.md).
* `s2Rect2PointLow`, `s2Rect2PointHi` — Índices de ponto S2 inferior e superior correspondentes ao segundo retângulo. [UInt64](../../data-types/int-uint.md).

**Valores retornados**

* `s2UnionRect2PointLow` — ID da célula S2 inferior correspondente ao retângulo que contém a interseção dos retângulos fornecidos. [UInt64](../../data-types/int-uint.md).
* `s2UnionRect2PointHi` — ID da célula S2 superior correspondente ao retângulo que contém a interseção dos retângulos fornecidos. [UInt64](../../data-types/int-uint.md).

**Exemplo**

```sql title="Query"
SELECT s2RectIntersection(5178914411069187297, 5177056748191934217, 5179062030687166815, 5177056748191934217) AS rectIntersection;
```

```text title="Response"
┌─rectIntersection──────────────────────────┐
│ (5178914411069187297,5177056748191934217) │
└───────────────────────────────────────────┘
```