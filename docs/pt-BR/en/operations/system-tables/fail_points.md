---
title: 'system.fail_points'
slug: /operations/system-tables/fail_points
description: 'Contém uma lista de todos os failpoints disponíveis com o tipo e o status atual.'
keywords: ['tabela do sistema', 'fail_points', 'failpoint', 'testes', 'depuração']
doc_type: 'referência'
---

<div id="description">
  ## Descrição
</div>

Contém uma lista de todos os failpoints disponíveis registrados no servidor, juntamente com seu tipo e se estão habilitados no momento.

Os failpoints podem ser habilitados e desabilitados em tempo de execução usando as instruções `SYSTEM ENABLE FAILPOINT` e `SYSTEM DISABLE FAILPOINT`.

<div id="columns">
  ## Colunas
</div>

{/*AUTOGENERATED_START*/ }

* `name` ([String](/pt-BR/sql-reference/data-types/string)) — Nome do failpoint.
* `type` ([Enum8(&#39;once&#39; = 0, &#39;regular&#39; = 1, &#39;pauseable&#95;once&#39; = 2, &#39;pauseable&#39; = 3)](/pt-BR/sql-reference/data-types/enum)) — Tipo do failpoint: &#39;once&#39; é acionado uma única vez e depois se desabilita automaticamente, &#39;regular&#39; é acionado sempre, &#39;pauseable&#95;once&#39; bloqueia a execução uma vez, &#39;pauseable&#39; bloqueia a execução todas as vezes até ser retomado.
* `enabled` ([UInt8](/pt-BR/sql-reference/data-types/int-uint)) — Indica se o failpoint está habilitado no momento (1) ou desabilitado (0).

{/*AUTOGENERATED_END*/ }

<div id="example">
  ## Exemplo
</div>

```sql title="Query"
SYSTEM ENABLE FAILPOINT replicated_merge_tree_insert_retry_pause;
SELECT * FROM system.fail_points WHERE enabled = 1
```

```text title="Response"
┌─name──────────────────────────────────────┬─type────────────┬─enabled─┐
│ replicated_merge_tree_insert_retry_pause  │ pauseable_once  │       1 │
└───────────────────────────────────────────┴─────────────────┴─────────┘
```