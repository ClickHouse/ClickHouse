---
description: 'Tabela de sistema que contém informações sobre arquivos de metadados lidos de tabelas do Delta Lake. Cada entrada
  representa um arquivo JSON raiz de metadados.'
keywords: ['tabela de sistema', 'delta_lake_metadata_log']
slug: /operations/system-tables/delta_lake_metadata_log
title: 'system.delta_lake_metadata_log'
doc_type: 'reference'
---

import SystemTableCloud from '@site/docs/_snippets/_system_table_cloud.md';

<SystemTableCloud />

<div id="purpose">
  ## Descrição
</div>

A tabela `system.delta_lake_metadata_log` registra eventos de acesso e análise de metadados de tabelas Delta Lake lidas pelo ClickHouse. Ela fornece informações detalhadas sobre cada arquivo de metadados, o que é útil para depuração, auditoria e para entender a evolução da estrutura da tabela Delta.

Esta tabela registra cada arquivo de metadados lido de tabelas Delta Lake. Ela ajuda os usuários a rastrear como o ClickHouse interpreta os metadados da tabela Delta e a diagnosticar problemas relacionados à evolução do esquema, à resolução de snapshots ou ao planejamento de consultas.

:::note
Esta tabela se destina principalmente à depuração.
:::

<div id="controlling-log-verbosity">
  ### Controle da verbosidade do log
</div>

Você pode controlar quais eventos de metadados são registrados usando a configuração [`delta_lake_log_metadata`](../../operations/settings/settings.md#delta_lake_log_metadata).

Para registrar todos os metadados usados na consulta atual:

```sql
SELECT * FROM my_delta_table SETTINGS delta_lake_log_metadata = 1;

SYSTEM FLUSH LOGS delta_lake_metadata_log;

SELECT *
FROM system.delta_lake_metadata_log
WHERE query_id = '{previous_query_id}';
```

<div id="columns">
  ## Colunas
</div>

{/*AUTOGENERATED_START*/ }

* `hostname` ([LowCardinality(String)](/pt-BR/sql-reference/data-types/lowcardinality)) — Hostname do servidor que executa a consulta.
* `event_date` ([Date](/pt-BR/sql-reference/data-types/date)) — Data do registro.
* `event_time` ([DateTime](/pt-BR/sql-reference/data-types/datetime)) — Horário do evento.
* `query_id` ([String](/pt-BR/sql-reference/data-types/string)) — ID da consulta.
* `table_path` ([String](/pt-BR/sql-reference/data-types/string)) — Caminho da tabela.
* `file_path` ([String](/pt-BR/sql-reference/data-types/string)) — Caminho do arquivo.
* `content` ([String](/pt-BR/sql-reference/data-types/string)) — Conteúdo em formato JSON.

{/*AUTOGENERATED_END*/ }