---
description: 'Tabela de sistema que contém informações de perfilamento no nível dos processadores
  (disponíveis em `EXPLAIN PIPELINE`)'
keywords: ['tabela de sistema', 'processors_profile_log', 'EXPLAIN PIPELINE']
slug: /operations/system-tables/processors_profile_log
title: 'system.processors_profile_log'
doc_type: 'reference'
---

import SystemTableCloud from '@site/docs/_snippets/_system_table_cloud.md';

<SystemTableCloud />

<div id="description">
  ## Descrição
</div>

Esta tabela contém dados de perfilamento no nível dos processadores (que podem ser encontrados em [`EXPLAIN PIPELINE`](../../sql-reference/statements/explain.md#explain-pipeline)).

<div id="columns">
  ## Colunas
</div>

{/*AUTOGENERATED_START*/ }

* `hostname` ([LowCardinality(String)](/pt-BR/sql-reference/data-types/lowcardinality)) — Hostname do servidor que executa a consulta.
* `event_date` ([Date](/pt-BR/sql-reference/data-types/date)) — A data em que o evento ocorreu.
* `event_time` ([DateTime](/pt-BR/sql-reference/data-types/datetime)) — A data e a hora em que o evento ocorreu.
* `event_time_microseconds` ([DateTime64(6)](/pt-BR/sql-reference/data-types/datetime64)) — A data e a hora, com precisão de microssegundos, em que o evento ocorreu.
* `id` ([UInt64](/pt-BR/sql-reference/data-types/int-uint)) — ID do processador.
* `parent_ids` ([Array(UInt64)](/pt-BR/sql-reference/data-types/array)) — IDs dos processadores pai.
* `plan_step` ([UInt64](/pt-BR/sql-reference/data-types/int-uint)) — ID da etapa do plano de consulta que criou este processador. O valor é zero se o processador não foi adicionado por nenhuma etapa.
* `plan_step_name` ([String](/pt-BR/sql-reference/data-types/string)) — Nome da etapa do plano de consulta que criou este processador. O valor fica vazio se o processador não foi adicionado por nenhuma etapa.
* `plan_step_description` ([String](/pt-BR/sql-reference/data-types/string)) — Descrição da etapa do plano de consulta que criou este processador. O valor fica vazio se o processador não foi adicionado por nenhuma etapa.
* `plan_group` ([UInt64](/pt-BR/sql-reference/data-types/int-uint)) — Grupo do processador, se ele foi criado por uma etapa do plano de consulta. Um grupo é um particionamento lógico de processadores adicionados pela mesma etapa do plano de consulta. O grupo é usado apenas para melhorar a visualização do resultado de EXPLAIN PIPELINE.
* `initial_query_id` ([String](/pt-BR/sql-reference/data-types/string)) — ID da consulta inicial (para execução distribuída de consultas).
* `query_id` ([String](/pt-BR/sql-reference/data-types/string)) — ID da consulta.
* `name` ([LowCardinality(String)](/pt-BR/sql-reference/data-types/lowcardinality)) — Nome do processador.
* `elapsed_us` ([UInt64](/pt-BR/sql-reference/data-types/int-uint)) — Número de microssegundos durante os quais este processador ficou em execução.
* `input_wait_elapsed_us` ([UInt64](/pt-BR/sql-reference/data-types/int-uint)) — Número de microssegundos durante os quais este processador ficou aguardando dados (de outro processador).
* `output_wait_elapsed_us` ([UInt64](/pt-BR/sql-reference/data-types/int-uint)) — Número de microssegundos durante os quais este processador ficou aguardando porque a porta de saída estava cheia.
* `input_rows` ([UInt64](/pt-BR/sql-reference/data-types/int-uint)) — Número de linhas consumidas pelo processador.
* `input_bytes` ([UInt64](/pt-BR/sql-reference/data-types/int-uint)) — Número de bytes consumidos pelo processador.
* `output_rows` ([UInt64](/pt-BR/sql-reference/data-types/int-uint)) — Número de linhas geradas pelo processador.
* `output_bytes` ([UInt64](/pt-BR/sql-reference/data-types/int-uint)) — Número de bytes gerados pelo processador.
* `processor_uniq_id` ([String](/pt-BR/sql-reference/data-types/string)) — ID único do processador no pipeline.
* `step_uniq_id` ([String](/pt-BR/sql-reference/data-types/string)) — ID único da etapa no plano.

{/*AUTOGENERATED_END*/ }

<div id="example">
  ## Exemplo
</div>

```sql title="Query"
EXPLAIN PIPELINE
SELECT sleep(1)
┌─explain─────────────────────────┐
│ (Expression)                    │
│ ExpressionTransform             │
│   (SettingQuotaAndLimits)       │
│     (ReadFromStorage)           │
│     SourceFromSingleChunk 0 → 1 │
└─────────────────────────────────┘

SELECT sleep(1)
SETTINGS log_processors_profiles = 1
Query id: feb5ed16-1c24-4227-aa54-78c02b3b27d4
┌─sleep(1)─┐
│        0 │
└──────────┘
1 rows in set. Elapsed: 1.018 sec.

SELECT
    name,
    elapsed_us,
    input_wait_elapsed_us,
    output_wait_elapsed_us
FROM system.processors_profile_log
WHERE query_id = 'feb5ed16-1c24-4227-aa54-78c02b3b27d4'
ORDER BY name ASC
```

```text title="Response"
┌─name────────────────────┬─elapsed_us─┬─input_wait_elapsed_us─┬─output_wait_elapsed_us─┐
│ ExpressionTransform     │    1000497 │                  2823 │                    197 │
│ LazyOutputFormat        │         36 │               1002188 │                      0 │
│ LimitsCheckingTransform │         10 │               1002994 │                    106 │
│ NullSource              │          5 │               1002074 │                      0 │
│ NullSource              │          1 │               1002084 │                      0 │
│ SourceFromSingleChunk   │         45 │                  4736 │                1000819 │
└─────────────────────────┴────────────┴───────────────────────┴────────────────────────┘
```

Aqui você pode ver:

* `ExpressionTransform` estava executando a função `sleep(1)`, então seu `work` levará 1e6 e, portanto, `elapsed_us` &gt; 1e6.
* `SourceFromSingleChunk` precisa esperar, porque `ExpressionTransform` não aceita nenhum dado durante a execução de `sleep(1)`, então ele ficará no estado `PortFull` por 1e6 us e, portanto, `output_wait_elapsed_us` &gt; 1e6.
* `LimitsCheckingTransform`/`NullSource`/`LazyOutputFormat` precisam esperar até que `ExpressionTransform` execute `sleep(1)` para processar o resultado, então `input_wait_elapsed_us` &gt; 1e6.

<div id="see-also">
  ## Veja também
</div>

* [`EXPLAIN PIPELINE`](../../sql-reference/statements/explain.md#explain-pipeline)