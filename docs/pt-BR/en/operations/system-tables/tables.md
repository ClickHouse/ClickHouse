---
description: 'Tabela do sistema que contém os metadados de cada tabela conhecida pelo servidor.'
keywords: ['tabela do sistema', 'tabelas']
slug: /operations/system-tables/tables
title: 'system.tables'
doc_type: 'reference'
---

<div id="description">
  ## Descrição
</div>

Contém os metadados de cada tabela que o servidor conhece.

As tabelas [desanexadas](../../sql-reference/statements/detach.md) não são mostradas em `system.tables`.

As [tabelas temporárias](../../sql-reference/statements/create/table.md#temporary-tables) ficam visíveis em `system.tables` apenas na sessão em que foram criadas. Elas são exibidas com o campo `database` vazio e com a flag `is_temporary` ativada.

<div id="columns">
  ## Colunas
</div>

{/*AUTOGENERATED_START*/ }

* `database` ([String](/pt-BR/sql-reference/data-types/string)) — O nome do banco de dados em que a tabela está.
* `name` ([String](/pt-BR/sql-reference/data-types/string)) — Nome da tabela.
* `uuid` ([UUID](/pt-BR/sql-reference/data-types/uuid)) — UUID da tabela (banco de dados Atomic).
* `engine` ([String](/pt-BR/sql-reference/data-types/string)) — Nome do motor da tabela (sem parâmetros).
* `is_temporary` ([UInt8](/pt-BR/sql-reference/data-types/int-uint)) — Indicador que informa se a tabela é temporária.
* `data_paths` ([Array(String)](/pt-BR/sql-reference/data-types/array)) — Caminhos para os dados da tabela nos sistemas de arquivos.
* `metadata_path` ([String](/pt-BR/sql-reference/data-types/string)) — Caminho para os metadados da tabela no sistema de arquivos.
* `metadata_modification_time` ([DateTime](/pt-BR/sql-reference/data-types/datetime)) — Hora da modificação mais recente dos metadados da tabela.
* `metadata_version` ([Int32](/pt-BR/sql-reference/data-types/int-uint)) — Versão dos metadados para tabela ReplicatedMergeTree, 0 para tabela que não é ReplicatedMergeTree.
* `dependencies_database` ([Array(String)](/pt-BR/sql-reference/data-types/array)) — Dependências do banco de dados.
* `dependencies_table` ([Array(String)](/pt-BR/sql-reference/data-types/array)) — Dependências da tabela (visões materializadas da tabela atual).
* `create_table_query` ([String](/pt-BR/sql-reference/data-types/string)) — A consulta usada para criar a tabela.
* `engine_full` ([String](/pt-BR/sql-reference/data-types/string)) — Parâmetros do motor da tabela.
* `as_select` ([String](/pt-BR/sql-reference/data-types/string)) — Consulta SELECT da visão.
* `parameterized_view_parameters` ([Array(Tuple(name String, type String))](/pt-BR/sql-reference/data-types/array)) — Parâmetros da visão parametrizada.
* `partition_key` ([String](/pt-BR/sql-reference/data-types/string)) — A expressão da chave de partição especificada na tabela.
* `sorting_key` ([String](/pt-BR/sql-reference/data-types/string)) — A expressão da chave de ordenação especificada na tabela.
* `primary_key` ([String](/pt-BR/sql-reference/data-types/string)) — A expressão da chave primária especificada na tabela.
* `sampling_key` ([String](/pt-BR/sql-reference/data-types/string)) — A expressão da chave de amostragem especificada na tabela.
* `storage_policy` ([String](/pt-BR/sql-reference/data-types/string)) — A política de armazenamento. Relevante para tabelas que usam os motores MergeTree e Distributed.
* `total_rows` ([Nullable(UInt64)](/pt-BR/sql-reference/data-types/nullable)) — Número total de linhas, se for possível determinar rapidamente o número exato de linhas na tabela; caso contrário, NULL (incluindo a Buffer table subjacente).
* `total_bytes` ([Nullable(UInt64)](/pt-BR/sql-reference/data-types/nullable)) — Número total de bytes, se for possível determinar rapidamente o número exato de bytes da tabela no armazenamento; caso contrário, NULL (não inclui nenhum armazenamento subjacente). Se a tabela armazena dados em disco, retorna o espaço usado em disco (isto é, comprimido). Se a tabela armazena dados em memória, retorna o número aproximado de bytes usados na memória.
* `total_bytes_uncompressed` ([Nullable(UInt64)](/pt-BR/sql-reference/data-types/nullable)) — Número total de bytes não comprimidos, se for possível determinar rapidamente o número exato de bytes com base nos checksum das partes da tabela no armazenamento; caso contrário, NULL (não leva em conta o armazenamento subjacente, se houver).
* `parts` ([Nullable(UInt64)](/pt-BR/sql-reference/data-types/nullable)) — O número total de partes nesta tabela.
* `active_parts` ([Nullable(UInt64)](/pt-BR/sql-reference/data-types/nullable)) — O número de partes ativas nesta tabela.
* `total_marks` ([Nullable(UInt64)](/pt-BR/sql-reference/data-types/nullable)) — O número total de marcas em todas as partes desta tabela.
* `active_on_fly_data_mutations` ([UInt64](/pt-BR/sql-reference/data-types/int-uint)) — Número total de mutações de dados ativas (UPDATEs e DELETEs) que podem ser aplicadas em tempo real.
* `active_on_fly_alter_mutations` ([UInt64](/pt-BR/sql-reference/data-types/int-uint)) — Número total de mutações ALTER ativas (MODIFY COLUMN) que podem ser aplicadas em tempo real.
* `active_on_fly_metadata_mutations` ([UInt64](/pt-BR/sql-reference/data-types/int-uint)) — Número total de mutações de metadados ativas (RENAMEs) que podem ser aplicadas em tempo real.
* `columns_descriptions_cache_size` ([UInt64](/pt-BR/sql-reference/data-types/int-uint)) — Tamanho do cache de descrições de colunas para tabelas *MergeTree
* `lifetime_rows` ([Nullable(UInt64)](/pt-BR/sql-reference/data-types/nullable)) — Número total de linhas com INSERT desde a inicialização do servidor (somente para tabelas Buffer).
* `lifetime_bytes` ([Nullable(UInt64)](/pt-BR/sql-reference/data-types/nullable)) — Número total de bytes com INSERT desde a inicialização do servidor (somente para tabelas Buffer).
* `comment` ([String](/pt-BR/sql-reference/data-types/string)) — O comentário da tabela.
* `has_own_data` ([UInt8](/pt-BR/sql-reference/data-types/int-uint)) — Flag que indica se a própria tabela armazena dados em disco ou apenas acessa outra origem.
* `loading_dependencies_database` ([Array(String)](/pt-BR/sql-reference/data-types/array)) — Dependências de carregamento do banco de dados (lista de objetos que devem ser carregados antes do objeto atual).
* `loading_dependencies_table` ([Array(String)](/pt-BR/sql-reference/data-types/array)) — Dependências de carregamento da tabela (lista de objetos que devem ser carregados antes do objeto atual).
* `loading_dependent_database` ([Array(String)](/pt-BR/sql-reference/data-types/array)) — Banco de dados dependente de carregamento.
* `loading_dependent_table` ([Array(String)](/pt-BR/sql-reference/data-types/array)) — Tabela dependente de carregamento.
* `target_database` ([String](/pt-BR/sql-reference/data-types/string)) — Para uma visão materializada, o banco de dados da tabela de destino na qual a visão grava (o destino `TO` ou a tabela implícita `.inner.*`). Vazio para outros motores.
* `target_table` ([String](/pt-BR/sql-reference/data-types/string)) — Para uma visão materializada, o nome da tabela de destino na qual a visão grava (o destino `TO` ou a tabela implícita `.inner.*`). Vazio para outros motores.
* `definer` ([String](/pt-BR/sql-reference/data-types/string)) — Nome do definidor de SQL security usado para a tabela.

**Aliases:**

* `table` — Alias para `name`.

{/*AUTOGENERATED_END*/ }

<div id="example">
  ## Exemplo
</div>

```sql
SELECT * FROM system.tables LIMIT 2 FORMAT Vertical;
```

```text
Row 1:
──────
database:                   base
name:                       t1
uuid:                       81b1c20a-b7c6-4116-a2ce-7583fb6b6736
engine:                     MergeTree
is_temporary:               0
data_paths:                 ['/var/lib/clickhouse/store/81b/81b1c20a-b7c6-4116-a2ce-7583fb6b6736/']
metadata_path:              /var/lib/clickhouse/store/461/461cf698-fd0b-406d-8c01-5d8fd5748a91/t1.sql
metadata_modification_time: 2021-01-25 19:14:32
dependencies_database:      []
dependencies_table:         []
create_table_query:         CREATE TABLE base.t1 (`n` UInt64) ENGINE = MergeTree ORDER BY n
engine_full:                MergeTree ORDER BY n
as_select:                  SELECT database AS table_catalog
partition_key:
sorting_key:                n
primary_key:                n
sampling_key:
storage_policy:             default
total_rows:                 1
total_bytes:                99
lifetime_rows:              ᴺᵁᴸᴸ
lifetime_bytes:             ᴺᵁᴸᴸ
comment:
has_own_data:               0
loading_dependencies_database: []
loading_dependencies_table:    []
loading_dependent_database:    []
loading_dependent_table:       []

Row 2:
──────
database:                   default
name:                       53r93yleapyears
uuid:                       00000000-0000-0000-0000-000000000000
engine:                     MergeTree
is_temporary:               0
data_paths:                 ['/var/lib/clickhouse/data/default/53r93yleapyears/']
metadata_path:              /var/lib/clickhouse/metadata/default/53r93yleapyears.sql
metadata_modification_time: 2020-09-23 09:05:36
dependencies_database:      []
dependencies_table:         []
create_table_query:         CREATE TABLE default.`53r93yleapyears` (`id` Int8, `febdays` Int8) ENGINE = MergeTree ORDER BY id
engine_full:                MergeTree ORDER BY id
as_select:                  SELECT name AS catalog_name
partition_key:
sorting_key:                id
primary_key:                id
sampling_key:
storage_policy:             default
total_rows:                 2
total_bytes:                155
lifetime_rows:              ᴺᵁᴸᴸ
lifetime_bytes:             ᴺᵁᴸᴸ
comment:
has_own_data:               0
loading_dependencies_database: []
loading_dependencies_table:    []
loading_dependent_database:    []
loading_dependent_table:       []
```