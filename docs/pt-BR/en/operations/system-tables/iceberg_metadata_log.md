---
description: 'Tabela do sistema que contém informações sobre arquivos de metadados lidos de tabelas Iceberg. Cada entrada
  representa um arquivo de metadados raiz, metadados extraídos de um arquivo Avro ou uma entrada em algum arquivo Avro.'
keywords: ['tabela do sistema', 'iceberg_metadata_log']
slug: /operations/system-tables/iceberg_metadata_log
title: 'system.iceberg_metadata_log'
doc_type: 'referência'
---

import SystemTableCloud from '@site/docs/_snippets/_system_table_cloud.md';

<SystemTableCloud />

<div id="description">
  ## Descrição
</div>

A tabela `system.iceberg_metadata_log` registra eventos de acesso e processamento de metadados de tabelas Iceberg lidas pelo ClickHouse. Ela fornece informações detalhadas sobre cada arquivo de metadados ou entrada processada, o que é útil para depuração, auditoria e para entender a evolução da estrutura das tabelas Iceberg.

Esta tabela registra cada arquivo de metadados e cada entrada lida de tabelas Iceberg, incluindo arquivos de metadados raiz, listas de manifestos e entradas de manifesto. Ela ajuda os usuários a rastrear como o ClickHouse interpreta os metadados de tabelas Iceberg e a diagnosticar problemas relacionados à evolução do esquema, à resolução de arquivos ou ao planejamento de consultas.

:::note
Esta tabela se destina principalmente à depuração.
:::

<div id="controlling-log-verbosity">
  ### Controlando a verbosidade do log
</div>

Você pode controlar quais eventos de metadados são registrados por meio da configuração [`iceberg_metadata_log_level`](../../operations/settings/settings.md#iceberg_metadata_log_level).

Para registrar todos os metadados usados na consulta atual:

```sql
SELECT * FROM my_iceberg_table SETTINGS iceberg_metadata_log_level = 'manifest_file_entry';

SYSTEM FLUSH LOGS iceberg_metadata_log;

SELECT content_type, file_path, row_in_file
FROM system.iceberg_metadata_log
WHERE query_id = '{previous_query_id}';
```

Para registrar em log apenas o arquivo JSON raiz de metadados usado na consulta atual:

```sql
SELECT * FROM my_iceberg_table SETTINGS iceberg_metadata_log_level = 'metadata';

SYSTEM FLUSH LOGS iceberg_metadata_log;

SELECT content_type, file_path, row_in_file
FROM system.iceberg_metadata_log
WHERE query_id = '{previous_query_id}';
```

Consulte mais informações na descrição da configuração [`iceberg_metadata_log_level`](../../operations/settings/settings.md#iceberg_metadata_log_level).

<div id="good-to-know">
  ### Bom saber
</div>

* Use `iceberg_metadata_log_level` no nível da consulta apenas quando precisar investigar sua tabela Iceberg em detalhes. Caso contrário, você poderá sobrecarregar a tabela de log com metadados em excesso e sofrer degradação de desempenho.
* A tabela contém entradas duplicadas, pois se destina principalmente à depuração e não garante unicidade por entidade. Linhas separadas armazenam o conteúdo e o status de poda porque eles são coletados em momentos diferentes do programa. O conteúdo é coletado quando os metadados são lidos; o status de poda é coletado quando os metadados são verificados para poda. **Nunca confie na própria tabela para desduplicação.**
* Se você usar um `content_type` mais verboso que `ManifestListMetadata`, o cache de metadados do Iceberg será desativado para as listas de manifestos.
* Da mesma forma, se você usar um `content_type` mais verboso que `ManifestFileMetadata`, o cache de metadados do Iceberg será desativado para os arquivos de manifesto.
* Se a consulta SELECT tiver sido cancelada ou falhar, a tabela de log ainda poderá conter entradas dos metadados processados antes da falha, mas não conterá informações sobre entidades de metadados que não foram processadas.

<div id="columns">
  ## Colunas
</div>

{/*AUTOGENERATED_START*/ }

* `hostname` ([LowCardinality(String)](/pt-BR/sql-reference/data-types/lowcardinality)) — Hostname do servidor que executa a consulta.
* `event_date` ([Date](/pt-BR/sql-reference/data-types/date)) — Data da entrada.
* `event_time` ([DateTime](/pt-BR/sql-reference/data-types/datetime)) — Horário do evento.
* `query_id` ([String](/pt-BR/sql-reference/data-types/string)) — ID da consulta.
* `content_type` ([Enum8(&#39;None&#39; = 0, &#39;Metadata&#39; = 1, &#39;ManifestListMetadata&#39; = 2, &#39;ManifestListEntry&#39; = 3, &#39;ManifestFileMetadata&#39; = 4, &#39;ManifestFileEntry&#39; = 5)](/pt-BR/sql-reference/data-types/enum)) — Tipo de conteúdo.
* `table_path` ([String](/pt-BR/sql-reference/data-types/string)) — Caminho da tabela.
* `file_path` ([String](/pt-BR/sql-reference/data-types/string)) — Caminho do arquivo.
* `content` ([String](/pt-BR/sql-reference/data-types/string)) — Conteúdo em formato JSON (conteúdo do arquivo JSON, metadados Avro ou entrada Avro).
* `row_in_file` ([Nullable(UInt64)](/pt-BR/sql-reference/data-types/nullable)) — Linha no arquivo.
* `pruning_status` ([Nullable(Enum8(&#39;NotPruned&#39; = 0, &#39;PartitionPruned&#39; = 1, &#39;MinMaxIndexPruned&#39; = 2))](/pt-BR/sql-reference/data-types/nullable)) — Status da poda de partições ou da poda pelo índice min-max do arquivo.

{/*AUTOGENERATED_END*/ }

<div id="content-type-values">
  ### valores de `content_type`
</div>

* `None`: Sem conteúdo.
* `Metadata`: Arquivo de metadados raiz.
* `ManifestListMetadata`: Metadados da lista de manifestos.
* `ManifestListEntry`: Entrada em uma lista de manifestos.
* `ManifestFileMetadata`: Metadados do arquivo de manifesto.
* `ManifestFileEntry`: Entrada em um arquivo de manifesto.

<div id="see-also">
  ## Veja também
</div>

* [Iceberg Table Engine](../../engines/table-engines/integrations/iceberg.md)
* [Iceberg Table Function](../../sql-reference/table-functions/iceberg.md)
* [system.iceberg&#95;history](./iceberg_history.md)