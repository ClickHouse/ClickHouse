---
description: 'Calcula o valor de `arg` correspondente ao valor máximo de `val`.'
slug: /sql-reference/aggregate-functions/reference/argmax
title: 'argMax'
doc_type: 'reference'
---

Alias: `max_by`

{/*AUTOGENERATED_START*/}

<div id="argMax">
  ## argMax
</div>

Introduzido em: v1.1.0

Calcula o valor de `arg` correspondente ao valor máximo de `val`. Se houver várias linhas com `val` iguais ao máximo, não é determinístico qual dos `arg` associados será retornado.
As duas partes, `arg` e `max`, se comportam como [funções de agregação](/pt-BR/reference/functions/aggregate-functions/index); ambas [ignoram `Null`](/pt-BR/reference/functions/aggregate-functions/index#null-processing) durante o processamento e retornam valores diferentes de `Null` se houver valores diferentes de `Null` disponíveis.

**Veja também**

* [Tuple](/pt-BR/reference/data-types/tuple)

**Sintaxe**

```sql
argMax(arg, val)
```

**Aliases**: `max_by`

**Argumentos**

* `arg` — Argumento para o qual será encontrado o valor máximo. [`const String`](/pt-BR/reference/data-types/string)
* `val` — O valor máximo. [`(U)Int8/16/32/64`](/pt-BR/reference/data-types/int-uint) ou [`Float*`](/pt-BR/reference/data-types/float) ou [`Date`](/pt-BR/reference/data-types/date) ou [`DateTime`](/pt-BR/reference/data-types/datetime) ou [`Tuple`](/pt-BR/reference/data-types/tuple)

**Valor retornado**

Retorna o valor de `arg` correspondente ao valor máximo de `val`. O tipo corresponde ao tipo de `arg`.

**Exemplos**

**Uso básico**

```sql title=Query
SELECT argMax(user, salary) FROM salary;
```

```response title=Response
┌─argMax(user, salary)─┐
│ director             │
└──────────────────────┘
```

**Exemplo mais completo com tratamento de NULL**

```sql title=Query
CREATE TABLE test
(
    a Nullable(String),
    b Nullable(Int64)
)
ENGINE = Memory AS
SELECT *
FROM VALUES(('a', 1), ('b', 2), ('c', 2), (NULL, 3), (NULL, NULL), ('d', NULL));

SELECT argMax(a, b), max(b) FROM test;
```

```response title=Response
┌─argMax(a, b)─┬─max(b)─┐
│ b            │      3 │
└──────────────┴────────┘
```

**Usando Tuple nos argumentos**

```sql title=Query
SELECT argMax(a, (b,a)) FROM test;
```

```response title=Response
┌─argMax(a, tuple(b, a))─┐
│ c                      │
└────────────────────────┘
```

{/*AUTOGENERATED_END*/}