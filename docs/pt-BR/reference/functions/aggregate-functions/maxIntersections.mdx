---
description: 'Função de agregação que calcula o número máximo de interseções entre
  um grupo de intervalos (se todos os intervalos se intersectarem pelo menos
  uma vez).'
slug: /sql-reference/aggregate-functions/reference/maxintersections
title: 'maxIntersections'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="maxIntersections">
  ## maxIntersections
</div>

Introduzido em: v20.1.0

Função de agregação que calcula o número máximo de vezes que um grupo de intervalos se sobrepõe (se todos os intervalos se sobrepuserem pelo menos uma vez).

**Sintaxe**

```sql
maxIntersections(start_column, end_column)
```

**Argumentos**

* `start_column` — Uma coluna numérica que representa o início de cada intervalo. Se `start_column` for `NULL` ou 0, o intervalo será ignorado. [`(U)Int*`](/pt-BR/reference/data-types/int-uint) ou [`Float*`](/pt-BR/reference/data-types/float)
* `end_column` — Uma coluna numérica que representa o fim de cada intervalo. Se `end_column` for `NULL` ou 0, o intervalo será ignorado. [`(U)Int*`](/pt-BR/reference/data-types/int-uint) ou [`Float*`](/pt-BR/reference/data-types/float)

**Valor retornado**

Retorna o número máximo de intervalos que se intersectam. [`UInt64`](/pt-BR/reference/data-types/int-uint)

**Exemplos**

**Calculando o número máximo de interseções**

```sql title=Query
CREATE TABLE my_events (
    start UInt32,
    end UInt32
)
ENGINE = MergeTree
ORDER BY tuple();

INSERT INTO my_events VALUES
(1, 3),
(1, 6),
(2, 5),
(3, 7);

SELECT maxIntersections(start, end) FROM my_events;
```

```response title=Response
┌─maxIntersections(start, end)─┐
│                            3 │
└──────────────────────────────┘
```

{/*AUTOGENERATED_END*/}