---
description: 'Soma um ou mais arrays de `value` de acordo com as chaves especificadas no array
  `key`. Retorna uma tupla de arrays: chaves em ordem crescente, seguidas dos valores
  somados para as chaves correspondentes, sem overflow.'
slug: /sql-reference/aggregate-functions/reference/summap
title: 'sumMap'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="sumMappedArrays">
  ## sumMappedArrays
</div>

Introduzido em: v1.1.0

Totaliza um ou mais arrays `value` de acordo com as chaves especificadas no array `key`. Retorna uma tupla de arrays: chaves em ordem crescente, seguidas dos valores somados para as chaves correspondentes, sem overflow.

<Note>
  * Passar uma tupla de chaves e arrays `value` é idêntico a passar um array de chaves e um array de valores.
  * O número de elementos em `key` e em todos os arrays `value` deve ser o mesmo para cada linha totalizada.
</Note>

**Sintaxe**

```sql
sumMappedArrays(key, value1 [, value2, ...])
sumMappedArrays(Tuple(key, value1 [, value2, ...]))
```

**Argumentos**

* `key` — Array de chaves. [`Array`](/pt-BR/reference/data-types/array)
* `value1, value2, ...` — Arrays de valores a serem somados para cada chave. [`Array`](/pt-BR/reference/data-types/array)

**Valor retornado**

Retorna uma tupla de arrays: o primeiro array contém as chaves em ordem crescente, seguido de arrays com os valores somados para as chaves correspondentes. [`Tuple`](/pt-BR/reference/data-types/tuple)

**Exemplos**

**Uso básico com o tipo Nested**

```sql title=Query
CREATE TABLE sum_map(
    date Date,
    timeslot DateTime,
    statusMap Nested(
        status UInt16,
        requests UInt64
    ),
    statusMapTuple Tuple(Array(Int32), Array(Int32))
) ENGINE = Memory;

INSERT INTO sum_map VALUES
    ('2000-01-01', '2000-01-01 00:00:00', [1, 2, 3], [10, 10, 10], ([1, 2, 3], [10, 10, 10])),
    ('2000-01-01', '2000-01-01 00:00:00', [3, 4, 5], [10, 10, 10], ([3, 4, 5], [10, 10, 10])),
    ('2000-01-01', '2000-01-01 00:01:00', [4, 5, 6], [10, 10, 10], ([4, 5, 6], [10, 10, 10])),
    ('2000-01-01', '2000-01-01 00:01:00', [6, 7, 8], [10, 10, 10], ([6, 7, 8], [10, 10, 10]));

SELECT
    timeslot,
    sumMappedArrays(statusMap.status, statusMap.requests),
    sumMappedArrays(statusMapTuple)
FROM sum_map
GROUP BY timeslot;
```

```response title=Response
┌────────────timeslot─┬─sumMappedArrays(statusMap.status, statusMap.requests)─┬─sumMappedArrays(statusMapTuple)─────────┐
│ 2000-01-01 00:00:00 │ ([1,2,3,4,5],[10,10,20,10,10])                        │ ([1,2,3,4,5],[10,10,20,10,10])          │
│ 2000-01-01 00:01:00 │ ([4,5,6,7,8],[10,10,20,10,10])                        │ ([4,5,6,7,8],[10,10,20,10,10])          │
└─────────────────────┴───────────────────────────────────────────────────────┴─────────────────────────────────────────┘
```

**Exemplo de arrays com múltiplos valores**

```sql title=Query
CREATE TABLE multi_metrics(
    date Date,
    browser_metrics Nested(
        browser String,
        impressions UInt32,
        clicks UInt32
    )
)
ENGINE = Memory;

INSERT INTO multi_metrics VALUES
    ('2000-01-01', ['Firefox', 'Chrome'], [100, 200], [10, 25]),
    ('2000-01-01', ['Chrome', 'Safari'], [150, 50], [20, 5]),
    ('2000-01-01', ['Firefox', 'Edge'], [80, 40], [8, 4]);

SELECT
    sumMappedArrays(browser_metrics.browser, browser_metrics.impressions, browser_metrics.clicks) AS result
FROM multi_metrics;
```

```response title=Response
┌─result────────────────────────────────────────────────────────────────────────┐
│ (['Chrome', 'Edge', 'Firefox', 'Safari'], [350, 40, 180, 50], [45, 4, 18, 5]) │
└───────────────────────────────────────────────────────────────────────────────┘
-- Neste exemplo:
-- A tupla resultante contém três Arrays
-- Primeiro Array: chaves (nomes de navegadores) em ordem crescente
-- Segundo Array: total de impressões por navegador
-- Terceiro Array: total de cliques por navegador
```

{/*AUTOGENERATED_END*/}

**Veja também**

* [Combinador Map para o tipo de dado Map](/pt-BR/reference/functions/aggregate-functions/combinators#-map)
* [sumMapWithOverflow](/pt-BR/reference/functions/aggregate-functions/sumMapWithOverflow)