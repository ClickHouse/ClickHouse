---
description: 'Função de agregação que calcula alterações no estilo do PromQL em dados de séries temporais na grade especificada.'
slug: /sql-reference/aggregate-functions/reference/timeSeriesChangesToGrid
title: 'timeSeriesChangesToGrid'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="timeSeriesChangesToGrid">
  ## timeSeriesChangesToGrid
</div>

Introduzido em: v25.6.0

Função de agregação que recebe dados de séries temporais como pares de timestamps e valores e calcula [alterações no estilo do PromQL](https://prometheus.io/docs/prometheus/latest/querying/functions/#changes) com base nesses dados em uma grade temporal regular definida por timestamp inicial, timestamp final e passo. Para cada ponto da grade, as amostras usadas no cálculo de `changes` são consideradas dentro da janela de tempo especificada.

Se várias amostras tiverem o mesmo timestamp, apenas uma delas será usada: a amostra com o maior valor. Um valor NaN perde para qualquer outro valor, portanto, um valor NaN será usado apenas se todas as amostras nesse timestamp forem NaN.

<Note>
  Esta função é experimental; habilite-a definindo `allow_experimental_time_series_aggregate_functions=true`.
</Note>

**Sintaxe**

```sql
timeSeriesChangesToGrid(start_timestamp, end_timestamp, grid_step, staleness)(timestamp, value)
```

**Parâmetros**

* `start_timestamp` — Especifica o início da grade. Quando o argumento de timestamp é do tipo `DateTime64`, também pode ser um número fracionário ou uma string contendo um número ou texto de data e hora. [`UInt32`](/pt-BR/reference/data-types/int-uint) ou [`DateTime`](/pt-BR/reference/data-types/datetime) ou [`DateTime64`](/pt-BR/reference/data-types/datetime64) ou [`Float*`](/pt-BR/reference/data-types/float) ou [`Decimal*`](/pt-BR/reference/data-types/decimal) ou [`String`](/pt-BR/reference/data-types/string)
* `end_timestamp` — Especifica o fim da grade. Quando o argumento de timestamp é do tipo `DateTime64`, também pode ser um número fracionário ou uma string contendo um número ou texto de data e hora. [`UInt32`](/pt-BR/reference/data-types/int-uint) ou [`DateTime`](/pt-BR/reference/data-types/datetime) ou [`DateTime64`](/pt-BR/reference/data-types/datetime64) ou [`Float*`](/pt-BR/reference/data-types/float) ou [`Decimal*`](/pt-BR/reference/data-types/decimal) ou [`String`](/pt-BR/reference/data-types/string)
* `grid_step` — Especifica o passo da grade em segundos. Quando o argumento de timestamp é do tipo `DateTime64`, também pode ser um número fracionário ou uma string contendo um número ou uma duração como &#39;15s&#39; ou &#39;1m&#39;. [`UInt32`](/pt-BR/reference/data-types/int-uint) ou [`Float*`](/pt-BR/reference/data-types/float) ou [`Decimal*`](/pt-BR/reference/data-types/decimal) ou [`String`](/pt-BR/reference/data-types/string)
* `staleness` — Especifica a &quot;staleness&quot; máxima, em segundos, das amostras consideradas. Quando o argumento de timestamp é do tipo `DateTime64`, também pode ser um número fracionário ou uma string contendo um número ou uma duração como &#39;15s&#39; ou &#39;1m&#39;. [`UInt32`](/pt-BR/reference/data-types/int-uint) ou [`Float*`](/pt-BR/reference/data-types/float) ou [`Decimal*`](/pt-BR/reference/data-types/decimal) ou [`String`](/pt-BR/reference/data-types/string)

**Argumentos**

* `timestamp` — Timestamp da amostra. Pode ser um valor individual ou arrays. - `value` — Valor da série temporal correspondente ao timestamp. Pode ser um valor individual ou arrays.

**Valor retornado**

Valores de `changes` na grade especificada como um `Array(Nullable(Float64))`. O array retornado contém um valor para cada ponto da grade de tempo. O valor é NULL se não houver amostras dentro da janela para calcular o valor de `changes` de um determinado ponto da grade.

**Exemplos**

**Calcular valores de `changes` na grade [90, 105, 120, 135, 150, 165, 180, 195, 210, 225]**

```sql title=Query
SET allow_experimental_time_series_aggregate_functions = 1;
WITH
    -- NOTE: the gap between 130 and 190 is to show how values are filled for ts = 180 according to window parameter
    [110, 120, 130, 190, 200, 210, 220, 230]::Array(DateTime) AS timestamps,
    [1, 1, 3, 5, 5, 8, 12, 13]::Array(Float32) AS values, -- array of values corresponding to timestamps above
    90 AS start_ts,       -- start of timestamp grid
    90 + 135 AS end_ts,   -- end of timestamp grid
    15 AS step_seconds,   -- step of timestamp grid
    45 AS window_seconds  -- "staleness" window
SELECT timeSeriesChangesToGrid(start_ts, end_ts, step_seconds, window_seconds)(timestamp, value)
FROM
(
    -- This subquery converts arrays of timestamps and values into rows of `timestamp`, `value`
    SELECT
        arrayJoin(arrayZip(timestamps, values)) AS ts_and_val,
        ts_and_val.1 AS timestamp,
        ts_and_val.2 AS value
);
```

```response title=Response
┌─timeSeriesChangesToGrid(start_ts, end_ts, step_seconds, window_seconds)(timestamp, value)─┐
│ [NULL,NULL,0,1,1,0,NULL,0,1,2]                                                            │
└───────────────────────────────────────────────────────────────────────────────────────────┘
```

**A mesma consulta com argumentos de Array**

```sql title=Query
SET allow_experimental_time_series_aggregate_functions = 1;
WITH
    [110, 120, 130, 190, 200, 210, 220, 230]::Array(DateTime) AS timestamps,
    [1, 1, 3, 5, 5, 8, 12, 13]::Array(Float32) AS values,
    90 AS start_ts,
    90 + 135 AS end_ts,
    15 AS step_seconds,
    45 AS window_seconds
SELECT timeSeriesChangesToGrid(start_ts, end_ts, step_seconds, window_seconds)(timestamps, values);
```

```response title=Response
┌─timeSeriesChangesToGrid(start_ts, end_ts, step_seconds, window_seconds)(timestamps, values)─┐
│ [NULL,NULL,0,1,1,0,NULL,0,1,2]                                                              │
└─────────────────────────────────────────────────────────────────────────────────────────────┘
```

{/*AUTOGENERATED_END*/}