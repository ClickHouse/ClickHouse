---
description: 'Calcula vários quantis de uma sequência de dados numéricos usando interpolação linear em diferentes níveis simultaneamente, levando em consideração o peso de cada elemento.'
slug: /sql-reference/aggregate-functions/reference/quantilesInterpolatedWeighted
title: 'quantilesInterpolatedWeighted'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="quantilesInterpolatedWeighted">
  ## quantilesInterpolatedWeighted
</div>

Introduzido em: v23.1.0

Calcula múltiplos [quantis](https://en.wikipedia.org/wiki/Quantile) de uma sequência de dados numéricos usando interpolação linear em diferentes níveis simultaneamente, considerando o peso de cada elemento.

Esta função é equivalente a [`quantileInterpolatedWeighted`](/pt-BR/reference/functions/aggregate-functions/quantileInterpolatedWeighted), mas permite calcular vários níveis de quantil em uma única passagem, o que é mais eficiente do que chamar funções de quantil individualmente.

**Sintaxe**

```sql
quantilesInterpolatedWeighted(level1, level2, ...)(expr, weight)
```

**Parâmetros**

* `level` — Níveis dos quantis. Um ou mais números de ponto flutuante constantes de 0 a 1. Recomendamos usar valores de `level` no intervalo `[0.01, 0.99]`. [`Float*`](/pt-BR/reference/data-types/float)

**Argumentos**

* `expr` — Expressão sobre os valores da coluna que resulta em tipos de dados numéricos, `Date`, `DateTime` ou `DateTime64`. [`(U)Int*`](/pt-BR/reference/data-types/int-uint) ou [`Int128`](/pt-BR/reference/data-types/int-uint) ou [`UInt128`](/pt-BR/reference/data-types/int-uint) ou [`Int256`](/pt-BR/reference/data-types/int-uint) ou [`UInt256`](/pt-BR/reference/data-types/int-uint) ou [`Float*`](/pt-BR/reference/data-types/float) ou [`Decimal*`](/pt-BR/reference/data-types/decimal) ou [`Date`](/pt-BR/reference/data-types/date) ou [`DateTime`](/pt-BR/reference/data-types/datetime) ou [`DateTime64`](/pt-BR/reference/data-types/datetime64)
* `weight` — Coluna com os pesos dos membros da sequência. O peso é o número de ocorrências de um valor. [`UInt*`](/pt-BR/reference/data-types/int-uint)

**Valor retornado**

Array de quantis dos níveis especificados, na mesma ordem em que os níveis foram especificados. Para tipos de dados numéricos, o formato de saída corresponde ao formato de entrada. [`Array((U)Int*)`](/pt-BR/reference/data-types/array) ou [`Array(Int128)`](/pt-BR/reference/data-types/array) ou [`Array(UInt128)`](/pt-BR/reference/data-types/array) ou [`Array(Int256)`](/pt-BR/reference/data-types/array) ou [`Array(UInt256)`](/pt-BR/reference/data-types/array) ou [`Array(Float*)`](/pt-BR/reference/data-types/array) ou [`Array(Decimal*)`](/pt-BR/reference/data-types/array) ou [`Array(Date)`](/pt-BR/reference/data-types/array) ou [`Array(DateTime)`](/pt-BR/reference/data-types/array) ou [`Array(DateTime64)`](/pt-BR/reference/data-types/array)

**Exemplos**

**Cálculo de vários quantis ponderados interpolados**

```sql title=Query
SELECT quantilesInterpolatedWeighted(0.25, 0.5, 0.75)(number, 1) FROM numbers(10)
```

```response title=Response
┌─quantilesInterpolatedWeighted(0.25, 0.5, 0.75)(number, 1)─┐
│ [2,4,7]                                                   │
└───────────────────────────────────────────────────────────┘
```

{/*AUTOGENERATED_END*/}