---
description: 'Calcula o coeficiente de correlação de postos.'
slug: /sql-reference/aggregate-functions/reference/rankCorr
title: 'rankCorr'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="rankCorr">
  ## rankCorr
</div>

Introduzido em: v20.9.0

Calcula um coeficiente de correlação de postos.

Retorna o coeficiente de correlação de postos entre os ranks de x e y. O valor do coeficiente de correlação varia de -1 a +1. Se menos de dois argumentos forem passados, a função retornará uma exceção. Um valor próximo de +1 denota uma forte relação linear e, com o aumento de uma variável aleatória, a segunda variável aleatória também aumenta. Um valor próximo de -1 denota uma forte relação linear e, com o aumento de uma variável aleatória, a segunda variável aleatória diminui. Um valor próximo ou igual a 0 denota que não há relação entre as duas variáveis aleatórias.

**Veja também**

* [Coeficiente de correlação de postos de Spearman](https://en.wikipedia.org/wiki/Spearman%27s_rank_correlation_coefficient)

**Sintaxe**

```sql
rankCorr(x, y)
```

**Argumentos**

* `x` — Valor arbitrário. [`Float*`](/pt-BR/reference/data-types/float)
* `y` — Valor arbitrário. [`Float*`](/pt-BR/reference/data-types/float)

**Valor retornado**

Retorna um coeficiente de correlação de postos de x e y. O valor varia de -1 a +1. [`Float64`](/pt-BR/reference/data-types/float)

**Exemplos**

**Correlação perfeita**

```sql title=Query
SELECT rankCorr(number, number) FROM numbers(100);
```

```response title=Response
┌─rankCorr(number, number)─┐
│                        1 │
└──────────────────────────┘
```

**Relação não linear**

```sql title=Query
SELECT roundBankers(rankCorr(exp(number), sin(number)), 3) FROM numbers(100);
```

```response title=Response
┌─roundBankers(rankCorr(exp(number), sin(number)), 3)─┐
│                                              -0.037 │
└─────────────────────────────────────────────────────┘
```

{/*AUTOGENERATED_END*/}

**Veja também**

* [coeficiente de correlação de postos de Spearman](https://en.wikipedia.org/wiki/Spearman%27s_rank_correlation_coefficient)