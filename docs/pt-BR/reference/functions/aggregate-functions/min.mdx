---
description: 'Função de agregação que calcula o valor mínimo em um grupo de valores.'
slug: /sql-reference/aggregate-functions/reference/min
title: 'min'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="min">
  ## min
</div>

Introduzido em: v1.1.0

Função de agregação que calcula o valor mínimo em um grupo de valores.

**Sintaxe**

```sql
min(column)
```

**Argumentos**

* `column` — Nome da coluna ou expressão. [`Any`](/pt-BR/reference/data-types/index)

**Valor retornado**

Retorna o valor mínimo do grupo, com tipo igual ao da entrada. [`Any`](/pt-BR/reference/data-types/index)

**Exemplos**

**Exemplo simples de min**

```sql title=Query
CREATE TABLE employees (name String, salary UInt32) ENGINE = Memory;
INSERT INTO employees VALUES ('Alice', 3000), ('Bob', 4000), ('Charlie', 3500);

SELECT min(salary) FROM employees;
```

```response title=Response
┌─min(salary)─┐
│        3000 │
└─────────────┘
```

**Min com GROUP BY**

```sql title=Query
CREATE TABLE sales (department String, revenue UInt32) ENGINE = Memory;
INSERT INTO sales VALUES ('Engineering', 100000), ('Engineering', 120000), ('Marketing', 80000), ('Marketing', 90000);

SELECT department, min(revenue) FROM sales GROUP BY department ORDER BY department;
```

```response title=Response
┌─department──┬─min(revenue)─┐
│ Engineering │       100000 │
│ Marketing   │        80000 │
└─────────────┴──────────────┘
```

{/*AUTOGENERATED_END*/}