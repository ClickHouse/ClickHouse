---
description: 'Tabela do sistema que contém informações sobre o número de eventos que
  ocorreram no sistema.'
keywords: ['tabela do sistema', 'eventos']
slug: /operations/system-tables/events
title: 'system.events'
doc_type: 'reference'
---

import SystemTableCloud from '/snippets/pt-BR/_system_table_cloud.mdx';

<SystemTableCloud />

{/*AUTOGENERATED_START*/}

## Descrição {#description}

Contém informações sobre o número de eventos ocorridos no sistema. Por exemplo, na tabela, é possível ver quantas consultas `SELECT` foram processadas desde o início do servidor ClickHouse.

## Descrições de eventos {#event-descriptions}

### ACMEAPIRequests {#acmeapirequests}

Número de solicitações feitas à API ACME.

### ACMECertificateOrders {#acmecertificateorders}

Número de solicitações de certificados ACME emitidas.

### AdaptiveAggregationBucketsRetired {#adaptiveaggregationbucketsretired}

Número de buckets de dois níveis cuja memória de trabalho (slot de arena, referências a fragmentos preparados) foi liberada logo após a conclusão da mesclagem e conversão de cada um, antes que a mesclagem completa terminasse.

### AdaptiveAggregationDrainedRecords {#adaptiveaggregationdrainedrecords}

Quantos registros atrasados a agregação adaptativa transferiu para a tabela compartilhada no momento da mesclagem.

### AdaptiveAggregationGiveUps {#adaptiveaggregationgiveups}

Quantas threads desistiram do congelamento na agregação adaptativa porque seu stream continha poucas chaves distintas.

### AdaptiveAggregationLocalFreezes {#adaptiveaggregationlocalfreezes}

Quantas tabelas hash locais a agregação adaptativa congelou ao atingir o limiar de congelamento.

### AdaptiveAggregationPressureDrainedRecords {#adaptiveaggregationpressuredrainedrecords}

Quantos registros preparados a agregação adaptativa descarregou antecipadamente sob pressão de memória.

### AdaptiveAggregationPressureStandDowns {#adaptiveaggregationpressurestanddowns}

Quantas threads da agregação adaptativa abandonaram definitivamente a fase de aprendizado porque a pressão de memória ultrapassou o limiar da external aggregation, de modo que suas tabelas locais fazem spill pela external aggregation comum.

### AdaptiveAggregationPressureSweeps {#adaptiveaggregationpressuresweeps}

Quantas vezes a agregação adaptativa descarregou antecipadamente os registros preparados devido à pressão de memória.

### AdaptiveAggregationProbeBypasses {#adaptiveaggregationprobebypasses}

Número de threads que deixaram de sondar sua tabela local congelada na agregação adaptativa porque quase nenhuma linha a acessava.

### AdaptiveAggregationResidueReleases {#adaptiveaggregationresiduereleases}

Quantas vezes a agregação adaptativa gravou sua tabela de drenagem compartilhada porque uma thread que havia voltado ao algoritmo de referência estava prestes a fazer spill por causa dela.

### AdaptiveAggregationSealedChunks {#adaptiveaggregationsealedchunks}

Quantos fragmentos coalescidos a agregação adaptativa selou a partir de lotes de staging em buffer.

### AdaptiveAggregationSealNormalizations {#adaptiveaggregationsealnormalizations}

Quantas colunas de argumentos preparadas o selo de agregação adaptativa normalizou de uma representação encapsulada (Const, Replicated, Sparse, LowCardinality) para a forma densa consumida pelo dreno.

### AdaptiveAggregationSharedTableSpills {#adaptiveaggregationsharedtablespills}

Quantas vezes a agregação adaptativa gravou em disco sua tabela compartilhada de drenagem por ter atingido o limite de partes sob pressão de memória.

### AdaptiveAggregationSpillBacklogSheds {#adaptiveaggregationspillbacklogsheds}

Quantas vezes a agregação adaptativa descartou o backlog preparado porque uma thread no algoritmo de referência — seja uma que o descongelamento devolveu para lá, seja uma que se retirou por conta própria — estava prestes a fazer spill enquanto registros preparados ainda estavam residentes na memória.

### AdaptiveAggregationStagedBytes {#adaptiveaggregationstagedbytes}

Quantos bytes de chave a agregação adaptativa preparou para o escoamento no momento da mesclagem.

### AdaptiveAggregationStagedChunkPiecesOverBound {#adaptiveaggregationstagedchunkpiecesoverbound}

Quantas partes de um fragmento preparado truncado a agregação adaptativa publicou que ainda excedem o limite para o qual o fragmento foi truncado. Um fragmento é truncado nos limites de bucket e, dentro de um bucket que por si só excede o limite, registro a registro; portanto, trata-se de um único registro cujos bytes preparados, sozinhos, já excedem o limite. Uma parte dessas é reivindicada por inteiro, e seu escoamento pode ultrapassar a parte de pressão nessa mesma medida.

### AdaptiveAggregationStagedChunkSplits {#adaptiveaggregationstagedchunksplits}

Quantos fragmentos preparados a agregação adaptativa truncou nos limites de bucket durante a publicação porque o escoamento de um fragmento foi estimado acima do limite da parte de pressão.

### AdaptiveAggregationStagedClaimsClosedAtBound {#adaptiveaggregationstagedclaimsclosedatbound}

Quantos claims de fragmentos preparados pelos drenos da agregação adaptativa foram fechados antes de um fragmento cuja adição levaria o lote ao limite da parte de pressão, deixando esse fragmento para o claim seguinte. O limite se aplica ao lote tal como drenado, de modo que dois fragmentos, cada um logo abaixo dele — dois pedaços de um fragmento truncado que ficam um único registro acima de meia parte —, são drenados para duas tabelas em vez de uma; apenas um primeiro fragmento que, sozinho, ultrapasse o limite é reivindicado de qualquer forma.

### AdaptiveAggregationStagedRecords {#adaptiveaggregationstagedrecords}

Quantos registros atrasados a agregação adaptativa preparou antes da desduplicação.

### AdaptiveAggregationStagedRecordsMerged {#adaptiveaggregationstagedrecordsmerged}

Quantos registros preparados a agregação adaptativa eliminou por mesclagem, por serem chaves duplicadas, na publicação e no selamento.

### AdaptiveAggregationThaws {#adaptiveaggregationthaws}

Quantas vezes a agregação adaptativa descongelou as tabelas locais porque o stream em stage se mostrou dominado por repetições.

### AddressesDiscovered {#addressesdiscovered}

Número total de novos endereços nos resultados da resolução de DNS para conexões HTTP

### Endereços expirados {#addressesexpired}

Número total de endereços expirados que não aparecem mais nos resultados da resolução de DNS para conexões HTTP

### AddressesMarkedAsFailed {#addressesmarkedasfailed}

Contagem total de endereços marcados como inválidos devido a erros de conexão em conexões HTTP

### AggregatingSortedMilliseconds {#aggregatingsortedmilliseconds}

Tempo total gasto na agregação de colunas ordenadas

### AggregationBucketTopKConversions {#aggregationbuckettopkconversions}

Número de buckets de dois níveis convertidos pela seleção Top-K local ao bucket (a otimização de plano aggregationBucketTopK).

### AggregationConvertedToTwoLevel {#aggregationconvertedtotwolevel}

Quantas vezes uma tabela hash de agregação de um nível foi convertida em dois níveis durante a execução.

### AggregationHashTablesInitializedAsTwoLevel {#aggregationhashtablesinitializedastwolevel}

Número de tabelas hash inicializadas como de dois níveis para agregação.

### AggregationOptimizedEqualRangesOfKeys {#aggregationoptimizedequalrangesofkeys}

Para quantos blocos foi aplicada a otimização de intervalos iguais de chaves

### AggregationPreallocatedElementsInHashTables {#aggregationpreallocatedelementsinhashtables}

Número de elementos pré-alocados em tabelas hash para agregação.

### AggregationTopKHeapsFrozen {#aggregationtopkheapsfrozen}

Quantos heaps de agregação top-K foram congelados, recorrendo à agregação comum. Ou o heap rejeitou quase nada dentro de sua janela de observação (por exemplo, o número de chaves de agrupamento distintas não excede o LIMIT), ou um conjunto de empates na fronteira do heap — que nunca pode ser removido — o fez crescer demais (consulte `enable_group_by_top_k_optimization`).

### AggregationTopKKeysEvicted {#aggregationtopkkeysevicted}

Quantas chaves de agrupamento foram removidas (evicted) do heap top-K limitado durante a agregação (consulte `enable_group_by_top_k_optimization`).

### AggregationTopKKeysPruned {#aggregationtopkkeyspruned}

Quantas chaves de agrupamento removidas (evicted) também foram apagadas da tabela hash intermediária, com seus estados de agregação destruídos (veja `enable_group_by_top_k_optimization`). Menor que `AggregationTopKKeysEvicted` quando o método de agregação não consegue apagar chaves, ou quando apenas um prefixo da chave é classificado: nesse caso, o heap ainda pula linhas, mas a tabela hash mantém todos os grupos admitidos.

### AggregationTopKRowsSkipped {#aggregationtopkrowsskipped}

Quantas linhas foram ignoradas durante a agregação porque sua chave de agrupamento não pôde entrar no resultado top-K (consulte `enable_group_by_top_k_optimization`).

### AIAPICalls {#aiapicalls}

Número de solicitações HTTP encaminhadas aos provedores de IA.

### AIInputTokens {#aiinputtokens}

Total de tokens do prompt consumidos em todas as chamadas de função de IA na consulta.

### AIORead {#aioread}

Número de leituras usando a interface AIO do Linux ou do FreeBSD

### AIOReadBytes {#aioreadbytes}

Número de bytes lidos usando a interface AIO do Linux ou do FreeBSD

### AIOutputTokens {#aioutputtokens}

Total de tokens de resposta consumidos em todas as chamadas da função AI na consulta.

### AIOWrite {#aiowrite}

Número de escritas com a interface AIO do Linux ou FreeBSD

### AIOWriteBytes {#aiowritebytes}

Número de bytes escritos com a interface AIO do Linux ou do FreeBSD

### AIRowsProcessed {#airowsprocessed}

Número de linhas que receberam um resultado de IA.

### AIRowsSkipped {#airowsskipped}

Número de linhas que receberam um valor padrão devido a QUOTA ou erro.

### AllUsersThrottlerBytes {#allusersthrottlerbytes}

Bytes que passaram pelo throttler &#39;max&#95;network&#95;bandwidth&#95;for&#95;all&#95;users&#39;.

### AllUsersThrottlerSleepMicroseconds {#allusersthrottlersleepmicroseconds}

Tempo total em que uma consulta ficou aguardando para obedecer ao throttling de &#39;max&#95;network&#95;bandwidth&#95;for&#95;all&#95;users&#39;.

### AnalyzePatchRangesMicroseconds {#analyzepatchrangesmicroseconds}

Tempo total gasto na análise do índice das partes de patch

### ApplyPatchesMicroseconds {#applypatchesmicroseconds}

Tempo total gasto na aplicação de partes de patch aos blocos

### ApplyPatchMergeOnKeyMicroseconds {#applypatchmergeonkeymicroseconds}

Tempo total gasto nos laços de mesclagem por chave de ordenação do applyPatchesMergeOnKey

### ArenaAllocBytes {#arenaallocbytes}

Número de bytes alocados na Arena de memória (usada para GROUP BY e operações semelhantes)

### ArenaAllocChunks {#arenaallocchunks}

Número de fragmentos alocados na Arena de memória (usada para GROUP BY e operações semelhantes)

### ASTFuzzerOracleChecks {#astfuzzeroraclechecks}

Número de verificações de oráculo realizadas pelo AST fuzzer do lado do servidor.

### ASTFuzzerOracleMismatches {#astfuzzeroraclemismatches}

Número de divergências de oráculo detectadas pelo AST fuzzer do lado do servidor.

### ASTFuzzerOracleTLPAggregateChecks {#astfuzzeroracletlpaggregatechecks}

Número de verificações do oráculo TLP Aggregate (State/Merge) tentadas pelo AST fuzzer lado do servidor.

### ASTFuzzerQueries {#astfuzzerqueries}

Número de consultas submetidas a fuzzing tentadas pelo AST fuzzer no servidor.

### ASTFuzzerSkippedBackupRestore {#astfuzzerskippedbackuprestore}

Número de consultas BACKUP/RESTORE geradas por fuzzing que o AST fuzzer lado do servidor ignorou em vez de executar.

### ASTFuzzerSkippedReplicatedDDLInternal {#astfuzzerskippedreplicatedddlinternal}

Número de vezes que o AST fuzzer do lado do servidor ignorou o fuzzing porque havia uma execução interna de DDL de banco de dados replicado (uma ZooKeeperMetadataTransaction ativa) em andamento no Context.

### AsynchronousReaderIgnoredBytes {#asynchronousreaderignoredbytes}

Número de bytes ignorados durante a leitura assíncrona

### AsynchronousReadWaitMicroseconds {#asynchronousreadwaitmicroseconds}

Tempo gasto na espera por leituras assíncronas em leitura local assíncrona.

### AsynchronousRemoteReadWaitMicroseconds {#asynchronousremotereadwaitmicroseconds}

Tempo gasto na espera por leituras remotas assíncronas.

### AsyncInsertBytes {#asyncinsertbytes}

Tamanho, em bytes, dos dados de consultas INSERT assíncronas.

### AsyncInsertCacheHits {#asyncinsertcachehits}

Número de vezes em que um ID hash duplicado foi encontrado no cache de IDs hash de INSERT assíncrono.

### AsyncInsertFlush {#asyncinsertflush}

Número de flushes da fila de INSERT assíncronos. Cada flush insere um lote de consultas INSERT assíncronas como uma única parte. Compare com AsyncInsertQuery para ver quantas consultas são agrupadas em cada lote, em média.

### AsyncInsertQuery {#asyncinsertquery}

Igual a InsertQuery, mas apenas para consultas INSERT assíncronas.

### AsyncInsertRows {#asyncinsertrows}

Número de linhas inseridas por consultas INSERT assíncronas.

### AsyncLoaderSpawnFailures {#asyncloaderspawnfailures}

Número de vezes em que o carregador assíncrono não conseguiu criar um worker porque o global thread pool não pôde fornecer uma thread. Nesses casos, os jobs enfileirados são executados por um worker que o pool já tem: um que esteja em execução ou um que esteja retomando após aguardar um job de outro pool.

### AsyncLoaderWaitMicroseconds {#asyncloaderwaitmicroseconds}

Tempo total em que uma consulta ficou aguardando tarefas do carregador assíncrono.

### AsyncLoggingConsoleDroppedMessages {#asyncloggingconsoledroppedmessages}

Quantas mensagens foram descartadas do log no console porque a fila de log assíncrono estava cheia

### AsyncLoggingConsoleTotalMessages {#asyncloggingconsoletotalmessages}

Número de mensagens (aceitas ou descartadas) enviadas para a fila assíncrona do log do console

### AsyncLoggingErrorFileLogDroppedMessages {#asyncloggingerrorfilelogdroppedmessages}

Quantas mensagens foram descartadas do arquivo de log de erros porque a fila de log assíncrono estava cheia

### AsyncLoggingErrorFileLogTotalMessages {#asyncloggingerrorfilelogtotalmessages}

Quantas mensagens (aceitas ou descartadas) foram enviadas para a fila assíncrona do arquivo de log de erro

### AsyncLoggingFileLogDroppedMessages {#asyncloggingfilelogdroppedmessages}

Quantas mensagens foram descartadas no log em arquivo porque a fila de logs assíncrona estava cheia

### AsyncLoggingFileLogTotalMessages {#asyncloggingfilelogtotalmessages}

Quantas mensagens (aceitas ou descartadas) foram enviadas para a fila assíncrona do log em arquivo

### AsyncLoggingSyslogDroppedMessages {#asyncloggingsyslogdroppedmessages}

Quantas mensagens foram descartadas do syslog porque a fila de logs assíncronos estava cheia

### AsyncLoggingSyslogTotalMessages {#asyncloggingsyslogtotalmessages}

Quantas mensagens (aceitas ou descartadas) foram enviadas para a fila assíncrona de syslog

### AsyncLoggingTextLogDroppedMessages {#asyncloggingtextlogdroppedmessages}

Quantas mensagens foram descartadas do text&#95;log porque a fila de log assíncrono estava cheia

### AsyncLoggingTextLogTotalMessages {#asyncloggingtextlogtotalmessages}

Número de mensagens (aceitas ou descartadas) enviadas para a fila assíncrona do text&#95;log

### AzureCommitBlockList {#azurecommitblocklist}

Número de chamadas CommitBlockList da API do Azure Blob Storage

### AzureCopyObject {#azurecopyobject}

Número de chamadas CopyObject da API do Azure Blob Storage

### AzureCreateContainer {#azurecreatecontainer}

Número de chamadas CreateContainer da API do Azure Blob Storage.

### AzureDeleteObjects {#azuredeleteobjects}

Número de chamadas DeleteObject(s) da API do Azure Blob Storage.

### AzureGetObject {#azuregetobject}

Número de chamadas GetObject para a API do Azure Blob Storage.

### AzureGetProperties {#azuregetproperties}

Número de chamadas GetProperties da API do Azure Blob Storage.

### AzureGetRequestThrottlerBlocked {#azuregetrequestthrottlerblocked}

Número de requisições GET do Azure bloqueadas pelo throttler.

### AzureGetRequestThrottlerCount {#azuregetrequestthrottlercount}

Número de solicitações GET do Azure que passaram pelo throttler: bloqueadas e não bloqueadas.

### AzureGetRequestThrottlerSleepMicroseconds {#azuregetrequestthrottlersleepmicroseconds}

Tempo total em que uma consulta permaneceu em espera para cumprir a limitação de taxa de solicitações GET do Azure.

### AzureListObjects {#azurelistobjects}

Número de chamadas ListObjects à API do Azure Blob Storage.

### AzurePutRequestThrottlerBlocked {#azureputrequestthrottlerblocked}

Número de solicitações PUT do Azure bloqueadas pelo throttler.

### AzurePutRequestThrottlerCount {#azureputrequestthrottlercount}

Número de solicitações PUT do Azure que passaram pelo throttler: bloqueadas e não bloqueadas.

### AzurePutRequestThrottlerSleepMicroseconds {#azureputrequestthrottlersleepmicroseconds}

Tempo total em que uma consulta ficou em espera para atender à limitação de taxa das solicitações PUT do Azure.

### AzureReadMicroseconds {#azurereadmicroseconds}

Tempo total de espera por solicitações de leitura do Azure.

### AzureReadRequestsCount {#azurereadrequestscount}

Número de solicitações de leitura no Azure.

### AzureReadRequestsErrors {#azurereadrequestserrors}

Número de erros em solicitações de leitura do Azure.

### AzureReadRequestsRedirects {#azurereadrequestsredirects}

Número de redirecionamentos de solicitações de leitura do Azure.

### AzureReadRequestsThrottling {#azurereadrequeststhrottling}

Número de solicitações de leitura do Azure com limitação de taxa.

### AzureStageBlock {#azurestageblock}

Número de chamadas à API StageBlock do Azure blob storage

### AzureUpload {#azureupload}

Número de chamadas de Upload da API do Azure Blob Storage

### AzureWriteMicroseconds {#azurewritemicroseconds}

Tempo total de espera por solicitações de gravação no Azure.

### AzureWriteRequestsCount {#azurewriterequestscount}

Número de solicitações de gravação no Azure.

### AzureWriteRequestsErrors {#azurewriterequestserrors}

Número de erros em solicitações de gravação no Azure.

### AzureWriteRequestsRedirects {#azurewriterequestsredirects}

Número de redirecionamentos de solicitações de escrita do Azure.

### AzureWriteRequestsThrottling {#azurewriterequeststhrottling}

Número de solicitações de gravação do Azure que foram limitadas.

### BackgroundLoadingMarksTasks {#backgroundloadingmarkstasks}

Número de tarefas em segundo plano para carregar marcas

### BackupEntriesCollectorForTablesDataMicroseconds {#backupentriescollectorfortablesdatamicroseconds}

Tempo gasto na criação de entradas de backup para dados de tabelas

### BackupEntriesCollectorMicroseconds {#backupentriescollectormicroseconds}

Tempo gasto na criação de entradas de backup

### BackupEntriesCollectorRunPostTasksMicroseconds {#backupentriescollectorrunposttasksmicroseconds}

Tempo gasto na execução de tarefas posteriores após a criação de entradas de backup

### BackupLockFileReads {#backuplockfilereads}

Quantas vezes o arquivo &#39;.lock&#39; foi lido ao criar o backup

### BackupPreparingFileInfosMicroseconds {#backuppreparingfileinfosmicroseconds}

Tempo gasto preparando informações de arquivos para as entradas do backup

### BackupReadLocalBytesToCalculateChecksums {#backupreadlocalbytestocalculatechecksums}

Tamanho total dos arquivos lidos localmente para calcular checksums dos itens do backup

### BackupReadLocalFilesToCalculateChecksums {#backupreadlocalfilestocalculatechecksums}

Número de arquivos lidos localmente para calcular checksum dos itens do backup

### BackupReadMetadataMicroseconds {#backupreadmetadatamicroseconds}

Tempo gasto na leitura dos metadados do backup a partir do arquivo .backup

### Bytes remotos lidos pelo Backup para calcular checksums {#backupreadremotebytestocalculatechecksums}

Tamanho total dos arquivos lidos em discos remotos para calcular os checksums dos itens de backup

### BackupReadRemoteFilesToCalculateChecksums {#backupreadremotefilestocalculatechecksums}

Número de arquivos lidos de discos remotos para calcular checksums de itens do backup

### BackupsOpenedForRead {#backupsopenedforread}

Número de backups abertos para leitura

### BackupsOpenedForUnlock {#backupsopenedforunlock}

Número de backups abertos para desbloqueio

### BackupsOpenedForWrite {#backupsopenedforwrite}

Número de backups abertos para gravação

### BackupThrottlerBytes {#backupthrottlerbytes}

Bytes que passaram pelo throttler &#39;max&#95;backup&#95;bandwidth&#95;for&#95;server&#39;.

### BackupThrottlerSleepMicroseconds {#backupthrottlersleepmicroseconds}

Tempo total em que uma consulta ficou aguardando para obedecer à limitação de taxa de &#39;max&#95;backup&#95;bandwidth&#95;for&#95;server&#39;.

### BackupWriteMetadataMicroseconds {#backupwritemetadatamicroseconds}

Tempo gasto ao gravar metadados do backup no arquivo .backup

### BlobCopierThreadLockBlobsErrors {#blobcopierthreadlockblobserrors}

Número de erros de bloqueio de blobs que ocorreram durante a execução do BlobCopier

### BlobCopierThreadLockedBlobs {#blobcopierthreadlockedblobs}

Número de blobs retornados pelo armazenamento de metadados

### BlobCopierThreadRecordBlobsErrors {#blobcopierthreadrecordblobserrors}

Número de erros ocorridos ao registrar blobs durante a execução do BlobCopier

### BlobCopierThreadRecordedBlobs {#blobcopierthreadrecordedblobs}

Número de blobs cuja replicação pelo BlobCopier foi registrada no armazenamento de metadados

### BlobCopierThreadReplicateBlobsErrors {#blobcopierthreadreplicateblobserrors}

Número de erros de replicação de blobs que ocorreram durante a execução do BlobCopier

### BlobCopierThreadReplicatedBlobs {#blobcopierthreadreplicatedblobs}

Número de blobs replicados pelo BlobCopier

### BlobCopierThreadRuns {#blobcopierthreadruns}

Número de execuções da thread BlobCopier

### BlobKillerThreadLockBlobsErrors {#blobkillerthreadlockblobserrors}

Número de erros de bloqueio de blobs durante a execução do BlobKiller

### BlobKillerThreadLockedBlobs {#blobkillerthreadlockedblobs}

Número de blobs retornados pelo armazenamento de metadados

### BlobKillerThreadRecordBlobsErrors {#blobkillerthreadrecordblobserrors}

Número de erros ao registrar blobs ocorridos durante a execução do BlobKiller

### BlobKillerThreadRecordedBlobs {#blobkillerthreadrecordedblobs}

Número de blobs cuja remoção pelo BlobKiller foi registrada no armazenamento de metadados

### BlobKillerThreadRemoveBlobsErrors {#blobkillerthreadremoveblobserrors}

Número de erros na remoção de blobs ocorridos durante a execução do BlobKiller

### BlobKillerThreadRemovedBlobs {#blobkillerthreadremovedblobs}

Número de blobs removidos pelo BlobKiller

### BlobKillerThreadRemoveTasks {#blobkillerthreadremovetasks}

Número de tarefas de remoção criadas pelo BlobKiller

### BlobKillerThreadRuns {#blobkillerthreadruns}

Número de execuções da thread BlobKiller

### BuildPatchesJoinMicroseconds {#buildpatchesjoinmicroseconds}

Tempo total gasto na criação de índices e tabelas hash para aplicar partes de patch no modo Join

### BuildPatchesMergeMicroseconds {#buildpatchesmergemicroseconds}

Tempo total gasto na criação de índices para aplicar parte de patch no modo Merge

### CachedReadBufferCacheWriteBytes {#cachedreadbuffercachewritebytes}

Bytes gravados da origem (sistema de arquivos remoto etc.) para o cache do sistema de arquivos

### CachedReadBufferCacheWriteMicroseconds {#cachedreadbuffercachewritemicroseconds}

Tempo gasto na gravação de dados no cache do sistema de arquivos

### CachedReadBufferCacheWriteStopped {#cachedreadbuffercachewritestopped}

Número de vezes em que a gravação de um file segment no cache do sistema de arquivos foi interrompida no meio do download (falha na reserva de espaço ou na gravação no cache), após o que a leitura prossegue ignorando o cache

### CachedReadBufferCreateBufferMicroseconds {#cachedreadbuffercreatebuffermicroseconds}

Tempo para preparar o buffer

### CachedReadBufferPredownloadedBytes {#cachedreadbufferpredownloadedbytes}

Bytes lidos da origem do cache do sistema de arquivos. Os segmentos do cache são lidos da esquerda para a direita como um todo; portanto, pode ser necessário baixar previamente alguma parte do segmento que não é relevante para a tarefa atual apenas para chegar aos dados necessários

### CachedReadBufferPredownloadedFromSourceBytes {#cachedreadbufferpredownloadedfromsourcebytes}

Bytes lidos da origem do cache do sistema de arquivos para pré-download (de sistema de arquivos remoto etc.)

### CachedReadBufferPredownloadedFromSourceMicroseconds {#cachedreadbufferpredownloadedfromsourcemicroseconds}

Tempo de leitura da origem do cache do sistema de arquivos durante o pré-download (do sistema de arquivos remoto etc.)

### CachedReadBufferReadFromCacheBytes {#cachedreadbufferreadfromcachebytes}

Bytes lidos do cache do sistema de arquivos

### CachedReadBufferReadFromCacheHits {#cachedreadbufferreadfromcachehits}

Número de vezes em que a leitura no cache do sistema de arquivos resultou em acerto no cache.

### CachedReadBufferReadFromCacheMicroseconds {#cachedreadbufferreadfromcachemicroseconds}

Tempo de leitura a partir do cache do sistema de arquivos

### CachedReadBufferReadFromCacheMisses {#cachedreadbufferreadfromcachemisses}

Número de vezes em que a leitura no cache do sistema de arquivos resultou em cache miss.

### CachedReadBufferReadFromSourceBytes {#cachedreadbufferreadfromsourcebytes}

Bytes lidos da origem do cache do sistema de arquivos (do sistema de arquivos remoto etc.)

### CachedReadBufferReadFromSourceMicroseconds {#cachedreadbufferreadfromsourcemicroseconds}

Tempo de leitura a partir da origem do cache do sistema de arquivos (sistema de arquivos remoto etc.)

### CachedReadBufferWaitReadBufferMicroseconds {#cachedreadbufferwaitreadbuffermicroseconds}

Tempo gasto aguardando o buffer interno de leitura (inclui a espera pelo cache)

### CachedWriteBufferCacheWriteBytes {#cachedwritebuffercachewritebytes}

Bytes gravados da origem (sistema de arquivos remoto etc.) para o cache do sistema de arquivos

### CachedWriteBufferCacheWriteMicroseconds {#cachedwritebuffercachewritemicroseconds}

Tempo gasto na gravação de dados no cache do sistema de arquivos

### CachedWriteBufferCacheWriteStopped {#cachedwritebuffercachewritestopped}

Número de vezes em que o caching write-through foi interrompido (falha na reserva de espaço ou na gravação no cache, ou um segmento de cobertura estava sendo removido), após o que a gravação prossegue sem popular o cache

### CachedWriteBufferCoveringSegmentShrinkFailed {#cachedwritebuffercoveringsegmentshrinkfailed}

Número de vezes que o caching write-through precisou ser interrompido porque um file segment de cobertura que ficou atrás do offset em que se estava gravando não pôde ser redimensionado (está retido por outro processo, portanto o buraco nele não pode ser resolvido) ou foi evicted de forma concurrent

### CachedWriteBufferCoveringSegmentShrunk {#cachedwritebuffercoveringsegmentshrunk}

Número de vezes que o caching write-through redimensionou um file segment de cobertura que ficou atrás do offset em que a gravação estava ocorrendo (os ranges intermediários foram para outro distributed cache server), a fim de continuar o cache em um novo file segment

### CacheWarmerBytesDownloaded {#cachewarmerbytesdownloaded}

Quantidade de dados carregados no cache do sistema de arquivos por threads dedicadas em segundo plano.

### CacheWarmerDataPartsDownloaded {#cachewarmerdatapartsdownloaded}

Número de partes de dados que foram baixadas integralmente pelo CacheWarmer.

### CannotRemoveEphemeralNode {#cannotremoveephemeralnode}

Número de vezes em que ocorreu um erro ao tentar remover um nó efêmero. Isso não é um problema, porque nossa implementação da biblioteca ZooKeeper garante que a sessão expirará e o nó será removido.

### CannotWriteToWriteBufferDiscard {#cannotwritetowritebufferdiscard}

Número de stack traces descartados pelo query profiler ou pelo manipulador de sinais porque o pipe está cheio ou porque não é possível gravar nele.

### CoalescingSortedMilliseconds {#coalescingsortedmilliseconds}

Tempo total gasto na consolidação de colunas ordenadas

### CollapsingSortedMilliseconds {#collapsingsortedmilliseconds}

Tempo total gasto no colapso de colunas ordenadas

### CommonBackgroundExecutorTaskCancelMicroseconds {#commonbackgroundexecutortaskcancelmicroseconds}

Tempo gasto em cancel() nas tarefas do Common executor.

### CommonBackgroundExecutorTaskExecuteStepMicroseconds {#commonbackgroundexecutortaskexecutestepmicroseconds}

Tempo gasto em executeStep() nas tarefas do Common executor.

### CommonBackgroundExecutorTaskResetMicroseconds {#commonbackgroundexecutortaskresetmicroseconds}

Tempo gasto ao redefinir a tarefa do Common executor.

### CommonBackgroundExecutorWaitMicroseconds {#commonbackgroundexecutorwaitmicroseconds}

Tempo gasto aguardando a conclusão no Common executor.

### CompiledFunctionExecute {#compiledfunctionexecute}

Número de vezes que uma função compilada foi executada.

### CompileExpressionsBytes {#compileexpressionsbytes}

Número de bytes usados na compilação de expressões.

### CompileExpressionsMicroseconds {#compileexpressionsmicroseconds}

Tempo total gasto na compilação de expressões em código LLVM.

### CompileFunction {#compilefunction}

Número de vezes em que foi iniciada a compilação do código LLVM gerado (para criar uma função unificada para expressões complexas).

### CompileRegexpFunction {#compileregexpfunction}

Número de vezes que uma expressão regular foi compilada em tempo de execução (JIT) para código de máquina.

### CompressedReadBufferBlocks {#compressedreadbufferblocks}

Número de blocos compactados (blocos de dados compactados independentemente uns dos outros) lidos de fontes compactadas (arquivos, rede).

### CompressedReadBufferBytes {#compressedreadbufferbytes}

Número de bytes não compactados (ou seja, o número de bytes após a descompressão) lidos de fontes compactadas (arquivos, rede).

### CompressedReadBufferChecksumDoesntMatch {#compressedreadbufferchecksumdoesntmatch}

Número de vezes em que o checksum do bloco compactado não correspondeu.

### CompressedReadBufferChecksumDoesntMatchMicroseconds {#compressedreadbufferchecksumdoesntmatchmicroseconds}

Tempo total gasto na detecção de inversões de bits causadas por divergências na soma de verificação de blocos comprimidos.

### CompressedReadBufferChecksumDoesntMatchSingleBitMismatch {#compressedreadbufferchecksumdoesntmatchsinglebitmismatch}

Número de vezes em que uma divergência no checksum de um bloco compactado foi causada por uma diferença de um único bit.

### ConcurrencyControlDownscales {#concurrencycontroldownscales}

Número total de eventos de redução de capacidade da CPU

### ConcurrencyControlPreemptedMicroseconds {#concurrencycontrolpreemptedmicroseconds}

Tempo total em que uma consulta ficou em espera devido à preempção de slots de CPU.

### ConcurrencyControlPreemptions {#concurrencycontrolpreemptions}

Número total de preempções da CPU

### ConcurrencyControlQueriesDelayed {#concurrencycontrolqueriesdelayed}

Número de consultas atrasadas pela pressão de capacidade dos slots de CPU. Indicador de contenção — exclui esperas por aumento de escala preguiçoso em um servidor sem contenção.

### ConcurrencyControlSlotsAcquired {#concurrencycontrolslotsacquired}

Número total de slots de CPU obtidos

### ConcurrencyControlSlotsAcquiredNonCompeting {#concurrencycontrolslotsacquirednoncompeting}

Número total de slots de CPU não concorrentes adquiridos

### ConcurrencyControlSlotsDelayed {#concurrencycontrolslotsdelayed}

Número de slots de CPU não concedidos no momento da alocação devido à pressão de capacidade. Exclui slots retidos pela alocação preguiçosa.

### ConcurrencyControlSlotsGranted {#concurrencycontrolslotsgranted}

Número de slots de CPU concedidos com base na garantia de 1 thread por consulta e para consultas com a configuração &#39;use&#95;concurrency&#95;control&#39; = 0

### ConcurrencyControlUpscales {#concurrencycontrolupscales}

Número total de eventos de escalonamento de CPU para cima

### ConcurrencyControlWaitMicroseconds {#concurrencycontrolwaitmicroseconds}

Tempo total que uma consulta ficou aguardando solicitações de recursos para slots de CPU.

### ConcurrentQuerySlotsAcquired {#concurrentqueryslotsacquired}

Número total de slots de consulta obtidos

### ConcurrentQueryWaitMicroseconds {#concurrentquerywaitmicroseconds}

Tempo total em que uma consulta ficou aguardando slots de consulta

### ConnectionPoolIsFullMicroseconds {#connectionpoolisfullmicroseconds}

Tempo total gasto aguardando um slot no pool de conexões.

### ContextLock {#contextlock}

Número de vezes que o bloqueio de Context foi adquirido ou que se tentou adquiri-lo. Trata-se de um bloqueio global.

### ContextLockWaitMicroseconds {#contextlockwaitmicroseconds}

Tempo de espera pelo bloqueio do Context em microssegundos

### CoordinatedMergesMergeAssignmentRequest {#coordinatedmergesmergeassignmentrequest}

Número total de solicitações de atribuição de merge

### CoordinatedMergesMergeAssignmentRequestMicroseconds {#coordinatedmergesmergeassignmentrequestmicroseconds}

Tempo total gasto no cliente responsável pela atribuição de merge

### CoordinatedMergesMergeAssignmentResponse {#coordinatedmergesmergeassignmentresponse}

Número total de solicitações de atribuição de merge

### CoordinatedMergesMergeAssignmentResponseMicroseconds {#coordinatedmergesmergeassignmentresponsemicroseconds}

Tempo total gasto no handler de atribuição de merge

### CoordinatedMergesMergeCoordinatorFetchMetadataMicroseconds {#coordinatedmergesmergecoordinatorfetchmetadatamicroseconds}

Tempo total gasto na obtenção de metadados atualizados no coordenador de merge

### CoordinatedMergesMergeCoordinatorFilterMicroseconds {#coordinatedmergesmergecoordinatorfiltermicroseconds}

Tempo total gasto na filtragem de merges preparados no coordenador de merge

### CoordinatedMergesMergeCoordinatorLockStateExclusivelyCount {#coordinatedmergesmergecoordinatorlockstateexclusivelycount}

Número total de aquisições exclusivas do bloqueio de estado do coordenador

### CoordinatedMergesMergeCoordinatorLockStateExclusivelyMicroseconds {#coordinatedmergesmergecoordinatorlockstateexclusivelymicroseconds}

Tempo total gasto no bloqueio exclusivo do mutex do estado do coordenador

### CoordinatedMergesMergeCoordinatorLockStateForShareCount {#coordinatedmergesmergecoordinatorlockstateforsharecount}

Número total de capturas do bloqueio do estado do coordenador em modo `for share`

### CoordinatedMergesMergeCoordinatorLockStateForShareMicroseconds {#coordinatedmergesmergecoordinatorlockstateforsharemicroseconds}

Tempo total gasto ao bloquear o mutex do estado do coordenador em modo compartilhado

### CoordinatedMergesMergeCoordinatorSelectMergesMicroseconds {#coordinatedmergesmergecoordinatorselectmergesmicroseconds}

Tempo total gasto para encontrar uma mesclagem usando seletores de mesclagem dentro do coordenador de merge

### CoordinatedMergesMergeCoordinatorUpdateCount {#coordinatedmergesmergecoordinatorupdatecount}

Número total de atualizações do coordenador de merge

### CoordinatedMergesMergeCoordinatorUpdateMicroseconds {#coordinatedmergesmergecoordinatorupdatemicroseconds}

Tempo total gasto para atualizar o estado do coordenador de merge

### CoordinatedMergesMergeWorkerUpdateCount {#coordinatedmergesmergeworkerupdatecount}

Número total de atualizações do worker de merge

### CoordinatedMergesMergeWorkerUpdateMicroseconds {#coordinatedmergesmergeworkerupdatemicroseconds}

Tempo total gasto para atualizar o estado local das mesclagens atribuídas no worker

### CreatedLogEntryForMerge {#createdlogentryformerge}

Registro de log criado com sucesso para mesclar partes no ReplicatedMergeTree.

### CreatedLogEntryForMutation {#createdlogentryformutation}

Entrada de log criada com sucesso para executar a mutação das partes no ReplicatedMergeTree.

### CreatedReadBufferDirectIO {#createdreadbufferdirectio}

Número de vezes em que um buffer de leitura com O&#95;DIRECT foi criado para ler dados (ao escolher entre outros métodos de leitura).

### CreatedReadBufferDirectIOFailed {#createdreadbufferdirectiofailed}

Número de vezes em que se tentou criar um buffer de leitura com O&#95;DIRECT para ler dados (ao escolher entre outros métodos de leitura), mas o SO não permitiu isso (devido à falta de suporte do filesystem ou por outros motivos), e o sistema recorreu ao método de leitura comum.

### CreatedReadBufferMMap {#createdreadbuffermmap}

Número de vezes em que um buffer de leitura com &#39;mmap&#39; foi criado para ler dados (ao optar entre outros métodos de leitura).

### CreatedReadBufferMMapFailed {#createdreadbuffermmapfailed}

Número de vezes em que se tentou criar um buffer de leitura com &#39;mmap&#39; para ler dados (ao escolher entre outros métodos de leitura), mas o sistema operacional não permitiu isso (devido à falta de suporte do filesystem ou por outros motivos), e houve fallback para o método de leitura comum.

### CreatedReadBufferOrdinary {#createdreadbufferordinary}

Número de vezes que o buffer de leitura padrão foi criado para ler dados (ao escolher entre outros métodos de leitura).

### DataAfterMergeDiffersFromReplica {#dataaftermergediffersfromreplica}

Número de vezes em que os dados após a mesclagem não são byte a byte idênticos aos dados em outras réplicas. Pode haver vários motivos:

1. Uso de uma versão mais recente da biblioteca de compressão após a atualização do servidor.
2. Uso de outro método de compressão.
3. Algoritmo de compressão não determinístico (altamente improvável).
4. Algoritmo de mesclagem não determinístico devido a um erro lógico no código.
5. Corrupção de dados na memória devido a um bug no código.
6. Corrupção de dados na memória devido a um problema de hardware.
7. Modificação manual dos dados de origem após a inicialização do servidor.
8. Modificação manual das somas de verificação armazenadas no ZooKeeper.
9. Configurações relacionadas ao formato das partes, como &#39;enable&#95;mixed&#95;granularity&#95;parts&#39;, são diferentes entre as réplicas.
   O servidor detectou essa situação com sucesso e baixará a parte mesclada da réplica para garantir um resultado byte a byte idêntico.

### DataAfterMutationDiffersFromReplica {#dataaftermutationdiffersfromreplica}

Número de vezes em que os dados após a mutação não são byte a byte idênticos aos dados em outras réplicas. Além dos motivos descritos em &#39;DataAfterMergeDiffersFromReplica&#39;, isso também pode ocorrer devido a uma mutação não determinística.

### DefaultImplementationForNullsRows {#defaultimplementationfornullsrows}

Número de linhas processadas pela implementação padrão para valores NULL durante a execução da função

### DefaultImplementationForNullsRowsWithNulls {#defaultimplementationfornullsrowswithnulls}

Número de linhas com valores nulos processadas pela implementação padrão para nulos durante a execução da função

### DelayedInserts {#delayedinserts}

Número de vezes que o INSERT de um bloco em uma tabela MergeTree foi limitado devido ao alto número de partes de dados ativas na partição.

### DelayedInsertsMilliseconds {#delayedinsertsmilliseconds}

Número total de milissegundos gastos enquanto o INSERT de um bloco em uma tabela MergeTree teve sua taxa limitada devido ao alto número de partes de dados ativas na partição.

### DelayedMutations {#delayedmutations}

Número de vezes em que a mutação de uma tabela MergeTree teve sua taxa limitada devido ao alto número de mutações pendentes na tabela.

### DelayedMutationsMilliseconds {#delayedmutationsmilliseconds}

Número total de milissegundos gastos enquanto a mutação de uma tabela MergeTree foi limitada devido ao alto número de mutações não concluídas na tabela.

### DeltaLakeDeltaLogExistenceChecks {#deltalakedeltalogexistencechecks}

Número de listagens no armazenamento de objetos realizadas para verificar se existe um `_delta_log` do DeltaLake (por exemplo, durante CREATE TABLE).

### DeltaLakePartitionPrunedFiles {#deltalakepartitionprunedfiles}

Número de arquivos ignorados durante a eliminação de partições do DeltaLake

### DeltaLakeScannedFiles {#deltalakescannedfiles}

Número de arquivos examinados durante os callbacks da varredura do DeltaLake

### DeltaLakeSnapshotInitializations {#deltalakesnapshotinitializations}

Número de vezes que o snapshot de uma tabela DeltaLake foi inicializado (carregado do armazenamento de objetos)

### DictCacheKeysExpired {#dictcachekeysexpired}

Número de chaves consultadas nos dicionários do tipo &#39;cache&#39; e encontradas no cache, mas que estavam obsoletas.

### DictCacheKeysHit {#dictcachekeyshit}

Número de chaves consultadas nos dicionários do tipo &#39;cache&#39; e encontradas no cache.

### DictCacheKeysNotFound {#dictcachekeysnotfound}

Número de chaves consultadas nos dicionários do tipo &#39;cache&#39; e não encontradas.

### DictCacheKeysRequested {#dictcachekeysrequested}

Número de chaves solicitadas à fonte de dados para os dicionários do tipo &#39;cache&#39;.

### DictCacheKeysRequestedFound {#dictcachekeysrequestedfound}

Número de chaves solicitadas à fonte de dados para dicionários do tipo &#39;cache&#39; e encontradas na fonte de dados.

### DictCacheKeysRequestedMiss {#dictcachekeysrequestedmiss}

Número de chaves solicitadas à fonte de dados para dicionários do tipo &#39;cache&#39;, mas não encontradas nela.

### DictCacheLockReadNs {#dictcachelockreadns}

Número de nanossegundos gastos aguardando o bloqueio de leitura para consultar os dados dos dicionários do tipo &#39;cache&#39;.

### DictCacheLockWriteNs {#dictcachelockwritens}

Número de nanossegundos gastos aguardando o bloqueio de escrita para atualizar os dados dos dicionários do tipo &#39;cache&#39;.

### DictCacheRequests {#dictcacherequests}

Número de requisições em lote às fontes de dados externas dos dicionários do tipo &#39;cache&#39;.

### DictCacheRequestTimeNs {#dictcacherequesttimens}

Número de nanossegundos gastos consultando as fontes de dados externas dos dicionários do tipo &#39;cache&#39;.

### DirectorySync {#directorysync}

Número de vezes que a função F&#95;FULLFSYNC/fsync/fdatasync foi chamada em diretórios.

### DirectorySyncElapsedMicroseconds {#directorysyncelapsedmicroseconds}

Tempo total gasto aguardando a syscall F&#95;FULLFSYNC/fsync/fdatasync em diretórios.

### DiskAzureCommitBlockList {#diskazurecommitblocklist}

Número de chamadas CommitBlockList da API de armazenamento de blobs do Disk Azure

### DiskAzureCopyObject {#diskazurecopyobject}

Número de chamadas à API CopyObject do armazenamento de blobs do Disk Azure

### DiskAzureCreateContainer {#diskazurecreatecontainer}

Número de chamadas CreateContainer da API de armazenamento de blobs do Disk Azure.

### DiskAzureDeleteObjects {#diskazuredeleteobjects}

Número de chamadas à API DeleteObject(s) do Armazenamento de Blobs do Azure.

### DiskAzureGetObject {#diskazuregetobject}

Número de chamadas à API GetObject do Disk Azure.

### DiskAzureGetProperties {#diskazuregetproperties}

Número de chamadas à API GetProperties do armazenamento de blobs do Disk Azure.

### DiskAzureGetRequestThrottlerBlocked {#diskazuregetrequestthrottlerblocked}

Número de solicitações GET ao disco do Azure bloqueadas pelo throttler.

### DiskAzureGetRequestThrottlerCount {#diskazuregetrequestthrottlercount}

Número de solicitações GET ao disco do Azure que passaram pelo throttler: bloqueadas e não bloqueadas.

### DiskAzureGetRequestThrottlerSleepMicroseconds {#diskazuregetrequestthrottlersleepmicroseconds}

Tempo total em que uma consulta permaneceu em espera para obedecer à limitação de taxa de solicitações GET do disco do Azure.

### DiskAzureListObjects {#diskazurelistobjects}

Número de chamadas à API ListObjects do armazenamento de blobs do Disk Azure.

### DiskAzurePutRequestThrottlerBlocked {#diskazureputrequestthrottlerblocked}

Número de solicitações PUT para disco no Azure bloqueadas pelo throttler.

### DiskAzurePutRequestThrottlerCount {#diskazureputrequestthrottlercount}

Número de solicitações PUT para disco no Azure que passaram pelo throttler: bloqueadas e não bloqueadas.

### DiskAzurePutRequestThrottlerSleepMicroseconds {#diskazureputrequestthrottlersleepmicroseconds}

Tempo total em que uma consulta permaneceu em espera para atender à limitação de taxa das solicitações PUT do disco do Azure.

### DiskAzureReadMicroseconds {#diskazurereadmicroseconds}

Tempo total gasto aguardando solicitações de leitura do disco do Azure.

### DiskAzureReadRequestsCount {#diskazurereadrequestscount}

Número de solicitações de leitura do disco do Azure.

### DiskAzureReadRequestsErrors {#diskazurereadrequestserrors}

Número de erros em solicitações de leitura do disco do Azure.

### Redirecionamentos de solicitações de leitura em disco do Azure {#diskazurereadrequestsredirects}

Número de redirecionamentos de solicitações de leitura em disco do Azure.

### DiskAzureReadRequestsThrottling {#diskazurereadrequeststhrottling}

Número de solicitações de leitura do disco do Azure limitadas por taxa.

### DiskAzureStageBlock {#diskazurestageblock}

Número de chamadas à API StageBlock do armazenamento de blobs do Disk Azure

### DiskAzureUpload {#diskazureupload}

Número de chamadas de upload da API de armazenamento de blobs do Disk Azure

### DiskAzureWriteMicroseconds {#diskazurewritemicroseconds}

Tempo total gasto aguardando solicitações de gravação no disco do Azure.

### DiskAzureWriteRequestsCount {#diskazurewriterequestscount}

Número de solicitações de gravação no disco do Azure.

### DiskAzureWriteRequestsErrors {#diskazurewriterequestserrors}

Número de erros em solicitações de gravação no disco do Azure.

### DiskAzureWriteRequestsRedirects {#diskazurewriterequestsredirects}

Número de redirecionamentos de solicitações de gravação em discos do Azure.

### DiskAzureWriteRequestsThrottling {#diskazurewriterequeststhrottling}

Número de solicitações de gravação em disco do Azure que sofreram limitação de taxa.

### DiskConnectionsCreated {#diskconnectionscreated}

Número de conexões de disco criadas

### DiskConnectionsElapsedMicroseconds {#diskconnectionselapsedmicroseconds}

Tempo total gasto na criação de conexões com o disco

### DiskConnectionsErrors {#diskconnectionserrors}

Número de casos em que a criação de uma conexão para o disco falhou

### DiskConnectionsExpired {#diskconnectionsexpired}

Número de conexões expiradas do disco

### DiskConnectionsPreserved {#diskconnectionspreserved}

Número de conexões preservadas do disco

### DiskConnectionsReset {#diskconnectionsreset}

Número de conexões com o disco que foram reiniciadas

### DiskConnectionsReused {#diskconnectionsreused}

Número de conexões reutilizadas do disco

### DiskObjectStorageWaitBlobRemovalMicroseconds {#diskobjectstoragewaitblobremovalmicroseconds}

Tempo gasto aguardando a remoção pendente de blob após confirmar a transação de metadados

### DiskPlainRewritableAzureDirectoryCreated {#diskplainrewritableazuredirectorycreated}

Número de diretórios criados pelo armazenamento de metadados &#39;plain&#95;rewritable&#39; no AzureObjectStorage.

### DiskPlainRewritableAzureDirectoryRemoved {#diskplainrewritableazuredirectoryremoved}

Número de diretórios removidos pelo armazenamento de metadados &#39;plain&#95;rewritable&#39; no AzureObjectStorage.

### DiskPlainRewritableLegacyLayoutDiskCount {#diskplainrewritablelegacylayoutdiskcount}

Número de discos &#39;plain&#95;rewritable&#39; com layout legado.

### DiskPlainRewritableLocalDirectoryCreated {#diskplainrewritablelocaldirectorycreated}

Número de diretórios criados pelo armazenamento de metadados &#39;plain&#95;rewritable&#39; para o LocalObjectStorage.

### DiskPlainRewritableLocalDirectoryRemoved {#diskplainrewritablelocaldirectoryremoved}

Número de diretórios removidos pelo armazenamento de metadados &#39;plain&#95;rewritable&#39; do LocalObjectStorage.

### DiskPlainRewritableS3DirectoryCreated {#diskplainrewritables3directorycreated}

Número de diretórios criados pelo armazenamento de metadados &#39;plain&#95;rewritable&#39; para o S3ObjectStorage.

### DiskPlainRewritableS3DirectoryRemoved {#diskplainrewritables3directoryremoved}

Número de diretórios removidos pelo armazenamento de metadados &#39;plain&#95;rewritable&#39; no S3ObjectStorage.

### DiskPlainRewritableUndoStageRetries {#diskplainrewritableundostageretries}

Número de vezes que um passo da reversão de uma transação de metadados &#39;plain&#95;rewritable&#39; com falha precisou ser repetido porque o armazenamento de objetos o rejeitou.

### DiskReadElapsedMicroseconds {#diskreadelapsedmicroseconds}

Tempo total gasto aguardando a syscall de leitura. Isso inclui leituras do page cache.

### DiskS3AbortMultipartUpload {#disks3abortmultipartupload}

Número de chamadas da API DiskS3 para AbortMultipartUpload.

### DiskS3CompleteMultipartUpload {#disks3completemultipartupload}

Número de chamadas à API CompleteMultipartUpload do DiskS3.

### DiskS3CopyObject {#disks3copyobject}

Número de chamadas CopyObject da API DiskS3.

### DiskS3CreateMultipartUpload {#disks3createmultipartupload}

Número de chamadas à API DiskS3 CreateMultipartUpload.

### DiskS3DeleteObjects {#disks3deleteobjects}

Número de chamadas DeleteObject da API DiskS3.

### DiskS3GetObject {#disks3getobject}

Número de chamadas à API GetObject do DiskS3.

### DiskS3GetObjectTagging {#disks3getobjecttagging}

Número de chamadas GetObjectTagging da API do DiskS3.

### DiskS3GetRequestThrottlerBlocked {#disks3getrequestthrottlerblocked}

Número de requisições GET e SELECT do DiskS3 bloqueadas pelo throttler.

### DiskS3GetRequestThrottlerCount {#disks3getrequestthrottlercount}

Número de solicitações GET e SELECT do DiskS3 que passaram pelo throttler: bloqueadas e não bloqueadas.

### DiskS3GetRequestThrottlerSleepMicroseconds {#disks3getrequestthrottlersleepmicroseconds}

Tempo total em que uma consulta ficou aguardando para atender à limitação de taxa das solicitações GET e SELECT do DiskS3.

### DiskS3HeadObject {#disks3headobject}

Número de chamadas HeadObject da API do DiskS3.

### DiskS3ListObjects {#disks3listobjects}

Número de chamadas à API ListObjects do DiskS3.

### DiskS3PutObject {#disks3putobject}

Número de chamadas PutObject da API do DiskS3.

### DiskS3PutRequestThrottlerBlocked {#disks3putrequestthrottlerblocked}

Número de solicitações PUT, COPY, POST e LIST do DiskS3 bloqueadas pelo throttler.

### DiskS3PutRequestThrottlerCount {#disks3putrequestthrottlercount}

Número de solicitações PUT, COPY, POST e LIST do DiskS3 que passaram pelo throttler: bloqueadas e não bloqueadas.

### DiskS3PutRequestThrottlerSleepMicroseconds {#disks3putrequestthrottlersleepmicroseconds}

Tempo total em que uma consulta ficou aguardando para se adequar à limitação de taxa das requisições PUT, COPY, POST e LIST do DiskS3.

### DiskS3ReadMicroseconds {#disks3readmicroseconds}

Tempo das solicitações GET e HEAD no armazenamento DiskS3.

### DiskS3ReadRequestAttempts {#disks3readrequestattempts}

Número de tentativas de solicitações GET e HEAD para o armazenamento DiskS3, incluindo a tentativa inicial e quaisquer retentativas, mas excluindo as retentativas executadas internamente pela estratégia de retry do S3

### DiskS3ReadRequestRetryableErrors {#disks3readrequestretryableerrors}

Número de erros com possibilidade de nova tentativa em solicitações GET e HEAD para o armazenamento DiskS3, excluindo as novas tentativas executadas internamente pela estratégia de retry do S3

### DiskS3ReadRequestsCount {#disks3readrequestscount}

Número de solicitações GET e HEAD para o armazenamento do DiskS3.

### DiskS3ReadRequestsErrors {#disks3readrequestserrors}

Número de erros não relacionados a throttling em solicitações GET e HEAD para o armazenamento do DiskS3.

### DiskS3ReadRequestsRedirects {#disks3readrequestsredirects}

Número de redirecionamentos em solicitações GET e HEAD para o armazenamento do DiskS3.

### DiskS3ReadRequestsThrottling {#disks3readrequeststhrottling}

Número de erros 429 e 503 nas requisições GET e HEAD ao armazenamento DiskS3.

### DiskS3UploadPart {#disks3uploadpart}

Número de chamadas à operação UploadPart da API DiskS3.

### DiskS3UploadPartCopy {#disks3uploadpartcopy}

Número de chamadas à API DiskS3 para UploadPartCopy.

### DiskS3WriteMicroseconds {#disks3writemicroseconds}

Duração das solicitações POST, DELETE, PUT e PATCH para o armazenamento DiskS3.

### DiskS3WriteRequestAttempts {#disks3writerequestattempts}

Número de tentativas de requisições POST, DELETE, PUT e PATCH para o armazenamento DiskS3, incluindo a tentativa inicial e quaisquer retentativas, mas excluindo as retentativas realizadas internamente pela estratégia de retry

### DiskS3WriteRequestRetryableErrors {#disks3writerequestretryableerrors}

Número de erros que permitem nova tentativa em solicitações POST, DELETE, PUT e PATCH para o armazenamento do DiskS3, excluindo as novas tentativas executadas internamente pela estratégia de retry

### DiskS3WriteRequestsCount {#disks3writerequestscount}

Número de requisições POST, DELETE, PUT e PATCH para o armazenamento do DiskS3.

### DiskS3WriteRequestsErrors {#disks3writerequestserrors}

Número de erros que não são de throttling em requisições POST, DELETE, PUT e PATCH para o armazenamento DiskS3.

### DiskS3WriteRequestsRedirects {#disks3writerequestsredirects}

Número de redirecionamentos em requisições POST, DELETE, PUT e PATCH para o armazenamento DiskS3.

### DiskS3WriteRequestsThrottling {#disks3writerequeststhrottling}

Número de erros 429 e 503 em requisições POST, DELETE, PUT e PATCH ao armazenamento DiskS3.

### DiskWriteElapsedMicroseconds {#diskwriteelapsedmicroseconds}

Tempo total gasto aguardando a syscall de gravação. Isso inclui gravações no page cache.

### DistinctTransformsAbandonedDeduplication {#distincttransformsabandoneddeduplication}

Quantas transformações de desduplicação descartaram sua tabela hash e deixaram de desduplicar porque a entrada observada era quase totalmente única e um consumer mais adiante faz a desduplicação de qualquer forma: o `DISTINCT` preliminar (veja `allow_preliminary_distinct_abandoning`) e a pré-desduplicação por stream antes do preenchimento de um Set de subconsulta `IN`.

### DistinctTransformsSwitchedToPassThrough {#distincttransformsswitchedtopassthrough}

Quantas transformações preliminares de `DISTINCT` liberaram sua tabela hash e passaram a repassar todas as linhas adiante porque a memória da consulta excedeu o limiar de `DISTINCT` externo, ou porque as allocations de hashing projetadas excederiam o orçamento restante do limiar (veja `max_bytes_before_external_distinct`): nesse caso, é o `DISTINCT` final, que faz spill para o disco, que resolve as duplicatas.

### DistrCacheBackgroundWrites {#distrcachebackgroundwrites}

Evento do buffer de gravação do Distributed Cache. Número de gravações no cache executadas em uma background thread (um slot de gravação em segundo plano foi adquirido)

### DistrCacheConnectAttempts {#distrcacheconnectattempts}

Evento de conexão do Distributed Cache. Número de tentativas de conexão com o Distributed Cache

### DistrCacheConnectErrors {#distrcacheconnecterrors}

Evento de cliente do Distributed Cache. Número de falhas ao se conectar a um servidor de distributed cache antes de fazer uma solicitação (contadas uma vez por solicitação, depois que todas as tentativas de conexão se esgotam, ao contrário de DistrCacheUnsuccessfulConnectAttempts, que conta por tentativa). Um timeout ao aguardar uma conexão livre no pool também é contado aqui. Falhas ao reconectar durante a criação da solicitação são contadas em DistrCacheMakeRequestErrors

### DistrCacheConnectMicroseconds {#distrcacheconnectmicroseconds}

Evento de conexão do Distributed Cache. Tempo gasto para se conectar ao Distributed Cache

### DistrCacheEstimatedInflightBytesOnSeek {#distrcacheestimatedinflightbytesonseek}

Evento do buffer de leitura do Distributed Cache. Estimativa de bytes in-flight nos seeks que consideraram descartar a connection (compare com DistrCacheUnusedDataPacketsBytesReadRangeIdChanged)

### DistrCacheFallbackReadMicroseconds {#distrcachefallbackreadmicroseconds}

Evento do buffer de leitura do Distributed Cache. Tempo gasto na leitura a partir do buffer de fallback em vez do Distributed Cache

### DistrCacheGetClientMicroseconds {#distrcachegetclientmicroseconds}

Evento de conexão do Distributed Cache. Tempo gasto ao obter um cliente do Distributed Cache

### DistrCacheGetResponseMicroseconds {#distrcachegetresponsemicroseconds}

Evento de cliente do Distributed Cache. Tempo gasto aguardando a resposta do Distributed Cache

### DistrCacheHashRingRebuilds {#distrcachehashringrebuilds}

Evento do registro do Distributed Cache. Número de reconstruções do hash ring do Distributed Cache

### DistrCacheInlineWrites {#distrcacheinlinewrites}

Evento do buffer de gravação do Distributed Cache. Número de gravações no cache executadas de forma inline na thread chamadora (gravações de dados temporários, que não possuem gravação no armazenamento de objetos com a qual se sobrepor)

### DistrCacheLockRegistryMicroseconds {#distrcachelockregistrymicroseconds}

Evento do registro do Distributed Cache. Tempo gasto para adquirir o bloqueio do DistributedCacheRegistry

### DistrCacheMakeRequestErrors {#distrcachemakerequesterrors}

Evento de cliente do Distributed Cache. Número de erros do Distributed Cache ao fazer uma requisição

### DistrCacheNextImplMicroseconds {#distrcachenextimplmicroseconds}

Evento do buffer de leitura do Distributed Cache. Tempo gasto em ReadBufferFromDistributedCache::nextImpl

### DistrCacheObjectStorageWriteBytes {#distrcacheobjectstoragewritebytes}

Evento do buffer de gravação do Distributed Cache. Número de bytes gravados no armazenamento de objetos

### DistrCacheObjectStorageWriteMicroseconds {#distrcacheobjectstoragewritemicroseconds}

Evento do buffer de gravação do Distributed Cache. Tempo gasto na gravação em armazenamento de objetos

### DistrCacheOpenedConnections {#distrcacheopenedconnections}

Evento de conexão do Distributed Cache. Número de conexões abertas com o Distributed Cache

### DistrCacheOpenedConnectionsBypassingPool {#distrcacheopenedconnectionsbypassingpool}

Evento de conexão do Distributed Cache. O número de conexões abertas com o Distributed Cache sem passar pelo pool

### DistrCachePrecomputeRangesMicroseconds {#distrcacheprecomputerangesmicroseconds}

Evento do buffer de leitura do Distributed Cache. Tempo gasto para pré-calcular intervalos de leitura

### DistrCacheRangeChange {#distrcacherangechange}

Evento do buffer de leitura do Distributed Cache. Número de vezes que alteramos o intervalo de leitura devido a uma mudança em seek/last&#95;position

### DistrCacheRangeResetBackward {#distrcacherangeresetbackward}

Evento do buffer de leitura do Distributed Cache. Número de vezes em que redefinimos o intervalo de leitura devido a uma alteração em `seek`/`last_position`

### DistrCacheRangeResetForward {#distrcacherangeresetforward}

Evento do buffer de leitura do Distributed Cache. Número de vezes em que o intervalo de leitura foi redefinido devido a uma alteração em seek/last&#95;position

### DistrCacheReadBytesFromFallbackBuffer {#distrcachereadbytesfromfallbackbuffer}

Evento do buffer de leitura do Distributed Cache. Bytes lidos do buffer de fallback

### DistrCacheReadBytesSkippedOnSeek {#distrcachereadbytesskippedonseek}

Evento do buffer de leitura do Distributed Cache. Bytes in-flight descartados em um seek para frente a fim de reutilizar o stream aberto

### DistrCacheReadBytesUnconsumedOnEndRequest {#distrcachereadbytesunconsumedonendrequest}

Evento do buffer de leitura do Distributed Cache. Bytes do intervalo de leitura solicitado que não foram consumidos quando a request foi encerrada antecipadamente

### DistrCacheReadErrors {#distrcachereaderrors}

Evento de cliente do Distributed Cache. Número total de erros no Distributed Cache durante solicitações de leitura (igual a DistrCacheReadErrorsRetriable + DistrCacheReadErrorsNonRetriable)

### DistrCacheReadErrorsNonRetriable {#distrcachereaderrorsnonretriable}

Evento do cliente do Distributed Cache. Número de erros não recuperáveis do Distributed Cache durante solicitações de leitura

### DistrCacheReadErrorsRetriable {#distrcachereaderrorsretriable}

Evento de cliente do Distributed Cache. Número de erros do Distributed Cache passíveis de retentativa durante solicitações de leitura (contados a cada ocorrência, incluindo aqueles recuperados por uma retentativa)

### DistrCacheReadMicroseconds {#distrcachereadmicroseconds}

Evento do buffer de leitura do Distributed Cache. Tempo gasto na leitura do distributed cache

### DistrCacheReadRequestRetries {#distrcachereadrequestretries}

Evento do buffer de leitura do Distributed Cache. Número de retentativas de solicitação de leitura após um erro passível de retentativa (incluindo retentativas da criação inicial da solicitação)

### DistrCacheReadRequestRetriesExhausted {#distrcachereadrequestretriesexhausted}

Evento de buffer de leitura do Distributed Cache. Número de vezes em que um erro de leitura com retentativa não foi concluído com sucesso dentro do limite de retentativas da solicitação de leitura

### DistrCacheReadThrottlerBytes {#distrcachereadthrottlerbytes}

Bytes que passaram pelo throttler &#39;max&#95;distributed&#95;cache&#95;read&#95;bandwidth&#95;for&#95;server&#39;.

### DistrCacheReadThrottlerSleepMicroseconds {#distrcachereadthrottlersleepmicroseconds}

Tempo total em que uma consulta permaneceu em espera para se adequar ao throttling de &#39;max&#95;distributed&#95;cache&#95;read&#95;bandwidth&#95;for&#95;server&#39;.

### DistrCacheReceivedCredentialsRefreshPackets {#distrcachereceivedcredentialsrefreshpackets}

Evento do cliente do Distributed Cache. Número total de pacotes RefreshCredentials recebidos do Distributed Cache

### DistrCacheReceivedDataPackets {#distrcachereceiveddatapackets}

Evento do cliente do Distributed Cache. Número total de pacotes de dados recebidos do Distributed Cache

### DistrCacheReceivedDataPacketsBytes {#distrcachereceiveddatapacketsbytes}

Evento de cliente do Distributed Cache. O número de bytes nos pacotes Data recebidos do Distributed Cache

### DistrCacheReceivedErrorPackets {#distrcachereceivederrorpackets}

Evento de cliente do Distributed Cache. Número total de pacotes de erro recebidos do Distributed Cache

### DistrCacheReceivedOkPackets {#distrcachereceivedokpackets}

Evento do cliente do Distributed Cache. Número total de pacotes Ok recebidos do Distributed Cache

### DistrCacheReceivedSlowDownPackets {#distrcachereceivedslowdownpackets}

Evento de cliente do Distributed Cache. Número total de packets SlowDown recebidos do distributed cache (o servidor rejeitou uma nova connection porque este cliente tem connections abertas em excesso em relação ao seu peso)

### DistrCacheReceivedStopPackets {#distrcachereceivedstoppackets}

Evento do cliente do Distributed Cache. Número total de pacotes Stop recebidos do Distributed Cache

### DistrCacheReceiveResponseErrors {#distrcachereceiveresponseerrors}

Evento de cliente do Distributed Cache. Número de erros do Distributed Cache ao receber a resposta de uma solicitação

### DistrCacheRegistryUpdateMicroseconds {#distrcacheregistryupdatemicroseconds}

Evento do registro do Distributed Cache. Tempo gasto na atualização do registro do Distributed Cache

### DistrCacheRegistryUpdates {#distrcacheregistryupdates}

Evento do registro do Distributed Cache. Número de atualizações do registro do Distributed Cache

### DistrCacheRemoveOutdatedMicroseconds {#distrcacheremoveoutdatedmicroseconds}

Evento de conexão do Distributed Cache. Tempo gasto na remoção de conexões desatualizadas do pool

### DistrCacheReusedConnections {#distrcachereusedconnections}

Evento de conexão do Distributed Cache. O número de conexões reutilizadas para o Distributed Cache

### DistrCacheSeekBufferedBytesShortfall {#distrcacheseekbufferedbytesshortfall}

Evento do buffer de leitura do Distributed Cache. O número de bytes que faltaram aos dados armazenados em buffer localmente para cobrir o gap quando um salto foi recusado

### DistrCacheSeekCancelledRequests {#distrcacheseekcancelledrequests}

Evento do buffer de leitura do Distributed Cache. O número de vezes que um seek cancelou a request aberta em vez de reutilizar o stream

### DistrCacheSeekReRequestFirstPacketMicroseconds {#distrcacheseekrerequestfirstpacketmicroseconds}

Evento do buffer de leitura do Distributed Cache. Tempo entre a solicitação de um novo intervalo de leitura no stream aberto em decorrência de um seek e o recebimento de seu primeiro packet

### DistrCacheSeekReRequests {#distrcacheseekrerequests}

Distributed Cache read buffer event. Número de vezes que um seek solicitou um novo intervalo de leitura na mesma connection

### DistrCacheSeekSkipDeclinedGapNotBuffered {#distrcacheseekskipdeclinedgapnotbuffered}

Evento do buffer de leitura do Distributed Cache. O número de seeks para frente que não puderam ser ignorados porque o gap não estava armazenado em buffer localmente

### DistrCacheSeekSkips {#distrcacheseekskips}

Evento do buffer de leitura do Distributed Cache. O número de seeks para frente que ignoraram o gap no stream aberto

### DistrCacheSentDataPackets {#distrcachesentdatapackets}

Evento do cliente do Distributed Cache. Número total de pacotes de dados enviados ao Distributed Cache

### DistrCacheSentDataPacketsBytes {#distrcachesentdatapacketsbytes}

Evento de cliente do Distributed Cache. O número de bytes nos pacotes Data enviados ao Distributed Cache

### DistrCacheServerAckRequestPackets {#distrcacheserverackrequestpackets}

Evento do servidor do Distributed Cache. Número de pacotes AckRequest em DistributedCacheServer

### DistrCacheServerCachedReadBufferCacheHits {#distrcacheservercachedreadbuffercachehits}

Evento do servidor Distributed Cache. Número de vezes em que o Distributed Cache encontrou dados no cache ao ler do cache do sistema de arquivos

### DistrCacheServerCachedReadBufferCacheMisses {#distrcacheservercachedreadbuffercachemisses}

Evento do servidor Distributed Cache. Número de vezes em que o Distributed Cache não encontrou dados no cache ao ler do cache do sistema de arquivos

### DistrCacheServerCachedReadBufferCachePredownloadBytes {#distrcacheservercachedreadbuffercachepredownloadbytes}

Evento do servidor do Distributed Cache. Número de bytes lidos do armazenamento de objetos para pré-download no Distributed Cache durante a leitura do cache do sistema de arquivos

### DistrCacheServerCachedReadBufferCacheReadBytes {#distrcacheservercachedreadbuffercachereadbytes}

Evento do servidor do Distributed Cache. O número de bytes lidos do cache do Distributed Cache durante a leitura do cache do sistema de arquivos

### DistrCacheServerCachedReadBufferCacheWrittenBytes {#distrcacheservercachedreadbuffercachewrittenbytes}

Evento do servidor Distributed Cache. Número de bytes gravados no cache do distributed cache durante a leitura do cache do sistema de arquivos

### DistrCacheServerCachedReadBufferObjectStorageReadBytes {#distrcacheservercachedreadbufferobjectstoragereadbytes}

Evento do servidor Distributed Cache. Número de bytes lidos do armazenamento de objetos no Distributed Cache ao ler do cache do sistema de arquivos

### DistrCacheServerConnectionRegistryClientLockWaitMicroseconds {#distrcacheserverconnectionregistryclientlockwaitmicroseconds}

Evento do servidor do Distributed Cache. Tempo gasto aguardando o mutex do bucket de connection por cliente em caso de contention (register/unregister/identify/eviction)

### DistrCacheServerConnectionRegistryLockWaitMicroseconds {#distrcacheserverconnectionregistrylockwaitmicroseconds}

Evento do servidor do Distributed Cache. Tempo gasto aguardando o mutex do registry global de conexões em caso de contenção (identify, setClientWeight, eviction e unregister quando uma reindexação ou remoção de bucket é necessária)

### DistrCacheServerConnectionsDroppedOverLimit {#distrcacheserverconnectionsdroppedoverlimit}

Evento do servidor do distributed cache. O número de conexões encerradas porque o número de conexões excedeu o limite (encerradas no cliente com o maior número de conexões em relação ao seu peso)

### DistrCacheServerConnectionsRejectedOverLimit {#distrcacheserverconnectionsrejectedoverlimit}

Evento do servidor do Distributed Cache. O número de novas conexões rejeitadas com um packet SlowDown porque o cliente que tentou se conectar excedeu sua cota de conexões

### DistrCacheServerContinueRequestPackets {#distrcacheservercontinuerequestpackets}

Evento do servidor da Distributed Cache. Número de pacotes ContinueRequest no DistributedCacheServer

### DistrCacheServerCredentialsRefresh {#distrcacheservercredentialsrefresh}

Evento do servidor Distributed Cache. Número de credenciais expiradas renovadas

### DistrCacheServerEndRequestPackets {#distrcacheserverendrequestpackets}

Evento do servidor de Distributed Cache. Número de pacotes EndRequest no DistributedCacheServer

### DistrCacheServerNewS3CachedClients {#distrcacheservernews3cachedclients}

Evento do servidor Distributed Cache. O número de novos clientes S3 em cache

### DistrCacheServerProcessRequestMicroseconds {#distrcacheserverprocessrequestmicroseconds}

Evento do servidor Distributed Cache. Tempo gasto no processamento da solicitação no lado do servidor DistributedCache

### DistrCacheServerReceivedCredentialsRefreshPackets {#distrcacheserverreceivedcredentialsrefreshpackets}

Evento do servidor Distributed Cache. Número de pacotes RefreshCredentials do cliente no DistributedCacheServer

### DistrCacheServerReusedS3CachedClients {#distrcacheserverreuseds3cachedclients}

Evento do servidor do Distributed Cache. O número de clientes S3 em cache reutilizados

### DistrCacheServerShutdownConnectionsMicroseconds {#distrcacheservershutdownconnectionsmicroseconds}

Evento do servidor do Distributed Cache. Tempo gasto em shutdownAllConnections aguardando a conclusão das conexões ativas (ou seu fechamento forçado) durante o desligamento do servidor

### DistrCacheServerSkipped {#distrcacheserverskipped}

Evento do servidor do Distributed Cache. Número de vezes em que o servidor do Distributed Cache foi ignorado devido a tentativas anteriores de conexão malsucedidas

### DistrCacheServerStartRequestPackets {#distrcacheserverstartrequestpackets}

Evento do servidor de Distributed Cache. Número de pacotes StartRequest no DistributedCacheServer

### DistrCacheServerSwitches {#distrcacheserverswitches}

Evento do buffer de leitura do Distributed Cache. Número de trocas de servidor entre servidores do Distributed Cache no cache read/write-through

### DistrCacheServerUpdates {#distrcacheserverupdates}

Evento do Distributed Cache. O número de trocas de servidor durante a leitura ou a gravação porque o servidor escolhido pelo hash mudou (por exemplo, devido ao desregistro do servidor)

### DistrCacheSkippedWrites {#distrcacheskippedwrites}

Evento do buffer de gravação do Distributed Cache. Número de write buffers que ignoraram a gravação no write-through cache porque nenhum slot de gravação em segundo plano estava livre (o limite definido pela configuração de servidor `distributed_cache_write_pool_size` foi atingido) ou porque não foi possível iniciar uma gravação em segundo plano. Os dados ainda assim são gravados no armazenamento de objetos

### DistrCacheStaleReconnections {#distrcachestalereconnections}

Evento de conexão do Distributed Cache. O número de reconexões devido a conexões do pool obsoletas (fechadas pela contraparte)

### DistrCacheStartRangeMicroseconds {#distrcachestartrangemicroseconds}

Evento do buffer de leitura do Distributed Cache. Tempo gasto para iniciar um novo intervalo de leitura com o Distributed Cache

### DistrCacheSuccessfulConnectAttempts {#distrcachesuccessfulconnectattempts}

Evento de conexão do Distributed Cache. Número de tentativas de conexão bem-sucedidas com o Distributed Cache

### DistrCacheSuccessfulRegistryUpdates {#distrcachesuccessfulregistryupdates}

Evento de registro do Distributed Cache. O número de atualizações bem-sucedidas no registro de servidores

### DistrCacheSystemTablesErrors {#distrcachesystemtableserrors}

Evento de cliente do Distributed Cache. Número de erros do distributed cache durante solicitações às tabelas de sistema de introspecção (`system.distributed_cache`, `system.distributed_cache_metrics`, `system.distributed_cache_events`). Essas solicitações não têm nova tentativa, portanto erros recuperáveis e não recuperáveis são contabilizados em conjunto

### DistrCacheTemporaryFilesBytesWritten {#distrcachetemporaryfilesbyteswritten}

Evento de conexão do Distributed Cache. Número de bytes gravados nos arquivos temporários criados no Distributed Cache

### DistrCacheTemporaryFilesCreated {#distrcachetemporaryfilescreated}

Evento de conexão do Distributed Cache. Número de arquivos temporários criados no Distributed Cache

### DistrCacheUnsuccessfulConnectAttempts {#distrcacheunsuccessfulconnectattempts}

Evento de conexão do Distributed Cache. Número de tentativas de conexão malsucedidas com o Distributed Cache

### DistrCacheUnsuccessfulRegistryUpdates {#distrcacheunsuccessfulregistryupdates}

Evento do registro do Distributed Cache. O número de atualizações malsucedidas no registro de servidores

### DistrCacheUnusedDataPacketsBytes {#distrcacheunuseddatapacketsbytes}

Evento de cliente do Distributed Cache. Número de bytes nos pacotes Data que foram ignorados

### DistrCacheUnusedDataPacketsBytesReadRangeIdChanged {#distrcacheunuseddatapacketsbytesreadrangeidchanged}

Evento de cliente do Distributed Cache. O número de bytes em pacotes Data ignorados (parte de DistrCacheUnusedDataPacketsBytes) que chegaram para a requisição monitorada, mas com um id de read range mais antigo (um read range mais recente foi solicitado na mesma requisição)

### DistrCacheUnusedDataPacketsBytesUnknownRequest {#distrcacheunuseddatapacketsbytesunknownrequest}

Distributed Cache client event. O número de bytes em pacotes Data ignorados (parte de DistrCacheUnusedDataPacketsBytes) que chegaram referentes a uma requisição que o cliente não rastreia mais (por exemplo, a leitura foi interrompida via EndRequest depois que o servidor já havia enviado mais dados)

### DistrCacheUnusedPackets {#distrcacheunusedpackets}

Evento de cliente do Distributed Cache. Número de pacotes não utilizados ignorados do Distributed Cache

### DistrCacheUnusedPacketsBufferAllocations {#distrcacheunusedpacketsbufferallocations}

Evento de cliente do Distributed Cache. Número de alocações adicionais de buffer quando não foi possível reutilizar o buffer existente

### DistrCacheWriteBytes {#distrcachewritebytes}

Evento do buffer de gravação do Distributed Cache. Quantidade de bytes gravados no Distributed Cache

### DistrCacheWriteErrors {#distrcachewriteerrors}

Evento de cliente do Distributed Cache. Número total de erros do Distributed Cache durante solicitações de gravação (igual a DistrCacheWriteErrorsRetriable + DistrCacheWriteErrorsNonRetriable). Um `Stop` de cache write-through iniciado pelo servidor é controle de fluxo, não um erro, e não é contabilizado

### DistrCacheWriteErrorsNonRetriable {#distrcachewriteerrorsnonretriable}

Evento do cliente do Distributed Cache. Número de erros do Distributed Cache que não permitem nova tentativa durante requisições de escrita

### DistrCacheWriteErrorsRetriable {#distrcachewriteerrorsretriable}

Evento do cliente do Distributed Cache. Número de erros recuperáveis do distributed cache durante solicitações de escrita (contados a cada ocorrência, incluindo os recuperados por nova tentativa)

### DistrCacheWriteMicroseconds {#distrcachewritemicroseconds}

Evento do buffer de gravação do Distributed Cache. Tempo gasto em WriteBufferFromDistributedCache::writeToFileSegment

### DistrCacheWriteRequestRetries {#distrcachewriterequestretries}

Evento do buffer de gravação do Distributed Cache. O número de novas tentativas de solicitação de gravação após um erro recuperável

### DistrCacheWriteRequestRetriesExhausted {#distrcachewriterequestretriesexhausted}

Evento do buffer de gravação do Distributed Cache. Número de vezes que um erro de gravação passível de nova tentativa não pôde ser resolvido dentro do limite de tentativas da solicitação de gravação

### DistrCacheWriteThrottlerBytes {#distrcachewritethrottlerbytes}

Bytes que passaram pelo throttler &#39;max&#95;distributed&#95;cache&#95;write&#95;bandwidth&#95;for&#95;server&#39;.

### DistrCacheWriteThrottlerSleepMicroseconds {#distrcachewritethrottlersleepmicroseconds}

Tempo total em que uma consulta permaneceu em espera para se adequar à limitação de taxa de &#39;max&#95;distributed&#95;cache&#95;write&#95;bandwidth&#95;for&#95;server&#39;.

### DistributedAsyncInsertionFailures {#distributedasyncinsertionfailures}

Número de falhas na inserção assíncrona em uma tabela distribuída (com &#39;distributed&#95;foreground&#95;insert&#39; = 0)

### DistributedConnectionConnectCount {#distributedconnectionconnectcount}

Número de conexões com outros servidores feitas durante a execução distribuída de consultas. Isso ocorre quando uma nova conexão é estabelecida em vez de reutilizar uma já existente no pool.

### DistributedConnectionFailAtAll {#distributedconnectionfailatall}

Contagem total quando a conexão distribuída falha após esgotadas todas as tentativas de repetição.

### DistributedConnectionFailTry {#distributedconnectionfailtry}

Contagem total quando a conexão distribuída falha e há nova tentativa.

### DistributedConnectionMissingTable {#distributedconnectionmissingtable}

Número de vezes em que rejeitamos uma réplica de uma consulta distribuída porque ela não continha uma tabela necessária para a consulta.

### DistributedConnectionReconnectCount {#distributedconnectionreconnectcount}

Número de reconexões com outros servidores feitas durante a execução distribuída de consultas. Isso pode acontecer quando uma conexão desatualizada é obtida do pool de conexões

### DistributedConnectionSkipReadOnlyReplica {#distributedconnectionskipreadonlyreplica}

Número de réplicas ignoradas durante o INSERT em uma tabela distribuída porque as réplicas estavam em modo somente leitura

### DistributedConnectionStaleReplica {#distributedconnectionstalereplica}

Número de vezes em que uma réplica foi rejeitada em uma consulta distribuída porque alguma tabela necessária para a consulta tinha atraso de replicação acima do limiar configurado.

### DistributedConnectionTries {#distributedconnectiontries}

Número total de tentativas de conexão distribuída.

### DistributedConnectionUsable {#distributedconnectionusable}

Número total de conexões distribuídas bem-sucedidas com um servidor utilizável (com a tabela necessária, mas possivelmente desatualizada).

### DistributedDelayedInserts {#distributeddelayedinserts}

Número de vezes em que o INSERT de um bloco em uma tabela distribuída teve sua taxa limitada devido à grande quantidade de bytes pendentes.

### DistributedDelayedInsertsMilliseconds {#distributeddelayedinsertsmilliseconds}

Número total de milissegundos gastos enquanto o INSERT de um bloco em uma tabela distribuída teve sua taxa limitada devido à grande quantidade de bytes pendentes.

### DistributedIndexAnalysisMicroseconds {#distributedindexanalysismicroseconds}

Tempo total gasto durante a análise distribuída de índice

### DistributedIndexAnalysisMissingParts {#distributedindexanalysismissingparts}

Número de partes ausentes durante a análise distribuída de índice que serão resolvidas localmente

### DistributedIndexAnalysisParts {#distributedindexanalysisparts}

Número de partes enviadas para a análise distribuída de índice

### DistributedIndexAnalysisReplicaFallback {#distributedindexanalysisreplicafallback}

Número de vezes em que a análise de índice distribuída falhou em uma das réplicas, com fallback para a réplica local

### DistributedIndexAnalysisReplicaUnavailable {#distributedindexanalysisreplicaunavailable}

Número de vezes em que a análise distribuída de índice falhou em uma das réplicas, sem fallback (falha ao conectar)

### DistributedIndexAnalysisScheduledReplicas {#distributedindexanalysisscheduledreplicas}

Número de réplicas (a réplica local é contabilizada uma vez) para as quais a análise distribuída de índice foi agendada

### DistributedPlanHostsUsed {#distributedplanhostsused}

Número de hosts distintos aos quais foi atribuída pelo menos uma tarefa durante a execução de uma consulta com `make_distributed_plan`.

### DistributedPlanLocalExecution {#distributedplanlocalexecution}

Definido como 1 quando uma consulta `make_distributed_plan` foi executada no processo local por meio do executor local (`distributed_plan_execute_locally`), em vez de ser encaminhada para workers remotos.

### DistributedPlanRemoteTasks {#distributedplanremotetasks}

Número de tarefas enviadas para workers remotos ao executar uma consulta com `make_distributed_plan`. Um valor diferente de zero significa que a consulta foi realmente executada de forma distribuída.

### DistributedPlanWorkerPartsPruned {#distributedplanworkerpartspruned}

Número de data parts atribuídas que a análise de índice do próprio worker descartou, de modo que o worker não as lê. O coordinator seleciona as partes sem realizar essa análise, portanto pode atribuir uma parte com a qual nenhuma linha da consulta possa corresponder. `DistributedPlanWorkerPartsReceived` é a soma deste valor com `DistributedPlanWorkerPartsScanned`.

### DistributedPlanWorkerPartsReceived {#distributedplanworkerpartsreceived}

Número de data parts que o coordinator atribuiu a um worker de um distributed query plan, somado ao longo das leituras por bucket que uma consulta executa nesse worker. Uma parte atribuída a vários buckets é contada uma vez por leitura.

### DistributedPlanWorkerPartsScanned {#distributedplanworkerpartsscanned}

Número de data parts atribuídas que o worker mantém para leitura.

### DistributedRejectedInserts {#distributedrejectedinserts}

Número de vezes em que o INSERT de um bloco em uma tabela distribuída foi rejeitado com a exceção &#39;Too many bytes&#39; devido ao grande número de bytes pendentes.

### DistributedShardsSkipped {#distributedshardsskipped}

Número de shards ignorados silenciosamente em uma consulta a uma tabela Distributed (ou às funções de tabela remote/cluster) quando `skip_unavailable_shards` está habilitado, seja porque o shard estava indisponível, seja porque ele gerou uma exceção contemplada por `skip_unavailable_shards_mode`.

### DistributedSyncInsertionTimeoutExceeded {#distributedsyncinsertiontimeoutexceeded}

O tempo limite foi excedido ao aguardar os shards durante a inserção síncrona em uma tabela distribuída (com &#39;distributed&#95;foreground&#95;insert&#39; = 1)

### DNSError {#dnserror}

Número total de erros na resolução de DNS

### DuplicatedAsyncInserts {#duplicatedasyncinserts}

Número de async inserts no bloco inserido em uma tabela ReplicatedMergeTree que foram desduplicados.

### DuplicatedInsertedBlocks {#duplicatedinsertedblocks}

Número de inserções síncronas em uma tabela *MergeTree que foram deduplicadas.

### DuplicationDataHashComputations {#duplicationdatahashcomputations}

Número de cálculos de hash de dados por coluna realizados durante a desduplicação de blocos inseridos com INSERT em tabelas *MergeTree (cache misses no hash de desduplicação unificado).

### DuplicationElapsedMicroseconds {#duplicationelapsedmicroseconds}

Tempo total gasto na verificação de duplicação de blocos inseridos em tabelas *MergeTree.

### EncryptionHeaderCacheHits {#encryptionheadercachehits}

Número de vezes em que os bytes do cabeçalho de criptografia foram encontrados no cache de cabeçalhos de criptografia, dispensando a leitura do cabeçalho na origem.

### EncryptionHeaderCacheMisses {#encryptionheadercachemisses}

Número de vezes em que os bytes do cabeçalho de criptografia não foram encontrados no cache de cabeçalho de criptografia e, portanto, precisaram ser lidos da origem.

### EngineFileLikeReadFiles {#enginefilelikereadfiles}

Número de arquivos lidos em motores de tabela que operam com arquivos (como File/S3/URL/HDFS).

### ExecutableUserDefinedFunctionElapsedMicroseconds {#executableuserdefinedfunctionelapsedmicroseconds}

Tempo de relógio gasto na execução de funções definidas pelo usuário do tipo executable e executable&#95;pool, em microssegundos.

### ExecutableUserDefinedFunctionInputBytes {#executableuserdefinedfunctioninputbytes}

Total de bytes gravados no stdin dos processos filhos de funções definidas pelo usuário do tipo executable e executable&#95;pool.

### ExecutableUserDefinedFunctionInvocations {#executableuserdefinedfunctioninvocations}

Número de invocações de funções definidas pelo usuário dos tipos `executable` e `executable_pool`.

### ExecutableUserDefinedFunctionOutputBytes {#executableuserdefinedfunctionoutputbytes}

Total de bytes lidos da stdout dos processos filhos de funções definidas pelo usuário dos tipos executable e executable&#95;pool.

### ExecutableUserDefinedFunctionPeakMemoryByteSeconds {#executableuserdefinedfunctionpeakmemorybyteseconds}

Pico de memória por processo consumido pelos processos filhos de funções definidas pelo usuário dos tipos executable e executable&#95;pool, integrado ao longo do tempo de relógio, em bytes-segundo.

### ExecutableUserDefinedFunctionPoolWaitMicroseconds {#executableuserdefinedfunctionpoolwaitmicroseconds}

Tempo gasto esperando em tryBorrowObject quando o pool da função definida pelo usuário executable&#95;pool está esgotado, em microssegundos.

### ExecutableUserDefinedFunctionSystemTimeMicroseconds {#executableuserdefinedfunctionsystemtimemicroseconds}

Tempo de CPU em modo kernel consumido pelos processos filhos de funções definidas pelo usuário do tipo executable e de executable&#95;pool, em microssegundos.

### ExecutableUserDefinedFunctionUserTimeMicroseconds {#executableuserdefinedfunctionusertimemicroseconds}

Tempo de CPU no modo de usuário consumido pelos processos filhos de funções definidas pelo usuário dos tipos executable e executable&#95;pool, em microssegundos.

### ExecuteShellCommand {#executeshellcommand}

Número de execuções de comandos no shell.

### ExternalAggregationCompressedBytes {#externalaggregationcompressedbytes}

Número de bytes gravados no disco para agregação em memória externa.

### ExternalAggregationMerge {#externalaggregationmerge}

Número de vezes em que arquivos temporários foram mesclados para agregação em memória externa.

### ExternalAggregationUncompressedBytes {#externalaggregationuncompressedbytes}

Quantidade de dados não compactados (antes da compressão) gravados em disco para agregação em memória externa.

### ExternalAggregationWritePart {#externalaggregationwritepart}

Número de vezes que um arquivo temporário foi escrito no disco para agregação em memória externa.

### ExternalDataSourceLocalCacheReadBytes {#externaldatasourcelocalcachereadbytes}

Bytes lidos do buffer do cache local no RemoteReadBufferCache

### ExternalDistinctCompressedBytes {#externaldistinctcompressedbytes}

Número de bytes comprimidos gravados para DISTINCT em memória externa.

### ExternalDistinctMerge {#externaldistinctmerge}

Número de vezes que arquivos temporários foram mesclados para DISTINCT em memória externa.

### ExternalDistinctUncompressedBytes {#externaldistinctuncompressedbytes}

Quantidade de dados (não comprimidos, antes da compressão) gravados para DISTINCT em memória externa.

### ExternalDistinctWritePart {#externaldistinctwritepart}

Número de vezes que um arquivo temporário foi gravado em disco para DISTINCT em memória externa.

### ExternalJoinCompressedBytes {#externaljoincompressedbytes}

Número de bytes comprimidos gravados para JOIN em memória externa.

### ExternalJoinMerge {#externaljoinmerge}

Número de vezes em que arquivos temporários foram mesclados para operações de JOIN em memória externa.

### ExternalJoinUncompressedBytes {#externaljoinuncompressedbytes}

Quantidade de dados (não compactados, antes da compressão) gravados para JOIN na memória externa.

### ExternalJoinWritePart {#externaljoinwritepart}

Número de vezes que um arquivo temporário foi gravado no disco para JOIN em memória externa.

### ExternalProcessingCompressedBytesTotal {#externalprocessingcompressedbytestotal}

Número de bytes compactados gravados pelo processamento externo (ordenação/agregação/join)

### Total de arquivos usados no processamento externo {#externalprocessingfilestotal}

Número de arquivos usados no processamento externo (ordenação/agregação/join)

### ExternalProcessingUncompressedBytesTotal {#externalprocessinguncompressedbytestotal}

Quantidade de dados (não comprimidos, antes da compressão) gravados pelo processamento externo (ordenação/agregação/JOIN)

### ExternalSortCompressedBytes {#externalsortcompressedbytes}

Número de bytes comprimidos gravados para ordenação em memória externa.

### ExternalSortMerge {#externalsortmerge}

Número de vezes que arquivos temporários foram mesclados durante a ordenação em memória externa.

### ExternalSortUncompressedBytes {#externalsortuncompressedbytes}

Quantidade de dados (sem compactação, antes da compactação) gravados durante a ordenação em memória externa.

### ExternalSortWritePart {#externalsortwritepart}

Número de vezes em que um arquivo temporário foi gravado em disco durante a ordenação com memória externa.

### FailedAsyncInsertQuery {#failedasyncinsertquery}

Número de consultas ASYNC INSERT que falharam.

### FailedInitialQuery {#failedinitialquery}

Número de consultas iniciais com falha.

### FailedInitialSelectQuery {#failedinitialselectquery}

O mesmo que FailedInitialQuery, mas apenas para consultas SELECT.

### FailedInsertQuery {#failedinsertquery}

Igual a FailedQuery, mas apenas para consultas do tipo INSERT.

### FailedInternalInsertQuery {#failedinternalinsertquery}

O mesmo que FailedInternalQuery, mas apenas para consultas INSERT.

### FailedInternalQuery {#failedinternalquery}

Número de consultas internas que falharam.

### FailedInternalSelectQuery {#failedinternalselectquery}

O mesmo que FailedInternalQuery, mas apenas para consultas SELECT.

### FailedQuery {#failedquery}

Número total de consultas que falharam, tanto internas quanto de usuários.

### FailedSelectQuery {#failedselectquery}

O mesmo que FailedQuery, mas apenas para consultas SELECT.

### FetchBackgroundExecutorTaskCancelMicroseconds {#fetchbackgroundexecutortaskcancelmicroseconds}

Tempo gasto em cancel() para tarefas do executor de fetch.

### FetchBackgroundExecutorTaskExecuteStepMicroseconds {#fetchbackgroundexecutortaskexecutestepmicroseconds}

Tempo gasto em executeStep() nas tarefas do executor Fetch.

### FetchBackgroundExecutorTaskResetMicroseconds {#fetchbackgroundexecutortaskresetmicroseconds}

Tempo gasto ao redefinir a tarefa do executor Fetch.

### FetchBackgroundExecutorWaitMicroseconds {#fetchbackgroundexecutorwaitmicroseconds}

Tempo de espera pela conclusão no executor Fetch.

### FiberStackAllocBytes {#fiberstackallocbytes}

Número de bytes alocados para pilhas de fibras, incluindo guard pages (as guard pages são usadas apenas em builds de debug e com sanitizer).

### FiberStackAllocNanoseconds {#fiberstackallocnanoseconds}

Tempo gasto na alocação de pilhas de fibras. Nanossegundos são usados porque uma única alocação normalmente é mais rápida que um microssegundo, a menos que haja contenção no allocator ou no kernel.

### FiberStackAllocs {#fiberstackallocs}

Número de pilhas alocadas para fibras (fibras são usadas na comunicação assíncrona com réplicas remotas).

### FiberStackFreeNanoseconds {#fiberstackfreenanoseconds}

Tempo gasto na desalocação de pilhas de fibras.

### FileOpen {#fileopen}

Número de arquivos abertos.

### Invocações do callback FileProgress {#fileprogresscallbackinvocations}

Número de vezes que o `FileProgressCallback` por consulta foi invocado (conta cada evento `FileProgress` por arquivo entregue ao cliente via TCP nativo ou por `clickhouse-local`).

### FileSegmentCompleteMicroseconds {#filesegmentcompletemicroseconds}

Duração de FileSegment::complete() no cache do sistema de arquivos

### FileSegmentFailToIncreasePriority {#filesegmentfailtoincreasepriority}

Número de vezes em que a prioridade não foi aumentada devido à alta contenção no bloqueio de cache

### FileSegmentHolderCompleteMicroseconds {#filesegmentholdercompletemicroseconds}

Tempo de execução de complete() do holder de segmentos de arquivo

### FileSegmentIncreasePriorityMicroseconds {#filesegmentincreaseprioritymicroseconds}

Tempo de aumento da prioridade do segmento de arquivo

### FileSegmentLockMicroseconds {#filesegmentlockmicroseconds}

Tempo de bloqueio do segmento de arquivo

### FileSegmentRemoveMicroseconds {#filesegmentremovemicroseconds}

Tempo de remove() do segmento de arquivo

### FileSegmentWaitMicroseconds {#filesegmentwaitmicroseconds}

Tempo de espera no estado DOWNLOADING

### FileSegmentWaitTimeouts {#filesegmentwaittimeouts}

Número de vezes que a espera por um segmento de arquivo no estado DOWNLOADING excedeu o tempo limite (consulte `filesystem_cache_wait_for_concurrent_download_timeout_milliseconds`)

### FileSegmentWriteMicroseconds {#filesegmentwritemicroseconds}

Tempo da operação write() do segmento de arquivo

### FileSync {#filesync}

Número de vezes que a função F&#95;FULLFSYNC/fsync/fdatasync foi chamada em arquivos.

### FileSyncElapsedMicroseconds {#filesyncelapsedmicroseconds}

Tempo total gasto aguardando a execução da chamada de sistema F&#95;FULLFSYNC/fsync/fdatasync em arquivos.

### FilesystemCacheBackgroundDownloadQueuePush {#filesystemcachebackgrounddownloadqueuepush}

Número de segmentos de arquivo enviados para download em segundo plano no cache do sistema de arquivos

### FilesystemCacheBackgroundEvictedBytes {#filesystemcachebackgroundevictedbytes}

Número de bytes removidos pela thread em segundo plano

### FilesystemCacheBackgroundEvictedFileSegments {#filesystemcachebackgroundevictedfilesegments}

Número de segmentos de arquivo removidos pela thread de segundo plano

### FilesystemCacheBackgroundRemovedInvalidatedEntries {#filesystemcachebackgroundremovedinvalidatedentries}

Número de entradas invalidadas (removidas de forma adiada) da fila de prioridade expurgadas pela thread de limpeza em segundo plano

### FilesystemCacheCheckCorrectness {#filesystemcachecheckcorrectness}

Número de vezes em que FileCache::assertCacheCorrectness foi chamado

### FilesystemCacheCheckCorrectnessMicroseconds {#filesystemcachecheckcorrectnessmicroseconds}

Quanto tempo o FileCache::assertCacheCorrectness leva para ser executado

### FilesystemCacheClientsMapLockWaitMicroseconds {#filesystemcacheclientsmaplockwaitmicroseconds}

Tempo gasto aguardando um bloqueio disputado do shard do map de uso por cliente (`user_id`) na política de cache de overcommit

### FilesystemCacheCreatedKeyDirectories {#filesystemcachecreatedkeydirectories}

Número de diretórios de chaves criados

### FilesystemCacheDowngradedFileSegments {#filesystemcachedowngradedfilesegments}

Número de segmentos de arquivo rebaixados (movidos) da fila protegida para a fila probatória na política de cache SLRU. Esta é uma movimentação interna no cache, não uma evicção.

### FilesystemCacheEvictedBytes {#filesystemcacheevictedbytes}

Número de bytes removidos do cache do sistema de arquivos

### FilesystemCacheEvictedFileSegments {#filesystemcacheevictedfilesegments}

Número de segmentos de arquivo removidos do cache do sistema de arquivos

### FilesystemCacheEvictionReusedIterator {#filesystemcacheevictionreusediterator}

Número de reutilizações do iterador do cache do sistema de arquivos

### FilesystemCacheEvictionSkippedEvictingFileSegments {#filesystemcacheevictionskippedevictingfilesegments}

Número de segmentos de arquivo ignorados na evicção por estarem em processo de evicção

### FilesystemCacheEvictionSkippedFileSegments {#filesystemcacheevictionskippedfilesegments}

Número de segmentos de arquivo ignorados durante a evicção por estarem em um estado que não pode ser liberado

### FilesystemCacheEvictionSkippedMovingFileSegments {#filesystemcacheevictionskippedmovingfilesegments}

Número de segmentos de arquivo ignorados na evicção por estarem em estado de moving

### FilesystemCacheEvictionTries {#filesystemcacheevictiontries}

Número de tentativas de evicção do cache do sistema de arquivos

### FilesystemCacheEvictMicroseconds {#filesystemcacheevictmicroseconds}

Tempo de evicção do cache do sistema de arquivos

### FilesystemCacheFailedEvictionCandidates {#filesystemcachefailedevictioncandidates}

Número de segmentos de arquivo que falharam inesperadamente ao serem removidos durante a evicção dinâmica do cache do sistema de arquivos

### FilesystemCacheFailedReserveAttempts {#filesystemcachefailedreserveattempts}

Número de tentativas malsucedidas de reserva de espaço no cache de arquivos.

### FilesystemCacheFailToReserveSpaceBecauseOfCacheResize {#filesystemcachefailtoreservespacebecauseofcacheresize}

Número de vezes que a reserva de espaço foi ignorada porque o cache está sendo redimensionado

### FilesystemCacheFailToReserveSpaceBecauseOfLockContention {#filesystemcachefailtoreservespacebecauseoflockcontention}

Número de vezes em que a reserva de espaço foi ignorada devido à alta contenção no bloqueio de cache

### Erros da Thread de Manutenção do Espaço Livre do Cache do Sistema de Arquivos {#filesystemcachefreespacekeepingthreaderrors}

Número de exceções capturadas pela tarefa em segundo plano de manutenção do espaço livre (coleta, remoção ou finalização)

### FilesystemCacheFreeSpaceKeepingThreadRun {#filesystemcachefreespacekeepingthreadrun}

Número de vezes que a thread em segundo plano executou a tarefa de manutenção do espaço livre

### FilesystemCacheFreeSpaceKeepingThreadWorkMilliseconds {#filesystemcachefreespacekeepingthreadworkmilliseconds}

Tempo durante o qual a thread em segundo plano executou o job de manutenção do espaço livre

### FilesystemCacheGetMicroseconds {#filesystemcachegetmicroseconds}

Tempo do `get()` no cache do sistema de arquivos

### FilesystemCacheGetOrSetMicroseconds {#filesystemcachegetorsetmicroseconds}

Tempo do `getOrSet()` no cache do sistema de arquivos

### FilesystemCacheHoldFileSegments {#filesystemcacheholdfilesegments}

Número de segmentos de arquivo no cache do sistema de arquivos que foram mantidos

### FilesystemCacheIdleClientEvictions {#filesystemcacheidleclientevictions}

Número de vezes em que todas as cache entries de um cliente ocioso de cache distribuído (`user_id`) foram purgadas por não ter ocorrido nenhum acesso dentro de `idle_client_ttl_sec`.

### FilesystemCacheInvalidatedEntriesCleanupThreadWorkMilliseconds {#filesystemcacheinvalidatedentriescleanupthreadworkmilliseconds}

Tempo em que a thread em segundo plano executou a tarefa de limpeza das entradas invalidadas da fila de prioridade

### FilesystemCacheLoadMetadataMicroseconds {#filesystemcacheloadmetadatamicroseconds}

Tempo gasto no carregamento dos metadados do cache do sistema de arquivos

### FilesystemCacheLockKeyMicroseconds {#filesystemcachelockkeymicroseconds}

Tempo da chave de bloqueio do cache

### FilesystemCacheLockMetadataMicroseconds {#filesystemcachelockmetadatamicroseconds}

Tempo de bloqueio dos metadados do cache do sistema de arquivos

### FilesystemCacheLockOriginPoolMicroseconds {#filesystemcachelockoriginpoolmicroseconds}

Tempo de bloqueio do pool compartilhado de origin do cache do sistema de arquivos

### FilesystemCacheOvercommitCandidatesIterationSteps {#filesystemcacheovercommitcandidatesiterationsteps}

Número de iterações externas que o loop de evicção do overcommit executou além da primeira passagem para atender ao déficit de uma reserva

### FilesystemCachePriorityReadLockMicroseconds {#filesystemcachepriorityreadlockmicroseconds}

Tempo de bloqueio para leitura do cache do sistema de arquivos na fila de prioridade

### FilesystemCachePriorityWriteLockMicroseconds {#filesystemcacheprioritywritelockmicroseconds}

Tempo de bloqueio do cache do sistema de arquivos ao gravar na fila de prioridade

### FilesystemCacheReserveAttempts {#filesystemcachereserveattempts}

Tentativa de reserva de espaço no cache do sistema de arquivos

### FilesystemCacheReserveMicroseconds {#filesystemcachereservemicroseconds}

Tempo de reserva de espaço no cache do sistema de arquivos

### FilesystemCacheStateLockMicroseconds {#filesystemcachestatelockmicroseconds}

Tempo de bloqueio do cache do sistema de arquivos durante o bloqueio de estado

### FilesystemCacheUnusedHoldFileSegments {#filesystemcacheunusedholdfilesegments}

Contagem de segmentos de arquivo do cache do sistema de arquivos que foram retidos, mas não usados (por causa de seek ou LIMIT n, etc.)

### FilteringMarksWithPrimaryKeyMicroseconds {#filteringmarkswithprimarykeymicroseconds}

Tempo gasto na filtragem de partes pela PK.

### FilteringMarksWithPrimaryKeyProcessedMarks {#filteringmarkswithprimarykeyprocessedmarks}

Total de marcas processadas durante a análise da chave primária.

### FilteringMarksWithSecondaryKeysMicroseconds {#filteringmarkswithsecondarykeysmicroseconds}

Tempo gasto filtrando partes por índices de salto.

### FilterPartsByVirtualColumnsMicroseconds {#filterpartsbyvirtualcolumnsmicroseconds}

Tempo total gasto na função filterPartsByVirtualColumns.

### FilterTransformPassedBytes {#filtertransformpassedbytes}

Número de bytes que passaram pelo filtro durante a consulta

### FilterTransformPassedRows {#filtertransformpassedrows}

Número de linhas que passaram pelo filtro na consulta

### FunctionExecute {#functionexecute}

Número de chamadas de funções SQL comuns (as funções SQL são chamadas por bloco; portanto, esse número representa a quantidade de blocos).

### GatheredColumns {#gatheredcolumns}

Número de colunas coletadas durante o estágio vertical das mesclagens.

### GatheringColumnMilliseconds {#gatheringcolumnmilliseconds}

Tempo total gasto durante a coleta de colunas para a mesclagem vertical

### GlobalMemoryLimitExceeded {#globalmemorylimitexceeded}

Número de vezes em que o limite global de memória foi excedido.

### GlobalThreadPoolExpansions {#globalthreadpoolexpansions}

Conta o número total de vezes em que novas threads foram adicionadas ao pool global de threads. Essa métrica indica a frequência de expansões no pool global de threads para atender ao aumento das demandas de processamento.

### GlobalThreadPoolJobs {#globalthreadpooljobs}

Conta o número de jobs enviados ao pool global de threads.

### GlobalThreadPoolJobWaitTimeMicroseconds {#globalthreadpooljobwaittimemicroseconds}

Mede o tempo decorrido entre o momento em que um job é agendado no pool de threads e o momento em que ele é pego para execução por uma thread de trabalho. Essa métrica ajuda a identificar atrasos no processamento de jobs, indicando a capacidade de resposta do pool de threads a novas tarefas.

### GlobalThreadPoolLockWaitMicroseconds {#globalthreadpoollockwaitmicroseconds}

Tempo total que as threads passaram aguardando bloqueios no pool global de threads.

### GlobalThreadPoolShrinks {#globalthreadpoolshrinks}

Conta o número total de vezes que o pool global de threads foi reduzido com a remoção de threads. Isso ocorre quando o número de threads ociosas excede `max_thread_pool_free_size`, indicando ajustes no tamanho do pool global de threads em resposta à redução do uso de threads.

### GlobalThreadPoolThreadCreationMicroseconds {#globalthreadpoolthreadcreationmicroseconds}

Tempo total de espera para o início de novas threads.

### HardPageFaults {#hardpagefaults}

O número de hard page faults nas threads de execução de consultas. Valores altos indicam que você se esqueceu de desativar a swap no seu servidor, a remoção de páginas de memória do binário do ClickHouse sob pressão de memória muito alta ou o uso bem-sucedido do método de leitura &#39;mmap&#39; para os dados das tabelas.

### HashJoinPreallocatedElementsInHashTables {#hashjoinpreallocatedelementsinhashtables}

Quantos elementos foram pré-alocados nas tabelas hash para hash join.

### HedgedRequestsChangeReplica {#hedgedrequestschangereplica}

Contagem total de vezes em que o timeout para trocar de réplica expirou em hedged requests.

### HTTPConnectionsCreated {#httpconnectionscreated}

Número de conexões HTTP de cliente criadas

### HTTPConnectionsElapsedMicroseconds {#httpconnectionselapsedmicroseconds}

Tempo total gasto para estabelecer conexões HTTP do cliente

### HTTPConnectionsErrors {#httpconnectionserrors}

Número de vezes em que a criação de uma conexão HTTP do cliente falhou

### HTTPConnectionsExpired {#httpconnectionsexpired}

Número de conexões HTTP do cliente expiradas

### HTTPConnectionsPreserved {#httpconnectionspreserved}

Número de conexões HTTP preservadas do cliente

### HTTPConnectionsReset {#httpconnectionsreset}

Número de conexões HTTP do cliente reiniciadas

### HTTPConnectionsReused {#httpconnectionsreused}

Número de conexões HTTP do cliente reutilizadas

### HTTPServerConnectionsClosed {#httpserverconnectionsclosed}

Número de conexões HTTP do servidor que foram fechadas. O Keep alive não foi negociado

### HTTPServerConnectionsCreated {#httpserverconnectionscreated}

Número de conexões HTTP do servidor criadas

### HTTPServerConnectionsExpired {#httpserverconnectionsexpired}

Número de conexões HTTP expiradas do servidor.

### HTTPServerConnectionsPreserved {#httpserverconnectionspreserved}

Número de conexões HTTP do servidor preservadas. Conexão mantida ativa com sucesso

### HTTPServerConnectionsReset {#httpserverconnectionsreset}

Número de conexões HTTP do servidor encerradas com reset. O servidor fecha a conexão

### HTTPServerConnectionsReused {#httpserverconnectionsreused}

Número de conexões HTTP do servidor reutilizadas

### IcebergIteratorInitializationMicroseconds {#icebergiteratorinitializationmicroseconds}

Tempo total gasto na inicialização síncrona dos iteradores de dados do Iceberg.

### IcebergMetadataFilesCacheHits {#icebergmetadatafilescachehits}

Número de vezes em que os arquivos de metadados do Iceberg foram encontrados no cache.

### IcebergMetadataFilesCacheMisses {#icebergmetadatafilescachemisses}

Número de vezes em que os arquivos de metadados do Iceberg não foram encontrados no cache de metadados do Iceberg e precisaram ser lidos do disco (remoto).

### IcebergMetadataFilesCacheStaleMisses {#icebergmetadatafilescachestalemisses}

Número de vezes em que arquivos de metadados do Iceberg foram encontrados no cache, mas foram considerados desatualizados e tiveram de ser lidos do disco (remoto).

### IcebergMetadataFilesCacheWeightLost {#icebergmetadatafilescacheweightlost}

Número aproximado de bytes removidos do cache de metadados do Iceberg.

### IcebergMetadataReadWaitTimeMicroseconds {#icebergmetadatareadwaittimemicroseconds}

Tempo total que os leitores de dados passam aguardando a leitura e a análise dos arquivos de metadados do Iceberg, somado entre todas as threads de leitura.

### IcebergMetadataReturnedObjectInfos {#icebergmetadatareturnedobjectinfos}

Número total de informações de objetos retornadas pelo iterador do Iceberg.

### IcebergMetadataUpdateMicroseconds {#icebergmetadataupdatemicroseconds}

Tempo total gasto na atualização dos metadados de armazenamento do Iceberg a partir do estado (buildStorageMetadataFromState).

### IcebergMinMaxIndexPrunedFiles {#icebergminmaxindexprunedfiles}

Número de arquivos ignorados com o uso do índice MinMax no Iceberg

### IcebergMinMaxNonPrunedDeleteFiles {#icebergminmaxnonpruneddeletefiles}

Número total de pares aceitos de arquivos de dados e arquivos de exclusão por posição na análise minmax, entre os pares adequados por particionamento e número de sequência.

### IcebergMinMaxPrunedDeleteFiles {#icebergminmaxpruneddeletefiles}

Número total de pares aceitos de arquivos de dados e arquivos de exclusão por posição, segundo a análise minmax, entre os pares adequados por particionamento e número de sequência.

### IcebergPartitionPrunedFiles {#icebergpartitionprunedfiles}

Número de arquivos ignorados durante a poda de partições do Iceberg

### IcebergPartitionPrunedManifestFiles {#icebergpartitionprunedmanifestfiles}

Número de arquivos de manifesto do Iceberg ignorados sem serem lidos, com base nos resumos de partição da manifest list

### IcebergTrivialCountOptimizationApplied {#icebergtrivialcountoptimizationapplied}

Otimização trivial de contagem aplicada durante a leitura do Iceberg

### IcebergVersionHintUsed {#icebergversionhintused}

Número de vezes em que version-hint.text foi usado.

### IgnoredColdParts {#ignoredcoldparts}

Consulte a configuração ignore&#95;cold&#95;parts&#95;seconds. Número de vezes em que consultas de leitura ignoraram partes recém-criadas que ainda não haviam sido carregadas no cache pelo CacheWarmer.

### IndexAnalysisRounds {#indexanalysisrounds}

Número de vezes em que a análise de índice foi realizada na consulta.

### IndexBinarySearchAlgorithm {#indexbinarysearchalgorithm}

Número de vezes que o algoritmo de busca binária é usado nas marcas do índice

### IndexGenericExclusionSearchAlgorithm {#indexgenericexclusionsearchalgorithm}

Número de vezes que o algoritmo de busca por exclusão genérica é usado nas marcas do índice

### IndexGenericExclusionSearchStepLimitReached {#indexgenericexclusionsearchsteplimitreached}

Número de vezes que a busca por exclusão genérica sobre as marcas do índice atingiu merge&#95;tree&#95;generic&#95;exclusion&#95;search&#95;max&#95;steps e aceitou os mark ranges restantes sem realizar novos splits

### InitialQuery {#initialquery}

O mesmo que Query, mas conta apenas consultas iniciais (consulte is&#95;initial&#95;query).

### InitialSelectQuery {#initialselectquery}

O mesmo que InitialQuery, mas apenas para consultas SELECT.

### InsertedBytes {#insertedbytes}

Número de bytes (não compactados; das colunas, conforme armazenadas na memória) inseridos em todas as tabelas.

### InsertedCompactParts {#insertedcompactparts}

Número de partes inseridas no formato Compact.

### InsertedRows {#insertedrows}

Número de linhas inseridas em todas as tabelas.

### InsertedWideParts {#insertedwideparts}

Número de partes inseridas no formato Wide.

### InsertQueriesWithSubqueries {#insertquerieswithsubqueries}

Conta consultas INSERT com todas as subconsultas

### InsertQuery {#insertquery}

O mesmo que Query, mas apenas para consultas INSERT.

### InsertQueryTimeMicroseconds {#insertquerytimemicroseconds}

Tempo total das consultas de INSERT.

### InterfaceHTTPReceiveBytes {#interfacehttpreceivebytes}

Número de bytes recebidos pelas interfaces HTTP

### InterfaceHTTPSendBytes {#interfacehttpsendbytes}

Número de bytes enviados pelas interfaces HTTP

### InterfaceInterserverReceiveBytes {#interfaceinterserverreceivebytes}

Número de bytes recebidos pelas interfaces entre servidores

### InterfaceInterserverSendBytes {#interfaceinterserversendbytes}

Número de bytes enviados pelas interfaces entre servidores

### InterfaceMySQLReceiveBytes {#interfacemysqlreceivebytes}

Número de bytes recebidos por interfaces MySQL

### InterfaceMySQLSendBytes {#interfacemysqlsendbytes}

Número de bytes enviados pelas interfaces MySQL

### InterfaceNativeReceiveBytes {#interfacenativereceivebytes}

Número de bytes recebidos pelas interfaces nativas

### InterfaceNativeSendBytes {#interfacenativesendbytes}

Número de bytes enviados por interfaces nativas

### InterfacePostgreSQLReceiveBytes {#interfacepostgresqlreceivebytes}

Número de bytes recebidos por interfaces PostgreSQL

### InterfacePostgreSQLSendBytes {#interfacepostgresqlsendbytes}

Número de bytes enviados por meio das interfaces do PostgreSQL

### InterfacePrometheusReceiveBytes {#interfaceprometheusreceivebytes}

Número de bytes recebidos pelas interfaces do Prometheus

### InterfacePrometheusSendBytes {#interfaceprometheussendbytes}

Número de bytes enviados por meio das interfaces do Prometheus

### IOBufferAllocBytes {#iobufferallocbytes}

Número de bytes alocados para os buffers de E/S (ReadBuffer/WriteBuffer).

### IOBufferAllocs {#iobufferallocs}

Número de alocações de buffers de E/S (para ReadBuffer/WriteBuffer).

### IOUringCQEsCompleted {#iouringcqescompleted}

Número total de CQEs do io&#95;uring concluídos com sucesso

### IOUringCQEsFailed {#iouringcqesfailed}

Número total de CQEs do io&#95;uring concluídos com falha

### IOUringSQEsResubmitsAsync {#iouringsqesresubmitsasync}

Número total de reenvios assíncronos de SQE do io&#95;uring realizados

### IOUringSQEsResubmitsSync {#iouringsqesresubmitssync}

Número total de reenvios síncronos de SQEs do io&#95;uring efetuados

### IOUringSQEsSubmitted {#iouringsqessubmitted}

Número total de SQEs de io&#95;uring submetidos

### JemallocFailedAllocationSampleTracking {#jemallocfailedallocationsampletracking}

Número total de vezes em que o rastreamento da amostra de alocação do jemalloc falhou.

### JemallocFailedDeallocationSampleTracking {#jemallocfaileddeallocationsampletracking}

Número total de vezes que o rastreamento da amostra de desalocação do jemalloc falhou

### JoinBuildPostProcessingMicroseconds {#joinbuildpostprocessingmicroseconds}

Tempo gasto nas etapas de pós-processamento após a construção do lado direito do JOIN.

### JoinBuildRowStoreMicroseconds {#joinbuildrowstoremicroseconds}

Tempo decorrido na transformação da carga útil do lado direito do JOIN para o formato orientado a linhas (row-major).

### JoinBuildTableRowCount {#joinbuildtablerowcount}

Número total de linhas na tabela de build em uma operação JOIN.

### JoinDelayedJoinedTransformBlockCount {#joindelayedjoinedtransformblockcount}

Número de blocos emitidos pelo DelayedJoinedBlocksWorkerTransform.

### JoinDelayedJoinedTransformRowCount {#joindelayedjoinedtransformrowcount}

Número de linhas emitidas por DelayedJoinedBlocksWorkerTransform.

### JoinNonJoinedTransformBlockCount {#joinnonjoinedtransformblockcount}

Número de blocos emitidos pelo NonJoinedBlocksTransform.

### JoinNonJoinedTransformRowCount {#joinnonjoinedtransformrowcount}

Número de linhas sem correspondência emitidas por NonJoinedBlocksTransform.

### JoinOptimizeMicroseconds {#joinoptimizemicroseconds}

Tempo total gasto na execução das otimizações do plano de JOIN.

### JoinProbeTableRowCount {#joinprobetablerowcount}

Número total de linhas na tabela de sondagem em uma operação JOIN.

### JoinReorderMicroseconds {#joinreordermicroseconds}

Tempo total gasto na execução do algoritmo de reordenação de JOIN.

### JoinResultRowCount {#joinresultrowcount}

Número total de linhas no resultado de uma operação de JOIN.

### JoinSpillingHashJoinSwitchedToGraceJoin {#joinspillinghashjoinswitchedtogracejoin}

Número de vezes em que um (Concurrent)HashJoin foi alterado para GraceHashJoin devido ao limite de memória no SpillingHashJoin.

### KafkaBackgroundReads {#kafkabackgroundreads}

Número de leituras em segundo plano que preenchem visões materializadas a partir do Kafka desde a inicialização do servidor

### KafkaCommitFailures {#kafkacommitfailures}

Número de falhas ao confirmar no Kafka os offsets consumidos (geralmente é um sinal de duplicação de dados)

### KafkaCommits {#kafkacommits}

Número de commits bem-sucedidos dos offsets consumidos no Kafka (normalmente deve ser o mesmo que KafkaBackgroundReads)

### KafkaConsumerErrors {#kafkaconsumererrors}

Número de erros reportados pela librdkafka durante as chamadas de poll

### KafkaDirectReads {#kafkadirectreads}

Número de consultas SELECT diretas em tabelas Kafka desde a inicialização do servidor

### KafkaMessagesFailed {#kafkamessagesfailed}

Número de mensagens do Kafka que o ClickHouse não conseguiu interpretar

### KafkaMessagesPolled {#kafkamessagespolled}

Número de mensagens do Kafka consumidas da `librdkafka` pelo ClickHouse

### KafkaMessagesProduced {#kafkamessagesproduced}

Número de mensagens enviadas ao Kafka

### KafkaMessagesRead {#kafkamessagesread}

Número de mensagens do Kafka já processadas pelo ClickHouse

### KafkaMVNotReady {#kafkamvnotready}

Número de tentativas malsucedidas de enviar dados para uma visão materializada que não está pronta

### KafkaProducerErrors {#kafkaproducererrors}

Número de erros ao produzir mensagens para o Kafka

### KafkaProducerFlushes {#kafkaproducerflushes}

Número de flushes explícitos do produtor Kafka

### Atribuições de rebalanceamento do Kafka {#kafkarebalanceassignments}

Número de atribuições de partição (estágio final do rebalanceamento do grupo de consumidores)

### KafkaRebalanceErrors {#kafkarebalanceerrors}

Número de rebalanceamentos de grupos de consumidores que falharam

### KafkaRebalanceRevocations {#kafkarebalancerevocations}

Número de revogações de partições (o primeiro estágio do rebalanceamento do grupo de consumidores)

### KafkaRowsRead {#kafkarowsread}

Número de linhas analisadas a partir de mensagens do Kafka

### KafkaRowsRejected {#kafkarowsrejected}

Número de linhas processadas que foram posteriormente rejeitadas (devido a rebalances, erros ou motivos semelhantes). Essas linhas serão consumidas novamente após o rebalance.

### KafkaRowsWritten {#kafkarowswritten}

Número de linhas inseridas nas tabelas Kafka

### KafkaWrites {#kafkawrites}

Número de gravações (inserts) nas tabelas Kafka

### KeeperAddWatchRequest {#keeperaddwatchrequest}

Número de solicitações de adição de watches

### KeeperBatchMaxCount {#keeperbatchmaxcount}

Número de lotes que atingiram o limite de max&#95;requests&#95;batch&#95;size

### KeeperBatchMaxReadCount {#keeperbatchmaxreadcount}

Número de lotes que atingiram o limite de max&#95;read&#95;batch&#95;size

### KeeperBatchMaxReadTotalSize {#keeperbatchmaxreadtotalsize}

Número de lotes que atingiram o limite `max_read_batch_bytes_size`

### KeeperBatchMaxTotalSize {#keeperbatchmaxtotalsize}

Número de lotes que atingiram o limite de max&#95;requests&#95;batch&#95;bytes&#95;size

### KeeperChangelogFileSyncMicroseconds {#keeperchangelogfilesyncmicroseconds}

Tempo gasto em `fsync` para o changelog do Keeper (somente logs não compactados)

### KeeperChangelogLockWaitMicroseconds {#keeperchangeloglockwaitmicroseconds}

Tempo gasto aguardando a aquisição do bloqueio do changelog do Keeper

### KeeperChangelogStartupReadBytes {#keeperchangelogstartupreadbytes}

Número de bytes físicos (checksum + cabeçalho + blob) lidos dos arquivos de changelog pelo leitor paralelo de inicialização do Keeper

### KeeperChangelogStartupReadEntries {#keeperchangelogstartupreadentries}

Número de registros de changelog validados pelo leitor paralelo de inicialização do Keeper

### KeeperChangelogStartupReadMicroseconds {#keeperchangelogstartupreadmicroseconds}

Wall time gasto na leitura paralela do changelog durante a inicialização do Keeper

### KeeperChangelogStartupStitchMicroseconds {#keeperchangelogstartupstitchmicroseconds}

Wall time gasto na combinação dos resultados das leituras paralelas de inicialização de cada arquivo no state do LogEntryStorage

### KeeperChangelogWrittenBytes {#keeperchangelogwrittenbytes}

Número de bytes gravados no changelog do Keeper

### KeeperCheckRequest {#keepercheckrequest}

Número de requisições de verificação

### KeeperCheckWatchRequest {#keepercheckwatchrequest}

Número de solicitações de remove watches

### KeeperCommits {#keepercommits}

Número de commits realizados com sucesso

### KeeperCommitsFailed {#keepercommitsfailed}

Número de commits que falharam

### KeeperCommitWaitElapsedMicroseconds {#keepercommitwaitelapsedmicroseconds}

Tempo gasto aguardando a confirmação de determinado log

### KeeperCreateRequest {#keepercreaterequest}

Número de requisições de criação

### KeeperExistsRequest {#keeperexistsrequest}

Número de requisições de exists

### KeeperGetRequest {#keepergetrequest}

Número de requisições de get

### KeeperLatency {#keeperlatency}

Latência do Keeper

### KeeperListRecursiveRequest {#keeperlistrecursiverequest}

Número de solicitações recursivas para obter nós filhos

### KeeperListRequest {#keeperlistrequest}

Número de requisições de listagem

### KeeperLiveSessionsLockWaitMicroseconds {#keeperlivesessionslockwaitmicroseconds}

Tempo gasto aguardando a aquisição do bloqueio de sessões ativas do Keeper

### KeeperLogsEntryReadFromCommitReadAhead {#keeperlogsentryreadfromcommitreadahead}

Número de entradas de log entregues à commit thread a partir da decoded window do leitor de read-ahead de commit (pop por fast-path ou pop de drenagem do primeiro elemento da window)

### KeeperLogsEntryReadFromFile {#keeperlogsentryreadfromfile}

Número de entradas de log no Keeper lidas diretamente do arquivo de changelog

### KeeperLogsEntryReadFromLatestCache {#keeperlogsentryreadfromlatestcache}

Número de entradas de log no Keeper lidas a partir do cache dos logs mais recentes

### KeeperLogsReadAheadCursorsInstalled {#keeperlogsreadaheadcursorsinstalled}

Número de novos cursores de preenchimento enfileirados em um leitor de read-ahead do changelog do Keeper (peer ou commit) que ainda não eram cobertos pela tarefa de preenchimento

### KeeperLogsReadAheadFillDecodedEntries {#keeperlogsreadaheadfilldecodedentries}

Número de entradas de log decodificadas pela tarefa de preenchimento de read-ahead do Keeper

### KeeperLogsReadAheadFillReopens {#keeperlogsreadaheadfillreopens}

Número de vezes que o preenchimento por leitura antecipada (read-ahead) do Keeper reabriu ou realizou seek em um changelog file

### KeeperLogsReadAheadPlanEpochMismatches {#keeperlogsreadaheadplanepochmismatches}

Número de vezes que um plano de leitura do changelog do Keeper foi descartado porque um truncamento concorrente por write&#95;at o invalidou antes que ele pudesse ser atendido

### KeeperLogsReadAheadReadersCreated {#keeperlogsreadaheadreaderscreated}

Número de leitores read-ahead do changelog do Keeper (e suas tasks de preenchimento) criados; permanecer plano ao cruzar limites de arquivo indica que um leitor está sendo reutilizado, em vez de destruído e recriado

### KeeperLogsReadAheadScheduleRejected {#keeperlogsreadaheadschedulerejected}

Número de vezes que não foi possível criar um leitor de read-ahead do changelog do Keeper porque a fila do pool de threads de read-ahead estava cheia; nesse caso, o chamador recorre a uma leitura direta

### KeeperLogsReadAheadTimeoutFallbacks {#keeperlogsreadaheadtimeoutfallbacks}

Número de vezes que uma requisição de atendimento por read-ahead do changelog do Keeper excedeu o tempo limite aguardando a tarefa de preenchimento e recorreu ao fallback de leitura direta do restante da cauda (tail)

### KeeperLSMTCommittedEntryBytes {#keeperlsmtcommittedentrybytes}

Bytes serializados das entradas anexadas à memtable confirmada do LSMT do Keeper

### KeeperLSMTCommittedMemtablesCreated {#keeperlsmtcommittedmemtablescreated}

Número de memtables confirmadas criadas pelo LSMT do Keeper

### KeeperLSMTFileDeletionExceptions {#keeperlsmtfiledeletionexceptions}

Número de exceções na exclusão de arquivos LSMT do Keeper

### KeeperLSMTFlushes {#keeperlsmtflushes}

Número de flushes da memtable LSMT do Keeper concluídos com sucesso

### KeeperLSMTFlushExceptions {#keeperlsmtflushexceptions}

Número de exceções em flushes de memtable LSMT do Keeper

### KeeperLSMTFlushWrittenCompressedBytes {#keeperlsmtflushwrittencompressedbytes}

Bytes comprimidos gravados em arquivos pelos flushes da memtable LSMT do Keeper

### KeeperLSMTFlushWrittenUncompressedBytes {#keeperlsmtflushwrittenuncompressedbytes}

Bytes não comprimidos gravados em arquivos pelos flushes de memtable do LSMT do Keeper

### KeeperLSMTGetBlockFromCache {#keeperlsmtgetblockfromcache}

Número de acessos a blocos LSMT do Keeper atendidos pelo cache de blocos

### KeeperLSMTGetBlockFromWeakPtr {#keeperlsmtgetblockfromweakptr}

Número de acessos a blocos LSMT do Keeper atendidos pelo ponteiro fraco nas informações de bloco do arquivo

### KeeperLSMTGetBlockLoadedGroup {#keeperlsmtgetblockloadedgroup}

Número de acessos a blocos LSMT do Keeper que precisaram carregar um grupo de blocos do disco

### KeeperLSMTGetCommittedNodeFromMemory {#keeperlsmtgetcommittednodefrommemory}

Número de lookups de nós confirmados do LSMT do Keeper que encontraram o bloco do nó já em memória

### KeeperLSMTGetCommittedNodeLoadedBlock {#keeperlsmtgetcommittednodeloadedblock}

Número de lookups de nós confirmados no LSMT do Keeper que precisaram carregar o bloco do nó

### KeeperLSMTGetCommittedNodeNonexistent {#keeperlsmtgetcommittednodenonexistent}

Número de lookups de nós confirmados do LSMT do Keeper para nós inexistentes

### KeeperLSMTGetUncommittedNodeHits {#keeperlsmtgetuncommittednodehits}

Número de lookups de nós LSMT do Keeper que encontraram o nó em memtables não confirmadas

### KeeperLSMTGetUncommittedNodeMisses {#keeperlsmtgetuncommittednodemisses}

Número de lookups de nós LSMT do Keeper que não encontraram o nó nas memtables não confirmadas e recorreram ao state confirmado

### KeeperLSMTListFilterFalsePositives {#keeperlsmtlistfilterfalsepositives}

Número de varreduras de arquivos não ignoradas pelo filtro de caminhos pai do LSMT do Keeper que não encontraram nenhum filho

### KeeperLSMTListFilterSkipped {#keeperlsmtlistfilterskipped}

Número de varreduras de arquivos ignoradas pelas operações de listagem LSMT do Keeper porque o filtro de caminhos pais excluiu todos os filhos

### KeeperLSMTListFilterTruePositives {#keeperlsmtlistfiltertruepositives}

Número de varreduras de arquivos não ignoradas pelo filtro de caminhos pais do LSMT do Keeper que encontraram filhos

### KeeperLSMTListNamesFromFiles {#keeperlsmtlistnamesfromfiles}

Nomes de filhos (incluindo tombstones) obtidos de arquivos pelas operações de listagem do LSMT do Keeper

### KeeperLSMTListNamesFromMemtables {#keeperlsmtlistnamesfrommemtables}

Nomes de filhos (incluindo tombstones) obtidos das memtables pelas operações de listagem do LSMT do Keeper

### KeeperLSMTListScannedBlocks {#keeperlsmtlistscannedblocks}

Blocos varridos pelas operações de listagem LSMT do Keeper em arquivos

### KeeperLSMTListScannedEntries {#keeperlsmtlistscannedentries}

Entradas percorridas pelas operações de listagem LSMT do Keeper em arquivos

### KeeperLSMTLoadedBlocks {#keeperlsmtloadedblocks}

Número de blocos carregados do disco pelo LSMT do Keeper

### KeeperLSMTLoadedUncompressedBytes {#keeperlsmtloadeduncompressedbytes}

Bytes não comprimidos de blocos carregados do disco pelo LSMT do Keeper

### KeeperLSMTMergeConsumedFiles {#keeperlsmtmergeconsumedfiles}

Número de arquivos de entrada totalmente consumidos e descartados pelas mesclagens LSMT do Keeper

### KeeperLSMTMergeConsumedUncompressedBytes {#keeperlsmtmergeconsumeduncompressedbytes}

Bytes não comprimidos em arquivos de entrada totalmente consumidos e descartados pelas mesclagens LSMT do Keeper

### KeeperLSMTMergeExceptions {#keeperlsmtmergeexceptions}

Número de exceções em mesclagens LSMT do Keeper

### KeeperLSMTMerges {#keeperlsmtmerges}

Número de mesclagens LSMT do Keeper concluídas com sucesso

### KeeperLSMTMergeWrittenCompressedBytes {#keeperlsmtmergewrittencompressedbytes}

Bytes comprimidos nos arquivos de saída publicados pelas mesclagens LSMT do Keeper

### KeeperLSMTMergeWrittenFiles {#keeperlsmtmergewrittenfiles}

Número de arquivos de saída publicados pelas mesclagens LSMT do Keeper

### KeeperLSMTMergeWrittenUncompressedBytes {#keeperlsmtmergewrittenuncompressedbytes}

Bytes não comprimidos nos arquivos de saída publicados pelas mesclagens LSMT do Keeper

### KeeperLSMTNodeCacheEntriesUpdated {#keeperlsmtnodecacheentriesupdated}

Número de entradas de cache de nós LSMT do Keeper reapontadas para um bloco recém-carregado

### KeeperLSMTThrottledWrites {#keeperlsmtthrottledwrites}

Número de gravações atrasadas porque os flushes ou as mesclagens em segundo plano do LSMT do Keeper não acompanharam o ritmo

### KeeperLSMTUncommittedCreateBytes {#keeperlsmtuncommittedcreatebytes}

Bytes serializados de entradas Create anexadas às memtables não confirmadas do LSMT do Keeper

### KeeperLSMTUncommittedCreates {#keeperlsmtuncommittedcreates}

Número de entradas Create anexadas às memtables não confirmadas do LSMT do Keeper

### KeeperLSMTUncommittedMemtablesCreated {#keeperlsmtuncommittedmemtablescreated}

Número de memtables não confirmadas criadas pelo LSMT do Keeper

### KeeperLSMTUncommittedRemoveBytes {#keeperlsmtuncommittedremovebytes}

Bytes serializados de entradas Remove anexadas às memtables não confirmadas do LSMT do Keeper

### KeeperLSMTUncommittedRemoves {#keeperlsmtuncommittedremoves}

Número de entradas Remove anexadas às memtables não confirmadas do LSMT do Keeper

### KeeperLSMTUncommittedUpdateBytes {#keeperlsmtuncommittedupdatebytes}

Bytes serializados de entradas de Update anexadas às memtables não confirmadas do LSMT do Keeper

### KeeperLSMTUncommittedUpdates {#keeperlsmtuncommittedupdates}

Número de entradas de Update anexadas às memtables não confirmadas do LSMT do Keeper

### KeeperMultiReadRequest {#keepermultireadrequest}

Número de solicitações de multileitura

### KeeperMultiRequest {#keepermultirequest}

Número de solicitações múltiplas

### KeeperPacketsReceived {#keeperpacketsreceived}

Pacotes recebidos pelo servidor do Keeper

### KeeperPacketsSent {#keeperpacketssent}

Pacotes enviados pelo servidor Keeper

### KeeperPreprocessElapsedMicroseconds {#keeperpreprocesselapsedmicroseconds}

Latência de pré-processamento do Keeper para uma única requisição

### KeeperProcessAndResponsesLockWaitMicroseconds {#keeperprocessandresponseslockwaitmicroseconds}

Tempo gasto aguardando a obtenção do bloqueio de processamento e respostas do Keeper

### KeeperProcessElapsedMicroseconds {#keeperprocesselapsedmicroseconds}

Latência de commit do Keeper para uma única solicitação

### KeeperReadBatchCount {#keeperreadbatchcount}

Número de lotes de requisições de leitura processados pelo Keeper

### KeeperReadBatchTotalRequests {#keeperreadbatchtotalrequests}

Número total de solicitações de leitura que o Keeper processou em lotes

### KeeperReadRequestQueueLockWaitMicroseconds {#keeperreadrequestqueuelockwaitmicroseconds}

Tempo gasto aguardando a obtenção do bloqueio da fila de solicitações de leitura do Keeper

### KeeperReadSnapshot {#keeperreadsnapshot}

Número de leituras de snapshot concluídas

### KeeperReadSnapshotDeferred {#keeperreadsnapshotdeferred}

Número de leituras de snapshot adiadas porque o snapshot solicitado não está mais disponível localmente; o NuRaft tentará novamente usando o snapshot mais recente.

### KeeperReadSnapshotFailed {#keeperreadsnapshotfailed}

Número de falhas na leitura de snapshot

### KeeperReadSnapshotObject {#keeperreadsnapshotobject}

Número de objetos de snapshot enviados aos nós seguidores

### KeeperReconfigRequest {#keeperreconfigrequest}

Número de solicitações de reconfiguração

### KeeperRemoveRequest {#keeperremoverequest}

Número de requisições de remoção

### KeeperRemoveWatchRequest {#keeperremovewatchrequest}

Número de solicitações de remoção de watches

### KeeperRequestRejectedDueToSoftMemoryLimitCount {#keeperrequestrejectedduetosoftmemorylimitcount}

Número de solicitações rejeitadas por excederem o limite flexível de memória

### KeeperRequestTotal {#keeperrequesttotal}

Número total de requisições no servidor Keeper

### Total de requisições do Keeper com sub-requisições {#keeperrequesttotalwithsubrequests}

Número total de requisições no servidor Keeper, contando cada sub-requisição dentro de uma requisição múltipla

### KeeperSaveSnapshot {#keepersavesnapshot}

Número de salvamentos de snapshot

### KeeperSaveSnapshotFailed {#keepersavesnapshotfailed}

Número de falhas ao salvar snapshots

### KeeperSaveSnapshotObject {#keepersavesnapshotobject}

Número de objetos de snapshot recebidos do nó líder

### KeeperServerWriteLockWaitMicroseconds {#keeperserverwritelockwaitmicroseconds}

Tempo gasto aguardando a aquisição do bloqueio de escrita do servidor Keeper

### KeeperSessionCallbackLockWaitMicroseconds {#keepersessioncallbacklockwaitmicroseconds}

Tempo gasto aguardando a obtenção do bloqueio de callback da sessão do Keeper

### KeeperSetRequest {#keepersetrequest}

Número de solicitações do tipo set

### KeeperSetWatchesRequest {#keepersetwatchesrequest}

Número de solicitações para definir watches

### KeeperSnapshotApplys {#keepersnapshotapplys}

Número de aplicações de snapshots

### KeeperSnapshotApplysFailed {#keepersnapshotapplysfailed}

Número de falhas ao aplicar snapshots

### KeeperSnapshotCreations {#keepersnapshotcreations}

Número de snapshots criados

### KeeperSnapshotCreationsFailed {#keepersnapshotcreationsfailed}

Número de falhas na criação de snapshots

### KeeperSnapshotFileSyncMicroseconds {#keepersnapshotfilesyncmicroseconds}

Tempo gasto no `fsync` dos arquivos de snapshot do Keeper

### KeeperSnapshotRemoteLoaderErrors {#keepersnapshotremoteloadererrors}

Número de erros de leitura remota no RemoteSnapshotLoader ao fornecer um snapshot a um seguidor

### KeeperSnapshotWrittenBytes {#keepersnapshotwrittenbytes}

Número de bytes gravados nos arquivos de snapshot do Keeper

### KeeperStaleRequestsSkipped {#keeperstalerequestsskipped}

Número de solicitações do Keeper ignoradas porque a sessão não está mais ativa

### KeeperStorageLockWaitMicroseconds {#keeperstoragelockwaitmicroseconds}

Tempo gasto aguardando a obtenção do bloqueio de armazenamento do Keeper

### KeeperStorageSharedLockWaitMicroseconds {#keeperstoragesharedlockwaitmicroseconds}

Tempo gasto aguardando a obtenção do bloqueio compartilhado do armazenamento do Keeper

### KeeperTotalElapsedMicroseconds {#keepertotalelapsedmicroseconds}

Latência total do Keeper para uma única requisição

### KeeperTTLRemoveRequestsDropped {#keeperttlremoverequestsdropped}

Número de solicitações de remoção de TTL descartadas porque a fila estava cheia

### KeeperTTLRemoveRequestsEnqueued {#keeperttlremoverequestsenqueued}

Número de solicitações de remoção de TTL colocadas na fila com sucesso

### KeeperWatchesTriggered {#keeperwatchestriggered}

Número de acionamentos de watch

### KeeperWatchTriggeredNodeChildrenChanged {#keeperwatchtriggerednodechildrenchanged}

Número de acionamentos de watch por operações de alteração nos filhos do nó

### KeeperWatchTriggeredNodeCreated {#keeperwatchtriggerednodecreated}

Número de acionamentos de watch pela operação `CREATE`

### KeeperWatchTriggeredNodeDataChanged {#keeperwatchtriggerednodedatachanged}

Número de acionamentos de watch por operações de modificação

### KeeperWatchTriggeredNodeDeleted {#keeperwatchtriggerednodedeleted}

Número de acionamentos de `watch` pela operação `DELETE`

### KeeperWriteBatchCount {#keeperwritebatchcount}

Número de lotes de solicitações de gravação processados pelo Keeper

### KeeperWriteBatchTotalRequests {#keeperwritebatchtotalrequests}

Número total de requisições de gravação que o Keeper processou em lotes

### LoadedDataParts {#loadeddataparts}

Número de partes de dados carregadas pelas tabelas MergeTree durante a inicialização.

### LoadedDataPartsMicroseconds {#loadeddatapartsmicroseconds}

Microssegundos gastos pelas tabelas MergeTree no carregamento de partes de dados durante a inicialização.

### Número de marcas carregadas {#loadedmarkscount}

Número de marcas carregadas (total em todas as colunas).

### LoadedMarksFiles {#loadedmarksfiles}

Número de arquivos de marcação carregados.

### LoadedMarksMemoryBytes {#loadedmarksmemorybytes}

Tamanho das representações em memória das marcas carregadas.

### LoadedPrimaryIndexBytes {#loadedprimaryindexbytes}

Quantidade de linhas da chave primária carregadas.

### Arquivos do índice primário carregados {#loadedprimaryindexfiles}

Número de arquivos do índice primário carregados.

### LoadedPrimaryIndexRows {#loadedprimaryindexrows}

Número de linhas da chave primária que foram carregadas.

### LoadedStatisticsMicroseconds {#loadedstatisticsmicroseconds}

Tempo decorrido para carregar estatísticas das partes

### LoadingMarksTasksCanceled {#loadingmarkstaskscanceled}

Número de vezes que tarefas em segundo plano de carregamento de marcas foram canceladas

### LocalReadThrottlerBytes {#localreadthrottlerbytes}

Bytes que passaram pelo limitador de taxa &#39;max&#95;local&#95;read&#95;bandwidth&#95;for&#95;server&#39;/&#39;max&#95;local&#95;read&#95;bandwidth&#39;.

### LocalReadThrottlerSleepMicroseconds {#localreadthrottlersleepmicroseconds}

Tempo total em que uma consulta permaneceu em espera para atender à limitação de taxa de `'max_local_read_bandwidth_for_server'`/`'max_local_read_bandwidth'`.

### LocalThreadPoolBusyMicroseconds {#localthreadpoolbusymicroseconds}

Tempo total que as threads passaram executando o trabalho propriamente dito.

### LocalThreadPoolExpansions {#localthreadpoolexpansions}

Conta o número total de vezes em que threads foram emprestadas do pool global de threads para expandir os pools locais de threads.

### LocalThreadPoolJobs {#localthreadpooljobs}

Conta o número de jobs enviados para os pools de threads locais.

### LocalThreadPoolJobWaitTimeMicroseconds {#localthreadpooljobwaittimemicroseconds}

Mede o tempo decorrido entre o momento em que um job é agendado no pool de threads e o momento em que ele é assumido para execução por uma thread worker. Essa métrica ajuda a identificar atrasos no processamento de jobs, indicando a capacidade de resposta do pool de threads a novas tarefas.

### LocalThreadPoolLockWaitMicroseconds {#localthreadpoollockwaitmicroseconds}

Tempo total que as threads passaram aguardando locks nos pools de threads locais.

### LocalThreadPoolShrinks {#localthreadpoolshrinks}

Conta o número total de vezes que threads foram devolvidas dos pools locais de threads ao pool global de threads.

### LocalThreadPoolThreadCreationMicroseconds {#localthreadpoolthreadcreationmicroseconds}

Tempo total que os pools de threads locais passaram aguardando para obter uma thread emprestada do pool global.

### LocalWriteThrottlerBytes {#localwritethrottlerbytes}

Bytes que passaram pelo limitador de taxa &#39;max&#95;local&#95;write&#95;bandwidth&#95;for&#95;server&#39;/&#39;max&#95;local&#95;write&#95;bandwidth&#39;.

### LocalWriteThrottlerSleepMicroseconds {#localwritethrottlersleepmicroseconds}

Tempo total em que uma consulta permaneceu em espera para se adequar à limitação de taxa de &#39;max&#95;local&#95;write&#95;bandwidth&#95;for&#95;server&#39;/&#39;max&#95;local&#95;write&#95;bandwidth&#39;.

### LogDebug {#logdebug}

Número de mensagens de log no nível Debug

### LogError {#logerror}

Número de mensagens de log no nível Error

### LogFatal {#logfatal}

Quantidade de mensagens de log de nível Fatal

### LoggerElapsedNanoseconds {#loggerelapsednanoseconds}

Tempo cumulativo gasto com logging

### LogInfo {#loginfo}

Número de mensagens de log no nível Info

### LogTest {#logtest}

Número de mensagens de log no nível Test

### LogTrace {#logtrace}

Número de mensagens de log no nível Trace

### LogWarning {#logwarning}

Número de mensagens de log no nível Warning

### MainConfigLoads {#mainconfigloads}

Número de vezes que a configuração principal foi recarregada.

### MarkCacheEvictedBytes {#markcacheevictedbytes}

Número de bytes removidos do cache de marcas.

### MarkCacheEvictedFiles {#markcacheevictedfiles}

Número de arquivos de marcação removidos do cache de marcações.

### MarkCacheEvictedMarks {#markcacheevictedmarks}

Número de marcas removidas do cache de marcas.

### MarkCacheHits {#markcachehits}

Número de vezes que uma entrada foi encontrada no cache de marcas, evitando a necessidade de carregar um arquivo de marcas.

### MarkCacheMisses {#markcachemisses}

Número de vezes em que uma entrada não foi encontrada no cache de marcas, e tivemos que carregar um arquivo de marca na memória, o que é uma operação custosa e aumenta a latência da consulta.

### MarksTasksFromCache {#markstasksfromcache}

Número de vezes em que as marcas foram carregadas de forma síncrona por já estarem presentes no cache.

### MemoryAllocatedWithoutCheck {#memoryallocatedwithoutcheck}

Número de vezes em que a memória foi alocada sem verificar os limites de memória.

### MemoryAllocatedWithoutCheckBytes {#memoryallocatedwithoutcheckbytes}

Quantidade de bytes alocados sem verificar as restrições de memória.

### MemoryAllocatorPurge {#memoryallocatorpurge}

Número total de vezes que a limpeza do alocador de memória foi solicitada

### MemoryAllocatorPurgeTimeMicroseconds {#memoryallocatorpurgetimemicroseconds}

Tempo total gasto na purga do alocador de memória

### MemoryOvercommitWaitTimeMicroseconds {#memoryovercommitwaittimemicroseconds}

Tempo total gasto aguardando a liberação de memória no OvercommitTracker.

### MemoryReservationAdmitMicroseconds {#memoryreservationadmitmicroseconds}

Tempo total que uma consulta ficou aguardando a reserva inicial de memória ser aprovada

### MemoryReservationDecreases {#memoryreservationdecreases}

Número total de solicitações aprovadas para redução de reserva de memória

### MemoryReservationFailed {#memoryreservationfailed}

Número total de reservas de memória que falharam

### MemoryReservationIncreaseMicroseconds {#memoryreservationincreasemicroseconds}

Tempo total que uma consulta passou aguardando a aprovação do aumento da reserva de memória

### MemoryReservationIncreases {#memoryreservationincreases}

Número total de solicitações aprovadas para aumento da reserva de memória

### MemoryReservationKilled {#memoryreservationkilled}

Número total de sinais de interrupção da reserva de memória

### MemoryWorkerRun {#memoryworkerrun}

Número de execuções do MemoryWorker em segundo plano

### MemoryWorkerRunElapsedMicroseconds {#memoryworkerrunelapsedmicroseconds}

Tempo total gasto pelo MemoryWorker em tarefas em segundo plano

### Merge {#merge}

Número de mesclagens em segundo plano iniciadas.

### MergeCommitMilliseconds {#mergecommitmilliseconds}

Tempo total gasto para efetivar os resultados da mesclagem (renomeação de partes, verificação de checksum, atualizações no ZooKeeper)

### MergedColumns {#mergedcolumns}

Número de colunas unificadas durante a etapa horizontal das operações de merge.

### MergedIntoCompactParts {#mergedintocompactparts}

Número de partes mescladas para o formato Compact.

### MergedIntoWideParts {#mergedintowideparts}

Número de partes mescladas para o formato Wide.

### MergedProjections {#mergedprojections}

Número de projeções mescladas (não reconstruídas) durante as mesclagens do MergeTree.

### MergedRows {#mergedrows}

Linhas lidas para mesclagens em segundo plano. Este é o número de linhas antes do merge.

### MergedUncompressedBytes {#mergeduncompressedbytes}

Bytes descomprimidos (das colunas, conforme armazenados na memória) lidos para mesclagens em segundo plano. Este é o valor antes do merge.

### MergeExecuteMilliseconds {#mergeexecutemilliseconds}

Tempo total de atividade gasto na execução de mesclagens em segundo plano

### MergeHorizontalStageExecuteMilliseconds {#mergehorizontalstageexecutemilliseconds}

Tempo total em atividade gasto na execução do estágio horizontal das mesclagens em segundo plano

### MergeHorizontalStageTotalMilliseconds {#mergehorizontalstagetotalmilliseconds}

Tempo total gasto na etapa horizontal das mesclagens em segundo plano

### MergeMutateBackgroundExecutorTaskCancelMicroseconds {#mergemutatebackgroundexecutortaskcancelmicroseconds}

Tempo gasto no cancel() das tarefas do executor MergeMutate.

### MergeMutateBackgroundExecutorTaskExecuteStepMicroseconds {#mergemutatebackgroundexecutortaskexecutestepmicroseconds}

Tempo gasto no `executeStep()` pelas tarefas do executor MergeMutate.

### MergeMutateBackgroundExecutorTaskResetMicroseconds {#mergemutatebackgroundexecutortaskresetmicroseconds}

Tempo gasto redefinindo a tarefa do executor MergeMutate.

### MergeMutateBackgroundExecutorWaitMicroseconds {#mergemutatebackgroundexecutorwaitmicroseconds}

Tempo gasto aguardando a conclusão no executor do MergeMutate.

### MergePrewarmStageExecuteMilliseconds {#mergeprewarmstageexecutemilliseconds}

Tempo total em atividade gasto na execução da etapa de pré-aquecimento das mesclagens em segundo plano

### MergePrewarmStageTotalMilliseconds {#mergeprewarmstagetotalmilliseconds}

Tempo total gasto na etapa de pré-aquecimento das mesclagens em segundo plano

### MergeProjectionStageExecuteMilliseconds {#mergeprojectionstageexecutemilliseconds}

Tempo total em atividade gasto na execução do estágio de projeção das mesclagens em segundo plano

### MergeProjectionStageTotalMilliseconds {#mergeprojectionstagetotalmilliseconds}

Tempo total gasto no estágio de projeção das mesclagens em segundo plano

### MergerMutatorPartsInRangesForMergeCount {#mergermutatorpartsinrangesformergecount}

Quantidade de partes candidatas à mesclagem

### MergerMutatorPrepareRangesForMergeElapsedMicroseconds {#mergermutatorpreparerangesformergeelapsedmicroseconds}

Tempo gasto para preparar os intervalos de partes que podem ser mescladas de acordo com o predicado de mesclagem.

### MergerMutatorRangesForMergeCount {#mergermutatorrangesformergecount}

Quantidade de intervalos candidatos para mesclagem

### MergerMutatorSelectPartsForMergeElapsedMicroseconds {#mergermutatorselectpartsformergeelapsedmicroseconds}

Tempo gasto para selecionar partes de intervalos que podem ser mescladas.

### MergerMutatorSelectRangePartsCount {#mergermutatorselectrangepartscount}

Quantidade de partes no intervalo selecionado para mesclagem

### MergerMutatorsGetPartsForMergeElapsedMicroseconds {#mergermutatorsgetpartsformergeelapsedmicroseconds}

Tempo gasto para obter um snapshot das partes de dados para criar intervalos a partir delas.

### MergeSourceParts {#mergesourceparts}

Número de partes de origem programadas para mesclagens.

### MergesRejectedByMemoryLimit {#mergesrejectedbymemorylimit}

Número de mesclagens em segundo plano rejeitadas devido ao limite de memória

### MergesThrottlerBytes {#mergesthrottlerbytes}

Bytes que passaram pelo limitador de taxa &#39;max&#95;merges&#95;bandwidth&#95;for&#95;server&#39;.

### MergesThrottlerSleepMicroseconds {#mergesthrottlersleepmicroseconds}

Tempo total em que uma consulta permaneceu em espera para obedecer à limitação de taxa de &#39;max&#95;merges&#95;bandwidth&#95;for&#95;server&#39;.

### MergeTextIndexStageExecuteMilliseconds {#mergetextindexstageexecutemilliseconds}

Tempo total ocupado na execução do estágio de índice de texto das mesclagens em segundo plano

### MergeTextIndexStageTotalMilliseconds {#mergetextindexstagetotalmilliseconds}

Tempo total gasto na etapa do índice de texto das mesclagens em segundo plano

### MergeTotalMilliseconds {#mergetotalmilliseconds}

Tempo total gasto em mesclagens em segundo plano

### MergeTreeAllRangesAnnouncementsSent {#mergetreeallrangesannouncementssent}

O número de anúncios enviados do servidor remoto ao servidor iniciador sobre o conjunto de partes de dados (para tabelas MergeTree). Medido no servidor remoto.

### MergeTreeAllRangesAnnouncementsSentElapsedMicroseconds {#mergetreeallrangesannouncementssentelapsedmicroseconds}

Tempo gasto para enviar o anúncio do servidor remoto ao servidor iniciador sobre o conjunto de partes de dados (para tabelas MergeTree). Medido no servidor remoto.

### MergeTreeDataProjectionWriterBlocks {#mergetreedataprojectionwriterblocks}

Número de blocos inseridos na projeção das tabelas MergeTree. Cada bloco forma uma parte de dados de nível zero.

### MergeTreeDataProjectionWriterBlocksAlreadySorted {#mergetreedataprojectionwriterblocksalreadysorted}

Número de blocos inseridos na projeção das tabelas MergeTree que aparentavam já estar ordenados.

### MergeTreeDataProjectionWriterCompressedBytes {#mergetreedataprojectionwritercompressedbytes}

Bytes gravados no sistema de arquivos para dados inseridos nas projeções de tabelas MergeTree.

### MergeTreeDataProjectionWriterMergingBlocksMicroseconds {#mergetreedataprojectionwritermergingblocksmicroseconds}

Tempo gasto mesclando blocos

### MergeTreeDataProjectionWriterRows {#mergetreedataprojectionwriterrows}

Número de linhas inseridas na projeção das tabelas MergeTree.

### MergeTreeDataProjectionWriterSortingBlocksMicroseconds {#mergetreedataprojectionwritersortingblocksmicroseconds}

Tempo gasto para ordenar blocos (para a projeção, pode ser uma chave diferente da chave de ordenação da tabela)

### MergeTreeDataProjectionWriterUncompressedBytes {#mergetreedataprojectionwriteruncompressedbytes}

Bytes não compactados (das colunas, conforme são armazenadas na memória) inseridos na projeção de tabelas MergeTree.

### MergeTreeDataWriterBlocks {#mergetreedatawriterblocks}

Número de blocos inseridos em tabelas MergeTree. Cada bloco forma uma parte de dados de nível zero.

### MergeTreeDataWriterBlocksAlreadySorted {#mergetreedatawriterblocksalreadysorted}

Número de blocos inseridos em tabelas MergeTree que aparentemente já estavam ordenados.

### MergeTreeDataWriterCompressedBytes {#mergetreedatawritercompressedbytes}

Bytes gravados no sistema de arquivos para dados inseridos nas tabelas MergeTree.

### MergeTreeDataWriterMergingBlocksMicroseconds {#mergetreedatawritermergingblocksmicroseconds}

Tempo gasto na mesclagem de blocos de entrada (para motores especiais do MergeTree)

### MergeTreeDataWriterProjectionsCalculationMicroseconds {#mergetreedatawriterprojectionscalculationmicroseconds}

Tempo gasto para calcular projeções

### MergeTreeDataWriterRows {#mergetreedatawriterrows}

Número de linhas inseridas em tabelas MergeTree.

### MergeTreeDataWriterSkipIndicesCalculationMicroseconds {#mergetreedatawriterskipindicescalculationmicroseconds}

Tempo gasto no cálculo de índices de salto

### MergeTreeDataWriterSortingBlocksMicroseconds {#mergetreedatawritersortingblocksmicroseconds}

Tempo gasto na ordenação dos blocos

### MergeTreeDataWriterStatisticsCalculationMicroseconds {#mergetreedatawriterstatisticscalculationmicroseconds}

Tempo gasto no cálculo das estatísticas

### MergeTreeDataWriterUncompressedBytes {#mergetreedatawriteruncompressedbytes}

Bytes não compactados (das colunas conforme são armazenadas na memória) inseridos em tabelas MergeTree.

### MergeTreePrefetchedReadPoolInit {#mergetreeprefetchedreadpoolinit}

Tempo gasto na preparação de tarefas no MergeTreePrefetchedReadPool

### MergeTreeReadTaskRequestsReceived {#mergetreereadtaskrequestsreceived}

O número de callbacks solicitados do servidor remoto ao servidor iniciador para selecionar a tarefa de leitura (para tabelas MergeTree). Medido no servidor iniciador.

### MergeTreeReadTaskRequestsSent {#mergetreereadtaskrequestssent}

O número de callbacks solicitados do servidor remoto para o servidor iniciador para escolher a tarefa de leitura (para tabelas MergeTree). Medido no servidor remoto.

### MergeTreeReadTaskRequestsSentElapsedMicroseconds {#mergetreereadtaskrequestssentelapsedmicroseconds}

Tempo gasto em callbacks solicitadas pelo servidor remoto ao servidor iniciador para escolher a tarefa de leitura (para tabelas MergeTree). Medido no lado do servidor remoto.

### MergeVerticalStageExecuteMilliseconds {#mergeverticalstageexecutemilliseconds}

Tempo total em atividade gasto na execução da etapa vertical das mesclagens em segundo plano

### MergeVerticalStageTotalMilliseconds {#mergeverticalstagetotalmilliseconds}

Tempo total gasto na etapa vertical das mesclagens em segundo plano

### MergeWrittenRows {#mergewrittenrows}

Número de linhas gravadas durante a mesclagem.

### MergingSortedMilliseconds {#mergingsortedmilliseconds}

Tempo total gasto na mesclagem de colunas ordenadas

### MetadataFromKeeperBackgroundCleanupBlobs {#metadatafromkeeperbackgroundcleanupblobs}

Número de vezes que uma parte vazia do layout de blob foi removida por uma tarefa em segundo plano

### MetadataFromKeeperBackgroundCleanupErrors {#metadatafromkeeperbackgroundcleanuperrors}

Número de vezes em que ocorreu um erro na tarefa de limpeza em segundo plano

### MetadataFromKeeperBackgroundCleanupObjects {#metadatafromkeeperbackgroundcleanupobjects}

Número de vezes em que a limpeza de um objeto excluído antigo foi executada por uma tarefa em segundo plano

### MetadataFromKeeperBackgroundCleanupTransactions {#metadatafromkeeperbackgroundcleanuptransactions}

Número de vezes que o token de idempotência de transação antigo foi removido pela tarefa em segundo plano

### MetadataFromKeeperCacheHit {#metadatafromkeepercachehit}

Número de vezes que uma solicitação de metadados do armazenamento de objetos foi atendida pelo cache sem fazer uma solicitação ao Keeper

### MetadataFromKeeperCacheMiss {#metadatafromkeepercachemiss}

Número de vezes em que uma solicitação de metadados do armazenamento de objetos precisou ser atendida pelo Keeper

### MetadataFromKeeperCacheTooManyInvalidated {#metadatafromkeepercachetoomanyinvalidated}

Número de vezes que o cache do sistema de arquivos retornou entradas inválidas em excesso

### MetadataFromKeeperCacheUpdateMicroseconds {#metadatafromkeepercacheupdatemicroseconds}

Tempo total gasto na atualização do cache, incluindo o tempo de espera por respostas do Keeper

### MetadataFromKeeperCleanupTransactionCommit {#metadatafromkeepercleanuptransactioncommit}

Número de vezes que se tentou fazer o commit da transação de metadados para a limpeza de objetos excluídos

### MetadataFromKeeperCleanupTransactionCommitRetry {#metadatafromkeepercleanuptransactioncommitretry}

Número de vezes que houve nova tentativa do commit da transação de metadados para a limpeza de objetos excluídos

### MetadataFromKeeperIndividualOperations {#metadatafromkeeperindividualoperations}

Número de caminhos lidos ou gravados por requisições simples ou múltiplas ao Keeper

### MetadataFromKeeperIndividualOperationsMicroseconds {#metadatafromkeeperindividualoperationsmicroseconds}

Tempo gasto em requisições únicas ou múltiplas ao Keeper

### MetadataFromKeeperOperations {#metadatafromkeeperoperations}

Número de vezes que uma solicitação foi feita ao Keeper

### MetadataFromKeeperReconnects {#metadatafromkeeperreconnects}

Número de vezes em que foi feita uma reconexão ao Keeper

### MetadataFromKeeperTransactionCommit {#metadatafromkeepertransactioncommit}

Número de vezes em que houve tentativa de commit da transação de metadados

### MetadataFromKeeperTransactionCommitRetry {#metadatafromkeepertransactioncommitretry}

Número de vezes que a confirmação da transação de metadados foi repetida

### MetadataFromKeeperUpdateCacheOneLevel {#metadatafromkeeperupdatecacheonelevel}

Número de vezes em que foi feita uma atualização do cache para um nível da árvore de diretórios

### MetadataTransactionRollbacks {#metadatatransactionrollbacks}

Número de transações de metadados que falharam ao fazer commit e foram revertidas.

### MetadataTransactionRollbacksFailed {#metadatatransactionrollbacksfailed}

Número de rollbacks de transações de metadados que não foram concluídos até o fim, de modo que os metadados retêm parte de uma transação que foi reportada como falha.

### MMappedFileCacheHits {#mmappedfilecachehits}

Número de vezes que um arquivo foi encontrado no cache MMap (para o `read_method` &#39;mmap&#39;), de modo que não foi necessário fazer o mmap novamente.

### MMappedFileCacheMisses {#mmappedfilecachemisses}

Número de vezes em que um arquivo não foi encontrado no cache MMap (para o `read_method` &#39;mmap&#39;), portanto foi necessário mapeá-lo em memória novamente.

### MoveBackgroundExecutorTaskCancelMicroseconds {#movebackgroundexecutortaskcancelmicroseconds}

Tempo gasto no `cancel()` para tarefas do executor Move.

### MoveBackgroundExecutorTaskExecuteStepMicroseconds {#movebackgroundexecutortaskexecutestepmicroseconds}

Tempo gasto em `executeStep()` nas tarefas do executor Move.

### MoveBackgroundExecutorTaskResetMicroseconds {#movebackgroundexecutortaskresetmicroseconds}

Tempo gasto ao redefinir a tarefa do executor Move.

### MoveBackgroundExecutorWaitMicroseconds {#movebackgroundexecutorwaitmicroseconds}

Tempo gasto aguardando a conclusão no executor de Move.

### MutatedRows {#mutatedrows}

Número de linhas lidas para mutações. Este é o número de linhas antes da mutação

### MutatedUncompressedBytes {#mutateduncompressedbytes}

Bytes não compactados (das colunas, conforme armazenadas na memória) lidos para as mutações. Esse é o valor antes da mutação.

### MutateTaskProjectionsCalculationMicroseconds {#mutatetaskprojectionscalculationmicroseconds}

Tempo gasto no cálculo de projeções em mutações

### MutationAffectedRowsUpperBound {#mutationaffectedrowsupperbound}

O limite superior do número de linhas afetadas pela mutação (por exemplo, o número de linhas que satisfazem o predicado da mutação de UPDATE ou DELETE). O número real pode ser ligeiramente menor

### MutationAllPartColumns {#mutationallpartcolumns}

Número de vezes em que foi criada a tarefa de aplicar mutação a todas as colunas na parte

### MutationCommitMilliseconds {#mutationcommitmilliseconds}

Tempo total gasto para efetivar os resultados da mutação (renomeação de partes, verificação de checksum, atualizações no ZooKeeper)

### MutationCreatedEmptyParts {#mutationcreatedemptyparts}

Número total de partes substituídas por partes vazias em vez de executar a mutação

### MutationExecuteMilliseconds {#mutationexecutemilliseconds}

Tempo total em atividade gasto na execução de mutações.

### MutationsAppliedOnFlyInAllReadTasks {#mutationsappliedonflyinallreadtasks}

Número total de mutações aplicadas em tempo real em todas as tarefas de leitura

### MutationSomePartColumns {#mutationsomepartcolumns}

Número de vezes em que foi criada uma tarefa para modificar algumas colunas em uma parte

### MutationsThrottlerBytes {#mutationsthrottlerbytes}

Bytes processados pelo limitador &#39;max&#95;mutations&#95;bandwidth&#95;for&#95;server&#39;.

### MutationsThrottlerSleepMicroseconds {#mutationsthrottlersleepmicroseconds}

Tempo total em que uma consulta ficou em espera para se adequar à limitação de taxa de &#39;max&#95;mutations&#95;bandwidth&#95;for&#95;server&#39;.

### MutationTotalMilliseconds {#mutationtotalmilliseconds}

Tempo total gasto com mutações.

### MutationTotalParts {#mutationtotalparts}

Número total de partes às quais se tentou aplicar mutações

### MutationUntouchedParts {#mutationuntouchedparts}

Número total de partes às quais se tentou aplicar mutações, mas que foram totalmente ignoradas com base no predicado

### NativeProtocolSend {#nativeprotocolsend}

Número de flushes não vazios do buffer de saída do protocolo nativo.

### NetworkReceiveBytes {#networkreceivebytes}

Número total de bytes recebidos da rede. Inclui apenas a interação de rede relacionada ao ClickHouse, não a realizada por bibliotecas de terceiros.

### NetworkReceiveElapsedMicroseconds {#networkreceiveelapsedmicroseconds}

Tempo total gasto aguardando o recebimento de dados ou recebendo dados pela rede. Apenas a interação de rede relacionada ao ClickHouse está incluída, não a de bibliotecas de terceiros.

### NetworkSendBytes {#networksendbytes}

Número total de bytes enviados pela rede. Inclui apenas a interação de rede relacionada ao ClickHouse, não a de bibliotecas de terceiros.

### NetworkSendElapsedMicroseconds {#networksendelapsedmicroseconds}

Tempo total gasto aguardando o envio de dados pela rede ou enviando dados pela rede. Apenas a interação de rede relacionada ao ClickHouse está incluída, não a realizada por bibliotecas de terceiros.

### Entrada de log não criada para merge {#notcreatedlogentryformerge}

A entrada de log para realizar o merge de partes no ReplicatedMergeTree não é criada devido a uma atualização concorrente do log por outra réplica.

### NotCreatedLogEntryForMutation {#notcreatedlogentryformutation}

A entrada de log para aplicar mutação às partes no ReplicatedMergeTree não é criada devido a uma atualização concorrente do log por outra réplica.

### ObjectStorageGlobFilteredObjects {#objectstorageglobfilteredobjects}

Objetos que não corresponderam ao padrão glob nem à expressão regular e foram ignorados durante a listagem.

### ObjectStorageListedObjects {#objectstoragelistedobjects}

Total de objetos retornados pela API de listagem do armazenamento de objetos antes de qualquer filtro.

### ObjectStoragePredicateFilteredObjects {#objectstoragepredicatefilteredobjects}

Objetos removidos pela filtragem por predicado nas colunas virtuais &#95;path/&#95;file.

### ObjectStorageQueueBucketLockLostOwnership {#objectstoragequeuebucketlocklostownership}

Número de vezes em que se detectou a perda da propriedade de um bloqueio do bucket no S3(Azure)Queue. Um valor diferente de zero indica um valor muito baixo para persistent&#95;processing&#95;node&#95;ttl&#95;seconds ou um bug

### ObjectStorageQueueBucketLockRefreshes {#objectstoragequeuebucketlockrefreshes}

Número de renovações bem-sucedidas do bloqueio do bucket no S3(Azure)Queue

### ObjectStorageQueueCancelledFiles {#objectstoragequeuecancelledfiles}

Número de arquivos cancelados na StorageS3(Azure)Queue

### ObjectStorageQueueCleanupMaxSetSizeOrTTLMicroseconds {#objectstoragequeuecleanupmaxsetsizeorttlmicroseconds}

Tempo gasto para marcar o arquivo como falho

### ObjectStorageQueueCommitRequests {#objectstoragequeuecommitrequests}

Número de solicitações ao Keeper para marcar arquivos como com falha ou processados

### ObjectStorageQueueExceptionsDuringInsert {#objectstoragequeueexceptionsduringinsert}

Número de exceções durante a inserção em S3(Azure)Queue

### Exceções de ObjectStorageQueue durante a leitura {#objectstoragequeueexceptionsduringread}

Número de exceções durante a leitura no S3(Azure)Queue

### ObjectStorageQueueExclusiveModeProcessingErrors {#objectstoragequeueexclusivemodeprocessingerrors}

Número de vezes que um arquivo em S3(Azure)Queue não finalizou o estado devido a um erro de processamento. Apenas para o modo &#39;exclusive&#39;.

### ObjectStorageQueueFailedFiles {#objectstoragequeuefailedfiles}

Número de arquivos cujo processamento falhou

### ObjectStorageQueueFailedToBatchSetProcessing {#objectstoragequeuefailedtobatchsetprocessing}

Número de vezes que a solicitação de processamento em lote do Set falhou

### ObjectStorageQueueFilteredFiles {#objectstoragequeuefilteredfiles}

Número de arquivos filtrados na StorageS3(Azure)Queue

### ObjectStorageQueueInsertIterations {#objectstoragequeueinsertiterations}

Número de iterações de inserção

### ObjectStorageQueueListedFiles {#objectstoragequeuelistedfiles}

Número de arquivos listados na StorageS3(Azure)Queue

### ObjectStorageQueueMovedObjects {#objectstoragequeuemovedobjects}

Número de objetos movidos quando after&#95;processing = move

### ObjectStorageQueueProcessedFiles {#objectstoragequeueprocessedfiles}

Número de arquivos processados

### ObjectStorageQueueProcessedRows {#objectstoragequeueprocessedrows}

Número de linhas processadas no StorageS3(Azure)Queue

### ObjectStorageQueuePullMicroseconds {#objectstoragequeuepullmicroseconds}

Tempo gasto para ler os dados do arquivo

### ObjectStorageQueueReadBytes {#objectstoragequeuereadbytes}

Número de bytes lidos (não corresponde ao número de bytes efetivamente inseridos)

### ObjectStorageQueueReadFiles {#objectstoragequeuereadfiles}

Número de arquivos lidos (não é igual ao número de arquivos efetivamente inseridos)

### ObjectStorageQueueReadRows {#objectstoragequeuereadrows}

Número de linhas lidas (não corresponde ao número de linhas realmente inseridas)

### ObjectStorageQueueRemovedObjects {#objectstoragequeueremovedobjects}

Número de objetos removidos quando after&#95;processing = delete

### ObjectStorageQueueRemoveObjectFailures {#objectstoragequeueremoveobjectfailures}

Número de objetos que não puderam ser removidos como parte de after&#95;processing = delete

### ObjectStorageQueueSuccessfulCommits {#objectstoragequeuesuccessfulcommits}

Número de commits bem-sucedidos no Keeper

### ObjectStorageQueueTaggedObjects {#objectstoragequeuetaggedobjects}

Número de objetos marcados quando after&#95;processing = tag

### ObjectStorageQueueTrySetProcessingFailed {#objectstoragequeuetrysetprocessingfailed}

O número de vezes em que não foi possível definir o arquivo como processing

### ObjectStorageQueueTrySetProcessingRequests {#objectstoragequeuetrysetprocessingrequests}

O número de vezes que tentamos enviar a solicitação para marcar como processing

### ObjectStorageQueueTrySetProcessingSucceeded {#objectstoragequeuetrysetprocessingsucceeded}

O número de vezes em que conseguimos marcar o arquivo como processing

### ObjectStorageQueueUnsuccessfulCommits {#objectstoragequeueunsuccessfulcommits}

Número de commits malsucedidos no Keeper

### ObjectStorageReadObjects {#objectstoragereadobjects}

Objetos efetivamente abertos para leitura a partir do armazenamento de objetos.

### ObsoleteReplicatedParts {#obsoletereplicatedparts}

Número de vezes que uma parte de dados foi coberta por outra parte de dados obtida de uma réplica (ou seja, marcamos a parte de dados coberta como obsoleta e não mais necessária).

### OneLakeAccessTokenExpirations {#onelakeaccesstokenexpirations}

Número de vezes em que o token de acesso do OneLake em cache foi identificado como expirado e renovado de forma transparente.

### OneLakeAccessTokenRequestFailures {#onelakeaccesstokenrequestfailures}

Número de requisições de token de acesso malsucedidas emitidas pelo catálogo OneLake por meio da concessão de refresh token do Entra ID (incluindo refresh tokens expirados ou revogados).

### OneLakeAccessTokenRequestMicroseconds {#onelakeaccesstokenrequestmicroseconds}

Tempo total gasto na solicitação de tokens de acesso por meio da concessão de refresh token do Entra ID no catálogo do OneLake.

### OneLakeAccessTokenRequests {#onelakeaccesstokenrequests}

Número de requisições de token de acesso emitidas pelo catálogo OneLake por meio da concessão de refresh token do Entra ID.

### Acertos no cache de arquivos abertos {#openedfilecachehits}

Número de vezes em que um arquivo foi encontrado no cache de arquivos abertos, sem precisar ser aberto novamente.

### OpenedFileCacheMicroseconds {#openedfilecachemicroseconds}

Tempo gasto na execução de métodos do OpenedFileCache.

### OpenedFileCacheMisses {#openedfilecachemisses}

Número de vezes que um arquivo não foi encontrado no cache de arquivos abertos e precisou ser aberto novamente.

### OSCPUVirtualTimeMicroseconds {#oscpuvirtualtimemicroseconds}

Tempo de CPU consumido, conforme visto pelo SO. Não inclui esperas involuntárias devido à virtualização.

### OSCPUWaitMicroseconds {#oscpuwaitmicroseconds}

Tempo total em que uma thread estava pronta para execução, mas aguardava ser escalonada pelo SO, na perspectiva do SO.

### OSIOWaitMicroseconds {#osiowaitmicroseconds}

Tempo total que uma thread ficou aguardando o resultado de uma operação de IO, do ponto de vista do SO. Trata-se de IO real e não inclui o cache de página.

### OSReadBytes {#osreadbytes}

Número de bytes lidos de discos ou dispositivos de bloco. Não inclui bytes lidos do cache de página. Pode incluir dados excedentes devido ao tamanho do bloco, à leitura antecipada etc.

### OSReadChars {#osreadchars}

Número de bytes lidos do sistema de arquivos, inclusive do cache de páginas, além da rede e de outros arquivos.

### OSWriteBytes {#oswritebytes}

Número de bytes gravados em discos ou dispositivos de bloco. Não inclui bytes em páginas sujas do cache de página. Pode não incluir dados gravados pelo SO de forma assíncrona.

### OSWriteChars {#oswritechars}

Número de bytes gravados no sistema de arquivos, inclusive no cache de páginas, bem como na rede e em outros arquivos.

### OtherQueryTimeMicroseconds {#otherquerytimemicroseconds}

Tempo total das consultas que não são SELECT nem INSERT.

### OverflowAny {#overflowany}

Número de vezes em que o GROUP BY aproximado foi aplicado: quando a agregação foi realizada apenas nas primeiras &#39;max&#95;rows&#95;to&#95;group&#95;by&#39; chaves únicas, e as demais chaves foram ignoradas devido a &#39;group&#95;by&#95;overflow&#95;mode&#39; = &#39;any&#39;.

### OverflowBreak {#overflowbreak}

Número de vezes em que o processamento de dados foi interrompido pela limitação de complexidade da consulta com a configuração &#39;*&#95;overflow&#95;mode&#39; = &#39;break&#39;, e o resultado ficou incompleto.

### OverflowThrow {#overflowthrow}

Número de vezes em que o processamento de dados foi cancelado pela limitação de complexidade da consulta com a configuração &#39;*&#95;overflow&#95;mode&#39; = &#39;throw&#39; e uma exceção foi lançada.

### PageCacheHits {#pagecachehits}

Número de vezes em que um bloco de dados foi encontrado no cache de páginas em espaço de usuário.

### PageCacheMisses {#pagecachemisses}

Número de vezes que um bloco de dados não foi encontrado no cache de páginas em espaço de usuário.

### PageCacheOvercommitResize {#pagecacheovercommitresize}

Número de vezes em que o cache de páginas em espaço de usuário foi redimensionado automaticamente para liberar memória durante uma alocação de memória.

### PageCacheReadBytes {#pagecachereadbytes}

Número de bytes lidos do cache de páginas no espaço do usuário.

### PageCacheResized {#pagecacheresized}

Número de vezes que o cache de páginas em espaço de usuário foi redimensionado automaticamente (isso normalmente acontece algumas vezes por segundo, controlado por `memory_worker_period_ms`).

### PageCacheWeightLost {#pagecacheweightlost}

Número de bytes removidos do cache de páginas em espaço de usuário

### PaimonMetadataFilesCacheHits {#paimonmetadatafilescachehits}

Número de vezes que arquivos de metadados do Paimon foram encontrados no cache.

### PaimonMetadataFilesCacheMisses {#paimonmetadatafilescachemisses}

Número de vezes que arquivos de metadados do Paimon não foram encontrados no cache de metadados do Paimon e precisaram ser lidos do disco (remoto).

### PaimonMetadataFilesCacheWeightLost {#paimonmetadatafilescacheweightlost}

Número aproximado de bytes removidos do cache de metadados do Paimon.

### ParallelReplicasAnnouncementMicroseconds {#parallelreplicasannouncementmicroseconds}

Tempo gasto no envio de um anúncio

### ParallelReplicasAvailableCount {#parallelreplicasavailablecount}

Número de réplicas disponíveis para executar uma consulta usando réplicas paralelas baseadas em tarefas

### ParallelReplicasCollectingOwnedSegmentsMicroseconds {#parallelreplicascollectingownedsegmentsmicroseconds}

Tempo gasto na coleta de segmentos determinados por hash

### ParallelReplicasDeniedRequests {#parallelreplicasdeniedrequests}

Número de requisições totalmente negadas ao iniciador

### ParallelReplicasHandleAnnouncementMicroseconds {#parallelreplicashandleannouncementmicroseconds}

Tempo gasto no processamento de anúncios das réplicas

### ParallelReplicasHandleRequestMicroseconds {#parallelreplicashandlerequestmicroseconds}

Tempo gasto processando solicitações de marcas de réplicas

### ParallelReplicasNumRequests {#parallelreplicasnumrequests}

Número de solicitações para o nó iniciador.

### ParallelReplicasProcessingPartsMicroseconds {#parallelreplicasprocessingpartsmicroseconds}

Tempo gasto no processamento de partes de dados

### ParallelReplicasQueryCount {#parallelreplicasquerycount}

Número de (sub)consultas executadas com réplicas paralelas durante a execução de uma consulta

### ParallelReplicasReadAssignedForStealingMarks {#parallelreplicasreadassignedforstealingmarks}

Soma, em todas as réplicas, de quantas marcas agendadas foram atribuídas para roubo via hash consistente

### ParallelReplicasReadAssignedMarks {#parallelreplicasreadassignedmarks}

Soma, em todas as réplicas, de quantas marcas agendadas foram atribuídas por hash consistente

### ParallelReplicasReadMarks {#parallelreplicasreadmarks}

Quantas marcas foram lidas pela réplica especificada

### ParallelReplicasReadRequestMicroseconds {#parallelreplicasreadrequestmicroseconds}

Tempo gasto em solicitações de leitura

### ParallelReplicasReadUnassignedMarks {#parallelreplicasreadunassignedmarks}

Soma, em todas as réplicas, de quantas marcas não atribuídas foram programadas

### ParallelReplicasStealingByHashMicroseconds {#parallelreplicasstealingbyhashmicroseconds}

Tempo gasto coletando segmentos destinados ao roubo por hash

### ParallelReplicasStealingLeftoversMicroseconds {#parallelreplicasstealingleftoversmicroseconds}

Tempo gasto na coleta de segmentos órfãos

### ParallelReplicasUnavailableCount {#parallelreplicasunavailablecount}

Número de réplicas que foram escolhidas, mas estavam indisponíveis durante a execução da consulta com réplicas paralelas baseadas em tarefas

### ParallelReplicasUsedCount {#parallelreplicasusedcount}

Número de réplicas usadas para executar uma consulta com réplicas paralelas baseadas em tarefas

### ParquetColumnsFilterExpression {#parquetcolumnsfilterexpression}

Número total de colunas que passaram pelo filtro

### ParquetDecodingTaskBatches {#parquetdecodingtaskbatches}

Grupos de tarefas que o leitor de Parquet envia a uma thread pool

### ParquetDecodingTasks {#parquetdecodingtasks}

Tarefas geradas pelo leitor de Parquet

### ParquetFetchWaitTimeMicroseconds {#parquetfetchwaittimemicroseconds}

Tempo de espera por leituras de arquivos Parquet pelas threads de decodificação (não pelas threads de prefetch)

### ParquetMetadataCacheHits {#parquetmetadatacachehits}

Número de vezes em que os metadados do Parquet foram encontrados em cache.

### ParquetMetadataCacheMisses {#parquetmetadatacachemisses}

Número de vezes em que os metadados do Parquet não foram encontrados no cache e precisaram ser lidos do disco.

### ParquetMetadataCacheWeightLost {#parquetmetadatacacheweightlost}

Número aproximado de bytes removidos do cache de metadados do Parquet.

### ParquetPrefetcherReadEntireFile {#parquetprefetcherreadentirefile}

O número total de leituras usando ReadMode::EntireFileIsInMemory pelo DB::Parquet::Prefetcher

### ParquetPrefetcherReadRandomRead {#parquetprefetcherreadrandomread}

O número total de leituras com ReadMode::RandomRead feitas pelo DB::Parquet::Prefetcher

### ParquetPrefetcherReadSeekAndRead {#parquetprefetcherreadseekandread}

O número total de leituras com ReadMode::SeekAndRead realizadas pelo DB::Parquet::Prefetcher

### ParquetPrunedPages {#parquetprunedpages}

O número total de pages descartadas dos dados Parquet por meio do índice de coluna

### ParquetPrunedRowGroups {#parquetprunedrowgroups}

O número total de grupos de linhas descartados dos dados Parquet

### ParquetReadPages {#parquetreadpages}

O número total de pages de dados Parquet lidas

### ParquetReadRowGroups {#parquetreadrowgroups}

O número total de grupos de linhas lidos dos dados Parquet

### ParquetRowsFilterExpression {#parquetrowsfilterexpression}

O número total de linhas que passaram pelo filtro

### PartsLockHoldMicroseconds {#partslockholdmicroseconds}

Tempo total em que o bloqueio das partes de dados foi mantido em tabelas MergeTree

### PartsLocks {#partslocks}

Número de vezes em que o bloqueio das partes de dados foi adquirido para tabelas MergeTree

### PartsLockWaitMicroseconds {#partslockwaitmicroseconds}

Tempo total de espera pelo bloqueio de partes de dados em tabelas MergeTree

### PatchesAcquireLockBadVersionRetries {#patchesacquirelockbadversionretries}

Número total de novas tentativas de aquisição do bloqueio para atualizações leves causadas por uma atualização concorrente que fez commit primeiro

### PatchesAcquireLockMicroseconds {#patchesacquirelockmicroseconds}

Número total de microssegundos gastos para adquirir o bloqueio a fim de executar atualizações leves

### PatchesAcquireLockTries {#patchesacquirelocktries}

Número total de tentativas de obter o bloqueio para executar atualizações leves

### PatchesAppliedInAllReadTasks {#patchesappliedinallreadtasks}

Número total de partes de patch aplicadas em todas as tarefas de leitura

### PatchesJoinAppliedInAllReadTasks {#patchesjoinappliedinallreadtasks}

Número total de partes de patch aplicadas no modo Join em todas as tarefas de leitura

### PatchesJoinRowsAddedToHashTable {#patchesjoinrowsaddedtohashtable}

Número total de linhas adicionadas a tabelas hash ao aplicar partes de patch no modo Join

### PatchesMergeAppliedInAllReadTasks {#patchesmergeappliedinallreadtasks}

Número total de partes de patch aplicadas no modo Merge em todas as tarefas de leitura

### PatchesMergeOnKeyAppliedInAllReadTasks {#patchesmergeonkeyappliedinallreadtasks}

Número total de partes de patch aplicadas com o modo MergeOnKey (v2) entre todas as tarefas de leitura

### PatchesReadRows {#patchesreadrows}

Número total de linhas lidas de partes de patch

### PatchesReadUncompressedBytes {#patchesreaduncompressedbytes}

Número total de bytes não compactados lidos a partir de partes de patch

### Falhas de alinhamento {#perfalignmentfaults}

Número de falhas de alinhamento. Elas ocorrem quando há acessos à memória não alinhados; o kernel consegue lidar com isso, mas há perda de desempenho. Isso ocorre apenas em algumas arquiteturas (nunca em x86).

### PerfBranchInstructions {#perfbranchinstructions}

Instruções de desvio concluídas. Antes do Linux 2.6.35, isso usava o evento incorreto em processadores AMD.

### PerfBranchMisses {#perfbranchmisses}

Instruções de desvio com previsão incorreta.

### PerfBusCycles {#perfbuscycles}

Ciclos de barramento, que podem ser diferentes do número total de ciclos.

### PerfCacheMisses {#perfcachemisses}

Falhas de cache. Em geral, isso indica falhas na Last Level Cache; deve ser usado em conjunto com o evento PERFCOUNTHWCACHEREFERENCES para calcular as taxas de falha de cache.

### PerfCacheReferences {#perfcachereferences}

Acessos ao cache. Em geral, isso indica acessos ao cache de último nível, mas isso pode variar dependendo da sua CPU. Isso pode incluir prefetches e mensagens de coerência; novamente, isso depende da arquitetura da sua CPU.

### PerfContextSwitches {#perfcontextswitches}

Número de trocas de contexto

### PerfCPUClock {#perfcpuclock}

O clock da CPU, um temporizador de alta resolução para cada CPU

### Ciclos de CPU {#perfcpucycles}

Ciclos totais. Fique atento ao que acontece durante o escalonamento da frequência da CPU.

### PerfCPUMigrations {#perfcpumigrations}

Número de vezes que o processo foi migrado para uma nova CPU

### PerfDataTLBMisses {#perfdatatlbmisses}

Falhas no TLB de dados

### PerfDataTLBReferences {#perfdatatlbreferences}

Referências ao TLB de dados

### PerfEmulationFaults {#perfemulationfaults}

Número de falhas de emulação. Às vezes, o kernel intercepta instruções não implementadas e as emula no espaço do usuário. Isso pode afetar negativamente o desempenho.

### PerfInstructions {#perfinstructions}

Instruções executadas. Tenha cuidado, pois isso pode ser afetado por vários fatores, principalmente pela contagem de interrupções de hardware.

### PerfInstructionTLBMisses {#perfinstructiontlbmisses}

Falhas no TLB de instruções

### Referências ao TLB de instruções {#perfinstructiontlbreferences}

Referências ao TLB de instruções

### PerfLocalMemoryMisses {#perflocalmemorymisses}

Falhas de leitura na memória do nó NUMA local

### PerfLocalMemoryReferences {#perflocalmemoryreferences}

Leituras da memória do nó NUMA local

### PerfMinEnabledRunningTime {#perfminenabledrunningtime}

Tempo de execução do evento com o menor tempo em que esteve habilitado. Usado para acompanhar o nível de multiplexação de eventos

### PerfMinEnabledTime {#perfminenabledtime}

Para todos os eventos, tempo mínimo durante o qual um evento permaneceu habilitado. Usado para acompanhar o efeito da multiplexação de eventos

### PerfRefCPUCycles {#perfrefcpucycles}

Ciclos totais; não são afetados pelo escalonamento da frequência da CPU.

### PerfStalledCyclesBackend {#perfstalledcyclesbackend}

Ciclos parados durante a retirada de instruções.

### PerfStalledCyclesFrontend {#perfstalledcyclesfrontend}

Ciclos estagnados durante a emissão.

### PerfTaskClock {#perftaskclock}

Uma contagem de ciclos de clock específica da tarefa em execução

### PreferredWarmedUnmergedParts {#preferredwarmedunmergedparts}

Veja a configuração `prefer_warmed_unmerged_parts_seconds`. Número de vezes em que consultas de leitura usaram partes desatualizadas antes do merge que estão no cache, em vez da parte mesclada que o CacheWarmer ainda não trouxe para o cache.

### PrimaryIndexCacheHits {#primaryindexcachehits}

Número de vezes que uma entrada foi encontrada no cache do índice primário, de modo que não foi necessário carregar um arquivo de índice.

### PrimaryIndexCacheMisses {#primaryindexcachemisses}

Número de vezes em que uma entrada não foi encontrada no cache de índice primário, o que exigiu carregar um arquivo de índice na memória — uma operação custosa que aumenta a latência da consulta.

### QueriesWithSubqueries {#querieswithsubqueries}

Número de consultas com todas as subconsultas

### Consulta {#query}

Número de consultas a serem interpretadas e potencialmente executadas. Não inclui consultas que não puderam ser analisadas ou que foram rejeitadas devido a limites de tamanho da AST, limites de quota ou limites no número de consultas executadas simultaneamente. Pode incluir consultas internas iniciadas pelo próprio ClickHouse. Não contabiliza subconsultas.

### QueryAnalysisMicroseconds {#queryanalysismicroseconds}

Tempo total gasto para construir a árvore de consulta e executar as etapas do analyzer (resolução de identificadores, tipos e expressões).

### QueryBackupThrottlerBytes {#querybackupthrottlerbytes}

Bytes que passaram pelo limitador &#39;max&#95;backup&#95;bandwidth&#39;.

### QueryBackupThrottlerSleepMicroseconds {#querybackupthrottlersleepmicroseconds}

Tempo total em que uma consulta ficou em espera para obedecer ao throttling de &#39;max&#95;backup&#95;bandwidth&#39;.

### QueryCacheAgeSeconds {#querycacheageseconds}

A soma, em segundos, da idade das entradas encontradas no cache de consultas. O valor é definido tanto para acertos quanto para falhas.

### QueryCacheHits {#querycachehits}

Número de vezes em que o resultado de uma consulta foi encontrado no cache de consultas (evitando o processamento da consulta). Atualizado apenas para consultas SELECT com SETTING use&#95;query&#95;cache = 1.

### QueryCacheMisses {#querycachemisses}

Número de vezes que o resultado de uma consulta não foi encontrado no cache de consultas (e exigiu o processamento da consulta). Atualizado apenas para consultas SELECT com SETTING use&#95;query&#95;cache = 1.

### QueryCacheReadBytes {#querycachereadbytes}

O número de bytes (descompactados) lidos do cache de consultas.

### QueryCacheReadRows {#querycachereadrows}

O número de linhas lidas a partir do cache de consultas.

### QueryCacheWrittenBytes {#querycachewrittenbytes}

A quantidade de bytes (não compactados) gravados no cache de consultas

### QueryCacheWrittenRows {#querycachewrittenrows}

O número de linhas gravadas no cache de consultas.

### Acertos do cache de condições de consulta {#queryconditioncachehits}

Número de vezes em que uma entrada foi encontrada no cache de condições de consulta (e a leitura das marcas pode ser ignorada). Atualizado apenas para consultas SELECT com SETTING use&#95;query&#95;condition&#95;cache = 1.

### QueryConditionCacheMisses {#queryconditioncachemisses}

Número de vezes que uma entrada não foi encontrada no cache de condições de consulta (e a leitura de `mark` não pode ser ignorada). Atualizado apenas para consultas SELECT com SETTING use&#95;query&#95;condition&#95;cache = 1.

### QueryLocalReadThrottlerBytes {#querylocalreadthrottlerbytes}

Bytes que passaram pelo limitador &#39;max&#95;local&#95;read&#95;bandwidth&#39;.

### QueryLocalReadThrottlerSleepMicroseconds {#querylocalreadthrottlersleepmicroseconds}

Tempo total em que uma consulta permaneceu em espera para se adequar ao throttling de &#39;max&#95;local&#95;read&#95;bandwidth&#39;.

### QueryLocalWriteThrottlerBytes {#querylocalwritethrottlerbytes}

Bytes que passaram pelo limitador &#39;max&#95;local&#95;write&#95;bandwidth&#39;.

### QueryLocalWriteThrottlerSleepMicroseconds {#querylocalwritethrottlersleepmicroseconds}

Tempo total em que uma consulta ficou em espera para se adequar ao throttling de `max_local_write_bandwidth`.

### QueryMaskingRulesMatch {#querymaskingrulesmatch}

Número de vezes em que as regras de mascaramento de consultas foram correspondidas com sucesso.

### QueryMemoryLimitExceeded {#querymemorylimitexceeded}

Número de vezes em que a consulta excedeu o limite de memória.

### QueryParseMicroseconds {#queryparsemicroseconds}

Tempo total gasto na análise sintática do texto da consulta para convertê-lo em uma AST.

### QueryPipelineBuildMicroseconds {#querypipelinebuildmicroseconds}

Tempo total gasto na construção do QueryPipeline a partir do QueryPlan otimizado.

### QueryPlanBuildMicroseconds {#queryplanbuildmicroseconds}

Tempo total gasto na criação do QueryPlan a partir da árvore de consulta analisada.

### QueryPlanOptimizeMicroseconds {#queryplanoptimizemicroseconds}

Tempo total gasto para executar otimizações do plano de consulta.

### QueryPreempted {#querypreempted}

Número de vezes em que as tarefas são pausadas e ficam aguardando por causa da configuração &#39;priority&#39;

### QueryProfilerConcurrencyOverruns {#queryprofilerconcurrencyoverruns}

Número de vezes em que descartamos o processamento de um sinal do profiler de consulta devido ao número excessivo de profilers de consulta em execução simultânea em outras threads, o que pode indicar sobrecarga.

### QueryProfilerErrors {#queryprofilererrors}

Acessos inválidos à memória durante o desenrolamento assíncrono da pilha.

### QueryProfilerRuns {#queryprofilerruns}

Número de vezes em que o QueryProfiler foi executado.

### QueryProfilerSignalOverruns {#queryprofilersignaloverruns}

Número de vezes em que o processamento de um sinal do profiler de consulta é descartado devido a overrun, mais o número de sinais que o sistema operacional não entregou devido a overrun.

### QueryRemoteReadThrottlerBytes {#queryremotereadthrottlerbytes}

Bytes que passaram pelo limitador &#39;max&#95;remote&#95;read&#95;network&#95;bandwidth&#39;.

### QueryRemoteReadThrottlerSleepMicroseconds {#queryremotereadthrottlersleepmicroseconds}

Tempo total em que uma consulta ficou em espera para obedecer ao throttling de &#39;max&#95;remote&#95;read&#95;network&#95;bandwidth&#39;.

### QueryRemoteWriteThrottlerBytes {#queryremotewritethrottlerbytes}

Bytes que passaram pelo limitador `max_remote_write_network_bandwidth`.

### QueryRemoteWriteThrottlerSleepMicroseconds {#queryremotewritethrottlersleepmicroseconds}

Tempo total em que uma consulta ficou em espera para atender ao throttling de &#39;max&#95;remote&#95;write&#95;network&#95;bandwidth&#39;.

### QueryTimeMicroseconds {#querytimemicroseconds}

Tempo total de todas as consultas.

### QuorumFailedInserts {#quorumfailedinserts}

Número de inserções que falharam porque o quórum não foi atingido.

### QuorumParts {#quorumparts}

Número de partes de dados gravadas com quórum. Conta como uma parte para insert síncrono e pode chegar ao número de async inserts no insert que faz o flush dos async inserts.

### QuorumWaitMicroseconds {#quorumwaitmicroseconds}

Tempo total gasto aguardando o quórum durante as inserções.

### QuotaCacheRecalculationMicroseconds {#quotacacherecalculationmicroseconds}

Tempo total gasto na reescolha de quotas para todos os conjuntos ativos habilitados (mantido sob o mutex de cache, que também é adquirido pelo caminho de compilação do ContextAccess).

### Recálculos do cache de quotas {#quotacacherecalculations}

Número de vezes que o cache de quotas voltou a selecionar quotas para todos os conjuntos habilitados em uso. Agrupado em uma única vez por lote de notificações de entidades de acesso.

### ReadBackoff {#readbackoff}

Número de vezes em que o número de threads de processamento de consultas foi reduzido devido à lentidão nas leituras.

### ReadBufferFromAzureBytes {#readbufferfromazurebytes}

Bytes lidos do Azure.

### ReadBufferFromAzureInitMicroseconds {#readbufferfromazureinitmicroseconds}

Tempo gasto na inicialização da conexão com o Azure.

### ReadBufferFromAzureMicroseconds {#readbufferfromazuremicroseconds}

Tempo gasto na leitura a partir do Azure.

### ReadBufferFromAzureRequestsErrors {#readbufferfromazurerequestserrors}

Número de exceções durante a leitura a partir do Azure

### ReadBufferFromFileDescriptorRead {#readbufferfromfiledescriptorread}

Número de leituras (`read`/`pread`) de um descritor de arquivo. Não inclui sockets.

### ReadBufferFromFileDescriptorReadBytes {#readbufferfromfiledescriptorreadbytes}

Número de bytes lidos de descritores de arquivo. Se o arquivo estiver compactado, será mostrado o tamanho dos dados compactados.

### ReadBufferFromFileDescriptorReadFailed {#readbufferfromfiledescriptorreadfailed}

Número de vezes em que a leitura (`read`/`pread`) de um descritor de arquivo falhou.

### ReadBufferFromS3Bytes {#readbufferfroms3bytes}

Bytes lidos do S3.

### ReadBufferFromS3InitMicroseconds {#readbufferfroms3initmicroseconds}

Tempo gasto na inicialização da conexão com o S3.

### ReadBufferFromS3Microseconds {#readbufferfroms3microseconds}

Tempo gasto na leitura a partir do S3.

### ReadBufferFromS3RequestsErrors {#readbufferfroms3requestserrors}

Número de exceções ao ler do S3.

### ReadBufferSeekCancelConnection {#readbufferseekcancelconnection}

Número de reposicionamentos que resultam em uma nova conexão (S3, HTTP)

### ReadCompressedBytes {#readcompressedbytes}

Número de bytes (antes da descompressão) lidos de fontes compactadas (arquivos, rede).

### ReaderExecutorBytesFromSource {#readerexecutorbytesfromsource}

Bytes físicos que o ReaderExecutor solicitou à fonte após não encontrar dados em nenhuma camada de cache (prefetch em primeiro plano e em segundo plano, incluindo bytes de prefetch desperdiçados por um descarte posterior); não inclui bytes fornecidos pelo consumer — consulte ReaderExecutorDeliveredBytes.

### ReaderExecutorCacheGetRequests {#readerexecutorcachegetrequests}

Número de invocações de ICacheHandle::get no ReaderExecutor. Permanece em zero até que as camadas de cache do ReaderExecutor sejam introduzidas.

### ReaderExecutorCachePopulateRequests {#readerexecutorcachepopulaterequests}

Número de invocações de ICacheHandle::put no ReaderExecutor. Permanece em zero até que as camadas de cache do ReaderExecutor sejam introduzidas.

### ReaderExecutorDecryptMicroseconds {#readerexecutordecryptmicroseconds}

Tempo que o ReaderExecutor gastou descriptografando in-place a carga útil entregue (CTR, endereçável por posição). Zero para leituras não criptografadas e em builds sem SSL.

### ReaderExecutorDeliveredBytes {#readerexecutordeliveredbytes}

Bytes úteis que o ReaderExecutor entregou às solicitações de leitura (a carga útil da janela solicitada, excluindo sobreleitura e write-back do cache). Denominador do KPI modelado de custo por byte (ReaderExecutorModeledCostMicroseconds / ReaderExecutorDeliveredBytes).

### ReaderExecutorIncompleteConnections {#readerexecutorincompleteconnections}

Número de conexões de origem que o ReaderExecutor descartou antes de esgotá-las até o limite direito; não reutilizáveis no pool, o que força seu restabelecimento. Zero até que seja introduzida a reutilização em tempo real do buffer de origem do ReaderExecutor.

### ReaderExecutorLongConnectionBytes {#readerexecutorlongconnectionbytes}

Total de bytes lidos por meio de long source connections.

### ReaderExecutorLongConnectionFallbacks {#readerexecutorlongconnectionfallbacks}

Número de vezes em que o ReaderExecutor solicitou uma long connection, mas recorreu a uma leitura única (one-shot) por não haver slot disponível.

### ReaderExecutorLongConnectionHits {#readerexecutorlongconnectionhits}

Número de janelas que o ReaderExecutor atendeu lendo de uma long source connection já aberta.

### ReaderExecutorLongConnectionOpened {#readerexecutorlongconnectionopened}

Número de long source connections abertas pelo ReaderExecutor para otimização de leitura sequential.

### ReaderExecutorModeledCostMicroseconds {#readerexecutormodeledcostmicroseconds}

Custo de E/S modelado das leituras do ReaderExecutor: um KPI sintético que serve como proxy da otimização do caminho de leitura, NÃO é uma latência medida. Soma ponderada dos contadores acima com pesos heurísticos de S3: 30 ms por solicitação à origem + 5 ms por conexão incompleta + 20 ms por MiB transferido da origem (carga útil útil mais leitura excedente) + 0,1 ms por gravação no cache + 0,05 ms por leitura do cache. Divida por ReaderExecutorDeliveredBytes para obter um custo por byte independente da carga. Experimental, acompanha o ReaderExecutor, que também é experimental.

### ReaderExecutorSourceRequests {#readerexecutorsourcerequests}

Número de solicitações abertas na origem pelo ReaderExecutor (exclui reutilizações do buffer ativo).

### ReaderExecutorWorkMicroseconds {#readerexecutorworkmicroseconds}

Tempo total decorrido dentro de ReaderExecutor::readNextWindow (abrindo, reposicionando e lendo a janela processada). Contribui diretamente para a latência de leitura da consulta.

### ReadPatchesMicroseconds {#readpatchesmicroseconds}

Tempo total gasto na leitura de partes de patch

### ReadPoolRangeRefinerDroppedCuts {#readpoolrangerefinerdroppedcuts}

Quantas vezes os ranges truncados de uma parte por um read pool do MergeTree foram totalmente descartados por um refinador de ranges, de modo que nenhuma task de leitura foi criada para eles

### ReadPoolRangeRefinerDroppedMarks {#readpoolrangerefinerdroppedmarks}

Quantas marcas foram descartadas das tarefas de leitura por um refinador de ranges nos pools de leitura MergeTree (por exemplo, marcas totalmente filtradas por um projection index)

### ReadTaskRequestsReceived {#readtaskrequestsreceived}

O número de callbacks solicitados ao servidor remoto de volta ao servidor iniciador para selecionar a tarefa de leitura (para a table function s3Cluster e semelhantes). Medido no servidor iniciador.

### ReadTaskRequestsSent {#readtaskrequestssent}

O número de callbacks que o servidor remoto solicita de volta ao servidor iniciador para escolher a tarefa de leitura (para a função de tabela s3Cluster e similares). Medido no servidor remoto.

### ReadTaskRequestsSentElapsedMicroseconds {#readtaskrequestssentelapsedmicroseconds}

Tempo gasto em callbacks solicitados pelo servidor remoto ao servidor iniciador para escolher a tarefa de leitura (para a função de tabela s3Cluster e similares). Medido no lado do servidor remoto.

### ReadTasksWithAppliedMutationsOnFly {#readtaskswithappliedmutationsonfly}

Número total de tarefas de leitura às quais alguma mutação foi aplicada em tempo real

### ReadTasksWithAppliedPatches {#readtaskswithappliedpatches}

Número total de tarefas de leitura às quais foi aplicada alguma patch part

### ReadWriteBufferFromHTTPBytes {#readwritebufferfromhttpbytes}

Tamanho total, em bytes, do payload recebido e enviado por ReadWriteBufferFromHTTP. Não inclui os cabeçalhos HTTP.

### ReadWriteBufferFromHTTPRequestsSent {#readwritebufferfromhttprequestssent}

Número de requisições HTTP enviadas por ReadWriteBufferFromHTTP

### RealTimeMicroseconds {#realtimemicroseconds}

Tempo total decorrido gasto pelas threads de processamento (consultas e outras tarefas) (observe que isso é uma soma).

### RebuiltProjections {#rebuiltprojections}

Número de projeções reconstruídas do zero durante os merges do MergeTree.

### RefreshableViewLockTableRetry {#refreshableviewlocktableretry}

Quantas vezes um SELECT de uma view materializada atualizável precisou mudar para uma nova tabela porque a tabela antiga foi excluída

### RefreshableViewRefreshFailed {#refreshableviewrefreshfailed}

Quantas vezes as views materializadas atualizáveis falharam ao serem atualizadas

### RefreshableViewRefreshSuccess {#refreshableviewrefreshsuccess}

Número de vezes que as views materializadas atualizáveis foram atualizadas

### RefreshableViewSyncReplicaRetry {#refreshableviewsyncreplicaretry}

Número de vezes que um SELECT em uma view materializada atualizável falhou e tentou novamente uma SYNC REPLICA implícita

### RefreshableViewSyncReplicaSuccess {#refreshableviewsyncreplicasuccess}

Quantas vezes um SELECT em uma view materializada atualizável realizou um SYNC REPLICA implícito

### RegexpLocalCacheHit {#regexplocalcachehit}

Número de vezes que buscamos uma expressão regular compilada em um cache local.

### RegexpLocalCacheMiss {#regexplocalcachemiss}

Número de vezes em que não foi possível buscar uma expressão regular compilada em um cache local.

### RegexpWithMultipleNeedlesCreated {#regexpwithmultipleneedlescreated}

Expressões regulares com múltiplos padrões (biblioteca VectorScan) compiladas.

### RegexpWithMultipleNeedlesGlobalCacheHit {#regexpwithmultipleneedlesglobalcachehit}

Número de vezes em que a expressão regular compilada com múltiplos padrões (biblioteca VectorScan) foi obtida do cache global.

### RegexpWithMultipleNeedlesGlobalCacheMiss {#regexpwithmultipleneedlesglobalcachemiss}

Número de vezes em que não foi possível obter do cache global a expressão regular compilada com múltiplos padrões (biblioteca VectorScan).

### RejectedInserts {#rejectedinserts}

Número de vezes em que o INSERT de um bloco em uma tabela MergeTree foi rejeitado com a exceção &#39;Too many parts&#39; devido ao alto número de partes de dados ativas na partição.

### RejectedLightweightUpdates {#rejectedlightweightupdates}

Número de vezes em que a atualização leve foi rejeitada devido ao excesso de bytes não compactados em patches.

### RejectedMutations {#rejectedmutations}

Número de vezes que a mutação de uma tabela MergeTree foi rejeitada com a exceção &#39;Too many mutations&#39; devido ao grande número de mutações não concluídas na tabela.

### RemoteFSBuffers {#remotefsbuffers}

Número de buffers criados para leitura assíncrona de um sistema de arquivos remoto

### RemoteFSCancelledPrefetches {#remotefscancelledprefetches}

Número de prefetches cancelados (devido a operações de seek)

### RemoteFSLazySeeks {#remotefslazyseeks}

Número de operações de seek lazy

### RemoteFSPrefetchedBytes {#remotefsprefetchedbytes}

Número de bytes provenientes do buffer de prefetch

### RemoteFSPrefetchedReads {#remotefsprefetchedreads}

Número de leituras a partir do buffer de prefetch

### RemoteFSPrefetches {#remotefsprefetches}

Número de prefetches realizados na leitura assíncrona do sistema de arquivos remoto

### RemoteFSSeeks {#remotefsseeks}

Número total de operações de seek do buffer assíncrono

### RemoteFSSeeksWithReset {#remotefsseekswithreset}

Número de operações de seek que resultam em uma nova conexão

### RemoteFSUnprefetchedBytes {#remotefsunprefetchedbytes}

Número de bytes de buffer não prefetch

### RemoteFSUnprefetchedReads {#remotefsunprefetchedreads}

Número de leituras de buffer sem prefetch

### RemoteFSUnusedPrefetches {#remotefsunusedprefetches}

Número de prefetches pendentes quando o buffer é destruído

### RemoteReadThrottlerBytes {#remotereadthrottlerbytes}

Bytes que passaram pelo throttler de taxa &#39;max&#95;remote&#95;read&#95;network&#95;bandwidth&#95;for&#95;server&#39;/&#39;max&#95;remote&#95;read&#95;network&#95;bandwidth&#39;.

### RemoteReadThrottlerSleepMicroseconds {#remotereadthrottlersleepmicroseconds}

Tempo total em que uma consulta permaneceu em espera para se adequar à limitação de taxa de &#39;max&#95;remote&#95;read&#95;network&#95;bandwidth&#95;for&#95;server&#39;/&#39;max&#95;remote&#95;read&#95;network&#95;bandwidth&#39;.

### RemoteWriteThrottlerBytes {#remotewritethrottlerbytes}

Bytes processados pelo throttler &#39;max&#95;remote&#95;write&#95;network&#95;bandwidth&#95;for&#95;server&#39;/&#39;max&#95;remote&#95;write&#95;network&#95;bandwidth&#39;.

### RemoteWriteThrottlerSleepMicroseconds {#remotewritethrottlersleepmicroseconds}

Tempo total em que uma consulta permaneceu em espera para se adequar à limitação de taxa de &#39;max&#95;remote&#95;write&#95;network&#95;bandwidth&#95;for&#95;server&#39;/&#39;max&#95;remote&#95;write&#95;network&#95;bandwidth&#39;.

### ReplacingSortedMilliseconds {#replacingsortedmilliseconds}

Tempo total gasto na substituição de colunas ordenadas

### ReplicaPartialShutdown {#replicapartialshutdown}

Número de vezes que uma tabela replicada precisa desinicializar seu estado devido à expiração da sessão no ZooKeeper. O estado é reinicializado sempre que o ZooKeeper volta a ficar disponível.

### ReplicatedCoveredPartsInZooKeeperOnStart {#replicatedcoveredpartsinzookeeperonstart}

Para fins de depuração. Número de partes no ZooKeeper que têm uma parte que as cobre, mas não existem no disco. Verificado na inicialização do servidor.

### ReplicatedDataLoss {#replicateddataloss}

Número de vezes em que uma parte de dados que queríamos não existe em nenhuma réplica (nem mesmo nas réplicas que estão offline no momento). Essas partes de dados estão definitivamente perdidas. Isso é normal devido à replicação assíncrona (se inserts com quórum não estiverem habilitados), quando a réplica na qual a parte de dados foi gravada falha e, ao voltar a ficar online após a falha, não contém essa parte de dados.

### ReplicatedPartChecks {#replicatedpartchecks}

Número de vezes em que foi necessário realizar uma busca avançada por uma parte de dados nas réplicas ou confirmar a necessidade de uma parte de dados existente.

### ReplicatedPartChecksFailed {#replicatedpartchecksfailed}

Número de vezes em que a busca avançada por uma parte de dados nas réplicas não produziu resultado ou em que uma parte inesperada foi encontrada e movida para outro local.

### ReplicatedPartFailedFetches {#replicatedpartfailedfetches}

Número de vezes em que falhou o download de uma parte de dados de uma réplica de uma tabela ReplicatedMergeTree.

### ReplicatedPartFetches {#replicatedpartfetches}

Número de vezes que uma parte de dados foi baixada de uma réplica de uma tabela ReplicatedMergeTree.

### ReplicatedPartFetchesOfMerged {#replicatedpartfetchesofmerged}

Número de vezes em que preferimos baixar uma parte já mesclada de uma réplica de uma tabela ReplicatedMergeTree em vez de fazer a mesclagem por conta própria (em geral, preferimos fazer a mesclagem por conta própria para economizar tráfego de rede). Isso acontece quando não temos todas as partes de origem para realizar a mesclagem ou quando a parte de dados é antiga o suficiente.

### ReplicatedPartMerges {#replicatedpartmerges}

Número de vezes em que as partes de dados das tabelas ReplicatedMergeTree foram mescladas com sucesso.

### ReplicatedPartMutations {#replicatedpartmutations}

Número de vezes em que partes de dados de tabelas ReplicatedMergeTree sofreram mutações com sucesso.

### RestorePartsSkippedBytes {#restorepartsskippedbytes}

Tamanho total dos arquivos ignorados durante a restauração das partes

### RestorePartsSkippedFiles {#restorepartsskippedfiles}

Número de arquivos ignorados durante a restauração das partes

### RoleCacheRecalculationMicroseconds {#rolecacherecalculationmicroseconds}

Tempo total gasto recalculando todos os conjuntos ativos habilitados de funções (mantidos sob o mutex do cache, que também é usado pelo caminho de compilação de ContextAccess).

### RoleCacheRecalculations {#rolecacherecalculations}

Número de vezes que o cache de funções recalculou todos os conjuntos ativos habilitados. Agrupado em uma única ocorrência por lote de notificações de entidades de acesso.

### RowPolicyCacheRecalculationMicroseconds {#rowpolicycacherecalculationmicroseconds}

Tempo total gasto recombinando filtros de política de linha para todos os conjuntos ativos habilitados, fora do mutex do RowPolicyCache (de modo que não bloqueie o caminho de compilação do ContextAccess).

### RowPolicyCacheRecalculations {#rowpolicycacherecalculations}

Número de vezes que o cache de políticas de linha recombinou os filtros para todos os conjuntos habilitados em uso. Agrupado em uma única ocorrência por lote de notificações de entidades de acesso.

### Linhas lidas pelo leitor principal {#rowsreadbymainreader}

Número de linhas lidas das tabelas MergeTree pelo leitor principal (após a etapa PREWHERE).

### RowsReadByPrewhereReaders {#rowsreadbyprewherereaders}

Número total de linhas lidas de tabelas MergeTree por leitores de prewhere.

### RuntimeDataflowStatisticsInputBytes {#runtimedataflowstatisticsinputbytes}

Estatísticas coletadas sobre o número de bytes que as réplicas leriam caso a consulta fosse executada com réplicas paralelas

### RuntimeDataflowStatisticsOutputBytes {#runtimedataflowstatisticsoutputbytes}

Estatísticas coletadas sobre a quantidade de bytes que as réplicas enviariam ao iniciador caso a consulta fosse executada com réplicas paralelas

### RuntimeFilterBlocksProcessed {#runtimefilterblocksprocessed}

Número de blocos processados pelos JOIN Runtime Filters

### RuntimeFilterBlocksSkipped {#runtimefilterblocksskipped}

Número de blocos ignorados pelos JOIN Runtime Filters sem serem processados, porque o filtro foi desabilitado dinamicamente devido à baixa taxa de filtragem

### RuntimeFilterBloomFilterBuildsSkipped {#runtimefilterbloomfilterbuildsskipped}

Número de compilações de filtro de Bloom do JOIN Runtime Filter ignoradas porque a contagem de chaves do lado de construção, obtida das estatísticas da tabela hash, indicou que o filtro excederia a razão máxima de bits definidos

### RuntimeFilterGranulesConsidered {#runtimefiltergranulesconsidered}

Número de grânulos examinados para pruning em tempo de leitura pelos JOIN Runtime Filters

### RuntimeFilterGranulesDropped {#runtimefiltergranulesdropped}

Número de grânulos descartados durante a leitura pelos JOIN Runtime Filters

### RuntimeFilterRowsChecked {#runtimefilterrowschecked}

Número de linhas verificadas pelos JOIN Runtime Filters

### RuntimeFilterRowsPassed {#runtimefilterrowspassed}

Número de linhas que passaram pelos JOIN Runtime Filters (ou seja, não foram filtradas)

### RuntimeFilterRowsSkipped {#runtimefilterrowsskipped}

Número de linhas nos blocos que foram ignoradas pelos JOIN Runtime Filters

### RuntimeFiltersCreated {#runtimefilterscreated}

Número de JOIN Runtime Filters distintos criados em uma consulta

### RWLockAcquiredReadLocks {#rwlockacquiredreadlocks}

Número de vezes em que um bloqueio de leitura foi adquirido (em um heavy RWLock).

### RWLockAcquiredWriteLocks {#rwlockacquiredwritelocks}

Número de vezes em que um bloqueio de escrita foi adquirido (em um heavy RWLock).

### RWLockReadersWaitMilliseconds {#rwlockreaderswaitmilliseconds}

Tempo total gasto aguardando a obtenção de um bloqueio de leitura (em um heavy RWLock).

### RWLockWritersWaitMilliseconds {#rwlockwriterswaitmilliseconds}

Tempo total gasto aguardando até que um bloqueio de escrita seja adquirido (em um heavy RWLock).

### S3AbortMultipartUpload {#s3abortmultipartupload}

Número de chamadas da API S3 AbortMultipartUpload.

### S3CachedCredentialsProvidersAdded {#s3cachedcredentialsprovidersadded}

Número total de provedores de credenciais recém-adicionados ao cache

### S3CachedCredentialsProvidersReused {#s3cachedcredentialsprovidersreused}

Número total de provedores de credenciais reutilizados a partir do cache

### S3Clients {#s3clients}

Número de clientes S3 criados.

### S3CompleteMultipartUpload {#s3completemultipartupload}

Número de chamadas CompleteMultipartUpload da API S3.

### S3CopyObject {#s3copyobject}

Número de chamadas à CopyObject da API S3.

### S3CreateMultipartUpload {#s3createmultipartupload}

Número de chamadas à API S3 CreateMultipartUpload.

### S3DeleteObjects {#s3deleteobjects}

Número de chamadas à API S3 DeleteObject(s).

### S3GetObject {#s3getobject}

Número de chamadas GetObject da API do S3.

### S3GetObjectTagging {#s3getobjecttagging}

Número de chamadas GetObjectTagging da API S3.

### S3GetRequestThrottlerBlocked {#s3getrequestthrottlerblocked}

Número de requisições GET e SELECT do S3 bloqueadas pelo throttler.

### S3GetRequestThrottlerCount {#s3getrequestthrottlercount}

Número de requisições S3 GET e SELECT que passaram pelo throttler: bloqueadas e não bloqueadas.

### S3GetRequestThrottlerSleepMicroseconds {#s3getrequestthrottlersleepmicroseconds}

Tempo total em que uma consulta permaneceu em espera para atender à limitação de taxa das solicitações GET e SELECT do S3.

### S3HeadObject {#s3headobject}

Número de chamadas à API HeadObject do S3.

### S3ListObjects {#s3listobjects}

Número de chamadas à API ListObjects do S3.

### S3PutObject {#s3putobject}

Número de chamadas PutObject da API do S3.

### S3PutRequestThrottlerBlocked {#s3putrequestthrottlerblocked}

Número de requisições S3 PUT, COPY, POST e LIST bloqueadas pelo throttler.

### S3PutRequestThrottlerCount {#s3putrequestthrottlercount}

Número de requisições S3 PUT, COPY, POST e LIST que passaram pelo throttler: bloqueadas e não bloqueadas.

### S3PutRequestThrottlerSleepMicroseconds {#s3putrequestthrottlersleepmicroseconds}

Tempo total em que uma consulta permaneceu em espera para se adequar à limitação de taxa das solicitações S3 PUT, COPY, POST e LIST.

### S3QueueSetFileFailedMicroseconds {#s3queuesetfilefailedmicroseconds}

Tempo gasto para marcar o arquivo como falho

### S3QueueSetFileProcessedMicroseconds {#s3queuesetfileprocessedmicroseconds}

Tempo gasto para marcar o arquivo como processado

### S3QueueSetFileProcessingMicroseconds {#s3queuesetfileprocessingmicroseconds}

Tempo gasto para marcar o arquivo como em processamento

### S3ReadMicroseconds {#s3readmicroseconds}

Tempo das requisições GET e HEAD para o armazenamento S3.

### S3ReadRequestAttempts {#s3readrequestattempts}

Número de tentativas de solicitações GET e HEAD, incluindo a tentativa inicial e quaisquer novas tentativas, mas excluindo as novas tentativas realizadas internamente pela estratégia de retry do S3

### S3ReadRequestRetryableErrors {#s3readrequestretryableerrors}

Número de erros que permitem nova tentativa em solicitações GET e HEAD, excluindo as novas tentativas realizadas internamente pela estratégia de retry do S3

### S3ReadRequestsCount {#s3readrequestscount}

Número de requisições GET e HEAD para o armazenamento S3.

### S3ReadRequestsErrors {#s3readrequestserrors}

Número de erros não relacionados a throttling em requisições GET e HEAD para o armazenamento S3.

### S3ReadRequestsRedirects {#s3readrequestsredirects}

Número de redirecionamentos em requisições GET e HEAD para o armazenamento S3.

### S3ReadRequestsThrottling {#s3readrequeststhrottling}

Número de erros 429 e 503 em requisições GET e HEAD para o armazenamento S3.

### S3UploadPart {#s3uploadpart}

Número de chamadas UploadPart da API do S3.

### S3UploadPartCopy {#s3uploadpartcopy}

Número de chamadas à API S3 UploadPartCopy.

### S3WriteMicroseconds {#s3writemicroseconds}

Tempo das solicitações POST, DELETE, PUT e PATCH para o armazenamento S3.

### S3WriteRequestAttempts {#s3writerequestattempts}

Número de tentativas para solicitações POST, DELETE, PUT e PATCH, incluindo a tentativa inicial e quaisquer novas tentativas, mas excluindo as tentativas executadas internamente pela estratégia de retry

### S3WriteRequestRetryableErrors {#s3writerequestretryableerrors}

Número de erros passíveis de nova tentativa em requisições POST, DELETE, PUT e PATCH, excluindo as novas tentativas executadas internamente pela estratégia de retry

### S3WriteRequestsCount {#s3writerequestscount}

Número de solicitações POST, DELETE, PUT e PATCH ao armazenamento S3.

### S3WriteRequestsErrors {#s3writerequestserrors}

Número de erros que não são de throttling em solicitações POST, DELETE, PUT e PATCH para o armazenamento S3.

### S3WriteRequestsRedirects {#s3writerequestsredirects}

Número de redirecionamentos em requisições POST, DELETE, PUT e PATCH para o armazenamento S3.

### S3WriteRequestsThrottling {#s3writerequeststhrottling}

Número de erros 429 e 503 em requisições POST, DELETE, PUT e PATCH no armazenamento S3.

### ScalarSubqueriesCacheMiss {#scalarsubqueriescachemiss}

Número de vezes em que a leitura de uma subconsulta escalar não foi encontrada no cache e precisou ser calculada integralmente

### ScalarSubqueriesGlobalCacheHit {#scalarsubqueriesglobalcachehit}

Número de vezes em que a leitura de uma subconsulta escalar foi feita usando o cache global

### ScalarSubqueriesLocalCacheHit {#scalarsubquerieslocalcachehit}

Número de vezes em que a leitura de uma subconsulta escalar foi feita usando o cache local

### SchedulerIOReadBytes {#schedulerioreadbytes}

Bytes que passaram pelo scheduler em leituras de E/S.

### SchedulerIOReadRequests {#schedulerioreadrequests}

Solicitações de recursos encaminhadas pelo scheduler para leituras de E/S.

### SchedulerIOReadWaitMicroseconds {#schedulerioreadwaitmicroseconds}

Tempo total em que uma consulta ficou aguardando solicitações de recursos para leituras de IO.

### SchedulerIOWriteBytes {#scheduleriowritebytes}

Bytes processados pelo scheduler em gravações de E/S.

### SchedulerIOWriteRequests {#scheduleriowriterequests}

Solicitações de recursos encaminhadas pelo scheduler para operações de gravação de E/S.

### SchedulerIOWriteWaitMicroseconds {#scheduleriowritewaitmicroseconds}

Tempo total que uma consulta ficou aguardando recursos solicitados para operações de gravação de E/S.

### SchemaInferenceCacheEvictions {#schemainferencecacheevictions}

Número de vezes que um esquema em cache foi removido por overflow

### SchemaInferenceCacheHits {#schemainferencecachehits}

Número de vezes em que a fonte solicitada é encontrada no cache de esquema

### SchemaInferenceCacheInvalidations {#schemainferencecacheinvalidations}

Número de vezes que um esquema no cache se tornou inválido devido a alterações nos dados

### SchemaInferenceCacheMisses {#schemainferencecachemisses}

Número de vezes em que a fonte solicitada não está no cache de esquema

### SchemaInferenceCacheNumRowsHits {#schemainferencecachenumrowshits}

Número de vezes em que o número de linhas é encontrado no cache de esquema durante a contagem de arquivos

### SchemaInferenceCacheNumRowsMisses {#schemainferencecachenumrowsmisses}

Número de vezes em que a fonte solicitada está no cache, mas a quantidade de linhas não está no cache durante a contagem a partir dos arquivos

### SchemaInferenceCacheSchemaHits {#schemainferencecacheschemahits}

Número de vezes em que o esquema é encontrado no cache de esquema durante a inferência de esquema

### SchemaInferenceCacheSchemaMisses {#schemainferencecacheschemamisses}

Número de vezes em que a fonte solicitada está no cache, mas o esquema não está no cache durante a inferência de esquema

### Seek {#seek}

Número de vezes em que a função &#39;lseek&#39; foi chamada.

### SelectedBytes {#selectedbytes}

Número de bytes (não compactados; para colunas conforme armazenadas na memória) selecionados de todas as tabelas.

### SelectedMarks {#selectedmarks}

Número de marcas (grânulos de índice) selecionadas para leitura em uma tabela MergeTree.

### SelectedMarksTotal {#selectedmarkstotal}

Número total de marcas (grânulos de índice) antes de selecionar quais delas serão lidas de uma tabela MergeTree.

### SelectedParts {#selectedparts}

Número de partes de dados selecionadas para leitura em uma tabela MergeTree.

### SelectedPartsTotal {#selectedpartstotal}

Número total de partes de dados antes de selecionar quais serão lidas de uma tabela MergeTree.

### SelectedRanges {#selectedranges}

Número de intervalos (não adjacentes) em todas as partes de dados selecionadas para leitura em uma tabela MergeTree.

### SelectedRows {#selectedrows}

Número de linhas selecionadas em todas as tabelas.

### SelectivityEstimatorInSetEstimatedFromSize {#selectivityestimatorinsetestimatedfromsize}

Número de condições `IN` cuja seletividade foi estimada a partir do tamanho e dos limites do Set, em vez de seus intervalos exatos, porque o Set excede `statistics_max_set_size_for_exact_selectivity_estimation`

### SelectivityEstimatorInSetNotBuilt {#selectivityestimatorinsetnotbuilt}

Número de condições `IN` que o estimador de seletividade não conseguiu analisar porque o set ainda não havia sido construído, e ele não deve executar a subconsulta para preenchê-lo

### SelectQueriesWithPrimaryKeyUsage {#selectquerieswithprimarykeyusage}

Número de consultas SELECT que usam a chave primária para avaliar a condição WHERE

### SelectQueriesWithSubqueries {#selectquerieswithsubqueries}

Número de consultas SELECT com todas as subconsultas

### SelectQuery {#selectquery}

O mesmo que Query, mas apenas para consultas SELECT.

### SelectQueryTimeMicroseconds {#selectquerytimemicroseconds}

Tempo total das consultas SELECT.

### SelfDuplicatedAsyncInserts {#selfduplicatedasyncinserts}

Número de inserts assíncronos no bloco inserido em uma tabela ReplicatedMergeTree que foram autoduplicados.

### ServerStartupMilliseconds {#serverstartupmilliseconds}

Tempo decorrido desde a inicialização do servidor até começar a escutar nos sockets, em milissegundos

### SetsBuiltFromSubquery {#setsbuiltfromsubquery}

Número de sets de `IN`/`JOIN` preenchidos pela execução de suas subconsultas. Um set obtido do cache de sets preparados, ou já construído e reutilizado, não é contabilizado.

### SettingsProfileCacheRecalculationMicroseconds {#settingsprofilecacherecalculationmicroseconds}

Tempo total gasto refazendo a mesclagem de configurações e restrições para todos os conjuntos ativos habilitados (sob o mutex do cache, também usado pelo caminho de compilação do ContextAccess).

### SettingsProfileCacheRecalculations {#settingsprofilecacherecalculations}

Número de vezes que o cache do perfil de configurações refez a mesclagem das configurações e restrições para todos os conjuntos habilitados ativos no momento. Agrupado em uma única ocorrência por lote de notificações de entidade de acesso.

### Shards {#shards}

O número de shards envolvidos em uma consulta, somado entre todas as tabelas distribuídas e funções de tabela. Um único host é contado várias vezes se aparecer em várias tabelas. Esse número representa o total esperado de shards, incluindo os shards ignorados com a configuração `skip_unavailable_shards`.

### SharedCatalogStateFetchFullFallback {#sharedcatalogstatefetchfullfallback}

Número de vezes em que o fetch incremental do estado do Shared Catalog falhou vezes consecutivas demais e, em vez disso, foi realizado um fetch completo do estado

### SharedDatabaseCatalogFailedToApplyState {#shareddatabasecatalogfailedtoapplystate}

Número de falhas ao aplicar o novo estado no SharedDatabaseCatalog

### SharedDatabaseCatalogStateApplicationMicroseconds {#shareddatabasecatalogstateapplicationmicroseconds}

Tempo total gasto na aplicação de um novo estado no SharedDatabaseCatalog

### SharedMergeTreeBlobRefCounterSharedBlobs {#sharedmergetreeblobrefcountersharedblobs}

Quantos blobs de dados foram compartilhados por caminho entre partes blob-list do SharedMergeTree (incrementos de contagem de referências preparados em transações de commit, antes de serem acumulados em uma única atualização por nó de contador)

### SharedMergeTreeBlobsListBlobsWritten {#sharedmergetreeblobslistblobswritten}

Quantos blobs de dados foram registrados nos maps blobs.list do SharedMergeTree

### SharedMergeTreeBlobsListInlineFilesWritten {#sharedmergetreeblobslistinlinefileswritten}

Quantos arquivos pequenos foram armazenados inline em maps blobs.list do SharedMergeTree em vez de blobs separados

### SharedMergeTreeBlobsListPartsWritten {#sharedmergetreeblobslistpartswritten}

Quantas partes blob-list do SharedMergeTree foram gravadas (um nó blobs.list consolidado para cada uma)

### SharedMergeTreeCondemnedPartsKillRequest {#sharedmergetreecondemnedpartskillrequest}

Quantas solicitações ao ZooKeeper foram usadas para remover partes marcadas para remoção

### SharedMergeTreeCondemnedPartsLockConflict {#sharedmergetreecondemnedpartslockconflict}

Quantas vezes não foi possível obter o bloqueio devido a um conflito

### SharedMergeTreeCondemnedPartsRemoved {#sharedmergetreecondemnedpartsremoved}

Quantas partes marcadas para remoção foram removidas

### SharedMergeTreeDataPartsFetchAttempt {#sharedmergetreedatapartsfetchattempt}

Quantas vezes tentamos obter partes de dados

### SharedMergeTreeDataPartsFetchFromPeer {#sharedmergetreedatapartsfetchfrompeer}

Número de vezes que buscamos partes de dados de um peer

### SharedMergeTreeDataPartsFetchFromPeerMicroseconds {#sharedmergetreedatapartsfetchfrompeermicroseconds}

Busca de partes de dados de um peer em microssegundos

### SharedMergeTreeDataPartsFetchFromS3 {#sharedmergetreedatapartsfetchfroms3}

Quantas vezes buscamos partes de dados a partir do S3

### SharedMergeTreeFetchDeduplicated {#sharedmergetreefetchdeduplicated}

Número de operações de fetch de partes deduplicadas porque a parte já estava sendo obtida ou produzida por outra thread

### SharedMergeTreeHandleBlockingParts {#sharedmergetreehandleblockingparts}

Número de partes bloqueadoras a serem tratadas em scheduleDataProcessingJob

### SharedMergeTreeHandleBlockingPartsMicroseconds {#sharedmergetreehandleblockingpartsmicroseconds}

Tempo de processamento das partes bloqueadoras em scheduleDataProcessingJob

### SharedMergeTreeHandleFetchPartsMicroseconds {#sharedmergetreehandlefetchpartsmicroseconds}

Tempo de tratamento das partes recuperadas em scheduleDataProcessingJob

### SharedMergeTreeHandleOutdatedParts {#sharedmergetreehandleoutdatedparts}

Quantas partes desatualizadas devem ser tratadas em scheduleDataProcessingJob

### SharedMergeTreeHandleOutdatedPartsMicroseconds {#sharedmergetreehandleoutdatedpartsmicroseconds}

Tempo para processar partes desatualizadas em scheduleDataProcessingJob

### SharedMergeTreeLoadChecksumAndIndexesMicroseconds {#sharedmergetreeloadchecksumandindexesmicroseconds}

Tempo de loadColumnsChecksumsIndexes somente para SharedMergeTree

### SharedMergeTreeMergeMutationAssignmentAttempt {#sharedmergetreemergemutationassignmentattempt}

Quantas vezes tentamos atribuir um merge ou uma mutation

### SharedMergeTreeMergeMutationAssignmentFailedWithConflict {#sharedmergetreemergemutationassignmentfailedwithconflict}

Quantas vezes tentamos atribuir um merge ou uma mutation e falhamos devido a um conflito no Keeper

### SharedMergeTreeMergeMutationAssignmentFailedWithNothingToDo {#sharedmergetreemergemutationassignmentfailedwithnothingtodo}

Quantas vezes tentamos atribuir uma operação de merge ou mutação e falhamos porque não havia nada para mesclar

### SharedMergeTreeMergeMutationAssignmentSuccessful {#sharedmergetreemergemutationassignmentsuccessful}

Quantas vezes tentamos atribuir uma operação de merge ou mutação

### SharedMergeTreeMergePartsMovedToCondemned {#sharedmergetreemergepartsmovedtocondemned}

Número de partes movidas para o diretório marcado para remoção

### SharedMergeTreeMergePartsMovedToOudated {#sharedmergetreemergepartsmovedtooudated}

Quantas partes foram movidas para o diretório desatualizado

### SharedMergeTreeMergeSelectingTaskMicroseconds {#sharedmergetreemergeselectingtaskmicroseconds}

Microssegundos da tarefa de seleção de merge do SMT

### SharedMergeTreeMetadataCacheHintLoadedFromCache {#sharedmergetreemetadatacachehintloadedfromcache}

Número de vezes que a dica do cache de metadados foi encontrada sem acessar o Keeper

### SharedMergeTreeOptimizeAsync {#sharedmergetreeoptimizeasync}

Execução de consultas OPTIMIZE assíncronas

### SharedMergeTreeOptimizeSync {#sharedmergetreeoptimizesync}

Consultas OPTIMIZE executadas de forma síncrona

### SharedMergeTreeOutdatedPartsConfirmationInvocations {#sharedmergetreeoutdatedpartsconfirmationinvocations}

Quantas invocações foram realizadas para confirmar partes desatualizadas

### SharedMergeTreeOutdatedPartsConfirmationRequest {#sharedmergetreeoutdatedpartsconfirmationrequest}

Quantas solicitações do ZooKeeper foram usadas para configurar as partes desatualizadas

### SharedMergeTreeOutdatedPartsHTTPRequest {#sharedmergetreeoutdatedpartshttprequest}

Quantas solicitações HTTP foram enviadas para confirmar as partes desatualizadas

### SharedMergeTreeOutdatedPartsHTTPResponse {#sharedmergetreeoutdatedpartshttpresponse}

Quantas respostas HTTP foram enviadas para confirmar partes desatualizadas

### SharedMergeTreePartitionOperationRetries {#sharedmergetreepartitionoperationretries}

Quantas vezes uma operação em nível de partição (DROP/MOVE/REPLACE/CLONE/TRUNCATE PARTITION) precisou repetir a gravação múltipla no Keeper devido a uma alteração concorrente

### SharedMergeTreePartsKillerMicroseconds {#sharedmergetreepartskillermicroseconds}

Quanto tempo a thread principal do parts killer leva para concluir

### SharedMergeTreePartsKillerParts {#sharedmergetreepartskillerparts}

Número de partes agendadas pelo killer

### SharedMergeTreePartsKillerPartsMicroseconds {#sharedmergetreepartskillerpartsmicroseconds}

Quanto tempo leva para remover partes (executado em múltiplas threads)

### SharedMergeTreePartsKillerRuns {#sharedmergetreepartskillerruns}

Número de vezes que o parts killer foi executado

### SharedMergeTreeReplicaSetUpdatesFromZooKeeper {#sharedmergetreereplicasetupdatesfromzookeeper}

Quantas vezes atualizamos o conjunto de réplicas a partir do ZooKeeper

### SharedMergeTreeReplicaSetUpdatesFromZooKeeperMicroseconds {#sharedmergetreereplicasetupdatesfromzookeepermicroseconds}

Tempo gasto para atualizar o conjunto de réplicas

### SharedMergeTreeReplicaSetUpdatesFromZooKeeperRequests {#sharedmergetreereplicasetupdatesfromzookeeperrequests}

Número total de solicitações ao ZooKeeper feitas para atualizar o conjunto de réplicas

### SharedMergeTreeReplicaSetUpdateTaskRuns {#sharedmergetreereplicasetupdatetaskruns}

Número de vezes que updateReplicaSetTask foi executada

### SharedMergeTreeScheduleDataProcessingJob {#sharedmergetreescheduledataprocessingjob}

Número de vezes que scheduleDataProcessingJob foi chamado/

### SharedMergeTreeScheduleDataProcessingJobMicroseconds {#sharedmergetreescheduledataprocessingjobmicroseconds}

tempo de execução do scheduleDataProcessingJob

### SharedMergeTreeScheduleDataProcessingJobNothingToScheduled {#sharedmergetreescheduledataprocessingjobnothingtoscheduled}

Número de vezes em que `scheduleDataProcessingJob` foi chamado, mas não havia nada a fazer

### SharedMergeTreeSelectPartsForCoordinatedFetchMicroseconds {#sharedmergetreeselectpartsforcoordinatedfetchmicroseconds}

Tempo do selectPartsForCoordinatedFetch

### SharedMergeTreeSelectPartsForCoordinatedFetchParts {#sharedmergetreeselectpartsforcoordinatedfetchparts}

Número de partes selecionadas por selectPartsForCoordinatedFetch

### SharedMergeTreeSelectPartsForFullFetchMicroseconds {#sharedmergetreeselectpartsforfullfetchmicroseconds}

Tempo do selectPartsForFullFetch

### SharedMergeTreeSelectPartsForFullFetchParts {#sharedmergetreeselectpartsforfullfetchparts}

Número de partes selecionadas pelo selectPartsForFullFetch

### SharedMergeTreeSelectPartsForRendezvousFetchMicroseconds {#sharedmergetreeselectpartsforrendezvousfetchmicroseconds}

Tempo de execução de selectPartsForRendezvousFetch

### SharedMergeTreeSelectPartsForRendezvousFetchParts {#sharedmergetreeselectpartsforrendezvousfetchparts}

Número de partes selecionadas por selectPartsForRendezvousFetch

### SharedMergeTreeSnapshotDeadLocksReclaimed {#sharedmergetreesnapshotdeadlocksreclaimed}

Quantos nós ou subárvores do Keeper pertencentes a lightweight snapshots que não estão mais ativos foram recuperados

### SharedMergeTreeSnapshotPartsCleanerMicroseconds {#sharedmergetreesnapshotpartscleanermicroseconds}

Há quanto tempo a thread SnapshotCleanerThread está em execução

### SharedMergeTreeSnapshotPartsCleanerParts {#sharedmergetreesnapshotpartscleanerparts}

Por quanto tempo a SnapshotCleanerThread tenta limpar uma parte

### SharedMergeTreeSnapshotPartsCleanerPartsMicroseconds {#sharedmergetreesnapshotpartscleanerpartsmicroseconds}

Quanto tempo a SnapshotCleanerThread leva para limpar as partes

### SharedMergeTreeSnapshotPartsCleanerRuns {#sharedmergetreesnapshotpartscleanerruns}

Número de vezes que o SnapshotCleanerThread é executado

### SharedMergeTreeSnapshotPartsCleanRequest {#sharedmergetreesnapshotpartscleanrequest}

Número de vezes que a SnapshotCleanerThread decide limpar uma parte

### SharedMergeTreeSnapshotPartsRemoved {#sharedmergetreesnapshotpartsremoved}

Quantas vezes a SnapshotCleanerThread limpa uma parte com sucesso

### SharedMergeTreeTryUpdateDiskMetadataCacheForPartMicroseconds {#sharedmergetreetryupdatediskmetadatacacheforpartmicroseconds}

Duração de tryUpdateDiskMetadataCacheForPart em scheduleDataProcessingJob

### SharedMergeTreeVirtualPartsUpdateMicroseconds {#sharedmergetreevirtualpartsupdatemicroseconds}

Microssegundos de atualização das partes virtuais

### SharedMergeTreeVirtualPartsUpdates {#sharedmergetreevirtualpartsupdates}

Contagem de atualizações de partes virtuais

### SharedMergeTreeVirtualPartsUpdatesByLeader {#sharedmergetreevirtualpartsupdatesbyleader}

Atualizações de partes virtuais por líder

### SharedMergeTreeVirtualPartsUpdatesForMergesOrStatus {#sharedmergetreevirtualpartsupdatesformergesorstatus}

Atualizações de partes virtuais provenientes de um job em segundo plano que não é o padrão

### SharedMergeTreeVirtualPartsUpdatesFromPeer {#sharedmergetreevirtualpartsupdatesfrompeer}

Contagem de atualizações de partes virtuais do peer

### SharedMergeTreeVirtualPartsUpdatesFromPeerMicroseconds {#sharedmergetreevirtualpartsupdatesfrompeermicroseconds}

Atualizações de partes virtuais do peer em microssegundos

### SharedMergeTreeVirtualPartsUpdatesFromZooKeeper {#sharedmergetreevirtualpartsupdatesfromzookeeper}

Número de atualizações de partes virtuais do ZooKeeper

### SharedMergeTreeVirtualPartsUpdatesFromZooKeeperFullyCached {#sharedmergetreevirtualpartsupdatesfromzookeeperfullycached}

Número de descobertas de partes virtuais em que todas as partições vieram do cache de descoberta e nada foi buscado do ZooKeeper

### SharedMergeTreeVirtualPartsUpdatesFromZooKeeperMicroseconds {#sharedmergetreevirtualpartsupdatesfromzookeepermicroseconds}

Atualizações de partes virtuais do ZooKeeper, em microssegundos

### SharedMergeTreeVirtualPartsUpdatesFromZooKeeperPartitions {#sharedmergetreevirtualpartsupdatesfromzookeeperpartitions}

Número de partições descobertas no ZooKeeper durante a descoberta de partes virtuais porque suas estatísticas não estavam presentes ou estavam desatualizadas no cache de descoberta

### SharedMergeTreeVirtualPartsUpdatesFromZooKeeperPartitionsFromCache {#sharedmergetreevirtualpartsupdatesfromzookeeperpartitionsfromcache}

Número de partições ignoradas durante a descoberta de partes virtuais porque suas estatísticas já estavam atualizadas no cache de descoberta

### SharedMergeTreeVirtualPartsUpdatesLeaderFailedElection {#sharedmergetreevirtualpartsupdatesleaderfailedelection}

Falha na eleição do líder das atualizações de partes virtuais

### SharedMergeTreeVirtualPartsUpdatesLeaderSuccessfulElection {#sharedmergetreevirtualpartsupdatesleadersuccessfulelection}

Eleição do líder de atualizações de partes virtuais bem-sucedida

### SharedMergeTreeVirtualPartsUpdatesPeerNotFound {#sharedmergetreevirtualpartsupdatespeernotfound}

As atualizações virtuais do peer falharam porque nenhum peer foi encontrado

### SharedPartsLockHoldMicroseconds {#sharedpartslockholdmicroseconds}

Tempo total durante o qual o bloqueio das partes de dados compartilhados foi mantido em tabelas MergeTree

### SharedPartsLocks {#sharedpartslocks}

Número de vezes que o bloqueio de partes de dados compartilhados foi adquirido para tabelas MergeTree

### SharedPartsLockWaitMicroseconds {#sharedpartslockwaitmicroseconds}

Tempo total de espera pelo bloqueio de partes de dados compartilhadas em tabelas MergeTree

### SleepFunctionCalls {#sleepfunctioncalls}

Número de vezes que uma função sleep (`sleep`, `sleepEachRow`) foi chamada.

### SleepFunctionElapsedMicroseconds {#sleepfunctionelapsedmicroseconds}

Tempo gasto em pausa em uma função sleep (sleep, sleepEachRow).

### SleepFunctionMicroseconds {#sleepfunctionmicroseconds}

Tempo definido para pausa em uma função sleep (`sleep`, `sleepEachRow`).

### SlowRead {#slowread}

Número de leituras lentas de um arquivo. Isso indica sobrecarga do sistema. Os limiares são controlados pelas configurações `read_backoff_*`.

### Falhas de página leves {#softpagefaults}

O número de falhas de página leves nas threads de execução de consultas. Uma falha de página leve geralmente indica que não houve correspondência no cache do alocador de memória, o que exige um novo mapeamento de memória pelo sistema operacional e a subsequente alocação de uma página de memória física.

### StatelessWorkerCreateLeaseErrors {#statelessworkercreateleaseerrors}

Número de requisições create&#95;lease malsucedidas ao serviço de discovery de workers sem estado (qualquer erro ou tempo limite).

### StatelessWorkerCreateLeaseMicroseconds {#statelessworkercreateleasemicroseconds}

Tempo total gasto em requests de create&#95;lease para o serviço de discovery de workers sem estado.

### StatelessWorkerCreateLeaseRequests {#statelessworkercreateleaserequests}

Número de requisições create&#95;lease enviadas ao serviço de discovery de workers sem estado para adquirir workers.

### StatelessWorkerDiscoveryCreateLeaseErrors {#statelessworkerdiscoverycreateleaseerrors}

Número de requisições create&#95;lease que o serviço de discovery de workers sem estado respondeu com erro.

### StatelessWorkerDiscoveryCreateLeaseMicroseconds {#statelessworkerdiscoverycreateleasemicroseconds}

Tempo total que o serviço de discovery de workers sem estado gastou processando requests create&#95;lease.

### StatelessWorkerDiscoveryCreateLeaseRequests {#statelessworkerdiscoverycreateleaserequests}

Número de requisições create&#95;lease processadas pelo serviço de discovery de workers sem estado.

### StatelessWorkerDiscoveryCreateWorkerErrors {#statelessworkerdiscoverycreateworkererrors}

Número de requests create&#95;worker que o serviço de discovery de workers sem estado respondeu com erro.

### StatelessWorkerDiscoveryCreateWorkerMicroseconds {#statelessworkerdiscoverycreateworkermicroseconds}

Tempo total que o serviço de discovery de workers sem estado gastou processando requisições create&#95;worker.

### StatelessWorkerDiscoveryCreateWorkerRequests {#statelessworkerdiscoverycreateworkerrequests}

Número de requests create&#95;worker atendidas pelo serviço de discovery de workers sem estado.

### StatelessWorkerDiscoveryDeleteWorkerErrors {#statelessworkerdiscoverydeleteworkererrors}

Número de requests `delete_worker` que o serviço de discovery de workers sem estado respondeu com um error.

### StatelessWorkerDiscoveryDeleteWorkerMicroseconds {#statelessworkerdiscoverydeleteworkermicroseconds}

Tempo total que o serviço de discovery de workers sem estado gastou processando requests delete&#95;worker.

### StatelessWorkerDiscoveryDeleteWorkerRequests {#statelessworkerdiscoverydeleteworkerrequests}

Número de requisições delete&#95;worker processadas pelo serviço de discovery de workers sem estado.

### StatelessWorkerDiscoveryGetLeaseErrors {#statelessworkerdiscoverygetleaseerrors}

Número de requisições get&#95;lease que o serviço de discovery de workers sem estado respondeu com erro.

### StatelessWorkerDiscoveryGetLeaseMicroseconds {#statelessworkerdiscoverygetleasemicroseconds}

Tempo total que o serviço de discovery de workers sem estado passou processando requisições get&#95;lease.

### StatelessWorkerDiscoveryGetLeaseRequests {#statelessworkerdiscoverygetleaserequests}

Número de requests get&#95;lease atendidas pelo serviço de discovery de workers sem estado.

### StatelessWorkerDiscoveryGetWorkerErrors {#statelessworkerdiscoverygetworkererrors}

Número de requisições get&#95;worker que o serviço de discovery de workers sem estado respondeu com erro.

### StatelessWorkerDiscoveryGetWorkerMicroseconds {#statelessworkerdiscoverygetworkermicroseconds}

Tempo total que o serviço de discovery de workers sem estado gastou processando requisições get&#95;worker.

### StatelessWorkerDiscoveryGetWorkerRequests {#statelessworkerdiscoverygetworkerrequests}

Número de requisições get&#95;worker atendidas pelo serviço de discovery de workers sem estado.

### StatelessWorkerDiscoveryHeartbeatErrors {#statelessworkerdiscoveryheartbeaterrors}

Número de requisições de heartbeat às quais o serviço de discovery de workers sem estado respondeu com erro.

### StatelessWorkerDiscoveryHeartbeatMicroseconds {#statelessworkerdiscoveryheartbeatmicroseconds}

Tempo total que o serviço de discovery de workers sem estado gastou processando requests de heartbeat.

### StatelessWorkerDiscoveryHeartbeatRequests {#statelessworkerdiscoveryheartbeatrequests}

Número de requisições de heartbeat processadas pelo serviço de discovery de workers sem estado.

### StatelessWorkerDiscoveryHeartbeatsRejected {#statelessworkerdiscoveryheartbeatsrejected}

Número de heartbeats rejeitados pelo serviço de discovery de workers sem estado porque o worker já havia sido removido.

### StatelessWorkerDiscoveryInternalErrors {#statelessworkerdiscoveryinternalerrors}

Número de requests que o serviço de discovery de workers sem estado não conseguiu tratar devido a um erro interno (uma exception ou uma falha no armazenamento de coordenação), em vez de rejeitar um request inválido.

### StatelessWorkerDiscoveryKeeperTransactionRetries {#statelessworkerdiscoverykeepertransactionretries}

Número de transações de escrita que o serviço de discovery de worker sem estado repetiu porque o estado de seu armazenamento de coordenação (Keeper) foi modificado concorrentemente.

### StatelessWorkerDiscoveryListWorkersErrors {#statelessworkerdiscoverylistworkerserrors}

Número de requisições list&#95;workers que o serviço de discovery de workers sem estado respondeu com erro.

### StatelessWorkerDiscoveryListWorkersMicroseconds {#statelessworkerdiscoverylistworkersmicroseconds}

Tempo total que o serviço de discovery de workers sem estado passou processando requisições `list_workers`.

### StatelessWorkerDiscoveryListWorkersRequests {#statelessworkerdiscoverylistworkersrequests}

Número de requisições list&#95;workers processadas pelo serviço de discovery de workers sem estado.

### StatelessWorkerDiscoveryTenantErrors {#statelessworkerdiscoverytenanterrors}

Número de requisições de gerenciamento de tenants que o serviço de discovery de workers sem estado respondeu com um erro.

### StatelessWorkerDiscoveryTenantMicroseconds {#statelessworkerdiscoverytenantmicroseconds}

Tempo total que o serviço de discovery de workers sem estado gastou processando requests de gerenciamento de tenants.

### StatelessWorkerDiscoveryTenantRequests {#statelessworkerdiscoverytenantrequests}

Número de requisições de gerenciamento de tenant (criar, atualizar, excluir, obter ou listar tenant) processadas pelo serviço de discovery de workers sem estado.

### StatelessWorkerDiscoveryUpdateLeaseErrors {#statelessworkerdiscoveryupdateleaseerrors}

Número de requisições update&#95;lease que o serviço de discovery de workers sem estado respondeu com erro.

### StatelessWorkerDiscoveryUpdateLeaseMicroseconds {#statelessworkerdiscoveryupdateleasemicroseconds}

Tempo total que o serviço de discovery de workers sem estado gastou processando requisições update&#95;lease.

### StatelessWorkerDiscoveryUpdateLeaseRequests {#statelessworkerdiscoveryupdateleaserequests}

Número de requisições update&#95;lease processadas pelo serviço de discovery de workers sem estado.

### StatelessWorkerDiscoveryWorkersHeartbeatExpired {#statelessworkerdiscoveryworkersheartbeatexpired}

Número de workers sem estado reivindicados pelo serviço de discovery de workers sem estado por terem parado de enviar heartbeats.

### StatelessWorkerDiscoveryWorkersLeased {#statelessworkerdiscoveryworkersleased}

Número de workers sem estado recém-alocados a tenants pelo serviço de discovery de workers sem estado.

### StatelessWorkerDiscoveryWorkersLeaseExpired {#statelessworkerdiscoveryworkersleaseexpired}

Número de workers sem estado recuperados pelo serviço de discovery de workers sem estado por terem o lease expirado.

### StatelessWorkerDiscoveryWorkersUnavailable {#statelessworkerdiscoveryworkersunavailable}

Número de workers sem estado solicitados pelos tenants que o serviço de discovery de workers sem estado não pôde fornecer, seja por não haver workers livres disponíveis, seja porque o limite de workers do tenant foi atingido.

### StatelessWorkerGetLeaseErrors {#statelessworkergetleaseerrors}

Número de requisições get&#95;lease malsucedidas ao serviço de discovery de workers sem estado (qualquer erro ou tempo limite).

### StatelessWorkerGetLeaseMicroseconds {#statelessworkergetleasemicroseconds}

Tempo total gasto em requests get&#95;lease ao serviço de discovery de workers sem estado.

### StatelessWorkerGetLeaseRequests {#statelessworkergetleaserequests}

Número de requisições get&#95;lease enviadas ao serviço de discovery de workers sem estado para atualizar a lista de workers em lease.

### StatelessWorkerNotAccessible {#statelessworkernotaccessible}

Número de vezes que um worker sem estado obtido por lease foi considerado ainda não acessível e excluído da alocação de consultas até passar na verificação de acessibilidade.

### StatelessWorkerProvided {#statelessworkerprovided}

Número de workers sem estado fornecidos às consultas para execução de consultas distribuídas.

### StatelessWorkerProvisioningMicroseconds {#statelessworkerprovisioningmicroseconds}

Tempo total que as consultas passaram aguardando o provisionamento de workers sem estado.

### StatelessWorkerProvisioningWaits {#statelessworkerprovisioningwaits}

Número de vezes que uma consulta teve de aguardar até que o serviço de discovery de workers sem estado fornecesse workers.

### StatelessWorkerRequested {#statelessworkerrequested}

Número de workers sem estado solicitados pelas consultas para execução distribuída de consultas.

### StatelessWorkerServerHeartbeatErrors {#statelessworkerserverheartbeaterrors}

Número de heartbeats com falha enviados pelos workers sem estado ao serviço de discovery de workers sem estado (qualquer erro ou tempo limite).

### StatelessWorkerServerHeartbeatMicroseconds {#statelessworkerserverheartbeatmicroseconds}

Tempo total que os workers sem estado passaram enviando heartbeats ao serviço de discovery de workers sem estado.

### StatelessWorkerServerHeartbeatRequests {#statelessworkerserverheartbeatrequests}

Número de heartbeats que um worker sem estado enviou ao serviço de discovery de workers sem estado.

### StatelessWorkerServerLeasesLost {#statelessworkerserverleaseslost}

Número de vezes em que o lease de um worker sem estado terminou e ele passou a aguardar o desligamento.

### StatelessWorkerServerRegisterErrors {#statelessworkerserverregistererrors}

Número de requisições de registro malsucedidas enviadas por workers sem estado ao serviço de discovery de workers sem estado (qualquer erro ou tempo limite).

### StatelessWorkerServerRegisterMicroseconds {#statelessworkerserverregistermicroseconds}

Tempo total que os workers sem estado passaram se registrando no serviço de discovery de workers sem estado.

### StatelessWorkerServerRegisterRequests {#statelessworkerserverregisterrequests}

Número de requisições de registro que um worker sem estado enviou ao serviço de discovery de workers sem estado.

### StatelessWorkerServerTenantBindings {#statelessworkerservertenantbindings}

Número de vezes que um worker sem estado foi atribuído a um tenant para atender às suas consultas.

### StatelessWorkerTimeToAvailableMicroseconds {#statelessworkertimetoavailablemicroseconds}

Tempo total (somado entre os workers) entre o momento em que um worker sem estado arrendado aparece pela primeira vez no serviço de discovery e o momento em que ele passa na verificação de acessibilidade e se torna disponível para a execução de consultas.

### StatelessWorkerUpdateLeaseErrors {#statelessworkerupdateleaseerrors}

Número de requisições update&#95;lease que falharam ao serviço de discovery de workers sem estado (qualquer erro ou tempo limite).

### StatelessWorkerUpdateLeaseMicroseconds {#statelessworkerupdateleasemicroseconds}

Tempo total gasto em requisições update&#95;lease ao serviço de discovery de workers sem estado.

### StatelessWorkerUpdateLeaseRequests {#statelessworkerupdateleaserequests}

Número de requisições update&#95;lease enviadas ao serviço de discovery de workers sem estado para renovar os leases dos workers.

### StorageBufferErrorOnFlush {#storagebuffererroronflush}

Número de vezes que um buffer na tabela &#39;Buffer&#39; não conseguiu descarregar devido a um erro de gravação na tabela de destino.

### StorageBufferFlush {#storagebufferflush}

Número de vezes que um buffer em uma tabela &#39;Buffer&#39; foi gravado.

### StorageBufferLayerLockReadersWaitMilliseconds {#storagebufferlayerlockreaderswaitmilliseconds}

Tempo de espera da camada Buffer durante a leitura.

### StorageBufferLayerLockWritersWaitMilliseconds {#storagebufferlayerlockwriterswaitmilliseconds}

Tempo de espera até que uma camada Buffer fique livre para gravação (pode ser usado para ajustar as camadas Buffer).

### StorageBufferPassedAllMinThresholds {#storagebufferpassedallminthresholds}

Número de vezes que um critério de limites mínimos foi atingido para fazer o flush de um buffer em uma tabela &#39;Buffer&#39;.

### StorageBufferPassedBytesFlushThreshold {#storagebufferpassedbytesflushthreshold}

Número de vezes que o limite de bytes para flush somente em segundo plano foi atingido para descarregar um buffer em uma tabela &#39;Buffer&#39;. Esta é uma métrica apenas para especialistas. Se você está lendo isto e não é um especialista, pare de ler.

### StorageBufferPassedBytesMaxThreshold {#storagebufferpassedbytesmaxthreshold}

Número de vezes que um critério de limite máximo de bytes foi atingido para realizar o flush de um buffer em uma tabela &#39;Buffer&#39;.

### StorageBufferPassedRowsFlushThreshold {#storagebufferpassedrowsflushthreshold}

Número de vezes que o limiar de flush apenas em segundo plano, com base no número de linhas, foi atingido para descarregar um buffer em uma tabela &#39;Buffer&#39;. Esta é uma métrica apenas para especialistas. Se você está lendo isto e não é um especialista, pare de ler.

### StorageBufferPassedRowsMaxThreshold {#storagebufferpassedrowsmaxthreshold}

Número de vezes que um critério do limite máximo de linhas foi atingido para descarregar o buffer em uma tabela &#39;Buffer&#39;.

### StorageBufferPassedTimeFlushThreshold {#storagebufferpassedtimeflushthreshold}

Número de vezes em que o limite de tempo de flush apenas em segundo plano foi atingido para descarregar um buffer em uma tabela &#39;Buffer&#39;. Esta métrica é apenas para especialistas. Se você está lendo isto e não é um especialista, pare de ler.

### StorageBufferPassedTimeMaxThreshold {#storagebufferpassedtimemaxthreshold}

Número de vezes em que o critério de tempo máximo foi atingido e acionou o flush de um buffer em uma tabela &#39;Buffer&#39;.

### StorageConnectionsCreated {#storageconnectionscreated}

Número de conexões criadas para os armazenamentos

### StorageConnectionsElapsedMicroseconds {#storageconnectionselapsedmicroseconds}

Tempo total gasto na criação de conexões para armazenamento

### StorageConnectionsErrors {#storageconnectionserrors}

Número de casos em que a criação de uma conexão com o armazenamento falhou

### StorageConnectionsExpired {#storageconnectionsexpired}

Número de conexões expiradas com os armazenamentos

### StorageConnectionsPreserved {#storageconnectionspreserved}

Número de conexões preservadas para os sistemas de armazenamento

### StorageConnectionsReset {#storageconnectionsreset}

Número de conexões reiniciadas com os armazenamentos

### StorageConnectionsReused {#storageconnectionsreused}

Número de conexões reutilizadas para os armazenamentos

### StreamingExchangeConnectionWaitMicroseconds {#streamingexchangeconnectionwaitmicroseconds}

Tempo que um sink de exchange de streaming aguardou até que a task receptora se conectasse.

### StreamingExchangeDeserializeMicroseconds {#streamingexchangedeserializemicroseconds}

Tempo gasto para descomprimir e desserializar pacotes de streaming exchange em fragmentos.

### StreamingExchangeEarlyCloses {#streamingexchangeearlycloses}

Quantas vezes uma origem de streaming exchange pediu ao seu remetente que parasse antes de o stream ser lido até o fim, por exemplo, porque um LIMIT foi atingido. Um remetente que já havia encerrado não é contabilizado.

### StreamingExchangePacketsReceived {#streamingexchangepacketsreceived}

Pacotes de dados lidos de exchanges de streaming, um por fragmento mais um pacote de fim de stream por stream.

### StreamingExchangePacketsSent {#streamingexchangepacketssent}

Pacotes de dados gravados por inteiro nos sockets dos exchanges de streaming, um por fragmento mais um pacote de fim de stream por stream. Um pacote é contabilizado quando é gravado por inteiro; um pacote truncado porque o receiver não precisava de mais dados não é contabilizado.

### StreamingExchangeReceiveBytes {#streamingexchangereceivebytes}

Bytes lidos dos sockets dos streaming exchanges de um plano de consulta distribuída. `NetworkReceiveBytes` não os contabiliza.

### StreamingExchangeReceiveWaitMicroseconds {#streamingexchangereceivewaitmicroseconds}

Tempo que uma fonte de streaming exchange aguardou pelos próximos bytes do seu remetente.

### StreamingExchangeSendBytes {#streamingexchangesendbytes}

Bytes gravados nos sockets dos streaming exchanges de um plano de consulta distribuída. `NetworkSendBytes` não os contabiliza.

### StreamingExchangeSendQueueFullMicroseconds {#streamingexchangesendqueuefullmicroseconds}

Tempo durante o qual um sink de streaming exchange não recebeu fragmentos porque sua fila de pacotes à espera do socket estava cheia.

### StreamingExchangeSerializedBytes {#streamingexchangeserializedbytes}

Bytes dos blocos Native nos pacotes de streaming exchange antes da compressão, contados uma vez por pacote em que são serializados. Em comparação com `StreamingExchangeSendBytes`, isso fornece a taxa de compressão de um shuffle ou de um gather; um broadcast envia cada pacote para cada destino, portanto, nesse caso, `StreamingExchangeSendBytes` cresce conforme o número de destinos.

### StreamingExchangeSerializeMicroseconds {#streamingexchangeserializemicroseconds}

Tempo gasto serializando e comprimindo fragmentos em pacotes de streaming exchange.

### SummingSortedMilliseconds {#summingsortedmilliseconds}

Tempo total gasto ao somar colunas classificadas

### SuspendSendingQueryToShard {#suspendsendingquerytoshard}

Número total de vezes em que o envio da consulta para o shard foi suspenso quando `async_query_sending_for_remote` está habilitado.

### SynchronousReadWaitMicroseconds {#synchronousreadwaitmicroseconds}

Tempo gasto aguardando leituras síncronas durante uma leitura local assíncrona.

### SynchronousRemoteReadWaitMicroseconds {#synchronousremotereadwaitmicroseconds}

Tempo gasto esperando por leituras remotas síncronas.

### SystemLogErrorOnFlush {#systemlogerroronflush}

Número de vezes em que algum dos logs do sistema falhou ao ser gravado na tabela de sistema correspondente. As tentativas de gravação são repetidas.

### SystemTimeMicroseconds {#systemtimemicroseconds}

Tempo total gasto pelas threads de processamento (consultas e outras tarefas) executando instruções da CPU no modo kernel do SO. É o tempo gasto em chamadas de sistema, excluindo o tempo de espera durante chamadas de sistema bloqueantes.

### TableFunctionExecute {#tablefunctionexecute}

Número de chamadas de funções de tabela.

### TextIndexDiscardHint {#textindexdiscardhint}

Número de grânulos de índice em que uma leitura direta do índice de texto foi considerada como dica, mas descartada devido à baixa seletividade.

### TextIndexDiscardPatternScan {#textindexdiscardpatternscan}

Número de vezes em que uma varredura por dicionário baseada em padrões em um índice de texto foi descartada porque o número de posting lists a serem lidas excedeu o limite.

### TextIndexGenericExclusionSearchAlgorithm {#textindexgenericexclusionsearchalgorithm}

Número de vezes que o generic exclusion search algorithm é utilizado sobre o índice de texto.

### TextIndexGenericExclusionSearchStepLimitReached {#textindexgenericexclusionsearchsteplimitreached}

Número de vezes em que a generic exclusion search sobre o índice de texto atingiu merge&#95;tree&#95;generic&#95;exclusion&#95;search&#95;max&#95;steps e aceitou os mark ranges restantes sem realizar novos splits.

### TextIndexHeaderCacheHits {#textindexheadercachehits}

Número de vezes em que um cabeçalho foi encontrado no cache.

### TextIndexHeaderCacheMisses {#textindexheadercachemisses}

Número de vezes em que um cabeçalho não foi encontrado no cache.

### TextIndexLazyAdvanceCount {#textindexlazyadvancecount}

Número de operações de avanço realizadas no modo preguiçoso da lista de postings.

### TextIndexLazyBlocksSkippedResolved {#textindexlazyblocksskippedresolved}

Número de blocos compactados ignorados porque a região de saída já havia sido resolvida (todos 1 para OR, todos 0 para AND) no modo preguiçoso de lista de postings.

### TextIndexLazyBruteForceIntersections {#textindexlazybruteforceintersections}

Número de interseções por força bruta realizadas no modo preguiçoso de lista de postings.

### TextIndexLazyLeapfrogIntersections {#textindexlazyleapfrogintersections}

Número de interseções leapfrog realizadas no modo preguiçoso de listas de postings.

### TextIndexLazyPackedBlocksDecoded {#textindexlazypackedblocksdecoded}

Número de blocos compactados decodificados no modo preguiçoso da lista de postings.

### TextIndexLazySegmentsBuilt {#textindexlazysegmentsbuilt}

Número de segmentos realmente lidos e decodificados (falhas de cache) no modo preguiçoso da lista de postings.

### TextIndexLazySegmentsPrepared {#textindexlazysegmentsprepared}

Número de segmentos preparados (lidos do disco ou recuperados do cache) no modo preguiçoso de lista de postings.

### TextIndexLazySegmentsSkippedDense {#textindexlazysegmentsskippeddense}

Número de segmentos totalmente densos preenchidos de uma só vez (`memset` para OR, incremento para AND), em vez de decodificar blocos, no modo preguiçoso de lista de postings.

### TextIndexLazySegmentsSkippedResolved {#textindexlazysegmentsskippedresolved}

Número de segmentos ignorados porque a região de saída já havia sido determinada (todos 1s para OR, todos 0s para AND) no modo preguiçoso de lista de postings.

### TextIndexPhraseCandidates {#textindexphrasecandidates}

Linhas candidatas (interseção de postings) examinadas pela busca de frase baseada em candidatos.

### TextIndexPhraseFallbacks {#textindexphrasefallbacks}

Número de vezes que uma consulta de phrase ignorou o índice de texto porque se estimou que a phrase corresponderia a mais linhas do que &#39;text&#95;index&#95;hint&#95;max&#95;selectivity&#39;, sendo avaliada diretamente na coluna.

### TextIndexPhraseMatchMicroseconds {#textindexphrasematchmicroseconds}

Tempo total gasto na correspondência de posições de frases nas linhas candidatas.

### TextIndexPhraseSearches {#textindexphrasesearches}

Número de buscas por frase (phrase searches) executadas sobre um índice de texto. Buscas repetidas da mesma phrase na mesma parte são atendidas pelo cache e não são contabilizadas.

### TextIndexPositionsBlocksRead {#textindexpositionsblocksread}

Número de blocos de posições lidos para corresponder a consultas de phrase sobre um índice de texto. Um block que abrange duas candidate windows é contado uma vez por leitura.

### TextIndexPositionsBlocksTotal {#textindexpositionsblockstotal}

Número total de blocos de posições dos tokens da phrase antes de selecionar quais deles serão lidos.

### TextIndexPositionsBytesRead {#textindexpositionsbytesread}

Bytes de blocos de posições lidos para corresponder a consultas de phrase em um índice de texto.

### TextIndexPositionsDecodeMicroseconds {#textindexpositionsdecodemicroseconds}

Tempo total gasto para decodificar listas de posições (.pos) do índice de texto em buscas por frase.

### TextIndexPostingsCacheHits {#textindexpostingscachehits}

Número de vezes que uma lista de postings do índice de texto foi encontrada no cache.

### TextIndexPostingsCacheMisses {#textindexpostingscachemisses}

Número de vezes em que uma lista de postings de um índice de texto não foi encontrada no cache.

### TextIndexReadDictionaryBlocks {#textindexreaddictionaryblocks}

Número de vezes que um bloco do dicionário do índice de texto foi lido do disco.

### TextIndexReaderTotalMicroseconds {#textindexreadertotalmicroseconds}

Tempo total gasto na leitura do índice de texto.

### TextIndexReadGranulesMicroseconds {#textindexreadgranulesmicroseconds}

Tempo total gasto na leitura e na análise dos grânulos do índice de texto.

### TextIndexReadPostings {#textindexreadpostings}

Número de vezes que uma lista de postings foi lida a partir do índice de texto.

### TextIndexReadSparseIndexBlocks {#textindexreadsparseindexblocks}

Número de vezes em que um bloco de índice esparso foi lido do índice de texto.

### TextIndexTemporarySegmentsWritten {#textindextemporarysegmentswritten}

Número de segmentos temporários gravados durante a construção de índices de texto.

### TextIndexTokensCacheHits {#textindextokenscachehits}

Número de vezes em que as informações de um token do índice de texto foram encontradas no cache.

### TextIndexTokensCacheMisses {#textindextokenscachemisses}

Número de vezes em que as informações sobre um token do índice de texto não foram encontradas no cache.

### TextIndexTokensCacheNegativeHits {#textindextokenscachenegativehits}

Número de vezes em que um token ausente de índice de texto foi encontrado no cache.

### TextIndexTokensCacheNegativeMisses {#textindextokenscachenegativemisses}

Número de tokens de índice de texto ausentes adicionados ao cache após um lookup no dicionário.

### TextIndexUsedEmbeddedPostings {#textindexusedembeddedpostings}

Número de vezes que uma lista de postings incorporada ao Dicionário foi utilizada.

### TextIndexUseHint {#textindexusehint}

Número de grânulos de índice para os quais uma leitura direta do índice de texto foi adicionada como dica e utilizada.

### ThreadPoolReaderPageCacheHit {#threadpoolreaderpagecachehit}

Número de vezes em que a leitura no ThreadPoolReader foi feita a partir do cache de páginas.

### ThreadPoolReaderPageCacheHitBytes {#threadpoolreaderpagecachehitbytes}

Número de bytes lidos pelo ThreadPoolReader a partir do cache de páginas.

### ThreadPoolReaderPageCacheHitElapsedMicroseconds {#threadpoolreaderpagecachehitelapsedmicroseconds}

Tempo gasto na leitura de dados do cache de página no ThreadPoolReader.

### ThreadPoolReaderPageCacheMiss {#threadpoolreaderpagecachemiss}

Número de vezes em que a leitura dentro de ThreadPoolReader não foi feita a partir do cache de página e foi passada para o pool de threads.

### ThreadPoolReaderPageCacheMissBytes {#threadpoolreaderpagecachemissbytes}

Número de bytes lidos no ThreadPoolReader quando a leitura não foi feita a partir do cache de página e foi encaminhada ao pool de threads.

### ThreadPoolReaderPageCacheMissElapsedMicroseconds {#threadpoolreaderpagecachemisselapsedmicroseconds}

Tempo gasto lendo dados no job assíncrono em ThreadPoolReader, quando a leitura não foi feita a partir do cache de páginas.

### ThreadpoolReaderPrepareMicroseconds {#threadpoolreaderpreparemicroseconds}

Tempo gasto na preparação (por exemplo, na chamada ao método `seek()` do leitor)

### ThreadpoolReaderReadBytes {#threadpoolreaderreadbytes}

Bytes lidos por uma tarefa do pool de thread durante a leitura assíncrona

### ThreadpoolReaderSubmit {#threadpoolreadersubmit}

Bytes lidos por uma task do pool de thread durante a leitura assíncrona

### ThreadpoolReaderSubmitLookupInCacheMicroseconds {#threadpoolreadersubmitlookupincachemicroseconds}

Quanto tempo foi gasto verificando se o conteúdo está em cache

### ThreadpoolReaderSubmitReadSynchronously {#threadpoolreadersubmitreadsynchronously}

Quantas vezes deixamos de agendar uma tarefa no pool de threads e, em vez disso, fizemos a leitura de forma síncrona

### ThreadpoolReaderSubmitReadSynchronouslyBytes {#threadpoolreadersubmitreadsynchronouslybytes}

Quantidade de bytes lidos de forma síncrona

### ThreadpoolReaderSubmitReadSynchronouslyMicroseconds {#threadpoolreadersubmitreadsynchronouslymicroseconds}

Quanto tempo foi gasto em leituras síncronas

### ThreadpoolReaderTaskMicroseconds {#threadpoolreadertaskmicroseconds}

Tempo gasto para obter os dados em leitura assíncrona

### ThrottlerSleepMicroseconds {#throttlersleepmicroseconds}

Tempo total em que uma consulta permaneceu em espera para obedecer a todas as configurações de limitação de taxa.

### TinyS3Clients {#tinys3clients}

Número de cópias de clientes S3 que reutilizam o provedor de autenticação existente de outro cliente.

### UncompressedCacheHits {#uncompressedcachehits}

Número de vezes em que um bloco de dados foi encontrado no cache de dados não compactados (evitando a descompressão).

### UncompressedCacheMisses {#uncompressedcachemisses}

Número de vezes em que um bloco de dados não foi encontrado no cache não compactado (exigindo descompressão).

### UncompressedCacheWeightLost {#uncompressedcacheweightlost}

Número de bytes removidos do cache não compactado.

### UniqueKeyBitmapLoadMicroseconds {#uniquekeybitmaploadmicroseconds}

Tempo gasto na leitura e desserialização de arquivos sidecar do bitmap de exclusão de UNIQUE KEY quando ocorre um miss no DeleteBitmapCache.

### Atualizações do bitmap de UNIQUE KEY {#uniquekeybitmapupdates}

Número de arquivos sidecar do bitmap de exclusão de UNIQUE KEY gravados (um por substituição / commit de DELETE).

### UniqueKeyIndexCacheHits {#uniquekeyindexcachehits}

Número de vezes em que uma entrada foi encontrada no cache de índice UNIQUE KEY, evitando a necessidade de carregar um bloco SST.

### UniqueKeyIndexCacheLookupMicroseconds {#uniquekeyindexcachelookupmicroseconds}

Tempo decorrido gasto em `UniqueKeyIndexCache::Lookup` + `UniqueKeyIndexCache::Insert` (adaptador `CacheBase` no lado do ClickHouse para o cache de blocos do RocksDB).

### UniqueKeyIndexCacheMisses {#uniquekeyindexcachemisses}

Número de vezes em que uma entrada não foi encontrada no cache de índice UNIQUE KEY, e por isso foi necessário carregar um bloco SST do disco.

### UniqueKeyLoadTimeSSTRebuildCount {#uniquekeyloadtimesstrebuildcount}

Número de partes UNIQUE KEY cujo `unique_key_index.sst` foi reconstruído no carregamento após a janela de crash antes do flush.

### UniqueKeyLoadTimeSSTRebuildMicroseconds {#uniquekeyloadtimesstrebuildmicroseconds}

Tempo total gasto na reconstrução de `unique_key_index.sst` no momento do carregamento (leitura sequencial das colunas UK + escrita do SST).

### UniqueKeySSTWriteMicroseconds {#uniquekeysstwritemicroseconds}

Tempo total decorrido durante o ciclo de vida de um `SSTIndexWriter` — cobre o `Open` do SST, cada Put de `addEncoded` e `Finish` + finalização do `WriteBuffer` (+ fsync) em `finish` (os bytes do SST são enviados diretamente para o `WriteBuffer` do armazenamento da parte). Exclui o trabalho que os helpers estáticos fazem antes da construção do writer (encode + ordenação de caminhos sem prefixo). Emitido uma vez por writer.

### USearchAddComputedDistances {#usearchaddcomputeddistances}

Número de vezes em que a distância foi calculada ao adicionar vetores aos índices do USearch.

### USearchAddCount {#usearchaddcount}

Número de vetores adicionados aos índices do USearch.

### USearchAddVisitedMembers {#usearchaddvisitedmembers}

Número de nós visitados ao adicionar vetores a índices usearch.

### USearchSearchComputedDistances {#usearchsearchcomputeddistances}

Número de vezes em que a distância foi calculada ao pesquisar índices USearch.

### USearchSearchCount {#usearchsearchcount}

Número de operações de busca realizadas nos índices do usearch.

### USearchSearchVisitedMembers {#usearchsearchvisitedmembers}

Número de nós visitados durante a busca em índices usearch.

### UserThrottlerBytes {#userthrottlerbytes}

Bytes processados pelo limitador &#39;max&#95;network&#95;bandwidth&#95;for&#95;user&#39;.

### UserThrottlerSleepMicroseconds {#userthrottlersleepmicroseconds}

Tempo total em que uma consulta ficou suspensa para obedecer ao throttling de `max_network_bandwidth_for_user`.

### UserTimeMicroseconds {#usertimemicroseconds}

Tempo total gasto por threads de processamento (consultas e outras tarefas) executando instruções da CPU em modo de usuário. Isso inclui o tempo em que o pipeline da CPU ficou bloqueado devido ao acesso à memória principal, falhas de cache, previsões incorretas de desvios, hyper-threading etc.

### VectorSimilarityIndexCacheHits {#vectorsimilarityindexcachehits}

Número de vezes que um grânulo do índice foi encontrado no cache do índice vetorial.

### VectorSimilarityIndexCacheMisses {#vectorsimilarityindexcachemisses}

Número de vezes que um grânulo de índice não foi encontrado no cache do índice vetorial e precisou ser lido do disco.

### VectorSimilarityIndexCacheWeightLost {#vectorsimilarityindexcacheweightlost}

Número aproximado de bytes removidos do cache do índice vetorial.

### VersionedCollapsingSortedMilliseconds {#versionedcollapsingsortedmilliseconds}

Tempo total gasto durante o colapso de versões de colunas ordenadas

### WaitMarksLoadMicroseconds {#waitmarksloadmicroseconds}

Tempo gasto no carregamento de marcas

### WaitPrefetchTaskMicroseconds {#waitprefetchtaskmicroseconds}

Tempo gasto à espera do leitor pré-carregado

### WasmDeserializationMicroseconds {#wasmdeserializationmicroseconds}

Tempo gasto na execução de código WebAssembly

### WasmGuestExecuteMicroseconds {#wasmguestexecutemicroseconds}

Tempo gasto na execução de código WebAssembly

### WasmMemoryAllocated {#wasmmemoryallocated}

Memória total alocada para compartimentos do WebAssembly

### WasmModuleInstatiate {#wasmmoduleinstatiate}

Número de compartimentos do WebAssembly criados

### WasmSerializationMicroseconds {#wasmserializationmicroseconds}

Tempo gasto na execução de código WebAssembly

### WasmTotalExecuteMicroseconds {#wasmtotalexecutemicroseconds}

Tempo gasto na execução de código WebAssembly

### WriteBufferFromFileDescriptorWrite {#writebufferfromfiledescriptorwrite}

Número de operações de escrita (`write`/`pwrite`) em um descritor de arquivo. Não inclui sockets.

### WriteBufferFromFileDescriptorWriteBytes {#writebufferfromfiledescriptorwritebytes}

Número de bytes gravados em descritores de arquivo. Se o arquivo estiver compactado, será exibido o tamanho dos dados compactados.

### WriteBufferFromFileDescriptorWriteFailed {#writebufferfromfiledescriptorwritefailed}

Número de vezes que a escrita (`write`/`pwrite`) em um descritor de arquivo falhou.

### WriteBufferFromHTTPBytes {#writebufferfromhttpbytes}

Tamanho total, em bytes, do payload recebido e enviado pelo WriteBufferFromHTTP. Não inclui os cabeçalhos HTTP.

### WriteBufferFromHTTPRequestsSent {#writebufferfromhttprequestssent}

Número de requisições HTTP enviadas por WriteBufferFromHTTP

### WriteBufferFromS3Bytes {#writebufferfroms3bytes}

Bytes gravados no S3.

### WriteBufferFromS3Microseconds {#writebufferfroms3microseconds}

Tempo gasto gravando no S3.

### WriteBufferFromS3RequestsErrors {#writebufferfroms3requestserrors}

Número de exceções durante a gravação no S3.

### WriteBufferFromS3WaitInflightLimitMicroseconds {#writebufferfroms3waitinflightlimitmicroseconds}

Tempo gasto aguardando até que algumas das solicitações atuais sejam concluídas quando sua quantidade atinge o limite definido por s3&#95;max&#95;inflight&#95;parts&#95;for&#95;one&#95;file.

### ZooKeeperBytesReceived {#zookeeperbytesreceived}

Número de bytes recebidos pela rede durante a comunicação com o ZooKeeper.

### ZooKeeperBytesSent {#zookeeperbytessent}

Número de bytes enviados pela rede durante a comunicação com o ZooKeeper.

### ZooKeeperCheck {#zookeepercheck}

Número de solicitações &#39;check&#39; para o ZooKeeper. Em geral, elas só fazem sentido como parte de uma transação complexa, não isoladamente.

### ZooKeeperClose {#zookeeperclose}

Número de vezes que a conexão com o ZooKeeper foi encerrada voluntariamente.

### ZooKeeperCreate {#zookeepercreate}

Número de solicitações &#39;create&#39; ao ZooKeeper.

### ZooKeeperExists {#zookeeperexists}

Número de requisições &#39;exists&#39; ao ZooKeeper.

### ZooKeeperGet {#zookeeperget}

Número de solicitações &#39;get&#39; ao ZooKeeper.

### ZooKeeperGetACL {#zookeepergetacl}

Número de solicitações `getACL` ao ZooKeeper.

### ZooKeeperHardwareExceptions {#zookeeperhardwareexceptions}

Número de exceções durante o trabalho com o ZooKeeper relacionadas à rede (perda de conexão ou semelhante).

### ZooKeeperInit {#zookeeperinit}

Número de vezes em que a conexão com o ZooKeeper foi estabelecida.

### ZooKeeperList {#zookeeperlist}

Número de solicitações de &#39;list&#39; (getChildren) ao ZooKeeper.

### ZooKeeperListRecursive {#zookeeperlistrecursive}

Número de requisições &#39;listRecursive&#39; ao ZooKeeper.

### ZooKeeperMulti {#zookeepermulti}

Número de requisições &#39;multi&#39; ao ZooKeeper (transações compostas).

### ZooKeeperMultiRead {#zookeepermultiread}

Número de solicitações de leitura &#39;multi&#39; ao ZooKeeper (transações compostas).

### ZooKeeperMultiWrite {#zookeepermultiwrite}

Número de solicitações &#39;multi&#39; de gravação para o ZooKeeper (transações compostas).

### ZooKeeperOtherExceptions {#zookeeperotherexceptions}

Número de exceções ao trabalhar com o ZooKeeper, além de ZooKeeperUserExceptions e ZooKeeperHardwareExceptions.

### ZooKeeperReconfig {#zookeeperreconfig}

Número de solicitações &#39;reconfig&#39; ao ZooKeeper.

### ZooKeeperRemove {#zookeeperremove}

Número de solicitações &#39;remove&#39; ao ZooKeeper.

### ZooKeeperSet {#zookeeperset}

Número de solicitações &#39;set&#39; ao ZooKeeper.

### ZooKeeperSync {#zookeepersync}

Número de solicitações de &#39;sync&#39; para o ZooKeeper. Essas solicitações raramente são necessárias ou aproveitáveis.

### ZooKeeperTransactions {#zookeepertransactions}

Número de operações do ZooKeeper, que incluem tanto operações de leitura quanto de escrita, bem como multitransações.

### ZooKeeperUserExceptions {#zookeeperuserexceptions}

Número de exceções ao trabalhar com o ZooKeeper relacionadas aos dados (nó inexistente, versão inválida ou algo semelhante).

### ZooKeeperWaitMicroseconds {#zookeeperwaitmicroseconds}

Número de microssegundos gastos aguardando respostas do ZooKeeper após a criação de uma solicitação, somado em todas as threads que fizeram a solicitação.

### ZooKeeperWatchCallbackDurationMicroseconds {#zookeeperwatchcallbackdurationmicroseconds}

Tempo total gasto nos callbacks de watch do ZooKeeper (thread de rede).

### ZooKeeperWatchCallbackErrors {#zookeeperwatchcallbackerrors}

Número de exceções geradas pelos callbacks de watch do ZooKeeper.

### ZooKeeperWatchResponse {#zookeeperwatchresponse}

Número de vezes que uma notificação de watch foi recebida do ZooKeeper.

### ZooKeeperWatchTriggeredBackupCoordination {#zookeeperwatchtriggeredbackupcoordination}

Número de notificações de watch encaminhadas para a coordenação de backup / restauração.

### ZooKeeperWatchTriggeredClusterDiscovery {#zookeeperwatchtriggeredclusterdiscovery}

Número de notificações de watch encaminhadas para o ClusterDiscovery.

### ZooKeeperWatchTriggeredDistributedDDL {#zookeeperwatchtriggereddistributedddl}

Número de notificações de watch despachadas para a fila DDLWorker.

### ZooKeeperWatchTriggeredMaterializedViewRefresh {#zookeeperwatchtriggeredmaterializedviewrefresh}

Número de notificações de watch enviadas à coordenação de atualização de MaterializedView.

### ZooKeeperWatchTriggeredObjectStorageQueue {#zookeeperwatchtriggeredobjectstoragequeue}

Número de notificações de watch encaminhadas para ObjectStorageQueue / S3Queue.

### ZooKeeperWatchTriggeredOther {#zookeeperwatchtriggeredother}

Número de notificações de watch encaminhadas para callbacks não categorizados.

### ZooKeeperWatchTriggeredReplicatedAccessControl {#zookeeperwatchtriggeredreplicatedaccesscontrol}

Número de notificações de watch enviadas para o ReplicatedAccessStorage.

### ZooKeeperWatchTriggeredReplicatedMergeTreeLeaderElection {#zookeeperwatchtriggeredreplicatedmergetreeleaderelection}

Número de notificações de watch encaminhadas para a eleição de líder / bloqueios de réplica do ReplicatedMergeTree.

### ZooKeeperWatchTriggeredReplicatedMergeTreeLog {#zookeeperwatchtriggeredreplicatedmergetreelog}

Número de notificações de watch encaminhadas para entradas de log do ReplicatedMergeTree.

### ZooKeeperWatchTriggeredReplicatedMergeTreeMutations {#zookeeperwatchtriggeredreplicatedmergetreemutations}

Número de notificações de watch encaminhadas para mutações do ReplicatedMergeTree.

### ZooKeeperWatchTriggeredReplicatedMergeTreeReplicaSync {#zookeeperwatchtriggeredreplicatedmergetreereplicasync}

Número de notificações de watch despachadas enquanto se aguarda que as réplicas processem registros de log (SYNC REPLICA, ALTER, mutações).

### ZooKeeperWatchTriggeredUserDefinedSQLObjects {#zookeeperwatchtriggereduserdefinedsqlobjects}

Número de notificações de watch encaminhadas para UserDefinedSQLObjectsZooKeeperStorage.

### ZooKeeperWatchTriggeredWorkloadEntity {#zookeeperwatchtriggeredworkloadentity}

Número de notificações de watch encaminhadas para WorkloadEntityKeeperStorage.

### KeeperLogsEntryReadFromCommitCache {#keeperlogsentryreadfromcommitcache}

Evento descontinuado que contava as entradas de log lidas do cache dos logs de commit do Keeper.

### KeeperLogsPrefetchedEntries {#keeperlogsprefetchedentries}

Evento descontinuado que contava as entradas de log do Keeper pré-carregadas do arquivo de changelog.

### NaiveBayesClassifierModelsAllocatedBytes {#naivebayesclassifiermodelsallocatedbytes}

Evento descontinuado que contava os bytes alocados para os modelos do classificador Naive Bayes.

### NaiveBayesClassifierModelsLoaded {#naivebayesclassifiermodelsloaded}

Evento descontinuado que contava os modelos do classificador Naive Bayes carregados.

### ReaderExecutorRequestedBytes {#readerexecutorrequestedbytes}

Evento descontinuado que contava os bytes úteis entregues pelas solicitações de leitura do `ReaderExecutor`.

## Colunas {#columns}

* `event` ([String](/pt-BR/reference/data-types/string)) — Nome do evento.
* `value` ([UInt64](/pt-BR/reference/data-types/int-uint)) — Número de ocorrências do evento.
* `description` ([String](/pt-BR/reference/data-types/string)) — Descrição do evento.

**Aliases:**

* `name` — Alias de `event`.

## Exemplo {#example}

```sql
SELECT * FROM system.events LIMIT 5
```

```text
┌─event─────────────────────────────────┬─value─┬─description────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ Query                                 │    12 │ Number of queries to be interpreted and potentially executed. Does not include queries that failed to parse or were rejected due to AST size limits, quota limits or limits on the number of simultaneously running queries. May include internal queries initiated by ClickHouse itself. Does not count subqueries.                  │
│ SelectQuery                           │     8 │ Same as Query, but only for SELECT queries.                                                                                                                                                                                                                │
│ FileOpen                              │    73 │ Number of files opened.                                                                                                                                                                                                                                    │
│ ReadBufferFromFileDescriptorRead      │   155 │ Number of reads (read/pread) from a file descriptor. Does not include sockets.                                                                                                                                                                             │
│ ReadBufferFromFileDescriptorReadBytes │  9931 │ Number of bytes read from file descriptors. If the file is compressed, this will show the compressed data size.                                                                                                                                              │
└───────────────────────────────────────┴───────┴────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```

## Veja também {#see-also}

* [system.asynchronous&#95;metrics](/pt-BR/reference/system-tables/asynchronous_metrics) — Contém métricas calculadas periodicamente.
* [system.metrics](/pt-BR/reference/system-tables/metrics) — Contém métricas calculadas instantaneamente.
* [system.metric&#95;log](/pt-BR/reference/system-tables/metric_log) — Contém um histórico dos valores das métricas das tabelas `system.metrics` e `system.events`.
* [Monitoramento](/pt-BR/guides/oss/deployment-and-scaling/monitoring/monitoring) — Conceitos básicos de monitoramento do ClickHouse.

{/*AUTOGENERATED_END*/}