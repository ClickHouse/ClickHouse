---
description: 'Tabela do sistema que contém métricas que podem ser calculadas instantaneamente ou
  que têm um valor atual.'
keywords: ['tabela do sistema', 'métricas']
slug: /operations/system-tables/metrics
title: 'system.metrics'
doc_type: 'referência'
---

import SystemTableCloud from '/snippets/pt-BR/_system_table_cloud.mdx';

<SystemTableCloud />

{/*AUTOGENERATED_START*/}

## Descrição

Contém métricas que podem ser calculadas instantaneamente ou que têm um valor atual. Por exemplo, o número de consultas processadas simultaneamente ou o atraso atual da réplica. Esta tabela está sempre atualizada.

## Descrições das métricas

### ActiveTimersInQueryProfiler

Número de temporizadores locais da thread ativos no QueryProfiler

### AddressesActive

Número total de endereços usados para criar conexões com pools de conexões

### AddressesBanned

Número total de endereços banidos por apresentarem falha na criação de conexões com pools de conexões

### AggregatorThreads

Número de threads no pool de threads do Aggregator.

### AggregatorThreadsActive

Número de threads no pool de threads do Aggregator executando uma tarefa.

### AggregatorThreadsScheduled

Número de jobs na fila ou ativos no pool de threads do Aggregator.

### ASTFuzzerAccumulatedFragments

Número de fragmentos de AST acumulados pelo fuzzer de AST no servidor para uso em mutações futuras.

### AsynchronousInsertQueueBytes

Número de bytes pendentes na fila de AsynchronousInsert.

### AsynchronousInsertQueueSize

Número de tarefas pendentes na fila AsynchronousInsert.

### AsynchronousInsertThreads

Número de threads no pool de threads do AsynchronousInsert.

### AsynchronousInsertThreadsActive

Número de threads do pool de threads AsynchronousInsert que estão executando uma tarefa.

### AsynchronousInsertThreadsScheduled

Número de jobs na fila ou ativos no pool de threads AsynchronousInsert.

### AsynchronousReadWait

Número de threads em espera por leitura assíncrona.

### AsyncInsertCacheSize

Número de IDs de hash de async insert em cache

### AttachedDatabase

Bancos de dados anexados.

### AttachedDictionary

Dicionários ativos.

### AttachedReplicatedTable

Tabelas replicadas ativas.

### TabelaAnexada

Tabelas ativas.

### AttachedView

Views ativas.

### AvroSchemaCacheBytes

Tamanho, em bytes, do cache de schema do Avro

### AvroSchemaCacheCells

Número de esquemas Avro armazenados em cache, incluindo tanto os esquemas registrados quanto os recuperados.

### Bytes do cache do registro de schemas do Avro

Tamanho do cache do registro de schemas do Avro em bytes

### AvroSchemaRegistryCacheCells

Número de entradas no cache do registro de esquemas Avro

### AzureRequests

Número de requisições do Azure atualmente em execução

### Tamanho do BackgroundBufferFlushSchedulePool

Limite para o número de tarefas no BackgroundBufferFlushSchedulePool

### BackgroundBufferFlushSchedulePoolTask

Número de tarefas ativas no BackgroundBufferFlushSchedulePool. Esse pool é usado para flushes periódicos do Buffer

### BackgroundCommonPoolSize

Limite para o número de tarefas em um pool em segundo plano associado

### BackgroundCommonPoolTask

Número de tarefas ativas no pool em segundo plano associado

### BackgroundDistributedSchedulePoolSize

Limite para a quantidade de tarefas no BackgroundDistributedSchedulePool

### BackgroundDistributedSchedulePoolTask

Número de tarefas ativas no BackgroundDistributedSchedulePool. Esse pool é usado para envios distribuídos realizados em segundo plano.

### BackgroundFetchesPoolSize

Limite para o número de fetches simultâneos em um pool em segundo plano associado

### BackgroundFetchesPoolTask

Número de fetches ativos no pool em segundo plano associado

### BackgroundMergesAndMutationsPoolSize

Limite para o número de merges e mutações ativas em um pool em segundo plano associado

### BackgroundMergesAndMutationsPoolTask

Número de merges e mutações ativas em um pool em segundo plano associado

### BackgroundMessageBrokerSchedulePoolSize

Limite para o número de tarefas no BackgroundMessageBrokerSchedulePool para streaming de mensagens

### BackgroundMessageBrokerSchedulePoolTask

Número de tarefas ativas no BackgroundMessageBrokerSchedulePool para o streaming de mensagens

### BackgroundMovePoolSize

Limite para o número de tarefas no BackgroundProcessingPool para movimentações

### BackgroundMovePoolTask

Número de tarefas ativas no BackgroundProcessingPool para operações de movimentação

### BackgroundQueryThreads

Número de threads no thread pool de consultas em segundo plano.

### BackgroundQueryThreadsActive

Número de threads no pool de threads de consultas em segundo plano que estão executando uma task.

### BackgroundQueryThreadsScheduled

Número de jobs enfileirados ou ativos no pool de threads de consultas em segundo plano.

### BackgroundSchedulePoolSize

Limite para o número de tarefas no BackgroundSchedulePool. Esse pool é usado para tarefas periódicas do ReplicatedMergeTree, como limpeza de partes de dados antigas, alteração de partes de dados, reinicialização de réplica etc.

### BackgroundSchedulePoolTask

Número de tarefas ativas no BackgroundSchedulePool. Esse pool é usado para tarefas periódicas do ReplicatedMergeTree, como limpeza de partes de dados antigas, alteração de partes de dados e reinicialização de réplicas etc.

### BackgroundStreamingSchedulePoolSize

Limite do número de tarefas no BackgroundStreamingSchedulePool para tarefas de consulta em streaming

### BackgroundStreamingSchedulePoolTask

Número de tarefas ativas no BackgroundStreamingSchedulePool para tarefas de consultas em streaming

### BackupsIOThreads

Número de threads no pool de threads BackupsIO.

### BackupsIOThreadsActive

Número de threads do pool de threads do BackupsIO que estão executando uma tarefa.

### BackupsIOThreadsScheduled

Número de jobs enfileirados ou ativos no pool de threads do BackupsIO.

### BackupsThreads

Número de threads no pool de threads do BACKUP.

### BackupsThreadsActive

Número de threads no pool de threads do BACKUP executando uma tarefa.

### BackupsThreadsScheduled

Número de tarefas na fila ou ativas do BACKUP.

### BcryptCacheBytes

Tamanho total do cache de autenticação do bcrypt em bytes

### BcryptCacheSize

Número total de entradas no cache de autenticação do bcrypt

### BlobCopierThreads

Número de threads no pool de threads do processo de replicação em segundo plano do disco de armazenamento de objetos

### BlobCopierThreadsActive

Número de threads no pool de threads do processo de replicação em segundo plano do disco de armazenamento de objetos que estão executando uma tarefa

### BlobCopierThreadsScheduled

Número de tarefas na fila ou ativas no pool de threads do processo de replicação em segundo plano do disco de armazenamento de objetos

### BlobKillerThreads

Número de threads no pool de threads do processo de remoção em segundo plano do disco de armazenamento de objetos

### BlobKillerThreadsActive

Número de threads no pool de threads do processo em segundo plano de remoção do disco de armazenamento de objetos que estão executando uma tarefa

### BlobKillerThreadsScheduled

Número de tarefas enfileiradas ou ativas no pool de threads do processo de remoção em segundo plano do disco de armazenamento de objetos

### BrokenDisks

Número de discos marcados como com defeito durante a verificação de disco.

### BrokenDistributedBytesToInsert

Número de bytes para inserção assíncrona em tabelas Distributed que foi marcado como com falha. O número de bytes de cada shard é somado.

### BrokenDistributedFilesToInsert

Número de arquivos para inserção assíncrona em tabelas Distributed marcados como corrompidos. O número de arquivos de cada shard é somado.

### BuildVectorSimilarityIndexThreads

Número de threads no pool de threads usado para criar o índice de similaridade vetorial.

### BuildVectorSimilarityIndexThreadsActive

Número de threads no pool de threads de criação do índice de similaridade vetorial que estão executando uma tarefa.

### BuildVectorSimilarityIndexThreadsScheduled

Número de jobs enfileirados ou ativos no pool de threads de criação do índice de similaridade vetorial.

### CacheDetachedFileSegments

Número de segmentos de arquivo do cache desanexados existentes

### CacheDictionaryThreads

Número de threads no pool de threads do CacheDictionary.

### CacheDictionaryThreadsActive

Número de threads do pool de threads do CacheDictionary executando uma tarefa.

### CacheDictionaryThreadsScheduled

Número de jobs enfileirados ou ativos no pool de threads do CacheDictionary.

### CacheDictionaryUpdateQueueBatches

Número de &#39;lotes&#39; (um conjunto de chaves) na fila de atualização do CacheDictionaries.

### Chaves da fila de atualização em CacheDictionaries

Número exato de chaves na fila de atualização em CacheDictionaries.

### CacheFileSegments

Número de segmentos de arquivo em cache existentes

### CacheWarmerBytesInProgress

Tamanho total dos segmentos de arquivo remotos que aguardam carregamento assíncrono no cache do sistema de arquivos.

### ColumnsDescriptionsCacheSize

Tamanho do cache de ColumnsDescriptions (cache por tabela)

### CompiledExpressionCacheBytes

Capacidade reservada de blocos de página (arredondada para páginas inteiras com um fator de superprovisionamento de 2x) mantida por `JITModuleMemoryManager` para seções executáveis/de dados de funções compiladas com JIT armazenadas em cache. NÃO corresponde aos bytes reais de código de máquina em uso (esse valor é menor). Alocada via `posix_memalign`, que é interceptado pelo jemalloc; portanto, isso é contabilizado na arena JIT dedicada e é um subconjunto de `jemalloc.jit_arena.active_bytes`.

### CompiledExpressionCacheCount

Total de entradas no cache de código de máquina compilado com JIT.

### Compressão

Número de operações de compressão usando codecs internos

### CompressionThread

Número de threads nos pools de threads de compressão.

### CompressionThreadActive

Número de threads nos pools de threads de compressão que estão executando uma tarefa.

### CompressionThreadScheduled

Número de jobs na fila ou ativos nos pools de threads de compressão.

### ConcurrencyControlAcquired

Número total de slots de CPU adquiridos

### ConcurrencyControlAcquiredNonCompeting

Número total de slots de CPU adquiridos que não são considerados concorrentes (a primeira thread, caso o escalonador `fair_round_robin` esteja em uso)

### ConcurrencyControlPreempted

Número total de threads preemptadas que aguardam um slot de CPU

### ConcurrencyControlScheduled

Número total de solicitações de slot de CPU sendo agendadas no momento

### ConcurrencyControlSoftLimit

Valor do limite flexível para o número de slots de CPU

### ConcurrentHashJoinPoolThreads

Número de threads no pool de threads do hash join concorrente.

### ConcurrentHashJoinPoolThreadsActive

Número de threads no pool de threads do concurrent hash join executando uma tarefa.

### ConcurrentHashJoinPoolThreadsScheduled

Número de jobs na fila ou ativos no pool de threads para hash join concorrente.

### ConcurrentQueryAcquired

Número total de slots de consulta adquiridos

### ConcurrentQueryScheduled

Número total de solicitações de slots de consulta que estão sendo agendadas no momento

### ContextLockWait

Número de threads aguardando um bloqueio no Context. Este é um bloqueio global.

### CoordinatedMergesCoordinatorAssignedMerges

Número de merges atribuídos no estado do coordenador de merge.

### CoordinatedMergesCoordinatorRunningMerges

Número de merges em execução visíveis ao coordenador de merges.

### CoordinatedMergesWorkerAssignedMerges

Número de merges atribuídos no estado do worker de merge.

### CreatedTimersInQueryProfiler

Número de temporizadores locais de thread criados no QueryProfiler

### DatabaseBackupThreads

Número de threads no pool de threads do DatabaseBackup.

### DatabaseBackupThreadsActive

Número de threads do pool de threads DatabaseBackup que estão executando uma tarefa.

### DatabaseBackupThreadsScheduled

Número de jobs na fila ou ativos no pool de threads DatabaseBackup.

### DatabaseCatalogThreads

Número de threads no pool de threads do DatabaseCatalog.

### DatabaseCatalogThreadsActive

Número de threads do pool de threads do DatabaseCatalog que estão executando uma tarefa.

### DatabaseCatalogThreadsScheduled

Número de tarefas enfileiradas ou ativas no pool de threads do DatabaseCatalog.

### DatabaseOnDiskThreads

Número de threads no pool de threads do DatabaseOnDisk.

### DatabaseOnDiskThreadsActive

Número de threads no pool de threads do DatabaseOnDisk executando uma tarefa.

### DatabaseOnDiskThreadsScheduled

Número de tarefas enfileiradas ou ativas no pool de threads do DatabaseOnDisk.

### DatabaseReplicatedCreateTablesThreads

Número de threads no threadpool para criação de tabelas no DatabaseReplicated.

### DatabaseReplicatedCreateTablesThreadsActive

Número de threads ativas no threadpool para criação de tabelas em DatabaseReplicated.

### DatabaseReplicatedCreateTablesThreadsScheduled

Número de jobs na fila ou ativos no threadpool para criação de tabelas no DatabaseReplicated.

### DDLWorkerThreads

Número de threads no pool de threads do DDLWorker para consultas ON CLUSTER.

### DDLWorkerThreadsActive

Número de threads no pool de threads DDLWORKER para consultas ON CLUSTER que estão executando uma tarefa.

### DDLWorkerThreadsScheduled

Número de jobs na fila ou ativos no pool de threads DDLWORKER para consultas ON CLUSTER.

### Descompressão

Número de operações de descompressão usando codecs de compressão internos

### DelayedInserts

Número de consultas INSERT que são limitadas devido ao grande número de partes de dados ativas em uma partição de uma tabela MergeTree.

### DeleteBitmapCacheBytes

Tamanho total, em bytes, do cache de bitmap de exclusão da UNIQUE KEY

### DeleteBitmapCacheEntries

Número total de bitmaps de exclusão de UNIQUE KEY em cache

### DeltaLakeSnapshotCacheSizeElements

Tamanho, em número de elementos, do cache de snapshots do DeltaLake.

### DestroyAggregatesThreads

Número de threads no pool de threads responsável por destruir estados agregados.

### DestroyAggregatesThreadsActive

Número de threads no pool de threads de destruição de estados agregados que estão executando uma tarefa.

### DestroyAggregatesThreadsScheduled

Número de tarefas enfileiradas ou ativas no pool de threads para destruir estados agregados.

### DictCacheRequests

Número de solicitações em andamento para as fontes de dados de dicionários do tipo cache.

### DiskConnectionsStored

Número total de sessões armazenadas no pool de sessões dos discos

### DiskConnectionsTotal

Contagem total de todas as sessões: armazenadas no pool e usadas ativamente no momento para discos

### DiskObjectStorageAsyncThreads

Métrica obsoleta; não exibe nada.

### DiskObjectStorageAsyncThreadsActive

Métrica obsoleta, não exibe nada.

### DiskObjectStorageCopyObjectThreads

Número de threads no pool de threads usado para paralelizar chamadas copyObjectToAnotherObjectStorage em transações do disco de armazenamento de objetos

### DiskObjectStorageCopyObjectThreadsActive

Número de threads no pool de threads de cópia de armazenamento de objetos em disco que estão executando uma tarefa

### DiskObjectStorageCopyObjectThreadsScheduled

Número de tarefas enfileiradas ou ativas no pool de threads de cópia de objetos do armazenamento de objetos em disco

### DiskPlainRewritableAzureDirectoryMapSize

Número de entradas de mapeamento de caminhos locais para remotos no map em memória &#39;plain&#95;rewritable&#39; do AzureObjectStorage.

### DiskPlainRewritableAzureFileCount

Número de entradas de arquivos no map em memória `plain_rewritable` do AzureObjectStorage.

### Tamanho do map de diretórios locais regraváveis simples em disco

Número de entradas de caminho de local para remoto no map em memória `plain_rewritable` do LocalObjectStorage.

### DiskPlainRewritableLocalFileCount

Número de entradas de arquivo no map em memória &#39;plain&#95;rewritable&#39; do LocalObjectStorage.

### DiskPlainRewritableS3DirectoryMapSize

Número de entradas de caminhos locais para remotos no map em memória `plain_rewritable` do S3ObjectStorage.

### DiskPlainRewritableS3FileCount

Número de entradas de arquivos no map em memória &#39;plain&#95;rewritable&#39; do S3ObjectStorage.

### DiskS3NoSuchKeyErrors

O número de erros `NoSuchKey` que ocorrem ao ler dados do armazenamento S3 em nuvem por meio de discos do ClickHouse.

### DiskSpaceReservedForMerge

Espaço em disco reservado para as operações de merge em segundo plano atualmente em execução. É ligeiramente maior que o tamanho total das partes que estão em processo de merge no momento.

### DistrCacheAllocatedConnections

Número de conexões atualmente alocadas no pool de conexões do Distributed Cache

### DistrCacheBorrowedConnections

Número de conexões atualmente em uso no pool de conexões do Distributed Cache

### DistrCacheOpenedConnections

Número de conexões abertas com o Distributed Cache

### DistrCacheReadBuffers

Número de buffers de leitura do Distributed Cache

### DistrCacheReadRequests

Número de solicitações de leitura executadas ao Distributed Cache

### DistrCacheRegisteredServers

Número de servidores registrados no Distributed Cache

### DistrCacheRegisteredServersCurrentAZ

Número de servidores registrados do Distributed Cache na AZ atual

### DistrCacheServerConnections

Número de conexões abertas para o servidor ClickHouse a partir do Distributed Cache

### DistrCacheServerRegistryConnections

Número de conexões ativas do Distributed Cache com o servidor ClickHouse (não marcadas como canceladas devido ao limite)

### DistrCacheServerS3CachedClients

Número de clientes S3 em cache do Distributed Cache

### DistrCacheSharedLimitCount

Número de conexões abertas conforme DistributedCache::ConnectionPool::SharedLimit

### DistrCacheUsedConnections

Número de conexões com o Distributed Cache atualmente em uso

### DistrCacheWriteBuffers

Número de buffers de gravação do Distributed Cache

### DistrCacheWriteRequests

Número de solicitações de Write executadas no Distributed Cache

### DistributedBytesToInsert

Número de bytes pendentes de processamento para inserção assíncrona em tabelas Distributed. O número de bytes de cada shard é somado.

### DistributedFilesToInsert

Número de arquivos pendentes de processamento para inserção assíncrona em tabelas Distributed. O número de arquivos de cada shard é somado.

### DistributedIndexAnalysisThreads

Número de threads no pool de threads para análise distribuída de índices.

### DistributedIndexAnalysisThreadsActive

Número de threads no pool de threads da análise de índice distribuída que estão executando uma tarefa.

### DistributedIndexAnalysisThreadsScheduled

Número de jobs enfileirados ou ativos no pool de threads da análise de índice distribuída.

### DistributedInsertThreads

Número de threads usadas no INSERT em Distributed.

### DistributedInsertThreadsActive

Número de threads usadas para INSERT no Distributed que estão executando uma tarefa.

### DistributedInsertThreadsScheduled

Número de jobs enfileirados ou ativos usados para INSERT em Distributed.

### DistributedSend

Número de conexões com servidores remotos que enviam dados inseridos em tabelas Distributed, tanto no modo síncrono quanto no assíncrono.

### DNSAddressesCacheBytes

Tamanho do cache de endereços DNS, em bytes

### DNSAddressesCacheSize

Número de endereços DNS armazenados em cache

### DNSHostsCacheBytes

Tamanho do cache de hosts DNS, em bytes

### DNSHostsCacheSize

Número de hosts DNS em cache

### DropDistributedCacheThreads

Número de threads no pool de threads da consulta drop distributed cache.

### DropDistributedCacheThreadsActive

Número de threads ativas no pool de threads da consulta drop distributed cache.

### DropDistributedCacheThreadsScheduled

Número de jobs enfileirados ou ativos no pool de threads de drop distributed cache.

### EncryptionHeaderCacheBytes

Tamanho total do cache de cabeçalhos de criptografia em bytes

### EncryptionHeaderCacheEntries

Número total de entradas no cache de cabeçalho de criptografia

### EphemeralNode

Número de nós efêmeros no ZooKeeper.

### ExchangeServerThreads

Número de threads no pool de threads responsável pelo handshake do servidor de exchange distribuído.

### ExchangeServerThreadsActive

Número de threads no pool de threads de handshake do servidor de exchange distribuído que estão executando uma tarefa.

### ExchangeServerThreadsScheduled

Número de jobs na fila ou ativos no pool de threads de handshake do servidor de exchange distribuído.

### FiberStackBytes

Soma dos tamanhos das pilhas de fibras atualmente alocadas.

### FiberStacks

Número de pilhas de fibras atualmente alocadas (as fibras são usadas para comunicação assíncrona com réplicas remotas).

### FilesystemCacheDelayedCleanupElements

Elementos do cache do sistema de arquivos na fila de limpeza em segundo plano

### Elementos da fila de download do cache do sistema de arquivos

Elementos do cache do sistema de arquivos na fila de download

### FilesystemCacheDropCacheThreads

Número de threads no pool do cache do sistema de arquivos para remoção paralela de chaves em SYSTEM DROP FILESYSTEM CACHE

### FilesystemCacheDropCacheThreadsActive

Número de threads no pool de remoção de keys do cache do sistema de arquivos executando uma task

### FilesystemCacheDropCacheThreadsScheduled

Número de jobs enfileirados ou ativos no pool de remoção de keys do cache do sistema de arquivos

### FilesystemCacheElements

Elementos do cache do sistema de arquivos (segmentos de arquivo)

### FilesystemCacheEvictionThreads

Número de threads no pool de remoção (eviction) em segundo plano do cache do sistema de arquivos

### FilesystemCacheEvictionThreadsActive

Número de threads no pool de remoção em segundo plano do cache do sistema de arquivos executando uma tarefa

### FilesystemCacheEvictionThreadsScheduled

Número de jobs enfileirados ou em execução no pool em segundo plano de remoção do cache do sistema de arquivos

### FilesystemCacheHoldFileSegments

Segmento de arquivo do cache do sistema de arquivos que atualmente está retido como não liberável

### FilesystemCacheInvalidatedElements

Número de entradas invalidadas (tamanho zero, com remoção pendente) na fila de prioridade do cache do sistema de arquivos

### FilesystemCacheKeys

Número de chaves no cache do sistema de arquivos

### FilesystemCacheOvercommitUsers

Número de usuários monitorados pela política de remoção do cache do sistema de arquivos com overcommit

### FilesystemCachePriorityQueueElements

Número total de entradas na fila de prioridade do cache do sistema de arquivos, incluindo as entradas invalidadas que aguardam remoção

### FilesystemCacheReadBuffers

Número de buffers ativos do cache

### FilesystemCacheReserveThreads

Número de threads tentando reservar espaço no cache

### FilesystemCacheSize

Tamanho do cache do sistema de arquivos, em bytes

### FilesystemCacheSizeLimit

Limite do tamanho do cache do sistema de arquivos em bytes

### FilteringMarksWithPrimaryKey

Número de threads que no momento estão filtrando intervalos de marcas pela chave primária

### FilteringMarksWithSecondaryKeys

Número de threads que estão atualmente filtrando intervalos de marcas por chaves secundárias

### FormatParsingThreads

Número de threads no pool de threads usado para fazer o parsing da entrada.

### FormatParsingThreadsActive

Número de threads no pool de threads usado para fazer o parsing da entrada que estão executando uma tarefa.

### FormatParsingThreadsScheduled

Número de jobs enfileirados ou ativos no pool de threads usado para fazer o parsing da entrada.

### FreezePartThreads

Número de threads no pool de threads usadas para congelar partes de dados.

### FreezePartThreadsActive

Número de threads ativas no pool de threads para congelar partes de dados.

### FreezePartThreadsScheduled

Número de jobs na fila ou ativos no pool de threads para o congelamento de partes de dados.

### GlobalThread

Número de threads no pool global de threads.

### GlobalThreadActive

Número de threads no pool global de threads que estão executando uma tarefa.

### GlobalThreadScheduled

Número de jobs na fila ou ativos no pool global de threads.

### HashedDictionaryThreads

Número de threads no pool de threads do HashedDictionary.

### HashedDictionaryThreadsActive

Número de threads no pool de threads do HashedDictionary executando uma tarefa.

### HashedDictionaryThreadsScheduled

Número de jobs enfileirados ou ativos no pool de threads do HashedDictionary.

### HiveFilesCacheBytes

Tamanho do cache do Hive em bytes

### HiveFilesCacheFiles

Número de arquivos armazenados em cache no cache do Hive

### HiveMetadataFilesCacheBytes

Tamanho do cache de metadados do Hive em bytes

### HiveMetadataFilesCacheFiles

Número de arquivos em cache no cache de metadados do Hive

### HTTPConnection

Número de conexões ao servidor HTTP

### HTTPConnectionsStored

Número total de sessões armazenadas no pool de sessões de hosts HTTP

### HTTPConnectionsTotal

Número total de todas as sessões: armazenadas no pool e usadas ativamente no momento para hosts HTTP

### IcebergCatalogThreads

Número de threads no pool de threads do IcebergCatalog.

### IcebergCatalogThreadsActive

Número de threads no pool de threads do IcebergCatalog executando uma tarefa.

### IcebergCatalogThreadsScheduled

Número de jobs enfileirados ou ativos no pool de threads do IcebergCatalog.

### IcebergCompactionCleanupTasks

Número de tarefas em segundo plano para remoção de arquivos de dados substituídos pela compactação Iceberg, que estão agendadas ou ainda retidas pelas tabelas Iceberg.

### IcebergCompactionMerge

Número de mesclagens de compactação Iceberg em segundo plano em execução.

### IcebergCompactionSnapshots

Número de compactações do Iceberg.

### IcebergCompactionSourceFiles

Número de arquivos de origem de entrada (arquivos de dados e position delete files) que participam das mesclagens de compactação Iceberg em execução no momento.

### IcebergCompactionThreads

Número de threads no pool de threads do IcebergCompaction.

### IcebergCompactionThreadsActive

Número de threads do pool de threads IcebergCompaction que estão executando uma tarefa.

### IcebergCompactionThreadsScheduled

Número de jobs enfileirados ou ativos no pool de threads do IcebergCompaction.

### IcebergManifestDecodeThreads

Número de threads no pool de threads usado para decodificar arquivos de manifesto de dados do Iceberg.

### IcebergManifestDecodeThreadsActive

Número de threads no pool de threads usado para decodificar arquivos de manifesto de dados do Iceberg que estão executando uma task.

### IcebergManifestDecodeThreadsScheduled

Número de jobs enfileirados ou ativos no pool de threads usado para decodificar arquivos de manifesto de dados do Iceberg.

### IcebergMetadataFilesCacheBytes

Tamanho do cache de metadados do Iceberg, em bytes

### IcebergMetadataFilesCacheFiles

Número de arquivos em cache no cache de metadados do Iceberg

### IcebergSchedulePoolSize

Limite do número de tarefas no pool de agendamento em segundo plano para tabelas Iceberg.

### IcebergSchedulePoolTask

Número de tarefas no pool de agendamento em segundo plano das tabelas Iceberg.

### IDiskCopierThreads

Número de threads para copiar dados entre discos de tipos distintos.

### IDiskCopierThreadsActive

Número de threads que executam uma tarefa de cópia de dados entre discos de tipos diferentes.

### IDiskCopierThreadsScheduled

Número de tarefas na fila ou ativas para copiar dados entre discos de tipos diferentes.

### IndexMarkCacheBytes

Tamanho total do mark cache de índices secundários, em bytes

### IndexMarkCacheFiles

Número total de arquivos de marcação em cache no mark cache para índices secundários

### IndexUncompressedCacheBytes

Tamanho total do cache não compactado em bytes para índices secundários. O cache não compactado normalmente não melhora o desempenho e deve ser evitado na maioria dos casos

### IndexUncompressedCacheCells

Número total de entradas no cache não compactado de índices secundários. Cada entrada representa um bloco de dados descompactado. O cache não compactado normalmente não melhora o desempenho e, em geral, deve ser evitado

### InterserverConnection

Número de conexões de outras réplicas usadas para buscar partes

### IOPrefetchThreads

Número de threads no pool de threads de pré-busca de E/S.

### IOPrefetchThreadsActive

Número de threads no pool de threads de prefetch de E/S que estão executando uma tarefa.

### IOPrefetchThreadsScheduled

Número de tarefas enfileiradas ou ativas no pool de threads de pré-busca de E/S.

### IOThreads

Número de threads no pool de threads de E/S.

### IOThreadsActive

Número de threads do pool de threads de E/S executando uma tarefa.

### IOThreadsScheduled

Número de tarefas enfileiradas ou ativas no pool de threads de E/S.

### IOUringInFlightEvents

Número de SQEs do `io_uring` em andamento

### IOUringPendingEvents

Número de SQEs do io&#95;uring aguardando envio

### IOWriterThreads

Número de threads no pool de threads de gravação de E/S.

### IOWriterThreadsActive

Número de threads no pool de threads de gravação de E/S que estão executando uma tarefa.

### IOWriterThreadsScheduled

Número de jobs enfileirados ou ativos no pool de threads de escrita de E/S.

### IsServerShuttingDown

Indica se o servidor está sendo desligado: 0 = não, 1 = sim

### KafkaAssignedPartitions

Número de partições atribuídas atualmente às tabelas Kafka

### KafkaBackgroundReads

Número de leituras em segundo plano atualmente em andamento (alimentando visões materializadas a partir do Kafka)

### KafkaConsumers

Número de consumidores ativos do Kafka

### KafkaConsumersInUse

Número de consumidores atualmente usados em leituras diretas ou em segundo plano

### KafkaConsumersWithAssignment

Número de consumidores do Kafka ativos aos quais foram atribuídas partições.

### KafkaLibrdkafkaThreads

Número de threads ativas do librdkafka

### KafkaProducers

Número de produtores ativos do Kafka criados

### KafkaWrites

Número de inserts atualmente em execução no Kafka

### KeeperAliveConnections

Número de conexões ativas

### KeeperBlockCacheBlocks

Número de blocos no cache de blocos do armazenamento do Keeper.

### KeeperBlockCacheBytes

Tamanho total dos blocos no cache de blocos do armazenamento do Keeper, em bytes.

### KeeperChangelogReadAheadThreads

Número de threads no pool de threads para as tarefas de preenchimento por leitura antecipada do changelog do Keeper.

### KeeperChangelogReadAheadThreadsActive

Número de threads ativas no pool de threads para tarefas de preenchimento por leitura antecipada do changelog do Keeper.

### KeeperChangelogReadAheadThreadsScheduled

Número de jobs enfileirados ou ativos no pool de threads das tarefas de preenchimento por leitura antecipada do changelog do Keeper.

### KeeperChangelogStartupReadThreads

Número de threads no pool de threads das leituras paralelas do changelog do Keeper durante a inicialização.

### KeeperChangelogStartupReadThreadsActive

Número de threads ativas no pool de threads das leituras paralelas do changelog do Keeper durante a inicialização.

### KeeperChangelogStartupReadThreadsScheduled

Número de jobs enfileirados ou ativos no pool de threads das leituras paralelas de inicialização do changelog do Keeper.

### KeeperContainerNodes

Número de nós de contêiner atualmente armazenados no Keeper.

### KeeperOutstandingRequests

Número de solicitações pendentes

### KeeperReadThreads

Número de threads no pool de threads para leituras do servidor Keeper.

### KeeperReadThreadsActive

Número de threads ativas no pool de threads de leituras do servidor Keeper.

### KeeperReadThreadsScheduled

Número de jobs enfileirados ou ativos no pool de threads para leituras do servidor Keeper. Métrica sem utilidade; as tarefas reais de leitura nesse pool de threads são agendadas por um mecanismo diferente.

### KeeperTTLNodes

Número de nós com TTL definido atualmente armazenados no Keeper.

### LicenseRemainingSeconds

Segundos restantes no período de validade da licença.

### LocalThread

Obsoleto. Número de threads nos pools de threads locais. As threads desses pools locais são obtidas do pool global de threads.

### LocalThreadActive

Obsoleto. Número de threads nos pools de threads locais executando uma tarefa.

### LocalThreadScheduled

Obsoleto. Número de jobs na fila ou ativos em pools de threads locais.

### MarkCacheBytes

Tamanho total do mark cache em bytes

### MarkCacheFiles

Número total de arquivos de marcação em cache no mark cache

### MarksLoaderThreads

Número de threads no thread pool para carregamento de marcas.

### MarksLoaderThreadsActive

Número de threads no pool de threads de carregamento de marcas que estão executando uma tarefa.

### MarksLoaderThreadsScheduled

Número de jobs enfileirados ou ativos no pool de threads de carregamento de marcas.

### MaxAllocatedEphemeralLockSequentialNumber

O número sequencial máximo alocado para os znodes de bloqueio efêmero no ZooKeeper. É determinado principalmente pelos números de bloco.

### MaxDDLEntryID

Maior entrada DDL processada pelo DDLWorker.

### MaxPushedDDLEntryID

Maior entrada DDL do DDLWorker enviada ao ZooKeeper.

### MemoryReservationApproved

Número total de bytes atualmente alocados (aprovados) em reservas de memória

### MemoryReservationDemand

Número total de bytes referentes a solicitações pendentes de aumento de reservas de memória

### MemoryTracking

Quantidade total de memória (em bytes) alocada pelo servidor.

### MemoryTrackingUncorrected

Quantidade total de memória (em bytes) alocada pelo servidor, não corrigida pelo RSS.

### Mesclagem

Número de mesclagens em segundo plano sendo executadas

### MergeJoinBlocksCacheBytes

Total de bytes usados por blocos armazenados em cache no MergeJoin

### MergeJoinBlocksCacheCount

Total de blocos armazenados em cache no MergeJoin

### MergeParts

Número de partes de origem que participam das mesclagens em segundo plano em andamento

### MergesMutationsMemoryTracking

Quantidade total de memória (bytes) alocada por tarefas em segundo plano (mesclagens e mutações).

### MergeTreeAllRangesAnnouncementsSent

O número atual de anúncios em andamento sendo enviados do servidor remoto para o servidor iniciador sobre o conjunto de partes de dados (para tabelas MergeTree). Medido no servidor remoto.

### MergeTreeBackgroundExecutorThreads

Número de threads no pool de threads do MergeTreeBackgroundExecutor.

### MergeTreeBackgroundExecutorThreadsActive

Número de threads no pool de threads MergeTreeBackgroundExecutor executando uma tarefa.

### MergeTreeBackgroundExecutorThreadsScheduled

Número de jobs enfileirados ou ativos no pool de threads do MergeTreeBackgroundExecutor.

### MergeTreeDataSelectExecutorThreads

Número de threads no pool de threads do MergeTreeDataSelectExecutor.

### MergeTreeDataSelectExecutorThreadsActive

Número de threads no pool de threads MergeTreeDataSelectExecutor que estão executando uma tarefa.

### MergeTreeDataSelectExecutorThreadsScheduled

Número de jobs na fila ou ativos no pool de threads do MergeTreeDataSelectExecutor.

### MergeTreeFetchPartitionThreads

Número de threads do ALTER TABLE FETCH PARTITION

### MergeTreeFetchPartitionThreadsActive

Número de threads para buscar partes com ALTER TABLE FETCH PARTITION

### MergeTreeFetchPartitionThreadsScheduled

Número de operações de fetch de partes enfileiradas ou ativas em ALTER TABLE FETCH PARTITION

### MergeTreeOutdatedPartsLoaderThreads

Número de threads no pool de threads para carregar partes de dados desatualizadas.

### MergeTreeOutdatedPartsLoaderThreadsActive

Número de threads ativas no threadpool para carregamento de partes de dados desatualizadas.

### MergeTreeOutdatedPartsLoaderThreadsScheduled

Número de jobs enfileirados ou ativos no threadpool de carregamento de partes de dados desatualizadas.

### MergeTreePartsCleanerThreads

Número de threads no pool de threads de limpeza das partes do MergeTree.

### MergeTreePartsCleanerThreadsActive

Número de threads no pool de threads de limpeza de partes do MergeTree que estão executando uma tarefa.

### MergeTreePartsCleanerThreadsScheduled

Número de jobs enfileirados ou ativos no pool de threads da limpeza de partes do MergeTree.

### MergeTreePartsLoaderThreads

Número de threads no pool de threads do carregador de partes do MergeTree.

### MergeTreePartsLoaderThreadsActive

Número de threads no pool de threads do carregador de partes do MergeTree que estão executando uma tarefa.

### MergeTreePartsLoaderThreadsScheduled

Número de jobs na fila ou ativos no pool de threads do carregador de partes do MergeTree.

### MergeTreeReadTaskRequestsSent

O número atual de solicitações de callback em andamento enviadas do servidor remoto de volta ao servidor iniciador para escolher a tarefa de leitura (para tabelas MergeTree). Medido no servidor remoto.

### MergeTreeSnapshotCommitThreads

Número de threads usadas para efetivar o commit do snapshot

### MergeTreeSnapshotCommitThreadsActive

Número de threads ativas usadas para efetuar o commit do snapshot

### MergeTreeSnapshotCommitThreadsScheduled

Número de threads programadas usadas para fazer commit do snapshot

### MergeTreeSubcolumnsReaderThreads

Número de threads no pool de threads usado para a leitura de subcolunas no MergeTree.

### MergeTreeSubcolumnsReaderThreadsActive

Número de threads no pool de threads usado para a leitura de subcolunas no MergeTree que estão executando uma tarefa.

### MergeTreeSubcolumnsReaderThreadsScheduled

Número de tarefas enfileiradas ou ativas no pool de threads usado para a leitura de subcolunas no MergeTree.

### MergeTreeUnexpectedPartsLoaderThreads

Número de threads no threadpool para carregar partes de dados inesperadas.

### MergeTreeUnexpectedPartsLoaderThreadsActive

Número de threads ativas no pool de threads para o carregamento de partes de dados inesperadas.

### MergeTreeUnexpectedPartsLoaderThreadsScheduled

Número de jobs enfileirados ou ativos no threadpool de carregamento de partes de dados inesperadas.

### MetadataFromKeeperCacheObjects

Número de objetos no cache de metadados em disco.

### MMapCacheCells

O número de arquivos abertos com `mmap` (mapeados na memória). Isso é usado para consultas com a configuração `local_filesystem_read_method` definida como `mmap`. Os arquivos abertos com `mmap` são mantidos no cache para evitar operações de flush da TLB, que são custosas.

### MMappedFileBytes

Soma do tamanho das regiões de arquivos mapeadas em memória.

### MMappedFiles

Número total de arquivos mapeados na memória.

### Movimentações

Número de movimentações em execução no momento

### MySQLConnection

Número de conexões de clientes via protocolo MySQL

### NamedCollection

Número de coleções nomeadas

### NetworkReceive

Número de threads que recebem dados da rede. Apenas as interações de rede relacionadas ao ClickHouse estão incluídas, não as de bibliotecas de terceiros.

### NetworkSend

Número de threads que enviam dados pela rede. Inclui apenas a interação de rede relacionada ao ClickHouse, não a de bibliotecas de terceiros.

### ObjectStorageAzureThreads

Número de threads no pool de threads do AzureObjectStorage.

### ObjectStorageAzureThreadsActive

Número de threads no pool de threads do AzureObjectStorage executando uma tarefa.

### ObjectStorageAzureThreadsScheduled

Número de jobs na fila ou ativos no pool de threads do AzureObjectStorage.

### ObjectStorageQueueMetadataCacheSizeBytes

Tamanho, em bytes, do cache de metadados do ObjectStorageQueue.

### ObjectStorageQueueMetadataCacheSizeElements

Tamanho, em número de elementos, do cache de metadados do ObjectStorageQueue.

### ObjectStorageQueueRegisteredServers

Número de servidores registrados no StorageS3(Azure)Queue

### ObjectStorageQueueShutdownThreads

Número de threads no pool de encerramento da fila de armazenamento de objetos.

### ObjectStorageQueueShutdownThreadsActive

Número de threads no pool de encerramento da fila de armazenamento de objetos que estão executando uma tarefa.

### ObjectStorageQueueShutdownThreadsScheduled

Número de jobs enfileirados ou ativos no pool de encerramento da fila de armazenamento de objetos.

### ObjectStorageS3Threads

Número de threads no pool de threads do S3ObjectStorage.

### ObjectStorageS3ThreadsActive

Número de threads no thread pool do S3ObjectStorage executando uma tarefa.

### ObjectStorageS3ThreadsScheduled

Número de jobs na fila ou ativos no pool de threads do S3ObjectStorage.

### OpenFileForRead

Número de arquivos abertos para leitura

### OpenFileForWrite

Número de arquivos abertos para gravação

### OutdatedPartsLoadingThreads

Número de threads no pool de threads para carregar partes de dados desatualizadas.

### OutdatedPartsLoadingThreadsActive

Número de threads ativas no threadpool para carregar partes de dados marcadas como desatualizadas.

### OutdatedPartsLoadingThreadsScheduled

Número de jobs enfileirados ou ativos no pool de threads para carregar partes de dados desatualizadas.

### PageCacheBytes

Tamanho total, em bytes, do cache de páginas em espaço de usuário

### PageCacheCells

Número total de entradas no cache de páginas do espaço do usuário

### PaimonMetadataFilesCacheBytes

Tamanho do cache de metadados do Paimon em bytes

### PaimonMetadataFilesCacheFiles

Número de arquivos em cache no cache de metadados do Paimon

### ParallelCompressedWriteBufferThreads

Número de threads em todas as instâncias de ParallelCompressedWriteBuffer — essas threads fazem compressão e gravação em paralelo

### ParallelCompressedWriteBufferWait

Número de threads em todas as instâncias de ParallelCompressedWriteBuffer que estão, no momento, aguardando que o buffer fique disponível para gravação

### ParallelFormattingOutputFormatThreads

Número de threads no pool de threads ParallelFormattingOutputFormatThreads.

### ParallelFormattingOutputFormatThreadsActive

Número de threads no thread pool ParallelFormattingOutputFormatThreads executando uma tarefa.

### ParallelFormattingOutputFormatThreadsScheduled

Número de jobs enfileirados ou ativos no pool de threads ParallelFormattingOutputFormatThreads.

### ParallelWithQueryActiveThreads

Número de threads ativas no threadpool de processamento de consultas PARALLEL WITH.

### ParallelWithQueryScheduledThreads

Número de jobs enfileirados ou ativos no threadpool que processa consultas PARALLEL WITH.

### ParallelWithQueryThreads

Número de threads no pool de threads usado para processar consultas PARALLEL WITH.

### ParquetEncoderThreads

Número de threads no thread pool do ParquetBlockOutputFormat.

### ParquetEncoderThreadsActive

Número de threads no pool de threads do ParquetBlockOutputFormat que estão executando uma tarefa.

### ParquetEncoderThreadsScheduled

Número de tarefas enfileiradas ou ativas no pool de threads do ParquetBlockOutputFormat.

### ParquetMetadataCacheBytes

Tamanho do cache de metadados Parquet em bytes

### ParquetMetadataCacheFiles

Número de arquivos armazenados em cache no cache de metadados do Parquet

### PartMutation

Quantidade de mutações (ALTER DELETE/UPDATE)

### Partes de dados ativas

Parte de dados ativa, usada pelas consultas SELECT atuais e futuras.

### PartsCommitted

Descontinuado. Consulte PartsActive.

### Partes compactas

Partes compactas.

### PartsDeleteOnDestroy

A parte foi movida para outro disco e deve ser excluída pelo próprio destrutor.

### PartsDeleting

Parte de dados inativa com contador de referências de identidade; ela está sendo excluída neste momento por um processo de limpeza.

### PartsOutdated

Parte de dados inativa, mas ainda pode ser usada apenas por SELECTs em execução e pode ser excluída após a conclusão desses SELECTs.

### PartsPreActive

A parte está em data&#95;parts, mas não é usada em SELECTs.

### PartsPreCommitted

Descontinuado. Consulte PartsPreActive.

### PartsTemporary

A parte está sendo gerada neste momento; ela não está na lista data&#95;parts.

### PartsWide

Partes no layout wide.

### PendingAsyncInsert

Número de inserções assíncronas que estão aguardando o flush.

### PointInPolygonCacheBytes

Tamanho em bytes do cache de polígonos constantes pré-processados para a função `pointInPolygon`

### PointInPolygonCacheCells

Número de polígonos constantes pré-processados em cache para a função `pointInPolygon`

### PointInPolygonCacheSizeLimit

Tamanho máximo, em bytes, do cache de polígonos constantes pré-processados para a função `pointInPolygon` (o valor efetivamente aplicado da configuração de servidor `point_in_polygon_cache_size`)

### PolygonDictionaryThreads

Número de threads no pool de threads para dicionários de polígonos.

### PolygonDictionaryThreadsActive

Número de threads ativas no pool de threads para dicionários de polígonos.

### PolygonDictionaryThreadsScheduled

Número de jobs na fila ou ativos no threadpool para dicionários de polígono.

### PostgreSQLConnection

Número de conexões de clientes usando o protocolo PostgreSQL

### PrimaryIndexCacheBytes

Tamanho total do cache do índice primário em bytes. Armazena índices de chave primária carregados sob demanda quando `primary_key_lazy_load=1` e `use_primary_key_cache=1`. Quando o pool dedicado de arenas de metadados do MergeTree está habilitado (`jemalloc_merge_tree_arenas` &gt; 0 com jemalloc), as colunas de índice são alocadas ali (`jemalloc.mergetree_arena.*`) mesmo quando pertencem a este cache. NUNCA se sobrepõe a `system.parts.primary_key_bytes_in_memory[_allocated]` — o índice de uma parte fica ou neste cache (contabilizado aqui) ou na própria parte (contabilizado ali); nunca nos dois. Para obter a memória total do índice primário em todas as partes, some ambos.

### PrimaryIndexCacheFiles

Número total de arquivos de índice em cache no cache do índice primário

### Consulta

Número de consultas em execução

### QueryCacheBytes

Tamanho total do cache de consultas em bytes

### QueryCacheEntries

Número total de entradas no cache de consultas

### QueryConditionCacheBytes

Tamanho total do cache de condições de consulta em bytes

### Entradas do cache de condições de consulta

Número total de entradas no cache de condições de consulta

### QueryNonInternal

Número de consultas não internas em execução (consultas iniciadas por usuários, excluindo as consultas internas do ClickHouse)

### QueryPipelineExecutorThreads

Número de threads no pool de threads do PipelineExecutor.

### QueryPipelineExecutorThreadsActive

Número de threads do pool de threads PipelineExecutor executando uma tarefa.

### QueryPipelineExecutorThreadsScheduled

Número de jobs enfileirados ou ativos no pool de threads do PipelineExecutor.

### QueryPreempted

Número de tarefas em execução que foram pausadas e estão aguardando devido à configuração &#39;priority&#39;.

### QueryRunnerThreads

Número de threads no pool de threads QueryRunner.

### QueryRunnerThreadsActive

Número de threads no pool de threads QueryRunner que estão executando uma task.

### QueryRunnerThreadsScheduled

Número de jobs enfileirados ou ativos no pool de threads QueryRunner.

### QueryThread

Número de threads de processamento de consultas

### Leitura

Número de chamadas de sistema de leitura (read, pread, io&#95;getevents etc.) em andamento

### ReaderExecutorActive

Número de instâncias do ReaderExecutor em execução.

### ReaderExecutorChainedBufferBytes

Bytes atualmente mantidos em buffers encadeados ativos do ReaderExecutor (OwnedChainedBuffer) — memória de leitura em uso no momento, não um total cumulativo.

### ReaderExecutorLongConnections

Número de long source connections atualmente mantidas abertas pelo ReaderExecutor para otimização de leitura sequential.

### ReadonlyDisks

Número de discos marcados como somente leitura durante a verificação do disco.

### ReadonlyReplica

Número de tabelas Replicated que estão atualmente em modo somente leitura devido à reinicialização após a perda da sessão do ZooKeeper ou à inicialização sem o ZooKeeper configurado.

### ReadTaskRequestsSent

O número atual de solicitações de callback em andamento do servidor remoto para o servidor iniciador para selecionar a tarefa de leitura (para a função de tabela s3Cluster e similares). Medido no servidor remoto.

### Visões Atualizáveis

Visões materializadas numéricas com atualização periódica (REFRESH)

### Visões em atualização

Número de visões materializadas que estão sendo atualizadas no momento

### RemoteRead

Número de leituras em andamento com leitor remoto

### ReplicaReady

Indica se a réplica está pronta para consultas: 0 = não, 1 = sim

### ReplicatedChecks

Número de partes de dados sendo verificadas quanto à consistência

### ReplicatedFetch

Número de partes de dados obtidas da réplica

### ReplicatedSend

Número de partes de dados enviadas para réplicas

### RestartReplicaThreads

Número de threads no pool de threads de RESTART REPLICA.

### RestartReplicaThreadsActive

Número de threads no pool de threads RESTART REPLICA que estão executando uma tarefa.

### RestartReplicaThreadsScheduled

Número de jobs na fila ou ativos no pool de threads `RESTART REPLICA`.

### RestoreThreads

Número de threads no pool de threads de RESTORE.

### RestoreThreadsActive

Número de threads no pool de threads do RESTORE executando uma tarefa.

### RestoreThreadsScheduled

Número de jobs na fila ou ativos para RESTORE.

### Revisão

Revisão do servidor. É um número incrementado a cada versão ou versão candidata, exceto nas versões de correção.

### RWLockActiveReaders

Número de threads que mantêm um bloqueio de leitura em uma RWLock de tabela.

### RWLockActiveWriters

Número de threads que mantêm o bloqueio de escrita em um RWLock de tabela.

### RWLockWaitingReaders

Número de threads aguardando para leitura em um RWLock de uma tabela.

### RWLockWaitingWriters

Número de threads aguardando permissão de escrita no RWLock de uma tabela.

### S3CachedCredentialsProviders

Número total de provedores de credenciais armazenados em cache

### S3Requests

Número de solicitações ao S3

### SchedulerIOReadScheduled

Número de leituras de E/S atualmente agendadas

### SchedulerIOWriteScheduled

Número de gravações de E/S atualmente agendadas

### SendExternalTables

Número de conexões que enviam dados de tabelas externas para servidores remotos. Tabelas externas são usadas para implementar os operadores GLOBAL IN e GLOBAL JOIN com subconsultas distribuídas.

### SendScalars

Número de conexões que estão enviando dados de valores escalares para servidores remotos.

### SerializationCacheBytesInMemory

Tamanho total, em bytes, do cache de serialização, incluindo apenas os valores

### SerializationCacheBytesInMemoryAllocated

Tamanho total do cache de serialização em bytes, incluindo chaves e a sobrecarga causada por slots vazios

### SerializationCacheCount

Número total de entradas no cache de serialização

### SharedCatalogDropDetachLocalTablesErrors

Número de erros ocorridos ao tentar remoção ou desanexar tabelas locais no Shared Catalog.

### SharedCatalogDropLocalThreads

Número de threads no threadpool para `remoção` de tabelas locais no Shared Catalog.

### SharedCatalogDropLocalThreadsActive

Número de threads ativas no threadpool para a remoção de tabelas locais no Shared Catalog.

### SharedCatalogDropLocalThreadsScheduled

Número de jobs enfileirados ou ativos no threadpool para remoção de tabelas locais no Shared Catalog.

### SharedCatalogDropZooKeeperThreads

Número de threads no threadpool para remoção de objeto no ZooKeeper no Shared Catalog.

### SharedCatalogDropZooKeeperThreadsActive

Número de threads ativas no threadpool para remoção de objeto no ZooKeeper no Shared Catalog.

### SharedCatalogDropZooKeeperThreadsScheduled

Número de jobs enfileirados ou ativos no threadpool para remoção de objeto no ZooKeeper do Shared Catalog.

### SharedCatalogNumberOfObjectsInState

Número de objetos no estado atual do Shared Catalog.

### SharedCatalogStateApplicationThreads

Número de threads no threadpool para a aplicação do estado no Shared Catalog.

### SharedCatalogStateApplicationThreadsActive

Número de threads ativas no threadpool para aplicação de estado no Shared Catalog.

### SharedCatalogStateApplicationThreadsScheduled

Número de jobs na fila ou ativos no threadpool para aplicação de estado no Shared Catalog.

### SharedDatabaseCatalogTablesInLocalDropDetachQueue

Número de tabelas na fila para remoção ou desanexar locais no Shared Catalog.

### SharedMergeTreeAssignedCurrentParts

Número de partes bloqueadas por merge ou mutação

### SharedMergeTreeBlobRefCounterNodes

Número de nós de contagem de referências de blobs (um para cada blob de dados compartilhado por 2 ou mais partes blob-list) nas subárvores `_blob_manager/refcount` do Keeper compartilhadas pelo cluster, medido periodicamente pela task de estatísticas do blob-manager e somado entre os disks de metadados do Keeper do servidor.

### SharedMergeTreeBrokenCondemnedPartsInKeeper

Número de registros de partes condenadas corrompidas armazenados no Keeper

### SharedMergeTreeCondemnedPartsInKeeper

Quantos registros de partes marcadas para remoção estão armazenados no Keeper

### SharedMergeTreeFetch

Número de fetches em andamento

### SharedMergeTreeMaxActiveReplicas

Número máximo de réplicas ativas registradas no Keeper

### SharedMergeTreeMaxInactiveReplicas

O número máximo de réplicas inativas registradas no Keeper

### SharedMergeTreeMaxPartitions

O número máximo de partições registradas no Keeper em todas as tabelas SharedMergeTree

### SharedMergeTreeMaxReplicas

O número máximo de réplicas registradas no Keeper em todas as tabelas. Observe que esse valor pode não corresponder à soma de SharedMergeTreeMaxActiveReplicas e SharedMergeTreeMaxInactiveReplicas

### SharedMergeTreeMinActiveReplicas

Número mínimo de réplicas ativas registradas no Keeper

### SharedMergeTreeMinInactiveReplicas

O número mínimo de réplicas inativas registradas no Keeper

### SharedMergeTreeMinPartitions

O número mínimo de partições registradas no Keeper entre todas as tabelas SharedMergeTree

### SharedMergeTreeMinReplicas

O número mínimo de réplicas registradas no Keeper considerando todas as tabelas. Observe que isso pode não corresponder à soma de SharedMergeTreeMinActiveReplicas e SharedMergeTreeMinInactiveReplicas

### SharedMergeTreeOutdatedPartsInKeeper

Quantos registros de partes desatualizadas estão armazenados no Keeper

### SharedMergeTreeThreads

Número de threads nos pools de threads dos componentes internos do SharedMergeTree

### SharedMergeTreeThreadsActive

Número de threads nos pools de threads internos do SharedMergeTree que estão executando uma tarefa

### SharedMergeTreeThreadsScheduled

Número de threads na fila ou ativas nos pools de threads internos do SharedMergeTree

### SharedPartColumnsSubstreamsCacheSize

Número de listas distintas de subfluxos de colunas compartilhadas entre as partes de dados (soma dos caches por tabela)

### SharedPartColumnSubstreamsEntriesCacheSize

Número de entradas distintas de subfluxos por coluna compartilhadas entre partes de dados (soma dos caches por tabela)

### SharedPartSerializationGroupsCacheSize

Número de grupos distintos de serialização por coluna compartilhados entre partes de dados (soma dos caches por tabela)

### SharedPartSerializationsCacheSize

Número de maps de serializações distintas compartilhados entre partes de dados (soma dos caches por tabela)

### StartupScriptsExecutionState

Estado da execução dos scripts de inicialização: 0 = não concluída, 1 = sucesso, 2 = falha.

### StartupSystemTablesThreads

Número de threads no pool de threads StartupSystemTables.

### StartupSystemTablesThreadsActive

Número de threads no pool de threads StartupSystemTables que estão executando uma tarefa.

### StartupSystemTablesThreadsScheduled

Número de jobs na fila ou ativos no pool de threads StartupSystemTables.

### StatelessWorkerDiscoveryDyingWorkers

Número de workers sem estado que foram descartados e aguardam remoção, conforme rastreado pelo serviço de discovery de workers sem estado.

### StatelessWorkerDiscoveryFreeWorkers

Número de workers sem estado registrados e disponíveis para lease, conforme monitorado pelo serviço de discovery de workers sem estado.

### StatelessWorkerDiscoveryLeasedWorkers

Número de workers sem estado atualmente cedidos em lease aos tenants, conforme rastreado pelo serviço de discovery de workers sem estado.

### StatelessWorkerDiscoveryTenants

Número de tenants registrados no serviço de discovery de workers sem estado.

### StatelessWorkerIdle

Número de workers sem estado atualmente mantidos pelo servidor, mas não utilizados por nenhuma consulta em execução.

### StatelessWorkerInUse

Número de workers sem estado atualmente em uso por consultas em execução.

### StatelessWorkerServerActiveTasks

Número de tasks de execução de consultas atualmente ativas em um worker sem estado (aceitas de um tenant e ainda não liberadas).

### StatelessWorkerThreads

Número de threads no pool de threads de workers sem estado.

### StatelessWorkerThreadsActive

Número de threads no pool de threads worker sem estado que executam uma tarefa.

### StatelessWorkerThreadsScheduled

Número de jobs na fila ou ativos no pool de threads de worker sem estado.

### StorageBufferBytes

Número de bytes nos buffers das tabelas Buffer

### StorageBufferFlushThreads

Número de threads para operações de flush em segundo plano no StorageBuffer

### StorageBufferFlushThreadsActive

Número de threads de flush em segundo plano no StorageBuffer que estão executando uma tarefa

### StorageBufferFlushThreadsScheduled

Número de threads enfileiradas ou ativas para operações de flush em segundo plano no StorageBuffer

### StorageBufferRows

Número de linhas nos buffers das tabelas Buffer

### StorageConnectionsStored

Contagem total de sessões armazenadas no pool de sessões para os armazenamentos

### StorageConnectionsTotal

Número total de todas as sessões: armazenadas no pool e em uso ativo no momento para os armazenamentos

### StorageDistributedThreads

Número de threads no pool de threads do StorageDistributed.

### StorageDistributedThreadsActive

Número de threads do pool de threads StorageDistributed que estão executando uma tarefa.

### StorageDistributedThreadsScheduled

Número de jobs na fila ou ativos no pool de threads do StorageDistributed.

### StorageHiveThreads

Número de threads no pool de threads do StorageHive.

### StorageHiveThreadsActive

Número de threads no pool de threads do StorageHive executando uma tarefa.

### StorageHiveThreadsScheduled

Número de jobs enfileirados ou ativos no pool de threads do StorageHive.

### StorageObjectStorageThreads

Número de threads nos thread pools dos motores de tabela remotos.

### StorageObjectStorageThreadsActive

Número de threads no pool de threads dos motores de tabela remotos que estão executando uma tarefa.

### StorageObjectStorageThreadsScheduled

Número de jobs na fila ou ativos no pool de threads dos motores de tabela remotos.

### StorageS3Threads

Número de threads no pool de threads do StorageS3.

### StorageS3ThreadsActive

Número de threads no pool de threads do StorageS3 executando uma tarefa.

### StorageS3ThreadsScheduled

Número de jobs na fila ou ativos no pool de threads do StorageS3.

### StreamingExchangeSinksWithFullSendQueue

Número de sinks de exchange de streaming que não recebem fragmentos porque sua fila de pacotes à espera do socket está cheia.

### SystemDatabaseReplicasThreads

Número de threads no pool de threads system.database&#95;replicas.

### SystemDatabaseReplicasThreadsActive

Número de threads do pool de threads system.database&#95;replicas que estão executando uma tarefa.

### SystemDatabaseReplicasThreadsScheduled

Número de jobs na fila ou ativos no pool de threads system.database&#95;replicas.

### SystemReplicasThreads

Número de threads no pool de threads de `system.replicas`.

### SystemReplicasThreadsActive

Número de threads do pool de threads `system.replicas` executando uma tarefa.

### SystemReplicasThreadsScheduled

Número de tarefas enfileiradas ou ativas no pool de threads `system.replicas`.

### TablesLoaderBackgroundThreads

Número de threads no pool de threads em segundo plano do carregador de tabelas.

### TablesLoaderBackgroundThreadsActive

Número de threads do pool em segundo plano do carregador de tabelas que estão executando uma tarefa.

### TablesLoaderBackgroundThreadsScheduled

Número de jobs enfileirados ou ativos no pool de threads em segundo plano do carregador de tabelas.

### TablesLoaderForegroundThreads

Número de threads no pool de threads em primeiro plano do carregador de tabelas.

### TablesLoaderForegroundThreadsActive

Número de threads do pool de threads de primeiro plano do carregador de tabelas que estão executando uma tarefa.

### TablesLoaderForegroundThreadsScheduled

Número de jobs enfileirados ou ativos no pool de threads de primeiro plano do carregador de tabelas.

### TablesToDropQueueSize

Número de tabelas removidas que aguardam a remoção de dados em segundo plano.

### TaskTrackerThreads

Número de threads usadas no rastreador remoto de tarefas da consulta distribuída.

### TaskTrackerThreadsActive

Número de threads do pool de threads do rastreador remoto de tarefas de consultas distribuídas que estão executando uma tarefa.

### TaskTrackerThreadsScheduled

Número de jobs na fila ou em execução no pool de threads do rastreador de tarefas remotas de consultas distribuídas.

### TCPConnection

Número de conexões ao servidor TCP (clientes com interface nativa), incluindo também conexões de consulta distribuída entre servidores

### TemporaryFilesForAggregation

Número de arquivos temporários criados para agregação externa

### TemporaryFilesForDistinct

Número de arquivos temporários criados para DISTINCT externo

### TemporaryFilesForJoin

Número de arquivos temporários criados para o JOIN

### TemporaryFilesForMerge

Número de arquivos temporários para a mesclagem vertical

### TemporaryFilesForSort

Número de arquivos temporários criados durante a ordenação externa

### TemporaryFilesUnknown

Número de arquivos temporários criados sem propósito conhecido

### TextIndexHeaderCacheBytes

Tamanho, em bytes, do cache do cabeçalho do índice de texto

### TextIndexHeaderCacheCells

Número de entradas no cache do cabeçalho do índice de texto

### TextIndexPostingsCacheBytes

Tamanho, em bytes, do cache das listas de postings do índice de texto

### TextIndexPostingsCacheCells

Número de entradas no cache das listas de postings do índice de texto

### TextIndexTokensCacheBytes

Tamanho, em bytes, do cache de tokens do índice de texto

### TextIndexTokensCacheCells

Número de entradas no cache de tokens do índice de texto

### ThreadPoolFSReaderThreads

Número de threads no pool de threads usado por local&#95;filesystem&#95;read&#95;method=threadpool.

### ThreadPoolFSReaderThreadsActive

Número de threads no pool de threads de `local_filesystem_read_method=threadpool` que estão executando uma tarefa.

### ThreadPoolFSReaderThreadsScheduled

Número de tarefas enfileiradas ou ativas no pool de threads para local&#95;filesystem&#95;read&#95;method=threadpool.

### ThreadPoolRemoteFSReaderThreads

Número de threads no pool de threads usado por remote&#95;filesystem&#95;read&#95;method=threadpool.

### ThreadPoolRemoteFSReaderThreadsActive

Número de threads no pool de threads de `remote_filesystem_read_method=threadpool` que estão executando uma tarefa.

### ThreadPoolRemoteFSReaderThreadsScheduled

Número de tarefas enfileiradas ou ativas no pool de threads para remote&#95;filesystem&#95;read&#95;method=threadpool.

### ThreadsInOvercommitTracker

Número de threads em espera no OvercommitTracker

### Total de arquivos temporários

Número de arquivos temporários criados

### UDFDriverInvokerThreads

Número de threads nos pools de threads que atendem aos pipes de comandos de driver de UDF executável.

### UDFDriverInvokerThreadsActive

Número de threads ativas nos pools de threads que atendem aos pipes de comando de driver de UDF executável.

### UDFDriverInvokerThreadsScheduled

Número de jobs enfileirados ou ativos em pools de threads que atendem aos pipes de comandos de driver de UDF executáveis.

### UncompressedCacheBytes

Tamanho total do cache não compactado em bytes. Em geral, o cache não compactado não melhora o desempenho e deve ser evitado na maioria dos casos.

### UncompressedCacheCells

Número total de entradas no cache não compactado. Cada entrada representa um bloco de dados descompactado. Em geral, o cache não compactado não melhora o desempenho e deve ser evitado sempre que possível.

### UniqueKeyIndexCacheBytes

Tamanho total do cache do índice UNIQUE KEY em bytes

### UniqueKeyIndexCacheEntries

Número total de blocos do índice UNIQUE KEY armazenados em cache

### VectorSimilarityIndexCacheBytes

Tamanho, em bytes, do cache do índice de similaridade vetorial

### VectorSimilarityIndexCacheCells

Número de entradas no cache do índice de similaridade vetorial

### VersionInteger

Versão do servidor em um único número inteiro na base 1000. Por exemplo, a versão 11.22.33 é representada como 11022033.

### Escrita

Número de chamadas de sistema de escrita (`write`, `pwrite`, `io_getevents` etc.) em andamento

### ZooKeeperConnectionLossStartedTimestampSeconds

Timestamp Unix em segundos de quando a conexão com o ZooKeeper foi perdida, ou 0 se a conexão estiver estabelecida com sucesso.

### ZooKeeperRequest

Número de solicitações ao ZooKeeper em andamento.

### ZooKeeperSession

Número de sessões (conexões) com o ZooKeeper. Deve haver no máximo uma, porque usar mais de uma conexão com o ZooKeeper pode causar bugs devido à falta de linearizabilidade (leituras desatualizadas) permitida pelo modelo de consistência do ZooKeeper.

### ZooKeeperSessionExpired

Número de sessões globais do ZooKeeper expiradas.

### ZooKeeperWatch

Número de watches (assinaturas de eventos) no ZooKeeper.

## Colunas

* `metric` ([String](/pt-BR/reference/data-types/string)) — Nome da métrica.
* `value` ([Int64](/pt-BR/reference/data-types/int-uint)) — Valor da métrica.
* `description` ([String](/pt-BR/reference/data-types/string)) — Descrição da métrica.

**Aliases:**

* `name` — Alias de `metric`.

Você pode encontrar todas as métricas suportadas no arquivo-fonte [src/Common/CurrentMetrics.cpp](https://github.com/ClickHouse/ClickHouse/blob/master/src/Common/CurrentMetrics.cpp).

## Exemplo

```sql
SELECT * FROM system.metrics LIMIT 10
```

```text
┌─metric───────────────────────────────┬─value─┬─description────────────────────────────────────────────────────────────┐
│ Query                                │     1 │ Number of executing queries                                            │
│ Merge                                │     0 │ Number of executing background merges                                  │
│ PartMutation                         │     0 │ Number of mutations (ALTER DELETE/UPDATE)                              │
│ ReplicatedFetch                      │     0 │ Number of data parts being fetched from replicas                       │
│ ReplicatedSend                       │     0 │ Number of data parts being sent to replicas                            │
│ ReplicatedChecks                     │     0 │ Number of data parts checking for consistency                          │
│ BackgroundMergesAndMutationsPoolTask │     0 │ Number of active merges and mutations in an associated background pool │
│ BackgroundFetchesPoolTask            │     0 │ Number of active fetches in an associated background pool              │
│ BackgroundCommonPoolTask             │     0 │ Number of active tasks in an associated background pool                │
│ BackgroundMovePoolTask               │     0 │ Number of active tasks in BackgroundProcessingPool for moves           │
└──────────────────────────────────────┴───────┴────────────────────────────────────────────────────────────────────────┘
```

{/*AUTOGENERATED_END*/}