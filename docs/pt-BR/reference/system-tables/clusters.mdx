---
description: 'Tabela do sistema contendo informações sobre os clusters disponíveis no
  arquivo de configuração e os servidores definidos neles.'
keywords: ['tabela do sistema', 'clusters']
slug: /operations/system-tables/clusters
title: 'system.clusters'
doc_type: 'reference'
---

<div id="description">
  ## Descrição
</div>

Contém informações sobre os clusters disponíveis no arquivo de configuração e os servidores que fazem parte deles.

<div id="Columns">
  ## Colunas
</div>

{/*AUTOGENERATED_START*/}

* `cluster` ([String](/pt-BR/reference/data-types/string)) — O nome do cluster.
* `shard_num` ([UInt32](/pt-BR/reference/data-types/int-uint)) — O número do shard no cluster, começando em 1.
* `shard_name` ([String](/pt-BR/reference/data-types/string)) — O nome do shard no cluster.
* `shard_weight` ([UInt32](/pt-BR/reference/data-types/int-uint)) — O peso relativo do shard durante a gravação de dados.
* `internal_replication` ([UInt8](/pt-BR/reference/data-types/int-uint)) — Flag que indica se este host faz parte de um ensemble capaz de replicar os dados por conta própria.
* `replica_num` ([UInt32](/pt-BR/reference/data-types/int-uint)) — O número da réplica no shard, começando em 1.
* `host_name` ([String](/pt-BR/reference/data-types/string)) — O nome do host, conforme especificado na configuração.
* `host_address` ([String](/pt-BR/reference/data-types/string)) — O endereço IP do host obtido via DNS.
* `port` ([UInt16](/pt-BR/reference/data-types/int-uint)) — A porta usada para conexão com o servidor.
* `is_local` ([UInt8](/pt-BR/reference/data-types/int-uint)) — Flag que indica se o host é local.
* `user` ([String](/pt-BR/reference/data-types/string)) — O nome do usuário usado para conexão com o servidor.
* `default_database` ([String](/pt-BR/reference/data-types/string)) — O nome do banco de dados padrão.
* `errors_count` ([UInt32](/pt-BR/reference/data-types/int-uint)) — O número de vezes que este host não conseguiu alcançar a réplica.
* `slowdowns_count` ([UInt32](/pt-BR/reference/data-types/int-uint)) — O número de lentidões que levaram à troca de réplica ao estabelecer uma conexão com solicitações hedged.
* `estimated_recovery_time` ([UInt32](/pt-BR/reference/data-types/int-uint)) — Segundos restantes até que a contagem de erros da réplica seja zerada e ela volte a ser considerada normal.
* `database_shard_name` ([String](/pt-BR/reference/data-types/string)) — O nome do shard do banco de dados `Replicated` (para clusters que pertencem a um banco de dados `Replicated`).
* `database_replica_name` ([String](/pt-BR/reference/data-types/string)) — O nome da réplica do banco de dados `Replicated` (para clusters que pertencem a um banco de dados `Replicated`).
* `is_shared_catalog_cluster` ([UInt8](/pt-BR/reference/data-types/int-uint)) — Bool que indica se o cluster pertence ao catálogo compartilhado.
* `is_active` ([Nullable(UInt8)](/pt-BR/reference/data-types/nullable)) — O status da réplica do banco de dados `Replicated` (para clusters que pertencem a um banco de dados `Replicated`): 1 significa &#39;a réplica está online&#39;, 0 significa &#39;a réplica está offline&#39;, NULL significa &#39;desconhecido&#39;.
* `unsynced_after_recovery` ([Nullable(UInt8)](/pt-BR/reference/data-types/nullable)) — Indica se uma réplica do banco de dados `Replicated` apresenta defasagem de replicação superior a max&#95;replication&#95;lag&#95;to&#95;enqueue após ser criada ou recuperada.
* `replication_lag` ([Nullable(UInt32)](/pt-BR/reference/data-types/nullable)) — A defasagem de replicação da réplica do banco de dados `Replicated` (para clusters que pertencem a um banco de dados `Replicated`).
* `recovery_time` ([Nullable(UInt64)](/pt-BR/reference/data-types/nullable)) — O tempo de recuperação da réplica do banco de dados `Replicated` (para clusters que pertencem a um banco de dados `Replicated`), em milissegundos.

**Aliases:**

* `name` — Alias de `cluster`.

{/*AUTOGENERATED_END*/}

<div id="example">
  ## Exemplo
</div>

```sql title="Query"
SELECT * FROM system.clusters LIMIT 2 FORMAT Vertical;
```

```text title="Response"
Row 1:
──────
cluster:                 test_cluster_two_shards
shard_num:               1
shard_name:              shard_01
shard_weight:            1
replica_num:             1
host_name:               127.0.0.1
host_address:            127.0.0.1
port:                    9000
is_local:                1
user:                    default
default_database:
errors_count:            0
slowdowns_count:         0
estimated_recovery_time: 0
database_shard_name:
database_replica_name:
is_active:               NULL

Row 2:
──────
cluster:                 test_cluster_two_shards
shard_num:               2
shard_name:              shard_02
shard_weight:            1
replica_num:             1
host_name:               127.0.0.2
host_address:            127.0.0.2
port:                    9000
is_local:                0
user:                    default
default_database:
errors_count:            0
slowdowns_count:         0
estimated_recovery_time: 0
database_shard_name:
database_replica_name:
is_active:               NULL
```

<div id="see-also">
  ## Veja também
</div>

* [motor de tabela Distributed](/pt-BR/reference/engines/table-engines/special/distributed)
* [configuração distributed&#95;replica&#95;error&#95;cap](/pt-BR/reference/settings/session-settings/distributed-replica#distributed_replica_error_cap)
* [configuração distributed&#95;replica&#95;error&#95;half&#95;life](/pt-BR/reference/settings/session-settings/distributed-replica#distributed_replica_error_half_life)