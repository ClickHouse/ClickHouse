---
description: 'Tabela do sistema com informações sobre colunas em partes de projeção de tabelas da família MergeTree'
keywords: ['tabela do sistema', 'projection_parts_columns']
slug: /operations/system-tables/projection_parts_columns
title: 'system.projection_parts_columns'
doc_type: 'referência'
---

<div id="description">
  ## Descrição
</div>

Esta tabela contém informações sobre colunas nas partes de projeção de tabelas da família MergeTree.

<div id="columns">
  ## Colunas
</div>

{/*AUTOGENERATED_START*/}

* `partition` ([String](/pt-BR/reference/data-types/string)) — O nome da partição.
* `name` ([String](/pt-BR/reference/data-types/string)) — Nome da parte de dados.
* `part_type` ([String](/pt-BR/reference/data-types/string)) — O formato de armazenamento da parte de dados.
* `parent_name` ([String](/pt-BR/reference/data-types/string)) — O nome da parte de dados de origem (pai).
* `parent_uuid` ([UUID](/pt-BR/reference/data-types/uuid)) — O UUID da parte de dados de origem (pai).
* `parent_part_type` ([String](/pt-BR/reference/data-types/string)) — O formato de armazenamento da parte de dados de origem (pai).
* `active` ([UInt8](/pt-BR/reference/data-types/int-uint)) — Flag que indica se a parte de dados está ativa.
* `marks` ([UInt64](/pt-BR/reference/data-types/int-uint)) — O número de marcas.
* `rows` ([UInt64](/pt-BR/reference/data-types/int-uint)) — O número de linhas.
* `bytes_on_disk` ([UInt64](/pt-BR/reference/data-types/int-uint)) — Tamanho total de todos os arquivos da parte de dados, em bytes.
* `data_compressed_bytes` ([UInt64](/pt-BR/reference/data-types/int-uint)) — Tamanho total dos dados comprimidos na parte de dados. Os arquivos auxiliares, como os arquivos com marcas, não estão incluídos.
* `data_uncompressed_bytes` ([UInt64](/pt-BR/reference/data-types/int-uint)) — Tamanho total dos dados não comprimidos na parte de dados. Os arquivos auxiliares, como os arquivos com marcas, não estão incluídos.
* `marks_bytes` ([UInt64](/pt-BR/reference/data-types/int-uint)) — O tamanho do arquivo com marcas.
* `parent_marks` ([UInt64](/pt-BR/reference/data-types/int-uint)) — O número de marcas na parte de origem (pai).
* `parent_rows` ([UInt64](/pt-BR/reference/data-types/int-uint)) — O número de linhas na parte de origem (pai).
* `parent_bytes_on_disk` ([UInt64](/pt-BR/reference/data-types/int-uint)) — Tamanho total de todos os arquivos da parte de dados de origem (pai), em bytes.
* `parent_data_compressed_bytes` ([UInt64](/pt-BR/reference/data-types/int-uint)) — Tamanho total dos dados comprimidos na parte de dados de origem (pai).
* `parent_data_uncompressed_bytes` ([UInt64](/pt-BR/reference/data-types/int-uint)) — Tamanho total dos dados não comprimidos na parte de dados de origem (pai).
* `parent_marks_bytes` ([UInt64](/pt-BR/reference/data-types/int-uint)) — O tamanho do arquivo com marcas na parte de dados de origem (pai).
* `modification_time` ([DateTime](/pt-BR/reference/data-types/datetime)) — O momento em que o diretório da parte de dados foi modificado. Geralmente, corresponde ao momento de criação da parte de dados.
* `remove_time` ([DateTime](/pt-BR/reference/data-types/datetime)) — O momento em que a parte de dados se tornou inativa.
* `refcount` ([UInt32](/pt-BR/reference/data-types/int-uint)) — O número de locais em que a parte de dados é usada. Um valor maior que 2 indica que ela é usada em consultas ou mesclagens.
* `min_date` ([Date](/pt-BR/reference/data-types/date)) — O valor mínimo da coluna Date, se ela estiver incluída na chave de partição.
* `max_date` ([Date](/pt-BR/reference/data-types/date)) — O valor máximo da coluna Date, se ela estiver incluída na chave de partição.
* `min_time` ([DateTime](/pt-BR/reference/data-types/datetime)) — O valor mínimo da coluna DateTime, se ela estiver incluída na chave de partição.
* `max_time` ([DateTime](/pt-BR/reference/data-types/datetime)) — O valor máximo da coluna DateTime, caso ela esteja incluída na chave de partição.
* `partition_id` ([String](/pt-BR/reference/data-types/string)) — ID da partição.
* `min_block_number` ([Int64](/pt-BR/reference/data-types/int-uint)) — O número mínimo de partes de dados que compõem a parte atual após a mesclagem.
* `max_block_number` ([Int64](/pt-BR/reference/data-types/int-uint)) — O número máximo de partes de dados que compõem a parte atual após a mesclagem.
* `level` ([UInt32](/pt-BR/reference/data-types/int-uint)) — Profundidade da árvore de mesclagem. Zero significa que a parte atual foi criada por inserção, e não pela mesclagem de outras partes.
* `data_version` ([UInt64](/pt-BR/reference/data-types/int-uint)) — Número usado para determinar quais mutações devem ser aplicadas à parte de dados (mutações com versão superior a data&#95;version).
* `primary_key_bytes_in_memory` ([UInt64](/pt-BR/reference/data-types/int-uint)) — A quantidade de memória (em bytes) usada pelos valores da chave primária.
* `primary_key_bytes_in_memory_allocated` ([UInt64](/pt-BR/reference/data-types/int-uint)) — A quantidade de memória (em bytes) reservada para os valores da chave primária.
* `database` ([String](/pt-BR/reference/data-types/string)) — Nome do banco de dados.
* `table` ([String](/pt-BR/reference/data-types/string)) — Nome da tabela.
* `engine` ([String](/pt-BR/reference/data-types/string)) — Nome do mecanismo de tabela sem parâmetros.
* `disk_name` ([String](/pt-BR/reference/data-types/string)) — Nome do disco que armazena a parte de dados.
* `path` ([String](/pt-BR/reference/data-types/string)) — Caminho absoluto para a pasta que contém os arquivos da parte de dados.
* `column` ([String](/pt-BR/reference/data-types/string)) — Nome da coluna.
* `type` ([String](/pt-BR/reference/data-types/string)) — Tipo da coluna.
* `column_position` ([UInt64](/pt-BR/reference/data-types/int-uint)) — Posição ordinal de uma coluna na tabela, começando em 1.
* `default_kind` ([String](/pt-BR/reference/data-types/string)) — Tipo de expressão (DEFAULT, MATERIALIZED, ALIAS) do valor padrão ou uma string vazia, caso não esteja definido.
* `default_expression` ([String](/pt-BR/reference/data-types/string)) — Expressão do valor padrão ou uma string vazia, caso não esteja definida.
* `column_bytes_on_disk` ([UInt64](/pt-BR/reference/data-types/int-uint)) — Tamanho total da coluna, em bytes.
* `column_data_compressed_bytes` ([UInt64](/pt-BR/reference/data-types/int-uint)) — Tamanho total dos dados comprimidos da coluna, em bytes.
* `column_data_uncompressed_bytes` ([UInt64](/pt-BR/reference/data-types/int-uint)) — Tamanho total dos dados descomprimidos da coluna, em bytes.
* `column_marks_bytes` ([UInt64](/pt-BR/reference/data-types/int-uint)) — Tamanho da coluna com marcas, em bytes.
* `column_modification_time` ([Nullable(DateTime)](/pt-BR/reference/data-types/nullable)) — Data e hora da última modificação da coluna.

**Aliases:**

* `bytes` — Alias de `bytes_on_disk`.
* `marks_size` — Alias de `marks_bytes`.
* `part_name` — Alias de `name`.

{/*AUTOGENERATED_END*/}