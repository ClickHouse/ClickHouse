---
description: 'Tabela de sistema com informações sobre tarefas em pools de agendamento em segundo plano.'
keywords: ['tabela de sistema', 'background_schedule_pool']
slug: /operations/system-tables/background_schedule_pool
title: 'system.background_schedule_pool'
doc_type: 'reference'
---

import SystemTableCloud from '/snippets/pt-BR/_system_table_cloud.mdx';

<SystemTableCloud />

<div id="description">
  ## Descrição
</div>

Contém informações sobre tarefas em pools de agendamento em segundo plano. Esses pools de agendamento em segundo plano são usados para executar tarefas periódicas, como envios distribuídos, flushes de buffer, operações do broker de mensagens, tarefas em segundo plano de consultas em streaming e atualização de metadados da tabela Iceberg.

<div id="columns">
  ## Colunas
</div>

{/*AUTOGENERATED_START*/}

* `pool` ([String](/pt-BR/reference/data-types/index)) — Nome do pool. Valores possíveis: `schedule` (pool de agendamento de uso geral), `buffer_flush` (pool para descarregar dados da tabela Buffer), `distributed` (pool para operações de tabela distribuída), `message_broker` (pool para operações do broker de mensagens), `streaming` (pool para tarefas em segundo plano de consultas de streaming), `iceberg` (pool para atualização de metadados da tabela Iceberg).
* `database` ([String](/pt-BR/reference/data-types/index)) — Nome do banco de dados.
* `table` ([String](/pt-BR/reference/data-types/index)) — Nome da tabela.
* `table_uuid` ([UUID](/pt-BR/reference/data-types/index)) — UUID da tabela.
* `query_id` ([String](/pt-BR/reference/data-types/index)) — ID da consulta (se estiver em execução no momento). Observação: esta não é uma consulta real, mas um ID gerado aleatoriamente para corresponder logs em `system.text_log`.
* `elapsed_ms` ([UInt64](/pt-BR/reference/data-types/index)) — Tempo de execução da tarefa (se estiver em execução no momento).
* `log_name` ([String](/pt-BR/reference/data-types/index)) — Nome do log da tarefa.
* `deactivated` ([UInt8](/pt-BR/reference/data-types/index)) — Indica se a tarefa está desativada (sempre falso, já que tarefas desativadas são removidas do pool).
* `scheduled` ([UInt8](/pt-BR/reference/data-types/index)) — Indica se a tarefa está agendada para execução.
* `delayed` ([UInt8](/pt-BR/reference/data-types/index)) — Indica se a tarefa está agendada com atraso.
* `executing` ([UInt8](/pt-BR/reference/data-types/index)) — Indica se a tarefa está sendo executada no momento.

{/*AUTOGENERATED_END*/}

<div id="example">
  ## Exemplo
</div>

```sql title="Query"
SELECT * FROM system.background_schedule_pool LIMIT 5 FORMAT Vertical;
```

```text title="Response"
Row 1:
──────
pool:        distributed
database:    default
table:       data
table_uuid:  00000000-0000-0000-0000-000000000000
query_id:
elapsed_ms:  0
log_name:    BackgroundJobsAssignee:DataProcessing
deactivated: 0
scheduled:   1
delayed:     0
executing:   0
```

<div id="see-also">
  ## Veja também
</div>

* [system.background&#95;schedule&#95;pool&#95;log](/pt-BR/reference/system-tables/background_schedule_pool_log) — Contém o histórico de execuções de tarefas do pool de agendamento em segundo plano.