---
description: 'Contém uma lista de tokens de preenchimento automático.'
keywords: ['tabela de sistema', 'completions']
slug: /operations/system-tables/completions
title: 'system.completions'
doc_type: 'referência'
---

{/*AUTOGENERATED_START*/}

## Descrição {#description}

Contém uma lista de tokens de preenchimento automático.

## Colunas {#columns}

* `word` ([String](/pt-BR/reference/data-types/string)) — Token de preenchimento automático.
* `context` ([String](/pt-BR/reference/data-types/string)) — Tipo de entidade do token (por exemplo, tabela).
* `belongs` ([Nullable(String)](/pt-BR/reference/data-types/nullable)) — Entidade à qual este token pertence (por exemplo, nome do banco de dados proprietário).

{/*AUTOGENERATED_END*/}