---
description: 'Tabela do sistema que contém informações sobre as cargas de trabalho no
  servidor local.'
keywords: ['tabela do sistema', 'cargas de trabalho']
slug: /operations/system-tables/workloads
title: 'system.workloads'
doc_type: 'referência'
---

{/*AUTOGENERATED_START*/}

## Descrição {#description}

Contém informações sobre as [cargas de trabalho](/pt-BR/concepts/features/configuration/server-config/workload-scheduling#workload_entity_storage) armazenadas no servidor local. A tabela contém uma linha para cada carga de trabalho.

## Colunas {#columns}

* `name` ([String](/pt-BR/reference/data-types/string)) — O nome da carga de trabalho.
* `parent` ([String](/pt-BR/reference/data-types/string)) — O nome da carga de trabalho pai.
* `create_query` ([String](/pt-BR/reference/data-types/string)) — A consulta CREATE da carga de trabalho.

## Exemplo {#example}

```sql
SELECT *
FROM system.workloads
FORMAT Vertical
```

```text
Row 1:
──────
name:         production
parent:       all
create_query: CREATE WORKLOAD production IN `all` SETTINGS weight = 9

Row 2:
──────
name:         development
parent:       all
create_query: CREATE WORKLOAD development IN `all`

Row 3:
──────
name:         all
parent:
create_query: CREATE WORKLOAD `all`
```

{/*AUTOGENERATED_END*/}