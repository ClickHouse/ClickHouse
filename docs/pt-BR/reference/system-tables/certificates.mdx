---
description: 'Contém informações sobre os certificados disponíveis e suas fontes.'
keywords: ['tabela de sistema', 'certificados']
slug: /operations/system-tables/certificates
title: 'system.certificates'
doc_type: 'referência'
---

<div id="description">
  ## Descrição
</div>

Contém informações sobre os certificados disponíveis e suas origens.

<div id="columns">
  ## Colunas
</div>

{/*AUTOGENERATED_START*/}

* `version` ([Int32](/pt-BR/reference/data-types/int-uint)) — Versão do certificado. Os valores são 0 para v1, 1 para v2 e 2 para v3.
* `serial_number` ([Nullable(String)](/pt-BR/reference/data-types/nullable)) — Número de série do certificado atribuído pelo emissor.
* `signature_algo` ([Nullable(String)](/pt-BR/reference/data-types/nullable)) — Algoritmo de assinatura — algoritmo usado pelo emissor para assinar este certificado.
* `issuer` ([Nullable(String)](/pt-BR/reference/data-types/nullable)) — Emissor — identificador exclusivo da autoridade certificadora que emite este certificado.
* `not_before` ([Nullable(String)](/pt-BR/reference/data-types/nullable)) — Início do período em que este certificado é válido.
* `not_after` ([Nullable(String)](/pt-BR/reference/data-types/nullable)) — Fim do período em que este certificado é válido.
* `subject` ([Nullable(String)](/pt-BR/reference/data-types/nullable)) — Titular — identifica o proprietário da chave pública.
* `pkey_algo` ([Nullable(String)](/pt-BR/reference/data-types/nullable)) — Algoritmo de chave pública — define o algoritmo com o qual a chave pública pode ser usada.
* `path` ([String](/pt-BR/reference/data-types/string)) — Caminho para o arquivo ou diretório que contém este certificado.
* `default` ([UInt8](/pt-BR/reference/data-types/int-uint)) — O certificado está no local padrão para certificados.
* `protocol` ([String](/pt-BR/reference/data-types/string)) — Nome do protocolo, caso o certificado seja proveniente de uma configuração TLS específica do protocolo; caso contrário, vazio.

{/*AUTOGENERATED_END*/}