---
description: 'Usado para implementar views (para mais informações, consulte a consulta `CREATE VIEW`).
  Não armazena dados, apenas a consulta `SELECT` especificada. Ao ler de uma tabela,
  executa essa consulta (e remove dela todas as colunas desnecessárias).'
sidebarTitle: 'View'
slug: /engines/table-engines/special/view
title: 'Mecanismo de tabela View'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

Usado para implementar views (para mais informações, consulte a consulta `CREATE VIEW`). Não armazena dados, apenas a consulta `SELECT` especificada. Ao ler de uma tabela, executa essa consulta (e remove dela todas as colunas desnecessárias).

{/*AUTOGENERATED_END*/}