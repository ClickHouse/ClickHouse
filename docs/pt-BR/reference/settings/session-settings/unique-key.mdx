---
title: 'Configurações de sessão de unique_key_*'
sidebarTitle: 'unique_key_*'
toc_max_heading_level: 2
description: 'Configurações de sessão do ClickHouse no grupo gerado unique_key_*.'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/pt-BR/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import { VersionHistory } from "/snippets/pt-BR/components/VersionHistory/VersionHistory.jsx";
import { ExperimentalBadge } from "/snippets/pt-BR/components/ExperimentalBadge/ExperimentalBadge.jsx";

Essas configurações estão disponíveis em [system.settings](/pt-BR/reference/system-tables/settings) e são geradas automaticamente a partir do [código-fonte](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp).

<div id="unique_key_max_encoded_size">
  ## unique_key_max_encoded_size
</div>

<ExperimentalBadge />

<SettingsInfoBlock type="UInt64" default_value="256" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.6"},{"label": "256"},{"label": "Nova configuração: tamanho máximo (em bytes) da codificação binária que preserva a ordem de uma única linha de UNIQUE KEY"}]}]} />

Tamanho máximo (em bytes) da codificação binária que preserva a ordem de uma única linha de `UNIQUE KEY`.

<div id="unique_key_probe_implementation">
  ## unique_key_probe_implementation
</div>

<ExperimentalBadge />

<SettingsInfoBlock type="UniqueKeyProbeImplementation" default_value="auto" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.8"},{"label": "auto"},{"label": "Nova configuração: seleciona a implementação da sondagem de UNIQUE KEY (atualmente, existe apenas a implementação simples de referência)"}]}]} />

Seleciona a implementação da sondagem no caminho de gravação de UNIQUE KEY. Atualmente, existe apenas uma implementação de referência de thread única,
portanto, `auto` e `simple` são ambos resolvidos para ela.

Reservado: esta configuração ainda não tem efeito — o valor não é lido até que o caminho de
gravação de INSERT de UNIQUE KEY que constrói a sondagem seja conectado. Declarada agora para que o recurso seja disponibilizado junto com a
implementação.

{/*AUTOGENERATED_END*/}