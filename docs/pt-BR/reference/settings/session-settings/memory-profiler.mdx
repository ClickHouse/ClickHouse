---
title: 'configurações de sessão memory_profiler_*'
sidebarTitle: 'memory_profiler_*'
toc_max_heading_level: 2
description: 'Configurações de sessão do ClickHouse no grupo gerado por memory_profiler_*.'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/pt-BR/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

Essas configurações estão disponíveis em [system.settings](/pt-BR/reference/system-tables/settings) e são geradas automaticamente com base no [código-fonte](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp).

<div id="memory_profiler_sample_max_allocation_size">
  ## memory_profiler_sample_max_allocation_size
</div>

<SettingsInfoBlock type="UInt64" default_value="0" />

Coleta alocações aleatórias com tamanho menor ou igual ao valor especificado, com probabilidade igual a `memory_profiler_sample_probability`. 0 significa desativado. Convém definir &#39;max&#95;untracked&#95;memory&#39; como 0 para que esse limite funcione como esperado.

<div id="memory_profiler_sample_min_allocation_size">
  ## memory_profiler_sample_min_allocation_size
</div>

<SettingsInfoBlock type="UInt64" default_value="0" />

Coleta alocações aleatórias com tamanho maior ou igual ao valor especificado, com probabilidade igual a `memory_profiler_sample_probability`. 0 significa desativado. Convém definir &#39;max&#95;untracked&#95;memory&#39; como 0 para que esse limite funcione como esperado.

<div id="memory_profiler_sample_probability">
  ## memory_profiler_sample_probability
</div>

<SettingsInfoBlock type="Float" default_value="0" />

Coleta alocações e desalocações aleatórias e as grava em system.trace&#95;log com `trace_type` &#39;MemorySample&#39;. A probabilidade vale para cada alloc/free, independentemente do tamanho da alocação (isso pode ser alterado com `memory_profiler_sample_min_allocation_size` e `memory_profiler_sample_max_allocation_size`). Observe que a amostragem ocorre somente quando a quantidade de memória não rastreada excede &#39;max&#95;untracked&#95;memory&#39;. Talvez seja interessante definir &#39;max&#95;untracked&#95;memory&#39; como 0 para uma amostragem ainda mais granular.

<div id="memory_profiler_step">
  ## memory_profiler_step
</div>

<SettingsInfoBlock type="UInt64" default_value="4194304" />

Define o passo do profiler de memória. Sempre que o uso de memória da consulta ultrapassar cada próximo passo em número de bytes, o profiler de memória coletará o stacktrace de alocação e o gravará em [trace&#95;log](/pt-BR/reference/system-tables/trace_log).

Valores possíveis:

* Um número inteiro positivo de bytes.

* 0 para desativar o profiler de memória.

{/*AUTOGENERATED_END*/}