---
title: 'configurações de sessão iceberg_data_*'
sidebarTitle: 'iceberg_data_*'
toc_max_heading_level: 2
description: 'Configurações de sessão do ClickHouse no grupo gerado iceberg_data_*.'
doc_type: 'reference'
---

{/*INÍCIO_AUTOGERADO*/}

import SettingsInfoBlock from "/snippets/pt-BR/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import { VersionHistory } from "/snippets/pt-BR/components/VersionHistory/VersionHistory.jsx";

Essas configurações estão disponíveis em [system.settings](/pt-BR/reference/system-tables/settings) e são geradas automaticamente a partir do [código-fonte](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp).

## iceberg_data_file_size_lower_threshold_compaction {#iceberg_data_file_size_lower_threshold_compaction}

<SettingsInfoBlock type="UInt64" default_value="402653184" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.9"},{"label": "402653184"},{"label": "Alinhado com a forma como o procedimento `rewrite_data_files` do Iceberg deriva `min-file-size-bytes`: 0,75 do tamanho de arquivo alvo (512 MiB). A compaction agora seleciona arquivos menores que 384 MiB, em vez de menores que 10 MiB."}]}, {"id": "row-2","items": [{"label": "26.5"},{"label": "10485760"},{"label": "Nova configuração"}]}]} />

Data files menores que esse valor são selecionados para compaction.

O padrão é `0,75` do valor padrão documentado da propriedade de Iceberg table `write.target-file-size-bytes`
(512 MiB), que é como o procedimento `rewrite_data_files` deriva sua opção `min-file-size-bytes`;
consulte https://iceberg.apache.org/docs/1.5.2/configuration/.

## iceberg_data_file_size_upper_threshold_compaction {#iceberg_data_file_size_upper_threshold_compaction}

<SettingsInfoBlock type="UInt64" default_value="966367641" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.9"},{"label": "966367641"},{"label": "Alinhado com a forma como o procedimento `rewrite_data_files` do Iceberg deriva o `max-file-size-bytes`: 1,8 do tamanho de arquivo alvo (512 MiB)."}]}, {"id": "row-2","items": [{"label": "26.5"},{"label": "10737418240"},{"label": "Nova configuração"}]}]} />

Data files maiores que este valor são selecionados para compaction.

O padrão é `1.8` do valor padrão documentado da propriedade de tabela do Iceberg `write.target-file-size-bytes`
(512 MiB), que é como o procedimento `rewrite_data_files` deriva sua opção `max-file-size-bytes`;
consulte https://iceberg.apache.org/docs/1.5.2/configuration/.

{/*AUTOGENERATED_END*/}