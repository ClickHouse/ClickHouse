---
title: 'configurações de sessão de low_cardinality_*'
sidebarTitle: 'low_cardinality_*'
toc_max_heading_level: 2
description: 'Configurações de sessão do ClickHouse no grupo low_cardinality_* gerado automaticamente.'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/pt-BR/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

Essas configurações estão disponíveis em [system.settings](/pt-BR/reference/system-tables/settings) e são geradas automaticamente a partir do [código-fonte](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp).

<div id="low_cardinality_allow_in_native_format">
  ## low_cardinality_allow_in_native_format
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

Permite ou restringe o uso do tipo de dados [LowCardinality](/pt-BR/reference/data-types/lowcardinality) com o formato [Native](/pt-BR/reference/formats/Native).

Se o uso de `LowCardinality` for restringido, o servidor ClickHouse converterá colunas `LowCardinality` em colunas comuns para consultas `SELECT` e colunas comuns em colunas `LowCardinality` para consultas `INSERT`.

Essa configuração é necessária principalmente para clientes de terceiros que não oferecem suporte ao tipo de dados `LowCardinality`.

Valores possíveis:

* 1 — O uso de `LowCardinality` não é restringido.
* 0 — O uso de `LowCardinality` é restringido.

<div id="low_cardinality_max_dictionary_size">
  ## low_cardinality_max_dictionary_size
</div>

<SettingsInfoBlock type="UInt64" default_value="8192" />

Define um tamanho máximo, em linhas, para um Dicionário global compartilhado do tipo de dado [LowCardinality](/pt-BR/reference/data-types/lowcardinality) que pode ser gravado em um sistema de arquivos de armazenamento. Essa configuração evita problemas de RAM em caso de crescimento ilimitado do dicionário. Todos os dados que não puderem ser codificados devido à limitação de tamanho máximo do dicionário são gravados pelo ClickHouse usando o método comum.

Valores possíveis:

* Qualquer inteiro positivo.

<div id="low_cardinality_use_single_dictionary_for_part">
  ## low_cardinality_use_single_dictionary_for_part
</div>

<SettingsInfoBlock type="Bool" default_value="0" />

Ativa ou desativa o uso de um único dicionário para a parte de dados.

Por padrão, o servidor ClickHouse monitora o tamanho dos dicionários e, se um dicionário exceder o limite, o servidor começa a gravar o próximo. Para impedir a criação de vários dicionários, defina `low_cardinality_use_single_dictionary_for_part = 1`.

Valores possíveis:

* 1 — A criação de vários dicionários para a parte de dados é proibida.
* 0 — A criação de vários dicionários para a parte de dados não é proibida.

{/*AUTOGENERATED_END*/}