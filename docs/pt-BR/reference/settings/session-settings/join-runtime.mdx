---
title: 'configurações de sessão join_runtime_*'
sidebarTitle: 'join_runtime_*'
toc_max_heading_level: 2
description: 'Configurações de sessão do ClickHouse no grupo gerado de join_runtime_*.'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/pt-BR/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import { VersionHistory } from "/snippets/pt-BR/components/VersionHistory/VersionHistory.jsx";
import { ExperimentalBadge } from "/snippets/pt-BR/components/ExperimentalBadge/ExperimentalBadge.jsx";

Essas configurações estão disponíveis em [system.settings](/pt-BR/reference/system-tables/settings) e são geradas automaticamente a partir do [código-fonte](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp).

<div id="join_runtime_bloom_filter_bytes">
  ## join_runtime_bloom_filter_bytes
</div>

<ExperimentalBadge />

<SettingsInfoBlock type="UInt64" default_value="524288" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "25.10"},{"label": "524288"},{"label": "Nova configuração"}]}]} />

Tamanho, em bytes, de um filtro de Bloom usado como filtro de runtime de JOIN (consulte a configuração enable&#95;join&#95;runtime&#95;filters).

<div id="join_runtime_bloom_filter_hash_functions">
  ## join_runtime_bloom_filter_hash_functions
</div>

<ExperimentalBadge />

<SettingsInfoBlock type="UInt64" default_value="3" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "25.10"},{"label": "3"},{"label": "Nova configuração"}]}]} />

Número de funções de hash em um filtro de Bloom usado como filtro de runtime em JOIN (consulte a configuração enable&#95;join&#95;runtime&#95;filters).

<div id="join_runtime_bloom_filter_max_ratio_of_set_bits">
  ## join_runtime_bloom_filter_max_ratio_of_set_bits
</div>

<ExperimentalBadge />

<SettingsInfoBlock type="Double" default_value="0.7" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.1"},{"label": "0.7"},{"label": "Nova configuração"}]}]} />

Se o número de bits definidos em um filtro de Bloom de runtime exceder essa razão, o filtro será totalmente desabilitado para reduzir a sobrecarga.

<div id="join_runtime_filter_blocks_to_skip_before_reenabling">
  ## join_runtime_filter_blocks_to_skip_before_reenabling
</div>

<ExperimentalBadge />

<SettingsInfoBlock type="UInt64" default_value="30" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.1"},{"label": "30"},{"label": "Nova configuração"}]}]} />

Número de blocos ignorados antes de tentar reativar dinamicamente um filtro de runtime que havia sido desativado anteriormente devido a uma baixa razão de filtragem.

<div id="join_runtime_filter_exact_values_limit">
  ## join_runtime_filter_exact_values_limit
</div>

<ExperimentalBadge />

<SettingsInfoBlock type="UInt64" default_value="10000" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "25.10"},{"label": "10000"},{"label": "Nova configuração"}]}]} />

Número máximo de elementos no filtro de runtime armazenados como estão em um conjunto; quando esse limite é excedido, ele passa a usar um filtro de Bloom.

<div id="join_runtime_filter_from_fixed_hash_table">
  ## join_runtime_filter_from_fixed_hash_table
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.6"},{"label": "1"},{"label": "Nova configuração."}]}]} />

Quando o lado de build do hash join for convertido em um FixedHashMap (consulte `enable_join_fixed_hash_table_conversion`), use esse mapa hash diretamente como filtro de runtime.

<div id="join_runtime_filter_min_probe_rows">
  ## join_runtime_filter_min_probe_rows
</div>

<ExperimentalBadge />

<SettingsInfoBlock type="UInt64" default_value="1000" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.8"},{"label": "1000"},{"label": "Nova configuração para controlar o tamanho mínimo do lado de sondagem para a instalação de filtros de runtime de JOIN. Antes, não havia limite, portanto o valor anterior era 0, o que significa que o filtro era sempre instalado."}]}]} />

Se, durante o planejamento da consulta, for estimado que o lado de sondagem de um JOIN produzirá no máximo este número de linhas, o filtro de runtime de JOIN não será criado. Criar e aplicar um filtro de runtime para um lado de sondagem muito pequeno custa mais do que economiza. Defina como 0 para sempre criar o filtro de runtime, independentemente do tamanho estimado do lado de sondagem.

<div id="join_runtime_filter_pass_ratio_threshold_for_disabling">
  ## join_runtime_filter_pass_ratio_threshold_for_disabling
</div>

<ExperimentalBadge />

<SettingsInfoBlock type="Double" default_value="0.7" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.1"},{"label": "0.7"},{"label": "Nova configuração"}]}]} />

Se a razão entre as linhas que passaram e as linhas verificadas for maior que esse limite, o filtro de runtime será considerado pouco eficiente e será desativado nos próximos `join_runtime_filter_blocks_to_skip_before_reenabling` blocos para reduzir a sobrecarga.

<div id="join_runtime_filter_size_from_hash_table_stats">
  ## join_runtime_filter_size_from_hash_table_stats
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.7"},{"label": "1"},{"label": "Usa estatísticas do tamanho da tabela hash coletadas em execuções anteriores para dimensionar o filtro de runtime do JOIN. Quando desabilitado, usa o valor fixo de `join_runtime_bloom_filter_bytes`."}]}]} />

Usa estatísticas do tamanho da tabela hash coletadas em execuções anteriores para dimensionar o filtro de runtime do JOIN. Quando desabilitado, usa o valor fixo de `join_runtime_bloom_filter_bytes`.

{/*AUTOGENERATED_END*/}