---
title: 'configurações de sessão join_to_*'
sidebarTitle: 'join_to_*'
toc_max_heading_level: 2
description: 'Configurações de sessão do ClickHouse no grupo gerado join_to_*.'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import ExperimentalBadge from "/snippets/pt-BR/components/ExperimentalBadge/ExperimentalBadge.jsx";
import SettingsInfoBlock from "/snippets/pt-BR/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import VersionHistory from "/snippets/pt-BR/components/VersionHistory/VersionHistory.jsx";

Essas configurações estão disponíveis em [system.settings](/pt-BR/reference/system-tables/settings) e são geradas automaticamente com base no [código-fonte](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp).

<div id="join_to_sort_maximum_table_rows">
  ## join_to_sort_maximum_table_rows
</div>

<ExperimentalBadge />

<SettingsInfoBlock type="UInt64" default_value="10000" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "24.9"},{"label": "10000"},{"label": "O número máximo de linhas na tabela à direita para determinar se ela deve ser reordenada pela chave em LEFT JOIN ou INNER JOIN"}]}]} />

O número máximo de linhas na tabela à direita para determinar se ela deve ser reordenada pela chave em LEFT JOIN ou INNER JOIN.

<div id="join_to_sort_minimum_perkey_rows">
  ## join_to_sort_minimum_perkey_rows
</div>

<ExperimentalBadge />

<SettingsInfoBlock type="UInt64" default_value="40" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "24.9"},{"label": "40"},{"label": "O limite mínimo da média de linhas por chave na tabela à direita para determinar se a tabela à direita deve ser reordenada pela chave em um LEFT JOIN ou INNER JOIN. Essa configuração garante que a otimização não seja aplicada a chaves esparsas na tabela"}]}]} />

O limite mínimo da média de linhas por chave na tabela à direita para determinar se a tabela à direita deve ser reordenada pela chave em um LEFT JOIN ou INNER JOIN. Essa configuração garante que a otimização não seja aplicada a chaves esparsas na tabela

{/*AUTOGENERATED_END*/}