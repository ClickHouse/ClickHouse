---
title: 'Outras configurações do servidor'
sidebarTitle: 'Outras'
toc_max_heading_level: 2
description: 'Configurações do servidor ClickHouse no grupo gerado "Other".'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SystemLogParameters from '/snippets/pt-BR/_system-log-parameters.mdx';
import SettingsInfoBlock from "/snippets/pt-BR/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

Essas configurações se aplicam ao servidor ClickHouse e são geradas automaticamente a partir do código-fonte do ClickHouse.

<div id="abort_on_logical_error">
  ## abort_on_logical_error
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="Sim" />Provoca falha no servidor em caso de exceções LOGICAL&#95;ERROR. Somente para especialistas.

<div id="allowed_disks_for_table_engines">
  ## allowed_disks_for_table_engines
</div>

Lista de discos permitidos para uso com Iceberg

<div id="auth_use_forwarded_address">
  ## auth_use_forwarded_address
</div>

Use o endereço de origem para autenticar clientes conectados por meio de proxy.

<Note>
  Esta configuração deve ser usada com cautela redobrada, já que endereços encaminhados podem ser facilmente falsificados — servidores que aceitam esse tipo de autenticação não devem ser acessados diretamente, mas sim exclusivamente por meio de um proxy confiável.
</Note>

<div id="bcrypt_workfactor">
  ## bcrypt_workfactor
</div>

Fator de trabalho para o tipo de autenticação `bcrypt_password`, que usa o [algoritmo Bcrypt](https://wildlyinaccurate.com/bcrypt-choosing-a-work-factor/).
O fator de trabalho define a quantidade de processamento e o tempo necessários para calcular o hash e verificar a senha.

```xml
<bcrypt_workfactor>12</bcrypt_workfactor>
```

<Warning>
  Para aplicações com autenticação em alta frequência,
  considere métodos alternativos de autenticação devido à
  sobrecarga computacional do bcrypt com fatores de trabalho mais altos.
</Warning>

<div id="blob_storage_log">
  ## blob_storage_log
</div>

Configurações da tabela de sistema [`blob_storage_log`](/pt-BR/reference/system-tables/blob_storage_log).

<SystemLogParameters />

Exemplo:

```xml
<blob_storage_log>
    <database>system</database
    <table>blob_storage_log</table
    <partition_by>toYYYYMM(event_date)</partition_by>
    <flush_interval_milliseconds>7500</flush_interval_milliseconds
    <ttl>event_date + INTERVAL 30 DAY</ttl>
</blob_storage_log>
```

<div id="builtin_dictionaries_reload_interval">
  ## builtin_dictionaries_reload_interval
</div>

O intervalo, em segundos, até recarregar os dicionários internos.

O ClickHouse recarrega os dicionários internos a cada x segundos. Isso permite editar dicionários &quot;em tempo real&quot; sem reiniciar o servidor.

**Exemplo**

```xml
<builtin_dictionaries_reload_interval>3600</builtin_dictionaries_reload_interval>
```

<div id="cache_size_to_ram_max_ratio">
  ## cache_size_to_ram_max_ratio
</div>

<SettingsInfoBlock type="Double" default_value="0.5" changeable_without_restart="No" />Define a razão máxima entre o tamanho do cache e a RAM. Permite reduzir o tamanho do cache em sistemas com pouca memória.

<div id="cannot_allocate_thread_fault_injection_probability">
  ## cannot_allocate_thread_fault_injection_probability
</div>

<SettingsInfoBlock type="Double" default_value="0" changeable_without_restart="No" />Para fins de teste.

<div id="cgroups_memory_usage_observer_wait_time">
  ## cgroups_memory_usage_observer_wait_time
</div>

<SettingsInfoBlock type="UInt64" default_value="15" changeable_without_restart="No" />

Intervalo, em segundos, durante o qual o consumo máximo de memória permitido do servidor é ajustado de acordo com o limite correspondente nos cgroups.

Para desativar o observador de cgroups, defina este valor como `0`.

<div id="compression">
  ## compressão
</div>

Configurações de compressão de dados para tabelas com o motor [MergeTree](/pt-BR/reference/engines/table-engines/mergetree-family/mergetree).

<Note>
  Recomendamos não alterar essa configuração se você estiver começando a usar o ClickHouse.
</Note>

**Modelo de configuração**:

```xml
<compression>
    <case>
      <min_part_size>...</min_part_size>
      <min_part_size_ratio>...</min_part_size_ratio>
      <method>...</method>
      <level>...</level>
    </case>
    ...
</compression>
```

**Campos de `<case>`**:

* `min_part_size` – O tamanho mínimo de uma parte de dados.
* `min_part_size_ratio` – A razão entre o tamanho da parte de dados e o tamanho da tabela.
* `method` – Método de compressão. Valores aceitos: `lz4`, `lz4hc`, `zstd`,`deflate_qpl`.
* `level` – Nível de compressão. Consulte [Codecs](/pt-BR/reference/statements/create/table#general-purpose-codecs).

<Note>
  Você pode configurar várias seções `<case>`.
</Note>

**Ações quando as condições são atendidas**:

* Se uma parte de dados corresponder a um conjunto de condições, o ClickHouse usará o método de compressão especificado.
* Se uma parte de dados corresponder a vários conjuntos de condições, o ClickHouse usará o primeiro conjunto de condições correspondente.

<Note>
  Se nenhuma condição for atendida para uma parte de dados, o ClickHouse usará a compressão `lz4`.
</Note>

**Exemplo**

```xml
<compression incl="clickhouse_compression">
    <case>
        <min_part_size>10000000000</min_part_size>
        <min_part_size_ratio>0.01</min_part_size_ratio>
        <method>zstd</method>
        <level>1</level>
    </case>
</compression>
```

<div id="config_reload_interval_ms">
  ## config_reload_interval_ms
</div>

<SettingsInfoBlock type="UInt64" default_value="2000" changeable_without_restart="Sim" />

Com que frequência o ClickHouse recarrega a config e verifica se há novas alterações

<div id="config-file">
  ## config-file
</div>

<SettingsInfoBlock type="String" default_value="config.xml" changeable_without_restart="No" />Indica o arquivo de configuração do servidor.

<div id="core_dump">
  ## core_dump
</div>

Configura o limite flexível do tamanho do arquivo de core dump.

<Note>
  O limite rígido é configurado por meio de ferramentas do sistema
</Note>

**Exemplo**

```xml
<core_dump>
     <size_limit>1073741824</size_limit>
</core_dump>
```

<div id="crash_log">
  ## crash_log
</div>

Configurações para a operação da tabela de sistema [crash&#95;log](/pt-BR/reference/system-tables/crash_log).

As configurações a seguir podem ser definidas por subtags:

| Setting                            | Description                                                                                                                                                                           | Default             | Note                                                                                                                                               |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| `buffer_size_rows_flush_threshold` | Limiar para a quantidade de linhas. Se esse limiar for atingido, a gravação dos logs em disco será iniciada em segundo plano.                                                         | `max_size_rows / 2` |                                                                                                                                                    |
| `database`                         | Nome do banco de dados.                                                                                                                                                               |                     |                                                                                                                                                    |
| `engine`                           | [Definição do motor MergeTree](/pt-BR/reference/engines/table-engines/mergetree-family/mergetree#table_engine-mergetree-creating-a-table) para uma tabela de sistema.                       |                     | Não pode ser usado se `partition_by` ou `order_by` estiver definido. Se não for especificado, `MergeTree` será selecionado por padrão              |
| `flush_interval_milliseconds`      | Intervalo para gravar os dados do buffer em memória na tabela.                                                                                                                        | `7500`              |                                                                                                                                                    |
| `flush_on_crash`                   | Define se os logs devem ser despejados em disco em caso de falha.                                                                                                                     | `false`             |                                                                                                                                                    |
| `max_size_rows`                    | Tamanho máximo, em linhas, para os logs. Quando a quantidade de logs ainda não gravados atinge max&#95;size, os logs são despejados em disco.                                         | `1024`              |                                                                                                                                                    |
| `order_by`                         | [Chave de ordenação personalizada](/pt-BR/reference/engines/table-engines/mergetree-family/mergetree#order_by) para uma tabela de sistema. Não pode ser usada se `engine` estiver definido. |                     | Se `engine` for especificado para a tabela de sistema, o parâmetro `order_by` deverá ser especificado diretamente dentro de &#39;engine&#39;       |
| `partition_by`                     | [Chave de particionamento personalizada](/pt-BR/reference/engines/table-engines/mergetree-family/custom-partitioning-key) para uma tabela de sistema.                                       |                     | Se `engine` for especificado para a tabela de sistema, o parâmetro `partition_by` deverá ser especificado diretamente dentro de &#39;engine&#39;   |
| `reserved_size_rows`               | Tamanho de memória pré-alocado, em linhas, para os logs.                                                                                                                              | `1024`              |                                                                                                                                                    |
| `settings`                         | [Parâmetros adicionais](/pt-BR/reference/engines/table-engines/mergetree-family/mergetree#settings) que controlam o comportamento do MergeTree (opcional).                                  |                     | Se `engine` for especificado para a tabela de sistema, o parâmetro `settings` deverá ser especificado diretamente dentro de &#39;engine&#39;       |
| `storage_policy`                   | Nome da política de armazenamento a ser usada para a tabela (opcional).                                                                                                               |                     | Se `engine` for especificado para a tabela de sistema, o parâmetro `storage_policy` deverá ser especificado diretamente dentro de &#39;engine&#39; |
| `table`                            | Nome da tabela de sistema.                                                                                                                                                            |                     |                                                                                                                                                    |
| `ttl`                              | Especifica o [TTL](/pt-BR/reference/engines/table-engines/mergetree-family/mergetree#table_engine-mergetree-ttl) da tabela.                                                                 |                     | Se `engine` for especificado para a tabela de sistema, o parâmetro `ttl` deverá ser especificado diretamente dentro de &#39;engine&#39;            |

O arquivo de configuração padrão do servidor `config.xml` contém a seguinte seção de configurações:

```xml
<crash_log>
    <database>system</database>
    <table>crash_log</table>
    <partition_by>toYYYYMM(event_date)</partition_by>
    <flush_interval_milliseconds>7500</flush_interval_milliseconds>
    <max_size_rows>1024</max_size_rows>
    <reserved_size_rows>1024</reserved_size_rows>
    <buffer_size_rows_flush_threshold>512</buffer_size_rows_flush_threshold>
    <flush_on_crash>false</flush_on_crash>
</crash_log>
```

<div id="database_atomic_delay_before_drop_table_sec">
  ## database_atomic_delay_before_drop_table_sec
</div>

<SettingsInfoBlock type="UInt64" default_value="480" changeable_without_restart="No" />

O intervalo durante o qual uma tabela removida pode ser restaurada usando a instrução [`UNDROP`](/pt-BR/reference/statements/undrop). Se `DROP TABLE` for executado com o modificador `SYNC`, essa configuração será ignorada.
O valor padrão dessa configuração é `480` (8 minutos).

<div id="dead_letter_queue">
  ## dead_letter_queue
</div>

Configuração da tabela de sistema &#39;dead&#95;letter&#95;queue&#39;.

<SystemLogParameters />

As configurações padrão são:

```xml
<dead_letter_queue>
    <database>system</database>
    <table>dead_letter</table>
    <partition_by>toYYYYMM(event_date)</partition_by>
    <flush_interval_milliseconds>7500</flush_interval_milliseconds>
</dead_letter_queue>
```

<div id="dictionary_background_reconnect_interval">
  ## dictionary_background_reconnect_interval
</div>

<SettingsInfoBlock type="UInt64" default_value="1000" changeable_without_restart="No" />Intervalo, em milissegundos, entre tentativas de reconexão de dicionários MySQL e Postgres que falharam e têm `background_reconnect` habilitado.

<div id="disk_transaction_wait_for_blob_removal">
  ## disk_transaction_wait_for_blob_removal
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />

Valor padrão da configuração `wait_for_blob_removal` por disco.
Quando habilitada, o servidor aguarda a conclusão da remoção de blob em segundo plano antes de confirmar a operação.

<div id="display_secrets_in_show_and_select">
  ## display_secrets_in_show_and_select
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />

Ativa ou desativa a exibição de secrets em consultas `SHOW` e `SELECT` para tabelas, bancos de dados, table functions e dicionários.

O usuário que quiser ver secrets também deve ter a
[`format_display_secrets_in_show_and_select` configuração de formato](/pt-BR/reference/settings/formats#format_display_secrets_in_show_and_select)
ativada e o privilégio
[`displaySecretsInShowAndSelect`](/pt-BR/reference/statements/grant#displaysecretsinshowandselect).

Valores possíveis:

* `0` — Desativado.
* `1` — Ativado.

<div id="dns_max_consecutive_failures">
  ## dns_max_consecutive_failures
</div>

<SettingsInfoBlock type="UInt32" default_value="5" changeable_without_restart="No" />

Interrompe novas tentativas de atualizar o cache DNS de um hostname após esse número de falhas consecutivas. As informações permanecem no cache DNS. Zero significa ilimitado.

**Veja também**

* [`SYSTEM DROP DNS CACHE`](/pt-BR/reference/statements/system#drop-dns-cache)

<div id="dynamic_user_defined_executable_functions_path">
  ## dynamic_user_defined_executable_functions_path
</div>

<SettingsInfoBlock type="String" default_value="/var/lib/clickhouse/dynamic_user_defined_executable_functions/" changeable_without_restart="No" />

O diretório usado para armazenar os arquivos de configuração de UDFs executáveis criadas dinamicamente por drivers (consulte `CREATE FUNCTION ... ENGINE = DriverName(...)`).
Ao reiniciar o servidor, o diretório é verificado em busca de arquivos de configuração, e as UDFs correspondentes são carregadas sem invocar o driver novamente.

**Exemplo**

```xml
<dynamic_user_defined_executable_functions_path>/var/lib/clickhouse/dynamic_user_defined_executable_functions/</dynamic_user_defined_executable_functions_path>
```

<div id="encryption">
  ## criptografia
</div>

Configura um comando para obter uma chave a ser usada pelos [codecs de criptografia](/pt-BR/reference/statements/create/table#encryption-codecs). A chave (ou as chaves) deve ser armazenada em variáveis de ambiente ou definida no arquivo de configuração.

As chaves podem estar em hexadecimal ou ser uma string com comprimento de 16 bytes.

**Exemplo**

Carregamento pela configuração:

```xml
<encryption_codecs>
    <aes_128_gcm_siv>
        <key>1234567812345678</key>
    </aes_128_gcm_siv>
</encryption_codecs>
```

<Note>
  Não é recomendável armazenar chaves no arquivo de configuração. Isso não é seguro. Você pode mover as chaves para um arquivo de configuração separado em um disco seguro e criar um link simbólico para esse arquivo na pasta `config.d/`.
</Note>

Carregamento via configuração, quando a chave está em hexadecimal:

```xml
<encryption_codecs>
    <aes_128_gcm_siv>
        <key_hex>00112233445566778899aabbccddeeff</key_hex>
    </aes_128_gcm_siv>
</encryption_codecs>
```

Carregando a chave a partir da variável de ambiente:

```xml
<encryption_codecs>
    <aes_128_gcm_siv>
        <key_hex from_env="ENVVAR"></key_hex>
    </aes_128_gcm_siv>
</encryption_codecs>
```

Aqui, `current_key_id` define a chave atual para criptografia, e todas as chaves especificadas podem ser usadas para descriptografia.

Cada um desses métodos pode ser aplicado a várias chaves:

```xml
<encryption_codecs>
    <aes_128_gcm_siv>
        <key_hex id="0">00112233445566778899aabbccddeeff</key_hex>
        <key_hex id="1" from_env="ENVVAR"></key_hex>
        <current_key_id>1</current_key_id>
    </aes_128_gcm_siv>
</encryption_codecs>
```

Aqui, `current_key_id` mostra a chave de criptografia atual.

Além disso, os usuários podem adicionar um nonce, que deve ter 12 bytes de comprimento (por padrão, os processos de criptografia e descriptografia usam um nonce composto por bytes zerados):

```xml
<encryption_codecs>
    <aes_128_gcm_siv>
        <nonce>012345678910</nonce>
    </aes_128_gcm_siv>
</encryption_codecs>
```

Ou também pode ser definido em hexadecimal:

```xml
<encryption_codecs>
    <aes_128_gcm_siv>
        <nonce_hex>abcdefabcdef</nonce_hex>
    </aes_128_gcm_siv>
</encryption_codecs>
```

<Note>
  Tudo o que foi mencionado acima pode ser aplicado a `aes_256_gcm_siv` (mas a chave deve ter 32 bytes de comprimento).
</Note>

<div id="enforce_keeper_component_tracking">
  ## enforce_keeper_component_tracking
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />

Se habilitada, toda solicitação ao ZooKeeper deve ter um nome de componente definido por meio de `Coordination::setCurrentComponent`. Lança uma exceção `LOGICAL_ERROR` se o componente não estiver definido.

<div id="error_log">
  ## error_log
</div>

Ele vem desativado por padrão.

**Habilitação**

Para ativar manualmente a coleta do histórico de erros [`system.error_log`](/pt-BR/reference/system-tables/error_log), crie `/etc/clickhouse-server/config.d/error_log.xml` com o seguinte conteúdo:

```xml
<clickhouse>
    <error_log>
        <database>system</database>
        <table>error_log</table>
        <flush_interval_milliseconds>7500</flush_interval_milliseconds>
        <collect_interval_milliseconds>1000</collect_interval_milliseconds>
        <max_size_rows>1048576</max_size_rows>
        <reserved_size_rows>8192</reserved_size_rows>
        <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
        <flush_on_crash>false</flush_on_crash>
    </error_log>
</clickhouse>
```

**Desativando**

Para desativar a configuração `error_log`, crie o arquivo `/etc/clickhouse-server/config.d/disable_error_log.xml` com o seguinte conteúdo:

```xml
<clickhouse>
    <error_log remove="1" />
</clickhouse>
```

<SystemLogParameters />

<div id="filesystem_caches_path">
  ## filesystem_caches_path
</div>

Esta configuração define o caminho do cache.

**Exemplo**

```xml
<filesystem_caches_path>/var/lib/clickhouse/filesystem_caches/</filesystem_caches_path>
```

<div id="google_protos_path">
  ## google_protos_path
</div>

<SettingsInfoBlock type="String" default_value="/usr/share/clickhouse/protos/" changeable_without_restart="No" />

Define um diretório que contém arquivos proto para os tipos Protobuf.

**Exemplo**

```xml
<google_protos_path>/usr/share/clickhouse/protos/</google_protos_path>
```

<div id="handshake_timeout_milliseconds">
  ## handshake_timeout_milliseconds
</div>

<SettingsInfoBlock type="UInt64" default_value="30000" changeable_without_restart="No" />Tempo limite, em milissegundos, medido pelo relógio local para toda a fase de handshake TCP (Hello + Addendum). Limita por quanto tempo uma conexão não autenticada pode manter uma thread ocupada. Defina como 0 para desabilitar.

<div id="hdfs.libhdfs3_conf">
  ## hdfs.libhdfs3_conf
</div>

Aponta o libhdfs3 para o local correto da sua configuração.

<div id="hsts_max_age">
  ## hsts_max_age
</div>

Tempo de expiração do HSTS, em segundos.

<Note>
  Um valor de `0` significa que o ClickHouse desativa o HSTS. Se você definir um número positivo, o HSTS será ativado e o max-age será o número definido.
</Note>

**Exemplo**

```xml
<hsts_max_age>600000</hsts_max_age>
```

<div id="iceberg_background_schedule_pool_size">
  ## iceberg_background_schedule_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="10" changeable_without_restart="No" />Tamanho do pool de threads para obter de forma assíncrona os metadados mais recentes de um catálogo Iceberg remoto; o pool é compartilhado por todas as tabelas ativas.

<div id="ignore_empty_sql_security_in_create_view_query">
  ## ignore_empty_sql_security_in_create_view_query
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />

Se definido como true, o ClickHouse não grava os valores padrão para uma instrução SQL SECURITY vazia em consultas `CREATE VIEW`.

<Note>
  Essa configuração é necessária apenas durante o período de migração e se tornará obsoleta na versão 24.4.
</Note>

<div id="include_from">
  ## include_from
</div>

<SettingsInfoBlock type="String" default_value="/etc/metrika.xml" changeable_without_restart="No" />

O caminho para o arquivo com substituições. Os formatos XML e YAML são suportados.

Para mais informações, consulte a seção [Arquivos de configuração](/pt-BR/concepts/features/configuration/server-config/configuration-files).

**Exemplo**

```xml
<include_from>/etc/metrica.xml</include_from>
```

<div id="insert_deduplication_version">
  ## insert_deduplication_version
</div>

<SettingsInfoBlock type="String" default_value="new_unified_hash" changeable_without_restart="No" />Mecanismo obsoleto de proteção de migração. Esta versão oferece suporte apenas ao hash unificado de desduplicação de insert (`new_unified_hash`); o servidor se recusa a iniciar se essa configuração estiver presente com qualquer outro valor (como `old_separate_hashes` ou `compatible_double_hashes`). Conclua a migração de desduplicação na versão anterior antes de fazer o upgrade executando `compatible_double_hashes` (que grava tanto os hashes legados quanto os unificados). Para tabelas replicadas, execute isso por pelo menos `replicated_deduplication_window_seconds` (uma hora por padrão); as janelas padrão retêm os hashes unificados de todos os inserts durante essa janela, o que é considerado suficiente para cobrir um ciclo de nova tentativa de insert. Para tabelas não replicadas com `non_replicated_deduplication_window` &gt; 0, a janela é baseada em contagem, e não em tempo; portanto, execute `compatible_double_hashes` por pelo menos essa quantidade de inserts antes de fazer o upgrade.

<div id="io_thread_pool_queue_size">
  ## io_thread_pool_queue_size
</div>

<SettingsInfoBlock type="UInt64" default_value="10000" changeable_without_restart="Yes" />

O número máximo de jobs que podem ser programados no pool de threads de E/S.

<Note>
  Um valor de `0` significa sem limite.
</Note>

<div id="keep_alive_timeout">
  ## keep_alive_timeout
</div>

<SettingsInfoBlock type="Segundos" default_value="30" changeable_without_restart="No" />

Número de segundos que o ClickHouse aguarda por solicitações recebidas pelo protocolo HTTP antes de fechar a conexão.

**Exemplo**

```xml
<keep_alive_timeout>30</keep_alive_timeout>
```

<div id="ldap_servers">
  ## ldap_servers
</div>

Liste aqui os servidores LDAP com seus parâmetros de conexão para:

* usá-los como autenticadores de usuários locais dedicados, que têm o mecanismo de authentication `ldap` especificado em vez de `password`
* usá-los como diretório de usuários remotos.

As configurações a seguir podem ser definidas por sub-tags:

| Setting                        | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `bind_dn`                      | Modelo usado para construir o bind DN. O DN resultante será construído substituindo todas as substrings `\{user_name\}` do modelo pelo nome de usuário real em cada tentativa de autenticação.                                                                                                                                                                                                                                                                                                 |
| `enable_tls`                   | Sinalizador para ativar o uso de uma conexão segura com o servidor LDAP. Especifique `no` para o protocolo em texto simples (`ldap://`) (não recomendado). Especifique `yes` para o protocolo LDAP sobre SSL/TLS (`ldaps://`) (recomendado, padrão). Especifique `starttls` para o protocolo StartTLS legacy (protocolo em texto simples (`ldap://`), com upgrade para TLS).                                                                                                                   |
| `host`                         | Hostname ou IP do servidor LDAP; esse parâmetro é obrigatório e não pode ficar vazio.                                                                                                                                                                                                                                                                                                                                                                                                          |
| `port`                         | Porta do servidor LDAP; o padrão é 636 se `enable_tls` estiver definido como true; caso contrário, `389`.                                                                                                                                                                                                                                                                                                                                                                                      |
| `tls_ca_cert_dir`              | caminho para o diretório que contém os certificados de CA.                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `tls_ca_cert_file`             | caminho para o arquivo do certificado de CA.                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `tls_cert_file`                | caminho para o arquivo do certificado.                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `tls_cipher_suite`             | conjunto de cifras permitido (na notação do OpenSSL).                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `tls_key_file`                 | caminho para o arquivo da chave do certificado.                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `tls_minimum_protocol_version` | A versão mínima do protocolo SSL/TLS. Os valores aceitos são: `ssl2`, `ssl3`, `tls1.0`, `tls1.1`, `tls1.2` (padrão).                                                                                                                                                                                                                                                                                                                                                                           |
| `tls_require_cert`             | Comportamento da verificação de certificados do peer SSL/TLS. Os valores aceitos são: `never`, `allow`, `try`, `demand` (padrão).                                                                                                                                                                                                                                                                                                                                                              |
| `user_dn_detection`            | Seção com parâmetros de LDAP search para detectar o user DN real do usuário autenticado via bind. Isso é usado principalmente em search filters para mapeamento de função adicional quando o servidor é Active Directory. O user DN resultante será usado na substituição de substrings `\{user_dn\}` onde quer que elas sejam permitidas. Por padrão, o user DN é definido como igual ao bind DN, mas, quando a pesquisa é executada, ele é atualizado com o valor real detectado do user DN. |
| `verification_cooldown`        | Período de tempo, em segundos, após uma tentativa de bind bem-sucedida, durante o qual será assumido que o usuário foi autenticado com sucesso em todas as solicitações consecutivas sem contatar o servidor LDAP. Especifique `0` (padrão) para desabilitar o caching e forçar o contato com o servidor LDAP em cada solicitação de autenticação.                                                                                                                                             |

A configuração `user_dn_detection` pode ser definida com sub-tags:

| Setting         | Description                                                                                                                                                                                                                                                                                                                                    |
| --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `base_dn`       | modelo usado para construir o base DN para a LDAP search. O DN resultante será construído substituindo todas as substrings `\{user_name\}` e `\{bind_dn\}` do modelo pelo nome de usuário real e pelo bind DN durante a LDAP search.                                                                                                           |
| `scope`         | escopo da LDAP search. Os valores aceitos são: `base`, `one_level`, `children`, `subtree` (padrão).                                                                                                                                                                                                                                            |
| `search_filter` | modelo usado para construir o search filter para a LDAP search. O filtro resultante será construído substituindo todas as substrings `\{user_name\}`, `\{bind_dn\}` e `\{base_dn\}` do modelo pelo nome de usuário real, bind DN e base DN durante a LDAP search. Observe que os caracteres especiais devem ser escapados corretamente em XML. |

Example:

```xml
<my_ldap_server>
    <host>localhost</host>
    <port>636</port>
    <bind_dn>uid={user_name},ou=users,dc=example,dc=com</bind_dn>
    <verification_cooldown>300</verification_cooldown>
    <enable_tls>yes</enable_tls>
    <tls_minimum_protocol_version>tls1.2</tls_minimum_protocol_version>
    <tls_require_cert>demand</tls_require_cert>
    <tls_cert_file>/path/to/tls_cert_file</tls_cert_file>
    <tls_key_file>/path/to/tls_key_file</tls_key_file>
    <tls_ca_cert_file>/path/to/tls_ca_cert_file</tls_ca_cert_file>
    <tls_ca_cert_dir>/path/to/tls_ca_cert_dir</tls_ca_cert_dir>
    <tls_cipher_suite>ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:AES256-GCM-SHA384</tls_cipher_suite>
</my_ldap_server>
```

Exemplo (Active Directory típico com detecção de user DN configurada para mapeamento de função posterior):

```xml
<my_ad_server>
    <host>localhost</host>
    <port>389</port>
    <bind_dn>EXAMPLE\{user_name}</bind_dn>
    <user_dn_detection>
        <base_dn>CN=Users,DC=example,DC=com</base_dn>
        <search_filter>(&amp;(objectClass=user)(sAMAccountName={user_name}))</search_filter>
    </user_dn_detection>
    <enable_tls>no</enable_tls>
</my_ad_server>
```

<div id="logger">
  ## logger
</div>

A localização e o formato das mensagens de log.

**Chaves**:

| Key                          | Description                                                                                                                                                                                                                                                                                                                                                     |
| ---------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `async`                      | Quando `true` (padrão), o logging acontecerá de forma assíncrona (uma thread em segundo plano por canal de saída). Caso contrário, o log será gravado na thread que chama LOG                                                                                                                                                                                   |
| `async_queue_max_size`       | Ao usar logging assíncrono, a quantidade máxima de mensagens mantidas na fila aguardando flush. Mensagens extras serão descartadas                                                                                                                                                                                                                              |
| `console`                    | Habilita o logging no console. Defina como `1` ou `true` para habilitar. O padrão é `1` se o ClickHouse não estiver em execução no modo daemon; caso contrário, `0`.                                                                                                                                                                                            |
| `console_log_level`          | Nível de log para a saída no console. O padrão é `level`.                                                                                                                                                                                                                                                                                                       |
| `console_shutdown_log_level` | O nível de desligamento é usado para definir o nível de log do console no desligamento do servidor.                                                                                                                                                                                                                                                             |
| `console_startup_log_level`  | O nível de inicialização é usado para definir o nível de log do console na inicialização do servidor. Após a inicialização, o nível de log volta para a configuração `console_log_level`                                                                                                                                                                        |
| `count`                      | Política de rotação: quantos arquivos de log históricos o ClickHouse mantém no máximo.                                                                                                                                                                                                                                                                          |
| `errorlog`                   | O caminho para o arquivo de log de erros.                                                                                                                                                                                                                                                                                                                       |
| `formatting.type`            | Formato de log para a saída no console. Atualmente, apenas `json` é compatível                                                                                                                                                                                                                                                                                  |
| `level`                      | Nível de log. Valores aceitos: `none` (desativa o logging), `fatal`, `critical`, `error`, `warning`, `notice`, `information`,`debug`, `trace`, `test`                                                                                                                                                                                                           |
| `log`                        | O caminho para o arquivo de log.                                                                                                                                                                                                                                                                                                                                |
| `rotation`                   | Política de rotação: controla quando os arquivos de log são rotacionados. A rotação pode ser baseada em tamanho, tempo ou uma combinação de ambos. Exemplos: 100M, daily, 100M,daily. Quando o arquivo de log excede o tamanho especificado ou quando o intervalo de tempo especificado é atingido, ele é renomeado e arquivado, e um novo arquivo de log é criado. |
| `shutdown_level`             | O nível de desligamento é usado para definir o nível do logger raiz no desligamento do servidor.                                                                                                                                                                                                                                                                |
| `size`                       | Política de rotação: tamanho máximo dos arquivos de log em bytes. Quando o tamanho do arquivo de log excede esse limite, ele é renomeado e arquivado, e um novo arquivo de log é criado.                                                                                                                                                                       |
| `startup_level`              | O nível de inicialização é usado para definir o nível do logger raiz na inicialização do servidor. Após a inicialização, o nível de log volta para a configuração `level`                                                                                                                                                                                       |
| `stream_compress`            | Comprime mensagens de log usando LZ4. Defina como `1` ou `true` para habilitar.                                                                                                                                                                                                                                                                                 |
| `syslog_level`               | Nível de log para logging em syslog.                                                                                                                                                                                                                                                                                                                            |
| `use_syslog`                 | Também encaminha a saída de log para o syslog.                                                                                                                                                                                                                                                                                                                  |

**Especificadores de formato de log**

Os nomes de arquivo nos caminhos `log` e `errorLog` aceitam os especificadores de formato abaixo para o nome do arquivo resultante (a parte do diretório não os aceita).

A coluna &quot;Exemplo&quot; mostra a saída em `2023-07-06 18:32:07`.

| Especificador | Descrição                                                                                                                                                                                  | Exemplo                    |
| ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------- |
| `%%`          | % literal                                                                                                                                                                                  | `%`                        |
| `%n`          | Caractere de nova linha                                                                                                                                                                    |                            |
| `%t`          | Caractere de tabulação horizontal                                                                                                                                                          |                            |
| `%Y`          | Ano como número decimal, por exemplo 2017                                                                                                                                                  | `2023`                     |
| `%y`          | Últimos 2 dígitos do ano como número decimal (intervalo [00,99])                                                                                                                           | `23`                       |
| `%C`          | Primeiros 2 dígitos do ano como número decimal (intervalo [00,99])                                                                                                                         | `20`                       |
| `%G`          | [Ano baseado em semanas ISO 8601](https://en.wikipedia.org/wiki/ISO_8601#Week_dates) com quatro dígitos, ou seja, o ano que contém a semana especificada. Normalmente útil apenas com `%V` | `2023`                     |
| `%g`          | Últimos 2 dígitos do [ano baseado em semanas ISO 8601](https://en.wikipedia.org/wiki/ISO_8601#Week_dates), ou seja, o ano que contém a semana especificada.                                | `23`                       |
| `%b`          | Nome abreviado do mês, por exemplo Oct (dependente da localidade)                                                                                                                          | `Jul`                      |
| `%h`          | Sinônimo de %b                                                                                                                                                                             | `Jul`                      |
| `%B`          | Nome completo do mês, por exemplo October (dependente da localidade)                                                                                                                       | `July`                     |
| `%m`          | Mês como número decimal (intervalo [01,12])                                                                                                                                                | `07`                       |
| `%U`          | Número da semana no ano como número decimal (domingo é o primeiro dia da semana) (intervalo [00,53])                                                                                       | `27`                       |
| `%W`          | Número da semana no ano como número decimal (segunda-feira é o primeiro dia da semana) (intervalo [00,53])                                                                                 | `27`                       |
| `%V`          | Número da semana ISO 8601 (intervalo [01,53])                                                                                                                                              | `27`                       |
| `%j`          | Dia do ano como número decimal (intervalo [001,366])                                                                                                                                       | `187`                      |
| `%d`          | Dia do mês como número decimal preenchido com zero à esquerda (intervalo [01,31]). Um único dígito é precedido por zero.                                                                   | `06`                       |
| `%e`          | Dia do mês como número decimal preenchido com espaço à esquerda (intervalo [1,31]). Um único dígito é precedido por um espaço.                                                             | `&nbsp; 6`                 |
| `%a`          | Nome abreviado do dia da semana, por exemplo Fri (dependente da localidade)                                                                                                                | `Thu`                      |
| `%A`          | Nome completo do dia da semana, por exemplo Friday (dependente da localidade)                                                                                                              | `Thursday`                 |
| `%w`          | Dia da semana como número inteiro, com domingo representado por 0 (intervalo [0-6])                                                                                                        | `4`                        |
| `%u`          | Dia da semana como número decimal, em que segunda-feira é 1 (formato ISO 8601) (intervalo [1-7])                                                                                           | `4`                        |
| `%H`          | Hora como número decimal, no formato de 24 horas (intervalo [00-23])                                                                                                                       | `18`                       |
| `%I`          | Hora como número decimal, no formato de 12 horas (intervalo [01,12])                                                                                                                       | `06`                       |
| `%M`          | Minuto como número decimal (intervalo [00,59])                                                                                                                                             | `32`                       |
| `%S`          | Segundo como número decimal (intervalo [00,60])                                                                                                                                            | `07`                       |
| `%c`          | Representação padrão de data e hora, por exemplo Sun Oct 17 04:41:13 2010 (dependente da localidade)                                                                                       | `Thu Jul  6 18:32:07 2023` |
| `%x`          | Representação localizada de data (dependente da localidade)                                                                                                                                | `07/06/23`                 |
| `%X`          | Representação localizada de hora, por exemplo 18:40:20 ou 6:40:20 PM (dependente da localidade)                                                                                            | `18:32:07`                 |
| `%D`          | Data curta no formato MM/DD/YY, equivalente a %m/%d/%y                                                                                                                                     | `07/06/23`                 |
| `%F`          | Data curta no formato AAAA-MM-DD, equivalente a %Y-%m-%d                                                                                                                                   | `2023-07-06`               |
| `%r`          | Hora localizada no formato de 12 horas (dependente da localidade)                                                                                                                          | `06:32:07 PM`              |
| `%R`          | Equivalente a &quot;%H:%M&quot;                                                                                                                                                            | `18:32`                    |
| `%T`          | Equivalente a &quot;%H:%M:%S&quot; (o formato de hora ISO 8601)                                                                                                                            | `18:32:07`                 |
| `%p`          | Indicador localizado de a.m. ou p.m. (dependente da localidade)                                                                                                                            | `PM`                       |
| `%z`          | Deslocamento em relação ao UTC no formato ISO 8601 (por exemplo, -0430), ou nenhum caractere se a informação de fuso horário não estiver disponível                                        | `+0800`                    |
| `%Z`          | Nome ou abreviação do fuso horário, dependente da localidade, ou nenhum caractere se a informação de fuso horário não estiver disponível                                                   | `Z AWST `                  |

**Exemplo**

```xml
<logger>
    <level>trace</level>
    <log>/var/log/clickhouse-server/clickhouse-server-%F-%T.log</log>
    <errorlog>/var/log/clickhouse-server/clickhouse-server-%F-%T.err.log</errorlog>
    <size>1000M</size>
    <count>10</count>
    <stream_compress>true</stream_compress>
</logger>
```

Para imprimir mensagens de log apenas no console:

```xml
<logger>
    <level>information</level>
    <console>true</console>
</logger>
```

**Sobrescritas por nível**

É possível sobrescrever o nível de log de loggers individuais. Por exemplo, para silenciar todas as mensagens dos loggers &quot;Backup&quot; e &quot;RBAC&quot;.

```xml
<logger>
    <levels>
        <logger>
            <name>Backup</name>
            <level>none</level>
        </logger>
        <logger>
            <name>RBAC</name>
            <level>none</level>
        </logger>
    </levels>
</logger>
```

**syslog**

Para também gravar mensagens de log no syslog:

```xml
<logger>
    <use_syslog>1</use_syslog>
    <syslog>
        <address>syslog.remote:10514</address>
        <hostname>myhost.local</hostname>
        <facility>LOG_LOCAL6</facility>
        <format>syslog</format>
    </syslog>
</logger>
```

Chaves para `<syslog>`:

| Key        | Description                                                                                                                                                                                                                                                                                                     |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `address`  | O endereço do syslog no formato `host\[:port\]`. Se omitido, o daemon local será usado.                                                                                                                                                                                                                         |
| `hostname` | O nome do host do qual os logs são enviados (opcional).                                                                                                                                                                                                                                                         |
| `facility` | A [palavra-chave](https://en.wikipedia.org/wiki/Syslog#Facility) de facility do syslog. Deve ser especificada em letras maiúsculas com o prefixo &quot;LOG&#95;&quot;, por exemplo, `LOG_USER`, `LOG_DAEMON`, `LOG_LOCAL3` etc. Padrão: `LOG_USER` se `address` for especificado; caso contrário, `LOG_DAEMON`. |
| `format`   | Formato da mensagem de log. Valores possíveis: `bsd` e `syslog.`                                                                                                                                                                                                                                                |

**Formatos de log**

Você pode especificar o formato de log que será emitido no log do console. Atualmente, apenas JSON é compatível.

**Exemplo**

Aqui está um exemplo de um log JSON gerado:

```json
{
  "date_time_utc": "2024-11-06T09:06:09Z",
  "date_time": "1650918987.180175",
  "thread_name": "#1",
  "thread_id": "254545",
  "level": "Trace",
  "query_id": "",
  "logger_name": "BaseDaemon",
  "message": "Received signal 2",
  "source_file": "../base/daemon/BaseDaemon.cpp; virtual void SignalListener::run()",
  "source_line": "192"
}
```

Para habilitar o suporte a logs em JSON, use o seguinte trecho:

```xml
<logger>
    <formatting>
        <type>json</type>
        <!-- Can be configured on a per-channel basis (log, errorlog, console, syslog), or globally for all channels (then just omit it). -->
        <!-- <channel></channel> -->
        <names>
            <date_time>date_time</date_time>
            <thread_name>thread_name</thread_name>
            <thread_id>thread_id</thread_id>
            <level>level</level>
            <query_id>query_id</query_id>
            <logger_name>logger_name</logger_name>
            <message>message</message>
            <source_file>source_file</source_file>
            <source_line>source_line</source_line>
        </names>
    </formatting>
</logger>
```

**Renomeando chaves em logs JSON**

Os nomes das chaves podem ser alterados modificando os valores das tags dentro da tag `<names>`. Por exemplo, para mudar `DATE_TIME` para `MY_DATE_TIME`, você pode usar `<date_time>MY_DATE_TIME</date_time>`.

**Omitindo chaves em logs JSON**

As propriedades do log podem ser omitidas comentando a propriedade correspondente. Por exemplo, se você não quiser que seu log exiba `query_id`, pode comentar a tag `<query_id>`.

<div id="logger.count">
  ## logger.count
</div>

<SettingsInfoBlock type="UInt64" default_value="1" changeable_without_restart="No" />Política de rotação: número máximo de arquivos de log históricos do ClickHouse mantidos.

<div id="logger.errorlog">
  ## logger.errorlog
</div>

O caminho do arquivo de log de erros.

<div id="logger.formatting.type">
  ## logger.formatting.type
</div>

<SettingsInfoBlock type="String" default_value="json" changeable_without_restart="No" />Formato de log para saída no console. Atualmente, apenas `<json>` é compatível.

<div id="logger.level">
  ## logger.level
</div>

<SettingsInfoBlock type="String" default_value="trace" changeable_without_restart="No" />Nível de log. Valores aceitos: `<none>` (desativa os logs), `<fatal>`, `<critical>`, `<error>`, `<warning>`, `<notice>`, `<information>`, `<debug>`, `<trace>`, `<test>`.

<div id="logger.log">
  ## logger.log
</div>

O caminho do arquivo de log.

<div id="logger.rotation">
  ## logger.rotation
</div>

<SettingsInfoBlock type="String" default_value="100M" changeable_without_restart="No" />Política de rotação: controla quando os arquivos de log são rotacionados. A rotação pode ser baseada em tamanho, tempo ou em uma combinação de ambos. Exemplos: 100M, daily, 100M,daily. Quando o arquivo de log excede o tamanho especificado ou o intervalo de tempo definido é atingido, ele é renomeado e arquivado, e um novo arquivo de log é criado.

<div id="logger.shutdown_level">
  ## logger.shutdown_level
</div>

O nível de desligamento é usado para definir o nível do root logger durante o desligamento do servidor.

<div id="logger.size">
  ## logger.size
</div>

<SettingsInfoBlock type="String" default_value="100M" changeable_without_restart="No" />Política de rotação: tamanho máximo dos arquivos de log em bytes. Quando o tamanho do arquivo de log excede esse limite, ele é renomeado e arquivado, e um novo arquivo de log é criado.

<div id="logger.startup_level">
  ## logger.startup_level
</div>

O nível de inicialização é usado para definir o nível do root logger durante a inicialização do servidor. Após a inicialização, o nível de log volta para a configuração `<level>`.

<div id="logger.stream_compress">
  ## logger.stream_compress
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Compacta as mensagens de log usando LZ4. Defina como `<1>` ou `<true>` para ativar.

<div id="logger.syslog_level">
  ## logger.syslog_level
</div>

<SettingsInfoBlock type="String" default_value="trace" changeable_without_restart="No" />Nível de log para registro no syslog.

<div id="logger.use_syslog">
  ## logger.use_syslog
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Também encaminha a saída de log ao syslog.

<div id="macros">
  ## macros
</div>

Substituições de parâmetros para tabelas replicadas.

Pode ser omitido caso não sejam usadas tabelas replicadas.

Para mais informações, consulte a seção [Criando tabelas replicadas](/pt-BR/reference/engines/table-engines/mergetree-family/replication#creating-replicated-tables).

**Exemplo**

```xml
<macros incl="macros" optional="true" />
```

<div id="message_queue_disable_insertion">
  ## message_queue_disable_insertion
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="Yes" />Desabilita a inserção dos motores de fila de mensagens (Kafka, RabbitMQ, NATS) nas visões materializadas anexadas

<div id="metric_log">
  ## metric_log
</div>

Ele fica desabilitado por padrão.

**Habilitação**

Para ativar manualmente a coleta do histórico de métricas [`system.metric_log`](/pt-BR/reference/system-tables/metric_log), crie `/etc/clickhouse-server/config.d/metric_log.xml` com o seguinte conteúdo:

```xml
<clickhouse>
    <metric_log>
        <database>system</database>
        <table>metric_log</table>
        <flush_interval_milliseconds>7500</flush_interval_milliseconds>
        <collect_interval_milliseconds>1000</collect_interval_milliseconds>
        <max_size_rows>1048576</max_size_rows>
        <reserved_size_rows>8192</reserved_size_rows>
        <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
        <flush_on_crash>false</flush_on_crash>
    </metric_log>
</clickhouse>
```

**Desativação**

Para desativar a configuração `metric_log`, crie o arquivo `/etc/clickhouse-server/config.d/disable_metric_log.xml` com o seguinte conteúdo:

```xml
<clickhouse>
    <metric_log remove="1" />
</clickhouse>
```

<SystemLogParameters />

<div id="mmap_cache_size">
  ## mmap_cache_size
</div>

<SettingsInfoBlock type="UInt64" default_value="1024" changeable_without_restart="Yes" />

Esta configuração permite evitar chamadas frequentes de open/close (que são muito custosas devido aos page faults resultantes) e reutilizar mapeamentos entre várias threads e consultas. O valor da configuração é o número de regiões mapeadas (geralmente igual ao número de arquivos mapeados).

A quantidade de dados em arquivos mapeados pode ser monitorada nas seguintes tabelas do sistema, usando as métricas a seguir:

* `MMappedFiles`/`MMappedFileBytes`/`MMapCacheCells` em [`system.metrics`](/pt-BR/reference/system-tables/metrics), [`system.metric_log`](/pt-BR/reference/system-tables/metric_log)
* `CreatedReadBufferMMap`/`CreatedReadBufferMMapFailed`/`MMappedFileCacheHits`/`MMappedFileCacheMisses` em [`system.events`](/pt-BR/reference/system-tables/events), [`system.processes`](/pt-BR/reference/system-tables/processes), [`system.query_log`](/pt-BR/reference/system-tables/query_log), [`system.query_thread_log`](/pt-BR/reference/system-tables/query_thread_log), [`system.query_views_log`](/pt-BR/reference/system-tables/query_views_log)

<Note>
  A quantidade de dados em arquivos mapeados não consome memória diretamente e não é contabilizada no uso de memória da consulta nem do servidor, porque essa memória pode ser descartada de forma semelhante ao cache de páginas do SO. O cache é descartado (os arquivos são fechados) automaticamente quando partes antigas são removidas em tabelas da família MergeTree; além disso, ele também pode ser descartado manualmente pela consulta `SYSTEM DROP MMAP CACHE`.

  Esta configuração pode ser modificada em tempo de execução e passará a valer imediatamente.
</Note>

<div id="mutation_workload">
  ## mutation_workload
</div>

<SettingsInfoBlock type="String" default_value="default" changeable_without_restart="Yes" />

Usado para controlar como os recursos são utilizados e compartilhados entre mutações e outros workloads. O valor especificado é usado como valor da configuração `workload` para todas as mutações em segundo plano. Pode ser substituído por uma configuração do MergeTree.

**Veja também**

* [Agendamento de workloads](/pt-BR/concepts/features/configuration/server-config/workload-scheduling)

<div id="oom_score">
  ## oom_score
</div>

<SettingsInfoBlock type="Int32" default_value="0" changeable_without_restart="No" />Em sistemas Linux, isso pode controlar o comportamento do OOM killer.

<div id="openssl">
  ## openSSL
</div>

Configuração SSL de cliente e servidor.

O suporte a SSL é fornecido pela biblioteca `libpoco`. As opções de configuração disponíveis são explicadas em [SSLManager.h](https://github.com/ClickHouse-Extras/poco/blob/master/NetSSL_OpenSSL/include/Poco/Net/SSLManager.h). Os valores padrão podem ser encontrados em [SSLManager.cpp](https://github.com/ClickHouse-Extras/poco/blob/master/NetSSL_OpenSSL/src/SSLManager.cpp).

Chaves das configurações de servidor/cliente:

| Opção                         | Descrição                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Valor padrão                                                                               |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| `cacheSessions`               | Ativa ou desativa o cache de sessões. Deve ser usado em conjunto com `sessionIdContext`. Valores aceitos: `true`, `false`.                                                                                                                                                                                                                                                                                                                                                                                                                  | `false`                                                                                    |
| `caConfig`                    | Caminho para o arquivo ou diretório que contém certificados de AC confiáveis. Se apontar para um arquivo, ele deve estar no formato PEM e pode conter vários certificados de AC. Se apontar para um diretório, ele deve conter um arquivo `.pem` por certificado de AC. Os nomes dos arquivos são pesquisados pelo valor de hash do nome do subject da AC. Os detalhes podem ser encontrados na página man de [SSL&#95;CTX&#95;load&#95;verify&#95;locations](https://www.openssl.org/docs/man3.0/man3/SSL_CTX_load_verify_locations.html). |                                                                                            |
| `certificateFile`             | Caminho para o arquivo de certificado de cliente/servidor em formato PEM. Você pode omiti-lo se `privateKeyFile` contiver o certificado.                                                                                                                                                                                                                                                                                                                                                                                                    |                                                                                            |
| `cipherList`                  | Cifras do OpenSSL compatíveis.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `ALL:!ADH:!LOW:!EXP:!MD5:!3DES:@STRENGTH`                                                  |
| `disableProtocols`            | Protocolos que não podem ser usados.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |                                                                                            |
| `extendedVerification`        | Se habilitado, verifica se o CN ou SAN do certificado corresponde ao hostname do peer.                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `false`                                                                                    |
| `fips`                        | Ativa o modo FIPS do OpenSSL. Compatível se a versão do OpenSSL da biblioteca oferecer suporte a FIPS.                                                                                                                                                                                                                                                                                                                                                                                                                                      | `false`                                                                                    |
| `invalidCertificateHandler`   | Classe (uma subclasse de CertificateHandler) para a verificação de certificados inválidos. Por exemplo: `<invalidCertificateHandler> <name>RejectCertificateHandler</name> </invalidCertificateHandler>` .                                                                                                                                                                                                                                                                                                                                  | `RejectCertificateHandler`                                                                 |
| `loadDefaultCAFile`           | Se os certificados de CA integrados do OpenSSL serão usados. O ClickHouse assume que os certificados de CA integrados estejam no arquivo `/etc/ssl/cert.pem` (respectivamente, no diretório `/etc/ssl/certs`) ou no arquivo (respectivamente, diretório) especificado pela variável de ambiente `SSL_CERT_FILE` (respectivamente, `SSL_CERT_DIR`).                                                                                                                                                                                          | `true`                                                                                     |
| `preferServerCiphers`         | Cifras de servidor preferidas pelo cliente.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `false`                                                                                    |
| `privateKeyFile`              | Caminho para o arquivo com a chave privada do certificado PEM. O arquivo pode conter a chave e o certificado ao mesmo tempo.                                                                                                                                                                                                                                                                                                                                                                                                                |                                                                                            |
| `privateKeyPassphraseHandler` | Classe (subclasse de PrivateKeyPassphraseHandler) que solicita a senha para acessar a chave privada. Por exemplo: `<privateKeyPassphraseHandler>`, `<name>KeyFileHandler</name>`, `<options><password>test</password></options>`, `</privateKeyPassphraseHandler>`.                                                                                                                                                                                                                                                                         | `KeyConsoleHandler`                                                                        |
| `requireTLSv1`                | Exige uma conexão TLSv1. Valores aceitáveis: `true`, `false`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `false`                                                                                    |
| `requireTLSv1_1`              | Exige uma conexão TLSv1.1. Valores aceitos: `true`, `false`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `false`                                                                                    |
| `requireTLSv1_2`              | Requer uma conexão TLSv1.2. Valores aceitos: `true`, `false`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `false`                                                                                    |
| `sessionCacheSize`            | O número máximo de sessões armazenadas em cache pelo servidor. Um valor de `0` significa um número ilimitado de sessões.                                                                                                                                                                                                                                                                                                                                                                                                                    | [1024*20](https://github.com/ClickHouse/boringssl/blob/master/include/openssl/ssl.h#L1978) |
| `sessionIdContext`            | Um conjunto único de caracteres aleatórios que o servidor acrescenta a cada identificador gerado. O comprimento da string não deve exceder `SSL_MAX_SSL_SESSION_ID_LENGTH`. Esse parâmetro é sempre recomendado, pois ajuda a evitar problemas tanto se o servidor armazenar a sessão em cache quanto se o cliente solicitar o armazenamento em cache.                                                                                                                                                                                      | `$\{application.name\}`                                                                    |
| `sessionTimeout`              | Tempo de cache da sessão no servidor, em horas.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `2`                                                                                        |
| `verificationDepth`           | O comprimento máximo da cadeia de verificação. A verificação falhará se o comprimento da cadeia de certificados exceder o valor definido.                                                                                                                                                                                                                                                                                                                                                                                                   | `9`                                                                                        |
| `verificationMode`            | O método usado para verificar os certificados do nó. Os detalhes estão na descrição da classe [Context](https://github.com/ClickHouse-Extras/poco/blob/master/NetSSL_OpenSSL/include/Poco/Net/Context.h). Valores possíveis: `none`, `relaxed`, `strict`, `once`.                                                                                                                                                                                                                                                                           | `relaxed`                                                                                  |

**Exemplo de configurações:**

```xml
<openSSL>
    <server>
        <!-- openssl req -subj "/CN=localhost" -new -newkey rsa:2048 -days 365 -nodes -x509 -keyout /etc/clickhouse-server/server.key -out /etc/clickhouse-server/server.crt -->
        <certificateFile>/etc/clickhouse-server/server.crt</certificateFile>
        <privateKeyFile>/etc/clickhouse-server/server.key</privateKeyFile>
        <!-- openssl dhparam -out /etc/clickhouse-server/dhparam.pem 4096 -->
        <dhParamsFile>/etc/clickhouse-server/dhparam.pem</dhParamsFile>
        <verificationMode>none</verificationMode>
        <loadDefaultCAFile>true</loadDefaultCAFile>
        <cacheSessions>true</cacheSessions>
        <disableProtocols>sslv2,sslv3</disableProtocols>
        <preferServerCiphers>true</preferServerCiphers>
    </server>
    <client>
        <loadDefaultCAFile>true</loadDefaultCAFile>
        <cacheSessions>true</cacheSessions>
        <disableProtocols>sslv2,sslv3</disableProtocols>
        <preferServerCiphers>true</preferServerCiphers>
        <!-- Use for self-signed: <verificationMode>none</verificationMode> -->
        <invalidCertificateHandler>
            <!-- Use for self-signed: <name>AcceptCertificateHandler</name> -->
            <name>RejectCertificateHandler</name>
        </invalidCertificateHandler>
    </client>
</openSSL>
```

<div id="openssl.client.caconfig">
  ## openSSL.client.caConfig
</div>

Caminho para o arquivo ou diretório que contém certificados de CA confiáveis. Se apontar para um arquivo, ele deverá estar no formato PEM e poderá conter vários certificados de CA. Se apontar para um diretório, ele deverá conter um arquivo .pem por certificado de CA. Os nomes dos arquivos são buscados com base no valor de hash do nome do assunto da CA. Mais detalhes podem ser encontrados na página de manual de [SSL&#95;CTX&#95;load&#95;verify&#95;locations](https://docs.openssl.org/3.0/man3/SSL_CTX_load_verify_locations/).

<div id="openssl.client.cachesessions">
  ## openSSL.client.cacheSessions
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Ativa ou desativa o cache de sessões. Deve ser usado em conjunto com `<sessionIdContext>`. Valores aceitos: `<true>`, `<false>`.

<div id="openssl.client.certificatefile">
  ## openSSL.client.certificateFile
</div>

Caminho para o arquivo de certificado de cliente/servidor no formato PEM. Você pode omiti-lo se `<privateKeyFile>` contiver o certificado.

<div id="openssl.client.cipherlist">
  ## openSSL.client.cipherList
</div>

<SettingsInfoBlock type="String" default_value="ALL:!ADH:!LOW:!EXP:!MD5:!3DES:@STRENGTH" changeable_without_restart="No" />Cifras OpenSSL suportadas.

<div id="openssl.client.disableprotocols">
  ## openSSL.client.disableProtocols
</div>

Protocolos que não podem ser usados.

<div id="openssl.client.extendedverification">
  ## openSSL.client.extendedVerification
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Se ativado, verifica se o CN ou SAN do certificado corresponde ao hostname do peer.

<div id="openssl.client.fips">
  ## openSSL.client.fips
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Ativa o modo FIPS do OpenSSL. Compatível se a versão do OpenSSL da biblioteca oferecer suporte a FIPS.

<div id="openssl.client.invalidcertificatehandler.name">
  ## openSSL.client.invalidCertificateHandler.name
</div>

<SettingsInfoBlock type="String" default_value="RejectCertificateHandler" changeable_without_restart="No" />Classe (uma subclasse de CertificateHandler) usada para verificar certificados inválidos. Por exemplo: `<<invalidCertificateHandler> <name>RejectCertificateHandler</name> </invalidCertificateHandler>>`.

<div id="openssl.client.loaddefaultcafile">
  ## openSSL.client.loadDefaultCAFile
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />Determina se os certificados de CA internos do OpenSSL serão usados. O ClickHouse pressupõe que os certificados de CA internos estejam no arquivo `</etc/ssl/cert.pem>` (ou, respectivamente, no diretório `</etc/ssl/certs>`) ou no arquivo (ou, respectivamente, no diretório) especificado pela variável de ambiente `<SSL_CERT_FILE>` (ou, respectivamente, `<SSL_CERT_DIR>`).

<div id="openssl.client.preferserverciphers">
  ## openSSL.client.preferServerCiphers
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Cifras de servidor preferidas pelo cliente.

<div id="openssl.client.privatekeyfile">
  ## openSSL.client.privateKeyFile
</div>

Caminho para o arquivo que contém a chave privada do certificado PEM. O arquivo pode conter a chave e o certificado ao mesmo tempo.

<div id="openssl.client.privatekeypassphrasehandler.name">
  ## openSSL.client.privateKeyPassphraseHandler.name
</div>

<SettingsInfoBlock type="String" default_value="KeyConsoleHandler" changeable_without_restart="No" />Classe (subclasse de PrivateKeyPassphraseHandler) que solicita a passphrase para desbloquear a chave privada. Por exemplo: `<<privateKeyPassphraseHandler>>`, `<<name>KeyFileHandler</name>>`, `<<options><password>test</password></options>>`, `<</privateKeyPassphraseHandler>>`

<div id="openssl.client.verificationdepth">
  ## openSSL.client.verificationDepth
</div>

<SettingsInfoBlock type="UInt64" default_value="9" changeable_without_restart="No" />O comprimento máximo da cadeia de verificação. A verificação falhará se o comprimento da cadeia de certificados exceder o valor configurado.

<div id="openssl.client.verificationmode">
  ## openSSL.client.verificationMode
</div>

<SettingsInfoBlock type="String" default_value="relaxed" changeable_without_restart="No" />Método de verificação dos certificados do nó. Os detalhes estão na descrição da classe [Context](https://github.com/ClickHouse/poco/blob/master/NetSSL_OpenSSL/include/Poco/Net/Context.h). Possíveis valores: `<none>`, `<relaxed>`, `<strict>`, `<once>`.

<div id="openssl.server.caconfig">
  ## openSSL.server.caConfig
</div>

Caminho para o arquivo ou diretório que contém certificados de CA confiáveis. Se apontar para um arquivo, ele deve estar no formato PEM e pode conter vários certificados de CA. Se apontar para um diretório, ele deve conter um arquivo .pem por certificado de CA. Os nomes dos arquivos são pesquisados pelo valor de hash do nome do subject da CA. Mais detalhes podem ser encontrados na página de manual de [SSL&#95;CTX&#95;load&#95;verify&#95;locations](https://docs.openssl.org/3.0/man3/SSL_CTX_load_verify_locations/).

<div id="openssl.server.cachesessions">
  ## openSSL.server.cacheSessions
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Ativa ou desativa o cache de sessões. Deve ser usado em conjunto com `<sessionIdContext>`. Valores aceitos: `<true>`, `<false>`.

<div id="openssl.server.certificatefile">
  ## openSSL.server.certificateFile
</div>

Caminho para o arquivo de certificado do cliente/servidor em formato PEM. Você pode omiti-lo se `<privateKeyFile>` contiver o certificado.

<div id="openssl.server.cipherlist">
  ## openSSL.server.cipherList
</div>

<SettingsInfoBlock type="String" default_value="ALL:!ADH:!LOW:!EXP:!MD5:!3DES:@STRENGTH" changeable_without_restart="No" />Cifras OpenSSL suportadas.

<div id="openssl.server.disableprotocols">
  ## openSSL.server.disableProtocols
</div>

Protocolos que não podem ser usados.

<div id="openssl.server.extendedverification">
  ## openSSL.server.extendedVerification
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Quando habilitada, verifica se o CN ou SAN do certificado corresponde ao hostname do peer.

<div id="openssl.server.fips">
  ## openSSL.server.fips
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Ativa o modo FIPS do OpenSSL. Compatível se a versão do OpenSSL da biblioteca oferecer suporte ao FIPS.

<div id="openssl.server.invalidcertificatehandler.name">
  ## openSSL.server.invalidCertificateHandler.name
</div>

<SettingsInfoBlock type="String" default_value="RejectCertificateHandler" changeable_without_restart="No" />Classe (uma subclasse de CertificateHandler) usada para verificar certificados inválidos. Por exemplo: `<<invalidCertificateHandler> <name>RejectCertificateHandler</name> </invalidCertificateHandler>>`.

<div id="openssl.server.loaddefaultcafile">
  ## openSSL.server.loadDefaultCAFile
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />Determina se os certificados de CA internos do OpenSSL serão usados. O ClickHouse pressupõe que os certificados de CA internos estejam no arquivo `</etc/ssl/cert.pem>` (respectivamente, no diretório `</etc/ssl/certs>`) ou no arquivo (respectivamente, diretório) especificado pela variável de ambiente `<SSL_CERT_FILE>` (respectivamente, `<SSL_CERT_DIR>`).

<div id="openssl.server.preferserverciphers">
  ## openSSL.server.preferServerCiphers
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Cifras de servidor preferidas pelo cliente.

<div id="openssl.server.privatekeyfile">
  ## openSSL.server.privateKeyFile
</div>

Caminho para o arquivo que contém a chave privada do certificado PEM. O arquivo pode conter a chave e o certificado ao mesmo tempo.

<div id="openssl.server.privatekeypassphrasehandler.name">
  ## openSSL.server.privateKeyPassphraseHandler.name
</div>

<SettingsInfoBlock type="String" default_value="KeyConsoleHandler" changeable_without_restart="No" />Classe (subclasse de PrivateKeyPassphraseHandler) que solicita a passphrase para acesso à chave privada. Por exemplo: `<<privateKeyPassphraseHandler>>`, `<<name>KeyFileHandler</name>>`, `<<options><password>test</password></options>>`, `<</privateKeyPassphraseHandler>>`

<div id="openssl.server.sessioncachesize">
  ## openSSL.server.sessionCacheSize
</div>

<SettingsInfoBlock type="UInt64" default_value="20480" changeable_without_restart="No" />O número máximo de sessões que o servidor mantém em cache. Um valor de 0 significa sessões ilimitadas.

<div id="openssl.server.sessionidcontext">
  ## openSSL.server.sessionIdContext
</div>

<SettingsInfoBlock type="String" default_value="application.name" changeable_without_restart="No" />Um conjunto único de caracteres aleatórios que o servidor anexa a cada identificador gerado. O tamanho da string não deve exceder `<SSL_MAX_SSL_SESSION_ID_LENGTH>`. Este parâmetro é sempre recomendado, pois ajuda a evitar problemas tanto se o servidor armazena a sessão em cache quanto se o cliente solicitou o uso de cache.

<div id="openssl.server.sessiontimeout">
  ## openSSL.server.sessionTimeout
</div>

<SettingsInfoBlock type="UInt64" default_value="2" changeable_without_restart="No" />Tempo de cache da sessão no servidor, em horas.

<div id="openssl.server.verificationdepth">
  ## openSSL.server.verificationDepth
</div>

<SettingsInfoBlock type="UInt64" default_value="9" changeable_without_restart="No" />O comprimento máximo da cadeia de verificação. A verificação falhará se o comprimento da cadeia de certificados exceder o valor configurado.

<div id="openssl.server.verificationmode">
  ## openSSL.server.verificationMode
</div>

<SettingsInfoBlock type="String" default_value="relaxed" changeable_without_restart="No" />Método de verificação dos certificados do nó. Os detalhes estão na descrição da classe [Context](https://github.com/ClickHouse/poco/blob/master/NetSSL_OpenSSL/include/Poco/Net/Context.h). Valores possíveis: `<none>`, `<relaxed>`, `<strict>`, `<once>`.

<div id="opentelemetry_span_log">
  ## opentelemetry_span_log
</div>

Configurações da tabela de sistema [`opentelemetry_span_log`](/pt-BR/reference/system-tables/opentelemetry_span_log).

<SystemLogParameters />

Exemplo:

```xml
<opentelemetry_span_log>
    <engine>
        engine MergeTree
        partition by toYYYYMM(finish_date)
        order by (finish_date, finish_time_us, trace_id)
    </engine>
    <database>system</database>
    <table>opentelemetry_span_log</table>
    <flush_interval_milliseconds>7500</flush_interval_milliseconds>
    <max_size_rows>1048576</max_size_rows>
    <reserved_size_rows>8192</reserved_size_rows>
    <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
    <flush_on_crash>false</flush_on_crash>
</opentelemetry_span_log>
```

<div id="part_log">
  ## part_log
</div>

Registro de eventos associados ao [MergeTree](/pt-BR/reference/engines/table-engines/mergetree-family/mergetree), como adição ou mesclagem de dados. Você pode usar o log para simular algoritmos de mesclagem e comparar suas características. Também é possível visualizar o processo de mesclagem.

As consultas são registradas na tabela [system.part&#95;log](/pt-BR/reference/system-tables/part_log), não em um arquivo separado. Você pode configurar o nome dessa tabela no parâmetro `table` (veja abaixo).

<SystemLogParameters />

**Exemplo**

```xml
<part_log>
    <database>system</database>
    <table>part_log</table>
    <partition_by>toMonday(event_date)</partition_by>
    <flush_interval_milliseconds>7500</flush_interval_milliseconds>
    <max_size_rows>1048576</max_size_rows>
    <reserved_size_rows>8192</reserved_size_rows>
    <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
    <flush_on_crash>false</flush_on_crash>
</part_log>
```

<div id="path">
  ## path
</div>

<SettingsInfoBlock type="String" default_value="/var/lib/clickhouse/" changeable_without_restart="No" />

O caminho para o diretório que contém os dados.

<Note>
  A barra final é obrigatória.
</Note>

**Exemplo**

```xml
<path>/var/lib/clickhouse/</path>
```

<div id="per_cpu_untracked_memory_thread_buffer">
  ## per_cpu_untracked_memory_thread_buffer
</div>

<SettingsInfoBlock type="UInt64" default_value="32768" changeable_without_restart="Yes" />

Quantidade de memória não rastreada, em bytes, que cada thread pode manter sem acessar o orçamento compartilhado por CPU. Isso amortiza o custo da contabilidade compartilhada por CPU para pequenas alocações e corresponde à folga adicionada a `max_per_cpu_untracked_memory * number_of_cpus` no pior caso. Um valor de `0` remove essa folga: cada alocação atualiza o contador compartilhado por CPU (mais preciso, mas com maior contenção). Somente no Linux.

<div id="point_in_polygon_cache_size">
  ## point_in_polygon_cache_size
</div>

<SettingsInfoBlock type="UInt64" default_value="268435456" changeable_without_restart="Sim" />

Tamanho máximo, em bytes, do cache de polígonos pré-processados usado pela função `pointInPolygon` com um argumento de polígono constante.
As entradas que excederem o limite são removidas em ordem de uso menos recente.
Defini-lo como `0` desabilita o cache: todos os polígonos armazenados em cache são removidos, e cada consulta subsequente pré-processa novamente seu polígono constante.
O cache também pode ser limpo manualmente, sem alterar esse limite, com a consulta [`SYSTEM DROP POINT IN POLYGON CACHE`](/pt-BR/reference/statements/system#drop-point-in-polygon-cache).

<Note>
  Esta configuração pode ser modificada em tempo de execução e terá efeito imediatamente.
</Note>

<div id="prefixes_deserialization_thread_pool_thread_pool_queue_size">
  ## prefixes_deserialization_thread_pool_thread_pool_queue_size
</div>

<SettingsInfoBlock type="UInt64" default_value="10000" changeable_without_restart="Yes" />

O número máximo de tarefas que podem ser agendadas no pool de threads de desserialização de prefixos.

<Note>
  Um valor de `0` significa ilimitado.
</Note>

<div id="prepare_system_log_tables_on_startup">
  ## prepare_system_log_tables_on_startup
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />

Se true, o ClickHouse cria todas as tabelas `system.*_log` configuradas antes da inicialização. Isso pode ser útil se alguns scripts de inicialização dependerem dessas tabelas.

<div id="process_query_plan_packet">
  ## process_query_plan_packet
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />

Esta configuração permite a leitura do pacote QueryPlan. Esse pacote é enviado em consultas distribuídas quando serialize&#95;query&#95;plan está habilitado.
Esse recurso fica desabilitado por padrão para evitar possíveis problemas de segurança causados por bugs na desserialização binária do plano de consulta.

**Exemplo**

```xml
<process_query_plan_packet>true</process_query_plan_packet>
```

<div id="processors_profile_log">
  ## processors_profile_log
</div>

Configurações para a tabela de sistema [`processors_profile_log`](/pt-BR/reference/system-tables/processors_profile_log).

<SystemLogParameters />

As configurações padrão são:

```xml
<processors_profile_log>
    <database>system</database>
    <table>processors_profile_log</table>
    <partition_by>toYYYYMM(event_date)</partition_by>
    <flush_interval_milliseconds>7500</flush_interval_milliseconds>
    <max_size_rows>1048576</max_size_rows>
    <reserved_size_rows>8192</reserved_size_rows>
    <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
    <flush_on_crash>false</flush_on_crash>
</processors_profile_log>
```

<div id="prometheus">
  ## prometheus
</div>

Expondo dados de métricas para coleta pelo [Prometheus](https://prometheus.io).

Configurações:

* `endpoint` – endpoint HTTP para a coleta de métricas pelo servidor prometheus. Comece com &#39;/&#39;.
* `port` – Porta do `endpoint`.
* `metrics` – Expõe métricas da tabela [system.metrics](/pt-BR/reference/system-tables/metrics).
* `events` – Expõe métricas da tabela [system.events](/pt-BR/reference/system-tables/events).
* `asynchronous_metrics` – Expõe os valores atuais das métricas da tabela [system.asynchronous&#95;metrics](/pt-BR/reference/system-tables/asynchronous_metrics).
* `errors` - Expõe o número de erros, por código de erro, ocorridos desde a última reinicialização do servidor. Essas informações também podem ser obtidas em [system.errors](/pt-BR/reference/system-tables/errors).

**Exemplo**

```xml highlight={5-12}
<clickhouse>
    <listen_host>0.0.0.0</listen_host>
    <http_port>8123</http_port>
    <tcp_port>9000</tcp_port>
    <prometheus>
        <endpoint>/metrics</endpoint>
        <port>9363</port>
        <metrics>true</metrics>
        <events>true</events>
        <asynchronous_metrics>true</asynchronous_metrics>
        <errors>true</errors>
    </prometheus>
</clickhouse>
```

Verifique (substitua `127.0.0.1` pelo endereço IP ou hostname do seu servidor ClickHouse):

```bash
curl 127.0.0.1:9363/metrics
```

<div id="prometheus.keeper_metrics_only">
  ## prometheus.keeper_metrics_only
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Expõe as métricas relacionadas ao Keeper

<div id="proxy">
  ## proxy
</div>

Defina servidores proxy para solicitações HTTP e HTTPS, atualmente com suporte em armazenamento S3, funções de tabela do S3 e funções de URL.

Há três maneiras de definir servidores proxy:

* variáveis de ambiente
* listas de proxies
* resolvedores remotos de proxy.

Também há suporte para ignorar servidores proxy para hosts específicos usando `no_proxy`.

**Variáveis de ambiente**

As variáveis de ambiente `http_proxy` e `https_proxy` permitem especificar um
servidor proxy para um determinado protocolo. Se elas estiverem configuradas no seu sistema, deverão funcionar sem problemas.

Essa é a abordagem mais simples se um determinado protocolo tiver
apenas um servidor proxy e esse servidor proxy não mudar.

**Listas de proxy**

Essa abordagem permite especificar um ou mais
servidores proxy para um protocolo. Se mais de um servidor proxy for definido,
o ClickHouse usa os diferentes proxies em round-robin, distribuindo a
carga entre os servidores. Essa é a abordagem mais simples se houver mais de
um servidor proxy para um protocolo e a lista de servidores proxy não mudar.

**Modelo de configuração**

```xml
<proxy>
    <http>
        <uri>http://proxy1</uri>
        <uri>http://proxy2:3128</uri>
    </http>
    <https>
        <uri>http://proxy1:3128</uri>
    </https>
</proxy>
```

Selecione um campo pai nas abas abaixo para ver seus campos filhos:

<Tabs>
  <Tab title="<proxy>">
    | Campo     | Descrição                             |
    | --------- | ------------------------------------- |
    | `<http>`  | Uma lista de um ou mais proxies HTTP  |
    | `<https>` | Uma lista de um ou mais proxies HTTPS |
  </Tab>

  <Tab title="<http> e <https>">
    | Campo   | Descrição      |
    | ------- | -------------- |
    | `<uri>` | A URI do proxy |
  </Tab>
</Tabs>

**Resolvedores remotos de proxy**

Os servidores proxy podem mudar dinamicamente. Nesse
caso, você pode definir o endpoint de um resolvedor. O ClickHouse envia
uma requisição GET vazia para esse endpoint, e o resolvedor remoto deve retornar o host do proxy.
O ClickHouse o usará para formar a URI do proxy usando o seguinte modelo: `\{proxy_scheme\}://\{proxy_host\}:{proxy_port}`

**Modelo de configuração**

```xml
<proxy>
    <http>
        <resolver>
            <endpoint>http://resolver:8080/hostname</endpoint>
            <proxy_scheme>http</proxy_scheme>
            <proxy_port>80</proxy_port>
            <proxy_cache_time>10</proxy_cache_time>
        </resolver>
    </http>

    <https>
        <resolver>
            <endpoint>http://resolver:8080/hostname</endpoint>
            <proxy_scheme>http</proxy_scheme>
            <proxy_port>3128</proxy_port>
            <proxy_cache_time>10</proxy_cache_time>
        </resolver>
    </https>

</proxy>
```

Selecione um campo pai nas abas abaixo para ver os campos filhos correspondentes:

<Tabs>
  <Tab title="<proxy>">
    | Campo     | Descrição                              |
    | --------- | -------------------------------------- |
    | `<http>`  | Uma lista de um ou mais resolvers* |
    | `<https>` | Uma lista de um ou mais resolvers* |
  </Tab>

  <Tab title="<http> e <https>">
    | Campo        | Descrição                                   |
    | ------------ | ------------------------------------------- |
    | `<resolver>` | O endpoint e outros detalhes de um resolver |

    <Note>
      Você pode ter vários elementos `<resolver>`, mas apenas o primeiro
      `<resolver>` de um determinado protocolo é usado. Quaisquer outros elementos `<resolver>`
      desse protocolo são ignorados. Isso significa que o balanceamento de carga
      (se necessário) deve ser implementado pelo resolver remoto.
    </Note>
  </Tab>

  <Tab title="<resolver>">
    | Campo                | Descrição                                                                                                                                                                                                           |
    | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
    | `<endpoint>`         | O URI do resolver de proxy                                                                                                                                                                                          |
    | `<proxy_scheme>`     | O protocolo do URI final do proxy. Pode ser `http` ou `https`.                                                                                                                                                      |
    | `<proxy_port>`       | O número da porta do resolver de proxy                                                                                                                                                                              |
    | `<proxy_cache_time>` | O tempo, em segundos, pelo qual os valores do resolver devem ser armazenados em cache pelo ClickHouse. Definir esse valor como `0` faz com que o ClickHouse contate o resolver para cada solicitação HTTP ou HTTPS. |
  </Tab>
</Tabs>

**Precedência**

As configurações de proxy são determinadas na seguinte ordem:

| Ordem | Configuração                  |
| ----- | ----------------------------- |
| 1.    | Resolvedores remotos de proxy |
| 2.    | Listas de proxy               |
| 3.    | Variáveis de ambiente         |

O ClickHouse verificará o tipo de resolvedor de maior prioridade para o protocolo da solicitação. Se esse tipo não estiver definido,
verificará o próximo tipo de resolvedor de maior prioridade, até chegar ao resolvedor de ambiente.
Isso também permite usar uma combinação de tipos de resolvedor.

<div id="remap_executable">
  ## remap_executable
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />

Configuração que realoca memória de código de máquina (&quot;text&quot;) usando huge pages.

<Note>
  Este recurso é altamente experimental.
</Note>

**Exemplo**

```xml
<remap_executable>false</remap_executable>
```

<div id="replica_group_name">
  ## replica_group_name
</div>

Nome do grupo de réplicas do banco de dados Replicated.

O cluster criado pelo banco de dados Replicated será composto por réplicas no mesmo grupo.
As consultas DDL aguardarão apenas as réplicas do mesmo grupo.

Vazio por padrão.

**Exemplo**

```xml
<replica_group_name>backups</replica_group_name>
```

<div id="replicated_merge_tree">
  ## replicated_merge_tree
</div>

Ajuste fino para tabelas em [ReplicatedMergeTree](/pt-BR/reference/engines/table-engines/mergetree-family/mergetree). Essa configuração tem prioridade mais alta.

Para mais informações, consulte o arquivo de cabeçalho MergeTreeSettings.h.

**Exemplo**

```xml
<replicated_merge_tree>
    <max_suspicious_broken_parts>5</max_suspicious_broken_parts>
</replicated_merge_tree>
```

<div id="restore_threads">
  ## restore_threads
</div>

<SettingsInfoBlock type="NonZeroUInt64" default_value="16" changeable_without_restart="No" />O número máximo de threads para executar requisições RESTORE.

<div id="send_crash_reports">
  ## send_crash_reports
</div>

Configurações para o envio de relatórios de falha para a equipe de desenvolvimento do núcleo do ClickHouse.

Ativá-lo, especialmente em ambientes de pré-produção, é muito bem-vindo.

Chaves:

| Chave                 | Descrição                                                                                                                         |
| --------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| `enabled`             | Flag booleana para habilitar o recurso, `true` por padrão. Defina como `false` para evitar o envio de relatórios de falha.        |
| `endpoint`            | Você pode substituir a URL do endpoint para o envio de relatórios de falha.                                                       |
| `send_logical_errors` | `LOGICAL_ERROR` é como um `assert`; é um bug no ClickHouse. Esta flag booleana habilita o envio dessas exceções (Padrão: `true`). |

**Uso recomendado**

```xml
<send_crash_reports>
    <enabled>true</enabled>
</send_crash_reports>
```

<div id="series_keeper_path">
  ## series_keeper_path
</div>

<SettingsInfoBlock type="String" default_value="/clickhouse/series" changeable_without_restart="No" />

Caminho no Keeper com números autoincrementais, gerados pela função `generateSerialID`. Cada série será um nó nesse caminho.

<div id="ssh_server">
  ## ssh_server
</div>

A parte pública da chave do host será gravada no arquivo known&#95;hosts
no cliente SSH na primeira conexão.

As configurações da chave do host ficam inativas por padrão.
Remova o comentário dessas configurações e forneça o caminho para a respectiva chave SSH para ativá-las:

Exemplo:

```xml
<ssh_server>
    <host_rsa_key>path_to_the_ssh_key</host_rsa_key>
    <host_ecdsa_key>path_to_the_ssh_key</host_ecdsa_key>
    <host_ed25519_key>path_to_the_ssh_key</host_ed25519_key>
</ssh_server>
```

<div id="table_engines_require_grant">
  ## table_engines_require_grant
</div>

Se definido como true, os usuários precisam de um grant para criar uma tabela com um motor específico, por exemplo, `GRANT TABLE ENGINE ON TinyLog to user`.

<Note>
  Por padrão, por compatibilidade retroativa, a criação de uma tabela com um motor de tabela específico ignora o grant; no entanto, você pode alterar esse comportamento definindo esta opção como true.
</Note>

<div id="tcp_ssh_port">
  ## tcp_ssh_port
</div>

Porta do servidor SSH que permite ao usuário se conectar e executar consultas interativamente usando o cliente embutido via PTY.

Exemplo:

```xml
<tcp_ssh_port>9022</tcp_ssh_port>
```

<div id="text_log">
  ## text_log
</div>

Configurações da [tabela de sistema text&#95;log](/pt-BR/reference/system-tables/text_log) para registrar mensagens de texto.

<SystemLogParameters />

Além disso:

| Configuração | Descrição                                                                    | Valor padrão |
| ------------ | ---------------------------------------------------------------------------- | ------------ |
| `level`      | Nível máximo de mensagem (o padrão é `Trace`) que será armazenado na tabela. | `Trace`      |

**Exemplo**

```xml
<clickhouse>
    <text_log>
        <level>notice</level>
        <database>system</database>
        <table>text_log</table>
        <flush_interval_milliseconds>7500</flush_interval_milliseconds>
        <max_size_rows>1048576</max_size_rows>
        <reserved_size_rows>8192</reserved_size_rows>
        <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
        <flush_on_crash>false</flush_on_crash>
        <!-- <partition_by>event_date</partition_by> -->
        <engine>Engine = MergeTree PARTITION BY event_date ORDER BY event_time TTL event_date + INTERVAL 30 day</engine>
    </text_log>
</clickhouse>
```

<div id="thread_pool_queue_size">
  ## thread_pool_queue_size
</div>

<SettingsInfoBlock type="UInt64" default_value="10000" changeable_without_restart="No" />

O número máximo de jobs que podem ser agendados no Global Thread pool. Aumentar o tamanho da fila resulta em maior uso de memória. Recomenda-se manter esse valor igual a [`max_thread_pool_size`](/pt-BR/reference/settings/server-settings/settings/max-thread#max_thread_pool_size).

<Note>
  Um valor de `0` significa ilimitado.
</Note>

**Exemplo**

```xml
<thread_pool_queue_size>12000</thread_pool_queue_size>
```

<div id="throw_on_unknown_workload">
  ## throw_on_unknown_workload
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="Yes" />

Define o comportamento ao acessar um WORKLOAD desconhecido com a configuração de consulta &#39;workload&#39;.

* Se `true`, a exceção RESOURCE&#95;ACCESS&#95;DENIED é lançada quando uma consulta tenta acessar um workload desconhecido. Isso é útil para impor o agendamento de recursos a todas as consultas depois que a hierarquia de WORKLOAD for estabelecida e contiver o WORKLOAD default.
* Se `false` (padrão), uma consulta com a configuração &#39;workload&#39; apontando para um WORKLOAD desconhecido recebe acesso ilimitado, sem agendamento de recursos. Isso é importante durante a configuração da hierarquia de WORKLOAD, antes de o WORKLOAD default ser adicionado.

**Exemplo**

```xml
<throw_on_unknown_workload>true</throw_on_unknown_workload>
```

**Veja também**

* [Agendamento de cargas de trabalho](/pt-BR/concepts/features/configuration/server-config/workload-scheduling)

<div id="timezone">
  ## timezone
</div>

O fuso horário do servidor.

Especificado como um identificador IANA para o fuso horário UTC ou uma localização geográfica (por exemplo, Africa/Abidjan).

O fuso horário é necessário para conversões entre os formatos String e DateTime quando campos DateTime são exibidos em formato de texto (na tela ou em um arquivo) e ao obter um DateTime a partir de uma string. Além disso, o fuso horário é usado em funções que trabalham com data e hora caso não tenham recebido o fuso horário nos parâmetros de entrada.

**Exemplo**

```xml
<timezone>Asia/Istanbul</timezone>
```

**Veja também**

* [session&#95;timezone](/pt-BR/reference/settings/session-settings#session_timezone)

<div id="trace_log">
  ## trace_log
</div>

Configurações para a operação da tabela de sistema [trace&#95;log](/pt-BR/reference/system-tables/trace_log).

<SystemLogParameters />

O arquivo de configuração padrão do servidor `config.xml` contém a seguinte seção de configurações:

```xml
<trace_log>
    <database>system</database>
    <table>trace_log</table>
    <partition_by>toYYYYMM(event_date)</partition_by>
    <flush_interval_milliseconds>7500</flush_interval_milliseconds>
    <max_size_rows>1048576</max_size_rows>
    <reserved_size_rows>8192</reserved_size_rows>
    <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
    <flush_on_crash>false</flush_on_crash>
    <symbolize>false</symbolize>
</trace_log>
```

<div id="url_scheme_mappers">
  ## url_scheme_mappers
</div>

Configuração para converter prefixos de URL abreviados ou simbólicos em URLs completas.

Exemplo:

```xml
<url_scheme_mappers>
    <s3>
        <to>https://{bucket}.s3.amazonaws.com</to>
    </s3>
    <gs>
        <to>https://storage.googleapis.com/{bucket}</to>
    </gs>
    <oss>
        <to>https://{bucket}.oss.aliyuncs.com</to>
    </oss>
</url_scheme_mappers>
```

<div id="validate_tcp_client_information">
  ## validate_tcp_client_information
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Determina se a validação das informações do cliente está habilitada quando um pacote de consulta é recebido.

Por padrão, é `false`:

```xml
<validate_tcp_client_information>false</validate_tcp_client_information>
```

<div id="wait_dictionaries_load_at_startup">
  ## wait_dictionaries_load_at_startup
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />

Esta configuração permite especificar o comportamento quando `dictionaries_lazy_load` é `false`.
(Se `dictionaries_lazy_load` for `true`, esta configuração não tem efeito.)

Se `wait_dictionaries_load_at_startup` for `false`, então o servidor
começará a carregar todos os dicionários na inicialização e receberá conexões em paralelo com esse carregamento.
Quando um dicionário for usado em uma consulta pela primeira vez, a consulta aguardará até que o dicionário seja carregado, caso ainda não tenha sido carregado.
Definir `wait_dictionaries_load_at_startup` como `false` pode fazer o ClickHouse iniciar mais rápido; no entanto, algumas consultas podem ser executadas mais lentamente
(porque terão de esperar que alguns dicionários sejam carregados).

Se `wait_dictionaries_load_at_startup` for `true`, então o servidor aguardará na inicialização
até que todos os dicionários concluam o carregamento (com sucesso ou não) antes de receber qualquer conexão.

**Exemplo**

```xml
<wait_dictionaries_load_at_startup>true</wait_dictionaries_load_at_startup>
```

<div id="webassembly_udf_engine">
  ## webassembly_udf_engine
</div>

<SettingsInfoBlock type="String" default_value="wasmtime" changeable_without_restart="No" />O motor usado para executar UDFs em WebAssembly. Os valores compatíveis são &#39;wasmtime&#39; e &#39;wasmedge&#39;.

<div id="webterminal_allowed_origins">
  ## webterminal_allowed_origins
</div>

Lista separada por vírgulas de origens completas (esquema + host + porta opcional) permitidas para abrir sessões WebSocket de `/webterminal`. Quando vazia, a política de mesma origem é aplicada de forma estrita (Origin deve corresponder ao esquema, host e porta da solicitação). Defina isso para implantações atrás de um reverse proxy com terminação TLS em que `request.isSecure()` é `false`, mesmo que o navegador use `https`. Exemplo: `https://example.com,https://app.example.com:8443`.

{/*AUTOGENERATED_END*/}