---
description: '`val` の最大値に対応する `arg` の値を計算します。'
slug: /sql-reference/aggregate-functions/reference/argmax
title: 'argMax'
doc_type: 'reference'
---

エイリアス: `max_by`

{/*AUTOGENERATED_START*/}

<div id="argMax">
  ## argMax
</div>

導入バージョン: v1.1.0

最大の `val` 値に対応する `arg` 値を計算します。最大値の `val` を持つ行が複数ある場合、対応する `arg` のうちどれが返されるかは決定論的ではありません。
`arg` 側と `max` 側はいずれも [集約関数](/ja/reference/functions/aggregate-functions/index) として動作します。どちらも処理時に [`Null` をスキップ](/ja/reference/functions/aggregate-functions/index#null-processing) し、`Null` でない値が存在する場合は `Null` でない値を返します。

**関連項目**

* [Tuple](/ja/reference/data-types/tuple)

**構文**

```sql
argMax(arg, val)
```

**別名**: `max_by`

**引数**

* `arg` — 最大値を求める対象の引数です。[`const String`](/ja/reference/data-types/string)
* `val` — 最大値です。[`(U)Int8/16/32/64`](/ja/reference/data-types/int-uint) または [`Float*`](/ja/reference/data-types/float) または [`Date`](/ja/reference/data-types/date) または [`DateTime`](/ja/reference/data-types/datetime) または [`Tuple`](/ja/reference/data-types/tuple)

**戻り値**

最大の `val` に対応する `arg` の値を返します。型は `arg` の型と同じです。

**例**

**基本的な使い方**

```sql title=Query
SELECT argMax(user, salary) FROM salary;
```

```response title=Response
┌─argMax(user, salary)─┐
│ director             │
└──────────────────────┘
```

**NULL処理を含む拡張例**

```sql title=Query
CREATE TABLE test
(
    a Nullable(String),
    b Nullable(Int64)
)
ENGINE = Memory AS
SELECT *
FROM VALUES(('a', 1), ('b', 2), ('c', 2), (NULL, 3), (NULL, NULL), ('d', NULL));

SELECT argMax(a, b), max(b) FROM test;
```

```response title=Response
┌─argMax(a, b)─┬─max(b)─┐
│ b            │      3 │
└──────────────┴────────┘
```

**引数でのTupleの使用**

```sql title=Query
SELECT argMax(a, (b,a)) FROM test;
```

```response title=Response
┌─argMax(a, tuple(b, a))─┐
│ c                      │
└────────────────────────┘
```

{/*AUTOGENERATED_END*/}