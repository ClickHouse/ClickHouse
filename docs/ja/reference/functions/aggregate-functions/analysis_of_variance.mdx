---
description: '一元配置分散分析（ANOVA検定）のための統計的検定を提供します。正規分布に従う観測値からなる複数のグループを対象に、すべてのグループの平均が等しいかどうかを調べる検定です。'
slug: /sql-reference/aggregate-functions/reference/analysis_of_variance
title: 'analysisOfVariance'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="analysisOfVariance">
  ## analysisOfVariance
</div>

導入バージョン: v22.10.0

一元配置分散分析 (ANOVA 検定) のための統計的検定です。正規分布に従う観測値からなる複数のグループを対象に、すべてのグループの平均が同一かどうかを調べます。

<Note>
  グループ番号は 0 から始まり、検定を実行するには少なくとも 2 つのグループが必要です。
  また、観測値の数が 1 を超えるグループが少なくとも 1 つ必要です。
</Note>

**構文**

```sql
analysisOfVariance(val, group_no)
```

**別名**: `anova`

**引数**

* `val` — 値。[`(U)Int*`](/ja/reference/data-types/int-uint) または [`Float*`](/ja/reference/data-types/float) または [`Decimal`](/ja/reference/data-types/decimal)
* `group_no` — `val` が属するグループの番号。[`(U)Int*`](/ja/reference/data-types/int-uint) または [`Float*`](/ja/reference/data-types/float) または [`Decimal`](/ja/reference/data-types/decimal)

**戻り値**

F 統計量と p 値を含むタプルを返します。[`Tuple(Float64, Float64)`](/ja/reference/data-types/tuple)

**例**

**基本的な使い方**

```sql title=Query
SELECT analysisOfVariance(number, number % 2) FROM numbers(1048575);
```

```response title=Response
┌─analysisOfVariance(number, modulo(number, 2))─┐
│ (0,1)                                         │
└───────────────────────────────────────────────┘
```

{/*AUTOGENERATED_END*/}