---
description: '`Σ((x - x̅)(y - y̅)) / (n - 1)` の値を算出します'
slug: /sql-reference/aggregate-functions/reference/covarsamp
title: 'covarSamp'
doc_type: 'reference'
---



{/*AUTOGENERATED_START*/}
## covarSamp {#covarSamp}

導入バージョン: v1.1.0

標本共分散を計算します:

$$
\frac{\Sigma{(x - \bar{x})(y - \bar{y})}}{n - 1}
$$

<Note>
この関数は数値的に不安定なアルゴリズムを使用しています。計算に[数値安定性](https://en.wikipedia.org/wiki/Numerical_stability)が必要な場合は、[`covarSampStable`](/reference/functions/aggregate-functions/covarSampStable)関数を使用してください。
処理速度は遅くなりますが、計算誤差を低く抑えられます。
</Note>
    

**Syntax**

```sql
covarSamp(x, y)
```

**エイリアス**: `COVAR_SAMP`

**引数**

- `x` — 第1変数。[`(U)Int*`](/reference/data-types/int-uint) または [`Float*`](/reference/data-types/float) または [`Decimal`](/reference/data-types/decimal)
- `y` — 第2変数。[`(U)Int*`](/reference/data-types/int-uint) または [`Float*`](/reference/data-types/float) または [`Decimal`](/reference/data-types/decimal)

**戻り値**

`x` と `y` の標本共分散を返します。`n <= 1` の場合、`nan` が返されます。[`Float64`](/reference/data-types/float)

**例**

**基本的な標本共分散の計算**

```sql title=Query
DROP TABLE IF EXISTS series;
CREATE TABLE series(i UInt32, x_value Float64, y_value Float64) ENGINE = Memory;
INSERT INTO series(i, x_value, y_value) VALUES (1, 5.6,-4.4),(2, -9.6,3),(3, -1.3,-4),(4, 5.3,9.7),(5, 4.4,0.037),(6, -8.6,-7.8),(7, 5.1,9.3),(8, 7.9,-3.6),(9, -8.2,0.62),(10, -3,7.3);

SELECT covarSamp(x_value, y_value)
FROM series
```

```response title=Response
┌─covarSamp(x_value, y_value)─┐
│           7.206275555555556 │
└─────────────────────────────┘
```

**単一の値はNaNを返す**

```sql title=Query
SELECT covarSamp(x_value, y_value)
FROM series LIMIT 1
```

```response title=Response
┌─covarSamp(x_value, y_value)─┐
│                         nan │
└─────────────────────────────┘
```
{/*AUTOGENERATED_END*/}