---
description: '母集団共分散の値を計算します'
slug: /sql-reference/aggregate-functions/reference/covarpopstable
title: 'covarPopStable'
doc_type: 'reference'
---



{/*AUTOGENERATED_START*/}
## covarPopStable {#covarPopStable}

導入バージョン: v1.1.0

母集団共分散を計算します：

$$
\frac{\Sigma{(x - \bar{x})(y - \bar{y})}}{n}
$$

<br/>

[`covarPop`](/reference/functions/aggregate-functions/covarPop) 関数と似ていますが、数値的に安定したアルゴリズムを使用します。そのため、`covarPopStable` は `covarPop` より処理速度は遅くなりますが、より正確な結果が得られます。
    

**構文**

```sql
covarPopStable(x, y)
```

**引数**

- `x` — 第1変数。[`(U)Int*`](/reference/data-types/int-uint) または [`Float*`](/reference/data-types/float) または [`Decimal`](/reference/data-types/decimal)
- `y` — 第2変数。[`(U)Int*`](/reference/data-types/int-uint) または [`Float*`](/reference/data-types/float) または [`Decimal`](/reference/data-types/decimal)

**戻り値**

`x` と `y` の母集団共分散を返します。[`Float64`](/reference/data-types/float)

**例**

**数値的に安定したアルゴリズムによる基本的な母集団共分散の計算**

```sql title=Query
DROP TABLE IF EXISTS series;
CREATE TABLE series(i UInt32, x_value Float64, y_value Float64) ENGINE = Memory;
INSERT INTO series(i, x_value, y_value) VALUES (1, 5.6,-4.4),(2, -9.6,3),(3, -1.3,-4),(4, 5.3,9.7),(5, 4.4,0.037),(6, -8.6,-7.8),(7, 5.1,9.3),(8, 7.9,-3.6),(9, -8.2,0.62),(10, -3,7.3);

SELECT covarPopStable(x_value, y_value)
FROM series
```

```response title=Response
┌─covarPopStable(x_value, y_value)─┐
│                         6.485648 │
└──────────────────────────────────┘
```
{/*AUTOGENERATED_END*/}