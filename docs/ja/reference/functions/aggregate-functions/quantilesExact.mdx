---
description: '異なるレベルの複数の分位点を、数値データ列に対して同時に正確に計算します。'
slug: /sql-reference/aggregate-functions/reference/quantilesExact
title: 'quantilesExact'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="quantilesExact">
  ## quantilesExact
</div>

導入バージョン: v1.1.0

数値データ列に対して、異なるレベルの複数の[分位点](https://en.wikipedia.org/wiki/Quantile)を同時に正確に計算します。

この関数は [`quantileExact`](/ja/reference/functions/aggregate-functions/quantileExact) と同等ですが、1 回の処理で複数の分位点レベルを計算できるため、個別の分位点関数を呼び出すよりも効率的です。

正確な値を得るため、渡されたすべての値は配列にまとめられ、その後で部分的にソートされます。したがって、この関数は `O(n)` のメモリを消費します。ここで `n` は渡された値の数です。

**構文**

```sql
quantilesExact(level1, level2, ...)(expr)
```

**パラメータ**

* `level` — 分位点のレベル。0 から 1 までの定数の浮動小数点数を 1 つ以上指定します。`level` の値には `[0.01, 0.99]` の範囲を使用することを推奨します。[`Float*`](/ja/reference/data-types/float)

**引数**

* `expr` — カラムの値に対する式で、結果のデータ型が数値データ型、`Date`、`DateTime`、または `DateTime64` となるもの。[`(U)Int*`](/ja/reference/data-types/int-uint) or [`Int128`](/ja/reference/data-types/int-uint) or [`UInt128`](/ja/reference/data-types/int-uint) or [`Int256`](/ja/reference/data-types/int-uint) or [`UInt256`](/ja/reference/data-types/int-uint) or [`Float*`](/ja/reference/data-types/float) or [`Decimal*`](/ja/reference/data-types/decimal) or [`Date`](/ja/reference/data-types/date) or [`DateTime`](/ja/reference/data-types/datetime) or [`DateTime64`](/ja/reference/data-types/datetime64)

**戻り値**

指定したレベルの分位点を、レベルの指定順と同じ順序で格納した Array。数値データ型では、出力フォーマットは入力フォーマットと同じです。[`Array((U)Int*)`](/ja/reference/data-types/array) or [`Array(Int128)`](/ja/reference/data-types/array) or [`Array(UInt128)`](/ja/reference/data-types/array) or [`Array(Int256)`](/ja/reference/data-types/array) or [`Array(UInt256)`](/ja/reference/data-types/array) or [`Array(Float*)`](/ja/reference/data-types/array) or [`Array(Decimal*)`](/ja/reference/data-types/array) or [`Array(Date)`](/ja/reference/data-types/array) or [`Array(DateTime)`](/ja/reference/data-types/array) or [`Array(DateTime64)`](/ja/reference/data-types/array)

**例**

**複数の厳密な分位点を計算する**

```sql title=Query
SELECT quantilesExact(0.25, 0.5, 0.75)(number) FROM numbers(10)
```

```response title=Response
┌─quantilesExact(0.25, 0.5, 0.75)(number)─┐
│ [2,5,7]                                 │
└─────────────────────────────────────────┘
```

{/*AUTOGENERATED_END*/}