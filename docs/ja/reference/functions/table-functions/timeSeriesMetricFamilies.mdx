---
description: 'timeSeriesMetricFamilies は、table engine が TimeSeries engine である table `db_name.time_series_table`
  で使用されるメトリクスファミリーのテーブルを返します。'
sidebarTitle: 'timeSeriesMetricFamilies'
slug: /sql-reference/table-functions/timeSeriesMetricFamilies
title: 'timeSeriesMetricFamilies'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

`timeSeriesMetricFamilies(db_name.time_series_table)` - テーブルエンジンに[TimeSeries](/ja/reference/engines/table-engines/integrations/time-series)エンジンを使用するテーブル`db_name.time_series_table`で使用される[メトリクスファミリー](/ja/reference/engines/table-engines/integrations/time-series#metric-families-table)テーブルを返します。

```sql
CREATE TABLE db_name.time_series_table ENGINE=TimeSeries METRIC FAMILIES metric_families_table
```

この関数は、*メトリクスファミリー* table が内部にある場合も動作します。

```sql
CREATE TABLE db_name.time_series_table ENGINE=TimeSeries METRIC FAMILIES INNER UUID '01234567-89ab-cdef-0123-456789abcdef'
```

以下のクエリは同等です:

```sql
SELECT * FROM timeSeriesMetricFamilies(db_name.time_series_table);
SELECT * FROM timeSeriesMetricFamilies('db_name.time_series_table');
SELECT * FROM timeSeriesMetricFamilies('db_name', 'time_series_table');
```

<Note>
  関数 `timeSeriesMetricFamilies` には、後方互換性のために `timeSeriesMetrics` という別名が維持されています。
</Note>

{/*AUTOGENERATED_END*/}