---
title: 'replicated_fetches_* サーバー設定'
sidebarTitle: 'replicated_fetches_*'
toc_max_heading_level: 2
description: 'replicated_fetches_* 生成グループに属する ClickHouse サーバー設定。'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/ja/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

これらの設定はClickHouse serverを設定するもので、ClickHouseのソースコードから自動生成されています。

<div id="replicated_fetches_http_connection_timeout">
  ## replicated_fetches_http_connection_timeout
</div>

<SettingsInfoBlock type="Seconds" default_value="0" changeable_without_restart="No" />part のフェッチリクエストに対する HTTP 接続タイムアウト。明示的に設定されていない場合は、継承元のデフォルトプロファイル `http_connection_timeout` から継承されます。

<div id="replicated_fetches_http_receive_timeout">
  ## replicated_fetches_http_receive_timeout
</div>

<SettingsInfoBlock type="Seconds" default_value="0" changeable_without_restart="No" />データパート取得リクエストに対する HTTP の受信タイムアウトです。明示的に設定されていない場合は、デフォルトプロファイルの `http_receive_timeout` から継承されます。

<div id="replicated_fetches_http_send_timeout">
  ## replicated_fetches_http_send_timeout
</div>

<SettingsInfoBlock type="Seconds" default_value="0" changeable_without_restart="No" />part のフェッチリクエストの HTTP 送信タイムアウト。明示的に設定されていない場合は、デフォルトプロファイルの `http_send_timeout` を継承します。

{/*AUTOGENERATED_END*/}