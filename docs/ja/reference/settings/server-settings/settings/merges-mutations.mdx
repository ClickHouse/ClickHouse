---
title: 'merges_mutations_* サーバー設定'
sidebarTitle: 'merges_mutations_*'
toc_max_heading_level: 2
description: 'merges_mutations_* 生成グループに属する ClickHouse サーバー設定。'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/ja/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

これらの設定はClickHouseサーバーを設定するもので、ClickHouseのソースコードから自動生成されています。

<div id="merges_mutations_memory_usage_soft_limit">
  ## merges_mutations_memory_usage_soft_limit
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />

マージおよびミューテーション操作の実行に使用できる RAM 量の上限を設定します。
ClickHouse が設定された上限に達すると、新しいバックグラウンドのマージまたはミューテーション操作はスケジュールされなくなりますが、すでにスケジュール済みのタスクは引き続き実行されます。

<Note>
  値 `0` は無制限を意味します。
</Note>

**例**

```xml
<merges_mutations_memory_usage_soft_limit>0</merges_mutations_memory_usage_soft_limit>
```

<div id="merges_mutations_memory_usage_to_ram_ratio">
  ## merges_mutations_memory_usage_to_ram_ratio
</div>

<SettingsInfoBlock type="Double" default_value="0.5" changeable_without_restart="No" />

`merges_mutations_memory_usage_soft_limit` のデフォルト値は、`memory_amount * merges_mutations_memory_usage_to_ram_ratio` で計算されます。

**関連項目:**

* [max&#95;memory&#95;usage](/ja/reference/settings/session-settings#max_memory_usage)
* [merges&#95;mutations&#95;memory&#95;usage&#95;soft&#95;limit](/ja/reference/settings/server-settings/settings/merges-mutations#merges_mutations_memory_usage_soft_limit)

{/*AUTOGENERATED_END*/}