---
title: 'interserver_https_* サーバー設定'
sidebarTitle: 'interserver_https_*'
toc_max_heading_level: 2
description: 'interserver_https_* グループで生成される ClickHouse サーバー設定。'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/ja/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

これらの設定は ClickHouse server を設定するためのものであり、ClickHouse のソースコードから自動生成されています。

<div id="interserver_https_host">
  ## interserver_https_host
</div>

`<interserver_http_host>` と同様ですが、このホスト名は他のサーバーが `<HTTPS>` 経由でこのサーバーにアクセスする際に使用できます。

**例**

```xml
<interserver_https_host>example.clickhouse.com</interserver_https_host>
```

<div id="interserver_https_port">
  ## interserver_https_port
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />

`<HTTPS>` 経由で ClickHouse サーバー間のデータ交換に使用するポート。

**例**

```xml
<interserver_https_port>9010</interserver_https_port>
```

{/*AUTOGENERATED_END*/}