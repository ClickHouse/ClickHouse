---
title: 'max_dictionary_* サーバー設定'
sidebarTitle: 'max_dictionary_*'
toc_max_heading_level: 2
description: 'max_dictionary_* グループに生成される ClickHouse サーバー設定。'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/ja/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

これらの設定は ClickHouse サーバー を構成するためのもので、ClickHouse のソースコードから自動生成されています。

<div id="max_dictionary_num_to_throw">
  ## max_dictionary_num_to_throw
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Yes" />

Dictionary の数がこの値を超えると、サーバー は例外を返します。

データベースエンジンについては、次のものに対する table のみをカウントします。

* Atomic
* Ordinary
* Replicated
* Lazy

<Note>
  `0` は制限なしを意味します。
</Note>

**例**

```xml
<max_dictionary_num_to_throw>400</max_dictionary_num_to_throw>
```

<div id="max_dictionary_num_to_warn">
  ## max_dictionary_num_to_warn
</div>

<SettingsInfoBlock type="UInt64" default_value="1000" changeable_without_restart="Yes" />

アタッチされたDictionaryの数が指定値を超えると、ClickHouseサーバーは `system.warnings` テーブルに警告メッセージを追加します。

**例**

```xml
<max_dictionary_num_to_warn>400</max_dictionary_num_to_warn>
```

{/*AUTOGENERATED_END*/}