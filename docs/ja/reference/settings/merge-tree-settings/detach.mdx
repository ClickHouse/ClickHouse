---
title: 'detach_* MergeTree テーブル設定'
sidebarTitle: 'detach_*'
toc_max_heading_level: 2
description: 'detach_* 生成グループ内の ClickHouse MergeTree テーブル設定。'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/ja/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

これらの設定は [system.merge&#95;tree&#95;settings](/ja/reference/system-tables/merge_tree_settings) で利用でき、ClickHouse のソースコードから自動生成されています。

<div id="detach_not_byte_identical_parts">
  ## detach_not_byte_identical_parts
</div>

<SettingsInfoBlock type="Bool" default_value="0" />

マージまたはミューテーションの後、他のレプリカ上のデータパーツとバイト単位で一致しない場合に、そのレプリカ上のデータパーツをデタッチするかどうかを設定します。無効な場合、そのデータパーツは削除されます。後でこのようなパーツを分析したい場合は、この設定を有効にしてください。

この設定は、[データレプリケーション](/ja/reference/engines/table-engines/mergetree-family/replacingmergetree) が有効な `MergeTree` テーブルに適用されます。

設定可能な値:

* `0` — パーツは削除されます。
* `1` — パーツはデタッチされます。

<div id="detach_old_local_parts_when_cloning_replica">
  ## detach_old_local_parts_when_cloning_replica
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

失われたレプリカを修復する際、古いローカルパーツを削除しません。

設定可能な値:

* `true`
* `false`

{/*AUTOGENERATED_END*/}