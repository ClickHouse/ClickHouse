---
title: 'remote_fs_* MergeTree テーブル設定'
sidebarTitle: 'remote_fs_*'
toc_max_heading_level: 2
description: 'remote_fs_* 生成グループの ClickHouse MergeTree テーブル設定。'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import ExperimentalBadge from "/snippets/ja/components/ExperimentalBadge/ExperimentalBadge.jsx";
import SettingsInfoBlock from "/snippets/ja/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

これらの設定は [system.merge&#95;tree&#95;settings](/ja/reference/system-tables/merge_tree_settings) で参照でき、ClickHouse のソースコードから自動生成されています。

<div id="remote_fs_execute_merges_on_single_replica_time_threshold">
  ## remote_fs_execute_merges_on_single_replica_time_threshold
</div>

<SettingsInfoBlock type="Seconds" default_value="10800" />

この設定の値が 0 より大きい場合、共有ストレージ上にマージ済みの part があると、
1 つのレプリカのみがただちに merge を開始します。

<Note>
  ゼロコピー レプリケーションは production 環境で使用できる段階にはありません。
  ゼロコピー レプリケーションは ClickHouse バージョン 22.8 以降では
  デフォルトで無効になっています。

  この機能を production 環境で使用することは推奨されません。
</Note>

設定可能な値:

* 任意の正の整数。

<div id="remote_fs_zero_copy_path_compatible_mode">
  ## remote_fs_zero_copy_path_compatible_mode
</div>

<ExperimentalBadge />

<SettingsInfoBlock type="Bool" default_value="0" />

変換処理中に、zero-copy を互換モードで実行します。

<div id="remote_fs_zero_copy_zookeeper_path">
  ## remote_fs_zero_copy_zookeeper_path
</div>

<ExperimentalBadge />

<SettingsInfoBlock type="String" default_value="/clickhouse/zero_copy" />

zero-copy のテーブルに依存しない情報のための ZooKeeper パス。

{/*AUTOGENERATED_END*/}