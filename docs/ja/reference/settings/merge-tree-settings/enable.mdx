---
title: 'enable_* MergeTree テーブル設定'
sidebarTitle: 'enable_*'
toc_max_heading_level: 2
description: '自動生成された enable_* グループ内の ClickHouse MergeTree テーブル設定。'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import ExperimentalBadge from "/snippets/ja/components/ExperimentalBadge/ExperimentalBadge.jsx";
import SettingsInfoBlock from "/snippets/ja/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import VersionHistory from "/snippets/ja/components/VersionHistory/VersionHistory.jsx";

これらの設定は [system.merge&#95;tree&#95;settings](/ja/reference/system-tables/merge_tree_settings) で確認でき、ClickHouse のソースコードから自動生成されています。

<div id="enable_index_granularity_compression">
  ## enable_index_granularity_compression
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

可能な場合、索引粒度の値をメモリ内で圧縮します

<div id="enable_max_bytes_limit_for_min_age_to_force_merge">
  ## enable_max_bytes_limit_for_min_age_to_force_merge
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.2"},{"label": "1"},{"label": "デフォルトで、min_age_to_force_merge_seconds を使用する場合でもパートサイズを制限"}]}, {"id": "row-2","items": [{"label": "25.1"},{"label": "0"},{"label": "min_age_to_force_merge の最大バイト数を制限するための設定を追加"}]}, {"id": "row-3","items": [{"label": "25.1"},{"label": "0"},{"label": "新しい設定"}]}]} />

設定 `min_age_to_force_merge_seconds` と
`min_age_to_force_merge_on_partition_only` が、設定
`max_bytes_to_merge_at_max_space_in_pool` に従うかどうか。

設定可能な値:

* `true`
* `false`

<div id="enable_mixed_granularity_parts">
  ## enable_mixed_granularity_parts
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

`index_granularity_bytes` 設定によるグラニュールサイズの制御への移行を有効または無効にします。
バージョン 19.11 より前は、グラニュールサイズを制限する設定は
`index_granularity` のみでした。
`index_granularity_bytes` 設定により、大きな行 (数十〜数百メガバイト) を持つテーブルから
データを取得する際の ClickHouse のパフォーマンスが向上します。
大きな行を持つテーブルがある場合は、そのテーブルでこの設定を有効にすることで、
`SELECT` クエリの効率を向上させることができます。

<div id="enable_replacing_merge_with_cleanup_for_min_age_to_force_merge">
  ## enable_replacing_merge_with_cleanup_for_min_age_to_force_merge
</div>

<ExperimentalBadge />

<SettingsInfoBlock type="Bool" default_value="0" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "25.3"},{"label": "0"},{"label": "ReplacingMergeTree に対する自動クリーンアップマージを有効にする新しい設定"}]}]} />

パーティションを 1 つのパートにマージする際に、ReplacingMergeTree で CLEANUP マージを使用するかどうかを指定します。
この設定を有効にするには、`allow_experimental_replacing_merge_with_cleanup`、
`min_age_to_force_merge_seconds`、`min_age_to_force_merge_on_partition_only`
を有効にしておく必要があります。

設定可能な値:

* `true`
* `false`

<div id="enable_the_endpoint_id_with_zookeeper_name_prefix">
  ## enable_the_endpoint_id_with_zookeeper_name_prefix
</div>

<SettingsInfoBlock type="Bool" default_value="0" />

ReplicatedMergeTree テーブルで、ZooKeeper 名のプレフィックス付き endpoint ID を有効にします。

<div id="enable_vertical_merge_algorithm">
  ## enable_vertical_merge_algorithm
</div>

<SettingsInfoBlock type="UInt64" default_value="1" />

Vertical merge アルゴリズムの使用を有効にします。

{/*AUTOGENERATED_END*/}