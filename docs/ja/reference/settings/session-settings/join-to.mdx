---
title: 'join_to_* セッション設定'
sidebarTitle: 'join_to_*'
toc_max_heading_level: 2
description: 'join_to_* 生成グループ内の ClickHouse セッション設定。'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import ExperimentalBadge from "/snippets/ja/components/ExperimentalBadge/ExperimentalBadge.jsx";
import SettingsInfoBlock from "/snippets/ja/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import VersionHistory from "/snippets/ja/components/VersionHistory/VersionHistory.jsx";

これらの設定は [system.settings](/ja/reference/system-tables/settings) で利用でき、[ソースコード](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp) から自動生成されています。

<div id="join_to_sort_maximum_table_rows">
  ## join_to_sort_maximum_table_rows
</div>

<ExperimentalBadge />

<SettingsInfoBlock type="UInt64" default_value="10000" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "24.9"},{"label": "10000"},{"label": "LEFT JOIN または INNER JOIN で、キーに基づいて右テーブルを再配置するかどうかを判定するための、右テーブルの最大行数"}]}]} />

LEFT JOIN または INNER JOIN で、キーに基づいて右テーブルを再配置するかどうかを判定するための、右テーブルの最大行数。

<div id="join_to_sort_minimum_perkey_rows">
  ## join_to_sort_minimum_perkey_rows
</div>

<ExperimentalBadge />

<SettingsInfoBlock type="UInt64" default_value="40" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "24.9"},{"label": "40"},{"label": "LEFT JOIN または INNER JOIN で、右テーブルをキーで再配置するかどうかを判断するための、キーごとの平均行数の下限。この設定により、スパースなテーブルのキーには最適化が適用されません。"}]}]} />

LEFT JOIN または INNER JOIN で、右テーブルをキーで再配置するかどうかを判断するための、キーごとの平均行数の下限。この設定により、スパースなテーブルのキーには最適化が適用されません。

{/*AUTOGENERATED_END*/}