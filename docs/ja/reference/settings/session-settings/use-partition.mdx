---
title: 'use_partition_* セッション設定'
sidebarTitle: 'use_partition_*'
toc_max_heading_level: 2
description: 'use_partition_* 生成グループの ClickHouse セッション設定。'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/ja/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import VersionHistory from "/snippets/ja/components/VersionHistory/VersionHistory.jsx";

これらの設定は [system.settings](/ja/reference/system-tables/settings) で確認でき、[ソースコード](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp) から自動生成されています。

<div id="use_partition_minmax_for_primary_key_pruning">
  ## use_partition_minmax_for_primary_key_pruning
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.7"},{"label": "1"},{"label": "主キーカラムがパーティションキーの入力カラムでもある場合に、`MergeTree` テーブルの主キー索引解析中にデータパートのパーティション minmax を使用して、より多くのグラニュールをプルーニングするための新しい設定です。"}]}]} />

主キーカラムがパーティションキーの入力カラムでもある場合に、`MergeTree` テーブルの主キー索引解析中に、パーティション minmax 索引の境界値を使用して、より多くのグラニュールをプルーニングします。

たとえば、`ORDER BY (id, event_time)` と `PARTITION BY toYYYYMM(event_time)` を持つ `MergeTree` テーブルでは、ClickHouse は主キー索引解析中に `event_time` のパーティション minmax 索引を使用し、グラニュールをより適切にプルーニングできるようにします。

設定可能な値:

* 0 — 無効。
* 1 — 有効。

<div id="use_partition_pruning">
  ## use_partition_pruning
</div>

**別名**: `use_partition_key`

<SettingsInfoBlock type="Bool" default_value="1" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.3"},{"label": "1"},{"label": "MergeTree がパーティションキーを使用してパーティションをプルーニングするかどうかを制御する新しい設定です。`use_partition_key` はこの設定の別名です。"}]}]} />

MergeTree テーブルのクエリ実行時に、パーティションキーを使用してパーティションをプルーニングします。

設定可能な値:

* 0 — 無効。
* 1 — 有効。

{/*AUTOGENERATED_END*/}