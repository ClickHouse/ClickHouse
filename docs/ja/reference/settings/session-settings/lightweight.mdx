---
title: 'lightweight_* セッション設定'
sidebarTitle: 'lightweight_*'
toc_max_heading_level: 2
description: '自動生成された lightweight_* グループ内の ClickHouse セッション設定。'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/ja/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import VersionHistory from "/snippets/ja/components/VersionHistory/VersionHistory.jsx";

これらの設定は [system.settings](/ja/reference/system-tables/settings) で参照でき、[ソースコード](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp) から自動生成されています。

<div id="lightweight_delete_mode">
  ## lightweight_delete_mode
</div>

<SettingsInfoBlock type="LightweightDeleteMode" default_value="alter_update" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "25.5"},{"label": "alter_update"},{"label": "新しい設定"}]}]} />

論理削除の一部として実行される、内部的な update クエリのモードです。

設定可能な値:

* `alter_update` - 高コストな mutation を作成する `ALTER UPDATE` クエリを実行します。
* `lightweight_update` - 可能であれば論理更新を実行し、できない場合は `ALTER UPDATE` を実行します。
* `lightweight_update_force` - 可能であれば論理更新を実行し、できない場合は例外をスローします。

<div id="lightweight_deletes_sync">
  ## lightweight_deletes_sync
</div>

<SettingsInfoBlock type="UInt64" default_value="2" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "24.4"},{"label": "2"},{"label": "'mutation_sync' と同じですが、論理削除の実行だけを制御します"}]}]} />

[`mutations_sync`](/ja/reference/settings/session-settings/mutations#mutations_sync) と同じですが、論理削除の実行だけを制御します。

設定可能な値:

| 値   | 説明                                                                                                                  |
| --- | ------------------------------------------------------------------------------------------------------------------- |
| `0` | ミューテーションは非同期で実行されます。                                                                                                |
| `1` | クエリは、現在のサーバー上で論理削除が完了するまで待機します。                                                                                     |
| `2` | クエリは、すべてのレプリカ (存在する場合) で論理削除が完了するまで待機します。                                                                           |
| `3` | クエリは、アクティブなレプリカでの完了のみを待機します。`SharedMergeTree` でのみサポートされます。`ReplicatedMergeTree` では `mutations_sync = 2` と同じ動作になります。 |

**関連項目**

* [ALTER クエリの同期性](/ja/reference/statements/alter/index#synchronicity-of-alter-queries)
* [ミューテーション](/ja/reference/statements/alter/index#mutations)

Cloud でのデフォルト値: `1`.

{/*AUTOGENERATED_END*/}