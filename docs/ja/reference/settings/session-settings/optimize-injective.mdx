---
title: 'optimize_injective_* セッション設定'
sidebarTitle: 'optimize_injective_*'
toc_max_heading_level: 2
description: 'optimize_injective_* グループに分類される ClickHouse のセッション設定。'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/ja/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import VersionHistory from "/snippets/ja/components/VersionHistory/VersionHistory.jsx";

これらの設定は [system.settings](/ja/reference/system-tables/settings) で確認でき、[ソースコード](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp) から自動生成されています。

<div id="optimize_injective_functions_in_group_by">
  ## optimize_injective_functions_in_group_by
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "24.1"},{"label": "1"},{"label": "アナライザで、GROUP BY 句内の単射関数をその引数に置き換えます"}]}]} />

GROUP BY 句内の単射関数をその引数に置き換えます

<div id="optimize_injective_functions_in_limit_by">
  ## optimize_injective_functions_in_limit_by
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.6"},{"label": "1"},{"label": "LIMIT BY キー内の単射関数をその引数に置き換える新しい設定。"}]}]} />

LIMIT BY 句のキー内の単射関数をその引数に置き換えます。

例: `LIMIT 5 BY toString(x)` は `LIMIT 5 BY x` になります。

<div id="optimize_injective_functions_inside_uniq">
  ## optimize_injective_functions_inside_uniq
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

uniq*() 関数内の一引数の単射関数を削除します。

{/*AUTOGENERATED_END*/}