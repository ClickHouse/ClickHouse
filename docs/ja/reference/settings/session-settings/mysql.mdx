---
title: 'mysql_* セッション設定'
sidebarTitle: 'mysql_*'
toc_max_heading_level: 2
description: 'mysql_* 生成グループに属する ClickHouse のセッション設定。'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/ja/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import VersionHistory from "/snippets/ja/components/VersionHistory/VersionHistory.jsx";

これらの設定は [system.settings](/ja/reference/system-tables/settings) で確認でき、[ソースコード](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp) から自動生成されています。

<div id="mysql_datatypes_support_level">
  ## mysql_datatypes_support_level
</div>

<SettingsInfoBlock type="MySQLDataTypesSupport" default_value="decimal,datetime64,date2Date32,geometry" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.7"},{"label": "decimal,datetime64,date2Date32,geometry"},{"label": "MySQL の具体的な空間型（LINESTRING、POLYGON、MULTILINESTRING、MULTIPOLYGON）と汎用の GEOMETRY 型を、既定で対応する ClickHouse のジオメトリ型にマッピングします。汎用の GEOMETRY カラムは包括的な Geometry 型にマッピングされます。サブタイプに対応する ClickHouse の型がない値（MULTIPOINT、GEOMETRYCOLLECTION）を読み取ると、読み取り時に例外が発生します。"}]}, {"id": "row-2","items": [{"label": "26.3"},{"label": "decimal,datetime64,date2Date32"},{"label": "既定で最新の MySQL 型マッピングを有効にします。"}]}]} />

MySQL 型を対応する ClickHouse 型にどのように変換するかを定義します。`decimal`、`datetime64`、`date2Date32`、`date2String`、`geometry` を任意に組み合わせ、カンマ区切りで指定します。最新のマッピング (`decimal`、`datetime64`、`date2Date32`、`geometry`) はすべて既定で有効です。

* `decimal`: 精度が許す場合、`NUMERIC` 型と `DECIMAL` 型を `Decimal` に変換します。
* `datetime64`: 精度が `0` でない場合、`DATETIME` 型と `TIMESTAMP` 型を `DateTime` ではなく `DateTime64` に変換します。
* `date2Date32`: `DATE` を `Date` ではなく `Date32` に変換します。`date2String` より優先されます。
* `date2String`: `DATE` を `Date` ではなく `String` に変換します。`datetime64` によって上書きされます。
* `geometry`: MySQL の空間型 (`LINESTRING`、`POLYGON`、`MULTILINESTRING`、`MULTIPOLYGON`) を対応する ClickHouse のジオメトリ型に変換し、汎用の `GEOMETRY` 型を包括的な `Geometry` 型に変換します。`POINT` はこのオプションに関係なく、常に `Point` に変換されます。汎用の `GEOMETRY` カラムは任意のサブタイプを保持できるため、対応する ClickHouse の型がないサブタイプの値 (`MULTIPOINT`、`GEOMETRYCOLLECTION`) を読み取ると、読み取り時に例外が発生します。一方、`MULTIPOINT` または `GEOMETRYCOLLECTION` として宣言されたカラムは `String` にマッピングされます。

<div id="mysql_max_rows_to_insert">
  ## mysql_max_rows_to_insert
</div>

<SettingsInfoBlock type="UInt64" default_value="65536" />

MySQL ストレージエンジンで MySQL バッチ挿入時に指定できる最大行数

{/*AUTOGENERATED_END*/}