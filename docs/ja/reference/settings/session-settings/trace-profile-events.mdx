---
title: 'trace_profile_events_* のセッション設定'
sidebarTitle: 'trace_profile_events_*'
toc_max_heading_level: 2
description: 'trace_profile_events_* 生成グループに含まれる ClickHouse のセッション設定。'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/ja/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import VersionHistory from "/snippets/ja/components/VersionHistory/VersionHistory.jsx";

これらの設定は [system.settings](/ja/reference/system-tables/settings) で利用でき、[ソースコード](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp) から自動生成されています。

<div id="trace_profile_events">
  ## trace_profile_events
</div>

<SettingsInfoBlock type="Bool" default_value="0" />

profile events が更新されるたびに、profile event の名前と増分の値とともに stacktrace を収集し、[trace&#95;log](/ja/reference/system-tables/trace_log) に送信するかどうかを設定します。

設定可能な値:

* 1 — profile events の tracing が有効になります。
* 0 — profile events の tracing は無効です。

<div id="trace_profile_events_list">
  ## trace_profile_events_list
</div>

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.1"},{"label": ""},{"label": "新しい設定"}]}]} />

設定 `trace_profile_events` が有効な場合、トレース対象のイベントを、カンマ区切りで指定した名前のリストに限定します。
`trace_profile_events_list` が空文字列 (デフォルト) の場合は、すべての profile events をトレースします。

値の例: &#39;DiskS3ReadMicroseconds,DiskS3ReadRequestsCount,SelectQueryTimeMicroseconds,ReadBufferFromS3Bytes&#39;

この設定を使用すると、多数のクエリに対するデータをより正確に収集できます。これを使用しない場合、膨大な数のイベントによって内部システムログキューがあふれ、一部が破棄される可能性があるためです。

{/*AUTOGENERATED_END*/}