---
description: 'すべてのフォーマットの一覧と、各フォーマットが入力/出力に適しているかどうか、および並列化をサポートしているかどうかを示すフラグが含まれます。'
keywords: ['システムテーブル', 'フォーマット']
slug: /operations/system-tables/formats
title: 'system.formats'
doc_type: 'リファレンス'
---

<div id="description">
  ## 説明
</div>

すべてのフォーマットの一覧に加え、各フォーマットが入力／出力に適しているかどうか、および並列化をサポートしているかどうかを示すフラグが含まれています。

<div id="columns">
  ## カラム
</div>

{/*AUTOGENERATED_START*/}

* `name` ([String](/ja/reference/data-types/index)) — フォーマット名。
* `is_input` ([UInt8](/ja/reference/data-types/index)) — このフォーマットがデータ入力に適しているかどうかを示すフラグ。
* `is_output` ([UInt8](/ja/reference/data-types/index)) — このフォーマットがデータ出力に適しているかどうかを示すフラグ。
* `supports_parallel_parsing` ([UInt8](/ja/reference/data-types/index)) — このフォーマットが並列パースをサポートしているかどうかを示すフラグ。
* `supports_parallel_formatting` ([UInt8](/ja/reference/data-types/index)) — このフォーマットが並列フォーマット処理をサポートしているかどうかを示すフラグ。
* `is_tty_friendly` ([UInt8](/ja/reference/data-types/index)) — このフォーマットが通常ターミナルで問題なく表示されるかどうかを示すフラグ。その他のフォーマットでは、CLI は出力前に確認を求めます。
* `content_type` ([String](/ja/reference/data-types/index)) — 出力フォーマットに対応する HTTP Content-Type。現在のフォーマット設定に依存する場合があります。
* `supports_random_access` ([UInt8](/ja/reference/data-types/index)) — このフォーマットが入力時のランダムアクセスをサポートしているかどうかを示すフラグ。
* `has_schema_inference` ([UInt8](/ja/reference/data-types/index)) — このフォーマットは、データからスキーマを動的に推論できます (埋め込みヘッダー/メタデータ、またはデータ自体から) 。
* `has_external_schema` ([UInt8](/ja/reference/data-types/index)) — このフォーマットは、固定スキーマを持つか、またはそのフォーマット自体で事前定義されたスキーマを受け入れます。
* `prefers_large_blocks` ([UInt8](/ja/reference/data-types/index)) — このフォーマットは、出力時にはより大きなブロックを書き込み、入力時にはより大きなブロックを生成します。
* `supports_append` ([UInt8](/ja/reference/data-types/index)) — このフォーマットでは、単一のファイルに追記できます。
* `supports_subsets_of_columns` ([UInt8](/ja/reference/data-types/index)) — 入力フォーマットは、特定のカラムが省略されていることを認識できます。
* `description` ([String](/ja/reference/data-types/index)) — フォーマットの概要説明。
* `examples` ([String](/ja/reference/data-types/index)) — 使用例。
* `introduced_in` ([String](/ja/reference/data-types/index)) — そのフォーマットが最初に導入された ClickHouse のバージョン。形式は major.minor です。
* `related` ([Array(String)](/ja/reference/data-types/index)) — 関連するフォーマットの名前。

{/*AUTOGENERATED_END*/}