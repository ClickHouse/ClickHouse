---
description: '構成済みの設定プロファイルのプロパティが含まれるシステムテーブル。'
keywords: ['システムテーブル', 'settings_profiles']
slug: /operations/system-tables/settings_profiles
title: 'system.settings_profiles'
doc_type: 'reference'
---

<div id="description">
  ## 説明
</div>

設定済みの設定プロファイルのプロパティが含まれています。

<div id="columns">
  ## カラム
</div>

{/*AUTOGENERATED_START*/}

* `name` ([String](/ja/reference/data-types/string)) — 設定プロファイルの名前。
* `id` ([UUID](/ja/reference/data-types/uuid)) — 設定プロファイル ID。
* `storage` ([String](/ja/reference/data-types/string)) — 設定プロファイルの保存先へのパス。`access_control_path` パラメータで設定します。
* `num_elements` ([UInt64](/ja/reference/data-types/int-uint)) — `system.settings_profile_elements` テーブル内にあるこのプロファイルの要素数。
* `apply_to_all` ([UInt8](/ja/reference/data-types/int-uint)) — すべてのロールおよび/またはユーザーに設定プロファイルが適用されることを示します。
* `apply_to_list` ([Array(String)](/ja/reference/data-types/array)) — 設定プロファイルを適用するロールおよび/またはユーザーのリスト。
* `apply_to_except` ([Array(String)](/ja/reference/data-types/array)) — リストに含まれるロールおよび/またはユーザーを除くすべてに設定プロファイルが適用されます。

{/*AUTOGENERATED_END*/}

<div id="see-also">
  ## 関連項目
</div>

* [SHOW PROFILES](/ja/reference/statements/show#show-profiles)