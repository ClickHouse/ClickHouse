---
description: 'サーバー設定で定義されたディスクに関する情報を含むシステムテーブル'
keywords: ['システムテーブル', 'disks']
slug: /operations/system-tables/disks
title: 'system.disks'
doc_type: 'reference'
---

import SystemTableCloud from '@site/docs/_snippets/_system_table_cloud.md';

<SystemTableCloud />

<div id="description">
  ## 説明
</div>

[サーバー設定](../../engines/table-engines/mergetree-family/mergetree.md#table_engine-mergetree-multiple-volumes_configure)で定義されているディスクに関する情報が含まれます。

<div id="columns">
  ## カラム
</div>

{/*AUTOGENERATED_START*/ }

* `name` ([String](/ja/sql-reference/data-types/string)) — サーバー設定におけるディスク名。
* `path` ([String](/ja/sql-reference/data-types/string)) — ファイルシステム内のマウントポイントへのパス。
* `free_space` ([UInt64](/ja/sql-reference/data-types/int-uint)) — ディスク上の空き容量 (バイト単位) 。
* `total_space` ([UInt64](/ja/sql-reference/data-types/int-uint)) — ディスクの総容量 (バイト単位) 。
* `unreserved_space` ([UInt64](/ja/sql-reference/data-types/int-uint)) — 予約で確保されていない空き容量 (free&#95;space から、現在実行中のマージ、挿入、およびその他のディスク書き込み操作で確保されている予約サイズを差し引いたもの) 。
* `keep_free_space` ([UInt64](/ja/sql-reference/data-types/int-uint)) — ディスク上で空きのまま維持すべき容量 (バイト単位) 。ディスク設定の keep&#95;free&#95;space&#95;bytes パラメータで定義されます。
* `type` ([String](/ja/sql-reference/data-types/string)) — このディスクがデータを保存する場所を示すディスク種別です。RAM、ローカルドライブ、またはリモートストレージのいずれかです。
* `object_storage_type` ([String](/ja/sql-reference/data-types/string)) — ディスク種別が object&#95;storage の場合のオブジェクトストレージの種別。
* `metadata_type` ([String](/ja/sql-reference/data-types/string)) — ディスク種別が object&#95;storage の場合のメタデータストレージの種別。
* `is_encrypted` ([UInt8](/ja/sql-reference/data-types/int-uint)) — このディスクが基盤となるデータを暗号化するかどうかを示すフラグ。
* `is_read_only` ([UInt8](/ja/sql-reference/data-types/int-uint)) — このディスクでは読み取り操作のみ実行できることを示すフラグ。
* `is_write_once` ([UInt8](/ja/sql-reference/data-types/int-uint)) — ディスクが write-once かどうかを示すフラグです。つまり、このディスクへの BACKUP はサポートされますが、このディスク上の MergeTree table への INSERT はサポートされません。
* `is_remote` ([UInt8](/ja/sql-reference/data-types/int-uint)) — このディスクに対する操作でネットワーク通信が発生するかどうかを示すフラグ。
* `is_broken` ([UInt8](/ja/sql-reference/data-types/int-uint)) — ディスクが破損しているかどうかを示すフラグ。破損したディスクの容量は 0 となり、使用できません。
* `cache_path` ([String](/ja/sql-reference/data-types/string)) — ディスクがキャッシュをサポートしている場合の、ローカルドライブ上のキャッシュディレクトリへのパス。

{/*AUTOGENERATED_END*/ }

<div id="example">
  ## 例
</div>

```sql
SELECT * FROM system.disks;
```

```response
┌─name────┬─path─────────────────┬───free_space─┬──total_space─┬─keep_free_space─┐
│ default │ /var/lib/clickhouse/ │ 276392587264 │ 490652508160 │               0 │
└─────────┴──────────────────────┴──────────────┴──────────────┴─────────────────┘

1 rows in set. Elapsed: 0.001 sec.
```