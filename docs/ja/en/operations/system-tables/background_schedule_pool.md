---
description: 'バックグラウンドスケジュールプール 内のタスクに関する情報を含むシステムテーブル。'
keywords: ['システムテーブル', 'background_schedule_pool']
slug: /operations/system-tables/background_schedule_pool
title: 'system.background_schedule_pool'
doc_type: 'reference'
---

import SystemTableCloud from '@site/docs/_snippets/_system_table_cloud.md';

<div id="systembackground_schedule_pool">
  # system.background_schedule_pool
</div>

<SystemTableCloud />

<div id="description">
  ## 説明
</div>

バックグラウンドスケジュールプール内のタスクに関する情報です。バックグラウンドスケジュールプールは、分散送信、バッファのフラッシュ、メッセージブローカーの処理といった定期的なタスクの実行に使用されます。

<div id="columns">
  ## カラム
</div>

{/*AUTOGENERATED_START*/ }

* `pool` ([String](/ja/sql-reference/data-types/string)) — プール名。設定可能な値: `schedule` — 汎用スケジュールプール、`buffer_flush` — Buffer table データのフラッシュ用プール、`distributed` — 分散テーブルの操作用プール、`message_broker` — メッセージブローカーの操作用プール。
* `database` ([String](/ja/sql-reference/data-types/string)) — データベース名。
* `table` ([String](/ja/sql-reference/data-types/string)) — テーブル名。
* `table_uuid` ([UUID](/ja/sql-reference/data-types/uuid)) — テーブル UUID。
* `query_id` ([String](/ja/sql-reference/data-types/string)) — クエリ ID (現在実行中の場合) 。注意: これは実際のクエリではなく、`system.text_log` 内のログと照合するためにランダムに生成された ID です。
* `elapsed_ms` ([UInt64](/ja/sql-reference/data-types/int-uint)) — タスクの実行時間 (現在実行中の場合) 。
* `log_name` ([String](/ja/sql-reference/data-types/string)) — タスクのログ名。
* `deactivated` ([UInt8](/ja/sql-reference/data-types/int-uint)) — タスクが無効化されているかどうか (無効化されたタスクはプールから削除されるため、常に false) 。
* `scheduled` ([UInt8](/ja/sql-reference/data-types/int-uint)) — タスクが実行対象としてスケジュールされているかどうか。
* `delayed` ([UInt8](/ja/sql-reference/data-types/int-uint)) — タスクが遅延付きでスケジュールされているかどうか。
* `executing` ([UInt8](/ja/sql-reference/data-types/int-uint)) — タスクが現在実行中かどうか。

{/*AUTOGENERATED_END*/ }

<div id="example">
  ## 例
</div>

```sql title="Query"
SELECT * FROM system.background_schedule_pool LIMIT 5 FORMAT Vertical;
```

```text title="Response"
Row 1:
──────
pool:        distributed
database:    default
table:       data
table_uuid:  00000000-0000-0000-0000-000000000000
query_id:
elapsed_ms:  0
log_name:    BackgroundJobsAssignee:DataProcessing
deactivated: 0
scheduled:   1
delayed:     0
executing:   0
```

<div id="see-also">
  ## 関連項目
</div>

* [system.background&#95;schedule&#95;pool&#95;log](background_schedule_pool_log.md) — バックグラウンドスケジュールプール のタスク実行の履歴が記録されています。