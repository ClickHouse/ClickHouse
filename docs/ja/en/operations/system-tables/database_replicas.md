---
description: 'Replicated データベースの情報とステータスを含むシステムテーブル。'
keywords: ['system table', 'database_replicas']
slug: /operations/system-tables/database_replicas
title: 'system.database_replicas'
doc_type: 'reference'
---

<div id="description">
  ## 説明
</div>

各Replicatedデータベースのレプリカに関する情報を含みます。

<div id="columns">
  ## カラム
</div>

{/*AUTOGENERATED_START*/ }

* `database` ([String](/ja/sql-reference/data-types/string)) — Replicated データベースの名前が属するデータベース名。
* `is_readonly` ([UInt8](/ja/sql-reference/data-types/int-uint)) — データベースのレプリカが読み取り専用モードかどうか。
* `max_log_ptr` ([Int32](/ja/sql-reference/data-types/int-uint)) — 一般アクティビティログ内の最大エントリ番号。
* `replica_name` ([String](/ja/sql-reference/data-types/string)) — ClickHouse Keeper 内のレプリカ名。
* `replica_path` ([String](/ja/sql-reference/data-types/string)) — ClickHouse Keeper 内のレプリカデータへのパス。
* `zookeeper_path` ([String](/ja/sql-reference/data-types/string)) — ClickHouse Keeper 内のデータベースデータへのパス。
* `shard_name` ([String](/ja/sql-reference/data-types/string)) — クラスター内の分片名。
* `log_ptr` ([Int32](/ja/sql-reference/data-types/int-uint)) — レプリカが実行キューにコピーした一般アクティビティログ内の最大エントリ番号に 1 を加えた値。
* `total_replicas` ([UInt32](/ja/sql-reference/data-types/int-uint)) — このデータベースで認識されているレプリカの総数。
* `zookeeper_exception` ([String](/ja/sql-reference/data-types/string)) — 最後の例外メッセージ。ClickHouse Keeper から情報を取得する際にエラーが発生した場合に取得されます。
* `is_session_expired` ([UInt8](/ja/sql-reference/data-types/int-uint)) — ClickHouse Keeper とのセッションの有効期限が切れているかどうか。基本的には `is_readonly` と同じです。

{/*AUTOGENERATED_END*/ }

<div id="example">
  ## 例
</div>

```sql
SELECT * FROM system.database_replicas FORMAT Vertical;
```

```text
Row 1:
──────
database:            db_2
is_readonly:         0
max_log_ptr:         2
replica_name:        replica1
replica_path:        /test/db_2/replicas/shard1|replica1
zookeeper_path:      /test/db_2
shard_name:          shard1
log_ptr:             2
total_replicas:      1
zookeeper_exception: 
is_session_expired:  0
```