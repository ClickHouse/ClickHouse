---
title: Template 関数
---



## functionName {#functionname-in-lower-case}

簡単な説明。

**構文**（SELECT を除く）

```sql
<function syntax>
```

エイリアス: `<alias name>`。（任意）

追加テキスト（任意）。

**引数**（任意）

-   `x` — 説明。任意（省略可能な引数のみ）。設定可能な値: <values list>。デフォルト値: <value>。[型名](relative/path/to/type/dscr.md#type)。
-   `y` — 説明。任意（省略可能な引数のみ）。設定可能な値: <values list>。デフォルト値: <value>。[型名](relative/path/to/type/dscr.md#type)。

**パラメータ**（任意、パラメトリック集約関数のみ）

-   `z` — 説明。任意（省略可能なパラメータのみ）。設定可能な値: <values list>。デフォルト値: <value>。[型名](relative/path/to/type/dscr.md#type)。

**戻り値**

-   戻り値の一覧。

型: [型名](relative/path/to/type/dscr.md#type)。

**例**

例では、使用方法やユースケースを示す必要があります。以下のテキストには、例として推奨される構成要素が含まれています。

入力テーブル（任意）:

```text
```

```sql title="Query"
```

```text title="Response"
```

**関連項目**（任意）

-   [リンク](#)