---
description: 'Table système contenant des informations sur les clusters disponibles dans le
  fichier de configuration ainsi que sur les serveurs qui y sont définis.'
keywords: ['table système', 'clusters']
slug: /operations/system-tables/clusters
title: 'system.clusters'
doc_type: 'reference'
---

<div id="description">
  ## Description
</div>

Contient des informations sur les clusters disponibles dans le fichier de configuration et sur les serveurs qu’ils regroupent.

<div id="Columns">
  ## Colonnes
</div>

{/*AUTOGENERATED_START*/ }

* `cluster` ([String](/fr/sql-reference/data-types/string)) — Le nom du cluster.
* `shard_num` ([UInt32](/fr/sql-reference/data-types/int-uint)) — Le numéro du shard dans le cluster, à partir de 1.
* `shard_name` ([String](/fr/sql-reference/data-types/string)) — Le nom du shard dans le cluster.
* `shard_weight` ([UInt32](/fr/sql-reference/data-types/int-uint)) — Le poids relatif du shard lors de l’écriture des données.
* `internal_replication` ([UInt8](/fr/sql-reference/data-types/int-uint)) — Indicateur signalant si cet hôte fait partie d’un ensemble capable de répliquer les données de manière autonome.
* `replica_num` ([UInt32](/fr/sql-reference/data-types/int-uint)) — Le numéro de la réplique dans le shard, à partir de 1.
* `host_name` ([String](/fr/sql-reference/data-types/string)) — Le nom d’hôte, tel qu’indiqué dans la config.
* `host_address` ([String](/fr/sql-reference/data-types/string)) — L’adresse IP de l’hôte obtenue via DNS.
* `port` ([UInt16](/fr/sql-reference/data-types/int-uint)) — Le port à utiliser pour se connecter au serveur.
* `is_local` ([UInt8](/fr/sql-reference/data-types/int-uint)) — Indicateur signalant si l’hôte est local.
* `user` ([String](/fr/sql-reference/data-types/string)) — Le nom de l’utilisateur à utiliser pour se connecter au serveur.
* `default_database` ([String](/fr/sql-reference/data-types/string)) — Le nom de la base de données par défaut.
* `errors_count` ([UInt32](/fr/sql-reference/data-types/int-uint)) — Le nombre de fois où cet hôte n’a pas réussi à joindre la réplique.
* `slowdowns_count` ([UInt32](/fr/sql-reference/data-types/int-uint)) — Le nombre de ralentissements ayant entraîné un changement de réplique lors de l’établissement d’une connexion avec des hedged requests.
* `estimated_recovery_time` ([UInt32](/fr/sql-reference/data-types/int-uint)) — Le nombre de secondes restantes jusqu’à ce que le compteur d’erreurs de la réplique soit remis à zéro et qu’elle soit de nouveau considérée comme normale.
* `database_shard_name` ([String](/fr/sql-reference/data-types/string)) — Le nom du shard de la base de données `Replicated` (pour les clusters appartenant à une base de données `Replicated`).
* `database_replica_name` ([String](/fr/sql-reference/data-types/string)) — Le nom de la réplique de la base de données `Replicated` (pour les clusters appartenant à une base de données `Replicated`).
* `is_shared_catalog_cluster` ([UInt8](/fr/sql-reference/data-types/int-uint)) — Bool indiquant si le cluster appartient au Shared Catalog.
* `is_active` ([Nullable(UInt8)](/fr/sql-reference/data-types/nullable)) — Le statut de la réplique de la base de données `Replicated` (pour les clusters appartenant à une base de données `Replicated`) : 1 signifie &#39;la réplique est en ligne&#39;, 0 signifie &#39;la réplique est hors ligne&#39;, NULL signifie &#39;inconnu&#39;.
* `unsynced_after_recovery` ([Nullable(UInt8)](/fr/sql-reference/data-types/nullable)) — Indique si une réplique de base de données `Replicated` présente un retard de réplication supérieur à max&#95;replication&#95;lag&#95;to&#95;enqueue après la création ou la récupération de la réplique.
* `replication_lag` ([Nullable(UInt32)](/fr/sql-reference/data-types/nullable)) — Le retard de réplication de la réplique de la base de données `Replicated` (pour les clusters appartenant à une base de données `Replicated`).
* `recovery_time` ([Nullable(UInt64)](/fr/sql-reference/data-types/nullable)) — Le temps de récupération de la réplique de la base de données `Replicated` (pour les clusters appartenant à une base de données `Replicated`), en millisecondes.

**Alias :**

* `name` — alias de `cluster`.

{/*AUTOGENERATED_END*/ }

<div id="example">
  ## Exemple
</div>

```sql title="Query"
SELECT * FROM system.clusters LIMIT 2 FORMAT Vertical;
```

```text title="Response"
Row 1:
──────
cluster:                 test_cluster_two_shards
shard_num:               1
shard_name:              shard_01
shard_weight:            1
replica_num:             1
host_name:               127.0.0.1
host_address:            127.0.0.1
port:                    9000
is_local:                1
user:                    default
default_database:
errors_count:            0
slowdowns_count:         0
estimated_recovery_time: 0
database_shard_name:
database_replica_name:
is_active:               NULL

Row 2:
──────
cluster:                 test_cluster_two_shards
shard_num:               2
shard_name:              shard_02
shard_weight:            1
replica_num:             1
host_name:               127.0.0.2
host_address:            127.0.0.2
port:                    9000
is_local:                0
user:                    default
default_database:
errors_count:            0
slowdowns_count:         0
estimated_recovery_time: 0
database_shard_name:
database_replica_name:
is_active:               NULL
```

<div id="see-also">
  ## Voir aussi
</div>

* [Moteur de table Distributed](../../engines/table-engines/special/distributed.md)
* [paramètre distributed&#95;replica&#95;error&#95;cap](../../operations/settings/settings.md#distributed_replica_error_cap)
* [paramètre distributed&#95;replica&#95;error&#95;half&#95;life](../../operations/settings/settings.md#distributed_replica_error_half_life)