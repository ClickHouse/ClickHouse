---
description: 'Table système contenant des informations de profilage au niveau des processeurs
  (figurant dans `EXPLAIN PIPELINE`)'
keywords: ['table système', 'processors_profile_log', 'EXPLAIN PIPELINE']
slug: /operations/system-tables/processors_profile_log
title: 'system.processors_profile_log'
doc_type: 'reference'
---

import SystemTableCloud from '@site/docs/_snippets/_system_table_cloud.md';

<SystemTableCloud />

<div id="description">
  ## Description
</div>

Cette table contient des données de profilage au niveau des processeurs (que vous trouverez dans [`EXPLAIN PIPELINE`](../../sql-reference/statements/explain.md#explain-pipeline)).

<div id="columns">
  ## Colonnes
</div>

{/*AUTOGENERATED_START*/ }

* `hostname` ([LowCardinality(String)](/fr/sql-reference/data-types/lowcardinality)) — Nom d’hôte du serveur qui exécute la requête.
* `event_date` ([Date](/fr/sql-reference/data-types/date)) — La date à laquelle l’événement s’est produit.
* `event_time` ([DateTime](/fr/sql-reference/data-types/datetime)) — La date et l’heure auxquelles l’événement s’est produit.
* `event_time_microseconds` ([DateTime64(6)](/fr/sql-reference/data-types/datetime64)) — La date et l’heure auxquelles l’événement s’est produit, avec une précision à la microseconde.
* `id` ([UInt64](/fr/sql-reference/data-types/int-uint)) — ID du processeur.
* `parent_ids` ([Array(UInt64)](/fr/sql-reference/data-types/array)) — ID des processeurs parents.
* `plan_step` ([UInt64](/fr/sql-reference/data-types/int-uint)) — ID de l’étape du plan de requête qui a créé ce processeur. La valeur est zéro si le processeur n’a été ajouté par aucune étape.
* `plan_step_name` ([String](/fr/sql-reference/data-types/string)) — Nom de l’étape du plan de requête qui a créé ce processeur. La valeur est vide si le processeur n’a été ajouté par aucune étape.
* `plan_step_description` ([String](/fr/sql-reference/data-types/string)) — Description de l’étape du plan de requête qui a créé ce processeur. La valeur est vide si le processeur n’a été ajouté par aucune étape.
* `plan_group` ([UInt64](/fr/sql-reference/data-types/int-uint)) — Groupe du processeur s’il a été créé par une étape du plan de requête. Un groupe est un partitionnement logique des processeurs ajoutés par la même étape du plan de requête. Le groupe sert uniquement à rendre le résultat d’EXPLAIN PIPELINE plus lisible.
* `initial_query_id` ([String](/fr/sql-reference/data-types/string)) — ID de la requête initiale (pour l’exécution distribuée des requêtes).
* `query_id` ([String](/fr/sql-reference/data-types/string)) — ID de la requête.
* `name` ([LowCardinality(String)](/fr/sql-reference/data-types/lowcardinality)) — Nom du processeur.
* `elapsed_us` ([UInt64](/fr/sql-reference/data-types/int-uint)) — Nombre de microsecondes pendant lesquelles ce processeur s’est exécuté.
* `input_wait_elapsed_us` ([UInt64](/fr/sql-reference/data-types/int-uint)) — Nombre de microsecondes pendant lesquelles ce processeur a attendu des données (provenant d’un autre processeur).
* `output_wait_elapsed_us` ([UInt64](/fr/sql-reference/data-types/int-uint)) — Nombre de microsecondes pendant lesquelles ce processeur a attendu parce que le port de sortie était plein.
* `input_rows` ([UInt64](/fr/sql-reference/data-types/int-uint)) — Nombre de lignes consommées par le processeur.
* `input_bytes` ([UInt64](/fr/sql-reference/data-types/int-uint)) — Nombre d’octets consommés par le processeur.
* `output_rows` ([UInt64](/fr/sql-reference/data-types/int-uint)) — Nombre de lignes générées par le processeur.
* `output_bytes` ([UInt64](/fr/sql-reference/data-types/int-uint)) — Nombre d’octets générés par le processeur.
* `processor_uniq_id` ([String](/fr/sql-reference/data-types/string)) — ID unique du processeur dans le pipeline.
* `step_uniq_id` ([String](/fr/sql-reference/data-types/string)) — ID unique de l’étape dans le plan.

{/*AUTOGENERATED_END*/ }

<div id="example">
  ## Exemple
</div>

```sql title="Query"
EXPLAIN PIPELINE
SELECT sleep(1)
┌─explain─────────────────────────┐
│ (Expression)                    │
│ ExpressionTransform             │
│   (SettingQuotaAndLimits)       │
│     (ReadFromStorage)           │
│     SourceFromSingleChunk 0 → 1 │
└─────────────────────────────────┘

SELECT sleep(1)
SETTINGS log_processors_profiles = 1
Query id: feb5ed16-1c24-4227-aa54-78c02b3b27d4
┌─sleep(1)─┐
│        0 │
└──────────┘
1 rows in set. Elapsed: 1.018 sec.

SELECT
    name,
    elapsed_us,
    input_wait_elapsed_us,
    output_wait_elapsed_us
FROM system.processors_profile_log
WHERE query_id = 'feb5ed16-1c24-4227-aa54-78c02b3b27d4'
ORDER BY name ASC
```

```text title="Response"
┌─name────────────────────┬─elapsed_us─┬─input_wait_elapsed_us─┬─output_wait_elapsed_us─┐
│ ExpressionTransform     │    1000497 │                  2823 │                    197 │
│ LazyOutputFormat        │         36 │               1002188 │                      0 │
│ LimitsCheckingTransform │         10 │               1002994 │                    106 │
│ NullSource              │          5 │               1002074 │                      0 │
│ NullSource              │          1 │               1002084 │                      0 │
│ SourceFromSingleChunk   │         45 │                  4736 │                1000819 │
└─────────────────────────┴────────────┴───────────────────────┴────────────────────────┘
```

Ici, on peut voir :

* `ExpressionTransform` exécutait la fonction `sleep(1)`, donc son `work` prendra 1e6, et `elapsed_us` &gt; 1e6.
* `SourceFromSingleChunk` doit attendre, car `ExpressionTransform` n&#39;accepte aucune donnée pendant l&#39;exécution de `sleep(1)` ; il restera donc dans l&#39;état `PortFull` pendant 1e6 us, et `output_wait_elapsed_us` &gt; 1e6.
* `LimitsCheckingTransform`/`NullSource`/`LazyOutputFormat` doivent attendre que `ExpressionTransform` exécute `sleep(1)` pour traiter le résultat, donc `input_wait_elapsed_us` &gt; 1e6.

<div id="see-also">
  ## Voir aussi
</div>

* [`EXPLAIN PIPELINE`](../../sql-reference/statements/explain.md#explain-pipeline)