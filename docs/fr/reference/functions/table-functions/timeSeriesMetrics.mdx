---
description: 'timeSeriesMetrics renvoie la table des métriques utilisée par la table `db_name.time_series_table`
  qui utilise le moteur de table TimeSeries.'
sidebarTitle: 'timeSeriesMetrics'
slug: /sql-reference/table-functions/timeSeriesMetrics
title: 'timeSeriesMetrics'
doc_type: 'référence'
---

{/*AUTOGENERATED_START*/}

`timeSeriesMetrics(db_name.time_series_table)` - Renvoie la [table des métriques](/fr/reference/engines/table-engines/integrations/time-series#metrics-table)
utilisée par la table `db_name.time_series_table` qui utilise le moteur de table [TimeSeries](/fr/reference/engines/table-engines/integrations/time-series) :

```sql
CREATE TABLE db_name.time_series_table ENGINE=TimeSeries METRICS metrics_table
```

La fonction fonctionne également si la table *metrics* est une table interne :

```sql
CREATE TABLE db_name.time_series_table ENGINE=TimeSeries METRICS INNER UUID '01234567-89ab-cdef-0123-456789abcdef'
```

Les requêtes suivantes sont équivalentes :

```sql
SELECT * FROM timeSeriesMetrics(db_name.time_series_table);
SELECT * FROM timeSeriesMetrics('db_name.time_series_table');
SELECT * FROM timeSeriesMetrics('db_name', 'time_series_table');
```

{/*AUTOGENERATED_END*/}