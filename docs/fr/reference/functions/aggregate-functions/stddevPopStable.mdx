---
description: "Le résultat est égal à la racine carrée de varPop. Contrairement à stddevPop,\n  cette fonction utilise un algorithme numériquement stable."
slug: /sql-reference/aggregate-functions/reference/stddevpopstable
title: 'stddevPopStable'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="stddevPopStable">
  ## stddevPopStable
</div>

Introduit dans : v1.1.0

Le résultat correspond à la racine carrée de [varPop](/fr/reference/functions/aggregate-functions/varPop). Contrairement à [stddevPop](/fr/reference/functions/aggregate-functions/stddevPop), cette fonction utilise un algorithme numériquement stable. Elle est plus lente, mais produit une erreur de calcul plus faible.

**Syntaxe**

```sql
stddevPopStable(x)
```

**Arguments**

* `x` — Ensemble de valeurs dont on cherche à calculer l&#39;écart type. [`(U)Int*`](/fr/reference/data-types/int-uint) ou [`Float*`](/fr/reference/data-types/float) ou [`Decimal*`](/fr/reference/data-types/decimal)

**Valeur renvoyée**

Renvoie la racine carrée de la variance de `x`. [`Float64`](/fr/reference/data-types/float)

**Exemples**

**Utilisation de base**

```sql title=Query
DROP TABLE IF EXISTS test_data;
CREATE TABLE test_data
(
    population Float64,
)
ENGINE = Log;

INSERT INTO test_data SELECT randUniform(5.5, 10) FROM numbers(1000000);

SELECT
    stddevPopStable(population) AS stddev
FROM test_data;
```

```response title=Response
┌─────────────stddev─┐
│ 1.2999977786592576 │
└────────────────────┘
```

{/*AUTOGENERATED_END*/}