---
description: "Le résultat est égal à la racine carrée de varSamp. Contrairement à stddevSamp, cette fonction utilise un algorithme numériquement stable."
slug: /sql-reference/aggregate-functions/reference/stddevsampstable
title: 'stddevSampStable'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="stddevSampStable">
  ## stddevSampStable
</div>

Introduit dans : v1.1.0

Le résultat est égal à la racine carrée de [varSamp](/fr/reference/functions/aggregate-functions/varSamp). Contrairement à [stddevSamp](/fr/reference/functions/aggregate-functions/stddevSamp), cette fonction utilise un algorithme numériquement stable. Elle est plus lente, mais produit une erreur de calcul plus faible.

**Syntaxe**

```sql
stddevSampStable(x)
```

**Arguments**

* `x` — Valeurs dont on veut calculer la racine carrée de la variance de l’échantillon. [`(U)Int*`](/fr/reference/data-types/int-uint) ou [`Float*`](/fr/reference/data-types/float) ou [`Decimal*`](/fr/reference/data-types/decimal)

**Valeur renvoyée**

Renvoie la racine carrée de la variance de l’échantillon pour `x`. [`Float64`](/fr/reference/data-types/float)

**Exemples**

**Utilisation de base**

```sql title=Query
DROP TABLE IF EXISTS test_data;
CREATE TABLE test_data
(
    population UInt8,
)
ENGINE = Log;

INSERT INTO test_data VALUES (3),(3),(3),(4),(4),(5),(5),(7),(11),(15);

SELECT
    stddevSampStable(population)
FROM test_data;
```

```response title=Response
┌─stddevSampStable(population)─┐
│                            4 │
└──────────────────────────────┘
```

{/*AUTOGENERATED_END*/}