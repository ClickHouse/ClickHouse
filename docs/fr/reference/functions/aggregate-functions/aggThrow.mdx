---
description: 'Cette fonction peut être utilisée pour tester la sécurité vis-à-vis des exceptions.
  Elle lèvera une exception à sa création avec la probabilité spécifiée.'
slug: /sql-reference/aggregate-functions/reference/aggthrow
title: 'aggThrow'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="aggThrow">
  ## aggThrow
</div>

Introduit dans : v20.1.0

Cette fonction peut être utilisée pour tester la sécurité vis-à-vis des exceptions. Elle lève une exception lors de sa création avec la probabilité spécifiée.

**Syntaxe**

```sql
aggThrow(throw_prob)
```

**Arguments**

* `throw_prob` — Probabilité qu&#39;une exception soit levée lors de la création. [`Float64`](/fr/reference/data-types/float)

**Valeur renvoyée**

Renvoie une exception : `Code: 503. DB::Exception: Aggregate function aggThrow has thrown exception successfully`.

**Exemples**

**Exemple d&#39;utilisation**

```sql title=Query
SELECT number % 2 AS even, aggThrow(number) FROM numbers(10) GROUP BY even;
```

```response title=Response
Received exception:
Code: 503. DB::Exception: Aggregate function aggThrow has thrown exception successfully: While executing AggregatingTransform. (AGGREGATE_FUNCTION_THROW)
```

{/*AUTOGENERATED_END*/}