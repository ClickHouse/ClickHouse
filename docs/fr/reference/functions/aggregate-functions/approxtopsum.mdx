---
description: 'Renvoie un tableau des valeurs les plus fréquentes estimées, ainsi que de leur
  nombre d''occurrences, dans la colonne spécifiée.'
slug: /sql-reference/aggregate-functions/reference/approxtopsum
title: 'approx_top_sum'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="approx_top_sum">
  ## approx_top_sum
</div>

Introduit dans : v1.1.0

Renvoie un tableau des valeurs estimées comme étant les plus fréquentes, ainsi que de leur nombre d’occurrences, dans la colonne spécifiée.
Le tableau résultant est trié par ordre décroissant de fréquence approximative des valeurs (et non des valeurs elles-mêmes).
De plus, le poids de la valeur est pris en compte.

Cette fonction ne garantit pas le résultat.
Dans certaines situations, des erreurs peuvent se produire et elle peut renvoyer des valeurs fréquentes qui ne sont pas les plus fréquentes.

**Voir aussi**

* [topK](/fr/reference/functions/aggregate-functions/topK)
* [topKWeighted](/fr/reference/functions/aggregate-functions/topKWeighted)
* [approx&#95;top&#95;k](/fr/reference/functions/aggregate-functions/approxtopk)

**Syntaxe**

```sql
approx_top_sum(N[, reserved])(column, weight)
```

**Paramètres**

* `N` — Le nombre d’éléments à renvoyer. Facultatif. Valeur par défaut : 10. [`UInt64`](/fr/reference/data-types/int-uint)
* `reserved` — Facultatif. Définit le nombre de cellules réservées aux valeurs. Si `uniq(column) > reserved`, le résultat de la fonction topK sera approximatif. Valeur par défaut : `N * 3`. Valeur maximale : `N = 65536`. [`UInt64`](/fr/reference/data-types/int-uint)

**Arguments**

* `column` — Le nom de la colonne pour laquelle trouver les valeurs les plus fréquentes. [`String`](/fr/reference/data-types/string)
* `weight` — Le poids. Chaque valeur est comptabilisée `weight` fois pour le calcul de la fréquence. [`UInt64`](/fr/reference/data-types/int-uint)

**Valeur renvoyée**

Renvoie un tableau des valeurs approximativement les plus fréquentes et de leur nombre d’occurrences, triés par ordre décroissant de fréquence approximative. [`Array`](/fr/reference/data-types/array)

**Exemples**

**Exemple d’utilisation**

```sql title=Query
SELECT approx_top_sum(2)(k, w)
FROM VALUES('k Char, w UInt64', ('y', 1), ('y', 1), ('x', 5), ('y', 1), ('z', 10));
```

```response title=Response
┌─approx_top_sum(2)(k, w)─┐
│ [('z',10,0),('x',5,0)]  │
└─────────────────────────┘
```

{/*AUTOGENERATED_END*/}

**Voir aussi**

* [topK](/fr/reference/functions/aggregate-functions/topK)
* [topKWeighted](/fr/reference/functions/aggregate-functions/topKWeighted)
* [approx&#95;top&#95;k](/fr/reference/functions/aggregate-functions/approxtopk)