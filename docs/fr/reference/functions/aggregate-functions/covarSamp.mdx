---
description: 'Calcule la valeur de `Σ((x - x̅)(y - y̅)) / (n - 1)`'
slug: /sql-reference/aggregate-functions/reference/covarsamp
title: 'covarSamp'
doc_type: 'reference'
---



{/*AUTOGENERATED_START*/}
## covarSamp {#covarSamp}

Introduit dans : v1.1.0

Calcule la covariance d'échantillon :

$$
\frac{\Sigma{(x - \bar{x})(y - \bar{y})}}{n - 1}
$$

<Note>
Cette fonction utilise un algorithme numériquement instable. Si vous avez besoin de [stabilité numérique](https://en.wikipedia.org/wiki/Numerical_stability) dans vos calculs, utilisez la fonction [`covarSampStable`](/reference/functions/aggregate-functions/covarSampStable).
Elle est plus lente, mais offre une erreur de calcul plus faible.
</Note>
    

**Syntaxe**

```sql
covarSamp(x, y)
```

**Alias** : `COVAR_SAMP`

**Arguments**

- `x` — Première variable. [`(U)Int*`](/reference/data-types/int-uint) ou [`Float*`](/reference/data-types/float) ou [`Decimal`](/reference/data-types/decimal)
- `y` — Deuxième variable. [`(U)Int*`](/reference/data-types/int-uint) ou [`Float*`](/reference/data-types/float) ou [`Decimal`](/reference/data-types/decimal)

**Valeur renvoyée**

Renvoie la covariance d'échantillon entre `x` et `y`. Pour `n <= 1`, `nan` est renvoyé. [`Float64`](/reference/data-types/float)

**Exemples**

**Calcul de base de la covariance d'échantillon**

```sql title=Query
DROP TABLE IF EXISTS series;
CREATE TABLE series(i UInt32, x_value Float64, y_value Float64) ENGINE = Memory;
INSERT INTO series(i, x_value, y_value) VALUES (1, 5.6,-4.4),(2, -9.6,3),(3, -1.3,-4),(4, 5.3,9.7),(5, 4.4,0.037),(6, -8.6,-7.8),(7, 5.1,9.3),(8, 7.9,-3.6),(9, -8.2,0.62),(10, -3,7.3);

SELECT covarSamp(x_value, y_value)
FROM series
```

```response title=Response
┌─covarSamp(x_value, y_value)─┐
│           7.206275555555556 │
└─────────────────────────────┘
```

**Une valeur unique renvoie NaN**

```sql title=Query
SELECT covarSamp(x_value, y_value)
FROM series LIMIT 1
```

```response title=Response
┌─covarSamp(x_value, y_value)─┐
│                         nan │
└─────────────────────────────┘
```

{/*AUTOGENERATED_END*/}