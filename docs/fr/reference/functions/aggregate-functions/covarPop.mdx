---
description: 'Calcule la covariance de la population'
slug: /sql-reference/aggregate-functions/reference/covarpop
title: 'covarPop'
doc_type: 'reference'
---



{/*AUTOGENERATED_START*/}
## covarPop {#covarPop}

Introduit dans : v1.1.0

Calcule la covariance de la population :

$$
\frac{\Sigma{(x - \bar{x})(y - \bar{y})}}{n}
$$

<br/>

<Note>
Cette fonction utilise un algorithme numériquement instable. Si vous avez besoin de [stabilité numérique](https://en.wikipedia.org/wiki/Numerical_stability) dans vos calculs, utilisez la fonction [`covarPopStable`](/reference/functions/aggregate-functions/covarPopStable). Elle est plus lente, mais produit une erreur de calcul plus faible.
</Note>
    

**Syntaxe**

```sql
covarPop(x, y)
```

**Alias** : `COVAR_POP`

**Arguments**

- `x` — Première variable. [`(U)Int*`](/reference/data-types/int-uint) ou [`Float*`](/reference/data-types/float) ou [`Decimal`](/reference/data-types/decimal)
- `y` — Deuxième variable. [`(U)Int*`](/reference/data-types/int-uint) ou [`Float*`](/reference/data-types/float) ou [`Decimal`](/reference/data-types/decimal)

**Valeur renvoyée**

Renvoie la covariance de la population entre `x` et `y`. [`Float64`](/reference/data-types/float)

**Exemples**

**Calcul de base de la covariance de la population**

```sql title=Query
DROP TABLE IF EXISTS series;
CREATE TABLE series(i UInt32, x_value Float64, y_value Float64) ENGINE = Memory;
INSERT INTO series(i, x_value, y_value) VALUES (1, 5.6, -4.4),(2, -9.6, 3),(3, -1.3, -4),(4, 5.3, 9.7),(5, 4.4, 0.037),(6, -8.6, -7.8),(7, 5.1, 9.3),(8, 7.9, -3.6),(9, -8.2, 0.62),(10, -3, 7.3);

SELECT covarPop(x_value, y_value)
FROM series
```

```response title=Response
┌─covarPop(x_value, y_value)─┐
│                   6.485648 │
└────────────────────────────┘
```
{/*AUTOGENERATED_END*/}