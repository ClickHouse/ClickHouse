---
description: 'Calcule la valeur `arg` pour une valeur `val` minimale. Si plusieurs
  lignes ont une valeur `val` égale au maximum, la valeur `arg` associée qui est renvoyée
  n''est pas déterministe.'
slug: /sql-reference/aggregate-functions/reference/argmin
title: 'argMin'
doc_type: 'reference'
---

Alias : `min_by`

{/*AUTOGENERATED_START*/}

<div id="argMin">
  ## argMin
</div>

Introduit dans : v1.1.0

Calcule la valeur `arg` pour une valeur `val` minimale. S&#39;il existe plusieurs lignes avec des `val` égales correspondant au maximum, le `arg` associé renvoyé n&#39;est pas déterministe.
`arg` et `min` se comportent tous deux comme des [fonctions d’agrégation](/fr/reference/functions/aggregate-functions/index) : ils [ignorent `NULL`](/fr/reference/functions/aggregate-functions/index#null-processing) pendant le traitement et renvoient des valeurs non `NULL` si de telles valeurs sont disponibles.

**Voir aussi**

* [Tuple](/fr/reference/data-types/tuple)

**Syntaxe**

```sql
argMin(arg, val)
```

**Alias** : `min_by`

**Arguments**

* `arg` — Argument pour lequel trouver la valeur minimale. [`const String`](/fr/reference/data-types/string)
* `val` — La valeur minimale. [`(U)Int8/16/32/64`](/fr/reference/data-types/int-uint) ou [`Float*`](/fr/reference/data-types/float) ou [`Date`](/fr/reference/data-types/date) ou [`DateTime`](/fr/reference/data-types/datetime) ou [`Tuple`](/fr/reference/data-types/tuple)

**Valeur renvoyée**

Renvoie la valeur de `arg` correspondant à la valeur minimale de `val`. Le type correspond à celui de `arg`.

**Exemples**

**Utilisation de base**

```sql title=Query
SELECT argMin(user, salary) FROM salary;
```

```response title=Response
┌─argMin(user, salary)─┐
│ worker               │
└──────────────────────┘
```

**Exemple détaillé avec prise en charge de NULL**

```sql title=Query
CREATE TABLE test
(
    a Nullable(String),
    b Nullable(Int64)
)
ENGINE = Memory AS
SELECT *
FROM VALUES((NULL, 0), ('a', 1), ('b', 2), ('c', 2), (NULL, NULL), ('d', NULL));

SELECT argMin(a, b), min(b) FROM test;
```

```response title=Response
┌─argMin(a, b)─┬─min(b)─┐
│ a            │      0 │
└──────────────┴────────┘
```

**Utilisation de Tuple dans les arguments**

```sql title=Query
SELECT argMin(a, (b, a)), min(tuple(b, a)) FROM test;
```

```response title=Response
┌─argMin(a, tuple(b, a))─┬─min(tuple(b, a))─┐
│ d                      │ (NULL,NULL)      │
└────────────────────────┴──────────────────┘
```

{/*AUTOGENERATED_END*/}