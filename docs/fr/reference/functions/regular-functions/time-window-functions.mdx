---
description: 'Documentation sur les fonctions de fenêtre temporelle'
sidebarTitle: 'Fenêtre temporelle'
slug: /sql-reference/functions/time-window-functions
title: 'Fonctions de fenêtre temporelle'
doc_type: 'reference'
keywords: ['fenêtre temporelle']
---

import { ExperimentalBadge } from "/snippets/fr/components/ExperimentalBadge/ExperimentalBadge.jsx";
import { CloudNotSupportedBadge } from "/snippets/fr/components/CloudNotSupportedBadge/CloudNotSupportedBadge.jsx";

<ExperimentalBadge />

<CloudNotSupportedBadge />

Les fonctions de fenêtre temporelle renvoient la borne inférieure incluse et la borne supérieure exclusive de la fenêtre correspondante.
Les fonctions pour utiliser les fenêtres temporelles sont présentées ci-dessous :

{/* 
  Le contenu interne des balises ci-dessous est remplacé, lors de la génération de la documentation, par 
  la documentation générée à partir de system.functions. Veuillez ne pas modifier ni supprimer ces balises.
  Voir : https://github.com/ClickHouse/clickhouse-docs/blob/main/contribute/autogenerated-documentation-from-source.md
  */}

{/*AUTOGENERATED_START*/}

## hop {#hop}

Introduit dans : v21.12.0

Une fenêtre temporelle à sauts a une durée fixe (`window_interval`) et avance selon un intervalle de saut donné (`hop_interval`). Si `hop_interval` est inférieur à `window_interval`, les fenêtres à sauts se chevauchent. Un enregistrement peut donc être affecté à plusieurs fenêtres.

Comme un enregistrement peut être affecté à plusieurs fenêtres à sauts, la fonction renvoie uniquement la borne de la première fenêtre.

**Syntaxe**

```sql
hop(time_attr, hop_interval, window_interval[, timezone])
```

**Arguments**

* `time_attr` — Date et heure. [`DateTime`](/fr/reference/data-types/datetime)
* `hop_interval` — Intervalle de saut positif. [`Interval`](/fr/reference/data-types/int-uint)
* `window_interval` — Intervalle de fenêtre positif. [`Interval`](/fr/reference/data-types/int-uint)
* `timezone` — Facultatif. Nom du fuseau horaire. [`String`](/fr/reference/data-types/string)

**Valeur renvoyée**

Renvoie la borne inférieure incluse et la borne supérieure exclusive de la fenêtre à sauts correspondante. [`Tuple(DateTime, DateTime)`](/fr/reference/data-types/tuple)

**Exemples**

**Fenêtre à sauts**

```sql title=Query
SELECT hop(toDateTime('2026-01-02 03:04:05', 'UTC'), INTERVAL '1' DAY, INTERVAL '2' DAY)
```

```response title=Response
('2026-01-01 00:00:00','2026-01-03 00:00:00')
```

## hopEnd {#hopEnd}

Introduit dans : v22.1.0

Renvoie la borne supérieure exclusive de la fenêtre à sauts correspondante.

Comme un enregistrement peut appartenir à plusieurs fenêtres à sauts, la fonction ne renvoie que la borne de la première fenêtre.

**Syntaxe**

```sql
hopEnd(time_attr, hop_interval, window_interval[, timezone])
```

**Arguments**

* `time_attr` — Date et heure. [`DateTime`](/fr/reference/data-types/datetime)
* `hop_interval` — intervalle de saut positif. [`Interval`](/fr/reference/data-types/int-uint)
* `window_interval` — intervalle de fenêtre positif. [`Interval`](/fr/reference/data-types/int-uint)
* `timezone` — Facultatif. Nom du fuseau horaire. [`String`](/fr/reference/data-types/string)

**Valeur renvoyée**

Renvoie la borne supérieure exclusive de la fenêtre à sauts correspondante. [`DateTime`](/fr/reference/data-types/datetime)

**Exemples**

**Fin de la fenêtre à sauts**

```sql title=Query
SELECT hopEnd(toDateTime('2026-01-02 03:04:05', 'UTC'), INTERVAL '1' DAY, INTERVAL '2' DAY)
```

```response title=Response
2026-01-03 00:00:00
```

## hopStart {#hopStart}

Introduit dans : v22.1.0

Renvoie la borne inférieure incluse de la fenêtre à sauts correspondante.

Comme un enregistrement peut être affecté à plusieurs fenêtres à sauts, la fonction ne renvoie que la borne de la première fenêtre.

**Syntaxe**

```sql
hopStart(time_attr, hop_interval, window_interval[, timezone])
```

**Arguments**

* `time_attr` — Date et heure. [`DateTime`](/fr/reference/data-types/datetime)
* `hop_interval` — Intervalle de saut positif. [`Interval`](/fr/reference/data-types/int-uint)
* `window_interval` — Intervalle de fenêtre positif. [`Interval`](/fr/reference/data-types/int-uint)
* `timezone` — Facultatif. Nom du fuseau horaire. [`String`](/fr/reference/data-types/string)

**Valeur renvoyée**

Renvoie la borne inférieure incluse de la fenêtre à sauts correspondante. [`DateTime`](/fr/reference/data-types/datetime)

**Exemples**

**Début de la fenêtre à sauts**

```sql title=Query
SELECT hopStart(toDateTime('2026-01-02 03:04:05', 'UTC'), INTERVAL '1' DAY, INTERVAL '2' DAY)
```

```response title=Response
2026-01-01 00:00:00
```

## tumble {#tumble}

Introduit dans : v21.12.0

Une fenêtre temporelle de type tumble attribue les enregistrements à des fenêtres contiguës, sans chevauchement, d&#39;une durée fixe (`interval`).

**Syntaxe**

```sql
tumble(time_attr, interval[, timezone])
```

**Arguments**

* `time_attr` — Date et heure. [`DateTime`](/fr/reference/data-types/datetime)
* `interval` — Intervalle de fenêtre de type Interval. [`Interval`](/fr/reference/data-types/int-uint)
* `timezone` — Facultatif. Nom du fuseau horaire. [`String`](/fr/reference/data-types/string)

**Valeur renvoyée**

Renvoie la borne inférieure incluse et la borne supérieure exclusive de la fenêtre tumbling correspondante. [`Tuple(DateTime, DateTime)`](/fr/reference/data-types/tuple)

**Exemples**

**Fenêtre tumbling**

```sql title=Query
SELECT tumble(toDateTime('2026-01-02 03:04:05', 'UTC'), toIntervalDay('1'))
```

```response title=Response
('2026-01-02 00:00:00','2026-01-03 00:00:00')
```

## tumbleEnd {#tumbleEnd}

Introduit dans : v22.1.0

Renvoie la borne supérieure exclusive de la fenêtre tumbling correspondante.

**Syntaxe**

```sql
tumbleEnd(time_attr, interval[, timezone])
```

**Arguments**

* `time_attr` — Date et heure. [`DateTime`](/fr/reference/data-types/datetime)
* `interval` — Intervalle de fenêtre, de type Interval. [`Interval`](/fr/reference/data-types/int-uint)
* `timezone` — Facultatif. Nom du fuseau horaire. [`String`](/fr/reference/data-types/string)

**Valeur renvoyée**

Renvoie la borne supérieure exclusive de la fenêtre tumbling correspondante. [`DateTime`](/fr/reference/data-types/datetime)

**Exemples**

**Fin de la fenêtre tumbling**

```sql title=Query
SELECT tumbleEnd(toDateTime('2026-01-02 03:04:05', 'UTC'), toIntervalDay('1'))
```

```response title=Response
2026-01-03 00:00:00
```

## tumbleStart {#tumbleStart}

Introduite dans : v22.1.0

Renvoie la borne inférieure incluse de la fenêtre tumbling correspondante.

**Syntaxe**

```sql
tumbleStart(time_attr, interval[, timezone])
```

**Arguments**

* `time_attr` — Date et heure. [`DateTime`](/fr/reference/data-types/datetime)
* `interval` — Intervalle de fenêtre, de type Interval. [`Interval`](/fr/reference/data-types/int-uint)
* `timezone` — Facultatif. Nom du fuseau horaire. [`String`](/fr/reference/data-types/string)

**Valeur renvoyée**

Renvoie la borne inférieure incluse de la fenêtre tumbling correspondante. [`DateTime`](/fr/reference/data-types/datetime)

**Exemples**

**Début de la fenêtre tumbling**

```sql title=Query
SELECT tumbleStart(toDateTime('2026-01-02 03:04:05', 'UTC'), toIntervalDay('1'))
```

```response title=Response
2026-01-02 00:00:00
```

{/*AUTOGENERATED_END*/}

## Contenu connexe {#related-content}

* [Guides de cas d’usage pour les séries temporelles](/fr/guides/use-cases/real-time-analytics/time-series/index)