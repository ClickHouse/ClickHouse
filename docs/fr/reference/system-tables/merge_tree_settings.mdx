---
description: 'Table système contenant des informations sur les paramètres des tables MergeTree.'
keywords: ['table système', 'merge_tree_settings']
slug: /operations/system-tables/merge_tree_settings
title: 'system.merge_tree_settings'
doc_type: 'reference'
---

<div id="description">
  ## Description
</div>

Contient des informations sur les paramètres des tables `MergeTree`.

<div id="columns">
  ## Colonnes
</div>

{/*AUTOGENERATED_START*/}

* `name` ([String](/fr/reference/data-types/index)) — Nom du paramètre.
* `value` ([String](/fr/reference/data-types/index)) — Valeur du paramètre.
* `default` ([String](/fr/reference/data-types/index)) — Valeur par défaut du paramètre.
* `changed` ([UInt8](/fr/reference/data-types/index)) — 1 si le paramètre a été explicitement défini dans la configuration ou explicitement modifié.
* `description` ([String](/fr/reference/data-types/index)) — Description du paramètre.
* `min` ([Nullable(String)](/fr/reference/data-types/index)) — Valeur minimale du paramètre, si une telle valeur est définie via des contraintes. Si le paramètre n’a pas de valeur minimale, contient NULL.
* `max` ([Nullable(String)](/fr/reference/data-types/index)) — Valeur maximale du paramètre, si une telle valeur est définie via des contraintes. Si le paramètre n’a pas de valeur maximale, contient NULL.
* `disallowed_values` ([Array(String)](/fr/reference/data-types/index)) — Liste des valeurs interdites
* `readonly` ([UInt8](/fr/reference/data-types/index)) — Indique si l’utilisateur actuel peut modifier le paramètre : 0 — L’utilisateur actuel peut modifier le paramètre, 1 — L’utilisateur actuel ne peut pas modifier le paramètre.
* `type` ([String](/fr/reference/data-types/index)) — Type du paramètre (valeur de chaîne spécifique à l’implémentation).
* `is_obsolete` ([UInt8](/fr/reference/data-types/index)) — Indique si le paramètre est obsolète.
* `tier` ([Enum8(&#39;Production&#39; = 0, &#39;Obsolete&#39; = 4, &#39;Experimental&#39; = 8, &#39;Beta&#39; = 12)](/fr/reference/data-types/index)) —
  Niveau de support de cette fonctionnalité. Les fonctionnalités de ClickHouse sont organisées par niveaux, qui varient selon l’état actuel de leur
  développement et le niveau d’attente raisonnable lors de leur utilisation :

- PRODUCTION: La fonctionnalité est stable, sûre à utiliser et ne pose pas de problèmes d’interaction avec d’autres fonctionnalités PRODUCTION.
- BETA: La fonctionnalité est stable et sûre. Le résultat de son utilisation avec d’autres fonctionnalités est inconnu et son bon fonctionnement n’est pas garanti. Les tests et les signalements sont les bienvenus.
- EXPERIMENTAL: La fonctionnalité est en cours de développement. Elle est destinée uniquement aux développeurs et aux passionnés de ClickHouse. Elle peut fonctionner ou non et peut être supprimée à tout moment.
- OBSOLETE: N’est plus prise en charge. Soit elle a déjà été supprimée, soit elle le sera dans de futures versions.

{/*AUTOGENERATED_END*/}

<div id="example">
  ## Exemple
</div>

```sql
SELECT * FROM system.merge_tree_settings LIMIT 3 FORMAT Vertical;
```

```response
SELECT *
FROM system.merge_tree_settings
LIMIT 3
FORMAT Vertical

Query id: 2580779c-776e-465f-a90c-4b7630d0bb70

Row 1:
──────
name:        min_compress_block_size
value:       0
default:     0
changed:     0
description: When granule is written, compress the data in buffer if the size of pending uncompressed data is larger or equal than the specified threshold. If this setting is not set, the corresponding global setting is used.
min:         ᴺᵁᴸᴸ
max:         ᴺᵁᴸᴸ
readonly:    0
type:        UInt64
is_obsolete: 0
tier:        Production

Row 2:
──────
name:        max_compress_block_size
value:       0
default:     0
changed:     0
description: Compress the pending uncompressed data in buffer if its size is larger or equal than the specified threshold. Block of data will be compressed even if the current granule is not finished. If this setting is not set, the corresponding global setting is used.
min:         ᴺᵁᴸᴸ
max:         ᴺᵁᴸᴸ
readonly:    0
type:        UInt64
is_obsolete: 0
tier:        Production

Row 3:
──────
name:        index_granularity
value:       8192
default:     8192
changed:     0
description: How many rows correspond to one primary key value.
min:         ᴺᵁᴸᴸ
max:         ᴺᵁᴸᴸ
readonly:    0
type:        UInt64
is_obsolete: 0
tier:        Production

3 rows in set. Elapsed: 0.001 sec. 
```