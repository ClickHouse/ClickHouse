---
description: 'Table système contenant des informations sur l’utilisation du quota de tous les utilisateurs.'
keywords: ['table système', 'quotas_usage', 'quota']
slug: /operations/system-tables/quotas_usage
title: 'system.quotas_usage'
doc_type: 'référence'
---

import SystemTableCloud from '/snippets/fr/_system_table_cloud.mdx';

<SystemTableCloud />

<div id="description">
  ## Description
</div>

Utilisation du quota par l’ensemble des utilisateurs.

<div id="columns">
  ## Colonnes
</div>

{/*AUTOGENERATED_START*/}

* `quota_name` ([String](/fr/reference/data-types/index)) — Nom du quota.
* `quota_key` ([String](/fr/reference/data-types/index)) — Valeur de la clé.
* `is_current` ([UInt8](/fr/reference/data-types/index)) — Utilisation du quota pour l&#39;utilisateur actuel.
* `start_time` ([Nullable(DateTime)](/fr/reference/data-types/index)) — Heure de début du calcul de la consommation de ressources.
* `end_time` ([Nullable(DateTime)](/fr/reference/data-types/index)) — Heure de fin du calcul de la consommation de ressources.
* `duration` ([Nullable(UInt32)](/fr/reference/data-types/index)) — Durée de l&#39;intervalle de temps pour calculer la consommation de ressources, en secondes.
* `queries` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Nombre actuel de requêtes exécutées.
* `max_queries` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Nombre maximal autorisé de requêtes de tout type pouvant être exécutées.
* `query_selects` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Nombre actuel de requêtes SELECT exécutées.
* `max_query_selects` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Nombre maximal autorisé de requêtes SELECT pouvant être exécutées.
* `query_inserts` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Nombre actuel de requêtes INSERT exécutées.
* `max_query_inserts` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Nombre maximal autorisé de requêtes INSERT pouvant être exécutées.
* `errors` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Nombre actuel de requêtes ayant généré une erreur.
* `max_errors` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Nombre maximal autorisé de requêtes ayant généré une erreur pendant la période spécifiée.
* `result_rows` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Nombre total actuel de lignes dans le jeu de résultats de toutes les requêtes pendant la période actuelle.
* `max_result_rows` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Nombre total maximal de lignes dans le jeu de résultats de toutes les requêtes autorisé pendant la période spécifiée.
* `result_bytes` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Nombre total actuel d&#39;octets dans le jeu de résultats de toutes les requêtes pendant la période actuelle.
* `max_result_bytes` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Nombre total maximal d&#39;octets dans le jeu de résultats de toutes les requêtes autorisé pendant la période spécifiée.
* `read_rows` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Nombre total actuel de lignes lues pendant l&#39;exécution de toutes les requêtes au cours de la période actuelle.
* `max_read_rows` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Nombre maximal autorisé de lignes à lire pendant l&#39;exécution de toutes les requêtes pendant la période spécifiée.
* `read_bytes` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Nombre total actuel d&#39;octets lus pendant l&#39;exécution de toutes les requêtes au cours de la période actuelle.
* `max_read_bytes` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Nombre maximal autorisé d&#39;octets à lire pendant l&#39;exécution de toutes les requêtes pendant la période spécifiée.
* `execution_time` ([Nullable(Float64)](/fr/reference/data-types/index)) — Temps total actuellement consacré à l&#39;exécution des requêtes (en nanosecondes) au cours de la période actuelle
* `max_execution_time` ([Nullable(Float64)](/fr/reference/data-types/index)) — Temps maximal autorisé pour l&#39;exécution de toutes les requêtes (en nanosecondes) pendant la période spécifiée
* `written_bytes` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Nombre total actuel d&#39;octets écrits pendant l&#39;exécution de toutes les requêtes au cours de la période actuelle.
* `max_written_bytes` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Nombre maximal autorisé d&#39;octets à écrire pendant l&#39;exécution de toutes les requêtes pendant la période spécifiée.
* `failed_sequential_authentications` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Nombre actuel d&#39;échecs d&#39;authentification consécutifs au cours de la période actuelle.
* `max_failed_sequential_authentications` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Nombre maximal autorisé d&#39;échecs d&#39;authentification consécutifs pendant la période spécifiée.
* `queries_per_normalized_hash` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Nombre maximal actuel d&#39;exécutions d&#39;une même requête normalisée au cours de la période actuelle.
* `max_queries_per_normalized_hash` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Nombre maximal autorisé d&#39;exécutions d&#39;une même requête normalisée pendant la période spécifiée.

{/*AUTOGENERATED_END*/}

<div id="see-also">
  ## Voir aussi
</div>

* [SHOW QUOTA](/fr/reference/statements/show#show-quota))