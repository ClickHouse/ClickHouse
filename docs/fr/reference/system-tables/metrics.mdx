---
description: 'Table système contenant des métriques calculables instantanément ou
  ayant une valeur actuelle.'
keywords: ['table système', 'métriques']
slug: /operations/system-tables/metrics
title: 'system.metrics'
doc_type: 'référence'
---

import SystemTableCloud from '/snippets/fr/_system_table_cloud.mdx';

<SystemTableCloud />

{/*AUTOGENERATED_START*/}

## Description {#description}

Contient des métriques pouvant être calculées instantanément ou ayant une valeur courante. Par exemple, le nombre de requêtes traitées simultanément ou le retard actuel de la réplique. Cette table est toujours à jour.

## Descriptions des métriques {#metric-descriptions}

### ActiveTimersInQueryProfiler {#activetimersinqueryprofiler}

Nombre de temporisateurs locaux par thread actifs dans QueryProfiler

### AddressesActive {#addressesactive}

Nombre total d’adresses utilisées pour établir des connexions avec des pools de connexions

### Adresses bannies {#addressesbanned}

Nombre total d’adresses bannies car jugées défectueuses pour l’établissement de connexions avec des pools de connexions

### AggregatorThreads {#aggregatorthreads}

Nombre de threads du pool de threads Aggregator.

### AggregatorThreadsActive {#aggregatorthreadsactive}

Nombre de threads du pool de threads Aggregator qui exécutent une tâche.

### AggregatorThreadsScheduled {#aggregatorthreadsscheduled}

Nombre de jobs en attente ou en cours d’exécution dans le pool de threads Aggregator.

### ASTFuzzerAccumulatedFragments {#astfuzzeraccumulatedfragments}

Nombre de fragments d’AST accumulés par le fuzzer AST côté serveur en vue de futures mutations.

### AsynchronousInsertQueueBytes {#asynchronousinsertqueuebytes}

Nombre d’octets en attente dans la file d’attente d’AsynchronousInsert.

### AsynchronousInsertQueueSize {#asynchronousinsertqueuesize}

Nombre de tâches en attente dans la file AsynchronousInsert.

### AsynchronousInsertThreads {#asynchronousinsertthreads}

Nombre de threads du pool de threads AsynchronousInsert.

### AsynchronousInsertThreadsActive {#asynchronousinsertthreadsactive}

Nombre de threads du pool de threads AsynchronousInsert qui exécutent une tâche.

### AsynchronousInsertThreadsScheduled {#asynchronousinsertthreadsscheduled}

Nombre de tâches en file d&#39;attente ou actives dans le pool de threads AsynchronousInsert.

### AsynchronousReadWait {#asynchronousreadwait}

Nombre de threads en attente d’une lecture asynchrone.

### AsyncInsertCacheSize {#asyncinsertcachesize}

Nombre d’identifiants de hachage d’async insert en cache

### AttachedDatabase {#attacheddatabase}

Bases de données actives.

### AttachedDictionary {#attacheddictionary}

Dictionnaires actifs.

### AttachedReplicatedTable {#attachedreplicatedtable}

Tables répliquées actives.

### Table attachée {#attachedtable}

Tables actives.

### AttachedView {#attachedview}

Vues attachées.

### AvroSchemaCacheBytes {#avroschemacachebytes}

Taille du cache des schémas Avro, en octets

### AvroSchemaCacheCells {#avroschemacachecells}

Nombre de schémas Avro en cache, y compris les schémas enregistrés et récupérés.

### AvroSchemaRegistryCacheBytes {#avroschemaregistrycachebytes}

Taille du cache du registre de schémas Avro, en octets

### AvroSchemaRegistryCacheCells {#avroschemaregistrycachecells}

Nombre d’éléments dans le cache du registre de schémas Avro

### AzureRequests {#azurerequests}

Nombre de requêtes Azure actuellement traitées

### BackgroundBufferFlushSchedulePoolSize {#backgroundbufferflushschedulepoolsize}

Nombre maximal de tâches dans BackgroundBufferFlushSchedulePool

### BackgroundBufferFlushSchedulePoolTask {#backgroundbufferflushschedulepooltask}

Nombre de tâches actives dans BackgroundBufferFlushSchedulePool. Ce pool est utilisé pour les opérations périodiques de flush du Buffer

### BackgroundCommonPoolSize {#backgroundcommonpoolsize}

Limite du nombre de tâches dans le pool d’arrière-plan associé

### BackgroundCommonPoolTask {#backgroundcommonpooltask}

Nombre de tâches actives dans le pool d’arrière-plan associé

### BackgroundDistributedSchedulePoolSize {#backgrounddistributedschedulepoolsize}

Nombre maximal de tâches dans BackgroundDistributedSchedulePool

### BackgroundDistributedSchedulePoolTask {#backgrounddistributedschedulepooltask}

Nombre de tâches actives dans BackgroundDistributedSchedulePool. Ce pool est utilisé pour les envois distribués réalisés en arrière-plan.

### BackgroundFetchesPoolSize {#backgroundfetchespoolsize}

Limite du nombre de fetches simultanés dans le pool d’arrière-plan associé

### BackgroundFetchesPoolTask {#backgroundfetchespooltask}

Nombre d&#39;opérations de récupération actives dans un pool d&#39;arrière-plan associé

### BackgroundMergesAndMutationsPoolSize {#backgroundmergesandmutationspoolsize}

Limite du nombre de fusions et de mutations actives dans le pool d’arrière-plan associé

### BackgroundMergesAndMutationsPoolTask {#backgroundmergesandmutationspooltask}

Nombre de fusions et de mutations actives dans le pool d’arrière-plan associé

### BackgroundMessageBrokerSchedulePoolSize {#backgroundmessagebrokerschedulepoolsize}

Nombre maximal de tâches dans BackgroundMessageBrokerSchedulePool pour le streaming de messages

### BackgroundMessageBrokerSchedulePoolTask {#backgroundmessagebrokerschedulepooltask}

Nombre de tâches actives dans BackgroundMessageBrokerSchedulePool pour le streaming de messages

### BackgroundMovePoolSize {#backgroundmovepoolsize}

Nombre maximal de tâches dans BackgroundProcessingPool pour les déplacements

### BackgroundMovePoolTask {#backgroundmovepooltask}

Nombre de tâches actives dans BackgroundProcessingPool pour les opérations de déplacement

### BackgroundQueryThreads {#backgroundquerythreads}

Nombre de threads dans le thread pool des requêtes en arrière-plan.

### BackgroundQueryThreadsActive {#backgroundquerythreadsactive}

Nombre de threads du thread pool de requêtes en arrière-plan exécutant une task.

### BackgroundQueryThreadsScheduled {#backgroundquerythreadsscheduled}

Nombre de jobs en file d&#39;attente ou actifs dans le thread pool des requêtes en arrière-plan.

### BackgroundSchedulePoolSize {#backgroundschedulepoolsize}

Limite du nombre de tâches dans BackgroundSchedulePool. Ce pool est utilisé pour les tâches périodiques de ReplicatedMergeTree, comme le nettoyage des anciens data parts, la modification des data parts, la réinitialisation des réplicas, etc.

### BackgroundSchedulePoolTask {#backgroundschedulepooltask}

Nombre de tâches actives dans BackgroundSchedulePool. Ce pool est utilisé pour les tâches périodiques de ReplicatedMergeTree, comme le nettoyage des anciennes data parts, la modification de data parts, la réinitialisation des répliques, etc.

### BackgroundStreamingSchedulePoolSize {#backgroundstreamingschedulepoolsize}

Limite du nombre de tasks dans BackgroundStreamingSchedulePool pour les tasks de requêtes en streaming

### BackgroundStreamingSchedulePoolTask {#backgroundstreamingschedulepooltask}

Nombre de tasks actives dans BackgroundStreamingSchedulePool pour les tasks de requêtes en streaming

### BackupsIOThreads {#backupsiothreads}

Nombre de threads du pool de threads BackupsIO.

### BackupsIOThreadsActive {#backupsiothreadsactive}

Nombre de threads du pool de threads BackupsIO qui exécutent une tâche.

### BackupsIOThreadsScheduled {#backupsiothreadsscheduled}

Nombre de jobs en attente ou actifs dans le pool de threads BackupsIO.

### BackupsThreads {#backupsthreads}

Nombre de threads dans le pool de threads BACKUP.

### BackupsThreadsActive {#backupsthreadsactive}

Nombre de threads du pool de threads BACKUP exécutant une tâche.

### BackupsThreadsScheduled {#backupsthreadsscheduled}

Nombre de jobs en file d’attente ou actifs pour BACKUP.

### BcryptCacheBytes {#bcryptcachebytes}

Taille totale du cache d’authentification bcrypt, en octets

### BcryptCacheSize {#bcryptcachesize}

Nombre total d’entrées du cache d’authentification bcrypt

### BlobCopierThreads {#blobcopierthreads}

Nombre de threads du pool de threads du processus de réplication en arrière-plan du disque de stockage d’objets

### BlobCopierThreadsActive {#blobcopierthreadsactive}

Nombre de threads du pool de threads du processus de réplication en arrière-plan du disque de stockage d’objets qui exécutent une tâche

### BlobCopierThreadsScheduled {#blobcopierthreadsscheduled}

Nombre de tâches en file d’attente ou en cours d’exécution dans le pool de threads du processus de réplication en arrière-plan du disque de stockage d’objets

### BlobKillerThreads {#blobkillerthreads}

Nombre de threads dans le pool de threads du processus de suppression en arrière-plan du disque de stockage d’objets

### BlobKillerThreadsActive {#blobkillerthreadsactive}

Nombre de threads dans le pool de threads du processus de suppression en arrière-plan du disque de stockage d’objets qui exécutent une tâche

### BlobKillerThreadsScheduled {#blobkillerthreadsscheduled}

Nombre de tâches en file d’attente ou en cours d’exécution dans le pool de threads du processus de suppression en arrière-plan du disque de stockage d’objets

### BrokenDisks {#brokendisks}

Nombre de disques marqués comme défectueux lors de la vérification des disques.

### BrokenDistributedBytesToInsert {#brokendistributedbytestoinsert}

Nombre d’octets destinés à l’insertion asynchrone dans des tables Distributed et marqués comme défectueux. Le nombre d’octets de chaque shard est additionné.

### BrokenDistributedFilesToInsert {#brokendistributedfilestoinsert}

Nombre de fichiers d’insertion asynchrone dans les tables Distributed marqués comme défectueux. Le nombre de fichiers de chaque shard est additionné.

### BuildVectorSimilarityIndexThreads {#buildvectorsimilarityindexthreads}

Nombre de threads dans le pool de threads dédié à la création de l’index de similarité vectorielle.

### BuildVectorSimilarityIndexThreadsActive {#buildvectorsimilarityindexthreadsactive}

Nombre de threads du pool chargé de construire les index de similarité vectorielle qui exécutent une tâche.

### BuildVectorSimilarityIndexThreadsScheduled {#buildvectorsimilarityindexthreadsscheduled}

Nombre de jobs en attente ou actifs dans le pool de threads de construction de l’index de similarité vectorielle.

### CacheDetachedFileSegments {#cachedetachedfilesegments}

Nombre de segments de fichier du cache actuellement détachés

### CacheDictionaryThreads {#cachedictionarythreads}

Nombre de threads du pool de threads CacheDictionary.

### CacheDictionaryThreadsActive {#cachedictionarythreadsactive}

Nombre de threads du pool de threads CacheDictionary qui exécutent une tâche.

### CacheDictionaryThreadsScheduled {#cachedictionarythreadsscheduled}

Nombre de jobs en file d’attente ou en cours d’exécution dans le pool de threads CacheDictionary.

### CacheDictionaryUpdateQueueBatches {#cachedictionaryupdatequeuebatches}

Nombre de « batches » (un ensemble de clés) dans la file d’attente des mises à jour de CacheDictionaries.

### Clés de la file d’attente des mises à jour de CacheDictionary {#cachedictionaryupdatequeuekeys}

Nombre exact de clés dans la file d’attente des mises à jour de CacheDictionaries.

### CacheFileSegments {#cachefilesegments}

Nombre de segments de fichier présents dans le cache

### CacheWarmerBytesInProgress {#cachewarmerbytesinprogress}

Taille totale des segments de fichiers distants en attente de chargement asynchrone dans le cache du système de fichiers.

### ColumnsDescriptionsCacheSize {#columnsdescriptionscachesize}

Taille du cache ColumnsDescriptions (cache par table)

### CompiledExpressionCacheBytes {#compiledexpressioncachebytes}

Capacité réservée en blocs de pages (arrondie à des pages entières avec un facteur de surprovisionnement de 2x) par `JITModuleMemoryManager` pour les sections exécutables et de données des fonctions compilées par JIT mises en cache. PAS le nombre réel d’octets de code machine utilisés (il est plus faible). L’allocation se fait via `posix_memalign`, redirigé vers jemalloc ; cette capacité est donc comptabilisée dans l’arène JIT dédiée et constitue un sous-ensemble de `jemalloc.jit_arena.active_bytes`.

### CompiledExpressionCacheCount {#compiledexpressioncachecount}

Nombre total d’entrées dans le cache de code machine compilé en JIT.

### Compression {#compressing}

Nombre d’opérations de compression utilisant les codecs de compression internes

### CompressionThread {#compressionthread}

Nombre de threads dans les pools de threads dédiés à la compression.

### CompressionThreadActive {#compressionthreadactive}

Nombre de threads des pools de threads de compression qui exécutent une tâche.

### CompressionThreadScheduled {#compressionthreadscheduled}

Nombre de jobs en file d&#39;attente ou en cours d&#39;exécution dans les pools de threads de compression.

### ConcurrencyControlAcquired {#concurrencycontrolacquired}

Nombre total de slots CPU obtenus

### ConcurrencyControlAcquiredNonCompeting {#concurrencycontrolacquirednoncompeting}

Nombre total de slots CPU obtenus qui ne sont pas considérés comme étant en concurrence (le premier thread si l’ordonnanceur fair&#95;round&#95;robin est utilisé)

### ConcurrencyControlPreempted {#concurrencycontrolpreempted}

Nombre total de threads préemptés en attente d’un slot CPU

### ConcurrencyControlScheduled {#concurrencycontrolscheduled}

Nombre total de demandes de CPU slot actuellement en cours de planification

### ConcurrencyControlSoftLimit {#concurrencycontrolsoftlimit}

Valeur de la limite souple du nombre de slots CPU

### ConcurrentHashJoinPoolThreads {#concurrenthashjoinpoolthreads}

Nombre de threads dans le pool de threads du hash join concurrent.

### ConcurrentHashJoinPoolThreadsActive {#concurrenthashjoinpoolthreadsactive}

Nombre de threads du pool de threads de jointure de hachage concurrente qui exécutent une tâche.

### ConcurrentHashJoinPoolThreadsScheduled {#concurrenthashjoinpoolthreadsscheduled}

Nombre de tâches en attente ou en cours d’exécution dans le pool de threads du hash join concurrent.

### ConcurrentQueryAcquired {#concurrentqueryacquired}

Nombre total de slots de requête obtenus

### ConcurrentQueryScheduled {#concurrentqueryscheduled}

Nombre total de demandes de slot de requête actuellement en cours de planification

### ContextLockWait {#contextlockwait}

Nombre de threads en attente du verrou de Context. Il s&#39;agit d&#39;un verrou global.

### CoordinatedMergesCoordinatorAssignedMerges {#coordinatedmergescoordinatorassignedmerges}

Nombre de fusions attribuées dans l&#39;état du coordinateur de fusion.

### CoordinatedMergesCoordinatorRunningMerges {#coordinatedmergescoordinatorrunningmerges}

Nombre de fusions en cours visibles par le coordinateur de fusion.

### CoordinatedMergesWorkerAssignedMerges {#coordinatedmergesworkerassignedmerges}

Nombre de fusions assignées dans l’état du merge worker.

### CreatedTimersInQueryProfiler {#createdtimersinqueryprofiler}

Nombre de temporisateurs locaux par thread créés dans QueryProfiler

### DatabaseBackupThreads {#databasebackupthreads}

Nombre de threads du pool de threads DatabaseBackup.

### DatabaseBackupThreadsActive {#databasebackupthreadsactive}

Nombre de threads du pool de threads DatabaseBackup exécutant une tâche.

### DatabaseBackupThreadsScheduled {#databasebackupthreadsscheduled}

Nombre de jobs en file d’attente ou actifs dans le pool de threads DatabaseBackup.

### DatabaseCatalogThreads {#databasecatalogthreads}

Nombre de threads du pool de threads DatabaseCatalog.

### DatabaseCatalogThreadsActive {#databasecatalogthreadsactive}

Nombre de threads du pool de threads DatabaseCatalog en train d’exécuter une tâche.

### DatabaseCatalogThreadsScheduled {#databasecatalogthreadsscheduled}

Nombre de tâches en attente ou actives dans le pool de threads DatabaseCatalog.

### DatabaseOnDiskThreads {#databaseondiskthreads}

Nombre de threads du pool de threads DatabaseOnDisk.

### DatabaseOnDiskThreadsActive {#databaseondiskthreadsactive}

Nombre de threads du pool de threads DatabaseOnDisk exécutant une tâche.

### DatabaseOnDiskThreadsScheduled {#databaseondiskthreadsscheduled}

Nombre de jobs en file d’attente ou actifs dans le pool de threads DatabaseOnDisk.

### DatabaseReplicatedCreateTablesThreads {#databasereplicatedcreatetablesthreads}

Nombre de threads du pool de threads pour la création de tables dans DatabaseReplicated.

### DatabaseReplicatedCreateTablesThreadsActive {#databasereplicatedcreatetablesthreadsactive}

Nombre de threads actifs dans le pool de threads dédié à la création de tables dans DatabaseReplicated.

### DatabaseReplicatedCreateTablesThreadsScheduled {#databasereplicatedcreatetablesthreadsscheduled}

Nombre de jobs en file d’attente ou actifs dans le pool de threads pour la création de tables dans DatabaseReplicated.

### DDLWorkerThreads {#ddlworkerthreads}

Nombre de threads du pool de threads DDLWorker pour les requêtes ON CLUSTER.

### DDLWorkerThreadsActive {#ddlworkerthreadsactive}

Nombre de threads du pool DDLWORKER pour les requêtes ON CLUSTER qui exécutent une tâche.

### DDLWorkerThreadsScheduled {#ddlworkerthreadsscheduled}

Nombre de jobs actifs ou en file d’attente dans le pool de threads DDLWORKER pour les requêtes ON CLUSTER.

### Décompression {#decompressing}

Nombre d’opérations de décompression avec les codecs de compression internes

### DelayedInserts {#delayedinserts}

Nombre de requêtes INSERT dont le débit est limité en raison du nombre élevé de data parts actives dans une partition d’une table MergeTree.

### DeleteBitmapCacheBytes {#deletebitmapcachebytes}

Taille totale, en octets, du cache des bitmaps de suppression de UNIQUE KEY

### DeleteBitmapCacheEntries {#deletebitmapcacheentries}

Nombre total de bitmaps de suppression de UNIQUE KEY mis en cache

### DeltaLakeSnapshotCacheSizeElements {#deltalakesnapshotcachesizeelements}

Taille, en nombre d’éléments, du cache d’instantanés DeltaLake.

### DestroyAggregatesThreads {#destroyaggregatesthreads}

Nombre de threads du pool de threads dédié à la destruction des états d&#39;agrégation.

### DestroyAggregatesThreadsActive {#destroyaggregatesthreadsactive}

Nombre de threads du pool de threads de destruction des états d’agrégation en train d’exécuter une tâche.

### DestroyAggregatesThreadsScheduled {#destroyaggregatesthreadsscheduled}

Nombre de jobs en attente ou actifs dans le pool de threads chargé de détruire les états d’agrégation.

### DictCacheRequests {#dictcacherequests}

Nombre de requêtes en cours adressées aux sources de données des dictionnaires de type cache.

### DiskConnectionsStored {#diskconnectionsstored}

Nombre total de sessions stockées dans le pool de sessions associé aux disques

### DiskConnectionsTotal {#diskconnectionstotal}

Nombre total de sessions : stockées dans le pool et actuellement utilisées pour les disques

### DiskObjectStorageAsyncThreads {#diskobjectstorageasyncthreads}

Métrique obsolète, n’affiche aucune valeur.

### DiskObjectStorageAsyncThreadsActive {#diskobjectstorageasyncthreadsactive}

Métrique obsolète, ne renvoie rien.

### DiskObjectStorageCopyObjectThreads {#diskobjectstoragecopyobjectthreads}

Nombre de threads du pool de threads utilisé pour paralléliser les appels `copyObjectToAnotherObjectStorage` au sein des transactions du disque de stockage objet

### DiskObjectStorageCopyObjectThreadsActive {#diskobjectstoragecopyobjectthreadsactive}

Nombre de threads du pool de threads de copie du stockage d’objets sur disque en cours d’exécution d’une tâche

### DiskObjectStorageCopyObjectThreadsScheduled {#diskobjectstoragecopyobjectthreadsscheduled}

Nombre de tâches en attente ou en cours d’exécution dans le pool de threads de copie du stockage objet sur disque

### DiskPlainRewritableAzureDirectoryMapSize {#diskplainrewritableazuredirectorymapsize}

Nombre d’entrées de correspondance entre chemins locaux et distants dans la map en mémoire &#39;plain&#95;rewritable&#39; pour AzureObjectStorage.

### DiskPlainRewritableAzureFileCount {#diskplainrewritableazurefilecount}

Nombre d’entrées de fichiers dans la map en mémoire &#39;plain&#95;rewritable&#39; d’AzureObjectStorage.

### DiskPlainRewritableLocalDirectoryMapSize {#diskplainrewritablelocaldirectorymapsize}

Nombre d’entrées de chemins locaux vers distants dans la `map` en mémoire `plain_rewritable` de LocalObjectStorage.

### DiskPlainRewritableLocalFileCount {#diskplainrewritablelocalfilecount}

Nombre d’entrées de fichiers dans la map en mémoire &#39;plain&#95;rewritable&#39; de LocalObjectStorage.

### DiskPlainRewritableS3DirectoryMapSize {#diskplainrewritables3directorymapsize}

Nombre d’entrées de correspondance de chemins locaux vers des chemins distants dans la map en mémoire &#39;plain&#95;rewritable&#39; de S3ObjectStorage.

### DiskPlainRewritableS3FileCount {#diskplainrewritables3filecount}

Nombre d&#39;entrées de fichier dans la map en mémoire &#39;plain&#95;rewritable&#39; de S3ObjectStorage.

### DiskS3NoSuchKeyErrors {#disks3nosuchkeyerrors}

Le nombre d’erreurs `NoSuchKey` qui se produisent lors de la lecture de données depuis le stockage cloud S3 via les disques ClickHouse.

### DiskSpaceReservedForMerge {#diskspacereservedformerge}

Espace disque réservé aux fusions en arrière-plan en cours. Il est légèrement supérieur à la taille totale des parties en cours de fusion.

### DistrCacheAllocatedConnections {#distrcacheallocatedconnections}

Nombre de connexions actuellement allouées au pool de connexions du Distributed Cache

### DistrCacheBorrowedConnections {#distrcacheborrowedconnections}

Nombre actuel de connexions empruntées au pool de connexions de Distributed Cache

### DistrCacheOpenedConnections {#distrcacheopenedconnections}

Nombre de connexions ouvertes à Distributed Cache

### DistrCacheReadBuffers {#distrcachereadbuffers}

Nombre de buffers de lecture du Distributed Cache

### DistrCacheReadRequests {#distrcachereadrequests}

Nombre de requêtes de lecture exécutées sur Distributed Cache

### DistrCacheRegisteredServers {#distrcacheregisteredservers}

Nombre de serveurs enregistrés du Distributed Cache

### DistrCacheRegisteredServersCurrentAZ {#distrcacheregisteredserverscurrentaz}

Nombre de serveurs du Distributed Cache enregistrés dans l’AZ actuelle

### DistrCacheServerConnections {#distrcacheserverconnections}

Nombre de connexions ouvertes entre Distributed Cache et le serveur ClickHouse

### DistrCacheServerRegistryConnections {#distrcacheserverregistryconnections}

Nombre de connexions actives ouvertes par Distributed Cache vers le serveur ClickHouse (non marquées comme annulées en raison d’une limite)

### DistrCacheServerS3CachedClients {#distrcacheservers3cachedclients}

Nombre de clients S3 mis en cache dans le Distributed Cache

### DistrCacheSharedLimitCount {#distrcachesharedlimitcount}

Nombre de connexions ouvertes en fonction de DistributedCache::ConnectionPool::SharedLimit

### DistrCacheUsedConnections {#distrcacheusedconnections}

Nombre de connexions actuellement utilisées avec Distributed Cache

### DistrCacheWriteBuffers {#distrcachewritebuffers}

Nombre de tampons d’écriture du Distributed Cache

### DistrCacheWriteRequests {#distrcachewriterequests}

Nombre de requêtes d’écriture exécutées sur le Distributed Cache

### DistributedBytesToInsert {#distributedbytestoinsert}

Nombre d’octets en attente de traitement pour une insertion asynchrone dans des tables Distributed. Le nombre d’octets de chaque segment est additionné.

### DistributedFilesToInsert {#distributedfilestoinsert}

Nombre de fichiers en attente de traitement pour l’insertion asynchrone dans les tables Distributed. Le nombre de fichiers de chaque segment est cumulé.

### DistributedIndexAnalysisThreads {#distributedindexanalysisthreads}

Nombre de threads dans le pool de threads de l’analyse d’index distribuée.

### DistributedIndexAnalysisThreadsActive {#distributedindexanalysisthreadsactive}

Nombre de threads du pool de threads de l’analyse d’index distribuée qui exécutent une tâche.

### DistributedIndexAnalysisThreadsScheduled {#distributedindexanalysisthreadsscheduled}

Nombre de tâches en file d’attente ou actives dans le pool de threads d’analyse distribuée des index.

### DistributedInsertThreads {#distributedinsertthreads}

Nombre de threads utilisés pour un INSERT dans une table Distributed.

### DistributedInsertThreadsActive {#distributedinsertthreadsactive}

Nombre de threads utilisés pour les INSERT dans Distributed et exécutant une tâche.

### DistributedInsertThreadsScheduled {#distributedinsertthreadsscheduled}

Nombre de jobs en file d’attente ou actifs utilisés pour les INSERT dans Distributed.

### DistributedSend {#distributedsend}

Nombre de connexions aux serveurs distants qui envoient des données insérées dans des tables Distributed, en mode synchrone comme asynchrone.

### DNSAddressesCacheBytes {#dnsaddressescachebytes}

Taille du cache d’adresses DNS en octets

### DNSAddressesCacheSize {#dnsaddressescachesize}

Nombre d’adresses DNS mises en cache

### DNSHostsCacheBytes {#dnshostscachebytes}

Taille du cache des hôtes DNS, en octets

### DNSHostsCacheSize {#dnshostscachesize}

Nombre d’hôtes DNS en cache

### DropDistributedCacheThreads {#dropdistributedcachethreads}

Nombre de threads dans le threadpool de la requête `drop distributed cache`.

### DropDistributedCacheThreadsActive {#dropdistributedcachethreadsactive}

Nombre de threads actifs dans le pool de threads de la requête drop distributed cache.

### DropDistributedCacheThreadsScheduled {#dropdistributedcachethreadsscheduled}

Nombre de jobs en file d’attente ou actifs dans le threadpool pour l’opération drop distributed cache.

### EncryptionHeaderCacheBytes {#encryptionheadercachebytes}

Taille totale, en octets, du cache d’en-têtes de chiffrement

### EncryptionHeaderCacheEntries {#encryptionheadercacheentries}

Nombre total d&#39;entrées dans le cache des en-têtes de chiffrement

### EphemeralNode {#ephemeralnode}

Nombre de nœuds éphémères présents dans ZooKeeper.

### ExchangeServerThreads {#exchangeserverthreads}

Nombre de threads dans le pool de threads chargé du handshake du serveur d’échange distribué.

### ExchangeServerThreadsActive {#exchangeserverthreadsactive}

Nombre de threads du pool de threads de handshake du serveur d’échange distribué en train d’exécuter une tâche.

### ExchangeServerThreadsScheduled {#exchangeserverthreadsscheduled}

Nombre de jobs en attente ou actifs dans le pool de threads de handshake du serveur d’échange distribué.

### FiberStackBytes {#fiberstackbytes}

Taille cumulée des piles de fibres actuellement allouées.

### FiberStacks {#fiberstacks}

Nombre de piles de fibres actuellement allouées (les fibres sont utilisées pour la communication asynchrone avec les répliques distantes).

### FilesystemCacheDelayedCleanupElements {#filesystemcachedelayedcleanupelements}

Éléments de la file d’attente de nettoyage en arrière-plan du cache du système de fichiers

### FilesystemCacheDownloadQueueElements {#filesystemcachedownloadqueueelements}

Éléments du cache du système de fichiers en file d&#39;attente de téléchargement

### FilesystemCacheDropCacheThreads {#filesystemcachedropcachethreads}

Nombre de threads dans le pool du cache du système de fichiers dédiés à la suppression parallèle des clés lors d&#39;un SYSTEM DROP FILESYSTEM CACHE

### FilesystemCacheDropCacheThreadsActive {#filesystemcachedropcachethreadsactive}

Nombre de threads exécutant une task dans le pool de suppression des clés du cache du système de fichiers

### FilesystemCacheDropCacheThreadsScheduled {#filesystemcachedropcachethreadsscheduled}

Nombre de jobs en file d&#39;attente ou actifs dans le pool de removal des clés du cache du système de fichiers

### FilesystemCacheElements {#filesystemcacheelements}

Éléments du cache du système de fichiers (segments de fichier)

### FilesystemCacheEvictionThreads {#filesystemcacheevictionthreads}

Nombre de threads du pool d’éviction (suppression) en arrière-plan du cache du système de fichiers

### FilesystemCacheEvictionThreadsActive {#filesystemcacheevictionthreadsactive}

Nombre de threads du pool d’éviction en arrière-plan du cache du système de fichiers qui exécutent une tâche

### FilesystemCacheEvictionThreadsScheduled {#filesystemcacheevictionthreadsscheduled}

Nombre de jobs en file d’attente ou en cours d’exécution dans le pool d’éviction en arrière-plan du cache du système de fichiers

### FilesystemCacheHoldFileSegments {#filesystemcacheholdfilesegments}

Segments de fichier du cache du système de fichiers actuellement retenus comme non libérables

### FilesystemCacheInvalidatedElements {#filesystemcacheinvalidatedelements}

Nombre d’entrées invalidées (de taille nulle, en attente de suppression) dans la file d’attente prioritaire du cache du système de fichiers

### FilesystemCacheKeys {#filesystemcachekeys}

Nombre de clés du cache du système de fichiers

### FilesystemCacheOvercommitUsers {#filesystemcacheovercommitusers}

Nombre d’utilisateurs suivis par la politique d’éviction avec overcommit du cache du système de fichiers

### Éléments de la file d’attente prioritaire du cache du système de fichiers {#filesystemcachepriorityqueueelements}

Nombre total d’éléments dans la file d’attente prioritaire du cache du système de fichiers, y compris les éléments invalidés en attente de suppression

### FilesystemCacheReadBuffers {#filesystemcachereadbuffers}

Nombre de tampons du cache actifs

### FilesystemCacheReserveThreads {#filesystemcachereservethreads}

Nombre de threads tentant de réserver de l’espace dans le cache

### FilesystemCacheSize {#filesystemcachesize}

Taille du cache du système de fichiers, en octets

### FilesystemCacheSizeLimit {#filesystemcachesizelimit}

Taille maximale du cache du système de fichiers, en octets

### FilteringMarksWithPrimaryKey {#filteringmarkswithprimarykey}

Nombre de threads effectuant actuellement le filtrage des plages de marks selon la clé primaire

### FilteringMarksWithSecondaryKeys {#filteringmarkswithsecondarykeys}

Nombre de threads effectuant actuellement le filtrage des intervalles de marks à l’aide de clés secondaires

### FormatParsingThreads {#formatparsingthreads}

Nombre de threads dans le pool de threads utilisé pour l’analyse syntaxique des entrées.

### FormatParsingThreadsActive {#formatparsingthreadsactive}

Nombre de threads du pool de threads utilisé pour l’analyse des données d’entrée qui exécutent une tâche.

### FormatParsingThreadsScheduled {#formatparsingthreadsscheduled}

Nombre de jobs en file d’attente ou en cours d’exécution dans le pool de threads utilisé pour l’analyse syntaxique des données d’entrée.

### FreezePartThreads {#freezepartthreads}

Nombre de threads du pool de threads utilisé pour geler les data parts.

### FreezePartThreadsActive {#freezepartthreadsactive}

Nombre de threads actifs du threadpool dédiés au gel des data parts.

### FreezePartThreadsScheduled {#freezepartthreadsscheduled}

Nombre de jobs en file d’attente ou en cours d’exécution dans le pool de threads chargé de figer les data parts.

### GlobalThread {#globalthread}

Nombre de threads dans le pool de threads global.

### GlobalThreadActive {#globalthreadactive}

Nombre de threads du pool de threads global exécutant une tâche.

### GlobalThreadScheduled {#globalthreadscheduled}

Nombre de jobs en file d&#39;attente ou en cours d&#39;exécution dans le pool global de threads.

### HashedDictionaryThreads {#hasheddictionarythreads}

Nombre de threads du pool de threads HashedDictionary.

### HashedDictionaryThreadsActive {#hasheddictionarythreadsactive}

Nombre de threads du pool de threads HashedDictionary exécutant une tâche.

### HashedDictionaryThreadsScheduled {#hasheddictionarythreadsscheduled}

Nombre de jobs en file d&#39;attente ou en cours d&#39;exécution dans le pool de threads HashedDictionary.

### HiveFilesCacheBytes {#hivefilescachebytes}

Taille du cache Hive en octets

### HiveFilesCacheFiles {#hivefilescachefiles}

Nombre de fichiers en cache dans le cache Hive

### HiveMetadataFilesCacheBytes {#hivemetadatafilescachebytes}

Taille du cache des métadonnées Hive, en octets

### HiveMetadataFilesCacheFiles {#hivemetadatafilescachefiles}

Nombre de fichiers mis en cache dans le cache de métadonnées Hive

### HTTPConnection {#httpconnection}

Nombre de connexions au serveur HTTP

### HTTPConnectionsStored {#httpconnectionsstored}

Nombre total de sessions stockées dans le pool de sessions des hôtes HTTP

### HTTPConnectionsTotal {#httpconnectionstotal}

Nombre total de toutes les sessions : celles stockées dans le pool et celles actuellement utilisées pour les hôtes HTTP

### IcebergCatalogThreads {#icebergcatalogthreads}

Nombre de threads du pool de threads IcebergCatalog.

### IcebergCatalogThreadsActive {#icebergcatalogthreadsactive}

Nombre de threads du pool de threads IcebergCatalog qui exécutent une tâche.

### IcebergCatalogThreadsScheduled {#icebergcatalogthreadsscheduled}

Nombre de jobs en attente ou actifs dans le pool de threads IcebergCatalog.

### IcebergCompactionCleanupTasks {#icebergcompactioncleanuptasks}

Nombre de tasks en arrière-plan chargées de supprimer les fichiers de données rendus obsolètes par la compaction Iceberg, qui sont planifiées ou encore conservées par les tables Iceberg.

### IcebergCompactionMerge {#icebergcompactionmerge}

Nombre de fusions de compaction Iceberg en cours d&#39;exécution en arrière-plan.

### IcebergCompactionSnapshots {#icebergcompactionsnapshots}

Nombre de compactions Iceberg.

### IcebergCompactionSourceFiles {#icebergcompactionsourcefiles}

Nombre de fichiers sources en entrée (fichiers de données et position delete files) participant aux fusions de compaction Iceberg en cours d&#39;exécution.

### IcebergCompactionThreads {#icebergcompactionthreads}

Nombre de threads du pool de threads IcebergCompaction.

### IcebergCompactionThreadsActive {#icebergcompactionthreadsactive}

Nombre de threads du pool de threads IcebergCompaction qui exécutent une tâche.

### IcebergCompactionThreadsScheduled {#icebergcompactionthreadsscheduled}

Nombre de jobs en file d’attente ou actifs dans le pool de threads IcebergCompaction.

### IcebergManifestDecodeThreads {#icebergmanifestdecodethreads}

Nombre de threads du thread pool utilisé pour décoder les fichiers manifest de données Iceberg.

### IcebergManifestDecodeThreadsActive {#icebergmanifestdecodethreadsactive}

Nombre de threads exécutant une task dans le thread pool utilisé pour le décodage des fichiers manifest de données Iceberg.

### IcebergManifestDecodeThreadsScheduled {#icebergmanifestdecodethreadsscheduled}

Nombre de jobs en file d&#39;attente ou actifs dans le thread pool utilisé pour décoder les fichiers manifest de données Iceberg.

### IcebergMetadataFilesCacheBytes {#icebergmetadatafilescachebytes}

Taille du cache des métadonnées Iceberg, en octets

### IcebergMetadataFilesCacheFiles {#icebergmetadatafilescachefiles}

Nombre de fichiers mis en cache dans le cache de métadonnées Iceberg

### IcebergSchedulePoolSize {#icebergschedulepoolsize}

Limite du nombre de tâches dans le pool de planification d’arrière-plan pour les tables Iceberg.

### IcebergSchedulePoolTask {#icebergschedulepooltask}

Nombre de tâches dans le pool d’ordonnancement en arrière-plan pour les tables Iceberg.

### IDiskCopierThreads {#idiskcopierthreads}

Nombre de threads utilisés pour copier des données entre des disques de types différents.

### IDiskCopierThreadsActive {#idiskcopierthreadsactive}

Nombre de threads exécutant une tâche de copie de données entre des disques de types différents.

### IDiskCopierThreadsScheduled {#idiskcopierthreadsscheduled}

Nombre de tâches en attente ou en cours pour la copie de données entre des disques de types différents.

### IndexMarkCacheBytes {#indexmarkcachebytes}

Taille totale, en octets, du cache de mark pour les index secondaires

### IndexMarkCacheFiles {#indexmarkcachefiles}

Nombre total de fichiers de marks mis en cache dans le cache de marks pour les indices secondaires

### IndexUncompressedCacheBytes {#indexuncompressedcachebytes}

Taille totale, en octets, du cache non compressé pour les index secondaires. Le cache non compressé n’améliore généralement pas les performances et doit donc être évité dans la plupart des cas

### IndexUncompressedCacheCells {#indexuncompressedcachecells}

Nombre total d’entrées dans le cache non compressé des index secondaires. Chaque entrée représente un bloc de données décompressé. Le cache non compressé n’améliore généralement pas les performances et doit donc être évité dans la plupart des cas

### InterserverConnection {#interserverconnection}

Nombre de connexions depuis d&#39;autres répliques pour récupérer des parts

### IOPrefetchThreads {#ioprefetchthreads}

Nombre de threads dans le pool de threads de prélecture des E/S.

### IOPrefetchThreadsActive {#ioprefetchthreadsactive}

Nombre de threads du pool de threads de prélecture d’E/S qui exécutent une tâche.

### IOPrefetchThreadsScheduled {#ioprefetchthreadsscheduled}

Nombre de jobs en file d’attente ou actifs dans le pool de threads de prélecture des E/S.

### IOThreads {#iothreads}

Nombre de threads du pool de threads IO.

### IOThreadsActive {#iothreadsactive}

Nombre de threads du pool de threads d’E/S qui exécutent une tâche.

### IOThreadsScheduled {#iothreadsscheduled}

Nombre de jobs en file d’attente ou actifs dans le pool de threads d’E/S.

### IOUringInFlightEvents {#iouringinflightevents}

Nombre de SQE io&#95;uring en cours

### IOUringPendingEvents {#iouringpendingevents}

Nombre de SQE io&#95;uring en attente de soumission

### IOWriterThreads {#iowriterthreads}

Nombre de threads du pool de threads d’écriture d’E/S.

### IOWriterThreadsActive {#iowriterthreadsactive}

Nombre de threads du pool de threads d’écriture d’E/S qui exécutent une tâche.

### IOWriterThreadsScheduled {#iowriterthreadsscheduled}

Nombre de jobs en file d’attente ou en cours d’exécution dans le pool de threads d’écriture d’E/S.

### IsServerShuttingDown {#isservershuttingdown}

Indique si le serveur est en cours d’arrêt : 0 = non, 1 = oui

### Partitions Kafka assignées {#kafkaassignedpartitions}

Nombre de partitions actuellement assignées aux tables Kafka

### KafkaBackgroundReads {#kafkabackgroundreads}

Nombre d&#39;opérations de lecture en arrière-plan en cours (alimentant des vues matérialisées à partir de Kafka)

### KafkaConsumers {#kafkaconsumers}

Nombre de consommateurs Kafka actifs

### KafkaConsumersInUse {#kafkaconsumersinuse}

Nombre de consommateurs actuellement utilisés pour les lectures directes ou en arrière-plan

### KafkaConsumersWithAssignment {#kafkaconsumerswithassignment}

Nombre de consommateurs Kafka actifs ayant des partitions assignées.

### KafkaLibrdkafkaThreads {#kafkalibrdkafkathreads}

Nombre de threads actifs de librdkafka

### KafkaProducers {#kafkaproducers}

Nombre de producteurs Kafka actifs créés

### KafkaWrites {#kafkawrites}

Nombre d&#39;insertions vers Kafka actuellement en cours

### KeeperAliveConnections {#keeperaliveconnections}

Nombre de connexions actives

### KeeperBlockCacheBlocks {#keeperblockcacheblocks}

Nombre de blocs dans le cache de blocs du stockage Keeper.

### KeeperBlockCacheBytes {#keeperblockcachebytes}

Taille totale, en octets, des blocs dans le cache de blocs du stockage Keeper.

### KeeperChangelogReadAheadThreads {#keeperchangelogreadaheadthreads}

Nombre de threads du threadpool dédié aux tasks de remplissage par lecture anticipée du changelog Keeper.

### KeeperChangelogReadAheadThreadsActive {#keeperchangelogreadaheadthreadsactive}

Nombre de threads actifs dans le threadpool dédié aux tasks de remplissage par lecture anticipée du changelog Keeper.

### KeeperChangelogReadAheadThreadsScheduled {#keeperchangelogreadaheadthreadsscheduled}

Nombre de jobs en file d&#39;attente ou actifs dans le threadpool pour les tasks de remplissage anticipé (read-ahead) du changelog de Keeper.

### KeeperChangelogStartupReadThreads {#keeperchangelogstartupreadthreads}

Nombre de threads du threadpool dédié aux lectures parallèles du changelog Keeper au démarrage.

### KeeperChangelogStartupReadThreadsActive {#keeperchangelogstartupreadthreadsactive}

Nombre de threads actifs dans le threadpool dédié aux lectures parallèles du changelog Keeper au démarrage.

### KeeperChangelogStartupReadThreadsScheduled {#keeperchangelogstartupreadthreadsscheduled}

Nombre de jobs en file d&#39;attente ou actifs dans le threadpool dédié aux lectures parallèles du changelog Keeper au démarrage.

### KeeperContainerNodes {#keepercontainernodes}

Nombre de nœuds conteneurs actuellement stockés dans Keeper.

### KeeperOutstandingRequests {#keeperoutstandingrequests}

Nombre de requêtes en attente

### KeeperReadThreads {#keeperreadthreads}

Nombre de threads dans le threadpool de lecture du serveur Keeper.

### KeeperReadThreadsActive {#keeperreadthreadsactive}

Nombre de threads actifs dans le threadpool dédié aux lectures du serveur Keeper.

### KeeperReadThreadsScheduled {#keeperreadthreadsscheduled}

Nombre de jobs en file d’attente ou actifs dans le threadpool pour les lectures du serveur Keeper. Metric dénuée de sens : les tâches de lecture réelles de ce thread pool sont planifiées par un autre mécanisme.

### KeeperTTLNodes {#keeperttlnodes}

Nombre de nœuds avec un TTL défini actuellement stockés dans Keeper.

### LicenseRemainingSeconds {#licenseremainingseconds}

Nombre de secondes restantes de validité de la licence.

### LocalThread {#localthread}

Obsolète. Nombre de threads dans les pools de threads locaux. Les threads de ces pools sont issus du pool global de threads.

### LocalThreadActive {#localthreadactive}

Obsolète. Nombre de threads des pools de threads locaux exécutant une tâche.

### LocalThreadScheduled {#localthreadscheduled}

Obsolète. Nombre de jobs en attente ou actifs dans les pools de threads locaux.

### MarkCacheBytes {#markcachebytes}

Taille totale du cache des marks, en octets

### MarkCacheFiles {#markcachefiles}

Nombre total de fichiers de mark stockés dans le cache des marques

### MarksLoaderThreads {#marksloaderthreads}

Nombre de threads du pool de threads pour le chargement des marks.

### MarksLoaderThreadsActive {#marksloaderthreadsactive}

Nombre de threads du pool de chargement des marks qui exécutent une tâche.

### MarksLoaderThreadsScheduled {#marksloaderthreadsscheduled}

Nombre de jobs en file d’attente ou actifs dans le pool de threads pour le chargement des marks.

### MaxAllocatedEphemeralLockSequentialNumber {#maxallocatedephemerallocksequentialnumber}

Le plus grand numéro séquentiel attribué aux znodes de verrouillage éphémères dans ZooKeeper. Il est principalement déterminé par les numéros de blocs.

### MaxDDLEntryID {#maxddlentryid}

Entrée DDL maximale traitée par DDLWorker.

### MaxPushedDDLEntryID {#maxpushedddlentryid}

Entrée DDL maximale de DDLWorker envoyée à ZooKeeper.

### Réservations mémoire approuvées {#memoryreservationapproved}

Nombre total d’octets actuellement alloués pour des réservations mémoire approuvées

### MemoryReservationDemand {#memoryreservationdemand}

Nombre total d’octets des demandes d’augmentation de réservation de mémoire en attente

### MemoryTracking {#memorytracking}

Quantité totale de mémoire (en octets) allouée par le serveur.

### MemoryTrackingUncorrected {#memorytrackinguncorrected}

Quantité totale de mémoire (en octets) allouée par le serveur et non corrigée par le RSS.

### Fusion {#merge}

Nombre de fusions en arrière-plan en cours

### MergeJoinBlocksCacheBytes {#mergejoinblockscachebytes}

Nombre total d’octets utilisés par les blocs mis en cache dans MergeJoin

### MergeJoinBlocksCacheCount {#mergejoinblockscachecount}

Nombre total de blocs en cache dans MergeJoin

### MergeParts {#mergeparts}

Nombre de parts source participant aux fusions en arrière-plan en cours

### MergesMutationsMemoryTracking {#mergesmutationsmemorytracking}

Quantité totale de mémoire (en octets) allouée aux tâches en arrière-plan (fusions et mutations).

### MergeTreeAllRangesAnnouncementsSent {#mergetreeallrangesannouncementssent}

Nombre actuel d’annonces en cours d’envoi du serveur distant vers le serveur initiateur concernant l’ensemble des data parts (pour les tables MergeTree). Mesuré côté serveur distant.

### MergeTreeBackgroundExecutorThreads {#mergetreebackgroundexecutorthreads}

Nombre de threads du pool de threads MergeTreeBackgroundExecutor.

### MergeTreeBackgroundExecutorThreadsActive {#mergetreebackgroundexecutorthreadsactive}

Nombre de threads du pool de threads MergeTreeBackgroundExecutor exécutant une tâche.

### MergeTreeBackgroundExecutorThreadsScheduled {#mergetreebackgroundexecutorthreadsscheduled}

Nombre de jobs en file d&#39;attente ou actifs dans le pool de threads MergeTreeBackgroundExecutor.

### MergeTreeDataSelectExecutorThreads {#mergetreedataselectexecutorthreads}

Nombre de threads du pool de threads MergeTreeDataSelectExecutor.

### MergeTreeDataSelectExecutorThreadsActive {#mergetreedataselectexecutorthreadsactive}

Nombre de threads du pool de threads MergeTreeDataSelectExecutor exécutant une tâche.

### MergeTreeDataSelectExecutorThreadsScheduled {#mergetreedataselectexecutorthreadsscheduled}

Nombre de jobs actifs ou en file d’attente dans le pool de threads MergeTreeDataSelectExecutor.

### MergeTreeFetchPartitionThreads {#mergetreefetchpartitionthreads}

Nombre de threads utilisés pour ALTER TABLE FETCH PARTITION

### MergeTreeFetchPartitionThreadsActive {#mergetreefetchpartitionthreadsactive}

Nombre de threads effectuant une récupération de part via ALTER TABLE FETCH PARTITION

### MergeTreeFetchPartitionThreadsScheduled {#mergetreefetchpartitionthreadsscheduled}

Nombre de récupérations de parts en attente ou en cours via ALTER TABLE FETCH PARTITION

### MergeTreeOutdatedPartsLoaderThreads {#mergetreeoutdatedpartsloaderthreads}

Nombre de threads dans le pool de threads utilisé pour charger les data parts obsolètes.

### MergeTreeOutdatedPartsLoaderThreadsActive {#mergetreeoutdatedpartsloaderthreadsactive}

Nombre de threads actifs dans le pool de threads dédié au chargement des data parts obsolètes.

### MergeTreeOutdatedPartsLoaderThreadsScheduled {#mergetreeoutdatedpartsloaderthreadsscheduled}

Nombre de jobs en file d’attente ou en cours d’exécution dans le threadpool pour le chargement des data parts obsolètes.

### Threads du nettoyeur de parts de MergeTree {#mergetreepartscleanerthreads}

Nombre de threads dans le pool de threads du nettoyeur de parts de MergeTree.

### MergeTreePartsCleanerThreadsActive {#mergetreepartscleanerthreadsactive}

Nombre de threads du pool de threads du nettoyeur de parts MergeTree qui exécutent une tâche.

### MergeTreePartsCleanerThreadsScheduled {#mergetreepartscleanerthreadsscheduled}

Nombre de tâches en file d’attente ou en cours d’exécution dans le pool de threads du nettoyeur de parts MergeTree.

### MergeTreePartsLoaderThreads {#mergetreepartsloaderthreads}

Nombre de threads dans le pool de threads du chargeur de parts de MergeTree.

### MergeTreePartsLoaderThreadsActive {#mergetreepartsloaderthreadsactive}

Nombre de threads du pool de threads du chargeur de parts MergeTree qui exécutent une tâche.

### MergeTreePartsLoaderThreadsScheduled {#mergetreepartsloaderthreadsscheduled}

Nombre de tâches en file d’attente ou en cours d’exécution dans le pool de threads du chargeur de parts de MergeTree.

### MergeTreeReadTaskRequestsSent {#mergetreereadtaskrequestssent}

Nombre actuel de requêtes de rappel en cours du serveur distant vers le serveur initiateur pour choisir la tâche de lecture (pour les tables MergeTree). Mesuré côté serveur distant.

### MergeTreeSnapshotCommitThreads {#mergetreesnapshotcommitthreads}

Nombre de threads utilisés pour finaliser l’instantané

### MergeTreeSnapshotCommitThreadsActive {#mergetreesnapshotcommitthreadsactive}

Nombre de threads actifs utilisés pour finaliser l’instantané

### MergeTreeSnapshotCommitThreadsScheduled {#mergetreesnapshotcommitthreadsscheduled}

Nombre de threads planifiés utilisés pour la finalisation de l’instantané

### MergeTreeSubcolumnsReaderThreads {#mergetreesubcolumnsreaderthreads}

Nombre de threads dans le pool de threads utilisé pour lire les sous-colonnes dans MergeTree.

### MergeTreeSubcolumnsReaderThreadsActive {#mergetreesubcolumnsreaderthreadsactive}

Nombre de threads du pool de threads utilisé par MergeTree pour la lecture des sous-colonnes qui exécutent une tâche.

### MergeTreeSubcolumnsReaderThreadsScheduled {#mergetreesubcolumnsreaderthreadsscheduled}

Nombre de tâches en attente ou en cours d’exécution dans le pool de threads utilisé pour la lecture des sous-colonnes dans MergeTree.

### MergeTreeUnexpectedPartsLoaderThreads {#mergetreeunexpectedpartsloaderthreads}

Nombre de threads dans le threadpool pour le chargement des data parts « Unexpected ».

### MergeTreeUnexpectedPartsLoaderThreadsActive {#mergetreeunexpectedpartsloaderthreadsactive}

Nombre de threads actifs dans le pool de threads utilisé pour charger les parties de données Unexpected.

### MergeTreeUnexpectedPartsLoaderThreadsScheduled {#mergetreeunexpectedpartsloaderthreadsscheduled}

Nombre de jobs en file d’attente ou actifs dans le pool de threads dédié au chargement des Unexpected data parts.

### MetadataFromKeeperCacheObjects {#metadatafromkeepercacheobjects}

Nombre d’objets dans le cache de métadonnées du disque.

### MMapCacheCells {#mmapcachecells}

Le nombre de fichiers ouverts avec `mmap` (mappés en mémoire). Utilisé pour les requêtes où le paramètre `local_filesystem_read_method` est défini sur `mmap`. Les fichiers ouverts avec `mmap` sont conservés dans le cache afin d&#39;éviter des flushes du TLB coûteux.

### MMappedFileBytes {#mmappedfilebytes}

Taille totale des zones de fichiers mappées en mémoire.

### MMappedFiles {#mmappedfiles}

Nombre total de fichiers mappés en mémoire.

### Déplacements {#move}

Nombre de déplacements actuellement en cours

### MySQLConnection {#mysqlconnection}

Nombre de connexions clientes via le protocole MySQL

### NamedCollection {#namedcollection}

Nombre de collections nommées

### NetworkReceive {#networkreceive}

Nombre de threads qui reçoivent des données depuis le réseau. Seules les interactions réseau liées à ClickHouse sont prises en compte, à l’exclusion de celles des bibliothèques tierces.

### NetworkSend {#networksend}

Nombre de threads qui envoient des données sur le réseau. Seules les interactions réseau liées à ClickHouse sont incluses, à l’exclusion de celles des bibliothèques tierces.

### ObjectStorageAzureThreads {#objectstorageazurethreads}

Nombre de threads du pool de threads AzureObjectStorage.

### ObjectStorageAzureThreadsActive {#objectstorageazurethreadsactive}

Nombre de threads du pool de threads AzureObjectStorage exécutant une tâche.

### ObjectStorageAzureThreadsScheduled {#objectstorageazurethreadsscheduled}

Nombre de jobs en file d’attente ou en cours d’exécution dans le pool de threads AzureObjectStorage.

### ObjectStorageQueueMetadataCacheSizeBytes {#objectstoragequeuemetadatacachesizebytes}

Taille, en octets, du cache de métadonnées d’ObjectStorageQueue.

### ObjectStorageQueueMetadataCacheSizeElements {#objectstoragequeuemetadatacachesizeelements}

Taille, en nombre d’éléments, du cache de métadonnées d’ObjectStorageQueue.

### ObjectStorageQueueRegisteredServers {#objectstoragequeueregisteredservers}

Nombre de serveurs enregistrés dans StorageS3(Azure)Queue

### ObjectStorageQueueShutdownThreads {#objectstoragequeueshutdownthreads}

Nombre de threads dans le pool d’arrêt de la file d’attente du stockage objet.

### ObjectStorageQueueShutdownThreadsActive {#objectstoragequeueshutdownthreadsactive}

Nombre de threads du pool d’arrêt de la file d’attente du stockage d’objets exécutant une tâche.

### ObjectStorageQueueShutdownThreadsScheduled {#objectstoragequeueshutdownthreadsscheduled}

Nombre de tâches actives ou en attente dans le pool d&#39;arrêt de la file d&#39;attente du stockage objet.

### ObjectStorageS3Threads {#objectstorages3threads}

Nombre de threads du pool de threads S3ObjectStorage.

### ObjectStorageS3ThreadsActive {#objectstorages3threadsactive}

Nombre de threads du pool S3ObjectStorage qui exécutent une tâche.

### ObjectStorageS3ThreadsScheduled {#objectstorages3threadsscheduled}

Nombre de jobs en attente ou actifs dans le pool de threads S3ObjectStorage.

### OpenFileForRead {#openfileforread}

Nombre de fichiers ouverts en lecture

### OpenFileForWrite {#openfileforwrite}

Nombre de fichiers ouverts pour l’écriture

### OutdatedPartsLoadingThreads {#outdatedpartsloadingthreads}

Nombre de threads du pool de threads utilisés pour charger les data parts obsolètes.

### OutdatedPartsLoadingThreadsActive {#outdatedpartsloadingthreadsactive}

Nombre de threads actifs dans le pool de threads chargé du chargement des data parts obsolètes.

### OutdatedPartsLoadingThreadsScheduled {#outdatedpartsloadingthreadsscheduled}

Nombre de jobs en file d’attente ou actifs dans le pool de threads pour le chargement des data parts obsolètes.

### PageCacheBytes {#pagecachebytes}

Taille totale, en octets, du cache de pages en espace utilisateur

### PageCacheCells {#pagecachecells}

Nombre total d’entrées du cache de pages en espace utilisateur

### PaimonMetadataFilesCacheBytes {#paimonmetadatafilescachebytes}

Taille, en octets, du cache des métadonnées de Paimon

### PaimonMetadataFilesCacheFiles {#paimonmetadatafilescachefiles}

Nombre de fichiers mis en cache dans le cache des métadonnées de Paimon

### ParallelCompressedWriteBufferThreads {#parallelcompressedwritebufferthreads}

Nombre de threads dans toutes les instances de ParallelCompressedWriteBuffer : ces threads effectuent la compression et l’écriture en parallèle

### ParallelCompressedWriteBufferWait {#parallelcompressedwritebufferwait}

Nombre de threads dans toutes les instances de ParallelCompressedWriteBuffer qui attendent actuellement qu’un tampon soit disponible pour l’écriture

### ParallelFormattingOutputFormatThreads {#parallelformattingoutputformatthreads}

Nombre de threads du pool de threads ParallelFormattingOutputFormatThreads.

### ParallelFormattingOutputFormatThreadsActive {#parallelformattingoutputformatthreadsactive}

Nombre de threads du pool de threads ParallelFormattingOutputFormatThreads exécutant une tâche.

### ParallelFormattingOutputFormatThreadsScheduled {#parallelformattingoutputformatthreadsscheduled}

Nombre de jobs en file d’attente ou en cours d’exécution dans le pool de threads ParallelFormattingOutputFormatThreads.

### ParallelWithQueryActiveThreads {#parallelwithqueryactivethreads}

Nombre de threads actifs dans le threadpool chargé du traitement des requêtes PARALLEL WITH.

### ParallelWithQueryScheduledThreads {#parallelwithqueryscheduledthreads}

Nombre de jobs en attente ou actifs dans le threadpool de traitement des requêtes PARALLEL WITH.

### ParallelWithQueryThreads {#parallelwithquerythreads}

Nombre de threads dans le threadpool traitant les requêtes PARALLEL WITH.

### ParquetEncoderThreads {#parquetencoderthreads}

Nombre de threads du thread pool de ParquetBlockOutputFormat.

### ParquetEncoderThreadsActive {#parquetencoderthreadsactive}

Nombre de threads du pool de threads ParquetBlockOutputFormat qui exécutent une tâche.

### ParquetEncoderThreadsScheduled {#parquetencoderthreadsscheduled}

Nombre de jobs en file d’attente ou en cours d’exécution dans le pool de threads ParquetBlockOutputFormat.

### ParquetMetadataCacheBytes {#parquetmetadatacachebytes}

Taille du cache de métadonnées Parquet, en octets

### ParquetMetadataCacheFiles {#parquetmetadatacachefiles}

Nombre de fichiers mis en cache dans le cache de métadonnées Parquet

### PartMutation {#partmutation}

Nombre de mutations (ALTER DELETE/UPDATE)

### PartsActive {#partsactive}

Partie de données active, utilisée par les requêtes SELECT actuelles et à venir.

### PartsCommitted {#partscommitted}

Déprécié. Voir PartsActive.

### PartsCompact {#partscompact}

Parts compactes.

### PartsDeleteOnDestroy {#partsdeleteondestroy}

La part a été déplacée vers un autre disque et doit être supprimée via son propre destructeur.

### PartsDeleting {#partsdeleting}

Part de données inactive avec un compteur de références d’identité ; sa suppression par le nettoyeur est en cours.

### PartsOutdated {#partsoutdated}

Part de données non active, mais pouvant encore être utilisée uniquement par les requêtes SELECT en cours, et pouvant être supprimée une fois ces requêtes terminées.

### PartsPreActive {#partspreactive}

La part se trouve dans data&#95;parts, mais n’est pas utilisée pour les requêtes SELECT.

### PartsPreCommitted {#partsprecommitted}

Obsolète. Voir PartsPreActive.

### PartsTemporary {#partstemporary}

La part est en cours de création, elle ne figure pas dans la liste data&#95;parts.

### PartsWide {#partswide}

Parts au format wide.

### PendingAsyncInsert {#pendingasyncinsert}

Nombre d’insertions asynchrones en attente de flush.

### PointInPolygonCacheBytes {#pointinpolygoncachebytes}

Taille en octets du cache de polygones constants prétraités pour la fonction `pointInPolygon`

### PointInPolygonCacheCells {#pointinpolygoncachecells}

Nombre de polygones constants prétraités en cache pour la fonction `pointInPolygon`

### PointInPolygonCacheSizeLimit {#pointinpolygoncachesizelimit}

Taille maximale, en octets, du cache des polygones constants prétraités pour la fonction `pointInPolygon` (valeur appliquée du paramètre serveur `point_in_polygon_cache_size`)

### PolygonDictionaryThreads {#polygondictionarythreads}

Nombre de threads dans le pool de threads des dictionnaires polygonaux.

### PolygonDictionaryThreadsActive {#polygondictionarythreadsactive}

Nombre de threads actifs dans le threadpool des dictionnaires polygonaux.

### Threads planifiés pour les dictionnaires polygonaux {#polygondictionarythreadsscheduled}

Nombre de jobs en file d’attente ou actifs dans le threadpool des dictionnaires polygonaux.

### PostgreSQLConnection {#postgresqlconnection}

Nombre de connexions de clients utilisant le protocole PostgreSQL

### PrimaryIndexCacheBytes {#primaryindexcachebytes}

Taille totale, en octets, du cache d’index primaire. Contient les index de clé primaire chargés à la demande lorsque `primary_key_lazy_load=1` et `use_primary_key_cache=1`. Lorsque le pool d’arènes dédié aux métadonnées MergeTree est activé (`jemalloc_merge_tree_arenas` &gt; 0 avec jemalloc), les colonnes d’index y sont allouées (`jemalloc.mergetree_arena.*`), même lorsqu’elles appartiennent à ce cache. Ne chevauche JAMAIS `system.parts.primary_key_bytes_in_memory[_allocated]` — l’index d’une part se trouve soit dans ce cache (comptabilisé ici), soit dans la part elle-même (comptabilisé là) ; jamais dans les deux. Pour obtenir la mémoire totale des index primaires sur l’ensemble des parts, additionnez les deux.

### PrimaryIndexCacheFiles {#primaryindexcachefiles}

Nombre total de fichiers d’index mis en cache dans le cache d’index primaire

### Requête {#query}

Nombre de requêtes en cours d’exécution

### QueryCacheBytes {#querycachebytes}

Taille totale du cache de requêtes, en octets

### QueryCacheEntries {#querycacheentries}

Nombre total d’entrées du cache de requêtes

### QueryConditionCacheBytes {#queryconditioncachebytes}

Taille totale, en octets, du cache des conditions de requête

### QueryConditionCacheEntries {#queryconditioncacheentries}

Nombre total d’entrées du cache des conditions de requête

### QueryNonInternal {#querynoninternal}

Nombre de requêtes non internes en cours d’exécution (requêtes lancées par les utilisateurs, hors requêtes internes de ClickHouse)

### QueryPipelineExecutorThreads {#querypipelineexecutorthreads}

Nombre de threads du pool de threads PipelineExecutor.

### QueryPipelineExecutorThreadsActive {#querypipelineexecutorthreadsactive}

Nombre de threads du pool de threads PipelineExecutor exécutant une tâche.

### QueryPipelineExecutorThreadsScheduled {#querypipelineexecutorthreadsscheduled}

Nombre de jobs en attente ou actifs dans le pool de threads PipelineExecutor.

### QueryPreempted {#querypreempted}

Nombre de tâches en cours d’exécution mises en pause et en attente en raison du paramètre &#39;priority&#39;.

### QueryRunnerThreads {#queryrunnerthreads}

Nombre de threads dans le thread pool QueryRunner.

### QueryRunnerThreadsActive {#queryrunnerthreadsactive}

Nombre de threads du thread pool QueryRunner exécutant une task.

### QueryRunnerThreadsScheduled {#queryrunnerthreadsscheduled}

Nombre de jobs en file d&#39;attente ou actifs dans le thread pool QueryRunner.

### QueryThread {#querythread}

Nombre de threads de traitement des requêtes

### Lecture {#read}

Nombre d’appels système de lecture (read, pread, io&#95;getevents, etc.) dans fly

### ReaderExecutorActive {#readerexecutoractive}

Nombre d’instances ReaderExecutor actives.

### ReaderExecutorChainedBufferBytes {#readerexecutorchainedbufferbytes}

Octets actuellement contenus dans les tampons chaînés actifs du ReaderExecutor (OwnedChainedBuffer) — mémoire de lecture active en transit, et non un total cumulatif.

### ReaderExecutorLongConnections {#readerexecutorlongconnections}

Nombre de connexions source longues (long source connections) actuellement maintenues ouvertes par le ReaderExecutor pour l&#39;optimisation des lectures séquentielles.

### ReadonlyDisks {#readonlydisks}

Nombre de disques marqués en lecture seule lors de la vérification des disques.

### ReadonlyReplica {#readonlyreplica}

Nombre de tables Replicated actuellement en lecture seule à cause d&#39;une réinitialisation après la perte de la session ZooKeeper ou d&#39;un démarrage sans ZooKeeper configuré.

### ReadTaskRequestsSent {#readtaskrequestssent}

Le nombre actuel de requêtes de rappel en cours envoyées par le serveur distant au serveur initiateur pour choisir la tâche de lecture (pour la fonction de table s3Cluster et les fonctions similaires). Mesuré du côté du serveur distant.

### Vues actualisables {#refreshableviews}

Vues matérialisées de type Number avec actualisation périodique (REFRESH)

### Actualisation des vues matérialisées {#refreshingviews}

Nombre de vues matérialisées actuellement en cours d’actualisation

### RemoteRead {#remoteread}

Nombre de lectures via le lecteur distant en cours

### ReplicaReady {#replicaready}

Indique si la réplique est prête à traiter des requêtes : 0 = non, 1 = oui

### ReplicatedChecks {#replicatedchecks}

Nombre de parties de données en cours de vérification de cohérence

### ReplicatedFetch {#replicatedfetch}

Nombre de parties de données en cours de transfert depuis une réplique

### ReplicatedSend {#replicatedsend}

Nombre de parties de données envoyées aux répliques

### RestartReplicaThreads {#restartreplicathreads}

Nombre de threads du pool de threads RESTART REPLICA.

### RestartReplicaThreadsActive {#restartreplicathreadsactive}

Nombre de threads du pool de threads RESTART REPLICA exécutant une tâche.

### RestartReplicaThreadsScheduled {#restartreplicathreadsscheduled}

Nombre de jobs en file d’attente ou en cours d’exécution dans le pool de threads RESTART REPLICA.

### RestoreThreads {#restorethreads}

Nombre de threads du pool de threads dédié à RESTORE.

### RestoreThreadsActive {#restorethreadsactive}

Nombre de threads du pool de threads RESTORE en train d’exécuter une tâche.

### RestoreThreadsScheduled {#restorethreadsscheduled}

Nombre de tâches RESTORE en file d’attente ou en cours d’exécution.

### Révision {#revision}

Révision du serveur. Il s’agit d’un numéro incrémenté à chaque version publiée ou version candidate, à l’exception des versions correctives.

### RWLockActiveReaders {#rwlockactivereaders}

Nombre de threads détenant un verrou de lecture dans le RWLock d&#39;une table.

### RWLockActiveWriters {#rwlockactivewriters}

Nombre de threads détenant un verrou d&#39;écriture sur le RWLock d&#39;une table.

### RWLockWaitingReaders {#rwlockwaitingreaders}

Nombre de threads en attente d&#39;un accès en lecture sur le RWLock d&#39;une table.

### RWLockWaitingWriters {#rwlockwaitingwriters}

Nombre de threads en attente d’un verrouillage en écriture sur le RWLock d’une table.

### S3CachedCredentialsProviders {#s3cachedcredentialsproviders}

Nombre total de fournisseurs d’identifiants mis en cache

### S3Requests {#s3requests}

Nombre de requêtes S3

### SchedulerIOReadScheduled {#schedulerioreadscheduled}

Nombre de lectures d’E/S actuellement planifiées

### SchedulerIOWriteScheduled {#scheduleriowritescheduled}

Nombre d’écritures d’E/S actuellement planifiées

### SendExternalTables {#sendexternaltables}

Nombre de connexions envoyant des données d’external tables vers des serveurs distants. Les external tables sont utilisées pour implémenter les opérateurs GLOBAL IN et GLOBAL JOIN avec des sous-requêtes Distributed.

### SendScalars {#sendscalars}

Nombre de connexions qui envoient des données de scalaires vers des serveurs distants.

### Cache de sérialisation en mémoire, en octets {#serializationcachebytesinmemory}

Taille totale du cache de sérialisation en mémoire, en octets, en incluant uniquement les valeurs

### SerializationCacheBytesInMemoryAllocated {#serializationcachebytesinmemoryallocated}

Taille totale, en octets, du cache de sérialisation, y compris les clés et le surcoût lié aux emplacements vides

### SerializationCacheCount {#serializationcachecount}

Nombre total d’entrées du cache de sérialisation

### SharedCatalogDropDetachLocalTablesErrors {#sharedcatalogdropdetachlocaltableserrors}

Nombre d’erreurs survenues lors des tentatives de suppression ou de détachement de tables locales dans Shared Catalog.

### SharedCatalogDropLocalThreads {#sharedcatalogdroplocalthreads}

Nombre de threads du threadpool dédiés à la suppression des tables locales dans Shared Catalog.

### SharedCatalogDropLocalThreadsActive {#sharedcatalogdroplocalthreadsactive}

Nombre de threads actifs dans le threadpool chargé de la suppression des tables locales dans Shared Catalog.

### SharedCatalogDropLocalThreadsScheduled {#sharedcatalogdroplocalthreadsscheduled}

Nombre de jobs en file d’attente ou en cours d’exécution dans le threadpool chargé de la suppression des tables locales dans Shared Catalog.

### SharedCatalogDropZooKeeperThreads {#sharedcatalogdropzookeeperthreads}

Nombre de threads dans le threadpool dédiés à la suppression d&#39;objets dans ZooKeeper au sein du Shared Catalog.

### SharedCatalogDropZooKeeperThreadsActive {#sharedcatalogdropzookeeperthreadsactive}

Nombre de threads actifs dans le threadpool chargé de la suppression d&#39;objets dans ZooKeeper dans Shared Catalog.

### SharedCatalogDropZooKeeperThreadsScheduled {#sharedcatalogdropzookeeperthreadsscheduled}

Nombre de jobs en file d’attente ou en cours d’exécution dans le threadpool chargé de la suppression d’objets dans ZooKeeper au sein du Shared Catalog.

### Nombre d&#39;objets dans l&#39;état courant de Shared Catalog {#sharedcatalognumberofobjectsinstate}

Nombre d&#39;objets dans l&#39;état courant de Shared Catalog.

### SharedCatalogStateApplicationThreads {#sharedcatalogstateapplicationthreads}

Nombre de threads du threadpool dédiés à l&#39;application de l&#39;état dans Shared Catalog.

### SharedCatalogStateApplicationThreadsActive {#sharedcatalogstateapplicationthreadsactive}

Nombre de threads actifs dans le threadpool d’application de l’état de Shared Catalog.

### SharedCatalogStateApplicationThreadsScheduled {#sharedcatalogstateapplicationthreadsscheduled}

Nombre de jobs en file d’attente ou actifs dans le threadpool chargé de l’application de l’état dans Shared Catalog.

### SharedDatabaseCatalogTablesInLocalDropDetachQueue {#shareddatabasecatalogtablesinlocaldropdetachqueue}

Nombre de tables dans la file d’attente des opérations locales de suppression ou de détachement dans Shared Catalog.

### SharedMergeTreeAssignedCurrentParts {#sharedmergetreeassignedcurrentparts}

Nombre de parts verrouillées par une opération de merge ou de mutation

### SharedMergeTreeBlobRefCounterNodes {#sharedmergetreeblobrefcounternodes}

Nombre de nœuds de comptage de références de blobs (un par blob de données partagé par au moins 2 parts blob-list) dans les sous-arbres Keeper `_blob_manager/refcount` partagés par le cluster, mesuré périodiquement par la task de statistiques du blob-manager et cumulé sur les disks de métadonnées Keeper du serveur.

### SharedMergeTreeBrokenCondemnedPartsInKeeper {#sharedmergetreebrokencondemnedpartsinkeeper}

Nombre d&#39;enregistrements de parts condamnés et défectueux stockés dans Keeper

### SharedMergeTreeCondemnedPartsInKeeper {#sharedmergetreecondemnedpartsinkeeper}

Nombre d’enregistrements de parts marquées pour suppression stockés dans Keeper

### SharedMergeTreeFetch {#sharedmergetreefetch}

Nombre de fetches en cours

### SharedMergeTreeMaxActiveReplicas {#sharedmergetreemaxactivereplicas}

Le nombre maximal de répliques actives enregistrées dans Keeper

### SharedMergeTreeMaxInactiveReplicas {#sharedmergetreemaxinactivereplicas}

Le nombre maximal de répliques inactives enregistrées dans Keeper

### SharedMergeTreeMaxPartitions {#sharedmergetreemaxpartitions}

Le nombre maximal de partitions enregistrées dans Keeper pour l’ensemble des tables SharedMergeTree

### SharedMergeTreeMaxReplicas {#sharedmergetreemaxreplicas}

Le nombre maximal de répliques enregistrées dans Keeper sur l’ensemble des tables. Notez qu’il ne correspond pas nécessairement à la somme de SharedMergeTreeMaxActiveReplicas et de SharedMergeTreeMaxInactiveReplicas

### SharedMergeTreeMinActiveReplicas {#sharedmergetreeminactivereplicas}

Le nombre minimal de répliques actives enregistrées dans Keeper

### SharedMergeTreeMinInactiveReplicas {#sharedmergetreemininactivereplicas}

Le nombre minimal de répliques inactives enregistrées dans Keeper

### SharedMergeTreeMinPartitions {#sharedmergetreeminpartitions}

Le nombre minimal de partitions enregistrées dans Keeper parmi toutes les tables SharedMergeTree

### SharedMergeTreeMinReplicas {#sharedmergetreeminreplicas}

Le nombre minimal de répliques enregistrées dans Keeper pour l’ensemble des tables. Notez qu’il peut ne pas être égal à la somme de SharedMergeTreeMinActiveReplicas et SharedMergeTreeMinInactiveReplicas

### SharedMergeTreeOutdatedPartsInKeeper {#sharedmergetreeoutdatedpartsinkeeper}

Nombre d’enregistrements de parts obsolètes stockés dans Keeper

### SharedMergeTreeThreads {#sharedmergetreethreads}

Nombre de threads dans les pools de threads internes de SharedMergeTree

### SharedMergeTreeThreadsActive {#sharedmergetreethreadsactive}

Nombre de threads dans les pools de threads des mécanismes internes de SharedMergeTree qui exécutent une tâche

### SharedMergeTreeThreadsScheduled {#sharedmergetreethreadsscheduled}

Nombre de threads en attente ou actifs dans les pools de threads internes de SharedMergeTree

### SharedPartColumnsSubstreamsCacheSize {#sharedpartcolumnssubstreamscachesize}

Nombre de listes distinctes de substreams de colonnes partagées entre les parts (somme sur les caches par table)

### SharedPartColumnSubstreamsEntriesCacheSize {#sharedpartcolumnsubstreamsentriescachesize}

Nombre d&#39;entrées de substream distinctes par colonne partagées entre les parts (somme des caches par table)

### SharedPartSerializationGroupsCacheSize {#sharedpartserializationgroupscachesize}

Nombre de groupes de sérialisation distincts par colonne partagés entre les parts (somme des caches par table)

### SharedPartSerializationsCacheSize {#sharedpartserializationscachesize}

Nombre de maps de sérialisations distinctes partagées entre les parts (somme des caches par table)

### StartupScriptsExecutionState {#startupscriptsexecutionstate}

État d’exécution des scripts de démarrage : 0 = non terminé, 1 = succès, 2 = échec.

### StartupSystemTablesThreads {#startupsystemtablesthreads}

Nombre de threads du pool de threads StartupSystemTables.

### StartupSystemTablesThreadsActive {#startupsystemtablesthreadsactive}

Nombre de threads du pool StartupSystemTables qui exécutent une tâche.

### StartupSystemTablesThreadsScheduled {#startupsystemtablesthreadsscheduled}

Nombre de jobs en attente ou actifs dans le pool de threads StartupSystemTables.

### StatelessWorkerDiscoveryDyingWorkers {#statelessworkerdiscoverydyingworkers}

Nombre de workers sans état qui ont été evicted et sont en attente de removal, tel que suivi par le service de discovery des workers sans état.

### StatelessWorkerDiscoveryFreeWorkers {#statelessworkerdiscoveryfreeworkers}

Nombre de workers sans état enregistrés et disponibles à la lease, tel que comptabilisé par le service de discovery des workers sans état.

### StatelessWorkerDiscoveryLeasedWorkers {#statelessworkerdiscoveryleasedworkers}

Nombre de workers sans état actuellement loués à des tenants, tel que comptabilisé par le service de discovery des workers sans état.

### StatelessWorkerDiscoveryTenants {#statelessworkerdiscoverytenants}

Nombre de tenants enregistrés auprès du service de discovery des workers sans état.

### StatelessWorkerIdle {#statelessworkeridle}

Nombre de workers sans état actuellement conservés par le serveur mais qui ne sont utilisés par aucune requête en cours d&#39;exécution.

### StatelessWorkerInUse {#statelessworkerinuse}

Nombre de workers sans état actuellement utilisés par les requêtes en cours d&#39;exécution.

### StatelessWorkerServerActiveTasks {#statelessworkerserveractivetasks}

Nombre de tasks d&#39;exécution de requêtes actuellement actives sur un worker sans état (acceptées d&#39;un tenant et pas encore libérées).

### StatelessWorkerThreads {#statelessworkerthreads}

Nombre de threads dans le pool de threads des workers sans état.

### StatelessWorkerThreadsActive {#statelessworkerthreadsactive}

Nombre de threads du pool de workers sans état en train d’exécuter une tâche.

### StatelessWorkerThreadsScheduled {#statelessworkerthreadsscheduled}

Nombre de jobs en file d’attente ou en cours d’exécution dans le pool de threads de workers sans état.

### StorageBufferBytes {#storagebufferbytes}

Nombre d’octets dans les buffers des tables Buffer

### StorageBufferFlushThreads {#storagebufferflushthreads}

Nombre de threads pour les opérations de vidage en arrière-plan de StorageBuffer

### StorageBufferFlushThreadsActive {#storagebufferflushthreadsactive}

Nombre de threads exécutant une tâche de flush en arrière-plan dans StorageBuffer

### StorageBufferFlushThreadsScheduled {#storagebufferflushthreadsscheduled}

Nombre de threads en file d’attente ou actifs pour les opérations de flush en arrière-plan dans StorageBuffer

### StorageBufferRows {#storagebufferrows}

Nombre de lignes dans les tampons des tables Buffer

### StorageConnectionsStored {#storageconnectionsstored}

Nombre total de sessions stockées dans le pool de sessions pour les systèmes de stockage

### StorageConnectionsTotal {#storageconnectionstotal}

Nombre total de sessions : celles stockées dans le pool et celles actuellement utilisées pour les stockages

### StorageDistributedThreads {#storagedistributedthreads}

Nombre de threads du pool de threads StorageDistributed.

### StorageDistributedThreadsActive {#storagedistributedthreadsactive}

Nombre de threads du pool de threads StorageDistributed qui exécutent une tâche.

### StorageDistributedThreadsScheduled {#storagedistributedthreadsscheduled}

Nombre de tâches en file d’attente ou actives dans le pool de threads de StorageDistributed.

### StorageHiveThreads {#storagehivethreads}

Nombre de threads du pool de threads StorageHive.

### StorageHiveThreadsActive {#storagehivethreadsactive}

Nombre de threads du pool de threads StorageHive exécutant une tâche.

### StorageHiveThreadsScheduled {#storagehivethreadsscheduled}

Nombre de jobs actifs ou en file d&#39;attente dans le pool de threads StorageHive.

### StorageObjectStorageThreads {#storageobjectstoragethreads}

Nombre de threads dans les pools de threads des moteurs de table distants.

### StorageObjectStorageThreadsActive {#storageobjectstoragethreadsactive}

Nombre de threads du pool de threads des moteurs de table distants qui exécutent une tâche.

### StorageObjectStorageThreadsScheduled {#storageobjectstoragethreadsscheduled}

Nombre de jobs en attente ou actifs dans le pool de threads des moteurs de table distants.

### StorageS3Threads {#storages3threads}

Nombre de threads dans le pool de threads StorageS3.

### StorageS3ThreadsActive {#storages3threadsactive}

Nombre de threads du pool de threads StorageS3 exécutant une tâche.

### StorageS3ThreadsScheduled {#storages3threadsscheduled}

Nombre de jobs en file d’attente ou en cours d’exécution dans le pool de threads StorageS3.

### StreamingExchangeSinksWithFullSendQueue {#streamingexchangesinkswithfullsendqueue}

Nombre de sinks d&#39;échange en streaming qui n&#39;acceptent aucun fragment parce que leur file de paquets en attente du socket est pleine.

### SystemDatabaseReplicasThreads {#systemdatabasereplicasthreads}

Nombre de threads du pool de threads system.database&#95;replicas.

### SystemDatabaseReplicasThreadsActive {#systemdatabasereplicasthreadsactive}

Nombre de threads du pool de threads system.database&#95;replicas exécutant une tâche.

### SystemDatabaseReplicasThreadsScheduled {#systemdatabasereplicasthreadsscheduled}

Nombre de jobs en file d’attente ou actifs dans le pool de threads de system.database&#95;replicas.

### SystemReplicasThreads {#systemreplicasthreads}

Nombre de threads du pool de threads system.replicas.

### SystemReplicasThreadsActive {#systemreplicasthreadsactive}

Nombre de threads du pool de threads system.replicas qui exécutent une tâche.

### SystemReplicasThreadsScheduled {#systemreplicasthreadsscheduled}

Nombre de tâches en file d’attente ou en cours d’exécution dans le pool de threads system.replicas.

### TablesLoaderBackgroundThreads {#tablesloaderbackgroundthreads}

Nombre de threads dans le pool de threads en arrière-plan du chargeur de tables.

### TablesLoaderBackgroundThreadsActive {#tablesloaderbackgroundthreadsactive}

Nombre de threads du pool de threads en arrière-plan du chargeur de tables qui exécutent une tâche.

### TablesLoaderBackgroundThreadsScheduled {#tablesloaderbackgroundthreadsscheduled}

Nombre de jobs en file d’attente ou en cours d’exécution dans le pool de threads d’arrière-plan du chargeur de tables.

### TablesLoaderForegroundThreads {#tablesloaderforegroundthreads}

Nombre de threads dans le pool de threads d’avant-plan du chargeur de tables.

### TablesLoaderForegroundThreadsActive {#tablesloaderforegroundthreadsactive}

Nombre de threads dans le pool de threads de premier plan du chargeur de tables qui exécutent une tâche.

### TablesLoaderForegroundThreadsScheduled {#tablesloaderforegroundthreadsscheduled}

Nombre de jobs en attente ou actifs dans le pool de threads de premier plan du chargeur de tables.

### TablesToDropQueueSize {#tablestodropqueuesize}

Nombre de tables supprimées en attente de la suppression des données en arrière-plan.

### TaskTrackerThreads {#tasktrackerthreads}

Nombre de threads utilisés par le composant de suivi des tâches distantes pour les requêtes distribuées.

### TaskTrackerThreadsActive {#tasktrackerthreadsactive}

Nombre de threads du pool de threads du suivi des tâches distantes des requêtes distribuées qui exécutent une tâche.

### TaskTrackerThreadsScheduled {#tasktrackerthreadsscheduled}

Nombre de jobs en file d’attente ou en cours d’exécution dans le pool de threads du tracker de tâches distantes des requêtes distribuées.

### TCPConnection {#tcpconnection}

Nombre de connexions au serveur TCP (clients via l’interface native), y compris les connexions inter-serveurs pour les requêtes distribuées

### Fichiers temporaires pour l’agrégation {#temporaryfilesforaggregation}

Nombre de fichiers temporaires créés lors d’une agrégation externe

### TemporaryFilesForDistinct {#temporaryfilesfordistinct}

Nombre de fichiers temporaires créés pour un DISTINCT externe

### TemporaryFilesForJoin {#temporaryfilesforjoin}

Nombre de fichiers temporaires créés pour un JOIN

### TemporaryFilesForMerge {#temporaryfilesformerge}

Nombre de fichiers temporaires pour la fusion verticale

### Fichiers temporaires pour le tri {#temporaryfilesforsort}

Nombre de fichiers temporaires créés lors du tri externe

### TemporaryFilesUnknown {#temporaryfilesunknown}

Nombre de fichiers temporaires créés sans raison connue

### TextIndexHeaderCacheBytes {#textindexheadercachebytes}

Taille, en octets, du cache des en-têtes de l’index de texte

### TextIndexHeaderCacheCells {#textindexheadercachecells}

Nombre d’entrées dans le cache d’en-tête de l’index de texte

### TextIndexPostingsCacheBytes {#textindexpostingscachebytes}

Taille, en octets, du cache des listes d&#39;occurrences de l&#39;index de texte

### TextIndexPostingsCacheCells {#textindexpostingscachecells}

Nombre d’entrées dans le cache des listes de postings de l’index de texte

### TextIndexTokensCacheBytes {#textindextokenscachebytes}

Taille, en octets, du cache des jetons de l’index de texte

### TextIndexTokensCacheCells {#textindextokenscachecells}

Nombre d’entrées du cache de jetons de l’index de texte

### ThreadPoolFSReaderThreads {#threadpoolfsreaderthreads}

Nombre de threads du pool de threads pour local&#95;filesystem&#95;read&#95;method=threadpool.

### ThreadPoolFSReaderThreadsActive {#threadpoolfsreaderthreadsactive}

Nombre de threads dans le pool de threads pour `local_filesystem_read_method=threadpool` qui exécutent une tâche.

### ThreadPoolFSReaderThreadsScheduled {#threadpoolfsreaderthreadsscheduled}

Nombre de tâches dans la file d’attente ou actives dans le pool de threads pour `local_filesystem_read_method=threadpool`.

### ThreadPoolRemoteFSReaderThreads {#threadpoolremotefsreaderthreads}

Nombre de threads dans le pool de threads pour remote&#95;filesystem&#95;read&#95;method=threadpool.

### ThreadPoolRemoteFSReaderThreadsActive {#threadpoolremotefsreaderthreadsactive}

Nombre de threads du pool de threads pour remote&#95;filesystem&#95;read&#95;method=threadpool qui exécutent une tâche.

### ThreadPoolRemoteFSReaderThreadsScheduled {#threadpoolremotefsreaderthreadsscheduled}

Nombre de jobs en attente ou actifs dans le pool de threads pour remote&#95;filesystem&#95;read&#95;method=threadpool.

### ThreadsInOvercommitTracker {#threadsinovercommittracker}

Nombre de threads en attente dans l’OvercommitTracker

### TotalTemporaryFiles {#totaltemporaryfiles}

Nombre de fichiers temporaires créés

### UDFDriverInvokerThreads {#udfdriverinvokerthreads}

Nombre de threads dans les pools de threads qui traitent les pipes des commandes du driver UDF exécutable.

### UDFDriverInvokerThreadsActive {#udfdriverinvokerthreadsactive}

Nombre de threads actifs dans les pools de threads qui traitent les pipes des commandes du driver UDF exécutable.

### UDFDriverInvokerThreadsScheduled {#udfdriverinvokerthreadsscheduled}

Nombre de jobs en file d’attente ou actifs dans les pools de threads qui traitent les pipes des commandes du driver UDF exécutable.

### UncompressedCacheBytes {#uncompressedcachebytes}

Taille totale du cache non compressé, en octets. Le cache non compressé n’améliore généralement pas les performances et devrait donc être évité dans la plupart des cas.

### UncompressedCacheCells {#uncompressedcachecells}

Nombre total d’entrées dans le cache non compressé. Chaque entrée représente un bloc de données décompressées. Le cache non compressé n’améliore généralement pas les performances et doit donc être évité dans la plupart des cas

### UniqueKeyIndexCacheBytes {#uniquekeyindexcachebytes}

Taille totale, en octets, du cache d’index UNIQUE KEY

### UniqueKeyIndexCacheEntries {#uniquekeyindexcacheentries}

Nombre total de blocs d’index UNIQUE KEY en cache

### VectorSimilarityIndexCacheBytes {#vectorsimilarityindexcachebytes}

Taille, en octets, du cache de l’index de similarité vectorielle

### VectorSimilarityIndexCacheCells {#vectorsimilarityindexcachecells}

Nombre d’entrées dans le cache de l’index de similarité vectorielle

### VersionInteger {#versioninteger}

Version du serveur représentée par un seul entier en base 1000. Par exemple, la version 11.22.33 est représentée par 11022033.

### Écriture {#write}

Nombre d&#39;appels système d&#39;écriture (write, pwrite, io&#95;getevents, etc.) en cours

### ZooKeeperConnectionLossStartedTimestampSeconds {#zookeeperconnectionlossstartedtimestampseconds}

Horodatage Unix en secondes du moment où la connexion à ZooKeeper a été perdue, ou 0 si la connexion a été établie avec succès.

### ZooKeeperRequest {#zookeeperrequest}

Nombre de requêtes ZooKeeper en cours.

### ZooKeeperSession {#zookeepersession}

Nombre de sessions (connexions) à ZooKeeper. Il ne devrait pas y en avoir plus d’une, car l’utilisation de plusieurs connexions à ZooKeeper peut entraîner des bogues en raison de l’absence de linéarisabilité (lectures périmées) qu’autorise le modèle de cohérence de ZooKeeper.

### ZooKeeperSessionExpired {#zookeepersessionexpired}

Nombre de sessions ZooKeeper globales ayant expiré.

### ZooKeeperWatch {#zookeeperwatch}

Nombre de watches (abonnements à des événements) dans ZooKeeper.

## Colonnes {#columns}

* `metric` ([String](/fr/reference/data-types/string)) — Nom de la métrique.
* `value` ([Int64](/fr/reference/data-types/int-uint)) — Valeur de la métrique.
* `description` ([String](/fr/reference/data-types/string)) — Description de la métrique.

**Alias :**

* `name` — alias de `metric`.

Vous trouverez toutes les métriques prises en charge dans le fichier source [src/Common/CurrentMetrics.cpp](https://github.com/ClickHouse/ClickHouse/blob/master/src/Common/CurrentMetrics.cpp).

## Exemple {#example}

```sql
SELECT * FROM system.metrics LIMIT 10
```

```text
┌─metric───────────────────────────────┬─value─┬─description────────────────────────────────────────────────────────────┐
│ Query                                │     1 │ Number of executing queries                                            │
│ Merge                                │     0 │ Number of executing background merges                                  │
│ PartMutation                         │     0 │ Number of mutations (ALTER DELETE/UPDATE)                              │
│ ReplicatedFetch                      │     0 │ Number of data parts being fetched from replicas                       │
│ ReplicatedSend                       │     0 │ Number of data parts being sent to replicas                            │
│ ReplicatedChecks                     │     0 │ Number of data parts checking for consistency                          │
│ BackgroundMergesAndMutationsPoolTask │     0 │ Number of active merges and mutations in an associated background pool │
│ BackgroundFetchesPoolTask            │     0 │ Number of active fetches in an associated background pool              │
│ BackgroundCommonPoolTask             │     0 │ Number of active tasks in an associated background pool                │
│ BackgroundMovePoolTask               │     0 │ Number of active tasks in BackgroundProcessingPool for moves           │
└──────────────────────────────────────┴───────┴────────────────────────────────────────────────────────────────────────┘
```

{/*AUTOGENERATED_END*/}