---
description: 'Table système contenant des informations sur les nœuds d’ordonnancement
  présents sur le serveur local, ainsi que sur leur état.'
keywords: ['table système', 'planificateur']
slug: /operations/system-tables/scheduler
title: 'system.scheduler'
doc_type: 'référence'
---

import SystemTableCloud from '/snippets/fr/_system_table_cloud.mdx';

<SystemTableCloud />

<div id="description">
  ## Description
</div>

Contient des informations sur les [nœuds d’ordonnancement](/fr/concepts/features/configuration/server-config/workload-scheduling#hierarchy) présents sur le serveur local, ainsi que sur leur statut.
Cette table peut être utilisée à des fins de surveillance. Elle contient une ligne pour chaque nœud d’ordonnancement.

<div id="columns">
  ## Colonnes
</div>

{/*AUTOGENERATED_START*/}

* `resource` ([String](/fr/reference/data-types/index)) — Nom de la ressource
* `path` ([String](/fr/reference/data-types/index)) — Chemin vers un nœud d’ordonnancement au sein de cette hiérarchie d’ordonnancement des ressources
* `type` ([String](/fr/reference/data-types/index)) — Type d’un nœud d’ordonnancement.
* `weight` ([Float64](/fr/reference/data-types/index)) — Poids d’un nœud, utilisé par un nœud parent de type `fair`.
* `priority` ([Int64](/fr/reference/data-types/index)) — Priorité d’un nœud, utilisée par un nœud parent de type &#39;priority&#39; (une valeur plus faible signifie une priorité plus élevée).
* `precedence` ([Int64](/fr/reference/data-types/index)) — Préséance d’un nœud, utilisée par les nœuds parents à partage d’espace (mémoire) pour ordonner l’admission et l’éviction (une valeur plus faible signifie une préséance plus élevée).
* `is_active` ([Nullable(UInt8)](/fr/reference/data-types/index)) — Indique si ce nœud est actuellement actif — s’il a des requêtes de ressource à extraire de la file et si les contraintes sont satisfaites.
* `active_children` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Le nombre d’enfants actifs.
* `dequeued_requests` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Le nombre total de requêtes de ressource extraites de la file depuis ce nœud.
* `canceled_requests` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Le nombre total de requêtes de ressource annulées depuis ce nœud.
* `rejected_requests` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Le nombre total de requêtes de ressource rejetées depuis ce nœud.
* `dequeued_cost` ([Nullable(Int64)](/fr/reference/data-types/index)) — La somme des coûts (par ex. la taille en octets) de toutes les requêtes extraites de la file depuis ce nœud.
* `canceled_cost` ([Nullable(Int64)](/fr/reference/data-types/index)) — La somme des coûts (par ex. la taille en octets) de toutes les requêtes annulées depuis ce nœud.
* `rejected_cost` ([Nullable(Int64)](/fr/reference/data-types/index)) — La somme des coûts (par ex. la taille en octets) de toutes les requêtes rejetées depuis ce nœud.
* `busy_periods` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Le nombre total de désactivations de ce nœud.
* `throughput` ([Nullable(Float64)](/fr/reference/data-types/index)) — Débit moyen actuel (coût extrait de la file par seconde).
* `vruntime` ([Nullable(Float64)](/fr/reference/data-types/index)) — Pour les enfants des nœuds `fair` uniquement. Temps d’exécution virtuel d’un nœud utilisé par l’algorithme SFQ pour sélectionner le prochain enfant à traiter de manière équitable selon le principe max-min.
* `system_vruntime` ([Nullable(Float64)](/fr/reference/data-types/index)) — Pour les nœuds `fair` uniquement. Temps d’exécution virtuel indiquant le `vruntime` de la dernière requête de ressource traitée. Utilisé lors de l’activation d’un enfant comme nouvelle valeur de `vruntime`.
* `queue_length` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Pour les nœuds `fifo` uniquement. Nombre actuel de requêtes de ressource présentes dans la file.
* `queue_cost` ([Nullable(Int64)](/fr/reference/data-types/index)) — Pour les nœuds `fifo` uniquement. Somme des coûts (par ex. la taille en octets) de toutes les requêtes présentes dans la file.
* `budget` ([Nullable(Int64)](/fr/reference/data-types/index)) — Pour les nœuds `fifo` uniquement. Le nombre d’&#39;unités de coût&#39; disponibles pour les nouvelles requêtes de ressource. Peut apparaître en cas d’écart entre les coûts estimés et réels des requêtes de ressource (par ex. après un échec de lecture/écriture)
* `is_satisfied` ([Nullable(UInt8)](/fr/reference/data-types/index)) — Pour les nœuds de contrainte uniquement (par ex. `inflight_limit`). Égal à `1` si toutes les contraintes de ce nœud sont satisfaites.
* `inflight_requests` ([Nullable(Int64)](/fr/reference/data-types/index)) — Pour les nœuds `inflight_limit` uniquement. Le nombre de requêtes de ressource extraites de la file depuis ce nœud qui sont actuellement en cours de consommation.
* `inflight_cost` ([Nullable(Int64)](/fr/reference/data-types/index)) — Pour les nœuds `inflight_limit` uniquement. La somme des coûts (par ex. en octets) de toutes les requêtes de ressource extraites de la file depuis ce nœud qui sont actuellement en cours de consommation.
* `max_requests` ([Nullable(Int64)](/fr/reference/data-types/index)) — Pour les nœuds `inflight_limit` uniquement. Limite supérieure de inflight&#95;requests entraînant une violation de contrainte.
* `max_cost` ([Nullable(Int64)](/fr/reference/data-types/index)) — Pour les nœuds `inflight_limit` uniquement. Limite supérieure de `inflight_cost` entraînant une violation de contrainte.
* `max_speed` ([Nullable(Float64)](/fr/reference/data-types/index)) — Pour les nœuds `bandwidth_limit` uniquement. Limite supérieure de bande passante en jetons par seconde.
* `max_burst` ([Nullable(Float64)](/fr/reference/data-types/index)) — Pour les nœuds `bandwidth_limit` uniquement. Limite supérieure du nombre de jetons disponibles dans le throttler à seau de jetons.
* `throttling_us` ([Nullable(Int64)](/fr/reference/data-types/index)) — Pour les nœuds `bandwidth_limit` uniquement. Nombre total de microsecondes pendant lesquelles ce nœud a été en état de limitation de débit.
* `tokens` ([Nullable(Float64)](/fr/reference/data-types/index)) — Pour les nœuds `bandwidth_limit` uniquement. Nombre de jetons actuellement disponibles dans le throttler à seau de jetons.
* `allocated` ([Nullable(Int64)](/fr/reference/data-types/index)) — Pour les nœuds space-shared uniquement. Quantité de ressource actuellement allouée sous ce nœud.
* `allocations` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Pour les nœuds space-shared uniquement. Le nombre actuel d’allocations de ressources en cours sous ce nœud.
* `updates` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Pour les nœuds space-shared uniquement. Le nombre total de mises à jour propagées via ce nœud.
* `increases` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Pour les nœuds space-shared uniquement. Le nombre total de demandes d’augmentation approuvées par ce nœud.
* `decreases` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Pour les nœuds space-shared uniquement. Le nombre total de demandes de réduction approuvées par ce nœud.
* `admits` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Pour les nœuds space-shared uniquement. Le nombre total d’allocations admises par ce nœud.
* `removes` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Pour les nœuds space-shared uniquement. Le nombre total d’allocations supprimées de ce nœud.
* `killers` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Pour les nœuds space-shared uniquement. Le nombre total d’évictions initiées par ce nœud en raison de demandes d’augmentation.
* `victims` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Pour les nœuds space-shared uniquement. Le nombre total d’allocations évincées de ce nœud.
* `rejects` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Pour les nœuds `allocation_queue` uniquement. Le nombre total d’allocations de ressources rejetées par ce nœud.
* `pending` ([Nullable(UInt64)](/fr/reference/data-types/index)) — Pour les nœuds `allocation_queue` uniquement. Le nombre actuel d’allocations de ressources en attente dans ce nœud.

{/*AUTOGENERATED_END*/}

Exemple :

```sql
SELECT *
FROM system.scheduler
WHERE resource = 'network_read' AND path = '/prio/fair/prod'
FORMAT Vertical
```

```text
Row 1:
──────
resource:          network_read
path:              /prio/fair/prod
type:              fifo
weight:            5
priority:          0
is_active:         0
active_children:   0
dequeued_requests: 67
canceled_requests: 0
dequeued_cost:     4692272
canceled_cost:     0
busy_periods:      63
vruntime:          938454.1999999989
system_vruntime:   ᴺᵁᴸᴸ
queue_length:      0
queue_cost:        0
budget:            -60524
is_satisfied:      ᴺᵁᴸᴸ
inflight_requests: ᴺᵁᴸᴸ
inflight_cost:     ᴺᵁᴸᴸ
max_requests:      ᴺᵁᴸᴸ
max_cost:          ᴺᵁᴸᴸ
max_speed:         ᴺᵁᴸᴸ
max_burst:         ᴺᵁᴸᴸ
throttling_us:     ᴺᵁᴸᴸ
tokens:            ᴺᵁᴸᴸ
```