---
description: 'Table système qui présente tous les tokenizers disponibles.'
keywords: ['table système', 'tokenizers']
slug: /operations/system-tables/tokenizers
title: 'system.tokenizers'
doc_type: 'reference'
---

import SystemTableCloud from '/snippets/fr/_system_table_cloud.mdx';

<SystemTableCloud />

{/*AUTOGENERATED_START*/}

## Description {#description}

Affiche tous les tokenizers disponibles.
Ils s’utilisent dans les fonctions [tokens](/fr/reference/functions/regular-functions/splitting-merging-functions#tokens), [hasAllTokens](/fr/reference/functions/regular-functions/string-search-functions#hasAllTokens), [hasAnyTokens](/fr/reference/functions/regular-functions/string-search-functions#hasAnyTokens) et dans l’[index de texte](/fr/reference/engines/table-engines/mergetree-family/textindexes).

## Colonnes {#columns}

* `name` ([String](/fr/reference/data-types/string)) — Nom du tokenizer

## Exemple {#example}

```sql
SELECT * FROM system.tokenizers;
```

```text
┌─name────────────┐
│ ngrams          │
│ splitByNonAlpha │
│ sparseGrams     │
│ tokenbf_v1      │
│ ngrambf_v1      │
│ array           │
│ splitByString   │
│ sparse_grams    │
│ asciiCJK        │
│ icu             │
│ japanese        │
└─────────────────┘
```

{/*AUTOGENERATED_END*/}