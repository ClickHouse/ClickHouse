---
description: 'Documentation décrivant le modificateur APPLY, qui permet d’appliquer une fonction à chaque ligne renvoyée par l’expression de table externe d’une requête.'
sidebarTitle: 'APPLY'
slug: /sql-reference/statements/select/apply-modifier
title: 'Modificateur APPLY'
keywords: ['APPLY', 'modificateur']
doc_type: 'reference'
---

> Permet d’appliquer une fonction à chaque ligne renvoyée par l’expression de table externe d’une requête.

<div id="syntax">
  ## Syntaxe
</div>

```sql
SELECT <expr> APPLY( <func> ) FROM [db.]table_name
```

<div id="example">
  ## Exemple
</div>

```sql
CREATE TABLE columns_transformers (i Int64, j Int16, k Int64) ENGINE = MergeTree ORDER by (i);
INSERT INTO columns_transformers VALUES (100, 10, 324), (120, 8, 23);
SELECT * APPLY(sum) FROM columns_transformers;
```

```response
┌─sum(i)─┬─sum(j)─┬─sum(k)─┐
│    220 │     18 │    347 │
└────────┴────────┴────────┘
```