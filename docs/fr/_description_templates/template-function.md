---
title: Fonction Template
---



## functionName {#functionname-in-lower-case}

Description courte.

**Syntaxe** (sans SELECT)

```sql
<function syntax>
```

Alias : `<alias name>`. (Facultatif)

Texte supplémentaire (Facultatif).

**Arguments** (Facultatif)

-   `x` — Description. Facultatif (uniquement pour les arguments facultatifs). Valeurs possibles : <values list>. Valeur par défaut : <value>. [Type name](relative/path/to/type/dscr.md#type).
-   `y` — Description. Facultatif (uniquement pour les arguments facultatifs). Valeurs possibles : <values list>. Valeur par défaut : <value>. [Type name](relative/path/to/type/dscr.md#type).

**Paramètres** (Facultatif, uniquement pour les fonctions d'agrégation paramétriques)

-   `z` — Description. Facultatif (uniquement pour les paramètres facultatifs). Valeurs possibles : <values list>. Valeur par défaut : <value>. [Type name](relative/path/to/type/dscr.md#type).

**Valeur(s) renvoyée(s)**

-   Liste des valeurs renvoyées.

Type : [Type name](relative/path/to/type/dscr.md#type).

**Exemple**

L'exemple doit illustrer l'utilisation et/ou des cas d'usage. Le texte suivant contient les parties recommandées d'un exemple.

Table d'entrée (Facultatif) :

```text
```

```sql title="Query"
```

```text title="Response"
```

**Voir aussi** (Facultatif)

-   [lien](#)