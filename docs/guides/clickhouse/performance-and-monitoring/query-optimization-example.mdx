---
slug: /optimize/query-optimization-example
sidebarTitle: 'Worked optimization example'
title: 'Worked query optimization example'
description: 'Follow a worked example of optimizing ClickHouse queries through schema and ordering-key improvements'
doc_type: 'guide'
keywords: ['query optimization', 'performance', 'NYC Taxi', 'data types', 'ordering key']
---

import NYCTaxiExampleDataset from '/snippets/_nyc_taxi_example_dataset.mdx';

Follow a complete optimization workflow on the NYC Taxi dataset. Establish a baseline, change one part of the table design at a time, and compare each result with the original measurements.

## Before you begin {#before-you-begin}

<NYCTaxiExampleDataset />

## How it works {#how-it-works}

The example first reduces the table's data footprint, then changes its ordering key so that ClickHouse can read less data for the example query. Each stage is measured independently.

## Establish the baseline {#establish-the-baseline}

Record the original table size, query duration, rows and bytes read, and peak memory usage before changing the schema.

## Optimize the schema {#basic-optimization}

### Avoid unnecessary Nullable columns {#nullable}

### Use LowCardinality where appropriate {#low-cardinality}

### Choose appropriate data types {#optimize-data-type}

### Apply the schema changes {#apply-the-optimizations}

## Optimize the primary key {#the-importance-of-primary-keys}

### Choose a primary key {#choose-primary-keys}

### Apply the primary-key change {#apply-the-primary-key-change}

## Compare the results {#compare-the-results}

Compare the optimized table and query measurements with the original baseline.

## Apply the method to your workload {#apply-the-method-to-your-workload}

Use the measured bottleneck in your own query to decide whether to reduce the data footprint, improve filtering, or move repeatable work to ingestion time.

## Next steps {#next-steps}

Return to [Optimization approaches](/guides/clickhouse/performance-and-monitoring/optimization-approaches) to evaluate projections, materialized views, data-skipping indexes, or other targeted interventions for your workload.
