---
title: "ClickStack: Update Connection"
openapi: "/_specs/cloud-openapi.json PUT /v1/organizations/{organizationId}/services/{serviceId}/clickstack/connections/{clickStackConnectionId}"
description: "**This endpoint is in beta.** API contract is stable, and no breaking changes are expected in the future. <br /><br /> ClickStack: Updates an existing ClickHouse connection.  Field semantics: if `password` is omitted or empty the existing password is kept. `hyperdxSettingPrefix` is cleared when set to null or an empty string, and kept unchanged when omitted."
tag: "Beta"
---

{/* AUTOGENERATED_START */}

<span data-endpoint-badge="Beta"><Badge color="blue">Beta</Badge></span>

**This endpoint is in beta.** API contract is stable, and no breaking changes are expected in the future. <br /><br /> ClickStack: Updates an existing ClickHouse connection.  Field semantics: if `password` is omitted or empty the existing password is kept. `hyperdxSettingPrefix` is cleared when set to null or an empty string, and kept unchanged when omitted.

{/* AUTOGENERATED_END */}
