---
description: 'A read-only disk backed by a static website: a directory of exported table parts served over HTTP.'
sidebarTitle: 'web'
slug: /operations/disk-types/web
title: 'web disk'
doc_type: 'reference'
---

{/* This page is fully autogenerated from the ClickHouse source by ci/jobs/scripts/docs/autogenerate -- do not edit; change the documentation embedded in the source instead. */}

A read-only disk backed by a static website: a directory of exported table parts served over HTTP. Retained for compatibility; equivalent to `object_storage` with `object_storage_type = web`.

## Syntax {#syntax}

```sql
disk(type = web, endpoint = 'https://.../')
```

## Related {#related}

- [`object_storage`](/reference/disks/object_storage)
