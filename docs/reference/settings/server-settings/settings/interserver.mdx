---
title: 'interserver_* server settings'
sidebarTitle: 'interserver_*'
toc_max_heading_level: 2
description: 'ClickHouse server settings in the interserver_* generated group.'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

These settings configure the ClickHouse server and are autogenerated from ClickHouse source.

## interserver_listen_host {#interserver_listen_host}

Restriction on hosts that can exchange data between ClickHouse servers.
If Keeper is used, the same restriction will be applied to the communication between different Keeper instances.

<Note>
By default, the value is equal to the [`listen_host`](/reference/settings/server-settings/settings/listen#listen_host) setting.
</Note>

**Example**

```xml
<interserver_listen_host>::ffff:a00:1</interserver_listen_host>
<interserver_listen_host>10.0.0.1</interserver_listen_host>
```

Type:

Default:

## interserver_tables_status_require_auth {#interserver_tables_status_require_auth}

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />
Require interserver `TablesStatusRequest` to be authenticated with the cluster
`<secret>`. Clients new enough to send a secret hash (protocol revision
`DBMS_MIN_REVISION_WITH_INTERSERVER_SECRET_TABLES_STATUS`) are always validated; this
setting additionally rejects older clients that send no hash, which is what closes the
unauthenticated table-status disclosure by default.

Defaults to `true` (secure by default). During a rolling upgrade a not-yet-upgraded
node speaks the old protocol and sends no hash, so a `Distributed` query initiated on
such a node against an already-upgraded node would have its `TablesStatusRequest`
rejected. If you must run a mixed-version cluster, set this to `false` on the upgraded
nodes until every node is upgraded, then remove the override.

{/*AUTOGENERATED_END*/}
