---
title: 'unique_key_* server settings'
sidebarTitle: 'unique_key_*'
toc_max_heading_level: 2
description: 'ClickHouse server settings in the unique_key_* generated group.'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

These settings configure the ClickHouse server and are autogenerated from ClickHouse source.

## unique_key_bitmap_cache_policy {#unique_key_bitmap_cache_policy}

<SettingsInfoBlock type="String" default_value="SLRU" changeable_without_restart="No" />UNIQUE KEY delete-bitmap cache policy name (SLRU or LRU).

## unique_key_bitmap_cache_size_bytes {#unique_key_bitmap_cache_size_bytes}

<SettingsInfoBlock type="UInt64" default_value="1073741824" changeable_without_restart="Yes" />Maximum size in bytes of the in-process cache for UNIQUE KEY per-part delete bitmaps. Set to 0 to disable the cache.

## unique_key_bitmap_cache_size_ratio {#unique_key_bitmap_cache_size_ratio}

<SettingsInfoBlock type="Double" default_value="0.5" changeable_without_restart="No" />The size of the protected queue (in case of SLRU policy) in the UNIQUE KEY delete-bitmap cache relative to the cache's total size.

## unique_key_index_cache_policy {#unique_key_index_cache_policy}

<SettingsInfoBlock type="String" default_value="SLRU" changeable_without_restart="No" />UNIQUE KEY index cache policy name (SLRU or LRU).

## unique_key_index_cache_size_bytes {#unique_key_index_cache_size_bytes}

<SettingsInfoBlock type="UInt64" default_value="1073741824" changeable_without_restart="No" />Maximum size (bytes) of the in-process cache for UNIQUE KEY index (SST) blocks. Set to 0 to disable the cache.

## unique_key_index_cache_size_ratio {#unique_key_index_cache_size_ratio}

<SettingsInfoBlock type="Double" default_value="0.5" changeable_without_restart="No" />The size of the protected queue (in case of SLRU policy) in the UNIQUE KEY index cache relative to the cache's total size.

{/*AUTOGENERATED_END*/}
