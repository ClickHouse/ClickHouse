---
title: 'network_* session settings'
sidebarTitle: 'network_*'
toc_max_heading_level: 2
description: 'ClickHouse session settings in the network_* generated group.'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import { VersionHistory } from "/snippets/components/VersionHistory/VersionHistory.jsx";

These settings are available in [system.settings](/reference/system-tables/settings) and are autogenerated from [source](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp).

## network_compression_method {#network_compression_method}

<SettingsInfoBlock type="String" default_value="ZSTD" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.9"},{"label": "ZSTD"},{"label": "Switched the default compression method for client\/server and server\/server communication from `LZ4` to `ZSTD` to reduce network traffic."}]}]}/>

The codec for compressing the client/server and server/server communication over the native protocol.

The setting does not apply to the streaming-exchange channel of distributed queries, which always uses the server default codec: every compressed frame is self-describing, so the receiver detects the codec automatically.

Possible values:

- `NONE` — no compression.
- `LZ4` — use the LZ4 codec.
- `LZ4HC` — use the LZ4HC codec.
- `ZSTD` — use the ZSTD codec.

**See Also**

- [network_zstd_compression_level](/reference/settings/session-settings/network#network_zstd_compression_level)

## network_zstd_compression_level {#network_zstd_compression_level}

<SettingsInfoBlock type="Int64" default_value="3" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.9"},{"label": "3"},{"label": "Aligned the default network `ZSTD` compression level with the new default on-disk `ZSTD(3)` compression."}]}]}/>

Adjusts the level of ZSTD compression. Used only when [network_compression_method](/reference/settings/session-settings/network#network_compression_method) is set to `ZSTD`.

Possible values:

- Positive integer from 1 to 15.

{/*AUTOGENERATED_END*/}
