---
description: 'System table containing a list of data skipping index types supported
  by the server along with their embedded documentation.'
keywords: ['system table', 'data_skipping_index_types']
slug: /operations/system-tables/data_skipping_index_types
title: 'system.data_skipping_index_types'
doc_type: 'reference'
---

## Description {#description}

Contains the list of data skipping index types supported by the server, along with embedded documentation for each type. A data skipping index type is specified in the `TYPE` of an `INDEX` declaration in a `CREATE TABLE` query and lets ClickHouse skip granules that cannot match a query's condition.

Note that this table lists the available index *types*, whereas [`system.data_skipping_indices`](/reference/system-tables/data_skipping_indices) lists the index instances defined on existing tables.

## Columns {#columns}

{/*AUTOGENERATED_START*/}
- `name` ([String](/reference/data-types/string)) — The name of the data skipping index type, as specified in the TYPE of an INDEX declaration.
- `description` ([String](/reference/data-types/string)) — A high-level description of what the data skipping index type does.
- `syntax` ([String](/reference/data-types/string)) — How the index is declared in an INDEX clause of a CREATE TABLE query.
- `examples` ([String](/reference/data-types/string)) — Usage examples.
- `introduced_in` ([String](/reference/data-types/string)) — The ClickHouse version in which the index type was first introduced, in the form major.minor.
- `related` ([Array(String)](/reference/data-types/array)) — The names of related data skipping index types.
{/*AUTOGENERATED_END*/}

## Example {#example}

```sql title="Query"
SELECT name, syntax
FROM system.data_skipping_index_types
WHERE name IN ('minmax', 'set')
ORDER BY name
```

```text title="Response"
┌─name───┬─syntax───────────────────────────────────────┐
│ minmax │ INDEX name expr TYPE minmax GRANULARITY n     │
│ set    │ INDEX name expr TYPE set(max_rows) GRANULARITY n │
└────────┴──────────────────────────────────────────────┘
```

## See also {#see-also}

- [Data skipping indices](/concepts/features/performance/skip-indexes/skipping-indexes) — Information about data skipping indexes.
- [`system.data_skipping_indices`](/reference/system-tables/data_skipping_indices) — The index instances defined on existing tables.
