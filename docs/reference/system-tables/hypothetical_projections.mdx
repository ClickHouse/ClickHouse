---
description: 'System table listing hypothetical (what-if) projections defined in the current session'
keywords: ['system table', 'hypothetical_projections', 'what-if']
sidebarTitle: 'hypothetical_projections'
slug: /operations/system-tables/hypothetical_projections
title: 'system.hypothetical_projections'
doc_type: 'reference'
---

Lists every hypothetical (what-if) projection defined in the current session. See [`CREATE HYPOTHETICAL PROJECTION`](/reference/statements/hypothetical-projection#create-hypothetical-projection) and [`EXPLAIN WHATIF`](/reference/statements/explain#explain-whatif).

The contents are session-scoped: each connection sees only its own hypothetical projections, and the table is empty when none have been created in the current session.

The current `(database, table)` are resolved by UUID at query time, so they reflect `RENAME TABLE` and entries for dropped tables are hidden automatically.

## Columns {#columns}

{/*AUTOGENERATED_START*/}
- `database` ([String](/reference/data-types/string)) — Database name
- `table` ([String](/reference/data-types/string)) — Table name
- `name` ([String](/reference/data-types/string)) — Projection name
- `type` ([Enum8('Normal' = 0, 'Aggregate' = 1)](/reference/data-types/enum)) — Projection type
- `sorting_key` ([Array(String)](/reference/data-types/array)) — Projection sorting key
- `query` ([String](/reference/data-types/string)) — Projection SELECT query, empty for the INDEX ... TYPE ... form
- `definition` ([String](/reference/data-types/string)) — Full projection declaration as written
- `settings` ([Map(String, String)](/reference/data-types/map)) — Projection settings from WITH SETTINGS
{/*AUTOGENERATED_END*/}

**Example**

```sql
CREATE HYPOTHETICAL PROJECTION p_by_b ON t (SELECT a, b ORDER BY b);
SELECT name, type, sorting_key FROM system.hypothetical_projections;
```

```text
┌─name───┬─type───┬─sorting_key─┐
│ p_by_b │ Normal │ ['b']       │
└────────┴────────┴─────────────┘
```

## See also {#see-also}

- [`CREATE HYPOTHETICAL PROJECTION`](/reference/statements/hypothetical-projection#create-hypothetical-projection)
- [`EXPLAIN WHATIF`](/reference/statements/explain#explain-whatif)
