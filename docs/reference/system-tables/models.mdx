---
description: "Historical page of the removed system.models table, which listed the CatBoost models loaded into a LibraryBridge's memory along with the time when each of them was loaded."
keywords: ['system table', 'models']
sidebarTitle: 'models'
slug: /operations/system-tables/models
title: 'system.models'
doc_type: 'reference'
---

## Description {#description}

<Warning>
The CatBoost integration was removed from ClickHouse and `system.models` no longer exists: querying it fails with `UNKNOWN_TABLE`. This page is kept for the documentation it published; see the [system tables overview](/reference/system-tables/overview) for the tables that exist today.
</Warning>

Contained a list of CatBoost models loaded into a `LibraryBridge`'s memory along with time when it was loaded.

## Columns {#columns}

{/*AUTOGENERATED_START*/}
- `model_path` ([String](/reference/data-types/string)) — Path to trained model.
- `type` ([String](/reference/data-types/string)) — Model type. Now catboost only.
- `loading_start_time` ([DateTime](/reference/data-types/datetime)) — The time when the loading of the model started.
- `loading_duration` ([Float32](/reference/data-types/float)) — How much time did it take to load the model.
{/*AUTOGENERATED_END*/}
