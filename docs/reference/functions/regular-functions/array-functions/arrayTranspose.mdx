---
description: 'Reference documentation for the ClickHouse `arrayTranspose` function.'
sidebar_label: 'arrayTranspose'
sidebar_position: 74
sidebarTitle: 'arrayTranspose'
slug: /reference/functions/regular-functions/array-functions/arrayTranspose
title: 'arrayTranspose'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

## arrayTranspose {#arrayTranspose}

Introduced in: v26.4.0

Transposes a two-dimensional array.

All inner arrays must have the same length.

**Syntax**

```sql
arrayTranspose(arr)
```

**Arguments**

- `arr` — A two-dimensional array to transpose. All inner arrays must have the same length. [`Array(Array(T))`](/reference/data-types/array)

**Returned value**

A transposed two-dimensional array where element `[i][j]` of the result equals element `[j][i]` of the input. [`Array(Array(T))`](/reference/data-types/array)

**Examples**

**Square matrix**

```sql title=Query
SELECT arrayTranspose([[1, 2], [3, 4]])
```

```response title=Response
[[1, 3], [2, 4]]
```

**Non-square matrix**

```sql title=Query
SELECT arrayTranspose([[1, 2, 3], [4, 5, 6]])
```

```response title=Response
[[1, 4], [2, 5], [3, 6]]
```

**String elements**

```sql title=Query
SELECT arrayTranspose([['a', 'b'], ['c', 'd']])
```

```response title=Response
[['a', 'c'], ['b', 'd']]
```

{/*AUTOGENERATED_END*/}
