---
description: 'Reference documentation for the ClickHouse `currentUser` function.'
sidebar_label: 'currentUser'
sidebar_position: 24
sidebarTitle: 'currentUser'
slug: /reference/functions/regular-functions/other-functions/currentUser
title: 'currentUser'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import { ExperimentalBadge } from "/snippets/components/ExperimentalBadge/ExperimentalBadge.jsx";

import { CloudNotSupportedBadge } from "/snippets/components/CloudNotSupportedBadge/CloudNotSupportedBadge.jsx";

import { DeprecatedBadge } from "/snippets/components/DeprecatedBadge/DeprecatedBadge.jsx";

## currentUser {#currentUser}

Introduced in: v20.1.0

Returns the name of the current user.
In case of a distributed query, the name of the user who initiated the query is returned.
    

<Note>
This function is non-deterministic: it can return different results for the same arguments.
</Note>

**Syntax**

```sql
currentUser()
```

**Aliases**: `session_user`, `current_user`, `user`

**Arguments**

- None.

**Returned value**

Returns the name of the current user, otherwise the login of the user who initiated the query. [`String`](/reference/data-types/string)

**Examples**

**Usage example**

```sql title=Query
SELECT currentUser()
```

```response title=Response
┌─currentUser()─┐
│ default       │
└───────────────┘
```

**SQL standard syntax without parentheses**

```sql title=Query
SELECT CURRENT_USER
```

```response title=Response
┌─CURRENT_USER─┐
│ default      │
└──────────────┘
```

{/*AUTOGENERATED_END*/}
