---
description: 'Reference documentation for the ClickHouse `shardCount` function.'
sidebar_label: 'shardCount'
sidebar_position: 107
sidebarTitle: 'shardCount'
slug: /reference/functions/regular-functions/other-functions/shardCount
title: 'shardCount'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import { ExperimentalBadge } from "/snippets/components/ExperimentalBadge/ExperimentalBadge.jsx";

import { CloudNotSupportedBadge } from "/snippets/components/CloudNotSupportedBadge/CloudNotSupportedBadge.jsx";

import { DeprecatedBadge } from "/snippets/components/DeprecatedBadge/DeprecatedBadge.jsx";

## shardCount {#shardCount}

Introduced in: v21.9.0

Returns the total number of shards for a distributed query.
If a query is not distributed then constant value `0` is returned.

<Note>
This function is non-deterministic: it can return different results for the same arguments.
</Note>

**Syntax**

```sql
shardCount()
```

**Arguments**

- None.

**Returned value**

Returns the total number of shards or `0`. [`UInt32`](/reference/data-types/int-uint)

**Examples**

**Usage example**

```sql title=Query
-- See shardNum() example above which also demonstrates shardCount()
CREATE TABLE shard_count_example (dummy UInt8)
ENGINE=Distributed(test_cluster_two_shards_localhost, system, one, dummy);
SELECT shardCount() FROM shard_count_example;
```

```response title=Response
┌─shardCount()─┐
│            2 │
│            2 │
└──────────────┘
```

{/*AUTOGENERATED_END*/}
