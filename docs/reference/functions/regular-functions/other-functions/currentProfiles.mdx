---
description: 'Reference documentation for the ClickHouse `currentProfiles` function.'
sidebar_label: 'currentProfiles'
sidebar_position: 20
sidebarTitle: 'currentProfiles'
slug: /reference/functions/regular-functions/other-functions/currentProfiles
title: 'currentProfiles'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import { ExperimentalBadge } from "/snippets/components/ExperimentalBadge/ExperimentalBadge.jsx";

import { CloudNotSupportedBadge } from "/snippets/components/CloudNotSupportedBadge/CloudNotSupportedBadge.jsx";

import { DeprecatedBadge } from "/snippets/components/DeprecatedBadge/DeprecatedBadge.jsx";

## currentProfiles {#currentProfiles}

Introduced in: v21.9.0

Returns an array of the setting profiles for the current user.

<Note>
This function is non-deterministic: it can return different results for the same arguments.
</Note>

**Syntax**

```sql
currentProfiles()
```

**Arguments**

- None.

**Returned value**

Returns an array of setting profiles for the current user. [`Array(String)`](/reference/data-types/array)

**Examples**

**Usage example**

```sql title=Query
SELECT currentProfiles();
```

```response title=Response
┌─currentProfiles()─────────────────────────────┐
│ ['default', 'readonly_user', 'web_analytics'] │
└───────────────────────────────────────────────┘
```

{/*AUTOGENERATED_END*/}
