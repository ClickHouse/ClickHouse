---
description: 'Reference documentation for the ClickHouse `tcpPort` function.'
sidebar_label: 'tcpPort'
sidebar_position: 114
sidebarTitle: 'tcpPort'
slug: /reference/functions/regular-functions/other-functions/tcpPort
title: 'tcpPort'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import { ExperimentalBadge } from "/snippets/components/ExperimentalBadge/ExperimentalBadge.jsx";

import { CloudNotSupportedBadge } from "/snippets/components/CloudNotSupportedBadge/CloudNotSupportedBadge.jsx";

import { DeprecatedBadge } from "/snippets/components/DeprecatedBadge/DeprecatedBadge.jsx";

## tcpPort {#tcpPort}

Introduced in: v20.12.0

Returns the [native interface](/concepts/features/interfaces/tcp) TCP port number listened to by the server.
If executed in the context of a distributed table, this function generates a normal column with values relevant to each shard.
Otherwise it produces a constant value.
    

<Note>
This function is non-deterministic: it can return different results for the same arguments.
</Note>

**Syntax**

```sql
tcpPort()
```

**Arguments**

- None.

**Returned value**

Returns the TCP port number. [`UInt16`](/reference/data-types/int-uint)

**Examples**

**Usage example**

```sql title=Query
SELECT tcpPort()
```

```response title=Response
┌─tcpPort()─┐
│      9000 │
└───────────┘
```

{/*AUTOGENERATED_END*/}
