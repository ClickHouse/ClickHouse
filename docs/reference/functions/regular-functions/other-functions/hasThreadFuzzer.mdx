---
description: 'Reference documentation for the ClickHouse `hasThreadFuzzer` function.'
sidebar_label: 'hasThreadFuzzer'
sidebar_position: 66
sidebarTitle: 'hasThreadFuzzer'
slug: /reference/functions/regular-functions/other-functions/hasThreadFuzzer
title: 'hasThreadFuzzer'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import { ExperimentalBadge } from "/snippets/components/ExperimentalBadge/ExperimentalBadge.jsx";

import { CloudNotSupportedBadge } from "/snippets/components/CloudNotSupportedBadge/CloudNotSupportedBadge.jsx";

import { DeprecatedBadge } from "/snippets/components/DeprecatedBadge/DeprecatedBadge.jsx";

## hasThreadFuzzer {#hasThreadFuzzer}

Introduced in: v20.6.0

Returns whether the thread fuzzer is enabled.
THis function is only useful for testing and debugging.
    

**Syntax**

```sql
hasThreadFuzzer()
```

**Arguments**

- None.

**Returned value**

Returns whether Thread Fuzzer is effective. [`UInt8`](/reference/data-types/int-uint)

**Examples**

**Check Thread Fuzzer status**

```sql title=Query
SELECT hasThreadFuzzer()
```

```response title=Response
┌─hasThreadFuzzer()─┐
│                 0 │
└───────────────────┘
```

{/*AUTOGENERATED_END*/}
