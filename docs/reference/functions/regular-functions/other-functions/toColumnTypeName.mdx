---
description: 'Reference documentation for the ClickHouse `toColumnTypeName` function.'
sidebar_label: 'toColumnTypeName'
sidebar_position: 116
sidebarTitle: 'toColumnTypeName'
slug: /reference/functions/regular-functions/other-functions/toColumnTypeName
title: 'toColumnTypeName'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import { ExperimentalBadge } from "/snippets/components/ExperimentalBadge/ExperimentalBadge.jsx";

import { CloudNotSupportedBadge } from "/snippets/components/CloudNotSupportedBadge/CloudNotSupportedBadge.jsx";

import { DeprecatedBadge } from "/snippets/components/DeprecatedBadge/DeprecatedBadge.jsx";

## toColumnTypeName {#toColumnTypeName}

Introduced in: v1.1.0

Returns the internal name of the data type of the given value.
Unlike function [`toTypeName`](/reference/functions/regular-functions/other-functions/toTypeName), the returned data type potentially includes internal wrapper columns like `Const` and `LowCardinality`.

<Note>
This function is non-deterministic: it can return different results for the same arguments.
</Note>

**Syntax**

```sql
toColumnTypeName(value)
```

**Arguments**

- `value` — Value for which to return the internal data type. [`Any`](/reference/data-types/index)

**Returned value**

Returns the internal data type used to represent the value. [`String`](/reference/data-types/string)

**Examples**

**Usage example**

```sql title=Query
SELECT toColumnTypeName(CAST('2025-01-01 01:02:03' AS DateTime));
```

```response title=Response
┌─toColumnTypeName(CAST('2025-01-01 01:02:03', 'DateTime'))─┐
│ Const(UInt32)                                             │
└───────────────────────────────────────────────────────────┘
```

{/*AUTOGENERATED_END*/}
