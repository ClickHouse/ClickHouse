---
description: 'Reference documentation for the ClickHouse `getSettingOrDefault` function.'
sidebar_label: 'getSettingOrDefault'
sidebar_position: 60
sidebarTitle: 'getSettingOrDefault'
slug: /reference/functions/regular-functions/other-functions/getSettingOrDefault
title: 'getSettingOrDefault'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import { ExperimentalBadge } from "/snippets/components/ExperimentalBadge/ExperimentalBadge.jsx";

import { CloudNotSupportedBadge } from "/snippets/components/CloudNotSupportedBadge/CloudNotSupportedBadge.jsx";

import { DeprecatedBadge } from "/snippets/components/DeprecatedBadge/DeprecatedBadge.jsx";

## getSettingOrDefault {#getSettingOrDefault}

Introduced in: v24.10.0

Returns the current value of a setting or returns the default value specified in the second argument if the setting is not set in the current profile.

<Note>
This function is non-deterministic: it can return different results for the same arguments.
</Note>

**Syntax**

```sql
getSettingOrDefault(setting_name, default_value)
```

**Arguments**

- `setting_name` — The setting name. [`String`](/reference/data-types/string)
- `default_value` — Value to return if custom_setting is not set. Value may be of any data type or Null. 

**Returned value**

Returns the current value of the specified setting or `default_value` if the setting is not set.

**Examples**

**Usage example**

```sql title=Query
SELECT getSettingOrDefault('custom_undef1', 'my_value');
SELECT getSettingOrDefault('custom_undef2', 100);
SELECT getSettingOrDefault('custom_undef3', NULL);
```

```response title=Response
my_value
100
NULL
```

{/*AUTOGENERATED_END*/}
