---
description: 'Reference documentation for the ClickHouse `connectionId` function.'
sidebar_label: 'connectionId'
sidebar_position: 17
sidebarTitle: 'connectionId'
slug: /reference/functions/regular-functions/other-functions/connectionId
title: 'connectionId'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import { ExperimentalBadge } from "/snippets/components/ExperimentalBadge/ExperimentalBadge.jsx";

import { CloudNotSupportedBadge } from "/snippets/components/CloudNotSupportedBadge/CloudNotSupportedBadge.jsx";

import { DeprecatedBadge } from "/snippets/components/DeprecatedBadge/DeprecatedBadge.jsx";

## connectionId {#connectionId}

Introduced in: v21.3.0

Returns the connection ID of the client that submitted the current query.
This function is most useful in debugging scenarios.
It was created for compatibility with MySQL's `CONNECTION_ID` function.
It is not typically used in production queries.

<Note>
This function is non-deterministic: it can return different results for the same arguments.
</Note>

**Syntax**

```sql
connectionId()
```

**Arguments**

- None.

**Returned value**

Returns the connection ID of the current client. [`UInt64`](/reference/data-types/int-uint)

**Examples**

**Usage example**

```sql title=Query
SELECT connectionId();
```

```response title=Response
┌─connectionId()─┐
│              0 │
└────────────────┘
```

{/*AUTOGENERATED_END*/}
