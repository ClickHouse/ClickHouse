---
description: 'Reference documentation for the ClickHouse `runningDifferenceStartingWithFirstValue` function.'
sidebar_label: 'runningDifferenceStartingWithFirstValue'
sidebar_position: 105
sidebarTitle: 'runningDifferenceStartingWithFirstValue'
slug: /reference/functions/regular-functions/other-functions/runningDifferenceStartingWithFirstValue
title: 'runningDifferenceStartingWithFirstValue'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import { ExperimentalBadge } from "/snippets/components/ExperimentalBadge/ExperimentalBadge.jsx";

import { CloudNotSupportedBadge } from "/snippets/components/CloudNotSupportedBadge/CloudNotSupportedBadge.jsx";

import { DeprecatedBadge } from "/snippets/components/DeprecatedBadge/DeprecatedBadge.jsx";

## runningDifferenceStartingWithFirstValue {#runningDifferenceStartingWithFirstValue}

Introduced in: v1.1.0

Calculates the difference between consecutive row values in a data block, but unlike [`runningDifference`](/reference/functions/regular-functions/other-functions/runningDifference), it returns the actual value of the first row instead of `0`.

<Warning>
**Deprecated**

Only returns differences inside the currently processed data block.
Because of this error-prone behavior, the function is deprecated.
It is advised to use [window functions](/reference/functions/window-functions/index) instead.

You can use setting `allow_deprecated_error_prone_window_functions` to allow usage of this function.
</Warning>

**Syntax**

```sql
runningDifferenceStartingWithFirstValue(x)
```

**Arguments**

- `x` — Column for which to calculate the running difference. [`Any`](/reference/data-types/index)

**Returned value**

Returns the difference between consecutive values, with the first row's value for the first row. [`Any`](/reference/data-types/index)

**Examples**

**Usage example**

```sql title=Query
SET allow_deprecated_error_prone_window_functions = 1;
SELECT
    number,
    runningDifferenceStartingWithFirstValue(number) AS diff
FROM numbers(5);
```

```response title=Response
┌─number─┬─diff─┐
│      0 │    0 │
│      1 │    1 │
│      2 │    1 │
│      3 │    1 │
│      4 │    1 │
└────────┴──────┘
```

{/*AUTOGENERATED_END*/}
