---
description: 'Reference documentation for the ClickHouse `identity` function.'
sidebar_label: 'identity'
sidebar_position: 71
sidebarTitle: 'identity'
slug: /reference/functions/regular-functions/other-functions/identity
title: 'identity'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import { ExperimentalBadge } from "/snippets/components/ExperimentalBadge/ExperimentalBadge.jsx";

import { CloudNotSupportedBadge } from "/snippets/components/CloudNotSupportedBadge/CloudNotSupportedBadge.jsx";

import { DeprecatedBadge } from "/snippets/components/DeprecatedBadge/DeprecatedBadge.jsx";

## identity {#identity}

Introduced in: v1.1.0

This function returns the argument you pass to it, which is useful for debugging and testing. It lets you bypass index usage to see full scan performance instead. The query analyzer ignores anything inside identity functions when looking for indexes to use, and it also disables constant folding.

**Syntax**

```sql
identity(x)
```

**Arguments**

- `x` — Input value. [`Any`](/reference/data-types/index)

**Returned value**

Returns the input value unchanged. [`Any`](/reference/data-types/index)

**Examples**

**Usage example**

```sql title=Query
SELECT identity(42)
```

```response title=Response
42
```

{/*AUTOGENERATED_END*/}
