---
description: 'Reference documentation for the ClickHouse `displayName` function.'
sidebar_label: 'displayName'
sidebar_position: 30
sidebarTitle: 'displayName'
slug: /reference/functions/regular-functions/other-functions/displayName
title: 'displayName'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import { ExperimentalBadge } from "/snippets/components/ExperimentalBadge/ExperimentalBadge.jsx";

import { CloudNotSupportedBadge } from "/snippets/components/CloudNotSupportedBadge/CloudNotSupportedBadge.jsx";

import { DeprecatedBadge } from "/snippets/components/DeprecatedBadge/DeprecatedBadge.jsx";

## displayName {#displayName}

Introduced in: v22.11.0

Returns the value of `display_name` from [config](/concepts/features/configuration/server-config/configuration-files) or the server's Fully Qualified Domain Name (FQDN) if not set.

<Note>
This function is non-deterministic: it can return different results for the same arguments.
</Note>

**Syntax**

```sql
displayName()
```

**Arguments**

- None.

**Returned value**

Returns the value of `display_name` from config or server FQDN if not set. [`String`](/reference/data-types/string)

**Examples**

**Usage example**

```sql title=Query
SELECT displayName();
```

```response title=Response
┌─displayName()─┐
│ production    │
└───────────────┘
```

{/*AUTOGENERATED_END*/}
