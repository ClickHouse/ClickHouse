---
description: 'Reference documentation for the ClickHouse `initialQueryStartTime` function.'
sidebar_label: 'initialQueryStartTime'
sidebar_position: 75
sidebarTitle: 'initialQueryStartTime'
slug: /reference/functions/regular-functions/other-functions/initialQueryStartTime
title: 'initialQueryStartTime'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import { ExperimentalBadge } from "/snippets/components/ExperimentalBadge/ExperimentalBadge.jsx";

import { CloudNotSupportedBadge } from "/snippets/components/CloudNotSupportedBadge/CloudNotSupportedBadge.jsx";

import { DeprecatedBadge } from "/snippets/components/DeprecatedBadge/DeprecatedBadge.jsx";

## initialQueryStartTime {#initialQueryStartTime}

Introduced in: v25.4.0

Returns the start time of the initial current query.
`initialQueryStartTime` returns the same results on different shards.

<Note>
This function is non-deterministic: it can return different results for the same arguments.
</Note>

**Syntax**

```sql
initialQueryStartTime()
```

**Aliases**: `initial_query_start_time`

**Arguments**

- None.

**Returned value**

Returns the start time of the initial current query. [`DateTime`](/reference/data-types/datetime)

**Examples**

**Usage example**

```sql title=Query
CREATE TABLE tmp (str String) ENGINE = Log;
INSERT INTO tmp (*) VALUES ('a');
SELECT count(DISTINCT t) FROM (SELECT initialQueryStartTime() AS t FROM remote('127.0.0.{1..3}', currentDatabase(), 'tmp') GROUP BY queryID());
```

```response title=Response
┌─count(DISTINCT t)─┐
│                 1 │
└───────────────────┘
```

{/*AUTOGENERATED_END*/}
