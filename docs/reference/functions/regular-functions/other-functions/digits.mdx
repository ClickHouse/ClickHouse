---
description: 'Reference documentation for the ClickHouse `digits` function.'
sidebar_label: 'digits'
sidebar_position: 29
sidebarTitle: 'digits'
slug: /reference/functions/regular-functions/other-functions/digits
title: 'digits'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import { ExperimentalBadge } from "/snippets/components/ExperimentalBadge/ExperimentalBadge.jsx";

import { CloudNotSupportedBadge } from "/snippets/components/CloudNotSupportedBadge/CloudNotSupportedBadge.jsx";

import { DeprecatedBadge } from "/snippets/components/DeprecatedBadge/DeprecatedBadge.jsx";

## digits {#digits}

Introduced in: v26.7.0

Returns the digits of a number `n` which starts at the specified index `offset`.
Counting starts from 1 with the following logic:
- If `offset` is `0`, an exception is thrown, as `offset` is 1-based.
- If `offset` is negative, counting starts `offset` digits from the end of the number, rather than from the beginning.
- If `offset` is greater than the number of digits in `n`, `0` is returned.

An optional argument `length` uses the following logic:
- If `length` is positive, it means number of digits to take from offset
- If `length` is negative, it means number of digits from the right of the number to exclude

See also the [`substring`](/reference/functions/regular-functions/string-functions#substring) function, which performs the analogous operation on strings.
    

**Syntax**

```sql
digits(n, offset[, length])
```

**Arguments**

- `n` — The number to calculate digits from. [`(U)Int8`](/reference/data-types/int-uint) or [`(U)Int16`](/reference/data-types/int-uint) or [`(U)Int32`](/reference/data-types/int-uint) or [`(U)Int64`](/reference/data-types/int-uint)
- `offset` — The starting position of the digit in `n`. [`(U)Int8`](/reference/data-types/int-uint) or [`(U)Int16`](/reference/data-types/int-uint) or [`(U)Int32`](/reference/data-types/int-uint) or [`(U)Int64`](/reference/data-types/int-uint)
- `length` — Optional. The maximum length of the digits. [`(U)Int8`](/reference/data-types/int-uint) or [`(U)Int16`](/reference/data-types/int-uint) or [`(U)Int32`](/reference/data-types/int-uint) or [`(U)Int64`](/reference/data-types/int-uint)

**Returned value**

The selected digits of `n`, interpreted as a `UInt64`. Returns `0` if the selected range is empty. Leading zeros are not preserved. [`UInt64`](/reference/data-types/int-uint)

**Examples**

**Positive offset**

```sql title=Query
SELECT digits(1234567890, 7)
```

```response title=Response
7890
```

**Positive offset and length**

```sql title=Query
SELECT digits(1234567890, 7, 2)
```

```response title=Response
78
```

**Negative offset counts from the right**

```sql title=Query
SELECT digits(1234567890, -3)
```

```response title=Response
890
```

**Negative length excludes digits from the right**

```sql title=Query
SELECT digits(1234567890, 3, -2)
```

```response title=Response
345678
```

**Offset past the end returns 0**

```sql title=Query
SELECT digits(1234567890, 11)
```

```response title=Response
0
```

{/*AUTOGENERATED_END*/}
