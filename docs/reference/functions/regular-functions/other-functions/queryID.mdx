---
description: 'Reference documentation for the ClickHouse `queryID` function.'
sidebar_label: 'queryID'
sidebar_position: 98
sidebarTitle: 'queryID'
slug: /reference/functions/regular-functions/other-functions/queryID
title: 'queryID'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import { ExperimentalBadge } from "/snippets/components/ExperimentalBadge/ExperimentalBadge.jsx";

import { CloudNotSupportedBadge } from "/snippets/components/CloudNotSupportedBadge/CloudNotSupportedBadge.jsx";

import { DeprecatedBadge } from "/snippets/components/DeprecatedBadge/DeprecatedBadge.jsx";

## queryID {#queryID}

Introduced in: v21.9.0

Returns the ID of the current query.
Other parameters of a query can be extracted from field `query_id` in the [`system.query_log`](/reference/system-tables/query_log) table.

In contrast to [`initialQueryID`](/reference/functions/regular-functions/other-functions/initialQueryID) function, `queryID` can return different results on different shards.

<Note>
This function is non-deterministic: it can return different results for the same arguments.
</Note>

**Syntax**

```sql
queryID()
```

**Aliases**: `query_id`

**Arguments**

- None.

**Returned value**

Returns the ID of the current query. [`String`](/reference/data-types/string)

**Examples**

**Usage example**

```sql title=Query
CREATE TABLE tmp (str String) ENGINE = Log;
INSERT INTO tmp (*) VALUES ('a');
SELECT count(DISTINCT t) FROM (SELECT queryID() AS t FROM remote('127.0.0.{1..3}', currentDatabase(), 'tmp') GROUP BY queryID());
```

```response title=Response
┌─count(DISTINCT t)─┐
│                 3 │
└───────────────────┘
```

{/*AUTOGENERATED_END*/}
