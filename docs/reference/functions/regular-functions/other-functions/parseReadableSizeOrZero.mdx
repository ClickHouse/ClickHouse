---
description: 'Reference documentation for the ClickHouse `parseReadableSizeOrZero` function.'
sidebar_label: 'parseReadableSizeOrZero'
sidebar_position: 95
sidebarTitle: 'parseReadableSizeOrZero'
slug: /reference/functions/regular-functions/other-functions/parseReadableSizeOrZero
title: 'parseReadableSizeOrZero'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import { ExperimentalBadge } from "/snippets/components/ExperimentalBadge/ExperimentalBadge.jsx";

import { CloudNotSupportedBadge } from "/snippets/components/CloudNotSupportedBadge/CloudNotSupportedBadge.jsx";

import { DeprecatedBadge } from "/snippets/components/DeprecatedBadge/DeprecatedBadge.jsx";

## parseReadableSizeOrZero {#parseReadableSizeOrZero}

Introduced in: v24.6.0

Given a string containing a byte size and `B`, `KiB`, `KB`, `MiB`, `MB`, etc. as a unit (i.e. [ISO/IEC 80000-13](https://en.wikipedia.org/wiki/ISO/IEC_80000) or decimal byte unit), this function returns the corresponding number of bytes.
If the function is unable to parse the input value, it returns `0`.

The inverse operations of this function are [`formatReadableSize`](/reference/functions/regular-functions/other-functions/formatReadableSize) and [`formatReadableDecimalSize`](/reference/functions/regular-functions/other-functions/formatReadableDecimalSize).

**Syntax**

```sql
parseReadableSizeOrZero(x)
```

**Arguments**

- `x` — Readable size with ISO/IEC 80000-13 or decimal byte unit. [`String`](/reference/data-types/string)

**Returned value**

Returns the number of bytes, rounded up to the nearest integer, or `0` if unable to parse the input. [`UInt64`](/reference/data-types/int-uint)

**Examples**

**Usage example**

```sql title=Query
SELECT arrayJoin(['1 B', '1 KiB', '3 MB', '5.314 KiB', 'invalid']) AS readable_sizes, parseReadableSizeOrZero(readable_sizes) AS sizes;
```

```response title=Response
┌─readable_sizes─┬───sizes─┐
│ 1 B            │       1 │
│ 1 KiB          │    1024 │
│ 3 MB           │ 3000000 │
│ 5.314 KiB      │    5442 │
│ invalid        │       0 │
└────────────────┴─────────┘
```

{/*AUTOGENERATED_END*/}
