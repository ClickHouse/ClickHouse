---
description: 'Reference documentation for the ClickHouse `hostName` function.'
sidebar_label: 'hostName'
sidebar_position: 68
sidebarTitle: 'hostName'
slug: /reference/functions/regular-functions/other-functions/hostName
title: 'hostName'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import { ExperimentalBadge } from "/snippets/components/ExperimentalBadge/ExperimentalBadge.jsx";

import { CloudNotSupportedBadge } from "/snippets/components/CloudNotSupportedBadge/CloudNotSupportedBadge.jsx";

import { DeprecatedBadge } from "/snippets/components/DeprecatedBadge/DeprecatedBadge.jsx";

## hostName {#hostName}

Introduced in: v20.5.0

Returns the name of the host on which this function was executed.
If the function executes on a remote server (distributed processing), the remote server name is returned.
If the function executes in the context of a distributed table, it generates a normal column with values relevant to each shard.
Otherwise it produces a constant value.
    

<Note>
This function is non-deterministic: it can return different results for the same arguments.
</Note>

**Syntax**

```sql
hostName()
```

**Aliases**: `hostname`

**Arguments**

- None.

**Returned value**

Returns the host name. [`String`](/reference/data-types/string)

**Examples**

**Usage example**

```sql title=Query
SELECT hostName()
```

```response title=Response
┌─hostName()─┐
│ clickhouse │
└────────────┘
```

{/*AUTOGENERATED_END*/}
