---
description: 'Reference documentation for the ClickHouse `localtime` function.'
sidebar_label: 'localtime'
sidebar_position: 39
sidebarTitle: 'localtime'
slug: /reference/functions/regular-functions/date-time-functions/localtime
title: 'localtime'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

## localtime {#localtime}

Introduced in: v26.7.0

Returns the current time of day in the server (or session) time zone, at the moment of query analysis.
The function is a constant expression. It is the SQL-standard / PostgreSQL `LOCALTIME`, and is equivalent to `CAST(now() AS Time)`.
    

<Note>
This function is non-deterministic: it can return different results for the same arguments.
</Note>

**Syntax**

```sql
localtime()
```

**Arguments**

- None.

**Returned value**

Returns the current time of day. [`Time`](/reference/data-types/time)

**Examples**

**SQL standard syntax without parentheses**

```sql title=Query
SELECT LOCALTIME
```

```response title=Response
┌───────LOCALTIME─┐
│        07:42:09 │
└─────────────────┘
```

{/*AUTOGENERATED_END*/}
