---
description: 'Reference documentation for the ClickHouse `toFloat64OrDefault` function.'
sidebar_label: 'toFloat64OrDefault'
sidebar_position: 103
sidebarTitle: 'toFloat64OrDefault'
slug: /reference/functions/regular-functions/type-conversion-functions/toFloat64OrDefault
title: 'toFloat64OrDefault'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

## toFloat64OrDefault {#toFloat64OrDefault}

Introduced in: v21.11.0

Like [`toFloat64`](/reference/functions/regular-functions/type-conversion-functions/toFloat64), this function converts an input value to a value of type [Float64](/reference/data-types/float) but returns the default value in case of an error.
If no `default` value is passed then `0` is returned in case of an error.
    

**Syntax**

```sql
toFloat64OrDefault(expr[, default])
```

**Arguments**

- `expr` — Expression returning a number or a string representation of a number. [`String`](/reference/data-types/string) or [`(U)Int*`](/reference/data-types/int-uint) or [`Float*`](/reference/data-types/float)
- `default` — Optional. The default value to return if parsing is unsuccessful. [`Float64`](/reference/data-types/float)

**Returned value**

Returns a value of type Float64 if successful, otherwise returns the default value if passed or 0 if not. [`Float64`](/reference/data-types/float)

**Examples**

**Successful conversion**

```sql title=Query
SELECT toFloat64OrDefault('8', CAST('0', 'Float64'))
```

```response title=Response
8
```

**Failed conversion**

```sql title=Query
SELECT toFloat64OrDefault('abc', CAST('0', 'Float64'))
```

```response title=Response
0
```

{/*AUTOGENERATED_END*/}
