---
description: 'Reference documentation for the ClickHouse `reinterpretAsString` function.'
sidebar_label: 'reinterpretAsString'
sidebar_position: 51
sidebarTitle: 'reinterpretAsString'
slug: /reference/functions/regular-functions/type-conversion-functions/reinterpretAsString
title: 'reinterpretAsString'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

## reinterpretAsString {#reinterpretAsString}

Introduced in: v1.1.0

Reinterprets the input value as a string.
For scalar values, the byte order is normalized to little endian, and null bytes at the end are ignored, for example, the function returns for UInt32 value 255 a string with a single character.
For an `Array` of fixed-size elements, the element bytes are copied verbatim in their native in-memory order, without trimming trailing zero bytes and without normalizing to little endian, so the result can be reinterpreted back to the original `Array` type on any architecture.
    

**Syntax**

```sql
reinterpretAsString(x)
```

**Arguments**

- `x` — Value to reinterpret to string. [`(U)Int*`](/reference/data-types/int-uint) or [`Float*`](/reference/data-types/float) or [`Date`](/reference/data-types/date) or [`DateTime`](/reference/data-types/datetime) or [`Array`](/reference/data-types/array)

**Returned value**

String containing bytes representing `x`. [`String`](/reference/data-types/string)

**Examples**

**Usage example**

```sql title=Query
SELECT
    reinterpretAsString(toDateTime('1970-01-01 01:01:05')),
    reinterpretAsString(toDate('1970-03-07'))
```

```response title=Response
┌─reinterpretAsString(toDateTime('1970-01-01 01:01:05'))─┬─reinterpretAsString(toDate('1970-03-07'))─┐
│ A                                                      │ A                                         │
└────────────────────────────────────────────────────────┴───────────────────────────────────────────┘
```

**Array to String example**

```sql title=Query
SELECT hex(reinterpretAsString([toUInt8(1), toUInt8(2), toUInt8(255)]::Array(UInt8))) AS array_of_UInt8_to_string
```

```response title=Response
┌─array_of_UInt8_to_string─┐
│ 0102FF                   │
└──────────────────────────┘
```

{/*AUTOGENERATED_END*/}
