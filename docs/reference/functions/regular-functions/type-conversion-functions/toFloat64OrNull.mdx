---
description: 'Reference documentation for the ClickHouse `toFloat64OrNull` function.'
sidebar_label: 'toFloat64OrNull'
sidebar_position: 104
sidebarTitle: 'toFloat64OrNull'
slug: /reference/functions/regular-functions/type-conversion-functions/toFloat64OrNull
title: 'toFloat64OrNull'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

## toFloat64OrNull {#toFloat64OrNull}

Introduced in: v1.1.0

Converts an input value to a value of type [Float64](/reference/data-types/float) but returns `NULL` in case of an error.
Like [`toFloat64`](/reference/functions/regular-functions/type-conversion-functions/toFloat64) but returns `NULL` instead of throwing an exception on conversion errors.

Supported arguments:
- Values of type (U)Int*.
- String representations of (U)Int8/16/32/128/256.
- Values of type Float*, including `NaN` and `Inf`.
- String representations of type Float*, including `NaN` and `Inf` (case-insensitive).

Unsupported arguments (return `NULL`):
- String representations of binary and hexadecimal values, e.g. `SELECT toFloat64OrNull('0xc0fe');`.
- Invalid string formats.

See also:
- [`toFloat64`](/reference/functions/regular-functions/type-conversion-functions/toFloat64).
- [`toFloat64OrZero`](/reference/functions/regular-functions/type-conversion-functions/toFloat64OrZero).
- [`toFloat64OrDefault`](/reference/functions/regular-functions/type-conversion-functions/toFloat64OrDefault).
    

**Syntax**

```sql
toFloat64OrNull(x)
```

**Arguments**

- `x` — A string representation of a number. [`String`](/reference/data-types/string)

**Returned value**

Returns a 64-bit Float value if successful, otherwise `NULL`. [`Float64`](/reference/data-types/float) or [`NULL`](/reference/syntax#null)

**Examples**

**Usage example**

```sql title=Query
SELECT
    toFloat64OrNull('42.7'),
    toFloat64OrNull('NaN'),
    toFloat64OrNull('abc')
FORMAT Vertical
```

```response title=Response
Row 1:
──────
toFloat64OrNull('42.7'): 42.7
toFloat64OrNull('NaN'):  nan
toFloat64OrNull('abc'):  \N
```

{/*AUTOGENERATED_END*/}
