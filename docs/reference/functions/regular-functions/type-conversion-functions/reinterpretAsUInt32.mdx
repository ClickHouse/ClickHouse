---
description: 'Reference documentation for the ClickHouse `reinterpretAsUInt32` function.'
sidebar_label: 'reinterpretAsUInt32'
sidebar_position: 55
sidebarTitle: 'reinterpretAsUInt32'
slug: /reference/functions/regular-functions/type-conversion-functions/reinterpretAsUInt32
title: 'reinterpretAsUInt32'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

## reinterpretAsUInt32 {#reinterpretAsUInt32}

Introduced in: v1.1.0

Reinterprets the input value as a value of type UInt32.
Unlike [`CAST`](/reference/functions/regular-functions/type-conversion-functions/CAST), the function does not attempt to preserve the original value - if the target type is not able to represent the input type, the output is undefined.
    

**Syntax**

```sql
reinterpretAsUInt32(x)
```

**Arguments**

- `x` — Value to reinterpret as UInt32. [`(U)Int*`](/reference/data-types/int-uint) or [`Float*`](/reference/data-types/float) or [`Date`](/reference/data-types/date) or [`DateTime`](/reference/data-types/datetime) or [`UUID`](/reference/data-types/uuid) or [`String`](/reference/data-types/string) or [`FixedString`](/reference/data-types/fixedstring)

**Returned value**

Returns the reinterpreted value `x`. [`UInt32`](/reference/data-types/int-uint)

**Examples**

**Usage example**

```sql title=Query
SELECT
    toUInt16(257) AS x,
    toTypeName(x),
    reinterpretAsUInt32(x) AS res,
    toTypeName(res)
```

```response title=Response
┌───x─┬─toTypeName(x)─┬─res─┬─toTypeName(res)─┐
│ 257 │ UInt16        │ 257 │ UInt32          │
└─────┴───────────────┴─────┴─────────────────┘
```

{/*AUTOGENERATED_END*/}
