---
description: 'Reference documentation for the ClickHouse `toInt256OrZero` function.'
sidebar_label: 'toInt256OrZero'
sidebar_position: 117
sidebarTitle: 'toInt256OrZero'
slug: /reference/functions/regular-functions/type-conversion-functions/toInt256OrZero
title: 'toInt256OrZero'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

## toInt256OrZero {#toInt256OrZero}

Introduced in: v20.8.0

Converts an input value to type [Int256](/reference/data-types/int-uint) but returns `0` in case of an error.
Like [`toInt256`](/reference/functions/regular-functions/type-conversion-functions/toInt256) but returns `0` instead of throwing an exception.

See also:
- [`toInt256`](/reference/functions/regular-functions/type-conversion-functions/toInt256).
- [`toInt256OrNull`](/reference/functions/regular-functions/type-conversion-functions/toInt256OrNull).
- [`toInt256OrDefault`](/reference/functions/regular-functions/type-conversion-functions/toInt256OrDefault).
    

**Syntax**

```sql
toInt256OrZero(x)
```

**Arguments**

- `x` — Input value to convert. [`String`](/reference/data-types/string) or [`FixedString`](/reference/data-types/fixedstring) or [`Float*`](/reference/data-types/float) or [`Decimal`](/reference/data-types/decimal) or [`(U)Int*`](/reference/data-types/int-uint) or [`Date`](/reference/data-types/date) or [`DateTime`](/reference/data-types/datetime)

**Returned value**

Returns the converted input value, otherwise `0` if conversion fails. [`Int256`](/reference/data-types/int-uint)

**Examples**

**Usage example**

```sql title=Query
SELECT toInt256OrZero('123')
```

```response title=Response
123
```

**Failed conversion returns zero**

```sql title=Query
SELECT toInt256OrZero('abc')
```

```response title=Response
0
```

{/*AUTOGENERATED_END*/}
