---
description: 'Reference documentation for the ClickHouse `reinterpret` function.'
sidebar_label: 'reinterpret'
sidebar_position: 39
sidebarTitle: 'reinterpret'
slug: /reference/functions/regular-functions/type-conversion-functions/reinterpret
title: 'reinterpret'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

## reinterpret {#reinterpret}

Introduced in: v1.1.0

Uses the same source in-memory bytes sequence for the provided value `x` and reinterprets it to the destination type.
When the destination type is `String`, a source `Array` is supported only if its element type is fixed size and contiguous in memory (for example, `Array(UInt8)` or `Array(FixedString(2))`); its element bytes are copied verbatim in their native in-memory order, so the result can be reinterpreted back to the original `Array` type on any architecture. Arrays whose elements are variable-length (for example, `Array(String)`) or nullable (for example, `Array(Nullable(Int32))`) are not supported.
    

**Syntax**

```sql
reinterpret(x, type)
```

**Arguments**

- `x` — Value to reinterpret. When reinterpreting to `String`, a source `Array` must have fixed-size, contiguous elements. [`Any`](/reference/data-types/index)
- `type` — Destination type. If it is an array, then the array element type must be a fixed length type. [`String`](/reference/data-types/string)

**Returned value**

Destination type value. [`Any`](/reference/data-types/index)

**Examples**

**Usage example**

```sql title=Query
SELECT reinterpret(toInt8(-1), 'UInt8') AS int_to_uint,
    reinterpret(toInt8(1), 'Float32') AS int_to_float,
    reinterpret('1', 'UInt32') AS string_to_int
```

```response title=Response
┌─int_to_uint─┬─int_to_float─┬─string_to_int─┐
│         255 │        1e-45 │            49 │
└─────────────┴──────────────┴───────────────┘
```

**Array example**

```sql title=Query
SELECT reinterpret(x'3108b4403108d4403108b4403108d440', 'Array(Float32)') AS string_to_array_of_Float32
```

```response title=Response
┌─string_to_array_of_Float32─┐
│ [5.626,6.626,5.626,6.626]  │
└────────────────────────────┘
```

**Array to String example**

```sql title=Query
SELECT hex(reinterpret([toUInt8(1), toUInt8(2), toUInt8(255)]::Array(UInt8), 'String')) AS array_of_UInt8_to_string
```

```response title=Response
┌─array_of_UInt8_to_string─┐
│ 0102FF                   │
└──────────────────────────┘
```

{/*AUTOGENERATED_END*/}
