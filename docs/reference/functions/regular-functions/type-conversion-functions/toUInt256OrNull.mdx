---
description: 'Reference documentation for the ClickHouse `toUInt256OrNull` function.'
sidebar_label: 'toUInt256OrNull'
sidebar_position: 161
sidebarTitle: 'toUInt256OrNull'
slug: /reference/functions/regular-functions/type-conversion-functions/toUInt256OrNull
title: 'toUInt256OrNull'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

## toUInt256OrNull {#toUInt256OrNull}

Introduced in: v20.8.0

Like [`toUInt256`](/reference/functions/regular-functions/type-conversion-functions/toUInt256), this function converts an input value to a value of type [`UInt256`](/reference/data-types/int-uint) but returns `NULL` in case of an error.

Supported arguments:
- String representations of (U)Int*.

Unsupported arguments (return `NULL`):
- String representations of Float* values, including `NaN` and `Inf`.
- String representations of binary and hexadecimal values, e.g. `SELECT toUInt256OrNull('0xc0fe');`.

<Note>
If the input value cannot be represented within the bounds of [`UInt256`](/reference/data-types/int-uint), overflow or underflow of the result occurs.
This is not considered an error.
</Note>

See also:
- [`toUInt256`](/reference/functions/regular-functions/type-conversion-functions/toUInt256).
- [`toUInt256OrZero`](/reference/functions/regular-functions/type-conversion-functions/toUInt256OrZero).
- [`toUInt256OrDefault`](/reference/functions/regular-functions/type-conversion-functions/toUInt256OrDefault).
    

**Syntax**

```sql
toUInt256OrNull(x)
```

**Arguments**

- `x` — A String representation of a number. [`String`](/reference/data-types/string)

**Returned value**

Returns a value of type UInt256, otherwise `NULL` if the conversion is unsuccessful. [`UInt256`](/reference/data-types/int-uint) or [`NULL`](/reference/syntax#null)

**Examples**

**Usage example**

```sql title=Query
SELECT
    toUInt256OrNull('256'),
    toUInt256OrNull('abc')
FORMAT Vertical
```

```response title=Response
Row 1:
──────
toUInt256OrNull('256'): 256
toUInt256OrNull('abc'): \N
```

{/*AUTOGENERATED_END*/}
