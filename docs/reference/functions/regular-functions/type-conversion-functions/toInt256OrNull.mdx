---
description: 'Reference documentation for the ClickHouse `toInt256OrNull` function.'
sidebar_label: 'toInt256OrNull'
sidebar_position: 116
sidebarTitle: 'toInt256OrNull'
slug: /reference/functions/regular-functions/type-conversion-functions/toInt256OrNull
title: 'toInt256OrNull'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

## toInt256OrNull {#toInt256OrNull}

Introduced in: v20.8.0

Like [`toInt256`](/reference/functions/regular-functions/type-conversion-functions/toInt256), this function converts an input value to a value of type [Int256](/reference/data-types/int-uint) but returns `NULL` in case of an error.

Supported arguments:
- String representations of (U)Int*.

Unsupported arguments (return `NULL`):
- String representations of Float* values, including `NaN` and `Inf`.
- String representations of binary and hexadecimal values, e.g. `SELECT toInt256OrNull('0xc0fe');`.

<Note>
If the input value cannot be represented within the bounds of [Int256](/reference/data-types/int-uint), overflow or underflow of the result occurs.
This is not considered an error.
</Note>

See also:
- [`toInt256`](/reference/functions/regular-functions/type-conversion-functions/toInt256).
- [`toInt256OrZero`](/reference/functions/regular-functions/type-conversion-functions/toInt256OrZero).
- [`toInt256OrDefault`](/reference/functions/regular-functions/type-conversion-functions/toInt256OrDefault).
    

**Syntax**

```sql
toInt256OrNull(x)
```

**Arguments**

- `x` — A String representation of a number. [`String`](/reference/data-types/string)

**Returned value**

Returns a value of type Int256, otherwise `NULL` if the conversion is unsuccessful. [`Int256`](/reference/data-types/int-uint) or [`NULL`](/reference/syntax#null)

**Examples**

**Usage example**

```sql title=Query
SELECT
    toInt256OrNull('-256'),
    toInt256OrNull('abc')
FORMAT Vertical
```

```response title=Response
Row 1:
──────
toInt256OrNull('-256'): -256
toInt256OrNull('abc'):  \N
```

{/*AUTOGENERATED_END*/}
