---
description: 'Reference documentation for the ClickHouse `parseDateTime32BestEffortOrNull` function.'
sidebar_label: 'parseDateTime32BestEffortOrNull'
sidebar_position: 14
sidebarTitle: 'parseDateTime32BestEffortOrNull'
slug: /reference/functions/regular-functions/type-conversion-functions/parseDateTime32BestEffortOrNull
title: 'parseDateTime32BestEffortOrNull'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

## parseDateTime32BestEffortOrNull {#parseDateTime32BestEffortOrNull}

Introduced in: v20.9.0

Same as [`parseDateTime32BestEffort`](/reference/functions/regular-functions/type-conversion-functions/parseDateTime32BestEffort) except that it returns `NULL` when it encounters a date format that cannot be processed.
    

**Syntax**

```sql
parseDateTime32BestEffortOrNull(time_string[, time_zone])
```

**Arguments**

- `time_string` — String containing a date and time to convert. [`String`](/reference/data-types/string)
- `time_zone` — Optional. Time zone according to which `time_string` is parsed. [`String`](/reference/data-types/string)

**Returned value**

Returns a `DateTime` object parsed from the string, or `NULL` if the parsing fails. [`DateTime`](/reference/data-types/datetime)

**Examples**

**Usage example**

```sql title=Query
SELECT
    parseDateTime32BestEffortOrNull('23/10/2025 12:12:57') AS valid,
    parseDateTime32BestEffortOrNull('invalid date') AS invalid
```

```response title=Response
┌─valid───────────────┬─invalid─┐
│ 2025-10-23 12:12:57 │    ᴺᵁᴸᴸ │
└─────────────────────┴─────────┘
```

{/*AUTOGENERATED_END*/}
