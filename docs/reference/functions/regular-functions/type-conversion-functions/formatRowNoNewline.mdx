---
description: 'Reference documentation for the ClickHouse `formatRowNoNewline` function.'
sidebar_label: 'formatRowNoNewline'
sidebar_position: 7
sidebarTitle: 'formatRowNoNewline'
slug: /reference/functions/regular-functions/type-conversion-functions/formatRowNoNewline
title: 'formatRowNoNewline'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

## formatRowNoNewline {#formatRowNoNewline}

Introduced in: v20.7.0

Same as [`formatRow`](/reference/functions/regular-functions/type-conversion-functions/formatRow), but trims the newline character of each row.

Converts arbitrary expressions into a string via given format, but removes any trailing newline characters from the result.
    

**Syntax**

```sql
formatRowNoNewline(format, x, y, ...)
```

**Arguments**

- `format` — Text format. For example, CSV, TSV. [`String`](/reference/data-types/string)
- `x, y, ...` — Expressions. [`Any`](/reference/data-types/index)

**Returned value**

Returns a formatted string with newlines removed. [`String`](/reference/data-types/string)

**Examples**

**Basic usage**

```sql title=Query
SELECT formatRowNoNewline('CSV', number, 'good')
FROM numbers(3)
```

```response title=Response
┌─formatRowNoNewline('CSV', number, 'good')─┐
│ 0,"good"                                  │
│ 1,"good"                                  │
│ 2,"good"                                  │
└───────────────────────────────────────────┘
```

{/*AUTOGENERATED_END*/}
