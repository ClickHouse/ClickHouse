---
description: 'Reference documentation for the ClickHouse `toDateOrDefault` function.'
sidebar_label: 'toDateOrDefault'
sidebar_position: 68
sidebarTitle: 'toDateOrDefault'
slug: /reference/functions/regular-functions/type-conversion-functions/toDateOrDefault
title: 'toDateOrDefault'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

## toDateOrDefault {#toDateOrDefault}

Introduced in: v21.11.0

Like [toDate](/reference/functions/regular-functions/type-conversion-functions/toDate) but if unsuccessful, returns a default value which is either the second argument (if specified), or otherwise the lower boundary of [Date](/reference/data-types/date).
    

**Syntax**

```sql
toDateOrDefault(expr[, default])
```

**Arguments**

- `expr` — Expression returning a number or a string representation of a number. [`String`](/reference/data-types/string) or [`(U)Int*`](/reference/data-types/int-uint) or [`Float*`](/reference/data-types/float)
- `default` — Optional. The default value to return if parsing is unsuccessful. [`Date`](/reference/data-types/date)

**Returned value**

Value of type Date if successful, otherwise returns the default value if passed or 1970-01-01 if not. [`Date`](/reference/data-types/date)

**Examples**

**Successful conversion**

```sql title=Query
SELECT toDateOrDefault('2022-12-30')
```

```response title=Response
2022-12-30
```

**Failed conversion**

```sql title=Query
SELECT toDateOrDefault('', CAST('2023-01-01', 'Date'))
```

```response title=Response
2023-01-01
```

{/*AUTOGENERATED_END*/}
