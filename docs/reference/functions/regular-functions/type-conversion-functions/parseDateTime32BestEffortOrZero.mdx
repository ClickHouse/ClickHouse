---
description: 'Reference documentation for the ClickHouse `parseDateTime32BestEffortOrZero` function.'
sidebar_label: 'parseDateTime32BestEffortOrZero'
sidebar_position: 15
sidebarTitle: 'parseDateTime32BestEffortOrZero'
slug: /reference/functions/regular-functions/type-conversion-functions/parseDateTime32BestEffortOrZero
title: 'parseDateTime32BestEffortOrZero'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

## parseDateTime32BestEffortOrZero {#parseDateTime32BestEffortOrZero}

Introduced in: v20.9.0

Same as [`parseDateTime32BestEffort`](/reference/functions/regular-functions/type-conversion-functions/parseDateTime32BestEffort) except that it returns a zero date or a zero date time when it encounters a date format that cannot be processed.
    

**Syntax**

```sql
parseDateTime32BestEffortOrZero(time_string[, time_zone])
```

**Arguments**

- `time_string` — String containing a date and time to convert. [`String`](/reference/data-types/string)
- `time_zone` — Optional. Time zone according to which `time_string` is parsed. [`String`](/reference/data-types/string)

**Returned value**

Returns a `DateTime` object parsed from the string, or zero date (`1970-01-01 00:00:00`) if the parsing fails. [`DateTime`](/reference/data-types/datetime)

**Examples**

**Usage example**

```sql title=Query
SELECT
    parseDateTime32BestEffortOrZero('23/10/2025 12:12:57') AS valid,
    parseDateTime32BestEffortOrZero('invalid date') AS invalid
```

```response title=Response
┌─valid───────────────┬─invalid─────────────┐
│ 2025-10-23 12:12:57 │ 1970-01-01 00:00:00 │
└─────────────────────┴─────────────────────┘
```

{/*AUTOGENERATED_END*/}
