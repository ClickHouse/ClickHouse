---
description: 'Reference documentation for the ClickHouse `toInt8OrDefault` function.'
sidebar_label: 'toInt8OrDefault'
sidebar_position: 127
sidebarTitle: 'toInt8OrDefault'
slug: /reference/functions/regular-functions/type-conversion-functions/toInt8OrDefault
title: 'toInt8OrDefault'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

## toInt8OrDefault {#toInt8OrDefault}

Introduced in: v21.11.0

Like [`toInt8`](/reference/functions/regular-functions/type-conversion-functions/toInt8), this function converts an input value to a value of type [Int8](/reference/data-types/int-uint) but returns the default value in case of an error.
If no `default` value is passed then `0` is returned in case of an error.
    

**Syntax**

```sql
toInt8OrDefault(expr[, default])
```

**Arguments**

- `expr` — Expression returning a number or a string representation of a number. [`String`](/reference/data-types/string) or [`(U)Int*`](/reference/data-types/int-uint) or [`Float*`](/reference/data-types/float)
- `default` — Optional. The default value to return if parsing is unsuccessful. [`Int8`](/reference/data-types/int-uint)

**Returned value**

Returns a value of type Int8 if successful, otherwise returns the default value if passed, or 0 if not. [`Int8`](/reference/data-types/int-uint)

**Examples**

**Successful conversion**

```sql title=Query
SELECT toInt8OrDefault('-8', CAST('-1', 'Int8'))
```

```response title=Response
-8
```

**Failed conversion**

```sql title=Query
SELECT toInt8OrDefault('abc', CAST('-1', 'Int8'))
```

```response title=Response
-1
```

{/*AUTOGENERATED_END*/}
