---
description: 'A full-text (inverted) index over the tokens of a string column, for speeding up text search functions such as `hasToken`, `hasAnyTokens`, `hasAllTokens`, and `hasPhrase`.'
sidebarTitle: 'text'
slug: /engines/table-engines/mergetree-family/skipping-indexes/text
title: 'text skipping index'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}
A full-text (inverted) index over the tokens of a string column, for speeding up text search functions such as `hasToken`, `hasAnyTokens`, `hasAllTokens`, and `hasPhrase`.

## Syntax {#syntax}

```sql
INDEX name expr TYPE text(tokenizer = splitByNonAlpha) GRANULARITY g
```

## Related {#related}

- [`tokenbf_v1`](/reference/engines/table-engines/mergetree-family/skipping-indexes/tokenbf_v1)

<Badge className="autogenerated-notice" size="xs" icon="edit" color="gray">Auto-generated: 2026-07-08</Badge>
{/*AUTOGENERATED_END*/}
