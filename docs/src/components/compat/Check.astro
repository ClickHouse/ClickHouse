---
/** Mintlify `<Check>` callout rendered with Nimbus's Aside. */
import { Aside } from "@/components/ui/aside";
interface Props { title?: string }
const title = Astro.props.title ?? "Success";
---

<Aside type="tip" title={title}><slot /></Aside>
