---
/** Mintlify `<Warning>` callout rendered with Nimbus's Aside. */
import { Aside } from "@/components/ui/aside";
interface Props { title?: string }
const title = Astro.props.title ?? "Warning";
---

<Aside type="caution" title={title}><slot /></Aside>
