---
/** Mintlify `<Info>` callout rendered with Nimbus's Aside. */
import { Aside } from "@/components/ui/aside";
interface Props { title?: string }
const title = Astro.props.title;
---

<Aside type="info" title={title}><slot /></Aside>
