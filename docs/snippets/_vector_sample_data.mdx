If you wish to experiment with a sample dataset, download the following example nginx sample.

```bash
curl -O https://datasets-documentation.s3.eu-west-3.amazonaws.com/clickstack-integrations/access.log
```

<Note>
This data has been collected from an Nginx instance configured to output logs in JSON format for easier parsing. For the Nginx configuration for these logs, see {<a href={nginxLogsUrl}>"Monitoring Nginx Logs with ClickStack"</a>}.
</Note>
