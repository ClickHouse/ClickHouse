---
description: 'MergeTree 테이블의 분리된 파트 정보를 담고 있는 시스템 테이블'
keywords: ['시스템 테이블', 'detached_parts']
slug: /operations/system-tables/detached_parts
title: 'system.detached_parts'
doc_type: '참고'
---

<div id="purpose">
  ## 설명
</div>

[MergeTree](../../engines/table-engines/mergetree-family/mergetree.md) 테이블의 분리된 파트에 대한 정보를 담고 있습니다. `reason` 컬럼은 파트가 분리된 이유를 나타냅니다.

사용자가 분리한 파트의 경우 reason은 비어 있습니다. 이러한 파트는 [ALTER TABLE ATTACH PARTITION|PART](/ko/sql-reference/statements/alter/partition#attach-partitionpart) 명령으로 다시 연결할 수 있습니다.

다른 컬럼에 대한 설명은 [system.parts](../../operations/system-tables/parts.md)를 참조하십시오.

파트 이름이 올바르지 않으면 일부 컬럼 값이 `NULL`일 수 있습니다. 이러한 파트는 [ALTER TABLE DROP DETACHED PART](/ko/sql-reference/statements/alter/partition#drop-detached-partitionpart)로 삭제할 수 있습니다.

<div id="columns">
  ## 컬럼
</div>

{/*AUTOGENERATED_START*/ }

* `database` ([String](/ko/sql-reference/data-types/string)) — 이 파트가 속한 데이터베이스의 이름입니다.
* `table` ([String](/ko/sql-reference/data-types/string)) — 이 파트가 속한 테이블의 이름입니다.
* `partition_id` ([Nullable(String)](/ko/sql-reference/data-types/nullable)) — 이 파트가 속한 파티션의 식별자입니다.
* `name` ([String](/ko/sql-reference/data-types/string)) — 이 파트의 이름입니다.
* `bytes_on_disk` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 모든 데이터 파트 파일의 총 크기(바이트)입니다.
* `modification_time` ([DateTime](/ko/sql-reference/data-types/datetime)) — 데이터 파트가 있는 디렉터리가 수정된 시각입니다. 일반적으로 파트가 분리된 시점에 해당합니다.
* `disk` ([String](/ko/sql-reference/data-types/string)) — 이 데이터 파트를 저장하는 디스크의 이름입니다.
* `path` ([String](/ko/sql-reference/data-types/string)) — 이 데이터 파트 파일이 저장된 디스크 경로입니다.
* `reason` ([Nullable(String)](/ko/sql-reference/data-types/nullable)) — 이 파트가 분리된 이유에 대한 설명입니다.
* `min_block_number` ([Nullable(Int64)](/ko/sql-reference/data-types/nullable)) — 머지 후 현재 파트를 구성하는 데이터 파트의 최소 번호입니다.
* `max_block_number` ([Nullable(Int64)](/ko/sql-reference/data-types/nullable)) — 머지 후 현재 파트를 구성하는 데이터 파트의 최대 번호입니다.
* `level` ([Nullable(UInt32)](/ko/sql-reference/data-types/nullable)) — 머지 트리의 깊이입니다. 0은 현재 파트가 다른 파트를 머지해서 생성된 것이 아니라 삽입으로 생성되었음을 의미합니다.

{/*AUTOGENERATED_END*/ }