---
description: '모든 파일 시스템 캐시 설정에 관한 정보를 제공합니다'
keywords: ['시스템 테이블', 'filesystem_cache_settings']
slug: /operations/system-tables/filesystem_cache_settings
title: 'system.filesystem_cache_settings'
doc_type: '참고'
---

<div id="description">
  ## 설명
</div>

모든 파일 시스템 캐시 설정에 관한 정보를 제공합니다

<div id="columns">
  ## 컬럼
</div>

{/*AUTOGENERATED_START*/ }

* `cache_name` ([String](/ko/sql-reference/data-types/string)) — 캐시 이름
* `path` ([String](/ko/sql-reference/data-types/string)) — 캐시 디렉터리 경로
* `max_size` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 캐시의 최대 크기
* `max_elements` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 캐시 요소의 최대 개수(예: file segment, 파일 시스템의 파일 수를 제한함)
* `max_file_segment_size` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 단일 file segment의 최대 크기
* `boundary_alignment` ([UInt64](/ko/sql-reference/data-types/int-uint)) — file segment 정렬
* `cache_on_write_operations` ([UInt8](/ko/sql-reference/data-types/int-uint)) — write-through 캐시를 활성화합니다(INSERT 및 MERGE 시 캐시됨)
* `cache_policy` ([String](/ko/sql-reference/data-types/string)) — 캐시 eviction 정책
* `slru_size_ratio` ([Float64](/ko/sql-reference/data-types/float)) — 보호된 요소와 probationary 요소 간 SLRU 캐시 정책의 크기 비율
* `background_download_threads` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 백그라운드 다운로드 스레드 수입니다. 값이 0이면 백그라운드 다운로드가 비활성화됩니다
* `background_download_queue_size_limit` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 백그라운드 다운로드 큐의 크기입니다. 값이 0이면 백그라운드 다운로드가 비활성화됩니다
* `background_download_max_file_segment_size` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 백그라운드 다운로드로 내려받을 수 있는 최대 크기
* `load_metadata_threads` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 서버 시작 시 캐시 메타데이터를 로드하는 데 사용하는 최대 스레드 수
* `load_metadata_asynchronously` ([UInt8](/ko/sql-reference/data-types/int-uint)) — 서버 시작 시 메타데이터를 비동기로 로드하도록 활성화합니다
* `keep_free_space_size_ratio` ([Float64](/ko/sql-reference/data-types/float)) — 캐시가 백그라운드에서 유지하려는 여유 공간 비율
* `keep_free_space_elements_ratio` ([Float64](/ko/sql-reference/data-types/float)) — 캐시가 백그라운드에서 유지하려는 여유 요소 비율
* `keep_free_space_remove_batch` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 여유 공간/요소 비율을 유지하는 백그라운드 스레드가 한 번에 제거하는 캐시 요소 수
* `invalidated_entries_cleanup_interval_ms` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 무효화된(지연 제거된) priority queue 항목을 purge하는 백그라운드 작업의 유휴 인터벌(밀리초)
* `invalidated_entries_cleanup_threshold` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 누적된 무효화 priority queue 항목 수이며, 이 값에 도달하면 백그라운드 제거가 트리거됩니다
* `invalidated_entries_cleanup_remove_batch` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 각 백그라운드 반복에서 단일 쓰기 잠금으로 제거되는 무효화 priority queue 항목의 최대 수
* `enable_filesystem_query_cache_limit` ([UInt8](/ko/sql-reference/data-types/int-uint)) — 쿼리 내에서 쓸 수 있는 캐시의 최대 크기 제한을 활성화합니다
* `cache_hits_threshold` ([UInt64](/ko/sql-reference/data-types/int-uint)) — Deprecated 설정
* `enable_bypass_cache_with_threshold` ([UInt8](/ko/sql-reference/data-types/int-uint)) — 문서화되지 않았습니다. 사용을 권장하지 않습니다
* `bypass_cache_threshold` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 문서화되지 않았습니다. 사용을 권장하지 않습니다
* `write_cache_per_user_id_directory` ([UInt8](/ko/sql-reference/data-types/int-uint)) — ClickHouse Cloud 내부 설정
* `allow_dynamic_cache_resize` ([UInt8](/ko/sql-reference/data-types/int-uint)) — 파일 시스템 캐시의 크기를 동적으로 조정하도록 허용합니다
* `max_size_ratio_to_total_space` ([Float64](/ko/sql-reference/data-types/float)) — 전체 디스크 공간 대비 `max_size`의 비율
* `skip_cache_on_disk_failure` ([UInt8](/ko/sql-reference/data-types/int-uint)) — true이면 디스크 IO 오류 발생 시 파일 시스템 캐시 작업을 오류 없이 자동으로 우회합니다. false(기본값)이면 디스크 IO 오류가 시작 실패로 전파됩니다.
* `use_split_cache` ([UInt8](/ko/sql-reference/data-types/int-uint)) — 파일을 시스템/데이터로 분리하는 캐시를 사용합니다.
* `split_cache_ratio` ([Float64](/ko/sql-reference/data-types/float)) — split&#95;cache에서 전체 캐시 크기 대비 시스템 세그먼트의 비율
* `overcommit_eviction_evict_step` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 오버커밋 축출 정책의 바이트 단위 축출 간격입니다. `keep&#95;free&#95;space&#95;*&#95;ratio` 설정에 사용됩니다
* `check_cache_probability` ([Float64](/ko/sql-reference/data-types/float)) — debug 또는 새니타이저 빌드에서만 동작합니다. 전체 캐시를 순회하며 각 캐시 요소의 상태를 확인해 캐시의 정확성을 검사합니다
* `expose_prometheus_eviction_metrics` ([UInt8](/ko/sql-reference/data-types/int-uint)) — 파일 시스템 캐시 축출 활동에 대한 Prometheus 메트릭(`filesystem_cache_evictions_total` 등)을 노출합니다. 기본적으로 비활성화되어 있습니다. `SYSTEM RELOAD CONFIG`를 통해 런타임에 전환할 수 있습니다.
* `expose_prometheus_eviction_metrics_per_user` ([UInt8](/ko/sql-reference/data-types/int-uint)) — 추가로 사용자 ID별 축출 메트릭을 노출합니다. `expose_prometheus_eviction_metrics`가 필요합니다. 카디널리티는 축출을 수행하는 고유 사용자 수에 따라 증가합니다.
* `is_initialized` ([UInt8](/ko/sql-reference/data-types/int-uint)) — 캐시가 성공적으로 초기화되었는지 나타냅니다
* `current_size` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 현재 캐시 크기
* `current_elements_num` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 현재 캐시 요소(파일 세그먼트) 개수

{/*AUTOGENERATED_END*/ }