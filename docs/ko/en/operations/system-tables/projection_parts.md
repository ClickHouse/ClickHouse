---
description: 'MergeTree 엔진 계열 테이블의 프로젝션 파트 정보를 담고 있는 시스템 테이블입니다.'
keywords: ['시스템 테이블', 'projection_parts']
slug: /operations/system-tables/projection_parts
title: 'system.projection_parts'
doc_type: 'reference'
---

<div id="description">
  ## 설명
</div>

이 테이블에는 MergeTree 엔진 계열 테이블의 프로젝션 파트 정보가 포함되어 있습니다.

<div id="columns">
  ## 컬럼
</div>

{/*AUTOGENERATED_START*/ }

* `partition` ([String](/ko/sql-reference/data-types/string)) — 파티션 이름입니다.
* `name` ([String](/ko/sql-reference/data-types/string)) — 데이터 파트의 이름입니다.
* `part_type` ([String](/ko/sql-reference/data-types/string)) — 데이터 파트의 저장 포맷입니다. 가능한 값: Wide(컬럼별 파일 1개) 및 Compact(모든 컬럼을 하나의 파일에 저장)입니다.
* `parent_name` ([String](/ko/sql-reference/data-types/string)) — 원본(상위) 데이터 파트의 이름입니다.
* `parent_uuid` ([UUID](/ko/sql-reference/data-types/uuid)) — 원본(상위) 데이터 파트의 UUID입니다.
* `parent_part_type` ([String](/ko/sql-reference/data-types/string)) — 원본(상위) 데이터 파트의 저장 포맷입니다.
* `active` ([UInt8](/ko/sql-reference/data-types/int-uint)) — 데이터 파트가 활성 상태인지 나타내는 플래그입니다. 데이터 파트가 활성 상태이면 테이블에서 사용됩니다. 그렇지 않으면 곧 삭제될 예정입니다. 비활성 데이터 파트는 머지 및 변경 작업 이후에 나타납니다.
* `marks` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 마크 수입니다. 데이터 파트의 대략적인 행 수를 구하려면 마크 수에 인덱스 세분화 수준(보통 8192)을 곱하십시오(이 힌트는 적응형 세분화 수준에는 적용되지 않습니다).
* `rows` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 행 수입니다.
* `bytes_on_disk` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 모든 데이터 파트 파일의 총 크기(바이트)입니다.
* `data_compressed_bytes` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 데이터 파트에 있는 압축된 데이터의 총 크기입니다. 모든 보조 파일(예: 마크 파일)은 포함되지 않습니다.
* `data_uncompressed_bytes` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 데이터 파트에 있는 비압축 데이터의 총 크기입니다. 모든 보조 파일(예: 마크 파일)은 포함되지 않습니다.
* `marks_bytes` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 마크 파일의 크기입니다.
* `parent_marks` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 원본(상위) 파트의 마크 수입니다.
* `parent_rows` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 원본(상위) 파트의 행 수입니다.
* `parent_bytes_on_disk` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 모든 원본(상위) 데이터 파트 파일의 총 크기(바이트)입니다.
* `parent_data_compressed_bytes` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 원본(상위) 데이터 파트에 있는 압축된 데이터의 총 크기입니다.
* `parent_data_uncompressed_bytes` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 원본(상위) 데이터 파트에 있는 비압축 데이터의 총 크기입니다.
* `parent_marks_bytes` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 원본(상위) 데이터 파트의 마크 파일 크기입니다.
* `modification_time` ([DateTime](/ko/sql-reference/data-types/datetime)) — 데이터 파트가 있는 디렉터리가 수정된 시각입니다. 일반적으로 데이터 파트가 생성된 시각에 해당합니다.
* `remove_time` ([DateTime](/ko/sql-reference/data-types/datetime)) — 데이터 파트가 비활성 상태가 된 시각입니다.
* `refcount` ([UInt32](/ko/sql-reference/data-types/int-uint)) — 데이터 파트가 사용되는 위치 수입니다. 값이 2보다 크면 해당 데이터 파트가 쿼리 또는 머지에 사용되고 있음을 나타냅니다.
* `min_date` ([Date](/ko/sql-reference/data-types/date)) — 데이터 파트 내 날짜 키의 최솟값입니다.
* `max_date` ([Date](/ko/sql-reference/data-types/date)) — 데이터 파트 내 날짜 키의 최댓값입니다.
* `min_time` ([DateTime](/ko/sql-reference/data-types/datetime)) — 데이터 파트 내 날짜 및 시간 키의 최솟값입니다.
* `max_time` ([DateTime](/ko/sql-reference/data-types/datetime)) — 데이터 파트의 날짜 및 시간 키 최댓값입니다.
* `partition_id` ([String](/ko/sql-reference/data-types/string)) — 파티션 ID입니다.
* `min_block_number` ([Int64](/ko/sql-reference/data-types/int-uint)) — 머지 후 현재 파트를 구성하는 데이터 파트 수의 최솟값입니다.
* `max_block_number` ([Int64](/ko/sql-reference/data-types/int-uint)) — 머지 후 현재 파트를 구성하는 데이터 파트 수의 최댓값입니다.
* `level` ([UInt32](/ko/sql-reference/data-types/int-uint)) — 머지 트리의 깊이입니다. 0은 현재 파트가 다른 파트를 머지해서 생성된 것이 아니라 삽입으로 생성되었음을 의미합니다.
* `data_version` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 데이터 파트에 적용할 뮤테이션을 결정하는 데 사용되는 숫자입니다(`data&#95;version`보다 높은 버전의 뮤테이션).
* `primary_key_bytes_in_memory` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 기본 키 값이 사용하는 메모리 크기(바이트)입니다.
* `primary_key_bytes_in_memory_allocated` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 기본 키 값에 예약된 메모리 크기(바이트)입니다.
* `is_frozen` ([UInt8](/ko/sql-reference/data-types/int-uint)) — 파티션 데이터 백업의 존재 여부를 나타내는 플래그입니다. 1이면 백업이 존재하고, 0이면 백업이 존재하지 않습니다.
* `database` ([String](/ko/sql-reference/data-types/string)) — 데이터베이스 이름입니다.
* `table` ([String](/ko/sql-reference/data-types/string)) — 테이블 이름입니다.
* `engine` ([String](/ko/sql-reference/data-types/string)) — 매개변수를 제외한 테이블 엔진 이름입니다.
* `disk_name` ([String](/ko/sql-reference/data-types/string)) — 데이터 파트를 저장하는 디스크 이름입니다.
* `path` ([String](/ko/sql-reference/data-types/string)) — 데이터 파트 파일이 들어 있는 폴더의 절대 경로입니다.
* `hash_of_all_files` ([String](/ko/sql-reference/data-types/string)) — 압축된 파일의 sipHash128입니다.
* `hash_of_uncompressed_files` ([String](/ko/sql-reference/data-types/string)) — 비압축 파일의 sipHash128입니다(마크 파일, 인덱스 파일 등).
* `uncompressed_hash_of_compressed_files` ([String](/ko/sql-reference/data-types/string)) — 압축 파일을 비압축된 것처럼 간주했을 때 해당 파일 내 데이터의 sipHash128입니다.
* `delete_ttl_info_min` ([DateTime](/ko/sql-reference/data-types/datetime)) — TTL DELETE 규칙에 대한 날짜 및 시간 키의 최솟값입니다.
* `delete_ttl_info_max` ([DateTime](/ko/sql-reference/data-types/datetime)) — TTL DELETE 규칙에 대한 날짜 및 시간 키의 최댓값입니다.
* `move_ttl_info.expression` ([Array(String)](/ko/sql-reference/data-types/array)) — 표현식 배열입니다. 각 표현식은 TTL MOVE 규칙을 정의합니다.
* `move_ttl_info.min` ([Array(DateTime)](/ko/sql-reference/data-types/array)) — 날짜 및 시간 값 배열입니다. 각 요소는 TTL MOVE 규칙의 최소 키 값을 나타냅니다.
* `move_ttl_info.max` ([Array(DateTime)](/ko/sql-reference/data-types/array)) — 날짜 및 시간 값 배열입니다. 각 요소는 TTL MOVE 규칙의 최대 키 값을 나타냅니다.
* `default_compression_codec` ([String](/ko/sql-reference/data-types/string)) — 이 데이터 파트를 압축하는 데 사용되는 코덱 이름입니다(컬럼에 명시적인 코덱이 없는 경우).
* `recompression_ttl_info.expression` ([Array(String)](/ko/sql-reference/data-types/array)) — TTL 표현식입니다.
* `recompression_ttl_info.min` ([Array(DateTime)](/ko/sql-reference/data-types/array)) — 이 파트에서 계산된 TTL 표현식의 최솟값입니다. TTL이 만료된 행이 하나 이상 있는지 확인하는 데 사용됩니다.
* `recompression_ttl_info.max` ([Array(DateTime)](/ko/sql-reference/data-types/array)) — 이 파트에서 계산된 TTL 표현식의 최댓값입니다. TTL이 만료된 모든 행이 포함되어 있는지 확인하는 데 사용됩니다.
* `group_by_ttl_info.expression` ([Array(String)](/ko/sql-reference/data-types/array)) — TTL 표현식입니다.
* `group_by_ttl_info.min` ([Array(DateTime)](/ko/sql-reference/data-types/array)) — 이 파트에서 계산된 TTL 표현식의 최솟값입니다. TTL이 만료된 행이 하나 이상 포함되어 있는지 확인하는 데 사용됩니다.
* `group_by_ttl_info.max` ([Array(DateTime)](/ko/sql-reference/data-types/array)) — 이 파트에서 계산된 TTL 표현식의 최댓값입니다. TTL이 만료된 모든 행이 포함되어 있는지 확인하는 데 사용됩니다.
* `rows_where_ttl_info.expression` ([Array(String)](/ko/sql-reference/data-types/array)) — TTL 표현식입니다.
* `rows_where_ttl_info.min` ([Array(DateTime)](/ko/sql-reference/data-types/array)) — 이 파트에서 계산된 TTL 표현식의 최솟값입니다. TTL이 만료된 행이 하나 이상 포함되어 있는지 확인하는 데 사용됩니다.
* `rows_where_ttl_info.max` ([Array(DateTime)](/ko/sql-reference/data-types/array)) — 이 파트에서 계산된 TTL 표현식의 최댓값입니다. TTL이 만료된 모든 행이 포함되어 있는지 확인하는 데 사용됩니다.
* `is_broken` ([UInt8](/ko/sql-reference/data-types/int-uint)) — 프로젝션 파트가 손상되었는지 여부입니다
* `exception_code` ([Int32](/ko/sql-reference/data-types/int-uint)) — 프로젝션 파트의 손상 상태를 설명하는 Exception 코드입니다
* `exception` ([String](/ko/sql-reference/data-types/string)) — 프로젝션 파트의 손상 상태를 설명하는 Exception 메시지입니다

**별칭:**

* `bytes` — `bytes_on_disk`의 다른 이름입니다.
* `marks_size` — `marks_bytes`의 다른 이름입니다.
* `part_name` — `name`의 다른 이름입니다.

{/*AUTOGENERATED_END*/ }