---
description: '모든 테이블의 기존 프로젝션 정보를 담고 있는 시스템 테이블.'
keywords: ['시스템 테이블', '프로젝션']
slug: /operations/system-tables/projections
title: 'system.projections'
doc_type: '참고'
---

<div id="description">
  ## 설명
</div>

모든 테이블에 존재하는 프로젝션 정보를 포함합니다.

<div id="columns">
  ## 컬럼
</div>

{/*AUTOGENERATED_START*/ }

* `database` ([String](/ko/sql-reference/data-types/string)) — 데이터베이스 이름.
* `table` ([String](/ko/sql-reference/data-types/string)) — 테이블 이름.
* `name` ([String](/ko/sql-reference/data-types/string)) — 프로젝션 이름.
* `type` ([Enum8(&#39;Normal&#39; = 0, &#39;Aggregate&#39; = 1)](/ko/sql-reference/data-types/enum)) — 프로젝션 유형.
* `sorting_key` ([Array(String)](/ko/sql-reference/data-types/array)) — 프로젝션 정렬 키.
* `query` ([String](/ko/sql-reference/data-types/string)) — 프로젝션 쿼리.
* `settings` ([Map(String, String)](/ko/sql-reference/data-types/map)) — 프로젝션 설정.

{/*AUTOGENERATED_END*/ }

<div id="example">
  ## 예시
</div>

```sql
SELECT * FROM system.projections LIMIT 2 FORMAT Vertical;
```

```text
Row 1:
──────
database:    default
table:       landing
name:        improved_sorting_key
type:        Normal
sorting_key: ['user_id','date']
query:       SELECT * ORDER BY user_id, date
settings:     {}

Row 2:
──────
database:    default
table:       landing
name:        agg_no_key
type:        Aggregate
sorting_key: []
query:       SELECT count()
settings:     {}
```