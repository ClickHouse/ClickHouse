---
description: '큐에서 처리 대기 중인 비동기 삽입에 대한 정보를 포함하는 시스템 테이블.'
keywords: ['시스템 테이블', 'asynchronous_inserts']
slug: /operations/system-tables/asynchronous_inserts
title: 'system.asynchronous_inserts'
doc_type: '참고'
---

import SystemTableCloud from '@site/docs/_snippets/_system_table_cloud.md';

<SystemTableCloud />

<div id="description">
  ## 설명
</div>

큐에 대기 중인 비동기 삽입에 대한 정보를 포함합니다.

<div id="columns">
  ## 컬럼
</div>

{/*AUTOGENERATED_START*/ }

* `query` ([String](/ko/sql-reference/data-types/string)) — 쿼리 텍스트.
* `database` ([String](/ko/sql-reference/data-types/string)) — 데이터베이스 이름.
* `table` ([String](/ko/sql-reference/data-types/string)) — 테이블 이름.
* `format` ([String](/ko/sql-reference/data-types/string)) — 포맷 이름.
* `first_update` ([DateTime64(6)](/ko/sql-reference/data-types/datetime64)) — 마이크로초 해상도의 최초 삽입 시각.
* `total_bytes` ([UInt64](/ko/sql-reference/data-types/int-uint)) — 큐에서 대기 중인 총 바이트 수.
* `entries.query_id` ([Array(String)](/ko/sql-reference/data-types/array)) — 큐에서 대기 중인 삽입의 쿼리 ID 배열.
* `entries.bytes` ([Array(UInt64)](/ko/sql-reference/data-types/array)) — 큐에서 대기 중인 각 삽입 쿼리의 바이트 수 배열.

{/*AUTOGENERATED_END*/ }

<div id="example">
  ## 예시
</div>

```sql title="Query"
SELECT * FROM system.asynchronous_inserts LIMIT 1 \G;
```

```text title="Response"
Row 1:
──────
query:            INSERT INTO public.data_guess (user_id, datasource_id, timestamp, path, type, num, str) FORMAT CSV
database:         public
table:            data_guess
format:           CSV
first_update:     2023-06-08 10:08:54.199606
total_bytes:      133223
entries.query_id: ['b46cd4c4-0269-4d0b-99f5-d27668c6102e']
entries.bytes:    [133223]
```

<div id="see-also">
  ## 관련 항목
</div>

* [system.query&#95;log](/ko/operations/system-tables/query_log) — 쿼리 실행에 관한 일반적인 정보를 담고 있는 `query_log` 시스템 테이블의 설명입니다.
* [system.asynchronous&#95;insert&#95;log](/ko/operations/system-tables/asynchronous_insert_log) — 이 테이블에는 수행된 비동기 삽입에 대한 정보가 포함되어 있습니다.