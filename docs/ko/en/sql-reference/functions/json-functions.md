---
description: 'JSON 함수 문서'
sidebar_label: 'JSON'
slug: /sql-reference/functions/json-functions
title: 'JSON 함수'
doc_type: 'reference'
---

<div id="types-of-functions">
  ## JSON 함수의 종류
</div>

JSON을 파싱하는 함수는 두 가지로 나뉩니다:

* 매우 제한적인 JSON 부분 집합을 매우 빠르게 파싱하도록 설계된 [`simpleJSON*` (`visitParam*`)](#simplejson-visitparam-functions)
* 일반적인 JSON을 파싱하도록 설계된 [`JSONExtract*`](#jsonextract-functions)

<div id="simplejson-visitparam-functions">
  ### simpleJSON (visitParam) 함수
</div>

ClickHouse에는 단순화된 JSON을 처리하기 위한 특수 함수가 있습니다. 이러한 JSON 함수는 모두 JSON이 어떤 형태일 수 있는지에 대한 강한 가정을 바탕으로 합니다. 가능한 한 빠르게 작업을 수행할 수 있도록 필요한 최소한의 처리만 수행합니다.

다음과 같은 가정이 적용됩니다:

1. 필드 이름(함수 인수)은 상수여야 합니다.
2. 필드 이름은 JSON에서 어떤 방식으로든 정규 인코딩되어 있어야 합니다. 예를 들면 다음과 같습니다: `simpleJSONHas('{"abc":"def"}', 'abc') = 1`, 하지만 `simpleJSONHas('{"\\u0061\\u0062\\u0063":"def"}', 'abc') = 0`
3. 필드는 중첩 수준에 관계없이 모두 검색됩니다. 일치하는 필드가 여러 개 있으면 첫 번째로 나타나는 항목이 사용됩니다.
4. JSON에는 문자열 리터럴 외부에 공백 문자가 없어야 합니다.

<div id="jsonextract-functions">
  ### JSONExtract 함수
</div>

이 함수들은 [simdjson](https://github.com/lemire/simdjson)을 기반으로 하며, 더 복잡한 JSON 파싱 요구를 처리하도록 설계되었습니다.

<div id="case-insensitive-jsonextract-functions">
  ### 대소문자를 구분하지 않는 JSONExtract 함수
</div>

이 함수들은 JSON 객체에서 값을 추출할 때 ASCII 기준의 대소문자를 구분하지 않는 키 매칭을 수행합니다.
동작 방식은 대소문자를 구분하는 대응 함수와 동일하지만, 객체 키를 매칭할 때 대소문자를 구분하지 않는다는 점만 다릅니다.
대소문자만 다른 여러 키가 일치할 경우, 첫 번째 일치 항목이 반환됩니다.

:::note
이 함수들은 대소문자를 구분하는 대응 함수보다 성능이 떨어질 수 있으므로, 가능하면 일반 JSONExtract 함수를 사용하십시오.
:::

{/* 
  아래 태그의 내부 콘텐츠는 문서 프레임워크의 빌드 시점에
  system.functions에서 생성된 문서로 대체됩니다. 태그를 수정하거나 제거하지 마십시오.
  참고: https://github.com/ClickHouse/clickhouse-docs/blob/main/contribute/autogenerated-documentation-from-source.md
  */ }

{/*AUTOGENERATED_START*/ }

{/*AUTOGENERATED_END*/ }