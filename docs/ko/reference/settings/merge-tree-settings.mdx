---
description: '`system.merge_tree_settings`의 MergeTree 설정'
slug: /operations/settings/merge-tree-settings
title: 'MergeTree 테이블 설정'
doc_type: 'reference'
---

import MergeTreeSettingsExplorer from "/snippets/ko/components/MergeTreeSettingsExplorer/MergeTreeSettingsExplorer.jsx";

시스템 테이블 `system.merge_tree_settings`는 전역으로 설정된 MergeTree 설정을 표시합니다.

MergeTree 설정은 서버 구성 파일의 `merge_tree` 섹션에서 설정하거나, 각 `MergeTree` 테이블별로 `CREATE TABLE` 문의 `SETTINGS` 절에서 개별적으로 지정할 수 있습니다.

설정 `max_suspicious_broken_parts`를 사용자 지정하는 예시:

서버 구성 파일에서 모든 `MergeTree` 테이블의 기본값을 설정합니다:

```text
<merge_tree>
    <max_suspicious_broken_parts>5</max_suspicious_broken_parts>
</merge_tree>
```

특정 테이블에 대한 설정:

```sql
CREATE TABLE tab
(
    `A` Int64
)
ENGINE = MergeTree
ORDER BY tuple()
SETTINGS max_suspicious_broken_parts = 500;
```

특정 테이블의 설정을 `ALTER TABLE ... MODIFY SETTING`을 사용하여 변경합니다:

```sql
ALTER TABLE tab MODIFY SETTING max_suspicious_broken_parts = 100;

-- reset to global default (value from system.merge_tree_settings)
ALTER TABLE tab RESET SETTING max_suspicious_broken_parts;
```

<div id="mergetree-settings">
  ## MergeTree 설정
</div>

{/* 아래 설정은 다음 스크립트에서 자동 생성됩니다. 
  https://github.com/ClickHouse/clickhouse-docs/blob/main/scripts/settings/autogenerate-settings.sh
  */}

{/*AUTOGENERATED_START*/}

{/*AUTOGENERATED_END*/}

<div id="browse-mergetree-table-settings">
  ## MergeTree 테이블 설정 살펴보기
</div>

<MergeTreeSettingsExplorer />