---
title: 'merges_mutations_* 서버 설정'
sidebarTitle: 'merges_mutations_*'
toc_max_heading_level: 2
description: '자동 생성된 merges_mutations_* 그룹의 ClickHouse 서버 설정입니다.'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/ko/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

다음 설정은 ClickHouse 서버를 구성하며 ClickHouse 소스에서 자동으로 생성됩니다.

<div id="merges_mutations_memory_usage_soft_limit">
  ## merges_mutations_memory_usage_soft_limit
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />

머지 및 뮤테이션 작업 수행에 사용할 수 있는 RAM 한도를 설정합니다.
ClickHouse가 설정된 한도에 도달하면 새로운 백그라운드 머지 또는 뮤테이션 작업은 더 이상 예약하지 않지만, 이미 예약된 작업은 계속 실행합니다.

<Note>
  `0` 값은 무제한을 의미합니다.
</Note>

**예시**

```xml
<merges_mutations_memory_usage_soft_limit>0</merges_mutations_memory_usage_soft_limit>
```

<div id="merges_mutations_memory_usage_to_ram_ratio">
  ## merges_mutations_memory_usage_to_ram_ratio
</div>

<SettingsInfoBlock type="Double" default_value="0.5" changeable_without_restart="No" />

기본 `merges_mutations_memory_usage_soft_limit` 값은 `memory_amount * merges_mutations_memory_usage_to_ram_ratio`로 계산됩니다.

**관련 항목:**

* [max&#95;memory&#95;usage](/ko/reference/settings/session-settings#max_memory_usage)
* [merges&#95;mutations&#95;memory&#95;usage&#95;soft&#95;limit](/ko/reference/settings/server-settings/settings/merges-mutations#merges_mutations_memory_usage_soft_limit)

{/*AUTOGENERATED_END*/}