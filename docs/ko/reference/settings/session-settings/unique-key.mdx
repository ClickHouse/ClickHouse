---
title: 'unique_key_* 세션 설정'
sidebarTitle: 'unique_key_*'
toc_max_heading_level: 2
description: 'unique_key_* 생성 그룹에 속하는 ClickHouse 세션 설정입니다.'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/ko/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import { VersionHistory } from "/snippets/ko/components/VersionHistory/VersionHistory.jsx";
import { ExperimentalBadge } from "/snippets/ko/components/ExperimentalBadge/ExperimentalBadge.jsx";

이러한 설정은 [system.settings](/ko/reference/system-tables/settings)에서 확인할 수 있으며, [소스](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp)에서 자동 생성됩니다.

<div id="unique_key_max_encoded_size">
  ## unique_key_max_encoded_size
</div>

<ExperimentalBadge />

<SettingsInfoBlock type="UInt64" default_value="256" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.6"},{"label": "256"},{"label": "새로운 설정: 단일 UNIQUE KEY 행의 순서 보존 이진 인코딩 최대 크기(바이트)"}]}]} />

단일 `UNIQUE KEY` 행의 순서 보존 이진 인코딩 최대 크기(바이트)입니다.

<div id="unique_key_probe_implementation">
  ## unique_key_probe_implementation
</div>

<ExperimentalBadge />

<SettingsInfoBlock type="UniqueKeyProbeImplementation" default_value="auto" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.8"},{"label": "auto"},{"label": "새로운 설정: UNIQUE KEY 프로브 구현을 선택합니다(현재는 단순 기준 구현만 존재함)"}]}]} />

UNIQUE KEY 쓰기 경로에 사용할 프로브 구현을 선택합니다. 현재는 단일 스레드
기준 구현만 존재하므로 `auto`와 `simple`은 모두 이를 선택합니다.

예약됨: 이 설정은 아직 적용되지 않습니다. 프로브를 구성하는 UNIQUE KEY INSERT
쓰기 경로가 연결되기 전까지는 값이 읽히지 않습니다. 구현과 함께 이 옵션을 제공하기 위해 미리 선언합니다.

{/*AUTOGENERATED_END*/}