---
title: 'min_external_* 세션 설정'
sidebarTitle: 'min_external_*'
toc_max_heading_level: 2
description: '자동 생성된 min_external_* 그룹에 속한 ClickHouse 세션 설정입니다.'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/ko/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import VersionHistory from "/snippets/ko/components/VersionHistory/VersionHistory.jsx";

이러한 설정은 [system.settings](/ko/reference/system-tables/settings)에서 확인할 수 있으며 [source](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp)에서 자동으로 생성됩니다.

<div id="min_external_table_block_size_bytes">
  ## min_external_table_block_size_bytes
</div>

<SettingsInfoBlock type="UInt64" default_value="268402944" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "24.2"},{"label": "268402944"},{"label": "블록이 충분히 크지 않으면 외부 테이블(외부 테이블)로 전달되는 블록을 지정된 바이트 크기로 합칩니다."}]}]} />

블록이 충분히 크지 않으면 외부 테이블(외부 테이블)로 전달되는 블록을 지정된 바이트 크기로 합칩니다.

<div id="min_external_table_block_size_rows">
  ## min_external_table_block_size_rows
</div>

<SettingsInfoBlock type="UInt64" default_value="1048449" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "24.2"},{"label": "1048449"},{"label": "블록이 충분히 크지 않으면 외부 테이블에 전달되는 블록을 지정된 행 수 크기로 합칩니다"}]}]} />

블록이 충분히 크지 않으면 외부 테이블에 전달되는 블록을 지정된 행 수 크기로 합칩니다.

{/*AUTOGENERATED_END*/}