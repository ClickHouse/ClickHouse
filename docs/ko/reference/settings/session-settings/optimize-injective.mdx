---
title: 'optimize_injective_* 세션 설정'
sidebarTitle: 'optimize_injective_*'
toc_max_heading_level: 2
description: '자동 생성된 optimize_injective_* 그룹의 ClickHouse 세션 설정입니다.'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/ko/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import VersionHistory from "/snippets/ko/components/VersionHistory/VersionHistory.jsx";

이 설정은 [system.settings](/ko/reference/system-tables/settings)에서 확인할 수 있으며 [소스 코드](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp)를 기반으로 자동 생성됩니다.

<div id="optimize_injective_functions_in_group_by">
  ## optimize_injective_functions_in_group_by
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "24.1"},{"label": "1"},{"label": "분석기에서 GROUP BY 절의 단사 함수를 인수로 대체"}]}]} />

GROUP BY 절의 단사 함수를 인수로 대체합니다

<div id="optimize_injective_functions_in_limit_by">
  ## optimize_injective_functions_in_limit_by
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.6"},{"label": "1"},{"label": "LIMIT BY 키에서 단사 함수를 인수로 대체하는 새로운 설정입니다."}]}]} />

LIMIT BY 절에서 단사 함수를 인수로 대체합니다.

예시: `LIMIT 5 BY toString(x)`는 `LIMIT 5 BY x`가 됩니다.

<div id="optimize_injective_functions_inside_uniq">
  ## optimize_injective_functions_inside_uniq
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

uniq*() 함수 내부의 단일 인수 단사 함수를 제거합니다.

{/*AUTOGENERATED_END*/}