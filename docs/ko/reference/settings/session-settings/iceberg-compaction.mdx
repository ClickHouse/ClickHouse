---
title: 'iceberg_compaction_* 세션 설정'
sidebarTitle: 'iceberg_compaction_*'
toc_max_heading_level: 2
description: 'iceberg_compaction_* 자동 생성 그룹에 속한 ClickHouse 세션 설정입니다.'
doc_type: '참고'
---

{/*자동 생성 시작*/}

import SettingsInfoBlock from "/snippets/ko/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import { VersionHistory } from "/snippets/ko/components/VersionHistory/VersionHistory.jsx";

이 설정은 [system.settings](/ko/reference/system-tables/settings)에서 확인할 수 있으며 [소스 코드](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp)로부터 자동 생성됩니다.

<div id="iceberg_compaction_commit_batch_size">
  ## iceberg_compaction_commit_batch_size
</div>

<SettingsInfoBlock type="UInt64" default_value="100" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.9"},{"label": "100"},{"label": "새로운 설정"}]}, {"id": "row-2","items": [{"label": "26.8"},{"label": "100"},{"label": "새로운 설정"}]}]} />

백그라운드 Iceberg compaction이 새 스냅샷에 게시하기 전에 누적하는 머지된 데이터 파일 수입니다.

더 이상 compaction할 후보가 없으면 compaction 결과는 항상 게시됩니다. 따라서 이 설정은 compaction이 계속 새 작업을 찾는 동안 이미 머지된 파일이 게시되지 않은 채 유지되는 기간만 제한합니다. `0`은 compaction할 후보가 모두 소진될 때만 게시하던 이전 동작을 복원합니다. 테이블에 새 데이터 파일이 계속 수신되는 동안에는 이 상태에 도달하지 않으므로, 모든 머지 결과는 객체 스토리지에 기록되지만 스냅샷에서 참조되지 않습니다.

<div id="iceberg_compaction_data_cleanup">
  ## iceberg_compaction_data_cleanup
</div>

<SettingsInfoBlock type="Seconds" default_value="10800" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.5"},{"label": "10800"},{"label": "새로운 설정"}]}]} />

데이터가 삭제되기까지의 시간입니다.

<div id="iceberg_compaction_delay_bias">
  ## iceberg_compaction_delay_bias
</div>

<SettingsInfoBlock type="Seconds" default_value="10800" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.5"},{"label": "10800"},{"label": "새로운 설정"}]}]} />

2회의 백그라운드 compaction 작업 사이에 필요한 최소 지연 시간입니다.

<div id="iceberg_compaction_max_bytes_in_data_file">
  ## iceberg_compaction_max_bytes_in_data_file
</div>

<SettingsInfoBlock type="UInt64" default_value="18446744073709551615" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.9"},{"label": "18446744073709551615"},{"label": "삽입 시 제한과 별도로, compaction으로 생성되는 Iceberg 데이터 파일의 최대 바이트 수를 지정하는 새로운 설정입니다."}]}, {"id": "row-2","items": [{"label": "26.7"},{"label": "18446744073709551615"},{"label": "삽입 시 제한과 별도로, compaction으로 생성되는 Iceberg 데이터 파일의 최대 바이트 수를 지정하는 새로운 설정입니다."}]}]} />

compaction으로 생성되는 Iceberg Parquet 데이터 파일의 최대 바이트 수입니다. 기본값은 최댓값이며, compaction은 적격 파일을 가능한 한 적은 수의 출력 파일로 머지합니다.

<div id="iceberg_compaction_max_rows_in_data_file">
  ## iceberg_compaction_max_rows_in_data_file
</div>

<SettingsInfoBlock type="UInt64" default_value="18446744073709551615" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.9"},{"label": "18446744073709551615"},{"label": "삽입 시 제한과 별도로, compaction으로 생성되는 Iceberg 데이터 파일의 최대 행 수를 지정하는 새로운 설정입니다."}]}, {"id": "row-2","items": [{"label": "26.7"},{"label": "18446744073709551615"},{"label": "삽입 시 제한과 별도로, compaction으로 생성되는 Iceberg 데이터 파일의 최대 행 수를 지정하는 새로운 설정입니다."}]}]} />

compaction으로 생성되는 Iceberg Parquet 데이터 파일의 최대 행 수입니다. 기본값은 최댓값으로, compaction이 대상 파일을 가능한 한 적은 수의 출력 파일로 머지하도록 합니다. 이 값을 compaction에서 실제로 생성할 수 있는 크기보다 크게 유지하면 모든 출력 파일이 `iceberg_data_file_size_lower_threshold_compaction` 미만으로 유지되어 영구적으로 다시 선택됩니다.

{/*AUTOGENERATED_END*/}