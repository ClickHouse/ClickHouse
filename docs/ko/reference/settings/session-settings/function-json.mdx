---
title: 'function_json_* 세션 설정'
sidebarTitle: 'function_json_*'
toc_max_heading_level: 2
description: 'function_json_* 자동 생성 그룹의 ClickHouse 세션 설정입니다.'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/ko/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

이러한 설정은 [system.settings](/ko/reference/system-tables/settings)에서 확인할 수 있으며, [소스 코드](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp)에서 자동 생성됩니다.

<div id="function_json_value_return_type_allow_complex">
  ## function_json_value_return_type_allow_complex
</div>

<SettingsInfoBlock type="Bool" default_value="0" />

json&#95;value 함수에서 구조체, 배열, 맵과 같은 복합 타입을 반환하도록 허용할지 여부를 제어합니다.

```sql
SELECT JSON_VALUE('{"hello":{"world":"!"}}', '$.hello') settings function_json_value_return_type_allow_complex=true

┌─JSON_VALUE('{"hello":{"world":"!"}}', '$.hello')─┐
│ {"world":"!"}                                    │
└──────────────────────────────────────────────────┘

1 row in set. Elapsed: 0.001 sec.
```

가능한 값:

* true — 허용
* false — 허용 안 함

<div id="function_json_value_return_type_allow_nullable">
  ## function_json_value_return_type_allow_nullable
</div>

<SettingsInfoBlock type="Bool" default_value="0" />

JSON&#95;VALUE 함수에서 값이 없을 때 `NULL` 반환을 허용할지 제어합니다.

```sql
SELECT JSON_VALUE('{"hello":"world"}', '$.b') settings function_json_value_return_type_allow_nullable=true;

┌─JSON_VALUE('{"hello":"world"}', '$.b')─┐
│ ᴺᵁᴸᴸ                                   │
└────────────────────────────────────────┘

1 row in set. Elapsed: 0.001 sec.
```

가능한 값:

* true — 허용합니다.
* false — 허용하지 않습니다.

{/*AUTOGENERATED_END*/}