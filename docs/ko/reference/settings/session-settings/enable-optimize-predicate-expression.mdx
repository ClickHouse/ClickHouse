---
title: 'enable_optimize_predicate_expression_* 세션 설정'
sidebarTitle: 'enable_optimize_predicate_expression_*'
toc_max_heading_level: 2
description: 'enable_optimize_predicate_expression_*로 생성된 그룹에 속한 ClickHouse 세션 설정입니다.'
doc_type: '참고'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/ko/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import VersionHistory from "/snippets/ko/components/VersionHistory/VersionHistory.jsx";

이러한 설정은 [system.settings](/ko/reference/system-tables/settings)에서 확인할 수 있으며 [소스 코드](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp)에서 자동 생성됩니다.

<div id="enable_optimize_predicate_expression">
  ## enable_optimize_predicate_expression
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "18.12.17"},{"label": "1"},{"label": "기본적으로 서브쿼리에 대한 프레디케이트 최적화 사용"}]}]} />

`SELECT` 쿼리에서 프레디케이트 푸시다운을 활성화합니다.

프레디케이트 푸시다운은 분산 쿼리의 네트워크 트래픽을 크게 줄일 수 있습니다.

가능한 값:

* 0 — 비활성화됨.
* 1 — 활성화됨.

사용법

다음 쿼리를 살펴보십시오:

1. `SELECT count() FROM test_table WHERE date = '2018-10-10'`
2. `SELECT count() FROM (SELECT * FROM test_table) WHERE date = '2018-10-10'`

`enable_optimize_predicate_expression = 1`이면 ClickHouse가 처리 중에 `WHERE`를 서브쿼리에 적용하므로 두 쿼리의 실행 시간은 동일합니다.

`enable_optimize_predicate_expression = 0`이면 `WHERE` 절이 서브쿼리 처리가 끝난 뒤 모든 데이터에 적용되므로 두 번째 쿼리의 실행 시간이 훨씬 더 길어집니다.

<div id="enable_optimize_predicate_expression_to_final_subquery">
  ## enable_optimize_predicate_expression_to_final_subquery
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

프레디케이트를 FINAL 서브쿼리로 푸시할 수 있도록 허용합니다.

{/*AUTOGENERATED_END*/}