---
title: 'dynamic_disk_* 세션 설정'
sidebarTitle: 'dynamic_disk_*'
toc_max_heading_level: 2
description: 'dynamic_disk_* 생성된 그룹에 속한 ClickHouse 세션 설정입니다.'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/ko/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import VersionHistory from "/snippets/ko/components/VersionHistory/VersionHistory.jsx";

이러한 설정은 [system.settings](/ko/reference/system-tables/settings)에서 확인할 수 있으며, [source](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp)로부터 자동 생성됩니다.

<div id="dynamic_disk_allow_from_env">
  ## dynamic_disk_allow_from_env
</div>

<SettingsInfoBlock type="Bool" default_value="0" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.5"},{"label": "0"},{"label": "동적 디스크 구성(dynamic disk configuration)에서 `from_env` 치환(즉, `disk()` 함수 인수에서)을 허용하는 새로운 설정입니다. 보안상 기본적으로 비활성화되어 있습니다."}]}]} />

동적 디스크 구성(즉, `disk()` 함수 인수에서)에서 `from_env` 치환을 사용할 수 있도록 합니다.
테이블 스토리지를 정의할 때 임의의 환경 변수를 읽지 못하도록 기본적으로 비활성화되어 있습니다.

<div id="dynamic_disk_allow_from_zk">
  ## dynamic_disk_allow_from_zk
</div>

<SettingsInfoBlock type="Bool" default_value="0" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.5"},{"label": "0"},{"label": "동적 디스크 구성(dynamic disk configuration)에서 `from_zk` 치환(즉, `disk()` 함수의 인수)을 허용하는 새로운 설정입니다. 기본적으로 비활성화되어 있습니다."}]}]} />

동적 디스크 구성(dynamic disk configuration)에서 `from_zk` 치환(즉, `disk()` 함수의 인수)을 사용할 수 있도록 허용합니다.
기본적으로 비활성화되어 있습니다.

<div id="dynamic_disk_allow_include">
  ## dynamic_disk_allow_include
</div>

<SettingsInfoBlock type="Bool" default_value="0" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.5"},{"label": "0"},{"label": "동적 디스크 구성(즉, `disk()` 함수)에서 `include`를 허용하는 새로운 설정입니다. 기본적으로 비활성화되어 있습니다."}]}]} />

동적 디스크 구성(즉, `disk()` 함수 인수)에서 `include` 사용을 허용합니다.
기본적으로 비활성화되어 있습니다.

{/*AUTOGENERATED_END*/}