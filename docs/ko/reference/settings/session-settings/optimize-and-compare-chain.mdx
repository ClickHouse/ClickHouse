---
title: 'optimize_and_compare_chain_* 세션 설정'
sidebarTitle: 'optimize_and_compare_chain_*'
toc_max_heading_level: 2
description: 'optimize_and_compare_chain_* 생성 그룹에 속한 ClickHouse 세션 설정입니다.'
doc_type: '참고'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/ko/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import VersionHistory from "/snippets/ko/components/VersionHistory/VersionHistory.jsx";

이 설정은 [system.settings](/ko/reference/system-tables/settings)에서 확인할 수 있으며, [소스 코드](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp)에서 자동 생성됩니다.

<div id="optimize_and_compare_chain">
  ## optimize_and_compare_chain
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "25.2"},{"label": "1"},{"label": "새로운 설정"}]}]} />

필터링 기능을 향상하기 위해 AND 체인에서 상수 비교를 전파합니다. `<`, `<=`, `>`, `>=`, `=` 연산자와 이들의 혼합을 지원합니다. 예를 들어 `(a < b) AND (b < c) AND (c < 5)`는 `(a < b) AND (b < c) AND (c < 5) AND (b < 5) AND (a < 5)`로 확장됩니다.

<div id="optimize_and_compare_chain_max_hash_work">
  ## optimize_and_compare_chain_max_hash_work
</div>

<SettingsInfoBlock type="UInt64" default_value="5000000" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.7"},{"label": "5000000"},{"label": "`optimize_and_compare_chain` 최적화의 작업량(해시된 쿼리 트리 노드 수 기준)에 상한을 두는 새로운 설정입니다. 비교 `AND`-chain이 매우 많거나 매우 큰 쿼리에서 이 최적화가 쿼리 분석 시간의 대부분을 차지하지 않도록 합니다. 이전 값인 `0`(무제한)은 이 최적화에 상한이 없던 26.7 이전 동작을 재현하므로, `compatibility`를 더 이전 버전으로 설정하면 예산 제한 없이 전이적 프레디케이트를 계속 도출합니다. 예산 제한을 비활성화하려면 `0`으로 설정하십시오."}]}]} />

쿼리 분석 중 `optimize_and_compare_chain` 최적화에 사용할 작업 예산으로, `getTreeHash`가 해시하는 쿼리 트리 노드 수(이 최적화의 주된 비용)로 측정합니다. 최적화 적용 중 해시한 노드 수가 이 값을 초과하면, 해당 쿼리의 나머지 부분에는 이 최적화를 더 이상 적용하지 않습니다. 이를 통해 비교 `AND`-chain이 매우 많거나 매우 큰 쿼리에서, 아무런 folding 없이 이 최적화가 분석 시간의 대부분을 차지할 수 있는 경우에도 분석 시간을 제한할 수 있습니다. 조기에 중단해도 항상 안전합니다. 단지 최적화 적용을 포기할 뿐이며, 결과는 절대 바뀌지 않습니다. 예산 제한을 비활성화하려면 `0`으로 설정하십시오(무제한).

{/*AUTOGENERATED_END*/}