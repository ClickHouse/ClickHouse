---
title: 'network_* 세션 설정'
sidebarTitle: 'network_*'
toc_max_heading_level: 2
description: '자동 생성된 network_* 그룹의 ClickHouse 세션 설정입니다.'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/ko/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import { VersionHistory } from "/snippets/ko/components/VersionHistory/VersionHistory.jsx";

이 설정은 [system.settings](/ko/reference/system-tables/settings)에서 확인할 수 있으며 [source](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp)에서 자동 생성됩니다.

## network_compression_method {#network_compression_method}

<SettingsInfoBlock type="String" default_value="ZSTD" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.9"},{"label": "ZSTD"},{"label": "네트워크 트래픽을 줄이기 위해 클라이언트\/서버 및 서버\/서버 간 통신의 기본 압축 방식을 `LZ4`에서 `ZSTD`로 변경했습니다."}]}]} />

native protocol을 통한 클라이언트/서버 및 서버/서버 간 통신을 압축할 때 사용하는 코덱입니다.

이 설정은 분산 쿼리의 스트리밍 exchange 채널에는 적용되지 않으며, 해당 채널은 항상 서버 기본 코덱을 사용합니다. 압축된 각 frame은 self-describing 형식이므로 수신기가 코덱을 자동으로 감지합니다.

사용 가능한 값:

* `NONE` — 압축을 사용하지 않습니다.
* `LZ4` — LZ4 코덱을 사용합니다.
* `LZ4HC` — LZ4HC 코덱을 사용합니다.
* `ZSTD` — ZSTD 코덱을 사용합니다.

**관련 항목**

* [network&#95;zstd&#95;compression&#95;level](/ko/reference/settings/session-settings/network#network_zstd_compression_level)

## network_zstd_compression_level {#network_zstd_compression_level}

<SettingsInfoBlock type="Int64" default_value="3" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.9"},{"label": "3"},{"label": "기본 네트워크 `ZSTD` 압축 수준을 새로운 기본 온디스크 `ZSTD(3)` 압축에 맞췄습니다."}]}]} />

ZSTD 압축 수준을 조정합니다. [network&#95;compression&#95;method](/ko/reference/settings/session-settings/network#network_compression_method)가 `ZSTD`로 설정된 경우에만 사용됩니다.

가능한 값:

* 1~15 사이의 양의 정수.

{/*AUTOGENERATED_END*/}