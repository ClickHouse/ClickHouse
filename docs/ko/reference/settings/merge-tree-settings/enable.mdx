---
title: 'enable_* MergeTree 테이블 설정'
sidebarTitle: 'enable_*'
toc_max_heading_level: 2
description: 'enable_*로 생성된 그룹의 ClickHouse MergeTree 테이블 설정입니다.'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import ExperimentalBadge from "/snippets/ko/components/ExperimentalBadge/ExperimentalBadge.jsx";
import SettingsInfoBlock from "/snippets/ko/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import VersionHistory from "/snippets/ko/components/VersionHistory/VersionHistory.jsx";

이 설정은 [system.merge&#95;tree&#95;settings](/ko/reference/system-tables/merge_tree_settings)에서 확인할 수 있으며, ClickHouse 소스에서 자동 생성됩니다.

<div id="enable_index_granularity_compression">
  ## enable_index_granularity_compression
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

가능한 경우 인덱스 세분화 수준 값을 메모리에서 압축합니다

<div id="enable_max_bytes_limit_for_min_age_to_force_merge">
  ## enable_max_bytes_limit_for_min_age_to_force_merge
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.2"},{"label": "1"},{"label": "기본적으로 `min_age_to_force_merge_seconds`가 설정되어 있어도 파트 크기를 제한합니다"}]}, {"id": "row-2","items": [{"label": "25.1"},{"label": "0"},{"label": "`min_age_to_force_merge`의 최대 바이트를 제한하는 새로운 설정이 추가되었습니다."}]}, {"id": "row-3","items": [{"label": "25.1"},{"label": "0"},{"label": "새로운 설정"}]}]} />

설정 `min_age_to_force_merge_seconds` 및
`min_age_to_force_merge_on_partition_only`에 설정
`max_bytes_to_merge_at_max_space_in_pool`를 적용할지 여부입니다.

가능한 값:

* `true`
* `false`

<div id="enable_mixed_granularity_parts">
  ## enable_mixed_granularity_parts
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

`index_granularity_bytes` 설정을 사용해 granule 크기를 제어하는 방식으로 전환할지 여부를 설정합니다. 19.11 버전 이전에는 granule 크기를 제한하는 설정으로 `index_granularity`만 있었습니다. `index_granularity_bytes` 설정은 큰 행(수십~수백 메가바이트)을 포함한 테이블에서 데이터를 조회할 때 ClickHouse 성능을 향상시킵니다.
큰 행이 있는 테이블이 있다면 `SELECT` 쿼리의 효율을 높이기 위해 해당 테이블에서 이 설정을 활성화할 수 있습니다.

<div id="enable_replacing_merge_with_cleanup_for_min_age_to_force_merge">
  ## enable_replacing_merge_with_cleanup_for_min_age_to_force_merge
</div>

<ExperimentalBadge />

<SettingsInfoBlock type="Bool" default_value="0" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "25.3"},{"label": "0"},{"label": "ReplacingMergeTree에 대한 자동 정리 머지를 허용하는 새로운 설정"}]}]} />

파티션이 단일 파트로 머지될 때 ReplacingMergeTree에 CLEANUP 머지를 사용할지 여부를 지정합니다.
이 기능을 사용하려면 `allow_experimental_replacing_merge_with_cleanup`,
`min_age_to_force_merge_seconds`, `min_age_to_force_merge_on_partition_only`
설정이 활성화되어 있어야 합니다.

가능한 값:

* `true`
* `false`

<div id="enable_the_endpoint_id_with_zookeeper_name_prefix">
  ## enable_the_endpoint_id_with_zookeeper_name_prefix
</div>

<SettingsInfoBlock type="Bool" default_value="0" />

복제된 MergeTree 테이블에서 ZooKeeper 이름 접두사와 함께 Endpoint ID를 활성화합니다.

<div id="enable_vertical_merge_algorithm">
  ## enable_vertical_merge_algorithm
</div>

<SettingsInfoBlock type="UInt64" default_value="1" />

수직 병합 알고리즘 사용을 활성화합니다.

{/*AUTOGENERATED_END*/}