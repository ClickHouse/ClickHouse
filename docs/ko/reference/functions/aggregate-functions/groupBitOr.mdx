---
description: '숫자들의 집합에 비트 단위 `OR`를 적용합니다.'
slug: /sql-reference/aggregate-functions/reference/groupbitor
title: 'groupBitOr'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="groupBitOr">
  ## groupBitOr
</div>

도입 버전: v1.1.0

일련의 숫자에 비트 단위 OR 연산을 적용합니다.

**구문**

```sql
groupBitOr(expr)
```

**별칭**: `BIT_OR`

**인수**

* `expr` — `(U)Int*` 타입의 표현식. [`(U)Int*`](/ko/reference/data-types/int-uint)

**반환 값**

`(U)Int*` 타입의 값을 반환합니다. [`(U)Int*`](/ko/reference/data-types/int-uint)

**예시**

**비트 단위 OR 예시**

```sql title=Query
CREATE TABLE t (num UInt32) ENGINE = Memory;
INSERT INTO t VALUES (44), (28), (13), (85);

-- 테스트 데이터:
-- 이진수     십진수
-- 00101100 = 44
-- 00011100 = 28
-- 00001101 = 13
-- 01010101 = 85

SELECT groupBitOr(num) FROM t;
```

```response title=Response
-- 결과:
-- 이진수     십진수
-- 01111101 = 125

┌─groupBitOr(num)─┐
│             125 │
└─────────────────┘
```

{/*AUTOGENERATED_END*/}