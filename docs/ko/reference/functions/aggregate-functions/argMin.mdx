---
description: '`val`의 최솟값에 대응하는 `arg` 값을 계산합니다. 최솟값인 동일한 `val`을 가진
  행이 여러 개인 경우, 해당 `arg` 중 어떤 값이 반환될지는 결정적이지 않습니다.'
slug: /sql-reference/aggregate-functions/reference/argmin
title: 'argMin'
doc_type: 'reference'
---

별칭: `min_by`

{/*AUTOGENERATED_START*/}

<div id="argMin">
  ## argMin
</div>

도입 버전: v1.1.0

최소 `val` 값에 대한 `arg` 값을 계산합니다. 동일한 `val`이 최소값인 행이 여러 개 있으면, 연결된 `arg` 중 어떤 값이 반환될지는 결정적이지 않습니다.
`arg`와 `min`은 모두 [집계 함수](/ko/reference/functions/aggregate-functions/index)처럼 동작합니다. 처리 중에는 둘 다 [`Null`을 건너뛰며](/ko/reference/functions/aggregate-functions/index#null-processing), `Null`이 아닌 값을 사용할 수 있으면 `Null`이 아닌 값을 반환합니다.

**관련 항목**

* [Tuple](/ko/reference/data-types/tuple)

**구문**

```sql
argMin(arg, val)
```

**별칭**: `min_by`

**인수**

* `arg` — 최댓값을 찾는 데 사용할 인수입니다. [`const String`](/ko/reference/data-types/string)
* `val` — 최솟값입니다. [`(U)Int8/16/32/64`](/ko/reference/data-types/int-uint) 또는 [`Float*`](/ko/reference/data-types/float) 또는 [`Date`](/ko/reference/data-types/date) 또는 [`DateTime`](/ko/reference/data-types/datetime) 또는 [`Tuple`](/ko/reference/data-types/tuple)

**반환 값**

최소 `val` 값에 해당하는 `arg` 값을 반환합니다. 반환 타입은 `arg` 타입과 일치합니다.

**예시**

**기본 사용법**

```sql title=Query
SELECT argMin(user, salary) FROM salary;
```

```response title=Response
┌─argMin(user, salary)─┐
│ worker               │
└──────────────────────┘
```

**NULL 처리를 포함한 확장된 예시**

```sql title=Query
CREATE TABLE test
(
    a Nullable(String),
    b Nullable(Int64)
)
ENGINE = Memory AS
SELECT *
FROM VALUES((NULL, 0), ('a', 1), ('b', 2), ('c', 2), (NULL, NULL), ('d', NULL));

SELECT argMin(a, b), min(b) FROM test;
```

```response title=Response
┌─argMin(a, b)─┬─min(b)─┐
│ a            │      0 │
└──────────────┴────────┘
```

**인수에 Tuple 사용하기**

```sql title=Query
SELECT argMin(a, (b, a)), min(tuple(b, a)) FROM test;
```

```response title=Response
┌─argMin(a, tuple(b, a))─┬─min(tuple(b, a))─┐
│ d                      │ (NULL,NULL)      │
└────────────────────────┴──────────────────┘
```

{/*AUTOGENERATED_END*/}