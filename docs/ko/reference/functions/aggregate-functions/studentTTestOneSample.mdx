---
description: '표본과 알려진 모집단 평균에 단일 표본 Student t-test를 적용합니다.'
sidebarTitle: 'studentTTestOneSample'
slug: /sql-reference/aggregate-functions/reference/studentttestonesample
title: 'studentTTestOneSample'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="studentTTestOneSample">
  ## studentTTestOneSample
</div>

도입 버전: v25.10.0

표본 평균이 알려진 모집단 평균과 다른지 확인하기 위해 단일 표본 Student t-test를 적용합니다.

정규성을 가정합니다. 귀무가설은 표본 평균이 모집단 평균과 같다는 것입니다.

선택적 `confidence_level`을 사용하면 신뢰구간을 계산할 수 있습니다.

**참고:**

* 최소 2개의 관측치가 필요합니다. 그렇지 않으면 결과는 `(nan, nan)`이 되며(요청한 경우 인터벌도 `nan`이 됩니다).
* 입력값이 상수이거나 거의 상수인 경우에도 표준 오차가 0(또는 사실상 0)이므로 `nan`을 반환합니다.

**관련 항목**

* [Student&#39;s t-test](https://en.wikipedia.org/wiki/Student%27s_t-test)
* [studentTTest 함수](/ko/reference/functions/aggregate-functions/studentTTest)

**구문**

```sql
studentTTestOneSample([confidence_level])(sample_data, population_mean)
```

**매개변수**

* `confidence_level` — 선택 사항입니다. 신뢰구간의 신뢰 수준입니다. (0, 1) 범위의 부동소수점 값입니다. [`Float*`](/ko/reference/data-types/float)

**인수**

* `sample_data` — 표본 데이터입니다. [`Integer`](/ko/reference/data-types/int-uint) 또는 [`Float`](/ko/reference/data-types/float) 또는 [`Decimal`](/ko/reference/data-types/decimal)
* `population_mean` — 검정 기준으로 사용하는 알려진 모집단 평균입니다(보통 상수). [`(U)Int*`](/ko/reference/data-types/int-uint) 또는 [`Float*`](/ko/reference/data-types/float) 또는 [`Decimal`](/ko/reference/data-types/decimal)

**반환 값**

2개 또는 4개의 요소로 이루어진 튜플을 반환합니다(`confidence_level`이 지정된 경우). 반환 요소는 계산된 t-통계량, 계산된 p-값(양측), [계산된 신뢰구간 하한], [계산된 신뢰구간 상한]입니다. 신뢰구간은 지정된 신뢰 수준에서 표본 평균에 대해 계산됩니다. [`Tuple(Float64, Float64)`](/ko/reference/data-types/tuple) 또는 [`Tuple(Float64, Float64, Float64, Float64)`](/ko/reference/data-types/tuple)

**예시**

**신뢰구간 없이**

```sql title=Query
SELECT studentTTestOneSample()(value, 20.0) FROM t;
```

```response title=Response
```

**95% 신뢰구간 포함**

```sql title=Query
SELECT studentTTestOneSample(0.95)(value, 20.0) FROM t;
```

```response title=Response
```

{/*AUTOGENERATED_END*/}

**관련 항목**

* [스튜던트 t-검정](https://en.wikipedia.org/wiki/Student%27s_t-test)
* [studentTTest 함수](/ko/reference/functions/aggregate-functions/studentTTest)