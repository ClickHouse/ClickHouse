---
description: '히스토그램 메트릭의 주기적 스냅샷이 저장되며 디스크로 플러시되는 시스템 테이블입니다.'
keywords: ['시스템 테이블', 'histogram_metric_log']
sidebarTitle: 'histogram_metric_log'
slug: /operations/system-tables/histogram_metric_log
title: 'system.histogram_metric_log'
doc_type: '참고'
---

import SystemTableCloud from '/snippets/ko/_system_table_cloud.mdx';

<SystemTableCloud />

<div id="description">
  ## 설명
</div>

`system.histogram_metrics`의 이력입니다. `collect_interval_milliseconds`마다 스냅샷이 생성되며 디스크에 플러시됩니다.

<div id="columns">
  ## 컬럼
</div>

{/*AUTOGENERATED_START*/}

* `hostname` ([LowCardinality(String)](/ko/reference/data-types/lowcardinality)) — 쿼리를 실행하는 서버의 호스트명입니다.
* `event_date` ([Date](/ko/reference/data-types/date)) — 이벤트 날짜입니다.
* `event_time` ([DateTime](/ko/reference/data-types/datetime)) — 이벤트 시간입니다.
* `event_time_microseconds` ([DateTime64(6)](/ko/reference/data-types/datetime64)) — 마이크로초 해상도의 이벤트 시간입니다.
* `metric` ([LowCardinality(String)](/ko/reference/data-types/lowcardinality)) — 메트릭 이름입니다.
* `labels` ([Map(LowCardinality(String), LowCardinality(String))](/ko/reference/data-types/map)) — 메트릭 레이블입니다.
* `histogram` ([Map(Float64, UInt64)](/ko/reference/data-types/map)) — 누적 히스토그램입니다. 버킷의 상한값을 그 경계 이하(≤)의 관측값 수에 매핑하며, 마지막 버킷으로 +inf를 포함합니다.
* `count` ([UInt64](/ko/reference/data-types/int-uint)) — 전체 관측값 수이며, histogram[+inf]와 같습니다.
* `sum` ([Float64](/ko/reference/data-types/float)) — 관측된 모든 값의 합계입니다.

{/*AUTOGENERATED_END*/}

<div id="example">
  ## 예시
</div>

```sql
SELECT event_time, metric, labels, histogram
FROM system.histogram_metric_log
WHERE metric = 'keeper_response_time_ms'
ORDER BY event_time DESC
LIMIT 1
FORMAT Vertical;
```

<div id="see-also">
  ## 관련 항목
</div>

* [system.histogram&#95;metrics](/ko/reference/system-tables/histogram_metrics) — 실시간 히스토그램 메트릭입니다.
* [system.metric&#95;log](/ko/reference/system-tables/metric_log) — `system.metrics` 및 `system.events`의 이력입니다.