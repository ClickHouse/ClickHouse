---
description: '로컬 서버에 상주하는 스케줄링 노드의 정보와 상태를 담고 있는 시스템 테이블입니다.'
keywords: ['시스템 테이블', 'scheduler']
slug: /operations/system-tables/scheduler
title: 'system.scheduler'
doc_type: '참고'
---

import SystemTableCloud from '/snippets/ko/_system_table_cloud.mdx';

<SystemTableCloud />

<div id="description">
  ## 설명
</div>

로컬 server에 있는 [스케줄링 노드](/ko/concepts/features/configuration/server-config/workload-scheduling#hierarchy)의 정보와 상태를 담고 있습니다.
이 table은 모니터링에 사용할 수 있습니다. 각 스케줄링 노드마다 하나의 행이 있습니다.

<div id="columns">
  ## 컬럼
</div>

{/*AUTOGENERATED_START*/}

* `resource` ([String](/ko/reference/data-types/index)) — 리소스 이름
* `path` ([String](/ko/reference/data-types/index)) — 이 리소스 스케줄링 계층 구조에서 해당 스케줄링 노드까지의 경로
* `type` ([String](/ko/reference/data-types/index)) — 스케줄링 노드의 유형
* `weight` ([Float64](/ko/reference/data-types/index)) — 노드의 가중치이며, `fair` 유형의 상위 노드에서 사용됩니다.
* `priority` ([Int64](/ko/reference/data-types/index)) — 노드의 우선순위이며, &#39;priority&#39; 유형의 상위 노드에서 사용됩니다(값이 낮을수록 우선순위가 높음).
* `precedence` ([Int64](/ko/reference/data-types/index)) — 노드의 선행순서이며, space-shared (memory) 상위 노드에서 admission 및 eviction 순서를 정하는 데 사용됩니다(값이 낮을수록 선행순서가 높음).
* `is_active` ([Nullable(UInt8)](/ko/reference/data-types/index)) — 이 노드가 현재 활성 상태인지 여부입니다. 즉, 큐에서 꺼낼 리소스 요청이 있고 제약 조건이 충족된 상태를 의미합니다.
* `active_children` ([Nullable(UInt64)](/ko/reference/data-types/index)) — 활성 상태인 하위 노드의 수입니다.
* `dequeued_requests` ([Nullable(UInt64)](/ko/reference/data-types/index)) — 이 노드에서 큐에서 꺼낸 리소스 요청의 총수입니다.
* `canceled_requests` ([Nullable(UInt64)](/ko/reference/data-types/index)) — 이 노드에서 취소된 리소스 요청의 총수입니다.
* `rejected_requests` ([Nullable(UInt64)](/ko/reference/data-types/index)) — 이 노드에서 거부된 리소스 요청의 총수입니다.
* `dequeued_cost` ([Nullable(Int64)](/ko/reference/data-types/index)) — 이 노드에서 큐에서 꺼낸 모든 요청 비용(예: 바이트 단위 크기)의 합계입니다.
* `canceled_cost` ([Nullable(Int64)](/ko/reference/data-types/index)) — 이 노드에서 취소된 모든 요청 비용(예: 바이트 단위 크기)의 합계입니다.
* `rejected_cost` ([Nullable(Int64)](/ko/reference/data-types/index)) — 이 노드에서 거부된 모든 요청 비용(예: 바이트 단위 크기)의 합계입니다.
* `busy_periods` ([Nullable(UInt64)](/ko/reference/data-types/index)) — 이 노드가 비활성화된 총횟수입니다.
* `throughput` ([Nullable(Float64)](/ko/reference/data-types/index)) — 현재 평균 처리량(초당 큐에서 꺼낸 비용)입니다.
* `vruntime` ([Nullable(Float64)](/ko/reference/data-types/index)) — `fair` 노드의 하위 노드에만 적용됩니다. SFQ 알고리즘이 max-min 공정 방식으로 다음에 처리할 하위 노드를 선택할 때 사용하는 노드의 가상 런타임입니다.
* `system_vruntime` ([Nullable(Float64)](/ko/reference/data-types/index)) — `fair` 노드에만 적용됩니다. 마지막으로 처리된 리소스 요청의 `vruntime`을 나타내는 가상 런타임입니다. 하위 노드를 활성화할 때 `vruntime`의 새 값으로 사용됩니다.
* `queue_length` ([Nullable(UInt64)](/ko/reference/data-types/index)) — `fifo` 노드에만 적용됩니다. 현재 큐에 있는 리소스 요청 수입니다.
* `queue_cost` ([Nullable(Int64)](/ko/reference/data-types/index)) — `fifo` 노드에만 적용됩니다. 현재 큐에 있는 모든 요청 비용(예: 바이트 단위 크기)의 합계입니다.
* `budget` ([Nullable(Int64)](/ko/reference/data-types/index)) — `fifo` 노드에만 적용됩니다. 새 리소스 요청에 사용할 수 있는 &#39;비용 단위&#39; 수입니다. 리소스 요청의 예상 비용과 실제 비용이 일치하지 않는 경우(예: 읽기/쓰기 실패 후) 나타날 수 있습니다.
* `is_satisfied` ([Nullable(UInt8)](/ko/reference/data-types/index)) — 제약 노드에만 적용됩니다(예: `inflight_limit`). 이 노드의 모든 제약 조건이 충족되면 `1`입니다.
* `inflight_requests` ([Nullable(Int64)](/ko/reference/data-types/index)) — `inflight_limit` 노드에만 적용됩니다. 이 노드에서 큐에서 꺼낸 리소스 요청 중 현재 활용 상태에 있는 요청 수입니다.
* `inflight_cost` ([Nullable(Int64)](/ko/reference/data-types/index)) — `inflight_limit` 노드에만 적용됩니다. 이 노드에서 큐에서 꺼낸 리소스 요청 중 현재 활용 상태에 있는 모든 요청 비용(예: 바이트)의 합계입니다.
* `max_requests` ([Nullable(Int64)](/ko/reference/data-types/index)) — `inflight_limit` 노드에만 적용됩니다. 제약 조건 위반을 유발하는 `inflight_requests`의 상한입니다.
* `max_cost` ([Nullable(Int64)](/ko/reference/data-types/index)) — `inflight_limit` 노드에만 적용됩니다. 제약 조건 위반을 유발하는 `inflight_cost`의 상한입니다.
* `max_speed` ([Nullable(Float64)](/ko/reference/data-types/index)) — `bandwidth_limit` 노드에만 적용됩니다. 초당 토큰 수 기준 대역폭의 상한입니다.
* `max_burst` ([Nullable(Float64)](/ko/reference/data-types/index)) — `bandwidth_limit` 노드에만 적용됩니다. token-bucket throttler에서 사용할 수 있는 토큰의 상한입니다.
* `throttling_us` ([Nullable(Int64)](/ko/reference/data-types/index)) — `bandwidth_limit` 노드에만 적용됩니다. 이 노드가 스로틀링 상태였던 총 마이크로초 수입니다.
* `tokens` ([Nullable(Float64)](/ko/reference/data-types/index)) — `bandwidth_limit` 노드에만 적용됩니다. 현재 token-bucket throttler에서 사용할 수 있는 토큰 수입니다.
* `allocated` ([Nullable(Int64)](/ko/reference/data-types/index)) — `space-shared` 노드에만 적용됩니다. 이 노드 아래에 현재 할당된 리소스의 양입니다.
* `allocations` ([Nullable(UInt64)](/ko/reference/data-types/index)) — `space-shared` 노드에만 적용됩니다. 이 노드 아래에서 현재 실행 중인 리소스 할당 수입니다.
* `updates` ([Nullable(UInt64)](/ko/reference/data-types/index)) — `space-shared` 노드에만 적용됩니다. 이 노드를 통해 전파된 업데이트의 총수입니다.
* `increases` ([Nullable(UInt64)](/ko/reference/data-types/index)) — `space-shared` 노드에만 적용됩니다. 이 노드에서 승인된 증가 요청의 총수입니다.
* `decreases` ([Nullable(UInt64)](/ko/reference/data-types/index)) — `space-shared` 노드에만 적용됩니다. 이 노드에서 승인된 감소 요청의 총수입니다.
* `admits` ([Nullable(UInt64)](/ko/reference/data-types/index)) — `space-shared` 노드에만 적용됩니다. 이 노드에서 허용된 할당의 총수입니다.
* `removes` ([Nullable(UInt64)](/ko/reference/data-types/index)) — `space-shared` 노드에만 적용됩니다. 이 노드에서 제거된 할당의 총수입니다.
* `killers` ([Nullable(UInt64)](/ko/reference/data-types/index)) — `space-shared` 노드에만 적용됩니다. 이 노드의 증가 요청으로 인해 시작된 축출의 총수입니다.
* `victims` ([Nullable(UInt64)](/ko/reference/data-types/index)) — `space-shared` 노드에만 적용됩니다. 이 노드에서 축출된 할당의 총수입니다.
* `rejects` ([Nullable(UInt64)](/ko/reference/data-types/index)) — `allocation_queue` 노드에만 적용됩니다. 이 노드에서 거부된 리소스 할당의 총수입니다.
* `pending` ([Nullable(UInt64)](/ko/reference/data-types/index)) — `allocation_queue` 노드에만 적용됩니다. 이 노드에서 현재 대기 중인 리소스 할당 수입니다.

{/*AUTOGENERATED_END*/}

예시:

```sql
SELECT *
FROM system.scheduler
WHERE resource = 'network_read' AND path = '/prio/fair/prod'
FORMAT Vertical
```

```text
Row 1:
──────
resource:          network_read
path:              /prio/fair/prod
type:              fifo
weight:            5
priority:          0
is_active:         0
active_children:   0
dequeued_requests: 67
canceled_requests: 0
dequeued_cost:     4692272
canceled_cost:     0
busy_periods:      63
vruntime:          938454.1999999989
system_vruntime:   ᴺᵁᴸᴸ
queue_length:      0
queue_cost:        0
budget:            -60524
is_satisfied:      ᴺᵁᴸᴸ
inflight_requests: ᴺᵁᴸᴸ
inflight_cost:     ᴺᵁᴸᴸ
max_requests:      ᴺᵁᴸᴸ
max_cost:          ᴺᵁᴸᴸ
max_speed:         ᴺᵁᴸᴸ
max_burst:         ᴺᵁᴸᴸ
throttling_us:     ᴺᵁᴸᴸ
tokens:            ᴺᵁᴸᴸ
```