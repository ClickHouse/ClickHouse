---
description: '쿼리 캐시의 내용을 보여주는 시스템 테이블입니다.'
keywords: ['시스템 테이블', 'query_cache']
slug: /operations/system-tables/query_cache
title: 'system.query_cache'
doc_type: '참고'
---

import SystemTableCloud from '/snippets/ko/_system_table_cloud.mdx';

<SystemTableCloud />

<div id="description">
  ## 설명
</div>

[쿼리 캐시](/ko/concepts/features/performance/caches/query-cache)의 내용을 보여줍니다.

<div id="columns">
  ## 컬럼
</div>

{/*AUTOGENERATED_START*/}

* `query` ([String](/ko/reference/data-types/string)) — 쿼리 문자열.
* `query_id` ([String](/ko/reference/data-types/string)) — 쿼리 ID.
* `is_subquery` ([UInt8](/ko/reference/data-types/int-uint)) — 쿼리 캐시 엔트리가 하위 쿼리 결과인지 여부.
* `result_size` ([UInt64](/ko/reference/data-types/int-uint)) — 쿼리 캐시 엔트리의 크기.
* `tag` ([LowCardinality(String)](/ko/reference/data-types/lowcardinality)) — 쿼리 캐시 엔트리의 태그.
* `stale` ([UInt8](/ko/reference/data-types/int-uint)) — 쿼리 캐시 엔트리가 오래되었는지 여부.
* `shared` ([UInt8](/ko/reference/data-types/int-uint)) — 쿼리 캐시 엔트리가 여러 사용자 간에 공유되는지 여부.
* `compressed` ([UInt8](/ko/reference/data-types/int-uint)) — 쿼리 캐시 엔트리가 압축되었는지 여부.
* `expires_at` ([DateTime](/ko/reference/data-types/datetime)) — 쿼리 캐시 엔트리가 오래되는 시점.
* `key_hash` ([UInt64](/ko/reference/data-types/int-uint)) — 쿼리 캐시 엔트리를 찾는 키로 사용되는 쿼리 문자열의 해시.

{/*AUTOGENERATED_END*/}

<div id="example">
  ## 예시
</div>

```sql
SELECT * FROM system.query_cache FORMAT Vertical;
```

```text
Row 1:
──────
query:       SELECT 1 SETTINGS use_query_cache = 1
query_id:    7c28bbbb-753b-4eba-98b1-efcbe2b9bdf6
result_size: 128
tag:
stale:       0
shared:      0
compressed:  1
expires_at:  2023-10-13 13:35:45
key_hash:    12188185624808016954

1 row in set. Elapsed: 0.004 sec.
```