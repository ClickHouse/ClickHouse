---
description: 'Delta Lake 테이블에서 읽은 메타데이터 파일 정보를 담고 있는 시스템 테이블입니다. 각 항목은
  루트 메타데이터 JSON 파일 하나를 나타냅니다.'
keywords: ['시스템 테이블', 'delta_lake_metadata_log']
slug: /operations/system-tables/delta_lake_metadata_log
title: 'system.delta_lake_metadata_log'
doc_type: 'reference'
---

import SystemTableCloud from '/snippets/ko/_system_table_cloud.mdx';

<SystemTableCloud />

<div id="purpose">
  ## 설명
</div>

`system.delta_lake_metadata_log` 테이블은 ClickHouse가 읽는 Delta Lake 테이블의 메타데이터 접근 및 파싱 이벤트를 기록합니다. 각 메타데이터 파일에 대한 자세한 정보를 제공하므로 디버깅, 감사, 그리고 Delta 테이블 구조 변화 과정을 이해하는 데 유용합니다.

이 테이블은 Delta Lake 테이블에서 읽은 모든 메타데이터 파일을 기록합니다. 이를 통해 사용자는 ClickHouse가 Delta 테이블 메타데이터를 어떻게 해석하는지 추적하고, 스키마(schema) 변경, snapshot 판별, 또는 쿼리 계획과 관련된 문제를 진단할 수 있습니다.

<Note>
  이 테이블은 주로 디버깅 목적으로 사용됩니다.
</Note>

<div id="controlling-log-verbosity">
  ### 로그 상세도 제어
</div>

[`delta_lake_log_metadata`](/ko/reference/settings/session-settings#delta_lake_log_metadata) 설정을 사용하면 어떤 메타데이터 이벤트를 로그에 기록할지 제어할 수 있습니다.

현재 쿼리에서 사용된 모든 메타데이터를 로그에 기록하려면:

```sql
SELECT * FROM my_delta_table SETTINGS delta_lake_log_metadata = 1;

SYSTEM FLUSH LOGS delta_lake_metadata_log;

SELECT *
FROM system.delta_lake_metadata_log
WHERE query_id = '{previous_query_id}';
```

<div id="columns">
  ## 컬럼
</div>

{/*AUTOGENERATED_START*/}

* `hostname` ([LowCardinality(String)](/ko/reference/data-types/lowcardinality)) — 쿼리를 실행하는 서버의 호스트명입니다.
* `event_date` ([Date](/ko/reference/data-types/date)) — 항목의 날짜입니다.
* `event_time` ([DateTime](/ko/reference/data-types/datetime)) — 이벤트 발생 시각입니다.
* `query_id` ([String](/ko/reference/data-types/string)) — 쿼리 ID입니다.
* `table_path` ([String](/ko/reference/data-types/string)) — 테이블 경로입니다.
* `file_path` ([String](/ko/reference/data-types/string)) — 파일 경로입니다.
* `content` ([String](/ko/reference/data-types/string)) — JSON 포맷의 내용입니다.

{/*AUTOGENERATED_END*/}