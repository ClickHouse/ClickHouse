---
description: '기여자 정보가 포함된 시스템 테이블입니다.'
keywords: ['시스템 테이블', '기여자']
slug: /operations/system-tables/contributors
title: 'system.contributors'
doc_type: '참고'
---

<div id="description">
  ## 설명
</div>

기여자에 대한 정보를 포함합니다. 순서는 쿼리 실행 시점마다 무작위로 결정됩니다.

<div id="columns">
  ## 컬럼
</div>

{/*AUTOGENERATED_START*/}

* `name` ([String](/ko/reference/data-types/index)) — git log에 기록된 기여자(author) 이름입니다.

{/*AUTOGENERATED_END*/}

<div id="example">
  ## 예시
</div>

```sql
SELECT * FROM system.contributors LIMIT 10
```

```text
┌─name─────────────┐
│ Olga Khvostikova │
│ Max Vetrov       │
│ LiuYangkuan      │
│ svladykin        │
│ zamulla          │
│ Šimon Podlipský  │
│ BayoNet          │
│ Ilya Khomutov    │
│ Amy Krishnevsky  │
│ Loud_Scream      │
└──────────────────┘
```

테이블에서 직접 확인하려면 다음 쿼리를 사용합니다:

```sql
SELECT * FROM system.contributors WHERE name = 'Olga Khvostikova'
```

```text
┌─name─────────────┐
│ Olga Khvostikova │
└──────────────────┘
```