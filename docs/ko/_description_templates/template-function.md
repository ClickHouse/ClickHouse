---
title: Template 함수
---



## functionName {#functionname-in-lower-case}

간단한 설명.

**구문** (SELECT 제외)

```sql
<function syntax>
```

별칭: `<alias name>`. (선택 사항)

추가 설명 (선택 사항).

**인수** (선택 사항)

-   `x` — 설명. 선택 사항 (선택적 인수에만 해당). 가능한 값: <values list>. 기본값: <value>. [Type name](relative/path/to/type/dscr.md#type).
-   `y` — 설명. 선택 사항 (선택적 인수에만 해당). 가능한 값: <values list>. 기본값: <value>. [Type name](relative/path/to/type/dscr.md#type).

**매개변수** (선택 사항, 매개변수화된 집계 함수에만 해당)

-   `z` — 설명. 선택 사항 (선택적 매개변수에만 해당). 가능한 값: <values list>. 기본값: <value>. [Type name](relative/path/to/type/dscr.md#type).

**반환 값**

-   반환 값 목록.

유형: [Type name](relative/path/to/type/dscr.md#type).

**예시**

예시는 사용법 및/또는 활용 사례를 보여주어야 합니다. 다음 텍스트는 예시의 권장 구성 요소를 포함합니다.

입력 테이블 (선택 사항):

```text
```

```sql title="Query"
```

```text title="Response"
```

**관련 항목** (선택 사항)

-   [link](#)