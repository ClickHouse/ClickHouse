---
title: Что такое колоночная база данных?
description: "Колоночная база данных хранит данные каждого столбца независимо. Это позволяет читать с диска данные только тех столбцов, которые используются в конкретном запросе."
date: 2021-09-01
tags: ['Основные концепции данных']
keywords: ['Колоночная база данных']
---

import { Image } from "/snippets/ru/components/Image.jsx";

{frontMatter.description}

{/* truncate */}

<div id="introduction">
  ## Введение
</div>

Колоночная база данных хранит данные каждого столбца независимо. Это позволяет читать с диска данные только тех столбцов, которые используются в конкретном запросе. Обратная сторона такого подхода в том, что операции, затрагивающие целые строки, становятся пропорционально дороже. Другое название колоночной базы данных — столбцовая система управления базами данных. ClickHouse — типичный пример такой системы.

<div id="advantages-of-a-columnar-database">
  ## Преимущества колоночной базы данных
</div>

Ключевые преимущества колоночной базы данных:

* Запросы, использующие лишь несколько столбцов из множества.
* Агрегирующие запросы по большим объёмам данных.
* Сжатие данных по столбцам.

<div id="row-oriented-vs-column-oriented-databases">
  ## Строко-ориентированные и столбцовые базы данных
</div>

Ниже показана иллюстрация различий между традиционными строко-ориентированными системами и столбцовыми базами данных при построении отчётов:

**Традиционная строко-ориентированная**

<Image img="/images/row-oriented.webp" alt="Иллюстрация хранения данных в традиционной строко-ориентированной базе данных" size="md" />

**Столбцовая**

<Image img="/images/column-oriented.webp" alt="Иллюстрация хранения данных в столбцовой базе данных" size="md" />

Столбцовая база данных — предпочтительный выбор для аналитических приложений, поскольку позволяет держать в таблице множество столбцов про запас, но не тратить ресурсы на чтение неиспользуемых столбцов при выполнении запросов (традиционная OLTP-база данных во время запросов читает все данные, так как они хранятся по строкам, а не по столбцам). Столбцовые базы данных предназначены для обработки больших данных и построения хранилищ данных; они часто масштабируются на основе распределённых кластеров из недорогого оборудования для увеличения пропускной способности. ClickHouse реализует это с помощью сочетания [distributed](/ru/reference/engines/table-engines/special/distributed) и [replicated](/ru/reference/engines/table-engines/mergetree-family/replication) таблиц.