---
description: 'Документация по географическим координатам'
sidebar_label: 'Географические координаты'
slug: /sql-reference/functions/geo/coordinates
title: 'Функции для работы с географическими координатами'
doc_type: 'reference'
---

<div id="greatcircledistance">
  ## greatCircleDistance
</div>

Вычисляет расстояние между двумя точками на поверхности Земли с помощью [формулы большого круга](https://en.wikipedia.org/wiki/Great-circle_distance).

```sql
greatCircleDistance(lon1Deg, lat1Deg, lon2Deg, lat2Deg)
```

**Входные параметры**

* `lon1Deg` — долгота первой точки в градусах. Диапазон: `[-180°, 180°]`.
* `lat1Deg` — широта первой точки в градусах. Диапазон: `[-90°, 90°]`.
* `lon2Deg` — долгота второй точки в градусах. Диапазон: `[-180°, 180°]`.
* `lat2Deg` — широта второй точки в градусах. Диапазон: `[-90°, 90°]`.

Положительные значения соответствуют северной широте и восточной долготе, а отрицательные — южной широте и западной долготе.

**Возвращаемое значение**

Расстояние между двумя точками на поверхности Земли в метрах.

Генерирует исключение, если значения входных параметров выходят за пределы допустимого диапазона.

**Пример**

```sql
SELECT greatCircleDistance(55.755831, 37.617673, -55.755831, -37.617673) AS greatCircleDistance
```

```text
┌─greatCircleDistance─┐
│            14128352 │
└─────────────────────┘
```

<div id="geodistance">
  ## geoDistance
</div>

Аналогична `greatCircleDistance`, но вычисляет расстояние на эллипсоиде WGS-84, а не на сфере. Это более точное приближение геоида Земли.
Производительность такая же, как у `greatCircleDistance` (без потери производительности). Для вычисления расстояний на Земле рекомендуется использовать `geoDistance`.

Техническое примечание: для достаточно близких точек расстояние вычисляется с использованием плоского приближения и метрики на касательной плоскости в средней точке между координатами.

```sql
geoDistance(lon1Deg, lat1Deg, lon2Deg, lat2Deg)
```

**Входные параметры**

* `lon1Deg` — долгота первой точки в градусах. Диапазон: `[-180°, 180°]`.
* `lat1Deg` — широта первой точки в градусах. Диапазон: `[-90°, 90°]`.
* `lon2Deg` — долгота второй точки в градусах. Диапазон: `[-180°, 180°]`.
* `lat2Deg` — широта второй точки в градусах. Диапазон: `[-90°, 90°]`.

Положительные значения соответствуют северной широте и восточной долготе, а отрицательные — южной широте и западной долготе.

**Возвращаемое значение**

Расстояние между двумя точками на поверхности Земли в метрах.

Генерирует исключение, если значения входных параметров выходят за пределы диапазона.

**Пример**

```sql
SELECT geoDistance(38.8976, -77.0366, 39.9496, -75.1503) AS geoDistance
```

```text
┌─geoDistance─┐
│   212458.73 │
└─────────────┘
```

<div id="greatcircleangle">
  ## greatCircleAngle
</div>

Вычисляет центральный угол между двумя точками на земной поверхности с помощью [формулы большого круга](https://en.wikipedia.org/wiki/Great-circle_distance).

```sql
greatCircleAngle(lon1Deg, lat1Deg, lon2Deg, lat2Deg)
```

**Входные параметры**

* `lon1Deg` — долгота первой точки в градусах.
* `lat1Deg` — широта первой точки в градусах.
* `lon2Deg` — долгота второй точки в градусах.
* `lat2Deg` — широта второй точки в градусах.

**Возвращаемое значение**

Центральный угол между двумя точками в градусах.

**Пример**

```sql
SELECT greatCircleAngle(0, 0, 45, 0) AS arc
```

```text
┌─arc─┐
│  45 │
└─────┘
```

<div id="geotoutm">
  ## geoToUTM
</div>

Преобразует географические координаты WGS84 `(longitude, latitude)` в координаты [Universal Transverse Mercator (UTM)](https://en.wikipedia.org/wiki/Universal_Transverse_Mercator_coordinate_system).

UTM — это набор из 60 поперечных проекций Меркатора, каждая из которых охватывает зону шириной 6° по долготе и отображает географические координаты на плоскую сетку в метрах. Зона выбирается автоматически по долготе с учётом стандартных исключений для Норвегии и Шпицбергена, если явно не указана `zone`. UTM определена только для широт в диапазоне `[-80°, 84°]`; для полярных областей используется отдельная система UPS.

```sql
geoToUTM(longitude, latitude[, zone])
```

**Аргументы**

* `longitude` — Долгота в градусах. Диапазон: `[-180°, 180°]`. [`Float32`](../../data-types/float.md)/[`Float64`](../../data-types/float.md).
* `latitude` — Широта в градусах. Диапазон: `[-80°, 84°]`. [`Float32`](../../data-types/float.md)/[`Float64`](../../data-types/float.md).
* `zone` — Необязательно. Принудительно использовать эту зону UTM вместо автоматического выбора. Диапазон: `[1, 60]`. [`(U)Int*`](../../data-types/int-uint.md).

**Возвращаемое значение**

Именованный кортеж `(easting, northing, zone, band)`: `easting` и `northing` в метрах ([`Float64`](../../data-types/float.md)), номер зоны UTM `zone` ([`UInt8`](../../data-types/int-uint.md)) и буква широтной полосы MGRS `band` ([`FixedString(1)`](../../data-types/fixedstring.md)). Значение `band` `'N'` или более поздняя буква обозначает северное полушарие.

Генерирует исключение, если широта выходит за пределы `[-80°, 84°]` или долгота выходит за пределы `[-180°, 180°]`.

**Пример**

```sql
SELECT geoToUTM(2.294497, 48.858222) AS utm; -- Eiffel Tower
```

```text
(448251.5978370684,5411935.125629659,31,'U')
```

<div id="utmtogeo">
  ## UTMToGeo
</div>

Преобразует координаты [UTM](https://en.wikipedia.org/wiki/Universal_Transverse_Mercator_coordinate_system) обратно в географические координаты WGS84 `(долгота, широта)`. Это функция, обратная [`geoToUTM`](#geotoutm).

```sql
UTMToGeo(easting, northing, zone, is_north)
```

**Аргументы**

* `easting` — Восточное смещение в метрах (включая ложное восточное смещение 500000 м). [`(U)Int*`](../../data-types/int-uint.md)/[`Float*`](../../data-types/float.md).
* `northing` — Северное смещение в метрах (включая ложное северное смещение 10000000 м в южном полушарии). [`(U)Int*`](../../data-types/int-uint.md)/[`Float*`](../../data-types/float.md).
* `zone` — Номер зоны UTM. Диапазон: `[1, 60]`. [`(U)Int*`](../../data-types/int-uint.md).
* `is_north` — Полушарие: `1` для северного полушария, `0` для южного. [`(U)Int*`](../../data-types/int-uint.md).

**Возвращаемое значение**

Именованный кортеж `(longitude, latitude)` в градусах. [`Tuple(Float64, Float64)`](../../data-types/tuple.md).

**Пример**

```sql
SELECT UTMToGeo(448251.6, 5411935.13, 31, 1) AS coord;
```

```text
(2.2944970289079203,48.85822204127082)
```

<div id="geotomgrs">
  ## geoToMGRS
</div>

Кодирует географические координаты WGS84 `(longitude, latitude)` в строку [Military Grid Reference System (MGRS)](https://en.wikipedia.org/wiki/Military_Grid_Reference_System).

Строка имеет формат `<zone><band><100km square><easting><northing>`, например `31UDQ4825111935`. Аргумент `precision` задаёт количество цифр, используемых для каждой из координат easting и northing: `5` (по умолчанию) для 1 м, `4` для 10 м, `3` для 100 м, `2` для 1 км, `1` для 10 км и `0` — только для квадрата сетки 100 км. MGRS определена только для широт в диапазоне `[-80°, 84°]`.

```sql
geoToMGRS(longitude, latitude[, precision])
```

**Аргументы**

* `longitude` — Долгота в градусах. Диапазон: `[-180°, 180°]`. [`Float32`](../../data-types/float.md)/[`Float64`](../../data-types/float.md).
* `latitude` — Широта в градусах. Диапазон: `[-80°, 84°]`. [`Float32`](../../data-types/float.md)/[`Float64`](../../data-types/float.md).
* `precision` — Необязательный параметр. Количество цифр для восточной и северной координат. По умолчанию: `5`. Диапазон: `[0, 5]`. [`(U)Int*`](../../data-types/int-uint.md).

**Возвращаемое значение**

Строка координатной привязки MGRS. [`String`](../../data-types/string.md).

**Пример**

```sql
SELECT geoToMGRS(2.294497, 48.858222) AS mgrs, geoToMGRS(2.294497, 48.858222, 3) AS mgrs_100m;
```

```text
┌─mgrs────────────┬─mgrs_100m───┐
│ 31UDQ4825111935 │ 31UDQ482119 │
└─────────────────┴─────────────┘
```

<div id="mgrstogeo">
  ## MGRSToGeo
</div>

Декодирует строку [MGRS](https://en.wikipedia.org/wiki/Military_Grid_Reference_System) в географические координаты WGS84 `(longitude, latitude)`. Это операция, обратная [`geoToMGRS`](#geotomgrs).

Возвращаемая точка — центр соответствующего квадрата сетки, поэтому точность результата соответствует точности, закодированной в строке. Пробельные символы во входных данных игнорируются, а буквы обрабатываются регистронезависимо.

```sql
MGRSToGeo(mgrs)
```

**Аргументы**

* `mgrs` — строка координатной ссылки MGRS для декодирования. [`String`](../../data-types/string.md)/[`FixedString`](../../data-types/fixedstring.md).

**Возвращаемое значение**

Именованный кортеж `(longitude, latitude)` в градусах. [`Tuple(Float64, Float64)`](../../data-types/tuple.md).

**Пример**

```sql
SELECT MGRSToGeo('31UDQ4825111935') AS coord;
```

```text
(2.294495618908297,48.85822536113692)
```

<div id="pointinellipses">
  ## pointInEllipses
</div>

Проверяет, принадлежит ли точка хотя бы одному из эллипсов.
Координаты задаются в декартовой системе координат.

```sql
pointInEllipses(x, y, x₀, y₀, a₀, b₀,...,xₙ, yₙ, aₙ, bₙ)
```

**Входные параметры**

* `x, y` — координаты точки на плоскости.
* `xᵢ, yᵢ` — координаты центра `i`-го эллипса.
* `aᵢ, bᵢ` — оси `i`-го эллипса в единицах координат `x`, `y`.

Количество входных параметров должно быть равно `2+4⋅n`, где `n` — количество эллипсов.

**Возвращаемые значения**

`1`, если точка находится внутри хотя бы одного из эллипсов; `0`, если нет.

**Пример**

```sql
SELECT pointInEllipses(10., 10., 10., 9.1, 1., 0.9999)
```

```text
┌─pointInEllipses(10., 10., 10., 9.1, 1., 0.9999)─┐
│                                               1 │
└─────────────────────────────────────────────────┘
```

<div id="pointinpolygon">
  ## pointInPolygon
</div>

Проверяет, принадлежит ли точка полигону на плоскости.

```sql
pointInPolygon((x, y), [(a, b), (c, d) ...], ...)
```

**Входные значения**

* `(x, y)` — Координаты точки на плоскости. Тип данных — [Tuple](../../data-types/tuple.md) — кортеж из двух чисел.
* `[(a, b), (c, d) ...]` — Вершины полигона. Тип данных — [Array](../../data-types/array.md). Каждая вершина задаётся парой координат `(a, b)`. Вершины должны быть указаны по часовой стрелке или против часовой стрелки. Минимальное число вершин — 3. Полигон должен быть константой.
* Функция поддерживает полигоны с отверстиями (вырезанными областями). Тип данных — [Polygon](../../data-types/geo.md/#polygon). Можно либо передать весь `Polygon` вторым аргументом, либо сначала передать внешнее кольцо, а затем каждое отверстие как отдельный дополнительный аргумент.
* Функция также поддерживает мультиполигоны. Тип данных — [MultiPolygon](../../data-types/geo.md/#multipolygon). Можно либо передать весь `MultiPolygon` вторым аргументом, либо указать каждый входящий в него полигон отдельным аргументом.

**Возвращаемые значения**

`1`, если точка находится внутри полигона, и `0`, если нет.
Если точка находится на границе полигона, функция может вернуть как 0, так и 1.

**Пример**

```sql
SELECT pointInPolygon((3., 3.), [(6, 0), (8, 4), (5, 8), (0, 2)]) AS res
```

```text
┌─res─┐
│   1 │
└─────┘
```

> **Примечание**
> • Вы можете установить `validate_polygons = 0`, чтобы отключить проверку геометрии.
> • `pointInPolygon` исходит из того, что каждый полигон корректно сформирован. Если во входных данных есть самопересечения, кольца идут в неправильном порядке или рёбра перекрываются, результаты становятся ненадёжными — особенно для точек, расположенных точно на ребре, в вершине или внутри самопересечения, где понятия &quot;внутри&quot; и &quot;снаружи&quot; не определены.
> • Когда аргумент полигона является константой, а точка задаётся с использованием индексируемых столбцов ключа (например, `pointInPolygon((x, y), constant_polygon)` в таблице, где `x, y` входят в `PRIMARY KEY` или покрываются индексом `minmax`), ClickHouse может использовать и первичный ключ, и индексы пропуска данных `minmax`, чтобы отсекать нерелевантные гранулы.