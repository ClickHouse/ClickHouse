---
description: 'Вычисляет сумму чисел и одновременно подсчитывает количество строк. Эта
  функция используется оптимизатором запросов ClickHouse: если в запросе есть несколько
  функций `sum`, `count` или `avg`, их можно заменить одной функцией `sumCount`,
  чтобы повторно использовать результаты вычислений. Явно использовать эту функцию
  требуется редко.'
slug: /sql-reference/aggregate-functions/reference/sumcount
title: 'sumCount'
doc_type: 'reference'
---

{/*АВТОСГЕНЕРИРОВАННОЕ_НАЧАЛО*/ }

{/*AUTOGENERATED_END*/ }

**См. также**

* настройку [optimize&#95;syntax&#95;fuse&#95;functions](../../../operations/settings/settings.md#optimize_syntax_fuse_functions).