---
description: 'Документация по предложению SAMPLE'
sidebar_label: 'SAMPLE'
slug: /sql-reference/statements/select/sample
title: 'Предложение SAMPLE'
doc_type: 'reference'
---

Предложение `SAMPLE` позволяет выполнять приблизительную обработку `SELECT`-запросов.

Когда сэмплирование данных включено, запрос выполняется не по всем данным, а только по определённой их доле (выборке). Например, если вам нужно вычислить статистику по всем визитам, достаточно выполнить запрос по 1/10 всех визитов, а затем умножить результат на 10.

Приблизительная обработка запросов может быть полезна в следующих случаях:

* Когда у вас жёсткие требования к задержке (например, менее 100 мс), но затраты на дополнительные аппаратные ресурсы для их выполнения неоправданны.
* Когда ваши необработанные данные неточны, и поэтому приближение не приводит к заметному снижению качества.
* Когда бизнес-требования допускают приблизительные результаты (ради экономической эффективности или чтобы предлагать точные результаты премиум-пользователям).

:::note
Использовать сэмплирование можно только с таблицами семейства [MergeTree](../../../engines/table-engines/mergetree-family/mergetree.md) и только если выражение для семплирования было указано при создании таблицы (см. [движок MergeTree](../../../engines/table-engines/mergetree-family/mergetree.md#table_engine-mergetree-creating-a-table)).
:::

Ниже перечислены особенности сэмплирования данных:

* Сэмплирование данных — детерминированный механизм. Результат одного и того же запроса `SELECT .. SAMPLE` всегда одинаков.
* Сэмплирование работает согласованно для разных таблиц. Для таблиц с одним ключом выборки выборка с одинаковым коэффициентом всегда включает одно и то же подмножество возможных данных. Например, выборка по идентификаторам пользователей берёт строки с одним и тем же подмножеством всех возможных идентификаторов пользователей из разных таблиц. Это означает, что выборку можно использовать в подзапросах в предложении [IN](../../../sql-reference/operators/in.md). Кроме того, выборки можно объединять с помощью предложения [JOIN](../../../sql-reference/statements/select/join.md).
* Сэмплирование позволяет считывать с диска меньше данных. Обратите внимание, что ключ выборки должен быть задан корректно. Дополнительные сведения см. в разделе [Создание таблицы семейства MergeTree](../../../engines/table-engines/mergetree-family/mergetree.md#table_engine-mergetree-creating-a-table).

Для предложения `SAMPLE` поддерживается следующий синтаксис:

| Синтаксис предложения SAMPLE | Описание                                                                                                                                                                                                                                     |
| ---------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `SAMPLE k`                   | Здесь `k` — число от 0 до 1. Запрос выполняется по доле данных `k`. Например, `SAMPLE 0.1` выполняет запрос по 10% данных. [Подробнее](#sample-k)                                                                                            |
| `SAMPLE n`                   | Здесь `n` — достаточно большое целое число. Запрос выполняется по выборке не менее чем из `n` строк (но не намного больше этого числа). Например, `SAMPLE 10000000` выполняет запрос как минимум по 10 000 000 строк. [Подробнее](#sample-n) |
| `SAMPLE k OFFSET m`          | Здесь `k` и `m` — числа от 0 до 1. Запрос выполняется по выборке размером `k` от общего объёма данных. Данные, используемые для выборки, смещаются на долю `m`. [Подробнее](#sample-k-offset-m)                                              |

<div id="sample-k">
  ## SAMPLE K
</div>

Здесь `k` — число от 0 до 1 (поддерживаются как дробная, так и десятичная запись). Например, `SAMPLE 1/2` или `SAMPLE 0.5`.

В конструкции `SAMPLE k` выборка берётся из `k`-й доли данных. Пример показан ниже:

```sql
SELECT
    Title,
    count() * 10 AS PageViews
FROM hits_distributed
SAMPLE 0.1
WHERE
    CounterID = 34
GROUP BY Title
ORDER BY PageViews DESC LIMIT 1000
```

В этом примере запрос выполняется на выборке размером 0,1 (10 %) от данных. Значения агрегатных функций автоматически не пересчитываются, поэтому для получения приблизительного результата значение `count()` вручную умножают на 10.

<div id="sample-n">
  ## SAMPLE N
</div>

Здесь `n` — достаточно большое целое число. Например, `SAMPLE 10000000`.

В этом случае запрос выполняется по выборке не менее чем из `n` строк (но и не намного больше). Например, `SAMPLE 10000000` выполняет запрос как минимум по 10 000 000 строк.

Поскольку минимальная единица чтения данных — одна гранула (её размер задаётся настройкой `index_granularity`), имеет смысл задавать выборку, размер которой значительно превышает размер гранулы.

При использовании конструкции `SAMPLE n` вы не знаете, какой относительный процент данных был обработан. Поэтому неизвестно, на какой коэффициент следует умножать агрегатные функции. Чтобы получить приблизительный результат, используйте виртуальный столбец `_sample_factor`.

Столбец `_sample_factor` содержит относительные коэффициенты, которые вычисляются динамически. Этот столбец создаётся автоматически, когда вы [создаёте](../../../engines/table-engines/mergetree-family/mergetree.md#table_engine-mergetree-creating-a-table) таблицу с указанным ключом выборки. Примеры использования столбца `_sample_factor` приведены ниже.

Рассмотрим таблицу `visits`, содержащую статистику по посещениям сайта. В первом примере показано, как вычислить количество просмотров страниц:

```sql
SELECT sum(PageViews * _sample_factor)
FROM visits
SAMPLE 10000000
```

Следующий пример показывает, как вычислить общее число визитов:

```sql
SELECT sum(_sample_factor)
FROM visits
SAMPLE 10000000
```

В примере ниже показано, как вычислить среднюю продолжительность сеанса. Обратите внимание, что для вычисления средних значений не нужно использовать относительный коэффициент.

```sql
SELECT avg(Duration)
FROM visits
SAMPLE 10000000
```

<div id="sample-k-offset-m">
  ## SAMPLE K OFFSET M
</div>

Здесь `k` и `m` — значения от 0 до 1. Примеры приведены ниже.

**Пример 1**

```sql
SAMPLE 1/10
```

В этом примере выборка составляет 1/10 всех данных:

`[++------------]`

**Пример 2**

```sql
SAMPLE 1/10 OFFSET 1/2
```

Здесь 10%-ная выборка берётся из второй половины данных.

`[------++------]`