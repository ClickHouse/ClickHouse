---
title: 'system.fail_points'
slug: /operations/system-tables/fail_points
description: 'Содержит список всех доступных failpoint’ов с указанием их типа и текущего статуса.'
keywords: ['системная таблица', 'fail_points', 'failpoint', 'тестирование', 'отладка']
doc_type: 'reference'
---

<div id="description">
  ## Описание
</div>

Содержит список всех доступных failpoint, зарегистрированных на сервере, а также их тип и информацию о том, включены ли они в данный момент.

Failpoint можно включать и отключать во время работы с помощью команд `SYSTEM ENABLE FAILPOINT` и `SYSTEM DISABLE FAILPOINT`.

<div id="columns">
  ## Столбцы
</div>

{/*AUTOGENERATED_START*/ }

* `name` ([String](/ru/sql-reference/data-types/string)) — Имя failpoint.
* `type` ([Enum8(&#39;once&#39; = 0, &#39;regular&#39; = 1, &#39;pauseable&#95;once&#39; = 2, &#39;pauseable&#39; = 3)](/ru/sql-reference/data-types/enum)) — Тип failpoint: &#39;once&#39; срабатывает один раз, после чего автоматически отключается; &#39;regular&#39; срабатывает каждый раз; &#39;pauseable&#95;once&#39; один раз блокирует выполнение; &#39;pauseable&#39; блокирует выполнение при каждом срабатывании до возобновления.
* `enabled` ([UInt8](/ru/sql-reference/data-types/int-uint)) — Указывает, включен ли failpoint в данный момент (1) или отключен (0).

{/*AUTOGENERATED_END*/ }

<div id="example">
  ## Пример
</div>

```sql title="Query"
SYSTEM ENABLE FAILPOINT replicated_merge_tree_insert_retry_pause;
SELECT * FROM system.fail_points WHERE enabled = 1
```

```text title="Response"
┌─name──────────────────────────────────────┬─type────────────┬─enabled─┐
│ replicated_merge_tree_insert_retry_pause  │ pauseable_once  │       1 │
└───────────────────────────────────────────┴─────────────────┴─────────┘
```