---
description: 'Системная таблица с информацией о словарях'
keywords: ['системная таблица', 'словари']
slug: /operations/system-tables/dictionaries
title: 'system.dictionaries'
doc_type: 'reference'
---

import SystemTableCloud from '@site/docs/_snippets/_system_table_cloud.md';

<SystemTableCloud />

<div id="description">
  ## Описание
</div>

Содержит сведения о [словарях](../../sql-reference/statements/create/dictionary/overview.md).

<div id="columns">
  ## Столбцы
</div>

{/*AUTOGENERATED_START*/ }

* `database` ([String](/ru/sql-reference/data-types/string)) — Имя базы данных, содержащей словарь, созданный DDL-запросом. Для остальных словарей — пустая строка.
* `name` ([String](/ru/sql-reference/data-types/string)) — Имя словаря.
* `uuid` ([UUID](/ru/sql-reference/data-types/uuid)) — UUID словаря.
* `status` ([Enum8(&#39;NOT&#95;LOADED&#39; = 0, &#39;LOADED&#39; = 1, &#39;FAILED&#39; = 2, &#39;LOADING&#39; = 3, &#39;FAILED&#95;AND&#95;RELOADING&#39; = 4, &#39;LOADED&#95;AND&#95;RELOADING&#39; = 5, &#39;NOT&#95;EXIST&#39; = 6)](/ru/sql-reference/data-types/enum)) — Статус словаря. Возможные значения: NOT&#95;LOADED — словарь не был загружен, поскольку не использовался, LOADED — словарь успешно загружен, FAILED — словарь не удалось загрузить из-за ошибки, LOADING — словарь загружается, LOADED&#95;AND&#95;RELOADING — словарь успешно загружен и в данный момент перезагружается (частые причины: запрос SYSTEM RELOAD DICTIONARY, тайм-аут, изменение конфигурации словаря), FAILED&#95;AND&#95;RELOADING — словарь не удалось загрузить из-за ошибки, и в данный момент он загружается.
* `origin` ([String](/ru/sql-reference/data-types/string)) — Путь к файлу конфигурации, описывающему словарь.
* `type` ([String](/ru/sql-reference/data-types/string)) — Тип размещения словаря. Хранение словарей в памяти.
* `key.names` ([Array(String)](/ru/sql-reference/data-types/array)) — Массив имен ключей, предоставляемых словарем.
* `key.types` ([Array(String)](/ru/sql-reference/data-types/array)) — Соответствующий массив типов ключей, предоставляемых словарем.
* `attribute.names` ([Array(String)](/ru/sql-reference/data-types/array)) — Массив имен атрибутов, предоставляемых словарем.
* `attribute.types` ([Array(String)](/ru/sql-reference/data-types/array)) — Соответствующий массив типов атрибутов, предоставляемых словарем.
* `bytes_allocated` ([UInt64](/ru/sql-reference/data-types/int-uint)) — Объем оперативной памяти, выделенной для словаря.
* `hierarchical_index_bytes_allocated` ([UInt64](/ru/sql-reference/data-types/int-uint)) — Объем оперативной памяти, выделенной для иерархического индекса.
* `query_count` ([UInt64](/ru/sql-reference/data-types/int-uint)) — Количество запросов с момента загрузки словаря или последней успешной перезагрузки.
* `hit_rate` ([Float64](/ru/sql-reference/data-types/float)) — Для cache-словарей — процент обращений, при которых значение находилось в кэше.
* `found_rate` ([Float64](/ru/sql-reference/data-types/float)) — Процент обращений, при которых значение было найдено.
* `element_count` ([UInt64](/ru/sql-reference/data-types/int-uint)) — Количество элементов, хранящихся в словаре.
* `load_factor` ([Float64](/ru/sql-reference/data-types/float)) — Степень заполнения словаря в процентах (для хешированного словаря — степень заполнения хеш-таблицы).
* `source` ([String](/ru/sql-reference/data-types/string)) — Текстовое описание источника данных для словаря.
* `lifetime_min` ([UInt64](/ru/sql-reference/data-types/int-uint)) — Минимальное время жизни словаря в памяти, по истечении которого ClickHouse пытается перезагрузить словарь (если задан `invalidate&#95;query`, то только если он изменился). Задается в секундах.
* `lifetime_max` ([UInt64](/ru/sql-reference/data-types/int-uint)) — Максимальное время жизни словаря в памяти, по истечении которого ClickHouse пытается перезагрузить словарь (если задан `invalidate&#95;query`, то только если он изменился). Задается в секундах.
* `loading_start_time` ([DateTime](/ru/sql-reference/data-types/datetime)) — Время начала загрузки словаря.
* `last_successful_update_time` ([DateTime](/ru/sql-reference/data-types/datetime)) — Время завершения загрузки или обновления словаря. Помогает отслеживать проблемы с источниками словарей и выяснять их причины.
* `error_count` ([UInt64](/ru/sql-reference/data-types/int-uint)) — Количество ошибок с момента последней успешной загрузки. Помогает отслеживать проблемы с источниками словарей и выяснять их причины.
* `loading_duration` ([Float32](/ru/sql-reference/data-types/float)) — Длительность загрузки словаря.
* `last_exception` ([String](/ru/sql-reference/data-types/string)) — Текст ошибки, возникающей при создании или перезагрузке словаря, если словарь не удалось создать.
* `comment` ([String](/ru/sql-reference/data-types/string)) — Текст комментария к словарю.

{/*AUTOGENERATED_END*/ }

<div id="example">
  ## Пример
</div>

Настройте словарь:

```sql
CREATE DICTIONARY dictionary_with_comment
(
    id UInt64,
    value String
)
PRIMARY KEY id
SOURCE(CLICKHOUSE(HOST 'localhost' PORT tcpPort() TABLE 'source_table'))
LAYOUT(FLAT())
LIFETIME(MIN 0 MAX 1000)
COMMENT 'The temporary dictionary';
```

Убедитесь, что словарь загружен.

```sql
SELECT * FROM system.dictionaries LIMIT 1 FORMAT Vertical;
```

```text
Row 1:
──────
database:                    default
name:                        dictionary_with_comment
uuid:                        4654d460-0d03-433a-8654-d4600d03d33a
status:                      NOT_LOADED
origin:                      4654d460-0d03-433a-8654-d4600d03d33a
type:
key.names:                   ['id']
key.types:                   ['UInt64']
attribute.names:             ['value']
attribute.types:             ['String']
bytes_allocated:             0
query_count:                 0
hit_rate:                    0
found_rate:                  0
element_count:               0
load_factor:                 0
source:
lifetime_min:                0
lifetime_max:                0
loading_start_time:          1970-01-01 00:00:00
last_successful_update_time: 1970-01-01 00:00:00
loading_duration:            0
last_exception:
comment:                     The temporary dictionary
```