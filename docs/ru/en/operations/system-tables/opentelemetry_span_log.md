---
description: 'Системная таблица, содержащая информацию о спанах трассировки в выполненных запросах.'
keywords: ['системная таблица', 'opentelemetry_span_log']
slug: /operations/system-tables/opentelemetry_span_log
title: 'system.opentelemetry_span_log'
doc_type: 'reference'
---

import SystemTableCloud from '@site/docs/_snippets/_system_table_cloud.md';

<SystemTableCloud />

<div id="description">
  ## Описание
</div>

Содержит информацию о [спанах трассировки](https://opentracing.io/docs/overview/spans/), относящихся к выполненным запросам.

<div id="columns">
  ## Столбцы
</div>

{/*AUTOGENERATED_START*/ }

* `hostname` ([LowCardinality(String)](/ru/sql-reference/data-types/lowcardinality)) — Имя хоста, на котором был зафиксирован этот спан.
* `trace_id` ([UUID](/ru/sql-reference/data-types/uuid)) — Идентификатор трассировки для выполненного запроса.
* `span_id` ([UInt64](/ru/sql-reference/data-types/int-uint)) — Идентификатор спана трассировки.
* `parent_span_id` ([UInt64](/ru/sql-reference/data-types/int-uint)) — Идентификатор родительского спана трассировки.
* `operation_name` ([LowCardinality(String)](/ru/sql-reference/data-types/lowcardinality)) — Имя операции.
* `kind` ([Enum8(&#39;INTERNAL&#39; = 0, &#39;SERVER&#39; = 1, &#39;CLIENT&#39; = 2, &#39;PRODUCER&#39; = 3, &#39;CONSUMER&#39; = 4)](/ru/sql-reference/data-types/enum)) — SpanKind спана. INTERNAL — указывает, что спан представляет внутреннюю операцию внутри приложения. SERVER — указывает, что спан охватывает серверную обработку синхронного RPC или другого удалённого запроса. CLIENT — указывает, что спан описывает запрос к некоторому удалённому сервису. PRODUCER — указывает, что спан описывает инициатора асинхронного запроса. Этот родительский спан часто завершается до соответствующего дочернего спана CONSUMER, возможно, даже до начала дочернего спана. CONSUMER — указывает, что спан описывает дочерний элемент асинхронного запроса PRODUCER.
* `start_time_us` ([UInt64](/ru/sql-reference/data-types/int-uint)) — Время начала спана трассировки (в микросекундах).
* `finish_time_us` ([UInt64](/ru/sql-reference/data-types/int-uint)) — Время завершения спана трассировки (в микросекундах).
* `finish_date` ([Date](/ru/sql-reference/data-types/date)) — Дата завершения спана трассировки.
* `status_code` ([Enum8(&#39;UNSET&#39; = 0, &#39;OK&#39; = 1, &#39;ERROR&#39; = 2)](/ru/sql-reference/data-types/enum)) — Код состояния спана.
* `status_message` ([LowCardinality(String)](/ru/sql-reference/data-types/lowcardinality)) — Сообщение об ошибке.
* `attribute` ([Map(LowCardinality(String), String)](/ru/sql-reference/data-types/map)) — Атрибуты спана трассировки. Они заполняются в соответствии с рекомендациями стандарта OpenTelemetry.

**Псевдонимы:**

* `attribute.names` — Псевдоним для `mapKeys(attribute)`.
* `attribute.values` — Псевдоним для `mapValues(attribute)`.

{/*AUTOGENERATED_END*/ }

<div id="example">
  ## Пример
</div>

```sql title="Query"
SELECT * FROM system.opentelemetry_span_log LIMIT 1 FORMAT Vertical;
```

```text title="Response"
Row 1:
──────
trace_id:         cdab0847-0d62-61d5-4d38-dd65b19a1914
span_id:          701487461015578150
parent_span_id:   2991972114672045096
operation_name:   DB::Block DB::InterpreterSelectQuery::getSampleBlockImpl()
kind:             INTERNAL
start_time_us:    1612374594529090
finish_time_us:   1612374594529108
finish_date:      2021-02-03
attribute.names:  []
attribute.values: []
```

<div id="see-also">
  ## См. также
</div>

* [OpenTelemetry](../../operations/opentelemetry.md)