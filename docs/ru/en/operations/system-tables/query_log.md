---
description: 'Системная таблица, содержащая информацию о выполненных запросах, например,
  время начала, длительность выполнения и сообщения об ошибках.'
keywords: ['system table', 'query_log']
slug: /operations/system-tables/query_log
title: 'system.query_log'
doc_type: 'reference'
---

import SystemTableCloud from '@site/docs/_snippets/_system_table_cloud.md';

<SystemTableCloud />

<div id="description">
  ## Описание
</div>

Хранит метаданные и статистику о выполненных запросах, такие как время начала, длительность, сообщения об ошибках, использование ресурсов и другие сведения о выполнении. Результаты запросов не хранятся.

Вы можете изменить настройки логирования запросов в разделе [query&#95;log](../../operations/server-configuration-parameters/settings.md#query_log) конфигурации сервера.

Вы можете отключить логирование запросов, установив [log&#95;queries = 0](/ru/operations/settings/settings#log_queries). Мы не рекомендуем отключать логирование, поскольку информация в этой таблице важна для устранения проблем.

Период сброса данных задаётся параметром `flush_interval_milliseconds` в разделе настроек сервера [query&#95;log](../../operations/server-configuration-parameters/settings.md#query_log). Чтобы принудительно выполнить сброс, используйте запрос [SYSTEM FLUSH LOGS](/ru/sql-reference/statements/system#flush-logs).

ClickHouse не удаляет данные из таблицы автоматически. Подробнее см. в разделе [Introduction](/ru/operations/system-tables/overview#system-tables-introduction).

Таблица `system.query_log` регистрирует два вида запросов:

1. Исходные запросы, которые были запущены непосредственно клиентом.
2. Дочерние запросы, которые были инициированы другими запросами (для выполнения распределённого запроса). Для таких запросов информация о родительских запросах отображается в столбцах `initial_*`.

Каждый запрос создаёт одну или две строки в таблице `query_log` в зависимости от статуса запроса (см. столбец `type`):

1. Если выполнение запроса прошло успешно, создаются две строки с типами `QueryStart` и `QueryFinish`.
2. Если во время обработки запроса произошла ошибка, создаются два события с типами `QueryStart` и `ExceptionWhileProcessing`.
3. Если ошибка произошла до запуска запроса, создаётся одно событие с типом `ExceptionBeforeStart`.

Вы можете использовать настройку [log&#95;queries&#95;probability](/ru/operations/settings/settings#log_queries_probability), чтобы уменьшить количество запросов, регистрируемых в таблице `query_log`.

Вы можете использовать настройку [log&#95;formatted&#95;queries](/ru/operations/settings/settings#log_formatted_queries), чтобы записывать отформатированные запросы в столбец `formatted_query`.

<div id="columns">
  ## Столбцы
</div>

{/*AUTOGENERATED_START*/ }

* `hostname` ([LowCardinality(String)](/ru/sql-reference/data-types/lowcardinality)) — Имя хоста сервера, выполняющего запрос.
* `type` ([Enum8(&#39;QueryStart&#39; = 1, &#39;QueryFinish&#39; = 2, &#39;ExceptionBeforeStart&#39; = 3, &#39;ExceptionWhileProcessing&#39; = 4)](/ru/sql-reference/data-types/enum)) — Тип события, произошедшего при выполнении запроса. Значения: `QueryStart` — успешное начало выполнения запроса, `QueryFinish` — успешное завершение выполнения запроса, `ExceptionBeforeStart` — исключение до начала выполнения запроса, `ExceptionWhileProcessing` — исключение во время выполнения запроса.
* `event_date` ([Date](/ru/sql-reference/data-types/date)) — Дата начала запроса.
* `event_time` ([DateTime](/ru/sql-reference/data-types/datetime)) — Время начала запроса.
* `event_time_microseconds` ([DateTime64(6)](/ru/sql-reference/data-types/datetime64)) — Время начала запроса с точностью до микросекунд.
* `query_start_time` ([DateTime](/ru/sql-reference/data-types/datetime)) — Время начала выполнения запроса.
* `query_start_time_microseconds` ([DateTime64(6)](/ru/sql-reference/data-types/datetime64)) — Время начала выполнения запроса с точностью до микросекунд.
* `query_duration_ms` ([UInt64](/ru/sql-reference/data-types/int-uint)) — Продолжительность выполнения запроса в миллисекундах.
* `read_rows` ([UInt64](/ru/sql-reference/data-types/int-uint)) — Общее количество строк, прочитанных из всех таблиц и табличных функций, участвующих в запросе. Сюда входят обычные подзапросы, подзапросы для IN и JOIN. Для распределённых запросов read&#95;rows включает общее количество строк, прочитанных на всех репликах. Каждая реплика отправляет своё значение read&#95;rows, а сервер-инициатор запроса суммирует все полученные и локальные значения. Тома кэша не влияют на это значение.
* `read_bytes` ([UInt64](/ru/sql-reference/data-types/int-uint)) — Общее количество байтов, прочитанных из всех таблиц и табличных функций, участвующих в запросе. Сюда входят обычные подзапросы, подзапросы для IN и JOIN. Для распределённых запросов read&#95;bytes включает общее количество байтов, прочитанных на всех репликах. Каждая реплика отправляет своё значение read&#95;bytes, а сервер-инициатор запроса суммирует все полученные и локальные значения. Тома кэша не влияют на это значение.
* `written_rows` ([UInt64](/ru/sql-reference/data-types/int-uint)) — Количество строк, записанных запросом, включая строки, записанные последующими вставками, запущенными конвейером, например присоединёнными materialized view. Для синхронной вставки эти последующие строки записываются в запись `query_kind` = `Insert`; для asynchronous insert они записываются в запись `query_kind` = `AsyncInsertFlush`, тогда как ориентированная на клиента запись `Insert` содержит только строки, принятые от клиента. Для запросов, которые не записывают строки, значение равно 0.
* `written_bytes` ([UInt64](/ru/sql-reference/data-types/int-uint)) — Количество байтов, записанных запросом (в несжатом виде), включая байты, записанные последующими вставками, запущенными конвейером, например присоединёнными materialized view. Для синхронной вставки эти последующие байты записываются в запись `query_kind` = `Insert`; для asynchronous insert они записываются в запись `query_kind` = `AsyncInsertFlush`, тогда как ориентированная на клиента запись `Insert` содержит только байты, принятые от клиента. Для запросов, которые не записывают данные, значение равно 0.
* `result_rows` ([UInt64](/ru/sql-reference/data-types/int-uint)) — Количество строк в результате запроса SELECT или количество строк, записанных при вставке. Для синхронной вставки сюда входят строки, записанные последующими вставками, запущенными конвейером (например, присоединёнными materialized view), в записи `query_kind` = `Insert`; для asynchronous insert эти последующие строки записываются в запись `query_kind` = `AsyncInsertFlush`, тогда как ориентированная на клиента запись `Insert` содержит только строки, принятые от клиента.
* `result_bytes` ([UInt64](/ru/sql-reference/data-types/int-uint)) — Объём оперативной памяти в байтах, используемый для хранения результата запроса.
* `memory_usage` ([UInt64](/ru/sql-reference/data-types/int-uint)) — Объём памяти, используемой запросом.
* `current_database` ([LowCardinality(String)](/ru/sql-reference/data-types/lowcardinality)) — Имя текущей базы данных.
* `query` ([String](/ru/sql-reference/data-types/string)) — Строка запроса.
* `formatted_query` ([String](/ru/sql-reference/data-types/string)) — Отформатированная строка запроса.
* `normalized_query_hash` ([UInt64](/ru/sql-reference/data-types/int-uint)) — Числовое хеш-значение, одинаковое, например, для запросов, различающихся только значениями литералов.
* `query_kind` ([LowCardinality(String)](/ru/sql-reference/data-types/lowcardinality)) — Тип запроса.
* `databases` ([Array(LowCardinality(String))](/ru/sql-reference/data-types/array)) — Имена баз данных, присутствующих в запросе.
* `tables` ([Array(LowCardinality(String))](/ru/sql-reference/data-types/array)) — Имена таблиц, присутствующих в запросе.
* `columns` ([Array(LowCardinality(String))](/ru/sql-reference/data-types/array)) — Имена столбцов, присутствующих в запросе.
* `partitions` ([Array(LowCardinality(String))](/ru/sql-reference/data-types/array)) — Имена партиций, присутствующих в запросе.
* `projections` ([Array(LowCardinality(String))](/ru/sql-reference/data-types/array)) — Имена проекций, использованных во время выполнения запроса.
* `views` ([Array(LowCardinality(String))](/ru/sql-reference/data-types/array)) — Имена представлений (материализованных или live), присутствующих в запросе.
* `exception_code` ([Int32](/ru/sql-reference/data-types/int-uint)) — Код исключения.
* `exception` ([String](/ru/sql-reference/data-types/string)) — Сообщение исключения.
* `stack_trace` ([String](/ru/sql-reference/data-types/string)) — Трассировка стека. Пустая строка, если запрос был успешно завершён.
* `is_initial_query` ([UInt8](/ru/sql-reference/data-types/int-uint)) — Тип запроса. Возможные значения: 1 — запрос был инициирован клиентом, 0 — запрос был инициирован другим запросом как часть выполнения распределённого запроса.
* `connection_address` ([IPv6](/ru/sql-reference/data-types/ipv6)) — IP-адрес клиента, с которого было установлено соединение. При подключении через прокси это будет адрес прокси.
* `connection_port` ([UInt16](/ru/sql-reference/data-types/int-uint)) — Порт клиента, с которого было установлено соединение. При подключении через прокси это будет порт прокси.
* `user` ([LowCardinality(String)](/ru/sql-reference/data-types/lowcardinality)) — Имя пользователя, инициировавшего текущий запрос.
* `query_id` ([String](/ru/sql-reference/data-types/string)) — Идентификатор запроса.
* `address` ([IPv6](/ru/sql-reference/data-types/ipv6)) — IP-адрес, использованный для выполнения запроса. При подключении через прокси и установленном `auth_use_forwarded_address` это будет адрес клиента, а не прокси.
* `port` ([UInt16](/ru/sql-reference/data-types/int-uint)) — Порт клиента, использованный для выполнения запроса. При подключении через прокси и установленном `auth_use_forwarded_address` это будет порт клиента, а не прокси.
* `initial_user` ([LowCardinality(String)](/ru/sql-reference/data-types/lowcardinality)) — Имя пользователя, выполнившего исходный запрос (для выполнения распределённого запроса).
* `initial_query_id` ([String](/ru/sql-reference/data-types/string)) — Идентификатор исходного запроса (для выполнения распределённого запроса).
* `initial_address` ([IPv6](/ru/sql-reference/data-types/ipv6)) — IP-адрес, с которого был запущен родительский запрос.
* `initial_port` ([UInt16](/ru/sql-reference/data-types/int-uint)) — Порт клиента, который был использован для выполнения родительского запроса.
* `initial_query_start_time` ([DateTime](/ru/sql-reference/data-types/datetime)) — Время начала исходного запроса (для выполнения распределённого запроса).
* `initial_query_start_time_microseconds` ([DateTime64(6)](/ru/sql-reference/data-types/datetime64)) — Время начала исходного запроса с точностью до микросекунд (для выполнения распределённого запроса).
* `authenticated_user` ([LowCardinality(String)](/ru/sql-reference/data-types/lowcardinality)) — Имя пользователя, прошедшего аутентификацию в сеансе.
* `interface` ([UInt8](/ru/sql-reference/data-types/int-uint)) — Интерфейс, через который был инициирован запрос. Возможные значения: 1 — TCP, 2 — HTTP.
* `is_secure` ([UInt8](/ru/sql-reference/data-types/int-uint)) — Флаг, указывающий, был ли запрос выполнен через защищённый интерфейс.
* `os_user` ([LowCardinality(String)](/ru/sql-reference/data-types/lowcardinality)) — Имя пользователя операционной системы, от имени которого запущен клиент ClickHouse.
* `client_hostname` ([LowCardinality(String)](/ru/sql-reference/data-types/lowcardinality)) — Имя хоста клиентской машины, на которой запущен клиент ClickHouse или другой TCP-клиент.
* `client_name` ([LowCardinality(String)](/ru/sql-reference/data-types/lowcardinality)) — Имя клиента ClickHouse или другого TCP-клиента.
* `client_agent` ([LowCardinality(String)](/ru/sql-reference/data-types/lowcardinality)) — ИИ-агент для написания кода, вызвавший клиент (например, `claude-code` или `cursor`), определяемый по переменным окружения. Пустое значение, если агент не обнаружен.
* `client_revision` ([UInt32](/ru/sql-reference/data-types/int-uint)) — Ревизия клиента ClickHouse или другого TCP-клиента.
* `client_version_major` ([UInt32](/ru/sql-reference/data-types/int-uint)) — Мажорная версия клиента ClickHouse или другого TCP-клиента.
* `client_version_minor` ([UInt32](/ru/sql-reference/data-types/int-uint)) — Минорная версия клиента ClickHouse или другого TCP-клиента.
* `client_version_patch` ([UInt32](/ru/sql-reference/data-types/int-uint)) — Компонент patch версии клиента ClickHouse или другого TCP-клиента.
* `script_query_number` ([UInt32](/ru/sql-reference/data-types/int-uint)) — Номер запроса в скрипте с несколькими запросами для клиента ClickHouse.
* `script_line_number` ([UInt32](/ru/sql-reference/data-types/int-uint)) — Номер строки, с которой начинается запрос, в скрипте с несколькими запросами для клиента ClickHouse.
* `http_method` ([UInt8](/ru/sql-reference/data-types/int-uint)) — HTTP-метод, которым был инициирован запрос. Возможные значения: 0 — запрос был запущен через интерфейс TCP, 1 — использовался метод GET, 2 — использовался метод POST.
* `http_user_agent` ([LowCardinality(String)](/ru/sql-reference/data-types/lowcardinality)) — HTTP-заголовок UserAgent, переданный в HTTP-запросе.
* `http_referer` ([String](/ru/sql-reference/data-types/string)) — HTTP-заголовок Referer, переданный в HTTP-запросе (содержит абсолютный или частичный адрес страницы, выполняющей запрос).
* `forwarded_for` ([String](/ru/sql-reference/data-types/string)) — HTTP-заголовок X-Forwarded-For, переданный в HTTP-запросе.
* `quota_key` ([String](/ru/sql-reference/data-types/string)) — Ключ квоты, указанный в настройке quotas (см. keyed).
* `distributed_depth` ([UInt64](/ru/sql-reference/data-types/int-uint)) — Сколько раз запрос был перенаправлен между серверами.
* `revision` ([UInt32](/ru/sql-reference/data-types/int-uint)) — Ревизия ClickHouse.
* `log_comment` ([String](/ru/sql-reference/data-types/string)) — Комментарий Log. Может быть задан в виде произвольной строки длиной не более max&#95;query&#95;size. Если не задан, содержит пустую строку.
* `thread_ids` ([Array(UInt64)](/ru/sql-reference/data-types/array)) — Идентификаторы потоков, участвующих в выполнении запроса. Эти потоки могли выполняться не одновременно.
* `peak_threads_usage` ([UInt64](/ru/sql-reference/data-types/int-uint)) — Максимальное количество потоков, одновременно выполнявших запрос.
* `ProfileEvents` ([Map(LowCardinality(String), UInt64)](/ru/sql-reference/data-types/map)) — ProfileEvents, измеряющие различные метрики. Их описание можно найти в таблице system.events.
* `Settings` ([Map(LowCardinality(String), LowCardinality(String))](/ru/sql-reference/data-types/map)) — Settings, которые были изменены при выполнении запроса клиентом. Чтобы включить журналирование изменений настроек, установите параметр log&#95;query&#95;settings в 1.
* `used_aggregate_functions` ([Array(LowCardinality(String))](/ru/sql-reference/data-types/array)) — Канонические имена агрегатных функций, использовавшихся при выполнении запроса.
* `used_aggregate_function_combinators` ([Array(LowCardinality(String))](/ru/sql-reference/data-types/array)) — Канонические имена комбинаторов агрегатных функций, использовавшихся при выполнении запроса.
* `used_database_engines` ([Array(LowCardinality(String))](/ru/sql-reference/data-types/array)) — Канонические имена движков баз данных, использовавшихся при выполнении запроса.
* `used_data_type_families` ([Array(LowCardinality(String))](/ru/sql-reference/data-types/array)) — Канонические имена семейств типов данных, использовавшихся при выполнении запроса.
* `used_dictionaries` ([Array(LowCardinality(String))](/ru/sql-reference/data-types/array)) — Канонические имена словарей, использовавшихся при выполнении запроса.
* `used_formats` ([Array(LowCardinality(String))](/ru/sql-reference/data-types/array)) — Канонические имена форматов, использовавшихся при выполнении запроса.
* `used_functions` ([Array(LowCardinality(String))](/ru/sql-reference/data-types/array)) — Канонические имена функций, использовавшихся при выполнении запроса.
* `used_storages` ([Array(LowCardinality(String))](/ru/sql-reference/data-types/array)) — Канонические имена хранилищ, использовавшихся при выполнении запроса.
* `used_table_functions` ([Array(LowCardinality(String))](/ru/sql-reference/data-types/array)) — Канонические имена табличных функций, использовавшихся при выполнении запроса.
* `used_executable_user_defined_functions` ([Array(LowCardinality(String))](/ru/sql-reference/data-types/array)) — Канонические имена исполняемых пользовательских функций, использовавшихся при выполнении запроса.
* `used_sql_user_defined_functions` ([Array(LowCardinality(String))](/ru/sql-reference/data-types/array)) — Канонические имена пользовательских функций SQL, использовавшихся при выполнении запроса.
* `used_row_policies` ([Array(LowCardinality(String))](/ru/sql-reference/data-types/array)) — Список имен политик строк, использовавшихся при выполнении запроса.
* `used_privileges` ([Array(LowCardinality(String))](/ru/sql-reference/data-types/array)) — Привилегии, успешно проверенные при выполнении запроса.
* `missing_privileges` ([Array(LowCardinality(String))](/ru/sql-reference/data-types/array)) — Привилегии, отсутствующие при выполнении запроса.
* `transaction_id` ([Tuple(UInt64, UInt64, UUID)](/ru/sql-reference/data-types/tuple)) — Идентификатор транзакции, в рамках которой был выполнен этот запрос.
* `query_cache_usage` ([Enum8(&#39;Unknown&#39; = 0, &#39;None&#39; = 1, &#39;Write&#39; = 2, &#39;Read&#39; = 3)](/ru/sql-reference/data-types/enum)) — Использование кэша запросов при выполнении запроса. Значения: &#39;Unknown&#39; = Статус неизвестен, &#39;None&#39; = Результат запроса не был ни записан в кэш результатов запросов, ни прочитан из него, &#39;Write&#39; = Результат запроса был записан в кэш результатов запросов, &#39;Read&#39; = Результат запроса был прочитан из кэша результатов запросов.
* `asynchronous_read_counters` ([Map(LowCardinality(String), UInt64)](/ru/sql-reference/data-types/map)) — Метрики асинхронного чтения.
* `is_internal` ([UInt8](/ru/sql-reference/data-types/int-uint)) — Указывает, является ли запрос вспомогательным и выполняется ли он внутренне.

**Псевдонимы:**

* `ProfileEvents.Names` — псевдоним для `mapKeys(ProfileEvents)`.
* `ProfileEvents.Values` — псевдоним для `mapValues(ProfileEvents)`.
* `Settings.Names` — псевдоним для `mapKeys(Settings)`.
* `Settings.Values` — псевдоним для `mapValues(Settings)`.

{/*AUTOGENERATED_END*/ }

<div id="examples">
  ## Примеры
</div>

**Базовый пример**

```sql
SELECT * FROM system.query_log WHERE type = 'QueryFinish' ORDER BY query_start_time DESC LIMIT 1 FORMAT Vertical;
```

```text
Row 1:
──────
hostname:                              clickhouse.eu-central1.internal
type:                                  QueryFinish
event_date:                            2021-11-03
event_time:                            2021-11-03 16:13:54
event_time_microseconds:               2021-11-03 16:13:54.953024
query_start_time:                      2021-11-03 16:13:54
query_start_time_microseconds:         2021-11-03 16:13:54.952325
query_duration_ms:                     0
read_rows:                             69
read_bytes:                            6187
written_rows:                          0
written_bytes:                         0
result_rows:                           69
result_bytes:                          48256
memory_usage:                          0
current_database:                      default
query:                                 DESCRIBE TABLE system.query_log
formatted_query:
normalized_query_hash:                 8274064835331539124
query_kind:
databases:                             []
tables:                                []
columns:                               []
projections:                           []
views:                                 []
exception_code:                        0
exception:
stack_trace:
is_initial_query:                      1
user:                                  default
query_id:                              7c28bbbb-753b-4eba-98b1-efcbe2b9bdf6
address:                               ::ffff:127.0.0.1
port:                                  40452
initial_user:                          default
initial_query_id:                      7c28bbbb-753b-4eba-98b1-efcbe2b9bdf6
initial_address:                       ::ffff:127.0.0.1
initial_port:                          40452
initial_query_start_time:              2021-11-03 16:13:54
initial_query_start_time_microseconds: 2021-11-03 16:13:54.952325
interface:                             1
os_user:                               sevirov
client_hostname:                       clickhouse.eu-central1.internal
client_name:                           ClickHouse
client_revision:                       54449
client_version_major:                  21
client_version_minor:                  10
client_version_patch:                  1
http_method:                           0
http_user_agent:
http_referer:
forwarded_for:
quota_key:
revision:                              54456
log_comment:
thread_ids:                            [30776,31174]
ProfileEvents:                         {'Query':1,'NetworkSendElapsedMicroseconds':59,'NetworkSendBytes':2643,'SelectedRows':69,'SelectedBytes':6187,'ContextLock':9,'RWLockAcquiredReadLocks':1,'RealTimeMicroseconds':817,'UserTimeMicroseconds':427,'SystemTimeMicroseconds':212,'OSCPUVirtualTimeMicroseconds':639,'OSReadChars':894,'OSWriteChars':319}
Settings:                              {'load_balancing':'random','max_memory_usage':'10000000000'}
used_aggregate_functions:              []
used_aggregate_function_combinators:   []
used_database_engines:                 []
used_data_type_families:               []
used_dictionaries:                     []
used_formats:                          []
used_functions:                        []
used_storages:                         []
used_table_functions:                  []
used_executable_user_defined_functions:[]
used_sql_user_defined_functions:       []
used_privileges:                       []
missing_privileges:                    []
query_cache_usage:                     None
```

**Пример для Cloud**

В ClickHouse Cloud `system.query_log` локален для каждого узла; чтобы увидеть все записи, необходимо выполнять запрос через [`clusterAllReplicas`](/ru/sql-reference/table-functions/cluster).

Например, чтобы агрегировать строки `query&#95;log` со всех реплик в кластере “default”, можно написать:

```sql
SELECT * 
FROM clusterAllReplicas('default', system.query_log)
WHERE event_time >= now() - toIntervalHour(1)
LIMIT 10
SETTINGS skip_unavailable_shards = 1;
```

<div id="see-also">
  ## См. также
</div>

* [system.query&#95;thread&#95;log](/ru/operations/system-tables/query_thread_log) — Эта таблица содержит информацию о каждом потоке, выполняющем запрос.