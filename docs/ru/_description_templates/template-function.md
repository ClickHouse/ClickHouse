---
title: Функция Template
---



## functionName {#functionname-in-lower-case}

Краткое описание.

**Синтаксис** (без SELECT)

```sql
<function syntax>
```

Псевдоним: `<alias name>`. (Необязательно)

Дополнительный текст (необязательно).

**Аргументы** (необязательно)

-   `x` — Описание. Необязательный (только для необязательных аргументов). Возможные значения: <values list>. Значение по умолчанию: <value>. [Type name](relative/path/to/type/dscr.md#type).
-   `y` — Описание. Необязательный (только для необязательных аргументов). Возможные значения: <values list>. Значение по умолчанию: <value>. [Type name](relative/path/to/type/dscr.md#type).

**Параметры** (необязательно, только для параметрических агрегатных функций)

-   `z` — Описание. Необязательный (только для необязательных параметров). Возможные значения: <values list>. Значение по умолчанию: <value>. [Type name](relative/path/to/type/dscr.md#type).

**Возвращаемые значения**

-   Список возвращаемых значений.

Тип: [Type name](relative/path/to/type/dscr.md#type).

**Пример**

Пример должен демонстрировать использование функции и/или варианты её применения. Ниже приведены рекомендуемые элементы примера.

Входная таблица (необязательно):

```text
```

```sql title="Query"
```

```text title="Response"
```

**Смотрите также** (необязательно)

-   [link](#)