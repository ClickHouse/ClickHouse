---
title: 'Обзор коннектора'
sidebarTitle: 'Обзор'
description: 'Сервисы ClickHouse, управляемые ClickHouse Cloud в эксплуатируемом вами кластере, с обсервабилити и поддержкой, только через исходящие подключения'
keywords: ['BYOC', 'cloud', 'собственное облако', 'коннектор', 'managed mode', 'исполнитель']
doc_type: 'guide'
---

import { PrivatePreviewBadge } from '/snippets/ru/components/PrivatePreviewBadge/PrivatePreviewBadge.jsx';

<PrivatePreviewBadge />

Чтобы запросить доступ к закрытой предварительной версии, обратитесь к команде ClickHouse, работающей с вашим аккаунтом.

## Что делает коннектор {#what-the-connector-does}

Коннектор ClickHouse — это компонент, который вы разворачиваете в собственной среде. Он позволяет ClickHouse Cloud управлять сервисами ClickHouse в вашем Kubernetes-кластере, отслеживать их состояние и оказывать поддержку. ClickHouse Cloud никогда не подключается к вашей среде: коннектор устанавливает только исходящие подключения и действует от имени учётных сущностей, ограниченных областью сервисов ClickHouse. Коннектор поставляется в виде одного бинарного файла `clicklink`, который содержит демоны и инструментарий `clicklink clctl` для их установки и эксплуатации.

Коннектор состоит из трёх компонентов:

* **Исполнитель** управляет жизненным циклом сервисов, которые вы передаёте под управление ClickHouse. Он создаёт каждый сервис по отправленному вами определению, а затем применяет команды scale, stop, start, restart, backup и delete, поступающие из ClickHouse Cloud. Работа с запущенным исполнителем называется managed-режимом.
* **Scraper** собирает метрики из разрешённого списка (allowlist) системных таблиц ClickHouse, а также метаданные инфраструктуры и данные о состоянии, и отправляет их в ClickHouse Cloud.
* **Средство диагностики** позволяет инженерам поддержки ClickHouse выполнять диагностику в режиме только для чтения в ходе [сеанса поддержки](/ru/products/bring-your-own-cloud/connector/support-sessions), который вы включаете и можете завершить в любой момент.

В терминале команда `init` спрашивает, включить ли managed-режим, и по умолчанию предлагает «да»; при запуске `init` из скрипта режим включается только с флагом `--managed`. Можно работать и без него, сохранив обсервабилити и поддержку для кластеров, которыми вы управляете самостоятельно; см. [запуск без managed-режима](#run-without-managed-mode).

Все соединения, которые устанавливает коннектор, — исходящие. ClickHouse Cloud никогда не инициирует подключение к вашей среде. На странице [архитектуры](/ru/products/bring-your-own-cloud/connector/architecture) перечислены все соединения, используемые ими протоколы и способы аутентификации.

## Когда его использовать {#when-to-use-it}

Разверните коннектор, если вы хотите, чтобы ClickHouse Cloud управлял сервисами ClickHouse внутри вашего собственного облачного аккаунта при следующих ограничениях:

* Все пути доступа начинаются внутри вашего периметра; входящие подключения отсутствуют.
* Запись в ваш кластер ограничена пространствами имен тех сервисов, которыми управляет ClickHouse, — за исключением собственного токена, реестра и обновленного сертификата исполнителя в его домашнем пространстве имен. Компоненты платформы, от которых зависят эти сервисы, изменяются только после того, как вы одобрите каждое обновление.
* Ваши данные остаются в бакетах и под защитой ролей IAM, которые вы создаете и удаляете самостоятельно. У исполнителя нет учетных данных для доступа к вашим бакетам или ролям IAM.
* Доступ для службы поддержки ограничен по времени, предоставляется вами и может быть отозван вами в любой момент. Каждая команда, выполненная в рамках сеанса поддержки, фиксируется в журнале аудита, который принадлежит вам.

## Как это работает {#how-it-works}

ClickHouse регистрирует вашу среду во время онбординга и предоставляет конечную точку коннектора и одноразовый токен регистрации. Команда `clicklink clctl init --enroll` выполняется один раз: с рабочей станции с доступом через kubeconfig — при установке в Kubernetes, либо непосредственно на хосте — при установке на Linux VM. Эта единственная команда активирует token, получает клиентский сертификат mTLS, разворачивает и запускает демоны, а также проверяет их работоспособность.

`init` включает managed-режим, если передать `--managed`; в терминале команда задаст соответствующий вопрос, ответ по умолчанию — «да». При включении разворачивается исполнитель и применяется его grant доступа к Kubernetes. Этот grant представляет собой ServiceAccount `pcm-executor`. Политика допуска ограничивает его операции записи пространствами имен сервисов (по умолчанию с префиксом `ns-`), за исключением обновления его собственного токена, одной ConfigMap реестра и, в случае Kubernetes, обновленного клиентского сертификата в его домашнем пространстве имен.

Создание сервиса выполняется двумя командами. `clicklink clctl executor prepare` создает бакеты сервиса и роль IAM с использованием ваших собственных AWS credentials и однократно показывает пароль пользователя `default`. `clicklink clctl instances create` отправляет определение сервиса в ClickHouse Cloud через вашу конечную точку коннектора. После этого ClickHouse Cloud управляет сервисом через исходящий командный канал исполнителя: scaling, остановка и запуск, перезапуски, резервные копии, обновления версий и изменения конфигурации. Полное пошаговое описание см. в разделе [управляемые сервисы](/ru/products/bring-your-own-cloud/connector/managed-services).

Два действия остаются за вами. Обновления компонентов платформы в вашем кластере ожидают вашего подтверждения; см. [обновления платформы](/ru/products/bring-your-own-cloud/connector/platform-updates). При удалении сервиса удаляются только его ресурсы Kubernetes; бакеты и роль IAM сохраняются, пока вы не выполните `clicklink clctl executor teardown`.

Scraper с фиксированным интервалом считывает разрешенные системные таблицы и отправляет метрики, метаданные и health status на вашу конечную точку коннектора по mTLS. Средство диагностики держит открытым исходящий командный канал, но по этому каналу ничего не передается, пока вы не включите support session. Сертификат mTLS обновляется автоматически.

## Запуск без managed-режима {#run-without-managed-mode}

Передайте `--no-managed` в `clicklink clctl init` либо ответьте «нет» на соответствующий вопрос, чтобы установить только scraper и средство диагностики. В этом случае коннектор наблюдает за кластерами ClickHouse, которыми вы управляете самостоятельно, и обеспечивает их поддержку. ClickHouse не может создавать, изменять или удалять сервисы. Коннектор по-прежнему разворачивает сам себя и создаёт пользователей ClickHouse только для чтения и ServiceAccounts; см. [модель привилегий](/ru/products/bring-your-own-cloud/connector/reference/privilege-model). Такая же схема применяется к целевым системам, которые managed-режим не охватывает. Managed-режим работает на Amazon EKS с хранилищем S3; обсервабилити и поддержка доступны на любом совместимом Kubernetes-кластере или Linux-хосте.

## Требования вкратце {#requirements-at-a-glance}

* **Целевая среда развертывания.** Для managed-режима требуется кластер Amazon EKS с хранилищем S3. Обсервабилити и поддержка сами по себе работают на любом совместимом кластере Kubernetes (устанавливаются с помощью Helm-чарта) или на любом Linux-хосте с systemd, amd64 или arm64.
* **Регистрация со стороны ClickHouse.** ClickHouse регистрирует вашу среду для managed-режима и в ходе онбординга предоставляет конечную точку коннектора и одноразовый токен регистрации.
* **Исходящий сетевой доступ по порту 443** к вашей конечной точке коннектора и её конечной точке регистрации, а также к `releases.clicklink.clickhouse.com` и ECR Public во время установки. Для каждого шага предусмотрены варианты для изолированных от интернета и зеркалированных окружений; см. [онбординг](/ru/products/bring-your-own-cloud/connector/onboarding). В managed-режиме узлы вашего кластера также загружают образы ClickHouse из реестра, с которым зарегистрирована ваша среда, а исполнитель обращается к Amazon ECR и STS, когда загружает чарт платформы из ECR.
* **Права администратора кластера во время настройки.** При включении managed-режима права доступа исполнителю выдаются однократно: ServiceAccount, привязанная к нему РольКластера и политика допуска, ограничивающая его запись пространствами имен сервисов. Утверждение обновления платформы также выполняется с правами администратора кластера. На обеих целевых средах установки пакеты доступа средства диагностики привязаны к Kubernetes ServiceAccount. См. [онбординг](/ru/products/bring-your-own-cloud/connector/onboarding).
* **Учетные данные AWS для `prepare`, `teardown` и `platform approve`.** Создание бакетов и роли IAM для сервиса, а также их удаление после удаления сервиса выполняются под вашими учетными данными на вашей рабочей станции или на хосте коннектора. Команде `platform approve` также нужны учетные данные с правом чтения Amazon ECR, если подготовленные чарты находятся там. Исполнитель никогда не располагает учетными данными к вашим бакетам или IAM. Его единственные облачные вызовы направлены в Amazon ECR и STS. Он выполняет вход в реестр, когда синхронизация платформы или создание загружает чарт. Во время обновления платформы он проверяет образы под ролью загрузки только для чтения. Вызовов к S3 или IAM он не делает.
* **Доступный слушатель собственного протокола ClickHouse** для кластеров, которыми вы управляете самостоятельно и которые наблюдаются отдельным развертыванием коннектора, установленным с флагом `--no-managed`. Scraper и средство диагностики взаимодействуют с каждым кластером по собственному протоколу; в Kubernetes они автоматически определяют защищенное (9440) или незашифрованное (9000) подключение.
* **Административный доступ к ClickHouse во время настройки** для кластеров, которыми вы управляете самостоятельно. При первичной подготовке создаются пользователи коннектора с правами только для чтения. Пароль администратора, если он задан, запрашивается однократно и нигде не сохраняется.

## Следующие шаги {#next-steps}

* [Онбординг](/ru/products/bring-your-own-cloud/connector/onboarding): установка и регистрация коннектора в Kubernetes или на Linux VM.
* [Управляемые сервисы](/ru/products/bring-your-own-cloud/connector/managed-services): включение управляемого режима, создание сервиса, а также его жизненный цикл, статус и удаление.
* [Обновления платформы](/ru/products/bring-your-own-cloud/connector/platform-updates): утверждение изменений компонентов платформы в вашем кластере.
* [Архитектура](/ru/products/bring-your-own-cloud/connector/architecture): компоненты, все подключения, жизненный цикл сертификатов и данные, покидающие вашу среду.
* [Сеансы поддержки](/ru/products/bring-your-own-cloud/connector/support-sessions): как включать, ограничивать, проверять и отзывать доступ службы поддержки.
* [Конфигурация](/ru/products/bring-your-own-cloud/connector/configuration) и [операции](/ru/products/bring-your-own-cloud/connector/operations): настройка, обновления и задачи по сопровождению.
* [FAQ](/ru/products/bring-your-own-cloud/connector/reference/faq): часто задаваемые вопросы, в том числе о том, кто может изменять ваши сервисы, об исходящем трафике данных и отзыве доступа.

Коннектор работает в кластере, который вы предоставляете и контролируете; ClickHouse Cloud управляет сервисами внутри него через исполнителя. Если вы предпочитаете, чтобы ClickHouse сам создавал и обслуживал кластер в вашем облачном аккаунте, используйте модель развертывания [BYOC](/ru/products/cloud/guides/infrastructure/deployment-options/byoc/overview). На странице [архитектуры BYOC](/ru/products/bring-your-own-cloud/overview/architecture) объясняется, чем различаются эти две модели.