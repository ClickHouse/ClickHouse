---
noindex: true
title: "Развертывание ClickHouse на физическом оборудовании"
sidebarTitle: "Развертывание на физическом оборудовании"
description: "В этом руководстве показано, как развернуть ClickHouse на узлах физического оборудования с помощью Terraform и Ansible на экземплярах AWS EC2, используя RKE2 в качестве дистрибутива Kubernetes"
---

В этом руководстве показано, как развернуть ClickHouse на узлах физического оборудования с помощью Terraform и Ansible на экземплярах AWS EC2, используя RKE2 в качестве дистрибутива Kubernetes. В результате вы получите работающий cluster RKE2 с ClickHouse, развернутым с помощью оператора.

Подробные требования к инфраструктуре см. в [reference/infrastructure-requirements.md](/ru/products/clickhouse-private/reference/infrastructure-requirements-reference).

***

<div id="prerequisites">
  ## Предварительные требования
</div>

* Рабочая станция под управлением Linux (RHEL/Rocky Linux или аналогичный дистрибутив)
* `terraform` v1.13+
* `ansible` с поддержкой ролей Galaxy
* `kubectl`
* `helm` v3.x
* Учетные данные AWS IAM с разрешениями на работу с EC2, VPC и S3
* Доступ к контейнерному реестру ClickHouse
* [example-scripts.zip](https://d9v141ecaehea.cloudfront.net/example-scripts.zip?Expires=1796083200\&Signature=gY-eqdeGHilnNqad8y6KVoXjEYnJlpE2-QzSXOkV-qQmMbiDX0VSrrw6TLgS4Ls8pRQ9tjMuK2pM-cr2G92ZnP7H0EMoA-5P--YLbtU5RRD-HEDnTVc1cyLfLqb2uPmiWgEuWvkNA2lrQB7THp-MwDzdueV4x0hYGY3IoZTsPo9NvbuXasLgKTZ3Ioo0-GYeAdWpxi7sTOpESGXar9regLk~36uU7HFLE4gm~-KbTYbLcgfdf4S9VQIauIQPVaDUaFZ7X7ojpC1NvKWL2mJgRW84s53WtZCp947LslH-h-6QX1wzy6FEHKQr47j7jS4TkIA1aO7f9iDM~woCD7UH6Q__\&Key-Pair-Id=K3MEXWCGKHWKPM) -- содержит модули Terraform, плейбуки Ansible и шаблоны значений Helm

***

<Steps>
  <Step>
    ## Этап 1: Установите Terraform

    ```text
    dnf install -y unzip git
    ```

    ```bash
    curl -O https://releases.hashicorp.com/terraform/1.13.2/terraform_1.13.2_linux_amd64.zip
    ```

    ```text
    unzip terraform_1.13.2_linux_amd64.zip
    ```

    ```bash
    sudo cp terraform /usr/local/bin
    ```

    Проверьте установку:

    ```text
    terraform version
    ```

    Ожидаемый результат:

    ```text
    Terraform v1.13.2
    on linux_amd64
    ```

    ***
  </Step>

  <Step>
    ## Этап 2: Загрузка примеров скриптов

    Скачайте и распакуйте примеры скриптов, предоставленные ClickHouse:

    ```text
    unzip example-scripts.zip
    cd ./bare-metal
    ```

    Архив содержит модули Terraform, плейбуки Ansible, шаблоны inventory и файлы values для Helm.

    ***
  </Step>

  <Step>
    ## Этап 3: Настройка учетных данных AWS

    Создайте файл учетных данных, указав в нем ключ доступа и секретный ключ AWS IAM:

    ```bash
    cat > aws-creds.sh <<EOF
    export AWS_ACCESS_KEY_ID=<Your AWS ACCESS KEY ID>
    export AWS_SECRET_ACCESS_KEY=<Your AWS SECRET ACCESS KEY>
    export AWS_REGION=us-east-2
    EOF
    ```

    Замените `<Your AWS ACCESS KEY ID>` и `<Your AWS SECRET ACCESS KEY>` на значения из консоли AWS IAM.

    Загрузите учетные данные:

    ```bash
    source ./aws-creds.sh
    ```

    ***
  </Step>

  <Step>
    ## Этап 4: Инициализация и применение Terraform

    Инициализируйте модуль Terraform для RKE2. Добавьте `-migrate-state`, если переносите состояние из локального хранилища в S3.

    ```text
    terraform -chdir=modules/rke2 init \
    -backend-config="encrypt=true" \
    -backend-config="region=us-east-2" \
    -backend-config="bucket=chb-tfstate-359774249275" \
    -backend-config="use_lockfile=true" \
    -backend-config="key=chb/rke2/terraform.tfstate"
    ```

    Примените конфигурацию Terraform, чтобы развернуть инфраструктуру:

    ```text
    terraform -chdir=modules/rke2 apply \
    -var-file=../../vars/common.tfvars \
    -var-file=../../vars/instances.tfvars \
    -var-file=../../vars/rke2.tfvars
    ```

    Это создаст экземпляры EC2, сетевые ресурсы и группы безопасности, необходимые для кластера RKE2.

    ***
  </Step>

  <Step>
    ## Этап 5: Установите зависимости для Ansible

    Перейдите в каталог Ansible и установите необходимые роли и коллекции из Galaxy:

    ```bash
    cd ~/bare-metal/ansible
    ```

    ```text
    ansible-galaxy role install lablabs.rke2
    ```

    ```text
    ansible-galaxy collection install kubernetes.core
    ```

    ***
  </Step>

  <Step>
    ## Этап 6: Развертывание RKE2

    ### Убедитесь, что узлы готовы

    ```text
    ansible all -i inventory.yml -a 'cloud-init status'
    ```

    ### Настройка NVMe-накопителей

    ```text
    ansible-playbook -i inventory.yml disks_nvme.yml
    ```

    ### Запустите плейбук RKE2

    ```text
    ansible-playbook -i inventory.yml rke2_playbook.yml
    ```

    Этот плейбук устанавливает и настраивает RKE2 на всех узлах. Он разворачивает первый серверный узел, дожидается его готовности, а затем подключает к кластеру оставшиеся серверные и агентские узлы.

    В выводе плейбука будут показаны задачи, выполняемые на всех узлах (например, от `chb-rke2s101` до `chb-rke2s302`). После завершения вы должны увидеть примерно такую сводку:

    ```text
    PLAY RECAP *********************************************************************
    chb-rke2s101               : ok=40   changed=12   unreachable=0    failed=0    skipped=45   rescued=0    ignored=0
    chb-rke2s102               : ok=23   changed=7    unreachable=0    failed=0    skipped=38   rescued=0    ignored=0
    chb-rke2s201               : ok=23   changed=7    unreachable=0    failed=0    skipped=38   rescued=0    ignored=0
    chb-rke2s202               : ok=23   changed=7    unreachable=0    failed=0    skipped=38   rescued=0    ignored=0
    chb-rke2s301               : ok=23   changed=7    unreachable=0    failed=0    skipped=38   rescued=0    ignored=0
    chb-rke2s302               : ok=23   changed=7    unreachable=0    failed=0    skipped=38   rescued=0    ignored=0
    ```

    Задача `K8s nodes state` также выведет сводку со всеми узлами в статусе `Ready`, их ролями и версиями.

    ***
  </Step>

  <Step>
    ## Этап 7: Настройка cert-manager и сертификатов

    Установите cert-manager и trust-manager, а затем сгенерируйте сертификаты для ClickHouse с помощью плейбука Ansible:

    ```bash
    cd ~/bare-metal/ansible
    ansible-playbook -i 127.0.0.1, --connection=local manage_helm_applications.yml
    ```

    Этот сценарий выполняет следующее:

    1. Устанавливает Helm-чарт cert-manager
    2. Устанавливает Helm-чарт trust-manager
    3. Создает самоподписанный issuer и CA‑сертификат
    4. Создает issuer для CA‑сертификата
    5. Настраивает Rancher (опционально)
    6. Создает пространство имен для кластера ClickHouse
    7. Генерирует сертификаты для ClickHouse server и Keeper
    8. Копирует сертификаты в соответствующие секреты Kubernetes

    Ожидаемый результат:

    ```text
    PLAY RECAP *****************************************************************
    127.0.0.1                  : ok=18   changed=13   unreachable=0    failed=0    skipped=0    rescued=0    ignored=0
    ```

    ***
  </Step>

  <Step>
    ## Этап 8: Установите локальное хранилище

    ### Установите local-path-provisioner

    ```bash
    kubectl apply -f https://raw.githubusercontent.com/rancher/local-path-provisioner/v0.0.32/deploy/local-path-storage.yaml
    ```

    ### Создайте StorageClass

    ```bash
    cd ~/bare-metal/ch-cluster-deployment
    kubectl apply -f local-nvme-sc.yaml
    ```

    YAML-манифест StorageClass должен выглядеть так (обновите значения topology в соответствии с именами ваших узлов):

    ```yaml
    apiVersion: storage.k8s.io/v1
    kind: StorageClass
    metadata:
      name: local-nvme-sc
    allowVolumeExpansion: true
    parameters:
      path: /nvme/disk
    provisioner: rancher.io/local-path
    reclaimPolicy: Delete
    volumeBindingMode: WaitForFirstConsumer
    allowedTopologies:
    - matchLabelExpressions:
        - key: topology.kubernetes.io/zone
          values:
            - <your_cluster_name>-keeper
            - <your_host_1>
            - <your_host_2>
            - <your_host_3>
            - <your_host_4>
            - <your_host_5>
            - <your_host_6>
        - key: directpv.min.io/region
          values:
            - default
    ```

    ***
  </Step>

  <Step>
    ## Этап 9: Пометьте узлы

    Пометьте узлы метками группы ClickHouse и зоны топологии, требуемыми оператором:

    ```bash
    kubectl get nodes |awk '{ print $1}'|grep '01'|xargs -I {} -n 1 kubectl label nodes '{}' clickhouseGroup=keeper-amd64  topology.kubernetes.io/zone=c-baremetal-ch-01-keeper --overwrite
    kubectl get nodes |awk '{ print $1}'|grep '02'|xargs -I {} -n 1 kubectl label nodes '{}' clickhouseGroup=server-amd64  --overwrite
    kubectl label nodes chb-rke2s102 topology.kubernetes.io/zone=chb-rke2s102 --overwrite
    kubectl label nodes chb-rke2s202 topology.kubernetes.io/zone=chb-rke2s202 --overwrite
    kubectl label nodes chb-rke2s302 topology.kubernetes.io/zone=chb-rke2s302 --overwrite
    ```

    Скорректируйте имена узлов и метки в соответствии с вашим окружением.

    ***
  </Step>

  <Step>
    ## Этап 10: Установка ClickHouse Operator и кластера

    ### Установка переменных окружения

    Создайте и подключите скрипт переменных Helm. Замените все значения-заполнители:

    ```bash
    #!/bin/bash
    # AWS/S3 creds
    export AWS_ACCESS_KEY_ID=<your_aws_key>
    export AWS_SECRET_ACCESS_KEY=<your_aws_secret>
    export AWS_REGION=<your_aws_region>
    export S3_ACCESS_KEY_ID=<s3_or_minio_key>
    export S3_SECRET_ACCESS_KEY=<s3_or_minio_secret>
    export S3_REGION=us-east-1
    export S3_BUCKET_NAME=<your_s3_bucket>
    export S3_KEY_PREFIX=<ch-s3-unique-uuid>
    export S3_ENDPOINT=https://s3.us-east-1.amazonaws.com
    export ECR_HOST=<ecr_host>
    # Set AZs as determined by VPC subnets. If not relevant then use your k8s host names.
    export AZ_LIST='["<your_host_1>", "<your_host_2>", "<your_host_3>","<your_host_4>", "<your_host_5>", "<your_host_6>"]'

    PASSWORD='My super secret p@$$w0rd'
    export HASHED_PASSWORD=$(echo -n "$PASSWORD" | sha256sum | awk '{printf $1}' | base64 -w 0)
    export CLUSTER_NAME="baremetal-ch-01"
    export CHART_VERSION="1.1.311"

    export SERVER_NG_GROUP="server-amd64"
    export KEEPER_NG_GROUP="keeper-amd64"
    export KEEPER_CPU="4"
    export KEEPER_MEMORY="16Gi"
    export SERVER_CPU="64"
    export SERVER_MEMORY="128Gi"
    export CACHE_DISK_SIZE_GI=379Gi
    ```

    Выполните скрипт:

    ```bash
    source ./set_helm_vars.sh
    ```

    ### Установка оператора

    Войдите в реестр Helm и установите оператор:

    ```text
    # Log into the Helm registry
    aws ecr get-login-password | helm registry login --username AWS --password-stdin $ECR_HOST

    # Create image pull secret
    kubectl create secret docker-registry all-icr-io \
      --docker-server=$ECR_HOST \
      --docker-username=AWS \
      --docker-password=$(aws ecr get-login-password) \
      -n clickhouse-operator-system

    OPERATOR_VERSION=1.18587.1

    helm upgrade clickhouse-operator \
      oci://$ECR_HOST/helm/clickhouse-operator-helm \
      --install --reuse-values \
      --version="$OPERATOR_VERSION" \
      --create-namespace \
      -n clickhouse-operator-system \
      --set-json="image.repository=\"$ECR_HOST/clickhouse-operator\"" \
      --set-json='cilium.enabled=false' \
      --set-json='idleScalerEnabled=false' \
      --set-json='webhooks.enabled=false' \
      --set-json='operator.debug=true' \
      --set-json='operator.metricsScraper.enabled=false' \
      --set-json='operator.featureFlags.backupOnASeparatePod=true' \
      --set-json='operator.featureFlags.serverCMEKEnabled=true' \
      --set-json='imagePullSecrets=[{"name":"all-icr-io"}]' \
      --set-json="operator.availabilityZones=$AZ_LIST"
    ```

    ### Развёртывание кластера ClickHouse

    Создайте файл значений Helm (`clickhouse-values.yaml`) с конфигурацией кластера:

    ```yaml expandable
    imagePullSecrets:
    - name: all-icr-io

    account:
      hashedPassword: "$HASHED_PASSWORD"
    server:
      ssdCacheConfiguration:
        cacheDiskSize: "$CACHE_DISK_SIZE_GI"
      openSSL:
        enabled: true
        required: true
      replicaCount: 3
      arm64: false
      podPolicy:
        nodeSelector:
          clickhouseGroup: "$SERVER_NG_GROUP"
        resources:
          limits:
            cpu: "$SERVER_CPU"
            memory: "$SERVER_MEMORY"
          requests:
            cpu: "$SERVER_CPU"
            memory: "$SERVER_MEMORY"
      tolerations:
        - effect: NoSchedule
          key: "clickhouse.com/do-not-schedule"
          operator: Exists
      storage:
        storageClassName: local-nvme-sc
        resources:
          requests: "300Gi"
        s3:
          bucketName: "$S3_BUCKET_NAME"
          keyPrefix: "$S3_KEY_PREFIX"
          endpoint: "$S3_ENDPOINT"
          region: "$S3_REGION"
      image:
        repository: "$ECR_HOST/clickhouse-server"
        tag: "25.12.1.1497"
      additionalEnvVars:
        - name: AWS_ACCESS_KEY_ID
          value: "${S3_ACCESS_KEY_ID}"
        - name: AWS_SECRET_ACCESS_KEY
          value: "${S3_SECRET_ACCESS_KEY}"

    keeper:
      openSSL:
        enabled: true
        required: true
      image:
        repository: "$ECR_HOST/clickhouse-keeper"
        tag: "26.2.1.258"
      arm64: false
      podPolicy:
        nodeSelector:
          clickhouseGroup: "$KEEPER_NG_GROUP"
        resources:
          limits:
            cpu: "$KEEPER_CPU"
            memory: "$KEEPER_MEMORY"
          requests:
            cpu: "$KEEPER_CPU"
            memory: "$KEEPER_MEMORY"
      tolerations:
        - effect: NoSchedule
          key: "clickhouse.com/do-not-schedule"
          operator: Exists
      storage:
        storageClassName: local-nvme-sc
        resources:
          requests: 30Gi

    serviceAccount:
      create: false
      name: default

    storageClass:
      create: false
      name: local-nvme-sc
      provisioner: rancher.io/local-path
    ```

    Установите кластер с помощью файла значений:

    ```text
    # Create namespace
    kubectl create namespace ns-$CLUSTER_NAME

    # Create image pull secret in the cluster namespace
    kubectl create secret docker-registry all-icr-io \
      --docker-server=$ECR_HOST \
      --docker-username=AWS \
      --docker-password=$(aws ecr get-login-password) \
      -n ns-$CLUSTER_NAME

    # Expand variables and install
    TEMP_VALUES_FILE=$(mktemp)
    envsubst < clickhouse-values.yaml > "$TEMP_VALUES_FILE"

    helm upgrade "$CLUSTER_NAME" \
      oci://$ECR_HOST/helm/onprem-clickhouse-cluster \
      --install --reuse-values \
      --version="$CHART_VERSION" \
      -n ns-$CLUSTER_NAME \
      --create-namespace \
      --values "$TEMP_VALUES_FILE" \
      --timeout 10m0s

    rm "$TEMP_VALUES_FILE"
    ```

    ### Исправления после установки

    После развёртывания кластера может потребоваться обновить StatefulSet Keeper, добавив секреты для загрузки образов и учётные данные S3:

    ```text
    # Copy pull secret to cluster namespace
    kubectl get secret all-icr-io -n default -o yaml | \
      sed "s/namespace: default/namespace: ns-$CLUSTER_NAME/" | \
      kubectl apply -n ns-$CLUSTER_NAME --force -f -

    # Patch keeper StatefulSet (use sts_keeper_patch_template.yaml from example scripts)
    kubectl patch sts c-$CLUSTER_NAME-keeper -n ns-$CLUSTER_NAME \
      --patch "$(envsubst < sts_keeper_patch_template.yaml)"

    # Restart keeper pods
    kubectl delete pod c-$CLUSTER_NAME-keeper-{0,1,2} -n ns-$CLUSTER_NAME
    ```

    Дождитесь повторного развёртывания подов Keeper, затем при необходимости исправьте PV сервера:

    ```text
    ./sts_server_patch.sh "ns-$CLUSTER_NAME" all-icr-io
    kubectl delete pod -l kind=clickhouse-server
    ```

    ***
  </Step>
</Steps>

<div id="verify-installation">
  ## Проверьте установку
</div>

Когда все поды перейдут в состояние Running, выполните проброс порта и проверьте установку. Поскольку в этом развертывании используется TLS (openSSL включен), используйте порт 9440:

```bash
kubectl port-forward svc/c-$CLUSTER_NAME-server-any 9440:9440 -n ns-$CLUSTER_NAME
```

Подключитесь через клиент ClickHouse:

```bash
clickhouse client --host localhost --port 9440 --password "$PASSWORD" --secure --accept-invalid-certificate
```

Выполните тестовый запрос:

```sql
SELECT 1;
```

Ожидаемый результат:

```text
   ┌─1─┐
1. │ 1 │
   └───┘
1 row in set. Elapsed: 0.001 sec.
```