---
description: 'Системная таблица с информацией о настройках таблиц MergeTree.'
keywords: ['системная таблица', 'merge_tree_settings']
slug: /operations/system-tables/merge_tree_settings
title: 'system.merge_tree_settings'
doc_type: 'reference'
---

<div id="description">
  ## Описание
</div>

Содержит информацию о настройках таблиц `MergeTree`.

<div id="columns">
  ## Столбцы
</div>

{/*AUTOGENERATED_START*/}

* `name` ([String](/ru/reference/data-types/index)) — Имя настройки.
* `value` ([String](/ru/reference/data-types/index)) — Значение настройки.
* `default` ([String](/ru/reference/data-types/index)) — Значение настройки по умолчанию.
* `changed` ([UInt8](/ru/reference/data-types/index)) — 1, если настройка была явно задана в конфигурации или явно изменена.
* `description` ([String](/ru/reference/data-types/index)) — Описание настройки.
* `min` ([Nullable(String)](/ru/reference/data-types/index)) — Минимальное значение настройки, если оно задано ограничениями. Если у настройки нет минимального значения, содержит NULL.
* `max` ([Nullable(String)](/ru/reference/data-types/index)) — Максимальное значение настройки, если оно задано ограничениями. Если у настройки нет максимального значения, содержит NULL.
* `disallowed_values` ([Array(String)](/ru/reference/data-types/index)) — Список недопустимых значений
* `readonly` ([UInt8](/ru/reference/data-types/index)) — Показывает, может ли текущий пользователь изменить настройку: 0 — текущий пользователь может изменить настройку, 1 — текущий пользователь не может изменить настройку.
* `type` ([String](/ru/reference/data-types/index)) — Тип настройки (строковое значение, зависящее от реализации).
* `is_obsolete` ([UInt8](/ru/reference/data-types/index)) — Показывает, является ли настройка устаревшей.
* `tier` ([Enum8(&#39;Production&#39; = 0, &#39;Obsolete&#39; = 4, &#39;Experimental&#39; = 8, &#39;Beta&#39; = 12)](/ru/reference/data-types/index)) —
  Уровень поддержки этой возможности. Возможности ClickHouse организованы по уровням, которые различаются в зависимости от текущего статуса их
  разработки и ожиданий от их использования:

- PRODUCTION: Возможность стабильна, безопасна в использовании и не вызывает проблем при взаимодействии с другими возможностями уровня PRODUCTION.
- BETA: Возможность стабильна и безопасна. Результат ее использования вместе с другими возможностями неизвестен, и корректность не гарантируется. Тестирование и отчеты приветствуются.
- EXPERIMENTAL: Возможность находится в разработке. Предназначена только для разработчиков и энтузиастов ClickHouse. Возможность может как работать, так и не работать, и может быть удалена в любой момент.
- OBSOLETE: Больше не поддерживается. Либо уже удалена, либо будет удалена в будущих релизах.

{/*AUTOGENERATED_END*/}

<div id="example">
  ## Пример
</div>

```sql
SELECT * FROM system.merge_tree_settings LIMIT 3 FORMAT Vertical;
```

```response
SELECT *
FROM system.merge_tree_settings
LIMIT 3
FORMAT Vertical

Query id: 2580779c-776e-465f-a90c-4b7630d0bb70

Row 1:
──────
name:        min_compress_block_size
value:       0
default:     0
changed:     0
description: When granule is written, compress the data in buffer if the size of pending uncompressed data is larger or equal than the specified threshold. If this setting is not set, the corresponding global setting is used.
min:         ᴺᵁᴸᴸ
max:         ᴺᵁᴸᴸ
readonly:    0
type:        UInt64
is_obsolete: 0
tier:        Production

Row 2:
──────
name:        max_compress_block_size
value:       0
default:     0
changed:     0
description: Compress the pending uncompressed data in buffer if its size is larger or equal than the specified threshold. Block of data will be compressed even if the current granule is not finished. If this setting is not set, the corresponding global setting is used.
min:         ᴺᵁᴸᴸ
max:         ᴺᵁᴸᴸ
readonly:    0
type:        UInt64
is_obsolete: 0
tier:        Production

Row 3:
──────
name:        index_granularity
value:       8192
default:     8192
changed:     0
description: How many rows correspond to one primary key value.
min:         ᴺᵁᴸᴸ
max:         ᴺᵁᴸᴸ
readonly:    0
type:        UInt64
is_obsolete: 0
tier:        Production

3 rows in set. Elapsed: 0.001 sec. 
```