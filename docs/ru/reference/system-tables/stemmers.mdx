---
description: 'Системная таблица, показывающая все доступные стеммеры.'
keywords: ['системная таблица', 'стеммеры']
sidebarTitle: 'system.stemmers'
slug: /operations/system-tables/stemmers
title: 'system.stemmers'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

## Описание {#description}

Показывает все доступные стеммеры.
Их можно использовать в функции [stem](/ru/reference/functions/regular-functions/nlp-functions).

**Доступность**

`system.stemmers` доступна только в сборках ClickHouse, скомпилированных с зависимостью `libstemmer` (`USE_LIBSTEMMER`). В сборках без нее эта таблица отсутствует, и запросы к ней завершаются ошибкой `UNKNOWN_TABLE`. Проверить, включена ли эта зависимость в вашей сборке, можно так:

```sql
SELECT value FROM system.build_options WHERE name = 'USE_LIBSTEMMER';
```

## Столбцы {#columns}

* `name` ([String](/ru/reference/data-types/string)) — идентификатор Snowball-стеммера (язык/алгоритм)

## Пример {#example}

```sql
SELECT * FROM system.stemmers;
```

```text
 ┌─name───────┐
 │ arabic     │
 │ armenian   │
 │ basque     │
 │ catalan    │
 │ danish     │
 │ dutch      │
 │ english    │
 │ [...]      │
 └────────────┘
```

{/*AUTOGENERATED_END*/}