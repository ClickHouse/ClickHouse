---
description: 'Системная таблица, содержащая исторические значения `system.asynchronous_metrics`,
  которые сохраняются через определённые интервалы времени (по умолчанию — раз в секунду)'
keywords: ['системная таблица', 'asynchronous_metric_log']
slug: /operations/system-tables/asynchronous_metric_log
title: 'system.asynchronous_metric_log'
doc_type: 'reference'
---

import SystemTableCloud from '/snippets/ru/_system_table_cloud.mdx';

<SystemTableCloud />

<div id="description">
  ## Описание
</div>

Содержит исторические значения `system.asynchronous_metrics`, сохраняемые один раз за интервал времени (по умолчанию — раз в секунду). Включено по умолчанию.

Метрики типа «ключ-значение» в `system.asynchronous_metrics` (с разбивкой по ядрам CPU, блочным устройствам, сетевым интерфейсам или дискам) записываются как одна строка для каждого ключа, при этом ключ хранится в столбце `key`. Для скалярных метрик столбец `key` пуст.

<div id="columns">
  ## Столбцы
</div>

{/*AUTOGENERATED_START*/}

* `hostname` ([LowCardinality(String)](/ru/reference/data-types/lowcardinality)) — Имя хоста сервера, выполняющего запрос.
* `event_date` ([Date](/ru/reference/data-types/date)) — Дата события.
* `event_time` ([DateTime](/ru/reference/data-types/datetime)) — Время события.
* `metric` ([LowCardinality(String)](/ru/reference/data-types/lowcardinality)) — Название метрики.
* `key` ([LowCardinality(String)](/ru/reference/data-types/lowcardinality)) — Ключ метрики типа ключ-значение, например номер ядра CPU или имя блочного устройства. Пусто для скалярных метрик.
* `value` ([Float64](/ru/reference/data-types/float)) — Значение метрики.

{/*AUTOGENERATED_END*/}

<div id="example">
  ## Пример
</div>

```sql
SELECT * FROM system.asynchronous_metric_log LIMIT 3 \G
```

```text
Row 1:
──────
hostname:   clickhouse.eu-central1.internal
event_date: 2023-11-14
event_time: 2023-11-14 14:39:07
metric:     AsynchronousHeavyMetricsCalculationTimeSpent
key:
value:      0.001

Row 2:
──────
hostname:   clickhouse.eu-central1.internal
event_date: 2023-11-14
event_time: 2023-11-14 14:39:08
metric:     AsynchronousHeavyMetricsCalculationTimeSpent
key:
value:      0

Row 3:
──────
hostname:   clickhouse.eu-central1.internal
event_date: 2023-11-14
event_time: 2023-11-14 14:39:09
metric:     AsynchronousHeavyMetricsCalculationTimeSpent
key:
value:      0
```

**См. также**

* [настройка asynchronous&#95;metric&#95;log](/ru/reference/settings/server-settings/settings/asynchronous#asynchronous_metric_log) — Включение и отключение настройки.
* [system.asynchronous&#95;metrics](/ru/reference/system-tables/asynchronous_metrics) — Содержит метрики, периодически вычисляемые в фоновом режиме.
* [system.metric&#95;log](/ru/reference/system-tables/metric_log) — Содержит историю значений метрик из таблиц `system.metrics` и `system.events`, которая периодически сбрасывается на диск.