---
description: 'Системная таблица со сведениями о задачах в фоновых пулах планировщика.'
keywords: ['системная таблица', 'background_schedule_pool']
slug: /operations/system-tables/background_schedule_pool
title: 'system.background_schedule_pool'
doc_type: 'reference'
---

import SystemTableCloud from '/snippets/ru/_system_table_cloud.mdx';

<SystemTableCloud />

<div id="description">
  ## Описание
</div>

Содержит сведения о задачах в фоновых пулах планировщика. Фоновые пулы планировщика используются для выполнения периодических задач, таких как распределённая отправка данных, сброс буферов, операции брокера сообщений, фоновые задания потоковых запросов и обновление метаданных таблицы Iceberg.

<div id="columns">
  ## Столбцы
</div>

{/*AUTOGENERATED_START*/}

* `pool` ([String](/ru/reference/data-types/index)) — Имя пула. Возможные значения: `schedule` (пул расписания общего назначения), `buffer_flush` (пул для сброса данных таблицы Buffer), `distributed` (пул для операций distributed таблицы), `message_broker` (пул для операций брокера сообщений), `streaming` (пул для фоновых задач потоковых запросов), `iceberg` (пул для обновления метаданных таблицы Iceberg).
* `database` ([String](/ru/reference/data-types/index)) — Имя базы данных.
* `table` ([String](/ru/reference/data-types/index)) — Имя таблицы.
* `table_uuid` ([UUID](/ru/reference/data-types/index)) — UUID таблицы.
* `query_id` ([String](/ru/reference/data-types/index)) — Query ID (если выполняется в данный момент). Примечание: это не настоящий запрос, а случайно сгенерированный ID для сопоставления с записями журнала в `system.text_log`.
* `elapsed_ms` ([UInt64](/ru/reference/data-types/index)) — Время выполнения задачи (если выполняется в данный момент).
* `log_name` ([String](/ru/reference/data-types/index)) — Имя журнала для задачи.
* `deactivated` ([UInt8](/ru/reference/data-types/index)) — Деактивирована ли задача (всегда false, так как деактивированные задачи удаляются из пула).
* `scheduled` ([UInt8](/ru/reference/data-types/index)) — Запланирована ли задача к выполнению.
* `delayed` ([UInt8](/ru/reference/data-types/index)) — Запланирована ли задача с задержкой.
* `executing` ([UInt8](/ru/reference/data-types/index)) — Выполняется ли задача в данный момент.

{/*AUTOGENERATED_END*/}

<div id="example">
  ## Пример
</div>

```sql title="Query"
SELECT * FROM system.background_schedule_pool LIMIT 5 FORMAT Vertical;
```

```text title="Response"
Row 1:
──────
pool:        distributed
database:    default
table:       data
table_uuid:  00000000-0000-0000-0000-000000000000
query_id:
elapsed_ms:  0
log_name:    BackgroundJobsAssignee:DataProcessing
deactivated: 0
scheduled:   1
delayed:     0
executing:   0
```

<div id="see-also">
  ## См. также
</div>

* [system.background&#95;schedule&#95;pool&#95;log](/ru/reference/system-tables/background_schedule_pool_log) — Содержит историю выполнения задач фонового пула планировщика.