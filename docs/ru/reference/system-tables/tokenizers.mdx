---
description: 'Системная таблица, в которой отображаются все доступные токенизаторы.'
keywords: ['системная таблица', 'токенизаторы']
slug: /operations/system-tables/tokenizers
title: 'system.tokenizers'
doc_type: 'reference'
---

import SystemTableCloud from '/snippets/ru/_system_table_cloud.mdx';

<SystemTableCloud />

{/*AUTOGENERATED_START*/}

## Описание {#description}

Показывает все доступные токенизаторы.
Их можно использовать в функциях [tokens](/ru/reference/functions/regular-functions/splitting-merging-functions#tokens), [hasAllTokens](/ru/reference/functions/regular-functions/string-search-functions#hasAllTokens), [hasAnyTokens](/ru/reference/functions/regular-functions/string-search-functions#hasAnyTokens), а также в [текстовом индексе](/ru/reference/engines/table-engines/mergetree-family/textindexes).

## Столбцы {#columns}

* `name` ([String](/ru/reference/data-types/string)) — Имя токенизатора

## Пример {#example}

```sql
SELECT * FROM system.tokenizers;
```

```text
┌─name────────────┐
│ ngrams          │
│ splitByNonAlpha │
│ sparseGrams     │
│ tokenbf_v1      │
│ ngrambf_v1      │
│ array           │
│ splitByString   │
│ sparse_grams    │
│ asciiCJK        │
│ icu             │
│ japanese        │
└─────────────────┘
```

{/*AUTOGENERATED_END*/}