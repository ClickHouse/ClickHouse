---
title: 'настройки формата type_json_*'
sidebarTitle: 'type_json_*'
toc_max_heading_level: 2
description: 'Настройки формата ClickHouse из сгенерированной группы type_json_*.'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/ru/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

Эти настройки автоматически сгенерированы на основе [исходного файла](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/FormatFactorySettings.h).

<div id="type_json_allow_duplicated_key_with_literal_and_nested_object">
  ## type_json_allow_duplicated_key_with_literal_and_nested_object
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

Если включено, допускается разбор JSON вида `{"a" : 42, "a" : {"b" : 42}}`, где некоторый ключ дублируется, но одно из его значений является вложенным объектом.

<div id="type_json_skip_duplicated_paths">
  ## type_json_skip_duplicated_paths
</div>

<SettingsInfoBlock type="Bool" default_value="0" />

Если параметр включен, при разборе объекта JSON в тип JSON дублирующиеся пути будут игнорироваться, и вместо исключения будет вставляться только первый из них

<div id="type_json_skip_invalid_typed_paths">
  ## type_json_skip_invalid_typed_paths
</div>

<SettingsInfoBlock type="Bool" default_value="0" />

Если параметр включен, поля со значениями, которые нельзя привести к объявленному для них типу в столбцах типа JSON с типизированными путями, пропускаются вместо того, чтобы вызывать ошибку. Пропущенные поля считаются отсутствующими и получают значения по умолчанию/NULL в соответствии с определением типизированного пути.

Этот параметр применяется только к столбцам типа JSON (например, JSON(a Int64, b String)), где для определённых путей объявлены типы. Он не применяется к обычным форматам ввода JSON, таким как JSONEachRow, при вставке в обычные типизированные столбцы.

Возможные значения:

* 0 — Отключить (вызывать ошибку при несоответствии типов).
* 1 — Включить (пропускать поле при несоответствии типов).

<div id="type_json_use_partial_match_to_skip_paths_by_regexp">
  ## type_json_use_partial_match_to_skip_paths_by_regexp
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

Если включено, при разборе объекта JSON в тип JSON для пропуска пути регулярным выражениям, указанным с помощью SKIP REGEXP, достаточно частичного совпадения. Если отключено, требуется полное совпадение.

{/*AUTOGENERATED_END*/}