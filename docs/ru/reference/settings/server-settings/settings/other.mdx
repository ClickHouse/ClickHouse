---
title: 'Другие настройки сервера'
sidebarTitle: 'Другое'
toc_max_heading_level: 2
description: 'Настройки сервера ClickHouse в автоматически созданной группе Other.'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SystemLogParameters from '/snippets/ru/_system-log-parameters.mdx';
import SettingsInfoBlock from "/snippets/ru/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

Эти настройки используются для настройки сервера ClickHouse и автоматически генерируются на основе исходного кода ClickHouse.

<div id="abort_on_logical_error">
  ## abort_on_logical_error
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="Yes" />Приводит к аварийному завершению работы сервера при исключениях LOGICAL&#95;ERROR. Только для опытных пользователей.

<div id="allowed_disks_for_table_engines">
  ## allowed_disks_for_table_engines
</div>

Список дисков, разрешённых для использования с Iceberg

<div id="auth_use_forwarded_address">
  ## auth_use_forwarded_address
</div>

Использовать исходный адрес для аутентификации клиентов, подключающихся через прокси.

<Note>
  Этот параметр следует использовать с особой осторожностью, поскольку forwarded-адреса легко подделать: к серверам, принимающим такую аутентификацию, не следует обращаться напрямую — только через доверенный прокси.
</Note>

<div id="bcrypt_workfactor">
  ## bcrypt_workfactor
</div>

Фактор сложности для типа аутентификации `bcrypt_password`, использующего [алгоритм Bcrypt](https://wildlyinaccurate.com/bcrypt-choosing-a-work-factor/).
Фактор сложности определяет объём вычислений и время, необходимые для вычисления хэша и проверки пароля.

```xml
<bcrypt_workfactor>12</bcrypt_workfactor>
```

<Warning>
  Для приложений с частой аутентификацией
  рассмотрите альтернативные методы аутентификации из-за
  вычислительной нагрузки bcrypt при более высоких значениях work factor.
</Warning>

<div id="blob_storage_log">
  ## blob_storage_log
</div>

Настройки для системной таблицы [`blob_storage_log`](/ru/reference/system-tables/blob_storage_log).

<SystemLogParameters />

Пример:

```xml
<blob_storage_log>
    <database>system</database
    <table>blob_storage_log</table
    <partition_by>toYYYYMM(event_date)</partition_by>
    <flush_interval_milliseconds>7500</flush_interval_milliseconds
    <ttl>event_date + INTERVAL 30 DAY</ttl>
</blob_storage_log>
```

<div id="builtin_dictionaries_reload_interval">
  ## builtin_dictionaries_reload_interval
</div>

Интервал в секундах до перезагрузки встроенных словарей.

ClickHouse перезагружает встроенные словари каждые x секунд. Это позволяет редактировать словари &quot;на лету&quot; без перезапуска сервера.

**Пример**

```xml
<builtin_dictionaries_reload_interval>3600</builtin_dictionaries_reload_interval>
```

<div id="cache_size_to_ram_max_ratio">
  ## cache_size_to_ram_max_ratio
</div>

<SettingsInfoBlock type="Double" default_value="0.5" changeable_without_restart="No" />Устанавливает максимальное соотношение размера кэша к объёму оперативной памяти. Позволяет уменьшить размер кэша на системах с небольшим объёмом памяти.

<div id="cannot_allocate_thread_fault_injection_probability">
  ## cannot_allocate_thread_fault_injection_probability
</div>

<SettingsInfoBlock type="Double" default_value="0" changeable_without_restart="No" />Для тестирования.

<div id="cgroups_memory_usage_observer_wait_time">
  ## cgroups_memory_usage_observer_wait_time
</div>

<SettingsInfoBlock type="UInt64" default_value="15" changeable_without_restart="No" />

Интервал в секундах, в течение которого максимально допустимое потребление памяти сервером корректируется в cgroups на соответствующий порог.

Чтобы отключить наблюдатель cgroups, установите это значение равным `0`.

<div id="compression">
  ## сжатие
</div>

Настройки сжатия данных для таблиц с движком [MergeTree](/ru/reference/engines/table-engines/mergetree-family/mergetree).

<Note>
  Если вы только начали использовать ClickHouse, рекомендуем не менять этот параметр.
</Note>

**Шаблон конфигурации**:

```xml
<compression>
    <case>
      <min_part_size>...</min_part_size>
      <min_part_size_ratio>...</min_part_size_ratio>
      <method>...</method>
      <level>...</level>
    </case>
    ...
</compression>
```

**Поля `<case>`**:

* `min_part_size` – Минимальный размер части данных.
* `min_part_size_ratio` – Отношение размера части данных к размеру таблицы.
* `method` – Метод сжатия. Допустимые значения: `lz4`, `lz4hc`, `zstd`,`deflate_qpl`.
* `level` – Уровень сжатия. См. [Кодеки](/ru/reference/statements/create/table#general-purpose-codecs).

<Note>
  Можно настроить несколько разделов `<case>`.
</Note>

**Действия при выполнении условий**:

* Если часть данных соответствует набору условий, ClickHouse использует указанный метод сжатия.
* Если часть данных соответствует нескольким наборам условий, ClickHouse использует первый из них.

<Note>
  Если для части данных не выполняется ни одно условие, ClickHouse использует сжатие `lz4`.
</Note>

**Пример**

```xml
<compression incl="clickhouse_compression">
    <case>
        <min_part_size>10000000000</min_part_size>
        <min_part_size_ratio>0.01</min_part_size_ratio>
        <method>zstd</method>
        <level>1</level>
    </case>
</compression>
```

<div id="config_reload_interval_ms">
  ## config_reload_interval_ms
</div>

<SettingsInfoBlock type="UInt64" default_value="2000" changeable_without_restart="Yes" />

Как часто ClickHouse будет перезагружать конфигурацию и проверять наличие изменений

<div id="config-file">
  ## файл конфигурации
</div>

<SettingsInfoBlock type="String" default_value="config.xml" changeable_without_restart="No" />Указывает на файл конфигурации сервера.

<div id="core_dump">
  ## core_dump
</div>

Настраивает мягкое ограничение размера файла дампа памяти.

<Note>
  Жесткое ограничение задается с помощью системных инструментов
</Note>

**Пример**

```xml
<core_dump>
     <size_limit>1073741824</size_limit>
</core_dump>
```

<div id="crash_log">
  ## crash_log
</div>

Настройки работы системной таблицы [crash&#95;log](/ru/reference/system-tables/crash_log).

Следующие настройки можно задать с помощью под-тегов:

| Setting                            | Description                                                                                                                                                               | Default             | Note                                                                                                                           |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| `buffer_size_rows_flush_threshold` | Порог по количеству строк. При достижении этого порога сброс журналов на диск запускается в фоновом режиме.                                                               | `max_size_rows / 2` |                                                                                                                                |
| `database`                         | Имя базы данных.                                                                                                                                                          |                     |                                                                                                                                |
| `engine`                           | [Определение движка MergeTree](/ru/reference/engines/table-engines/mergetree-family/mergetree#table_engine-mergetree-creating-a-table) для системной таблицы.                |                     | Нельзя использовать, если заданы `partition_by` или `order_by`. Если параметр не указан, по умолчанию используется `MergeTree` |
| `flush_interval_milliseconds`      | Интервал сброса данных из буфера в памяти в таблицу.                                                                                                                      | `7500`              |                                                                                                                                |
| `flush_on_crash`                   | Определяет, нужно ли сбрасывать журналы на диск в случае аварийного завершения.                                                                                           | `false`             |                                                                                                                                |
| `max_size_rows`                    | Максимальный размер журналов в строках. Когда количество несброшенных журналов достигает max&#95;size, журналы сбрасываются на диск.                                      | `1024`              |                                                                                                                                |
| `order_by`                         | [Пользовательский ключ сортировки](/ru/reference/engines/table-engines/mergetree-family/mergetree#order_by) для системной таблицы. Нельзя использовать, если задан `engine`. |                     | Если для системной таблицы указан `engine`, параметр `order_by` следует задавать непосредственно внутри &#39;engine&#39;       |
| `partition_by`                     | [Пользовательский ключ партиционирования](/ru/reference/engines/table-engines/mergetree-family/custom-partitioning-key) для системной таблицы.                               |                     | Если для системной таблицы указан `engine`, параметр `partition_by` следует задавать непосредственно внутри &#39;engine&#39;   |
| `reserved_size_rows`               | Объём предварительно выделенной памяти для журналов в строках.                                                                                                            | `1024`              |                                                                                                                                |
| `settings`                         | [Дополнительные параметры](/ru/reference/engines/table-engines/mergetree-family/mergetree#settings), управляющие поведением MergeTree (необязательно).                       |                     | Если для системной таблицы указан `engine`, параметр `settings` следует задавать непосредственно внутри &#39;engine&#39;       |
| `storage_policy`                   | Имя политики хранения, которую следует использовать для таблицы (необязательно).                                                                                          |                     | Если для системной таблицы указан `engine`, параметр `storage_policy` следует задавать непосредственно внутри &#39;engine&#39; |
| `table`                            | Имя системной таблицы.                                                                                                                                                    |                     |                                                                                                                                |
| `ttl`                              | Задаёт [TTL](/ru/reference/engines/table-engines/mergetree-family/mergetree#table_engine-mergetree-ttl) таблицы.                                                             |                     | Если для системной таблицы указан `engine`, параметр `ttl` следует задавать непосредственно внутри &#39;engine&#39;            |

Файл конфигурации сервера `config.xml`, используемый по умолчанию, содержит следующий раздел настроек:

```xml
<crash_log>
    <database>system</database>
    <table>crash_log</table>
    <partition_by>toYYYYMM(event_date)</partition_by>
    <flush_interval_milliseconds>7500</flush_interval_milliseconds>
    <max_size_rows>1024</max_size_rows>
    <reserved_size_rows>1024</reserved_size_rows>
    <buffer_size_rows_flush_threshold>512</buffer_size_rows_flush_threshold>
    <flush_on_crash>false</flush_on_crash>
</crash_log>
```

<div id="database_atomic_delay_before_drop_table_sec">
  ## database_atomic_delay_before_drop_table_sec
</div>

<SettingsInfoBlock type="UInt64" default_value="480" changeable_without_restart="No" />

Задержка, в течение которой удалённую таблицу можно восстановить с помощью оператора [`UNDROP`](/ru/reference/statements/undrop). Если команда `DROP TABLE` была выполнена с модификатором `SYNC`, этот параметр игнорируется.
Значение этого параметра по умолчанию — `480` (8 минут).

<div id="dead_letter_queue">
  ## dead_letter_queue
</div>

Настройка системной таблицы &#39;dead&#95;letter&#95;queue&#39;.

<SystemLogParameters />

Настройки по умолчанию:

```xml
<dead_letter_queue>
    <database>system</database>
    <table>dead_letter</table>
    <partition_by>toYYYYMM(event_date)</partition_by>
    <flush_interval_milliseconds>7500</flush_interval_milliseconds>
</dead_letter_queue>
```

<div id="dictionary_background_reconnect_interval">
  ## dictionary_background_reconnect_interval
</div>

<SettingsInfoBlock type="UInt64" default_value="1000" changeable_without_restart="No" />Интервал в миллисекундах между попытками повторного подключения для словарей MySQL и Postgres, завершившихся с ошибкой, у которых включен `background_reconnect`.

<div id="disk_transaction_wait_for_blob_removal">
  ## disk_transaction_wait_for_blob_removal
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />

Значение по умолчанию для настройки `wait_for_blob_removal` для каждого диска.
Если настройка включена, сервер ожидает завершения фонового удаления blob, прежде чем подтвердить операцию.

<div id="display_secrets_in_show_and_select">
  ## display_secrets_in_show_and_select
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />

Включает или отключает отображение секретов в запросах `SHOW` и `SELECT` для таблиц, баз данных, табличных функций и словарей.

Пользователь, который хочет видеть секреты, также должен иметь
включенную
[`format_display_secrets_in_show_and_select` настройку формата](/ru/reference/settings/formats#format_display_secrets_in_show_and_select)
и
[`displaySecretsInShowAndSelect`](/ru/reference/statements/grant#displaysecretsinshowandselect) привилегию.

Возможные значения:

* `0` — Отключено.
* `1` — Включено.

<div id="dns_max_consecutive_failures">
  ## dns_max_consecutive_failures
</div>

<SettingsInfoBlock type="UInt32" default_value="5" changeable_without_restart="No" />

Прекращает дальнейшие попытки обновления DNS-кэша хоста после указанного числа последовательных сбоев. Информация при этом сохраняется в DNS-кэше. Ноль означает отсутствие ограничений.

**См. также**

* [`SYSTEM DROP DNS CACHE`](/ru/reference/statements/system#drop-dns-cache)

<div id="dynamic_user_defined_executable_functions_path">
  ## dynamic_user_defined_executable_functions_path
</div>

<SettingsInfoBlock type="String" default_value="/var/lib/clickhouse/dynamic_user_defined_executable_functions/" changeable_without_restart="No" />

Каталог, в котором хранятся файлы конфигурации исполняемых UDF, динамически создаваемых драйверами (см. `CREATE FUNCTION ... ENGINE = DriverName(...)`).
При перезапуске сервера каталог сканируется на наличие файлов конфигурации, и соответствующие UDF загружаются без повторного вызова драйвера.

**Пример**

```xml
<dynamic_user_defined_executable_functions_path>/var/lib/clickhouse/dynamic_user_defined_executable_functions/</dynamic_user_defined_executable_functions_path>
```

<div id="encryption">
  ## шифрование
</div>

Задает команду для получения ключа, который будет использоваться [кодеками шифрования](/ru/reference/statements/create/table#encryption-codecs). Ключ (или ключи) должен храниться в переменных окружения или задаваться в конфигурационном файле.

Ключи могут быть представлены в шестнадцатеричном виде или в виде строки длиной 16 байт.

**Пример**

Загрузка из конфигурационного файла:

```xml
<encryption_codecs>
    <aes_128_gcm_siv>
        <key>1234567812345678</key>
    </aes_128_gcm_siv>
</encryption_codecs>
```

<Note>
  Хранить ключи в файле конфигурации не рекомендуется. Это небезопасно. Вы можете вынести ключи в отдельный файл конфигурации на защищённом диске и поместить в папку `config.d/` символическую ссылку на него.
</Note>

Загрузка из файла конфигурации, если ключ задан в hex:

```xml
<encryption_codecs>
    <aes_128_gcm_siv>
        <key_hex>00112233445566778899aabbccddeeff</key_hex>
    </aes_128_gcm_siv>
</encryption_codecs>
```

Загрузка ключа из переменной окружения:

```xml
<encryption_codecs>
    <aes_128_gcm_siv>
        <key_hex from_env="ENVVAR"></key_hex>
    </aes_128_gcm_siv>
</encryption_codecs>
```

Здесь `current_key_id` задаёт текущий ключ для шифрования, а все указанные ключи могут использоваться для расшифровки.

Каждый из этих методов можно применять для нескольких ключей:

```xml
<encryption_codecs>
    <aes_128_gcm_siv>
        <key_hex id="0">00112233445566778899aabbccddeeff</key_hex>
        <key_hex id="1" from_env="ENVVAR"></key_hex>
        <current_key_id>1</current_key_id>
    </aes_128_gcm_siv>
</encryption_codecs>
```

Здесь `current_key_id` обозначает текущий ключ шифрования.

Кроме того, пользователи могут добавить nonce длиной 12 байт (по умолчанию в процессах шифрования и расшифровки используется nonce, состоящий из нулевых байтов):

```xml
<encryption_codecs>
    <aes_128_gcm_siv>
        <nonce>012345678910</nonce>
    </aes_128_gcm_siv>
</encryption_codecs>
```

Или можно задать его в шестнадцатеричном виде:

```xml
<encryption_codecs>
    <aes_128_gcm_siv>
        <nonce_hex>abcdefabcdef</nonce_hex>
    </aes_128_gcm_siv>
</encryption_codecs>
```

<Note>
  Всё, что сказано выше, можно применить и к `aes_256_gcm_siv` (но ключ должен быть длиной 32 байта).
</Note>

<div id="enforce_keeper_component_tracking">
  ## enforce_keeper_component_tracking
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />

Если параметр включен, для каждого запроса к ZooKeeper должно быть задано имя компонента через `Coordination::setCurrentComponent`. Если компонент отсутствует, генерируется исключение `LOGICAL_ERROR`.

<div id="error_log">
  ## error_log
</div>

По умолчанию отключено.

**Включение**

Чтобы вручную включить сбор истории ошибок [`system.error_log`](/ru/reference/system-tables/error_log), создайте файл `/etc/clickhouse-server/config.d/error_log.xml` со следующим содержимым:

```xml
<clickhouse>
    <error_log>
        <database>system</database>
        <table>error_log</table>
        <flush_interval_milliseconds>7500</flush_interval_milliseconds>
        <collect_interval_milliseconds>1000</collect_interval_milliseconds>
        <max_size_rows>1048576</max_size_rows>
        <reserved_size_rows>8192</reserved_size_rows>
        <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
        <flush_on_crash>false</flush_on_crash>
    </error_log>
</clickhouse>
```

**Отключение**

Чтобы отключить параметр `error_log`, создайте файл `/etc/clickhouse-server/config.d/disable_error_log.xml` со следующим содержимым:

```xml
<clickhouse>
    <error_log remove="1" />
</clickhouse>
```

<SystemLogParameters />

<div id="filesystem_caches_path">
  ## filesystem_caches_path
</div>

Этот параметр указывает путь к кэшу.

**Пример**

```xml
<filesystem_caches_path>/var/lib/clickhouse/filesystem_caches/</filesystem_caches_path>
```

<div id="google_protos_path">
  ## google_protos_path
</div>

<SettingsInfoBlock type="String" default_value="/usr/share/clickhouse/protos/" changeable_without_restart="No" />

Задает каталог с proto-файлами для типов Protobuf.

**Пример**

```xml
<google_protos_path>/usr/share/clickhouse/protos/</google_protos_path>
```

<div id="handshake_timeout_milliseconds">
  ## handshake_timeout_milliseconds
</div>

<SettingsInfoBlock type="UInt64" default_value="30000" changeable_without_restart="No" />Тайм-аут по реальному времени в миллисекундах для всей фазы TCP-рукопожатия (Hello + Addendum). Ограничивает время, в течение которого неаутентифицированное соединение может занимать поток. Установите значение 0, чтобы отключить.

<div id="hdfs.libhdfs3_conf">
  ## hdfs.libhdfs3_conf
</div>

Указывает libhdfs3 правильное расположение его конфигурации.

<div id="hsts_max_age">
  ## hsts_max_age
</div>

Время действия HSTS в секундах.

<Note>
  Значение `0` означает, что ClickHouse отключает HSTS. Если задать положительное число, HSTS будет включен, а значение max-age будет равно указанному числу.
</Note>

**Пример**

```xml
<hsts_max_age>600000</hsts_max_age>
```

<div id="iceberg_background_schedule_pool_size">
  ## iceberg_background_schedule_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="10" changeable_without_restart="No" />Размер пула потоков для асинхронного получения актуальных метаданных из удалённого каталога Iceberg; этот пул используется совместно всеми активными таблицами.

<div id="ignore_empty_sql_security_in_create_view_query">
  ## ignore_empty_sql_security_in_create_view_query
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />

Если значение — true, ClickHouse не записывает значения по умолчанию для пустого оператора безопасность SQL в запросах `CREATE VIEW`.

<Note>
  Этот параметр нужен только в период миграции и станет устаревшим в 24.4
</Note>

<div id="include_from">
  ## include_from
</div>

<SettingsInfoBlock type="String" default_value="/etc/metrika.xml" changeable_without_restart="No" />

Путь к файлу, содержащему подстановки. Поддерживаются форматы XML и YAML.

Дополнительные сведения см. в разделе [Файлы конфигурации](/ru/concepts/features/configuration/server-config/configuration-files).

**Пример**

```xml
<include_from>/etc/metrica.xml</include_from>
```

<div id="insert_deduplication_version">
  ## insert_deduplication_version
</div>

<SettingsInfoBlock type="String" default_value="new_unified_hash" changeable_without_restart="No" />Устаревший защитный механизм миграции. Эта версия поддерживает только единый хеш дедупликации вставки (`new_unified_hash`); сервер откажется запускаться, если этот параметр указан с любым другим значением (например, `old_separate_hashes` или `compatible_double_hashes`). Перед обновлением завершите миграцию дедупликации в предыдущей версии, запустив `compatible_double_hashes` (он записывает и устаревшие, и унифицированные хеши). Для реплицированных таблиц запускайте его как минимум в течение `replicated_deduplication_window_seconds` (по умолчанию один час); окна по умолчанию хранят унифицированные хеши всех вставок за этот период, и этого считается достаточно, чтобы покрыть цикл повторных попыток вставки. Для нереплицируемых таблиц с `non_replicated_deduplication_window` &gt; 0 окно определяется количеством, а не временем, поэтому перед обновлением запускайте `compatible_double_hashes` как минимум для такого числа вставок.

<div id="io_thread_pool_queue_size">
  ## io_thread_pool_queue_size
</div>

<SettingsInfoBlock type="UInt64" default_value="10000" changeable_without_restart="Yes" />

Максимальное количество задач, которые можно поставить в очередь пула потоков IO.

<Note>
  Значение `0` означает отсутствие ограничений.
</Note>

<div id="keep_alive_timeout">
  ## keep_alive_timeout
</div>

<SettingsInfoBlock type="Секунды" default_value="30" changeable_without_restart="No" />

Количество секунд, в течение которых ClickHouse ожидает входящих запросов по HTTP-протоколу, прежде чем закрыть соединение.

**Пример**

```xml
<keep_alive_timeout>30</keep_alive_timeout>
```

<div id="ldap_servers">
  ## ldap_servers
</div>

Здесь перечисляются LDAP-серверы с параметрами подключения, чтобы:

* использовать их как аутентификаторы для выделенных локальных пользователей, у которых вместо `'password'` указан механизм аутентификации `'ldap'`
* использовать их как удалённые каталоги пользователей.

Следующие настройки можно задать с помощью под-тегов:

| Настройка                      | Описание                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `bind_dn`                      | Шаблон, используемый для формирования DN, с которым выполняется bind. Итоговый DN формируется заменой всех подстрок `\{user_name\}` в шаблоне на фактическое имя пользователя при каждой попытке аутентификации.                                                                                                                                                                                                                                                |
| `enable_tls`                   | Флаг, включающий использование защищённого соединения с LDAP-сервером. Укажите `no` для протокола в открытом виде (`ldap://`) (не рекомендуется). Укажите `yes` для протокола LDAP поверх SSL/TLS (`ldaps://`) (рекомендуется, значение по умолчанию). Укажите `starttls` для устаревшего протокола StartTLS (протокол в открытом виде (`ldap://`), повышаемый до TLS).                                                                                         |
| `host`                         | Имя хоста или IP-адрес LDAP-сервера; этот параметр обязателен и не может быть пустым.                                                                                                                                                                                                                                                                                                                                                                           |
| `port`                         | Порт LDAP-сервера; по умолчанию `636`, если `enable_tls` установлено в true, иначе `389`.                                                                                                                                                                                                                                                                                                                                                                       |
| `tls_ca_cert_dir`              | путь к каталогу, содержащему CA‑сертификаты.                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `tls_ca_cert_file`             | путь к файлу CA‑сертификата.                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `tls_cert_file`                | путь к файлу сертификата.                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `tls_cipher_suite`             | разрешённый набор шифров (в нотации OpenSSL).                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `tls_key_file`                 | путь к файлу ключа сертификата.                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `tls_minimum_protocol_version` | Минимальная версия протокола SSL/TLS. Допустимые значения: `ssl2`, `ssl3`, `tls1.0`, `tls1.1`, `tls1.2` (по умолчанию).                                                                                                                                                                                                                                                                                                                                         |
| `tls_require_cert`             | Режим проверки сертификата узла SSL/TLS. Допустимые значения: `never`, `allow`, `try`, `demand` (по умолчанию).                                                                                                                                                                                                                                                                                                                                                 |
| `user_dn_detection`            | Раздел с параметрами LDAP search для определения фактического user DN пользователя, выполнившего bind. В основном используется в search filter для дальнейшего сопоставления ролей, когда сервером является Active Directory. Полученный user DN будет использоваться при замене подстрок `\{user_dn\}` везде, где это допускается. По умолчанию user DN равен bind DN, но после выполнения поиска он будет обновлён фактически обнаруженным значением user DN. |
| `verification_cooldown`        | Период времени в секундах после успешной попытки bind, в течение которого пользователь будет считаться успешно аутентифицированным для всех последующих запросов без обращения к LDAP-серверу. Укажите `0` (по умолчанию), чтобы отключить кэширование и принудительно обращаться к LDAP-серверу для каждого запроса аутентификации.                                                                                                                            |

Настройку `user_dn_detection` можно задать с помощью под-тегов:

| Настройка       | Описание                                                                                                                                                                                                                                                                                                                                  |
| --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `base_dn`       | шаблон, используемый для формирования base DN для LDAP search. Итоговый DN формируется заменой всех подстрок `\{user_name\}` и `\{bind_dn\}` в шаблоне на фактическое имя пользователя и bind DN во время LDAP search.                                                                                                                    |
| `scope`         | область LDAP search. Допустимые значения: `base`, `one_level`, `children`, `subtree` (по умолчанию).                                                                                                                                                                                                                                      |
| `search_filter` | шаблон, используемый для формирования search filter для LDAP search. Итоговый filter формируется заменой всех подстрок `\{user_name\}`, `\{bind_dn\}` и `\{base_dn\}` в шаблоне на фактическое имя пользователя, bind DN и base DN во время LDAP search. Обратите внимание: специальные символы должны быть корректно экранированы в XML. |

Пример:

```xml
<my_ldap_server>
    <host>localhost</host>
    <port>636</port>
    <bind_dn>uid={user_name},ou=users,dc=example,dc=com</bind_dn>
    <verification_cooldown>300</verification_cooldown>
    <enable_tls>yes</enable_tls>
    <tls_minimum_protocol_version>tls1.2</tls_minimum_protocol_version>
    <tls_require_cert>demand</tls_require_cert>
    <tls_cert_file>/path/to/tls_cert_file</tls_cert_file>
    <tls_key_file>/path/to/tls_key_file</tls_key_file>
    <tls_ca_cert_file>/path/to/tls_ca_cert_file</tls_ca_cert_file>
    <tls_ca_cert_dir>/path/to/tls_ca_cert_dir</tls_ca_cert_dir>
    <tls_cipher_suite>ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:AES256-GCM-SHA384</tls_cipher_suite>
</my_ldap_server>
```

Пример (типичная Active Directory с настроенным определением DN пользователя для последующего сопоставления ролей):

```xml
<my_ad_server>
    <host>localhost</host>
    <port>389</port>
    <bind_dn>EXAMPLE\{user_name}</bind_dn>
    <user_dn_detection>
        <base_dn>CN=Users,DC=example,DC=com</base_dn>
        <search_filter>(&amp;(objectClass=user)(sAMAccountName={user_name}))</search_filter>
    </user_dn_detection>
    <enable_tls>no</enable_tls>
</my_ad_server>
```

<div id="logger">
  ## logger
</div>

Расположение и формат сообщений лога.

**Ключи**:

| Key                          | Description                                                                                                                                                                                                                                                                                                                                      |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `async`                      | Если `true` (по умолчанию), логирование выполняется асинхронно (один фоновый поток на каждый канал вывода). В противном случае запись в журнал выполняется в потоке, вызывающем LOG                                                                                                                                                              |
| `async_queue_max_size`       | При использовании асинхронного логирования это максимальное количество сообщений, которое хранится в очереди в ожидании сброса. Лишние сообщения будут отброшены                                                                                                                                                                                 |
| `console`                    | Включает логирование в консоль. Установите `1` или `true`, чтобы включить. По умолчанию — `1`, если ClickHouse не запущен в режиме демона, иначе `0`.                                                                                                                                                                                            |
| `console_log_level`          | Уровень логирования для вывода в консоль. По умолчанию используется `level`.                                                                                                                                                                                                                                                                     |
| `console_shutdown_log_level` | Shutdown level используется для задания уровня логирования консоли при остановке сервера.                                                                                                                                                                                                                                                        |
| `console_startup_log_level`  | уровень при запуске используется для задания уровня логирования консоли при запуске сервера. После завершения запуска уровень логирования возвращается к значению настройки `console_log_level`                                                                                                                                                        |
| `count`                      | Политика ротации: максимальное количество исторических файлов журналов ClickHouse, которые хранятся.                                                                                                                                                                                                                                             |
| `errorlog`                   | Путь к файлу журнала ошибок.                                                                                                                                                                                                                                                                                                                     |
| `formatting.type`            | Формат логирования для вывода в консоль. В настоящее время поддерживается только `json`                                                                                                                                                                                                                                                          |
| `level`                      | Уровень логирования. Допустимые значения: `none` (отключает логирование), `fatal`, `critical`, `error`, `warning`, `notice`, `information`,`debug`, `trace`, `test`                                                                                                                                                                              |
| `log`                        | Путь к файлу журнала.                                                                                                                                                                                                                                                                                                                            |
| `rotation`                   | Политика ротации: определяет, когда выполняется ротация файлов журналов. Ротация может основываться на размере, времени или их сочетании. Примеры: 100M, daily, 100M,daily. Когда файл журнала превышает указанный размер или наступает заданный интервал времени, он переименовывается и архивируется, после чего создаётся новый файл журнала. |
| `shutdown_level`             | Shutdown level используется для задания уровня корневого логгера при остановке сервера.                                                                                                                                                                                                                                                          |
| `size`                       | Политика ротации: максимальный размер файлов журналов в байтах. Когда размер файла журнала превышает этот порог, он переименовывается и архивируется, после чего создаётся новый файл журнала.                                                                                                                                                   |
| `startup_level`              | уровень при запуске используется для задания уровня корневого логгера при запуске сервера. После завершения запуска уровень логирования возвращается к значению настройки `level`                                                                                                                                                                      |
| `stream_compress`            | Сжимает сообщения лога с помощью LZ4. Установите `1` или `true`, чтобы включить.                                                                                                                                                                                                                                                              |
| `syslog_level`               | Уровень логирования для отправки в syslog.                                                                                                                                                                                                                                                                                                       |
| `use_syslog`                 | Также пересылает вывод логов в syslog.                                                                                                                                                                                                                                                                                                           |

**Спецификаторы формата логов**

Имена файлов в путях `log` и `errorLog` поддерживают приведённые ниже спецификаторы формата для итогового имени файла (часть пути с каталогом их не поддерживает).

Столбец &quot;Пример&quot; показывает вывод для `2023-07-06 18:32:07`.

| Спецификатор | Описание                                                                                                                                                                                          | Пример                     |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- |
| `%%`         | Буквальный символ %                                                                                                                                                                               | `%`                        |
| `%n`         | Символ новой строки                                                                                                                                                                               |                            |
| `%t`         | Символ горизонтальной табуляции                                                                                                                                                                   |                            |
| `%Y`         | Год в виде десятичного числа, например 2017                                                                                                                                                       | `2023`                     |
| `%y`         | Последние 2 цифры года в виде десятичного числа (диапазон [00,99])                                                                                                                                | `23`                       |
| `%C`         | Первые 2 цифры года в виде десятичного числа (диапазон [00,99])                                                                                                                                   | `20`                       |
| `%G`         | Четырёхзначный [год по ISO 8601 с нумерацией недель](https://en.wikipedia.org/wiki/ISO_8601#Week_dates), то есть год, в который входит указанная неделя. Обычно используется только вместе с `%V` | `2023`                     |
| `%g`         | Последние 2 цифры [года по ISO 8601 с нумерацией недель](https://en.wikipedia.org/wiki/ISO_8601#Week_dates), то есть года, в который входит указанная неделя.                                     | `23`                       |
| `%b`         | Сокращённое название месяца, например Oct (зависит от локали)                                                                                                                                     | `Jul`                      |
| `%h`         | Синоним `%b`                                                                                                                                                                                      | `Jul`                      |
| `%B`         | Полное название месяца, например October (зависит от локали)                                                                                                                                      | `July`                     |
| `%m`         | Месяц в виде десятичного числа (диапазон [01,12])                                                                                                                                                 | `07`                       |
| `%U`         | Номер недели в году в виде десятичного числа (воскресенье — первый день недели) (диапазон [00,53])                                                                                                | `27`                       |
| `%W`         | Номер недели в году в виде десятичного числа (понедельник — первый день недели) (диапазон [00,53])                                                                                                | `27`                       |
| `%V`         | Номер недели по ISO 8601 (диапазон [01,53])                                                                                                                                                       | `27`                       |
| `%j`         | порядковый день года в виде десятичного числа (диапазон [001,366])                                                                                                                                | `187`                      |
| `%d`         | день месяца в виде десятичного числа с ведущим нулём (диапазон [01,31]). Перед однозначным числом ставится ноль.                                                                                  | `06`                       |
| `%e`         | день месяца в виде десятичного числа, дополненного пробелом (диапазон [1,31]). Перед однозначным числом ставится пробел.                                                                          | `&nbsp; 6`                 |
| `%a`         | Сокращённое название дня недели, например Fri (зависит от локали)                                                                                                                                 | `Thu`                      |
| `%A`         | Полное название дня недели, например Friday (зависит от локали)                                                                                                                                   | `Thursday`                 |
| `%w`         | День недели в виде целого числа, где воскресенье соответствует 0 (диапазон [0-6])                                                                                                                 | `4`                        |
| `%u`         | День недели в виде десятичного числа, где понедельник соответствует 1 (формат ISO 8601) (диапазон [1-7])                                                                                          | `4`                        |
| `%H`         | Час в виде десятичного числа, 24-часовой формат (диапазон [00-23])                                                                                                                                | `18`                       |
| `%I`         | Час в виде десятичного числа, 12-часовой формат (диапазон [01,12])                                                                                                                                | `06`                       |
| `%M`         | Минута в виде десятичного числа (диапазон [00,59])                                                                                                                                                | `32`                       |
| `%S`         | Секунда в виде десятичного числа (диапазон [00,60])                                                                                                                                               | `07`                       |
| `%c`         | Стандартная строка даты и времени, например Sun Oct 17 04:41:13 2010 (зависит от локали)                                                                                                          | `Thu Jul  6 18:32:07 2023` |
| `%x`         | Локализованное представление даты (зависит от локали)                                                                                                                                             | `07/06/23`                 |
| `%X`         | Локализованное представление времени, например 18:40:20 или 6:40:20 PM (зависит от локали)                                                                                                        | `18:32:07`                 |
| `%D`         | Короткая дата в формате MM/DD/YY, эквивалентна `%m/%d/%y`                                                                                                                                         | `07/06/23`                 |
| `%F`         | Короткий формат даты YYYY-MM-DD, эквивалентен %Y-%m-%d                                                                                                                                            | `2023-07-06`               |
| `%r`         | Локализованное время в 12-часовом формате (зависит от локали)                                                                                                                                     | `06:32:07 PM`              |
| `%R`         | Эквивалентно &quot;%H:%M&quot;                                                                                                                                                                    | `18:32`                    |
| `%T`         | Эквивалентно &quot;%H:%M:%S&quot; (формат времени ISO 8601)                                                                                                                                       | `18:32:07`                 |
| `%p`         | Локализованное обозначение a.m. или p.m. (зависит от локали)                                                                                                                                      | `PM`                       |
| `%z`         | Смещение относительно UTC в формате ISO 8601 (например, -0430) или отсутствие символов, если информация о часовом поясе недоступна                                                                | `+0800`                    |
| `%Z`         | Зависящее от локали название или аббревиатура часового пояса либо отсутствие символов, если информация о часовом поясе недоступна                                                                 | `Z AWST `                  |

**Пример**

```xml
<logger>
    <level>trace</level>
    <log>/var/log/clickhouse-server/clickhouse-server-%F-%T.log</log>
    <errorlog>/var/log/clickhouse-server/clickhouse-server-%F-%T.err.log</errorlog>
    <size>1000M</size>
    <count>10</count>
    <stream_compress>true</stream_compress>
</logger>
```

Чтобы выводить в консоль только сообщения лога:

```xml
<logger>
    <level>information</level>
    <console>true</console>
</logger>
```

**Переопределения по уровням**

Можно переопределить уровень логирования для отдельных логгеров. Например, чтобы отключить все сообщения логгеров &quot;Backup&quot; и &quot;RBAC&quot;.

```xml
<logger>
    <levels>
        <logger>
            <name>Backup</name>
            <level>none</level>
        </logger>
        <logger>
            <name>RBAC</name>
            <level>none</level>
        </logger>
    </levels>
</logger>
```

**syslog**

Чтобы также записывать сообщения лога в syslog:

```xml
<logger>
    <use_syslog>1</use_syslog>
    <syslog>
        <address>syslog.remote:10514</address>
        <hostname>myhost.local</hostname>
        <facility>LOG_LOCAL6</facility>
        <format>syslog</format>
    </syslog>
</logger>
```

Ключи для `<syslog>`:

| Ключ       | Описание                                                                                                                                                                                                                                                                                                             |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `address`  | Адрес syslog в формате `host\[:port\]`. Если не указан, используется локальный демон.                                                                                                                                                                                                                                |
| `hostname` | Имя хоста, с которого отправляются журналы (необязательно).                                                                                                                                                                                                                                                          |
| `facility` | [Ключевое слово facility](https://en.wikipedia.org/wiki/Syslog#Facility) в syslog. Должно быть указано прописными буквами с префиксом &quot;LOG&#95;&quot;, например: `LOG_USER`, `LOG_DAEMON`, `LOG_LOCAL3` и т. д. По умолчанию используется `LOG_USER`, если указан `address`, в противном случае — `LOG_DAEMON`. |
| `format`   | Формат сообщения журнала. Возможные значения: `bsd` и `syslog.`                                                                                                                                                                                                                                                      |

**Форматы Log**

Можно указать формат логирования, который будет использоваться в журнале консоли. Сейчас поддерживается только JSON.

**Пример**

Ниже приведён пример выходного JSON-журнала:

```json
{
  "date_time_utc": "2024-11-06T09:06:09Z",
  "date_time": "1650918987.180175",
  "thread_name": "#1",
  "thread_id": "254545",
  "level": "Trace",
  "query_id": "",
  "logger_name": "BaseDaemon",
  "message": "Received signal 2",
  "source_file": "../base/daemon/BaseDaemon.cpp; virtual void SignalListener::run()",
  "source_line": "192"
}
```

Чтобы включить поддержку логирования в формате JSON, используйте следующий фрагмент:

```xml
<logger>
    <formatting>
        <type>json</type>
        <!-- Can be configured on a per-channel basis (log, errorlog, console, syslog), or globally for all channels (then just omit it). -->
        <!-- <channel></channel> -->
        <names>
            <date_time>date_time</date_time>
            <thread_name>thread_name</thread_name>
            <thread_id>thread_id</thread_id>
            <level>level</level>
            <query_id>query_id</query_id>
            <logger_name>logger_name</logger_name>
            <message>message</message>
            <source_file>source_file</source_file>
            <source_line>source_line</source_line>
        </names>
    </formatting>
</logger>
```

**Переименование ключей в JSON-журналах**

Имена ключей можно изменить, поменяв значения тегов внутри тега `<names>`. Например, чтобы заменить `DATE_TIME` на `MY_DATE_TIME`, можно использовать `<date_time>MY_DATE_TIME</date_time>`.

**Исключение ключей из JSON-журналов**

Свойства журнала можно исключить, закомментировав соответствующее свойство. Например, если вы не хотите, чтобы в журнале выводился `query_id`, можно закомментировать тег `<query_id>`.

<div id="logger.count">
  ## logger.count
</div>

<SettingsInfoBlock type="UInt64" default_value="1" changeable_without_restart="No" />Политика ротации: максимальное количество хранимых исторических файлов журнала ClickHouse.

<div id="logger.errorlog">
  ## logger.errorlog
</div>

Путь к файлу журнала ошибок.

<div id="logger.formatting.type">
  ## logger.formatting.type
</div>

<SettingsInfoBlock type="String" default_value="json" changeable_without_restart="No" />Формат логирования для вывода в консоль. На данный момент поддерживается только `<json>`.

<div id="logger.level">
  ## logger.level
</div>

<SettingsInfoBlock type="String" default_value="trace" changeable_without_restart="No" />Уровень логирования. Допустимые значения: `<none>` (отключает логирование), `<fatal>`, `<critical>`, `<error>`, `<warning>`, `<notice>`, `<information>`, `<debug>`, `<trace>`, `<test>`.

<div id="logger.log">
  ## logger.log
</div>

Путь к файлу журнала.

<div id="logger.rotation">
  ## logger.rotation
</div>

<SettingsInfoBlock type="String" default_value="100M" changeable_without_restart="No" />Политика ротации: определяет, когда ротируются файлы журналов. Ротация может выполняться по размеру, по времени или по их сочетанию. Примеры: 100M, daily, 100M,daily. Когда файл журнала превышает указанный размер или истекает указанный интервал времени, он переименовывается и архивируется, после чего создаётся новый файл журнала.

<div id="logger.shutdown_level">
  ## logger.shutdown_level
</div>

Shutdown level используется для задания уровня корневого логгера при завершении работы server.

<div id="logger.size">
  ## logger.size
</div>

<SettingsInfoBlock type="String" default_value="100M" changeable_without_restart="No" />Политика ротации: максимальный размер файлов журнала в байтах. Когда размер файла журнала превышает этот порог, файл переименовывается и архивируется, а затем создаётся новый файл журнала.

<div id="logger.startup_level">
  ## logger.startup_level
</div>

Уровень при запуске используется для установки уровня корневого логгера во время запуска сервера. После завершения запуска уровень логирования возвращается к значению, заданному в настройке `<level>`.

<div id="logger.stream_compress">
  ## logger.stream_compress
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Сжимает сообщения лога с помощью LZ4. Для включения установите значение `<1>` или `<true>`.

<div id="logger.syslog_level">
  ## logger.syslog_level
</div>

<SettingsInfoBlock type="String" default_value="trace" changeable_without_restart="No" />Уровень логирования для syslog.

<div id="logger.use_syslog">
  ## logger.use_syslog
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Также направляет вывод логов в syslog.

<div id="macros">
  ## macros
</div>

Подстановки параметров для реплицированных таблиц.

Можно опустить, если реплицированные таблицы не используются.

Дополнительные сведения см. в разделе [Создание реплицированных таблиц](/ru/reference/engines/table-engines/mergetree-family/replication#creating-replicated-tables).

**Пример**

```xml
<macros incl="macros" optional="true" />
```

<div id="message_queue_disable_insertion">
  ## message_queue_disable_insertion
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="Да" />Отключает вставку из движков очередей сообщений (Kafka, RabbitMQ, NATS) в присоединенные materialized view

<div id="metric_log">
  ## metric_log
</div>

По умолчанию отключено.

**Включение**

Чтобы вручную включить сбор истории метрик [`system.metric_log`](/ru/reference/system-tables/metric_log), создайте файл `/etc/clickhouse-server/config.d/metric_log.xml` со следующим содержимым:

```xml
<clickhouse>
    <metric_log>
        <database>system</database>
        <table>metric_log</table>
        <flush_interval_milliseconds>7500</flush_interval_milliseconds>
        <collect_interval_milliseconds>1000</collect_interval_milliseconds>
        <max_size_rows>1048576</max_size_rows>
        <reserved_size_rows>8192</reserved_size_rows>
        <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
        <flush_on_crash>false</flush_on_crash>
    </metric_log>
</clickhouse>
```

**Отключение**

Чтобы отключить параметр `metric_log`, создайте файл `/etc/clickhouse-server/config.d/disable_metric_log.xml` со следующим содержимым:

```xml
<clickhouse>
    <metric_log remove="1" />
</clickhouse>
```

<SystemLogParameters />

<div id="mmap_cache_size">
  ## mmap_cache_size
</div>

<SettingsInfoBlock type="UInt64" default_value="1024" changeable_without_restart="Yes" />

Этот параметр позволяет избежать частых вызовов open/close (которые очень затратны из-за связанных с ними page fault), а также повторно использовать отображения памяти между несколькими потоками и запросами. Значение параметра — количество отображённых областей (обычно оно равно числу отображённых файлов).

Объём данных в отображённых файлах можно отслеживать в следующих системных таблицах по следующим метрикам:

* `MMappedFiles`/`MMappedFileBytes`/`MMapCacheCells` в [`system.metrics`](/ru/reference/system-tables/metrics), [`system.metric_log`](/ru/reference/system-tables/metric_log)
* `CreatedReadBufferMMap`/`CreatedReadBufferMMapFailed`/`MMappedFileCacheHits`/`MMappedFileCacheMisses` в [`system.events`](/ru/reference/system-tables/events), [`system.processes`](/ru/reference/system-tables/processes), [`system.query_log`](/ru/reference/system-tables/query_log), [`system.query_thread_log`](/ru/reference/system-tables/query_thread_log), [`system.query_views_log`](/ru/reference/system-tables/query_views_log)

<Note>
  Объём данных в отображённых файлах сам по себе не потребляет память и не учитывается в использовании памяти запроса или сервера, поскольку эта память может быть освобождена так же, как и кэш страниц ОС. Кэш автоматически очищается (файлы закрываются) при удалении старых частей в таблицах семейства MergeTree; кроме того, его можно очистить вручную запросом `SYSTEM DROP MMAP CACHE`.

  Этот параметр можно изменять во время работы, и он вступает в силу немедленно.
</Note>

<div id="mutation_workload">
  ## mutation_workload
</div>

<SettingsInfoBlock type="String" default_value="default" changeable_without_restart="Да" />

Используется для управления использованием и распределением ресурсов между мутациями и другими рабочими нагрузками. Указанное значение используется в качестве значения настройки `workload` для всех фоновых мутаций. Может быть переопределено настройкой MergeTree.

**См. также**

* [Планирование рабочих нагрузок](/ru/concepts/features/configuration/server-config/workload-scheduling)

<div id="oom_score">
  ## oom_score
</div>

<SettingsInfoBlock type="Int32" default_value="0" changeable_without_restart="No" />В системах Linux этот параметр позволяет управлять поведением OOM killer.

<div id="openssl">
  ## openSSL
</div>

Настройка SSL для клиента и сервера.

Поддержка SSL обеспечивается библиотекой `libpoco`. Доступные параметры конфигурации описаны в [SSLManager.h](https://github.com/ClickHouse-Extras/poco/blob/master/NetSSL_OpenSSL/include/Poco/Net/SSLManager.h). Значения по умолчанию приведены в [SSLManager.cpp](https://github.com/ClickHouse-Extras/poco/blob/master/NetSSL_OpenSSL/src/SSLManager.cpp).

Ключи настроек сервера/клиента:

| Параметр                      | Описание                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Значение по умолчанию                                                                      |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------ |
| `cacheSessions`               | Включает или отключает кэширование сеансов. Используется только в сочетании с `sessionIdContext`. Допустимые значения: `true`, `false`.                                                                                                                                                                                                                                                                                                                      | `false`                                                                                    |
| `caConfig`                    | Путь к файлу или каталогу с доверенными CA‑сертификатами. Если указан файл, он должен быть в формате PEM и может содержать несколько CA‑сертификатов. Если указан каталог, в нём должен быть один файл .pem для каждого CA‑сертификата. Имена файлов ищутся по хеш-значению имени субъекта CA. Подробности см. на man-странице [SSL&#95;CTX&#95;load&#95;verify&#95;locations](https://www.openssl.org/docs/man3.0/man3/SSL_CTX_load_verify_locations.html). |                                                                                            |
| `certificateFile`             | Путь к файлу сертификата клиента/сервера в формате PEM. Можно не указывать, если `privateKeyFile` содержит сертификат.                                                                                                                                                                                                                                                                                                                                       |                                                                                            |
| `cipherList`                  | Поддерживаемые алгоритмы шифрования OpenSSL.                                                                                                                                                                                                                                                                                                                                                                                                                 | `ALL:!ADH:!LOW:!EXP:!MD5:!3DES:@STRENGTH`                                                  |
| `disableProtocols`            | Протоколы, использование которых не допускается.                                                                                                                                                                                                                                                                                                                                                                                                             |                                                                                            |
| `extendedVerification`        | Если включено, проверяет, что CN или SAN сертификата соответствует имени хоста peer.                                                                                                                                                                                                                                                                                                                                                                         | `false`                                                                                    |
| `fips`                        | Включает режим FIPS в OpenSSL. Поддерживается, если версия OpenSSL в библиотеке supports FIPS.                                                                                                                                                                                                                                                                                                                                                               | `false`                                                                                    |
| `invalidCertificateHandler`   | Класс (подкласс CertificateHandler) для проверки недействительных сертификатов. Например: `<invalidCertificateHandler> <name>RejectCertificateHandler</name> </invalidCertificateHandler>`.                                                                                                                                                                                                                                                                  | `RejectCertificateHandler`                                                                 |
| `loadDefaultCAFile`           | Будут ли использоваться встроенные CA‑сертификаты OpenSSL. ClickHouse исходит из того, что встроенные CA‑сертификаты находятся в файле `/etc/ssl/cert.pem` (соответственно, в каталоге `/etc/ssl/certs`) или в файле (соответственно, каталоге), указанном в переменной окружения `SSL_CERT_FILE` (соответственно, `SSL_CERT_DIR`).                                                                                                                          | `true`                                                                                     |
| `preferServerCiphers`         | Наборы шифров сервера, предпочтительные для клиента.                                                                                                                                                                                                                                                                                                                                                                                                         | `false`                                                                                    |
| `privateKeyFile`              | Путь к файлу с закрытым ключом PEM-сертификата. Файл может одновременно содержать ключ и сертификат.                                                                                                                                                                                                                                                                                                                                                         |                                                                                            |
| `privateKeyPassphraseHandler` | Класс (подкласс PrivateKeyPassphraseHandler), запрашивающий парольную фразу для доступа к закрытому ключу. Например: `<privateKeyPassphraseHandler>`, `<name>KeyFileHandler</name>`, `<options><password>test</password></options>`, `</privateKeyPassphraseHandler>`.                                                                                                                                                                                       | `KeyConsoleHandler`                                                                        |
| `requireTLSv1`                | Требуется соединение по TLSv1. Допустимые значения: `true`, `false`.                                                                                                                                                                                                                                                                                                                                                                                         | `false`                                                                                    |
| `requireTLSv1_1`              | Требуется соединение по TLSv1.1. Допустимые значения: `true`, `false`.                                                                                                                                                                                                                                                                                                                                                                                       | `false`                                                                                    |
| `requireTLSv1_2`              | Требуется соединение по TLSv1.2. Допустимые значения: `true`, `false`.                                                                                                                                                                                                                                                                                                                                                                                       | `false`                                                                                    |
| `sessionCacheSize`            | Максимальное количество сеансов, хранимых сервером в кэше. Значение `0` означает неограниченное количество сеансов.                                                                                                                                                                                                                                                                                                                                          | [1024*20](https://github.com/ClickHouse/boringssl/blob/master/include/openssl/ssl.h#L1978) |
| `sessionIdContext`            | Уникальный набор случайных символов, который сервер добавляет к каждому сгенерированному идентификатору. Длина строки не должна превышать `SSL_MAX_SSL_SESSION_ID_LENGTH`. Этот параметр рекомендуется указывать всегда, так как он помогает избежать проблем как при кэшировании сеанса на сервере, так и при запросе кэширования со стороны клиента.                                                                                                       | `$\{application.name\}`                                                                    |
| `sessionTimeout`              | Время кэширования сеанса на сервере в часах.                                                                                                                                                                                                                                                                                                                                                                                                                 | `2`                                                                                        |
| `verificationDepth`           | Максимальная длина цепочки проверки. Проверка завершится ошибкой, если длина цепочки сертификатов превышает заданное значение.                                                                                                                                                                                                                                                                                                                               | `9`                                                                                        |
| `verificationMode`            | Режим проверки сертификатов узла. Подробности приведены в описании класса [Context](https://github.com/ClickHouse-Extras/poco/blob/master/NetSSL_OpenSSL/include/Poco/Net/Context.h). Возможные значения: `none`, `relaxed`, `strict`, `once`.                                                                                                                                                                                                               | `relaxed`                                                                                  |

**Пример настроек:**

```xml
<openSSL>
    <server>
        <!-- openssl req -subj "/CN=localhost" -new -newkey rsa:2048 -days 365 -nodes -x509 -keyout /etc/clickhouse-server/server.key -out /etc/clickhouse-server/server.crt -->
        <certificateFile>/etc/clickhouse-server/server.crt</certificateFile>
        <privateKeyFile>/etc/clickhouse-server/server.key</privateKeyFile>
        <!-- openssl dhparam -out /etc/clickhouse-server/dhparam.pem 4096 -->
        <dhParamsFile>/etc/clickhouse-server/dhparam.pem</dhParamsFile>
        <verificationMode>none</verificationMode>
        <loadDefaultCAFile>true</loadDefaultCAFile>
        <cacheSessions>true</cacheSessions>
        <disableProtocols>sslv2,sslv3</disableProtocols>
        <preferServerCiphers>true</preferServerCiphers>
    </server>
    <client>
        <loadDefaultCAFile>true</loadDefaultCAFile>
        <cacheSessions>true</cacheSessions>
        <disableProtocols>sslv2,sslv3</disableProtocols>
        <preferServerCiphers>true</preferServerCiphers>
        <!-- Use for self-signed: <verificationMode>none</verificationMode> -->
        <invalidCertificateHandler>
            <!-- Use for self-signed: <name>AcceptCertificateHandler</name> -->
            <name>RejectCertificateHandler</name>
        </invalidCertificateHandler>
    </client>
</openSSL>
```

<div id="openssl.client.caconfig">
  ## openSSL.client.caConfig
</div>

Путь к файлу или каталогу с доверенными CA‑сертификатами. Если указан файл, он должен быть в формате PEM и может содержать несколько CA‑сертификатов. Если указан каталог, в нём должен быть один файл `.pem` на каждый CA‑сертификат. Имена файлов определяются по хеш-значению имени субъекта CA. Подробности см. на man‑странице [SSL&#95;CTX&#95;load&#95;verify&#95;locations](https://docs.openssl.org/3.0/man3/SSL_CTX_load_verify_locations/).

<div id="openssl.client.cachesessions">
  ## openSSL.client.cacheSessions
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Включает или отключает кэширование сеансов. Должно использоваться вместе с `<sessionIdContext>`. Допустимые значения: `<true>`, `<false>`.

<div id="openssl.client.certificatefile">
  ## openSSL.client.certificateFile
</div>

Путь к файлу сертификата клиента или сервера в формате PEM. Этот параметр можно опустить, если `<privateKeyFile>` содержит сертификат.

<div id="openssl.client.cipherlist">
  ## openSSL.client.cipherList
</div>

<SettingsInfoBlock type="String" default_value="ALL:!ADH:!LOW:!EXP:!MD5:!3DES:@STRENGTH" changeable_without_restart="No" />Поддерживаемые шифры OpenSSL.

<div id="openssl.client.disableprotocols">
  ## openSSL.client.disableProtocols
</div>

Протоколы, которые нельзя использовать.

<div id="openssl.client.extendedverification">
  ## openSSL.client.extendedVerification
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Если включено, проверяет, что значение CN или SAN в сертификате соответствует имени хоста peer.

<div id="openssl.client.fips">
  ## openSSL.client.fips
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Включает режим FIPS в OpenSSL. Поддерживается, если используемая библиотекой версия OpenSSL поддерживает FIPS.

<div id="openssl.client.invalidcertificatehandler.name">
  ## openSSL.client.invalidCertificateHandler.name
</div>

<SettingsInfoBlock type="String" default_value="RejectCertificateHandler" changeable_without_restart="No" />Класс (подкласс CertificateHandler), используемый для проверки недействительных сертификатов. Например: `<<invalidCertificateHandler> <name>RejectCertificateHandler</name> </invalidCertificateHandler>>`.

<div id="openssl.client.loaddefaultcafile">
  ## openSSL.client.loadDefaultCAFile
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />Определяет, использовать ли встроенные CA‑сертификаты OpenSSL. ClickHouse предполагает, что встроенные CA‑сертификаты находятся в файле `</etc/ssl/cert.pem>` (соответственно, в каталоге `</etc/ssl/certs>`) либо в файле (или каталоге), указанном в переменной окружения `<SSL_CERT_FILE>` (или `<SSL_CERT_DIR>` соответственно).

<div id="openssl.client.preferserverciphers">
  ## openSSL.client.preferServerCiphers
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Предпочитаемые клиентом шифры сервера.

<div id="openssl.client.privatekeyfile">
  ## openSSL.client.privateKeyFile
</div>

Путь к файлу с закрытым ключом PEM-сертификата. Файл также может одновременно содержать и ключ, и сертификат.

<div id="openssl.client.privatekeypassphrasehandler.name">
  ## openSSL.client.privateKeyPassphraseHandler.name
</div>

<SettingsInfoBlock type="String" default_value="KeyConsoleHandler" changeable_without_restart="No" />Класс (подкласс PrivateKeyPassphraseHandler), запрашивающий парольную фразу для доступа к закрытому ключу. Например: `<<privateKeyPassphraseHandler>>`, `<<name>KeyFileHandler</name>>`, `<<options><password>test</password></options>>`, `<</privateKeyPassphraseHandler>>`

<div id="openssl.client.verificationdepth">
  ## openSSL.client.verificationDepth
</div>

<SettingsInfoBlock type="UInt64" default_value="9" changeable_without_restart="No" />Максимальная длина цепочки проверки. Если длина цепочки сертификатов превышает заданное значение, проверка завершится ошибкой.

<div id="openssl.client.verificationmode">
  ## openSSL.client.verificationMode
</div>

<SettingsInfoBlock type="String" default_value="relaxed" changeable_without_restart="No" />Способ проверки сертификатов узла. Подробности см. в описании класса [Context](https://github.com/ClickHouse/poco/blob/master/NetSSL_OpenSSL/include/Poco/Net/Context.h). Возможные значения: `<none>`, `<relaxed>`, `<strict>`, `<once>`.

<div id="openssl.server.caconfig">
  ## openSSL.server.caConfig
</div>

Путь к файлу или каталогу, содержащему доверенные CA‑сертификаты. Если указан файл, он должен быть в формате PEM и может содержать несколько CA‑сертификатов. Если указан каталог, в нём должен быть один файл `.pem` на каждый CA‑сертификат. Имена файлов определяются по хеш-значению имени субъекта CA. Подробности см. на man-странице [SSL&#95;CTX&#95;load&#95;verify&#95;locations](https://docs.openssl.org/3.0/man3/SSL_CTX_load_verify_locations/).

<div id="openssl.server.cachesessions">
  ## openSSL.server.cacheSessions
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Включает или отключает кэширование сеансов. Следует использовать вместе с `<sessionIdContext>`. Допустимые значения: `<true>`, `<false>`.

<div id="openssl.server.certificatefile">
  ## openSSL.server.certificateFile
</div>

Путь к файлу сертификата клиента или сервера в формате PEM. Этот параметр можно не указывать, если `<privateKeyFile>` содержит сертификат.

<div id="openssl.server.cipherlist">
  ## openSSL.server.cipherList
</div>

<SettingsInfoBlock type="String" default_value="ALL:!ADH:!LOW:!EXP:!MD5:!3DES:@STRENGTH" changeable_without_restart="No" />Список поддерживаемых шифров OpenSSL.

<div id="openssl.server.disableprotocols">
  ## openSSL.server.disableProtocols
</div>

Протоколы, использование которых запрещено.

<div id="openssl.server.extendedverification">
  ## openSSL.server.extendedVerification
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Если включено, проверяет, что CN или SAN сертификата соответствует имени хоста peer.

<div id="openssl.server.fips">
  ## openSSL.server.fips
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Включает режим FIPS в OpenSSL. Поддерживается, если версия библиотеки OpenSSL поддерживает FIPS.

<div id="openssl.server.invalidcertificatehandler.name">
  ## openSSL.server.invalidCertificateHandler.name
</div>

<SettingsInfoBlock type="String" default_value="RejectCertificateHandler" changeable_without_restart="No" />Класс (подкласс CertificateHandler) для проверки недействительных сертификатов. Например: `<<invalidCertificateHandler> <name>RejectCertificateHandler</name> </invalidCertificateHandler>>`.

<div id="openssl.server.loaddefaultcafile">
  ## openSSL.server.loadDefaultCAFile
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />Определяет, будут ли использоваться встроенные CA‑сертификаты OpenSSL. ClickHouse исходит из того, что встроенные CA‑сертификаты находятся в файле `</etc/ssl/cert.pem>` (соответственно, в каталоге `</etc/ssl/certs>`) либо в файле (соответственно, каталоге), указанном в переменной окружения `<SSL_CERT_FILE>` (соответственно, `<SSL_CERT_DIR>`).

<div id="openssl.server.preferserverciphers">
  ## openSSL.server.preferServerCiphers
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Наборы шифров сервера, предпочитаемые клиентом.

<div id="openssl.server.privatekeyfile">
  ## openSSL.server.privateKeyFile
</div>

Путь к файлу с закрытым ключом PEM-сертификата. Файл может одновременно содержать ключ и сертификат.

<div id="openssl.server.privatekeypassphrasehandler.name">
  ## openSSL.server.privateKeyPassphraseHandler.name
</div>

<SettingsInfoBlock type="String" default_value="KeyConsoleHandler" changeable_without_restart="No" />Класс (подкласс PrivateKeyPassphraseHandler), запрашивающий парольную фразу для доступа к закрытому ключу. Например: `<<privateKeyPassphraseHandler>>`, `<<name>KeyFileHandler</name>>`, `<<options><password>test</password></options>>`, `<</privateKeyPassphraseHandler>>`

<div id="openssl.server.sessioncachesize">
  ## openSSL.server.sessionCacheSize
</div>

<SettingsInfoBlock type="UInt64" default_value="20480" changeable_without_restart="No" />Максимальное количество сеансов, кэшируемых сервером. Значение 0 означает неограниченное количество сеансов.

<div id="openssl.server.sessionidcontext">
  ## openSSL.server.sessionIdContext
</div>

<SettingsInfoBlock type="String" default_value="application.name" changeable_without_restart="No" />Уникальный набор случайных символов, который сервер добавляет к каждому создаваемому идентификатору. Длина строки не должна превышать `<SSL_MAX_SSL_SESSION_ID_LENGTH>`. Этот параметр рекомендуется указывать всегда, поскольку он помогает избежать проблем как в случае, если сервер кэширует сеанс, так и в случае, если клиент запросил кэширование.

<div id="openssl.server.sessiontimeout">
  ## openSSL.server.sessionTimeout
</div>

<SettingsInfoBlock type="UInt64" default_value="2" changeable_without_restart="No" />Время кэширования сеанса на сервере, в часах.

<div id="openssl.server.verificationdepth">
  ## openSSL.server.verificationDepth
</div>

<SettingsInfoBlock type="UInt64" default_value="9" changeable_without_restart="No" />Максимальная длина цепочки проверки. Проверка завершится ошибкой, если длина цепочки сертификатов превысит заданное значение.

<div id="openssl.server.verificationmode">
  ## openSSL.server.verificationMode
</div>

<SettingsInfoBlock type="String" default_value="relaxed" changeable_without_restart="No" />Способ проверки сертификатов узла. Подробности приведены в описании класса [Context](https://github.com/ClickHouse/poco/blob/master/NetSSL_OpenSSL/include/Poco/Net/Context.h). Возможные значения: `<none>`, `<relaxed>`, `<strict>`, `<once>`.

<div id="opentelemetry_span_log">
  ## opentelemetry_span_log
</div>

Настройки для системной таблицы [`opentelemetry_span_log`](/ru/reference/system-tables/opentelemetry_span_log).

<SystemLogParameters />

Пример:

```xml
<opentelemetry_span_log>
    <engine>
        engine MergeTree
        partition by toYYYYMM(finish_date)
        order by (finish_date, finish_time_us, trace_id)
    </engine>
    <database>system</database>
    <table>opentelemetry_span_log</table>
    <flush_interval_milliseconds>7500</flush_interval_milliseconds>
    <max_size_rows>1048576</max_size_rows>
    <reserved_size_rows>8192</reserved_size_rows>
    <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
    <flush_on_crash>false</flush_on_crash>
</opentelemetry_span_log>
```

<div id="part_log">
  ## part_log
</div>

Журнал событий, связанных с [MergeTree](/ru/reference/engines/table-engines/mergetree-family/mergetree), например добавления данных или их слияния. Этот лог можно использовать для моделирования алгоритмов слияния и сравнения их характеристик. Также можно визуализировать процесс слияния.

Записи сохраняются в таблице [system.part&#95;log](/ru/reference/system-tables/part_log), а не в отдельном файле. Имя этой таблицы можно настроить с помощью параметра `table` (см. ниже).

<SystemLogParameters />

**Пример**

```xml
<part_log>
    <database>system</database>
    <table>part_log</table>
    <partition_by>toMonday(event_date)</partition_by>
    <flush_interval_milliseconds>7500</flush_interval_milliseconds>
    <max_size_rows>1048576</max_size_rows>
    <reserved_size_rows>8192</reserved_size_rows>
    <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
    <flush_on_crash>false</flush_on_crash>
</part_log>
```

<div id="path">
  ## path
</div>

<SettingsInfoBlock type="String" default_value="/var/lib/clickhouse/" changeable_without_restart="No" />

Путь к каталогу с данными.

<Note>
  Завершающая косая черта обязательна.
</Note>

**Пример**

```xml
<path>/var/lib/clickhouse/</path>
```

<div id="per_cpu_untracked_memory_thread_buffer">
  ## per_cpu_untracked_memory_thread_buffer
</div>

<SettingsInfoBlock type="UInt64" default_value="32768" changeable_without_restart="Да" />

Объём неотслеживаемой памяти в байтах, который каждый поток может удерживать, не затрагивая общий per-CPU-бюджет. Это позволяет снизить накладные расходы на per-CPU-учёт для небольших выделений памяти и представляет собой запас, добавляемый сверх `max_per_cpu_untracked_memory * number_of_cpus` в худшем случае. Значение `0` убирает этот запас: каждое выделение памяти обновляет общий per-CPU-счётчик (максимальная точность, но выше конкуренция). Только Linux.

<div id="point_in_polygon_cache_size">
  ## point_in_polygon_cache_size
</div>

<SettingsInfoBlock type="UInt64" default_value="268435456" changeable_without_restart="Yes" />

Максимальный размер в байтах для кэша предварительно обработанных полигонов, используемого функцией `pointInPolygon` с константным аргументом-полигоном.
Записи, превышающие этот лимит, вытесняются в порядке наименее недавнего использования.
При значении `0` кэш отключается: все кэшированные полигоны удаляются, и каждый последующий запрос заново предварительно обрабатывает свой константный полигон.
Кэш также можно очистить вручную, не меняя этот лимит, с помощью запроса [`SYSTEM DROP POINT IN POLYGON CACHE`](/ru/reference/statements/system#drop-point-in-polygon-cache).

<Note>
  Этот параметр можно изменять в процессе работы, и он вступает в силу немедленно.
</Note>

<div id="prefixes_deserialization_thread_pool_thread_pool_queue_size">
  ## prefixes_deserialization_thread_pool_thread_pool_queue_size
</div>

<SettingsInfoBlock type="UInt64" default_value="10000" changeable_without_restart="Yes" />

Максимальное количество задач, которые можно поставить в очередь в пуле потоков десериализации префиксов.

<Note>
  Значение `0` означает отсутствие ограничений.
</Note>

<div id="prepare_system_log_tables_on_startup">
  ## prepare_system_log_tables_on_startup
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />

Если значение равно true, ClickHouse создаёт все настроенные таблицы `system.*_log` до запуска. Это может быть полезно, если от этих таблиц зависят некоторые скрипты запуска.

<div id="process_query_plan_packet">
  ## process_query_plan_packet
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />

Эта настройка позволяет читать пакет QueryPlan. Этот пакет отправляется для распределённых запросов, когда включён serialize&#95;query&#95;plan.
По умолчанию отключено, чтобы избежать возможных проблем с безопасностью, которые могут быть вызваны ошибками при бинарной десериализации плана запроса.

**Пример**

```xml
<process_query_plan_packet>true</process_query_plan_packet>
```

<div id="processors_profile_log">
  ## processors_profile_log
</div>

Настройки системной таблицы [`processors_profile_log`](/ru/reference/system-tables/processors_profile_log).

<SystemLogParameters />

Настройки по умолчанию:

```xml
<processors_profile_log>
    <database>system</database>
    <table>processors_profile_log</table>
    <partition_by>toYYYYMM(event_date)</partition_by>
    <flush_interval_milliseconds>7500</flush_interval_milliseconds>
    <max_size_rows>1048576</max_size_rows>
    <reserved_size_rows>8192</reserved_size_rows>
    <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
    <flush_on_crash>false</flush_on_crash>
</processors_profile_log>
```

<div id="prometheus">
  ## prometheus
</div>

Экспорт данных метрик для сбора с помощью [Prometheus](https://prometheus.io).

Настройки:

* `endpoint` – HTTP-конечная точка, с которой сервер Prometheus собирает метрики. Должна начинаться с &#39;/&#39;.
* `port` – Порт для `endpoint`.
* `metrics` – Экспортировать метрики из таблицы [system.metrics](/ru/reference/system-tables/metrics).
* `events` – Экспортировать метрики из таблицы [system.events](/ru/reference/system-tables/events).
* `asynchronous_metrics` – Экспортировать текущие значения метрик из таблицы [system.asynchronous&#95;metrics](/ru/reference/system-tables/asynchronous_metrics).
* `errors` - Экспортировать количество ошибок по кодам, произошедших с момента последнего перезапуска сервера. Эту информацию также можно получить из [system.errors](/ru/reference/system-tables/errors).

**Пример**

```xml highlight={5-12}
<clickhouse>
    <listen_host>0.0.0.0</listen_host>
    <http_port>8123</http_port>
    <tcp_port>9000</tcp_port>
    <prometheus>
        <endpoint>/metrics</endpoint>
        <port>9363</port>
        <metrics>true</metrics>
        <events>true</events>
        <asynchronous_metrics>true</asynchronous_metrics>
        <errors>true</errors>
    </prometheus>
</clickhouse>
```

Проверьте (замените `127.0.0.1` на IP-адрес или имя хоста вашего сервера ClickHouse):

```bash
curl 127.0.0.1:9363/metrics
```

<div id="prometheus.keeper_metrics_only">
  ## prometheus.keeper_metrics_only
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Показывает метрики, относящиеся к Keeper

<div id="proxy">
  ## прокси
</div>

Определите прокси-серверы для HTTP- и HTTPS-запросов; сейчас это поддерживается для хранилища S3, табличных функций S3 и URL-функций.

Существует три способа задать прокси-серверы:

* переменные окружения
* списки прокси
* удалённые резолверы прокси.

Также поддерживается обход прокси-серверов для определённых хостов с помощью `no_proxy`.

**Переменные окружения**

Переменные окружения `http_proxy` и `https_proxy` позволяют указать
прокси-сервер для конкретного протокола. Если они заданы в системе, всё должно работать без дополнительной настройки.

Это самый простой подход, если для конкретного протокола есть
только один прокси-сервер и он не меняется.

**Списки прокси**

Этот подход позволяет указать один или несколько
прокси-серверов для протокола. Если задано более одного прокси-сервера,
ClickHouse использует их по кругу (round-robin), распределяя
нагрузку между серверами. Это самый простой подход, если для протокола есть
более одного прокси-сервера и список прокси-серверов не меняется.

**Шаблон конфигурации**

```xml
<proxy>
    <http>
        <uri>http://proxy1</uri>
        <uri>http://proxy2:3128</uri>
    </http>
    <https>
        <uri>http://proxy1:3128</uri>
    </https>
</proxy>
```

Выберите родительское поле на вкладках ниже, чтобы просмотреть соответствующие дочерние поля:

<Tabs>
  <Tab title="<proxy>">
    | Поле      | Описание                                     |
    | --------- | -------------------------------------------- |
    | `<http>`  | Список из одного или нескольких HTTP-прокси  |
    | `<https>` | Список из одного или нескольких HTTPS-прокси |
  </Tab>

  <Tab title="<http> и <https>">
    | Поле    | Описание   |
    | ------- | ---------- |
    | `<uri>` | URI прокси |
  </Tab>
</Tabs>

**Удалённые резолверы прокси**

Прокси-серверы могут меняться динамически. В таком
случае можно указать конечную точку резолвера. ClickHouse отправляет
пустой GET-запрос на эту конечную точку, а удалённый резолвер должен вернуть хост прокси.
Затем ClickHouse использует его для формирования URI прокси по следующему шаблону: `\{proxy_scheme\}://\{proxy_host\}:{proxy_port}`

**Шаблон конфигурации**

```xml
<proxy>
    <http>
        <resolver>
            <endpoint>http://resolver:8080/hostname</endpoint>
            <proxy_scheme>http</proxy_scheme>
            <proxy_port>80</proxy_port>
            <proxy_cache_time>10</proxy_cache_time>
        </resolver>
    </http>

    <https>
        <resolver>
            <endpoint>http://resolver:8080/hostname</endpoint>
            <proxy_scheme>http</proxy_scheme>
            <proxy_port>3128</proxy_port>
            <proxy_cache_time>10</proxy_cache_time>
        </resolver>
    </https>

</proxy>
```

Выберите родительское поле на вкладках ниже, чтобы просмотреть его дочерние поля:

<Tabs>
  <Tab title="<proxy>">
    | Поле      | Описание                                        |
    | --------- | ----------------------------------------------- |
    | `<http>`  | Список из одного или нескольких резолверов* |
    | `<https>` | Список из одного или нескольких резолверов* |
  </Tab>

  <Tab title="<http> и <https>">
    | Поле         | Описание                                    |
    | ------------ | ------------------------------------------- |
    | `<resolver>` | Конечная точка и другие параметры резолвера |

    <Note>
      Можно указать несколько элементов `<resolver>`, но для каждого
      протокола используется только первый `<resolver>`. Все остальные
      элементы `<resolver>` для этого протокола игнорируются. Это значит, что
      балансировка нагрузки (если она нужна) должна быть реализована
      удалённым резолвером.
    </Note>
  </Tab>

  <Tab title="<resolver>">
    | Поле                 | Описание                                                                                                                                                                                                              |
    | -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
    | `<endpoint>`         | URI резолвера прокси                                                                                                                                                                                                  |
    | `<proxy_scheme>`     | Протокол итогового URI прокси. Это может быть `http` или `https`.                                                                                                                                                     |
    | `<proxy_port>`       | Номер порта резолвера прокси                                                                                                                                                                                          |
    | `<proxy_cache_time>` | Время в секундах, в течение которого ClickHouse должен кэшировать значения, полученные от резолвера. Если задать это значение равным `0`, ClickHouse будет обращаться к резолверу при каждом HTTP- или HTTPS-запросе. |
  </Tab>
</Tabs>

**Приоритет**

Настройки прокси определяются в следующем порядке:

| Порядок | Настройка                  |
| ------- | -------------------------- |
| 1.      | Удалённые резолверы прокси |
| 2.      | Списки прокси              |
| 3.      | Переменные окружения       |

ClickHouse проверяет тип резолвера с наивысшим приоритетом для протокола запроса. Если он не задан,
проверяется следующий по приоритету тип резолвера, и так далее, пока не дойдёт до резолвера среды.
Это также позволяет использовать сочетание разных типов резолвера.

<div id="remap_executable">
  ## remap_executable
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />

Настройка для перераспределения памяти под машинный код (&quot;text&quot;) с использованием больших страниц памяти.

<Note>
  Эта возможность пока крайне экспериментальна.
</Note>

**Пример**

```xml
<remap_executable>false</remap_executable>
```

<div id="replica_group_name">
  ## replica_group_name
</div>

Имя группы реплик для базы данных Replicated.

Кластер, созданный базой данных Replicated, будет состоять из реплик одной и той же группы.
DDL-запросы будут ожидать только реплики из той же группы.

По умолчанию — пусто.

**Пример**

```xml
<replica_group_name>backups</replica_group_name>
```

<div id="replicated_merge_tree">
  ## replicated_merge_tree
</div>

Параметры тонкой настройки для таблиц [ReplicatedMergeTree](/ru/reference/engines/table-engines/mergetree-family/mergetree). Эта настройка имеет более высокий приоритет.

Дополнительные сведения см. в заголовочном файле MergeTreeSettings.h.

**Пример**

```xml
<replicated_merge_tree>
    <max_suspicious_broken_parts>5</max_suspicious_broken_parts>
</replicated_merge_tree>
```

<div id="restore_threads">
  ## restore_threads
</div>

<SettingsInfoBlock type="NonZeroUInt64" default_value="16" changeable_without_restart="No" />Максимальное количество потоков для выполнения запросов RESTORE.

<div id="send_crash_reports">
  ## send_crash_reports
</div>

Настройки отправки отчетов о сбоях команде основных разработчиков ClickHouse.

Будем очень признательны, если вы включите эту возможность, особенно в средах, предшествующих продакшн.

Ключи:

| Key                   | Description                                                                                                                             |
| --------------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| `enabled`             | Булевый флаг, включающий эту возможность; по умолчанию — `true`. Установите `false`, чтобы не отправлять отчеты о сбоях.                |
| `endpoint`            | Можно переопределить URL конечной точки для отправки отчетов о сбоях.                                                                   |
| `send_logical_errors` | `LOGICAL_ERROR` аналогичен `assert`; это bug в ClickHouse. Этот булевый флаг включает отправку таких исключений (по умолчанию: `true`). |

**Рекомендуемое использование**

```xml
<send_crash_reports>
    <enabled>true</enabled>
</send_crash_reports>
```

<div id="series_keeper_path">
  ## series_keeper_path
</div>

<SettingsInfoBlock type="String" default_value="/clickhouse/series" changeable_without_restart="No" />

Путь в Keeper с автоинкрементными номерами, создаваемыми функцией `generateSerialID`. Каждая серия будет представлена узлом в этом пути.

<div id="ssh_server">
  ## ssh_server
</div>

Открытая часть ключа хоста будет записана в файл known&#95;hosts
на стороне SSH-клиента при первом подключении.

Конфигурации ключей хоста по умолчанию отключены.
Раскомментируйте конфигурации ключей хоста и укажите путь к соответствующему SSH-ключу, чтобы активировать их:

Пример:

```xml
<ssh_server>
    <host_rsa_key>path_to_the_ssh_key</host_rsa_key>
    <host_ecdsa_key>path_to_the_ssh_key</host_ecdsa_key>
    <host_ed25519_key>path_to_the_ssh_key</host_ed25519_key>
</ssh_server>
```

<div id="table_engines_require_grant">
  ## table_engines_require_grant
</div>

Если установлено значение true, для создания таблицы с определённым движком пользователям требуется grant, например `GRANT TABLE ENGINE ON TinyLog to user`.

<Note>
  По умолчанию из соображений обратной совместимости при создании таблицы с определённым движком таблицы grant игнорируется, однако это поведение можно изменить, установив для этого параметра значение true.
</Note>

<div id="tcp_ssh_port">
  ## tcp_ssh_port
</div>

Порт SSH-сервера, через который пользователь может подключаться и выполнять запросы в интерактивном режиме с помощью встроенного клиента через PTY.

Пример:

```xml
<tcp_ssh_port>9022</tcp_ssh_port>
```

<div id="text_log">
  ## text_log
</div>

Настройки системной таблицы [text&#95;log](/ru/reference/system-tables/text_log) для журналирования текстовых сообщений.

<SystemLogParameters />

Дополнительно:

| Параметр | Описание                                                                                    | Значение по умолчанию |
| -------- | ------------------------------------------------------------------------------------------- | --------------------- |
| `level`  | Максимальный уровень сообщений (по умолчанию `Trace`), который будет сохраняться в таблице. | `Trace`               |

**Пример**

```xml
<clickhouse>
    <text_log>
        <level>notice</level>
        <database>system</database>
        <table>text_log</table>
        <flush_interval_milliseconds>7500</flush_interval_milliseconds>
        <max_size_rows>1048576</max_size_rows>
        <reserved_size_rows>8192</reserved_size_rows>
        <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
        <flush_on_crash>false</flush_on_crash>
        <!-- <partition_by>event_date</partition_by> -->
        <engine>Engine = MergeTree PARTITION BY event_date ORDER BY event_time TTL event_date + INTERVAL 30 day</engine>
    </text_log>
</clickhouse>
```

<div id="thread_pool_queue_size">
  ## thread_pool_queue_size
</div>

<SettingsInfoBlock type="UInt64" default_value="10000" changeable_without_restart="No" />

Максимальное число задач, которые можно поставить в очередь в глобальном пуле потоков. Увеличение размера очереди приводит к большему использованию памяти. Рекомендуется, чтобы это значение было равно [`max_thread_pool_size`](/ru/reference/settings/server-settings/settings/max-thread#max_thread_pool_size).

<Note>
  Значение `0` означает отсутствие ограничения.
</Note>

**Пример**

```xml
<thread_pool_queue_size>12000</thread_pool_queue_size>
```

<div id="throw_on_unknown_workload">
  ## throw_on_unknown_workload
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="Yes" />

Определяет поведение при обращении к неизвестной WORKLOAD с настройкой запроса &#39;workload&#39;.

* Если `true`, для запроса, который пытается обратиться к неизвестной рабочей нагрузке, генерируется исключение RESOURCE&#95;ACCESS&#95;DENIED. Полезно для принудительного применения планирования ресурсов ко всем запросам после того, как иерархия WORKLOAD создана и содержит WORKLOAD default.
* Если `false` (по умолчанию), запросу с настройкой &#39;workload&#39;, указывающей на неизвестную WORKLOAD, предоставляется неограниченный доступ без планирования ресурсов. Это важно при настройке иерархии WORKLOAD до добавления WORKLOAD default.

**Пример**

```xml
<throw_on_unknown_workload>true</throw_on_unknown_workload>
```

**См. также**

* [Планирование рабочих нагрузок](/ru/concepts/features/configuration/server-config/workload-scheduling)

<div id="timezone">
  ## timezone
</div>

Часовой пояс сервера.

Указывается как идентификатор IANA для часового пояса UTC или географического региона (например, Africa/Abidjan).

Часовой пояс нужен для преобразований между форматами String и DateTime, когда поля DateTime выводятся в текстовом формате (на экран или в файл), а также при получении значения DateTime из строки. Кроме того, часовой пояс используется в функциях, работающих с датой и временем, если он не был передан во входных параметрах.

**Пример**

```xml
<timezone>Asia/Istanbul</timezone>
```

**См. также**

* [session&#95;timezone](/ru/reference/settings/session-settings#session_timezone)

<div id="trace_log">
  ## trace_log
</div>

Настройки для работы системной таблицы [trace&#95;log](/ru/reference/system-tables/trace_log).

<SystemLogParameters />

Серверный файл конфигурации `config.xml`, используемый по умолчанию, содержит следующий раздел настроек:

```xml
<trace_log>
    <database>system</database>
    <table>trace_log</table>
    <partition_by>toYYYYMM(event_date)</partition_by>
    <flush_interval_milliseconds>7500</flush_interval_milliseconds>
    <max_size_rows>1048576</max_size_rows>
    <reserved_size_rows>8192</reserved_size_rows>
    <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
    <flush_on_crash>false</flush_on_crash>
    <symbolize>false</symbolize>
</trace_log>
```

<div id="url_scheme_mappers">
  ## url_scheme_mappers
</div>

Настройка для преобразования сокращённых или символьных префиксов URL в полные URL.

Пример:

```xml
<url_scheme_mappers>
    <s3>
        <to>https://{bucket}.s3.amazonaws.com</to>
    </s3>
    <gs>
        <to>https://storage.googleapis.com/{bucket}</to>
    </gs>
    <oss>
        <to>https://{bucket}.oss.aliyuncs.com</to>
    </oss>
</url_scheme_mappers>
```

<div id="validate_tcp_client_information">
  ## validate_tcp_client_information
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Определяет, включена ли проверка информации о клиенте при получении пакета запроса.

По умолчанию это `false`:

```xml
<validate_tcp_client_information>false</validate_tcp_client_information>
```

<div id="wait_dictionaries_load_at_startup">
  ## wait_dictionaries_load_at_startup
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />

Эта настройка позволяет задать поведение, если `dictionaries_lazy_load` имеет значение `false`.
(Если `dictionaries_lazy_load` имеет значение `true`, эта настройка ни на что не влияет.)

Если `wait_dictionaries_load_at_startup` имеет значение `false`, то сервер
начнет загружать все словари при запуске и будет принимать подключения параллельно с этой загрузкой.
Когда словарь впервые используется в запросе, запрос будет ждать, пока словарь не загрузится, если он еще не загружен.
Установка `wait_dictionaries_load_at_startup` в значение `false` может ускорить запуск ClickHouse, однако некоторые запросы могут выполняться медленнее
(поскольку им придется ждать загрузки некоторых словарей).

Если `wait_dictionaries_load_at_startup` имеет значение `true`, то сервер будет ждать при запуске,
пока все словари не завершат загрузку (успешно или нет), прежде чем принимать какие-либо подключения.

**Пример**

```xml
<wait_dictionaries_load_at_startup>true</wait_dictionaries_load_at_startup>
```

<div id="webassembly_udf_engine">
  ## webassembly_udf_engine
</div>

<SettingsInfoBlock type="String" default_value="wasmtime" changeable_without_restart="No" />Движок, используемый для исполнения WebAssembly UDF. Поддерживаются значения &#39;wasmtime&#39; и &#39;wasmedge&#39;.

<div id="webterminal_allowed_origins">
  ## webterminal_allowed_origins
</div>

Разделённый запятыми список полных origin-адресов (схема + хост + необязательный порт), которым разрешено открывать WebSocket-сеансы `/webterminal`. Если список пуст, политика same-origin применяется строго (Origin должен совпадать со схемой, хостом и портом запроса). Задайте этот параметр для развертываний за обратным прокси с терминацией TLS, где `request.isSecure()` имеет значение `false`, хотя браузер использует `https`. Пример: `https://example.com,https://app.example.com:8443`.

{/*AUTOGENERATED_END*/}