---
title: 'max_read_buffer_size_* настройки сеанса'
sidebarTitle: 'max_read_buffer_size_*'
toc_max_heading_level: 2
description: 'Настройки сеанса ClickHouse из сгенерированной группы max_read_buffer_size_*.'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/ru/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

Эти настройки доступны в [system.settings](/ru/reference/system-tables/settings) и автоматически сгенерированы на основе [исходного кода](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp).

<div id="max_read_buffer_size">
  ## max_read_buffer_size
</div>

<SettingsInfoBlock type="NonZeroUInt64" default_value="1048576" />

Максимальный размер буфера для чтения из файловой системы. Значения свыше 256 МиБ ограничиваются до 256 МиБ, так как буфер чтения большего размера не требуется.

<div id="max_read_buffer_size_local_fs">
  ## max_read_buffer_size_local_fs
</div>

<SettingsInfoBlock type="UInt64" default_value="131072" />

Максимальный размер буфера для чтения из локальной файловой системы. Если установлено значение 0, используется max&#95;read&#95;buffer&#95;size. Значения выше 256 MiB принудительно ограничиваются до 256 MiB, так как буфер чтения не требует большего размера.

<div id="max_read_buffer_size_remote_fs">
  ## max_read_buffer_size_remote_fs
</div>

<SettingsInfoBlock type="UInt64" default_value="0" />

Максимальный размер буфера для чтения из удалённой файловой системы. Если установлено значение 0, используется max&#95;read&#95;buffer&#95;size. Значения свыше 256 MiB ограничиваются 256 MiB, так как буфер чтения никогда не должен быть больше.

{/*AUTOGENERATED_END*/}