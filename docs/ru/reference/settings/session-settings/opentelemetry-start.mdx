---
title: 'настройки сеанса opentelemetry_start_*'
sidebarTitle: 'opentelemetry_start_*'
toc_max_heading_level: 2
description: 'Настройки сеанса ClickHouse в сгенерированной группе opentelemetry_start_*.'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/ru/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import VersionHistory from "/snippets/ru/components/VersionHistory/VersionHistory.jsx";

Эти настройки доступны в [system.settings](/ru/reference/system-tables/settings) и автоматически генерируются на основе [исходного кода](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp).

<div id="opentelemetry_start_keeper_trace_probability">
  ## opentelemetry_start_keeper_trace_probability
</div>

<SettingsInfoBlock type="FloatAuto" default_value="auto" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.2"},{"label": "auto"},{"label": "новая настройка"}]}]} />

Вероятность запуска трассировки для запроса ZooKeeper — независимо от наличия родительской трассировки.

Возможные значения:

* &#39;auto&#39; - Соответствует настройке opentelemetry&#95;start&#95;trace&#95;probability
* 0 — Трассировка отключена
* от 0 до 1 — Вероятность (например, 1.0 = всегда включено)

<div id="opentelemetry_start_trace_probability">
  ## opentelemetry_start_trace_probability
</div>

<SettingsInfoBlock type="Float" default_value="0" />

Задает вероятность того, что ClickHouse сможет начать трассировку для выполняемых запросов (если не передан родительский [контекст трассировки](https://www.w3.org/TR/trace-context/)).

Возможные значения:

* 0 — Трассировка для всех выполняемых запросов отключена (если не передан родительский контекст трассировки).
* Положительное число с плавающей точкой в диапазоне [0..1]. Например, если значение настройки равно `0,5`, ClickHouse будет в среднем начинать трассировку для половины запросов.
* 1 — Трассировка для всех выполняемых запросов включена.

{/*AUTOGENERATED_END*/}