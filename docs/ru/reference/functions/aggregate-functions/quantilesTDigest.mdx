---
description: 'Вычисляет несколько приблизительных квантилей на разных уровнях одновременно с помощью алгоритма t-digest.'
slug: /sql-reference/aggregate-functions/reference/quantilesTDigest
title: 'quantilesTDigest'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="quantilesTDigest">
  ## quantilesTDigest
</div>

Добавленный в: v1.1.0

Вычисляет несколько приблизительных [квантилей](https://en.wikipedia.org/wiki/Quantile) числовой последовательности данных на разных уровнях одновременно с помощью алгоритма [t-digest](https://github.com/tdunning/t-digest/blob/master/docs/t-digest-paper/histo.pdf).

Эта функция эквивалентна [`quantileTDigest`](/ru/reference/functions/aggregate-functions/quantileTDigest), но позволяет вычислять несколько уровней квантилей за один проход, что эффективнее, чем вызывать отдельные функции для каждого квантиля.

Потребление памяти составляет `log(n)`, где `n` — количество значений. Результат зависит от порядка выполнения запроса и является недетерминированным.

Производительность этой функции ниже, чем у [`quantiles`](/ru/reference/functions/aggregate-functions/quantiles) или [`quantilesTiming`](/ru/reference/functions/aggregate-functions/quantilesTiming). По соотношению размера состояния и точности эта функция значительно превосходит `quantiles`.

**Синтаксис**

```sql
quantilesTDigest(level1, level2, ...)(expr)
```

**Параметры**

* `level` — Уровни квантилей. Одно или несколько константных чисел с плавающей запятой в диапазоне от 0 до 1. Рекомендуется использовать значения `level` из диапазона `[0.01, 0.99]`. [`Float*`](/ru/reference/data-types/float)

**Аргументы**

* `expr` — Выражение над значениями столбца, результатом вычисления которого являются числовые типы данных, `Date` или `DateTime`. [`(U)Int*`](/ru/reference/data-types/int-uint) или [`Float*`](/ru/reference/data-types/float) или [`Date`](/ru/reference/data-types/date) или [`DateTime`](/ru/reference/data-types/datetime)

**Возвращаемое значение**

Массив приближённых квантилей указанных уровней в том же порядке, в котором эти уровни были заданы. Для входных значений `Date` и `DateTime` выходной формат совпадает с входным. [`Array(Float32)`](/ru/reference/data-types/array) или [`Array(Date)`](/ru/reference/data-types/array) или [`Array(DateTime)`](/ru/reference/data-types/array)

**Примеры**

**Вычисление нескольких квантилей с помощью t-digest**

```sql title=Query
SELECT quantilesTDigest(0.25, 0.5, 0.75)(number) FROM numbers(100);
```

```response title=Response
┌─quantilesTDigest(0.25, 0.5, 0.75)(number)─┐
│ [24.75,49.5,74.25]                        │
└───────────────────────────────────────────┘
```

{/*AUTOGENERATED_END*/}