---
description: 'Результат равен квадратному корню из varSamp. В отличие от stddevSamp,
  в этой функции используется численно стабильный алгоритм.'
slug: /sql-reference/aggregate-functions/reference/stddevsampstable
title: 'stddevSampStable'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="stddevSampStable">
  ## stddevSampStable
</div>

Добавленный в: v1.1.0

Результат равен квадратному корню из [varSamp](/ru/reference/functions/aggregate-functions/varSamp). В отличие от [stddevSamp](/ru/reference/functions/aggregate-functions/stddevSamp), эта функция использует численно стабильный алгоритм. Она работает медленнее, но обеспечивает меньшую вычислительную погрешность.

**Синтаксис**

```sql
stddevSampStable(x)
```

**Аргументы**

* `x` — Значения, для которых вычисляется квадратный корень из выборочной дисперсии. [`(U)Int*`](/ru/reference/data-types/int-uint) или [`Float*`](/ru/reference/data-types/float) или [`Decimal*`](/ru/reference/data-types/decimal)

**Возвращаемое значение**

Возвращает квадратный корень из выборочной дисперсии `x`. [`Float64`](/ru/reference/data-types/float)

**Примеры**

**Базовое использование**

```sql title=Query
DROP TABLE IF EXISTS test_data;
CREATE TABLE test_data
(
    population UInt8,
)
ENGINE = Log;

INSERT INTO test_data VALUES (3),(3),(3),(4),(4),(5),(5),(7),(11),(15);

SELECT
    stddevSampStable(population)
FROM test_data;
```

```response title=Response
┌─stddevSampStable(population)─┐
│                            4 │
└──────────────────────────────┘
```

{/*AUTOGENERATED_END*/}