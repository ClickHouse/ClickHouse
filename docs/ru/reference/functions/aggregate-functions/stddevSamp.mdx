---
description: 'Результат равен квадратному корню от varSamp'
slug: /sql-reference/aggregate-functions/reference/stddevsamp
title: 'stddevSamp'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="stddevSamp">
  ## stddevSamp
</div>

Добавленный в: v1.1.0

Возвращает выборочное стандартное отклонение числовой последовательности данных.
Результат равен квадратному корню из [`varSamp`](/ru/reference/functions/aggregate-functions/varSamp).

<Note>
  Эта функция использует численно неустойчивый алгоритм.
  Если вам нужна [численная устойчивость](https://en.wikipedia.org/wiki/Numerical_stability) в вычислениях, используйте функцию [`stddevSampStable`](/ru/reference/functions/aggregate-functions/stddevSampStable). Она работает медленнее, но обеспечивает меньшую погрешность вычислений.
</Note>

**Синтаксис**

```sql
stddevSamp(x)
```

**Псевдонимы**: `STDDEV_SAMP`, `STDDEV`

**Аргументы**

* `x` — Значения, для которых вычисляется квадратный корень из выборочной дисперсии. [`(U)Int*`](/ru/reference/data-types/int-uint) или [`Float*`](/ru/reference/data-types/float) или [`Decimal*`](/ru/reference/data-types/decimal)

**Возвращаемое значение**

Возвращает квадратный корень из выборочной дисперсии `x`. [`Float64`](/ru/reference/data-types/float)

**Примеры**

**Вычисление выборочного стандартного отклонения**

```sql title=Query
DROP TABLE IF EXISTS test_data;
CREATE TABLE test_data
(
    population UInt8,
)
ENGINE = Log;

INSERT INTO test_data VALUES (3),(3),(3),(4),(4),(5),(5),(7),(11),(15);

SELECT
    stddevSamp(population)
FROM test_data;
```

```response title=Response
┌─stddevSamp(population)─┐
│                      4 │
└────────────────────────┘
```

{/*AUTOGENERATED_END*/}