---
description: 'timeSeriesMetricFamilies возвращает таблицу семейств метрик, используемую таблицей `db_name.time_series_table`,
  движок которой — TimeSeries.'
sidebarTitle: 'timeSeriesMetricFamilies'
slug: /sql-reference/table-functions/timeSeriesMetricFamilies
title: 'timeSeriesMetricFamilies'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

`timeSeriesMetricFamilies(db_name.time_series_table)` — возвращает таблицу [семейств метрик](/ru/reference/engines/table-engines/integrations/time-series#metric-families-table), используемую таблицей `db_name.time_series_table` с движком [TimeSeries](/ru/reference/engines/table-engines/integrations/time-series):

```sql
CREATE TABLE db_name.time_series_table ENGINE=TimeSeries METRIC FAMILIES metric_families_table
```

Функция также работает, если таблица *metric families* является внутренней:

```sql
CREATE TABLE db_name.time_series_table ENGINE=TimeSeries METRIC FAMILIES INNER UUID '01234567-89ab-cdef-0123-456789abcdef'
```

Следующие запросы эквивалентны:

```sql
SELECT * FROM timeSeriesMetricFamilies(db_name.time_series_table);
SELECT * FROM timeSeriesMetricFamilies('db_name.time_series_table');
SELECT * FROM timeSeriesMetricFamilies('db_name', 'time_series_table');
```

<Note>
  Функция `timeSeriesMetricFamilies` имеет псевдоним `timeSeriesMetrics`, который сохранён для обратной совместимости.
</Note>

{/*AUTOGENERATED_END*/}