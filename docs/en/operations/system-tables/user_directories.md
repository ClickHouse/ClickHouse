---
description: 'Contains the information about configured user directories - directories on the file system from which ClickHouse server is allowed to read user provided data.'
keywords: ['system table', 'user_directories']
slug: /operations/system-tables/user_directories
title: 'system.user_directories'
doc_type: 'reference'
---

## Description {#description}

Contains the information about configured user directories - directories on the file system from which ClickHouse server is allowed to read user provided data.

## Columns {#columns}

<!--AUTOGENERATED_START-->
- `name` ([String](/sql-reference/data-types/string)) — The name of the directory.
- `type` ([String](/sql-reference/data-types/string)) — The type of the access storage e.g. users.xml or replicated or memory etc.
- `params` ([String](/sql-reference/data-types/string)) — JSON with the parameters of the access storage.
- `precedence` ([UInt64](/sql-reference/data-types/int-uint)) — The order in which this directory is declared in the config. The same order is used when ClickHouse tries to find a user or role.
<!--AUTOGENERATED_END-->
