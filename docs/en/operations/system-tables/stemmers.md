---
description: 'System table which shows all available stemmers.'
keywords: ['system table', 'stemmers']
sidebar_label: 'system.stemmers'
sidebar_position: 84
slug: /operations/system-tables/stemmers
title: 'system.stemmers'
doc_type: 'reference'
---

## Description {#description}

Shows all available stemmers.
These can be used in the function [stem](../../sql-reference/functions/nlp-functions.md).

:::note Availability
`system.stemmers` is present only in ClickHouse builds compiled with the `libstemmer` dependency (`USE_LIBSTEMMER`). On builds without it, the table does not exist and queries against it will fail with `UNKNOWN_TABLE`. You can check whether your build has it enabled with:

```sql
SELECT value FROM system.build_options WHERE name = 'USE_LIBSTEMMER';
```
:::

Columns:

<!--AUTOGENERATED_START-->
- `name` ([String](/sql-reference/data-types/string)) — Identifier of the Snowball stemmer (language/algorithm)
<!--AUTOGENERATED_END-->

## Example {#example}

```sql
SELECT * FROM system.stemmers;
```

```text
 ┌─name───────┐
 │ arabic     │
 │ armenian   │
 │ basque     │
 │ catalan    │
 │ danish     │
 │ dutch      │
 │ english    │
 │ [...]      │
 └────────────┘
```
