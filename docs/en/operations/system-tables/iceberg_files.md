---
description: 'System table containing per-file metadata of Iceberg tables'
keywords: ['system iceberg_files']
slug: /operations/system-tables/iceberg_files
title: 'system.iceberg_files'
doc_type: 'reference'
---

## Description {#description}

This system table contains per-file metadata for Iceberg tables existing in ClickHouse, with one row per data or delete file referenced by the current snapshot of each table. It will be empty if you don't have any Iceberg tables in ClickHouse.

## Columns {#columns}

<!--AUTOGENERATED_START-->
- `database` ([String](/sql-reference/data-types/string)) — Database name.
- `table` ([String](/sql-reference/data-types/string)) — Table name.
- `snapshot_id` ([Int64](/sql-reference/data-types/int-uint)) — Snapshot ID at which the file was added.
- `content` ([Enum8](/sql-reference/data-types/enum)) — File content kind: `DATA`, `POSITION_DELETE`, or `EQUALITY_DELETE`.
- `file_path` ([String](/sql-reference/data-types/string)) — Resolved storage path of the file.
- `file_format` ([String](/sql-reference/data-types/string)) — File format, e.g. `'PARQUET'`.
- `record_count` ([Int64](/sql-reference/data-types/int-uint)) — Number of records in the file.
- `file_size_in_bytes` ([Int64](/sql-reference/data-types/int-uint)) — Size of the file in bytes.
- `partition` ([String](/sql-reference/data-types/string)) — Textual representation of the partition tuple.
- `schema_id` ([Int32](/sql-reference/data-types/int-uint)) — Schema ID resolved for this manifest entry.
- `sequence_number` ([Int64](/sql-reference/data-types/int-uint)) — Resolved sequence number of the manifest entry (always 0 for format v1).
- `sort_order_id` ([Nullable(Int32)](/sql-reference/data-types/nullable)) — Sort order ID of the file, if specified.
- `null_value_counts` ([Map(Int32, Int64)](/sql-reference/data-types/map)) — Per-column null value count (column id -> count).
- `column_sizes` ([Map(Int32, Int64)](/sql-reference/data-types/map)) — Per-column on-disk size in bytes (column id -> bytes).
- `value_counts` ([Map(Int32, Int64)](/sql-reference/data-types/map)) — Per-column total value count (column id -> count).
- `equality_ids` ([Array(Int32)](/sql-reference/data-types/array)) — Equality field IDs for equality delete files (empty for non-equality-delete files).
<!--AUTOGENERATED_END-->
