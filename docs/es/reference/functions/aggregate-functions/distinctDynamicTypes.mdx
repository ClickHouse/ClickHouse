---
description: 'Calcula la lista de tipos de datos distintos almacenados en la columna Dynamic.'
slug: /sql-reference/aggregate-functions/reference/distinctdynamictypes
title: 'distinctDynamicTypes'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="distinctDynamicTypes">
  ## distinctDynamicTypes
</div>

Introducido en: v24.9.0

Calcula la lista de tipos de datos distintos almacenados en la columna [Dynamic](/es/reference/data-types/dynamic).

**Sintaxis**

```sql
distinctDynamicTypes(dynamic)
```

**Argumentos**

* `dynamic` — Columna dinámica. [`Dynamic`](/es/reference/data-types/dynamic)

**Valor devuelto**

Devuelve la lista ordenada de nombres de tipos de datos. [`Array(String)`](/es/reference/data-types/array)

**Ejemplos**

**Uso básico con tipos mixtos**

```sql title=Query
DROP TABLE IF EXISTS test_dynamic;
CREATE TABLE test_dynamic(d Dynamic) ENGINE = Memory;
INSERT INTO test_dynamic VALUES (42), (NULL), ('Hello'), ([1, 2, 3]), ('2020-01-01'), (map(1, 2)), (43), ([4, 5]), (NULL), ('World'), (map(3, 4));

SELECT distinctDynamicTypes(d) FROM test_dynamic;
```

```response title=Response
┌─distinctDynamicTypes(d)──────────────────────────────────────────┐
│ ['Array(Int64)', 'Date', 'Int64', 'Map(UInt8, UInt8)', 'String'] │
└──────────────────────────────────────────────────────────────────┘
```

{/*AUTOGENERATED_END*/}