---
description: 'Función de agregación que calcula el máximo de un grupo de valores.'
slug: /sql-reference/aggregate-functions/reference/max
title: 'max'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="max">
  ## max
</div>

Introducido en: v1.1.0

Función de agregación que calcula el valor máximo de un grupo de valores.

**Sintaxis**

```sql
max(column)
```

**Argumentos**

* `column` — Nombre de la columna o expresión. [`Any`](/es/reference/data-types/index)

**Valor devuelto**

El valor máximo del grupo, con el mismo tipo que la entrada. [`Any`](/es/reference/data-types/index)

**Ejemplos**

**Ejemplo sencillo de max**

```sql title=Query
CREATE TABLE employees (name String, salary UInt32) ENGINE = Memory;
INSERT INTO employees VALUES ('Alice', 3000), ('Bob', 4000), ('Charlie', 3500);

SELECT max(salary) FROM employees;
```

```response title=Response
┌─max(salary)─┐
│        4000 │
└─────────────┘
```

**Max con GROUP BY**

```sql title=Query
CREATE TABLE sales (department String, revenue UInt32) ENGINE = Memory;
INSERT INTO sales VALUES ('Engineering', 100000), ('Engineering', 120000), ('Marketing', 80000), ('Marketing', 90000);

SELECT department, max(revenue) FROM sales GROUP BY department ORDER BY department;
```

```response title=Response
┌─department──┬─max(revenue)─┐
│ Engineering │       120000 │
│ Marketing   │        90000 │
└─────────────┴──────────────┘
```

**Nota sobre el máximo sin agregación**

```sql title=Query
-- If you need non-aggregate function to choose a maximum of two values, see greatest():
SELECT greatest(a, b) FROM table;
```

{/*AUTOGENERATED_END*/}