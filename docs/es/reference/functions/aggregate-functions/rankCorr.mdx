---
description: 'Calcula un coeficiente de correlación de rangos.'
slug: /sql-reference/aggregate-functions/reference/rankCorr
title: 'rankCorr'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="rankCorr">
  ## rankCorr
</div>

Introducido en: v20.9.0

Calcula un coeficiente de correlación de rangos.

Devuelve el coeficiente de correlación de rangos entre x e y. El valor del coeficiente de correlación varía de -1 a +1. Si se pasan menos de dos argumentos, la función devolverá una excepción. Un valor cercano a +1 denota una relación lineal fuerte y, al aumentar una variable aleatoria, la segunda variable aleatoria también aumenta. Un valor cercano a -1 denota una relación lineal fuerte y, al aumentar una variable aleatoria, la segunda variable aleatoria disminuye. Un valor cercano o igual a 0 denota que no existe relación entre las dos variables aleatorias.

**Véase también**

* [Coeficiente de correlación de rangos de Spearman](https://en.wikipedia.org/wiki/Spearman%27s_rank_correlation_coefficient)

**Sintaxis**

```sql
rankCorr(x, y)
```

**Argumentos**

* `x` — Valor arbitrario. [`Float*`](/es/reference/data-types/float)
* `y` — Valor arbitrario. [`Float*`](/es/reference/data-types/float)

**Valor devuelto**

Devuelve un coeficiente de correlación entre los rangos de x e y. El valor varía de -1 a +1. [`Float64`](/es/reference/data-types/float)

**Ejemplos**

**Correlación perfecta**

```sql title=Query
SELECT rankCorr(number, number) FROM numbers(100);
```

```response title=Response
┌─rankCorr(number, number)─┐
│                        1 │
└──────────────────────────┘
```

**Relación no lineal**

```sql title=Query
SELECT roundBankers(rankCorr(exp(number), sin(number)), 3) FROM numbers(100);
```

```response title=Response
┌─roundBankers(rankCorr(exp(number), sin(number)), 3)─┐
│                                              -0.037 │
└─────────────────────────────────────────────────────┘
```

{/*AUTOGENERATED_END*/}

**Véase también**

* [coeficiente de correlación de rangos de Spearman](https://en.wikipedia.org/wiki/Spearman%27s_rank_correlation_coefficient)