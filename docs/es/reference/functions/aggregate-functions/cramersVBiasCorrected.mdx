---
description: 'Calcula la V de Cramer, pero aplica una corrección de sesgo.'
slug: /sql-reference/aggregate-functions/reference/cramersvbiascorrected
title: 'cramersVBiasCorrected'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="cramersVBiasCorrected">
  ## cramersVBiasCorrected
</div>

Introducido en: v22.1.0

La V de Cramer es una medida de asociación entre dos columnas de una tabla.
El resultado de la [función `cramersV`](/es/reference/functions/aggregate-functions/cramersV) va de 0 (lo que corresponde a la ausencia de asociación entre las variables) a 1, y solo puede alcanzar 1 cuando cada valor está completamente determinado por el otro.
La función puede presentar un sesgo importante, por lo que esta versión de la V de Cramer utiliza la [corrección de sesgo](https://en.wikipedia.org/wiki/Cram%C3%A9r%27s_V#Bias_correction).

**Sintaxis**

```sql
cramersVBiasCorrected(column1, column2)
```

**Argumentos**

* `column1` — Primera columna que se va a comparar. [`Any`](/es/reference/data-types/index)
* `column2` — Segunda columna que se va a comparar. [`Any`](/es/reference/data-types/index)

**Valor devuelto**

Devuelve un valor entre 0 (que corresponde a la ausencia de asociación entre los valores de las columnas) y 1 (asociación completa). [`Float64`](/es/reference/data-types/float)

**Ejemplos**

**Comparación con cramersV estándar**

```sql title=Query
SELECT
    cramersV(a, b),
    cramersVBiasCorrected(a, b)
FROM
    (
        SELECT
            number % 10 AS a,
            number % 4 AS b
        FROM
            numbers(150)
    );
```

```response title=Response
┌─────cramersV(a, b)─┬─cramersVBiasCorrected(a, b)─┐
│ 0.5798088336225178 │          0.5305112825189074 │
└────────────────────┴─────────────────────────────┘
```

{/*AUTOGENERATED_END*/}