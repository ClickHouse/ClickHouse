---
description: 'Selecciona un valor que aparece con frecuencia mediante el algoritmo de heavy hitters.
  Si hay un valor que aparece en más de la mitad de los casos en cada uno de los hilos
  de ejecución de la consulta, se devuelve dicho valor. Normalmente, el resultado es no determinista.'
slug: /sql-reference/aggregate-functions/reference/anyheavy
title: 'anyHeavy'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="anyHeavy">
  ## anyHeavy
</div>

Introducido en: v1.1.0

Selecciona un valor frecuente mediante el algoritmo de [heavy hitters](https://doi.org/10.1145/762471.762473).
Si hay un valor que aparece en más de la mitad de los casos en cada uno de los hilos de ejecución de la consulta, se devuelve ese valor.
Normalmente, el resultado es no determinista.

**Sintaxis**

```sql
anyHeavy(column)
```

**Argumentos**

* `column` — El nombre de la columna. [`String`](/es/reference/data-types/string)

**Valor devuelto**

Devuelve un valor frecuente. El resultado es no determinista. [`Any`](/es/reference/data-types/index)

**Ejemplos**

**Ejemplo de uso**

```sql title=Query
CREATE TABLE ontime (AirlineID UInt32) ENGINE = Memory;

-- Three quarters of the flights belong to one airline, so it occurs in more than half of the
-- rows seen by every thread and the result is the same on every run.
INSERT INTO ontime SELECT if(number % 4 = 0, 19393, 19690) FROM numbers(1000);

SELECT anyHeavy(AirlineID) AS res
FROM ontime;
```

```response title=Response
┌───res─┐
│ 19690 │
└───────┘
```

{/*AUTOGENERATED_END*/}