---
description: 'Calcula varios cuantiles de una secuencia de datos numéricos mediante interpolación lineal en distintos niveles simultáneamente, teniendo en cuenta el peso de cada elemento.'
slug: /sql-reference/aggregate-functions/reference/quantilesExactWeightedInterpolated
title: 'quantilesExactWeightedInterpolated'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="quantilesExactWeightedInterpolated">
  ## quantilesExactWeightedInterpolated
</div>

Introducido en: v24.10.0

Calcula varios [cuantiles](https://en.wikipedia.org/wiki/Quantile) de una secuencia de datos numéricos mediante interpolación lineal en distintos niveles de forma simultánea, teniendo en cuenta el peso de cada elemento.

Esta función es equivalente a [`quantileExactWeightedInterpolated`](/es/reference/functions/aggregate-functions/quantileExactWeightedInterpolated), pero permite calcular varios niveles de cuantiles en una sola pasada, lo que resulta más eficiente que llamar a funciones de cuantiles individuales.

**Sintaxis**

```sql
quantilesExactWeightedInterpolated(level1, level2, ...)(expr, weight)
```

**Parámetros**

* `level` — Niveles de cuantiles. Uno o más números constantes de coma flotante de 0 a 1. Recomendamos usar valores de `level` dentro del rango `[0.01, 0.99]`. [`Float*`](/es/reference/data-types/float)

**Argumentos**

* `expr` — Expresión sobre los valores de la columna que produce tipos de datos numéricos, `Date`, `DateTime` o `DateTime64`. [`(U)Int*`](/es/reference/data-types/int-uint) o [`Int128`](/es/reference/data-types/int-uint) o [`UInt128`](/es/reference/data-types/int-uint) o [`Int256`](/es/reference/data-types/int-uint) o [`UInt256`](/es/reference/data-types/int-uint) o [`Float*`](/es/reference/data-types/float) o [`Decimal*`](/es/reference/data-types/decimal) o [`Date`](/es/reference/data-types/date) o [`DateTime`](/es/reference/data-types/datetime) o [`DateTime64`](/es/reference/data-types/datetime64)
* `weight` — Columna con los pesos de los elementos de la secuencia. El peso es el número de apariciones del valor. [`UInt*`](/es/reference/data-types/int-uint)

**Valor devuelto**

Array de cuantiles de los niveles especificados, en el mismo orden en que se indicaron esos niveles. Los argumentos numéricos producen `Float64`, mientras que los argumentos `Decimal`, `Date`, `DateTime` y `DateTime64` conservan su formato de entrada. [`Array(Float64)`](/es/reference/data-types/array) o [`Array(Decimal*)`](/es/reference/data-types/array) o [`Array(Date)`](/es/reference/data-types/array) o [`Array(DateTime)`](/es/reference/data-types/array) o [`Array(DateTime64)`](/es/reference/data-types/array)

**Ejemplos**

**Cálculo de múltiples cuantiles exactos ponderados con interpolación**

```sql title=Query
SELECT quantilesExactWeightedInterpolated(0.25, 0.5, 0.75)(number, 1) FROM numbers(10)
```

```response title=Response
┌─quantilesExactWeightedInterpolated(0.25, 0.5, 0.75)(number, 1)─┐
│ [2.25,4.5,6.75]                                                │
└────────────────────────────────────────────────────────────────┘
```

{/*AUTOGENERATED_END*/}