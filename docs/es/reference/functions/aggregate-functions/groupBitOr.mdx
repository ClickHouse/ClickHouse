---
description: 'Aplica el operador `OR` bit a bit a una serie de números.'
slug: /sql-reference/aggregate-functions/reference/groupbitor
title: 'groupBitOr'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="groupBitOr">
  ## groupBitOr
</div>

Introducido en: v1.1.0

Aplica OR bit a bit a una serie de números.

**Sintaxis**

```sql
groupBitOr(expr)
```

**Alias**: `BIT_OR`

**Argumentos**

* `expr` — Expresión de tipo `(U)Int*`. [`(U)Int*`](/es/reference/data-types/int-uint)

**Valor devuelto**

Devuelve un valor de tipo `(U)Int*`. [`(U)Int*`](/es/reference/data-types/int-uint)

**Ejemplos**

**Ejemplo de OR bit a bit**

```sql title=Query
CREATE TABLE t (num UInt32) ENGINE = Memory;
INSERT INTO t VALUES (44), (28), (13), (85);

-- Datos de prueba:
-- binario     decimal
-- 00101100 = 44
-- 00011100 = 28
-- 00001101 = 13
-- 01010101 = 85

SELECT groupBitOr(num) FROM t;
```

```response title=Response
-- Resultado:
-- binario    decimal
-- 01111101 = 125

┌─groupBitOr(num)─┐
│             125 │
└─────────────────┘
```

{/*AUTOGENERATED_END*/}