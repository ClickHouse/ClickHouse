---
description: 'Calcula la covarianza poblacional'
slug: /sql-reference/aggregate-functions/reference/covarpop
title: 'covarPop'
doc_type: 'reference'
---



{/*AUTOGENERATED_START*/}
## covarPop {#covarPop}

Introducido en: v1.1.0

Calcula la covarianza poblacional:

$$
\frac{\Sigma{(x - \bar{x})(y - \bar{y})}}{n}
$$

<br/>

<Note>
Esta función utiliza un algoritmo numéricamente inestable. Si necesita [estabilidad numérica](https://en.wikipedia.org/wiki/Numerical_stability) en los cálculos, utilice la función [`covarPopStable`](/reference/functions/aggregate-functions/covarPopStable). Es más lenta, pero produce un menor error computacional.
</Note>
    

**Sintaxis**

```sql
covarPop(x, y)
```

**Aliases**: `COVAR_POP`

**Argumentos**

- `x` — Primera variable. [`(U)Int*`](/reference/data-types/int-uint) o [`Float*`](/reference/data-types/float) o [`Decimal`](/reference/data-types/decimal)
- `y` — Segunda variable. [`(U)Int*`](/reference/data-types/int-uint) o [`Float*`](/reference/data-types/float) o [`Decimal`](/reference/data-types/decimal)

**Valor devuelto**

Devuelve la covarianza poblacional entre `x` e `y`. [`Float64`](/reference/data-types/float)

**Ejemplos**

**Cálculo básico de covarianza poblacional**

```sql title=Query
DROP TABLE IF EXISTS series;
CREATE TABLE series(i UInt32, x_value Float64, y_value Float64) ENGINE = Memory;
INSERT INTO series(i, x_value, y_value) VALUES (1, 5.6, -4.4),(2, -9.6, 3),(3, -1.3, -4),(4, 5.3, 9.7),(5, 4.4, 0.037),(6, -8.6, -7.8),(7, 5.1, 9.3),(8, 7.9, -3.6),(9, -8.2, 0.62),(10, -3, 7.3);

SELECT covarPop(x_value, y_value)
FROM series
```

```response title=Response
┌─covarPop(x_value, y_value)─┐
│                   6.485648 │
└────────────────────────────┘
```
{/*AUTOGENERATED_END*/}