---
description: 'Calcula la varianza muestral de un conjunto de datos. A diferencia de `varSamp`, esta
  función usa un algoritmo numéricamente estable. Es más lenta, pero produce un menor
  error computacional.'
slug: /sql-reference/aggregate-functions/reference/varsampstable
title: 'varSampStable'
doc_type: 'reference'
---



{/*AUTOGENERATED_START*/}
## varSampStable {#varSampStable}

Introducido en: v1.1.0

Calcula la varianza muestral de un conjunto de datos. A diferencia de [`varSamp`](/reference/functions/aggregate-functions/varSamp), esta función utiliza un algoritmo [numéricamente estable](https://en.wikipedia.org/wiki/Numerical_stability). Es más lenta, pero produce un menor error computacional.

La varianza muestral se calcula con la misma fórmula que [`varSamp`](/reference/functions/aggregate-functions/varSamp):

$$
\frac{\Sigma{(x - \bar{x})^2}}{n-1}
$$

<br/>

Donde:
- $x$ es cada punto de datos individual del conjunto de datos
- $\bar{x}$ es la media aritmética del conjunto de datos
- $n$ es el número de puntos de datos del conjunto de datos
    

**Sintaxis**

```sql
varSampStable(x)
```

**Argumentos**

- `x` — La población para la que se desea calcular la varianza muestral. [`(U)Int*`](/reference/data-types/int-uint) o [`Float*`](/reference/data-types/float) o [`Decimal*`](/reference/data-types/decimal)

**Valor devuelto**

Devuelve la varianza muestral del conjunto de datos de entrada. [`Float64`](/reference/data-types/float)

**Ejemplos**

**Cálculo de la varianza muestral estable**

```sql title=Query
DROP TABLE IF EXISTS test_data;
CREATE TABLE test_data
(
    x Float64
)
ENGINE = Memory;

INSERT INTO test_data VALUES (10.5), (12.3), (9.8), (11.2), (10.7);

SELECT round(varSampStable(x),3) AS var_samp_stable FROM test_data;
```

```response title=Response
┌─var_samp_stable─┐
│           0.865 │
└─────────────────┘
```
{/*AUTOGENERATED_END*/}