---
title: 'ajustes del servidor asynchronous_*'
sidebarTitle: 'asynchronous_*'
toc_max_heading_level: 2
description: 'Ajustes del servidor de ClickHouse del grupo generado asynchronous_*.'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SystemLogParameters from '/snippets/es/_system-log-parameters.mdx';
import SettingsInfoBlock from "/snippets/es/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

Estos ajustes configuran el servidor de ClickHouse y se generan automáticamente a partir del código fuente de ClickHouse.

<div id="asynchronous_heavy_metrics_update_period_s">
  ## asynchronous_heavy_metrics_update_period_s
</div>

<SettingsInfoBlock type="UInt32" default_value="120" changeable_without_restart="No" />Intervalo en segundos para actualizar las métricas asíncronas pesadas.

<div id="asynchronous_insert_log">
  ## asynchronous_insert_log
</div>

Configuración de la tabla del sistema [asynchronous&#95;insert&#95;log](/es/reference/system-tables/asynchronous_insert_log) para registrar inserciones asíncronas.

<SystemLogParameters />

**Ejemplo**

```xml
<clickhouse>
    <asynchronous_insert_log>
        <database>system</database>
        <table>asynchronous_insert_log</table>
        <flush_interval_milliseconds>7500</flush_interval_milliseconds>
        <partition_by>toYYYYMM(event_date)</partition_by>
        <max_size_rows>1048576</max_size_rows>
        <reserved_size_rows>8192</reserved_size_rows>
        <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
        <flush_on_crash>false</flush_on_crash>
        <!-- <engine>Engine = MergeTree PARTITION BY event_date ORDER BY event_time TTL event_date + INTERVAL 30 day</engine> -->
    </asynchronous_insert_log>
</clickhouse>
```

<div id="asynchronous_metric_log">
  ## asynchronous_metric_log
</div>

Habilitado de forma predeterminada en los despliegues de ClickHouse Cloud.

Si esta configuración no está habilitada de forma predeterminada en su entorno, según cómo se haya instalado ClickHouse, puede seguir las instrucciones a continuación para habilitarla o deshabilitarla.

**Habilitación**

Para activar manualmente la recopilación del historial de registros de métricas asíncronas [`system.asynchronous_metric_log`](/es/reference/system-tables/asynchronous_metric_log), cree `/etc/clickhouse-server/config.d/asynchronous_metric_log.xml` con el siguiente contenido:

```xml
<clickhouse>
     <asynchronous_metric_log>
        <database>system</database>
        <table>asynchronous_metric_log</table>
        <flush_interval_milliseconds>7500</flush_interval_milliseconds>
        <collect_interval_milliseconds>1000</collect_interval_milliseconds>
        <max_size_rows>1048576</max_size_rows>
        <reserved_size_rows>8192</reserved_size_rows>
        <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
        <flush_on_crash>false</flush_on_crash>
    </asynchronous_metric_log>
</clickhouse>
```

**Desactivación**

Para desactivar la configuración `asynchronous_metric_log`, debe crear el siguiente archivo `/etc/clickhouse-server/config.d/disable_asynchronous_metric_log.xml` con el siguiente contenido:

```xml
<clickhouse><asynchronous_metric_log remove="1" /></clickhouse>
```

<SystemLogParameters />

{/*AUTOGENERATED_END*/}
