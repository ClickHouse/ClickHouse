---
description: 'Esta sección contiene descripciones de las configuraciones del servidor, es decir, de las configuraciones
que no pueden cambiarse a nivel de sesión ni de consulta.'
keywords: ['configuración global del servidor']
sidebarTitle: 'Configuración del servidor'
slug: /operations/server-configuration-parameters/settings
title: 'Configuración del servidor'
doc_type: 'reference'
---

import SystemLogParameters from '/snippets/es/_system-log-parameters.mdx';
import SettingsInfoBlock from "/snippets/es/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

Esta sección contiene descripciones de las opciones de configuración del servidor. Son opciones de configuración que
no pueden cambiarse a nivel de sesión ni de consulta.

Para obtener más información sobre los archivos de configuración en ClickHouse, consulte [&quot;&quot;Archivos de configuración&quot;&quot;](/es/concepts/features/configuration/server-config/configuration-files).

Otras opciones de configuración se describen en la sección &quot;&quot;[Settings](/es/concepts/features/configuration/settings/overview)&quot;&quot;.
Antes de revisar estas opciones de configuración, recomendamos leer la sección [Archivos de configuración](/es/concepts/features/configuration/server-config/configuration-files)
y tener en cuenta el uso de sustituciones (los atributos `incl` y `optional`).

<div id="abort_on_logical_error">
  ## abort_on_logical_error
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="Sí" />Hace que el servidor se cierre ante excepciones LOGICAL&#95;ERROR. Solo para expertos.

<div id="access_control_improvements">
  ## access_control_improvements
</div>

Configuración de mejoras opcionales del sistema de control de acceso.

| Configuración                                   | Descripción                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Predeterminado |
| ----------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- |
| `on_cluster_queries_require_cluster_grant`      | Establece si las consultas `ON CLUSTER` requieren el privilegio `CLUSTER`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `true`         |
| `role_cache_expiration_time_seconds`            | Establece la cantidad de segundos desde el último acceso durante los que un rol se almacena en la caché de roles.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `600`          |
| `select_from_information_schema_requires_grant` | Establece si `SELECT * FROM information_schema.<table>` requiere algún privilegio o puede ejecutarlo cualquier usuario. Si se establece en true, esta consulta requiere `GRANT SELECT ON information_schema.<table>`, igual que con las tablas normales.                                                                                                                                                                                                                                                                                                                           | `true`         |
| `select_from_system_db_requires_grant`          | Establece si `SELECT * FROM system.<table>` requiere algún privilegio o puede ejecutarlo cualquier usuario. Si se establece en true, esta consulta requiere `GRANT SELECT ON system.<table>`, igual que con las tablas que no son del sistema. Excepciones: algunas tablas del sistema (`tables`, `columns`, `databases` y algunas tablas constantes como `one`, `contributors`) siguen siendo accesibles para todos; además, si se ha concedido un privilegio `SHOW` (por ejemplo, `SHOW USERS`), la tabla del sistema correspondiente (es decir, `system.users`) será accesible. | `true`         |
| `settings_constraints_replace_previous`         | Establece si una restricción de un perfil de configuración para determinada configuración anulará los efectos de la restricción anterior (definida en otros perfiles) para esa configuración, incluidos los campos que la nueva restricción no establezca. También habilita el tipo de restricción `changeable_in_readonly`.                                                                                                                                                                                                                                                       | `true`         |
| `table_engines_require_grant`                   | Establece si la creación de una tabla con un motor de tabla específico requiere un privilegio.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `false`        |
| `throw_on_unmatched_row_policies`               | Establece si al leer de una tabla se debe generar una excepción cuando la tabla tenga políticas de fila, pero ninguna corresponda al usuario actual                                                                                                                                                                                                                                                                                                                                                                                                                                | `false`        |
| `users_without_row_policies_can_read_rows`      | Establece si los usuarios sin políticas de fila permisivas pueden seguir leyendo filas mediante una consulta `SELECT`. Por ejemplo, si hay dos usuarios, A y B, y se define una política de fila solo para A, entonces, si esta configuración es true, el usuario B verá todas las filas. Si esta configuración es false, el usuario B no verá ninguna fila.                                                                                                                                                                                                                       | `true`         |

Ejemplo:

```xml
<access_control_improvements>
    <throw_on_unmatched_row_policies>true</throw_on_unmatched_row_policies>
    <users_without_row_policies_can_read_rows>true</users_without_row_policies_can_read_rows>
    <on_cluster_queries_require_cluster_grant>true</on_cluster_queries_require_cluster_grant>
    <select_from_system_db_requires_grant>true</select_from_system_db_requires_grant>
    <select_from_information_schema_requires_grant>true</select_from_information_schema_requires_grant>
    <settings_constraints_replace_previous>true</settings_constraints_replace_previous>
    <table_engines_require_grant>false</table_engines_require_grant>
    <role_cache_expiration_time_seconds>600</role_cache_expiration_time_seconds>
</access_control_improvements>
```

<div id="access_control_path">
  ## access_control_path
</div>

Ruta a la carpeta donde el servidor de ClickHouse almacena las configuraciones de usuarios y roles creadas mediante comandos SQL.

**Véase también**

* [Control de acceso y gestión de cuentas](/es/concepts/features/security/access-rights#access-control-usage)

<div id="aggregate_function_group_array_action_when_limit_is_reached">
  ## aggregate_function_group_array_action_when_limit_is_reached
</div>

<SettingsInfoBlock type="GroupArrayActionWhenLimitReached" default_value="throw" changeable_without_restart="No" />Acción que se ejecuta cuando se supera el tamaño máximo del elemento del array en groupArray: lanzar una excepción (`throw`) o descartar los valores adicionales (`discard`)

<div id="aggregate_function_group_array_max_element_size">
  ## aggregate_function_group_array_max_element_size
</div>

<SettingsInfoBlock type="UInt64" default_value="16777215" changeable_without_restart="No" />Tamaño máximo del elemento de Array, en bytes, para la función groupArray. Este límite se comprueba durante la serialización y ayuda a evitar un tamaño de state excesivo.

<div id="allow_experimental_webassembly_udf">
  ## allow_experimental_webassembly_udf
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Habilita el soporte experimental para UDF de WebAssembly

<div id="allow_experimental_webterminal">
  ## allow_experimental_webterminal
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />Nombre anterior (experimental) de `enable_webterminal`. Se sigue admitiendo por compatibilidad con versiones anteriores cuando `enable_webterminal` no está configurado. Esta configuración está obsoleta.

<div id="allow_feature_tier">
  ## allow_feature_tier
</div>

<SettingsInfoBlock type="UInt32" default_value="0" changeable_without_restart="Yes" />

Controla si el usuario puede cambiar la configuración relacionada con los distintos niveles de funcionalidad.

* `0` - Se permiten cambios en cualquier configuración (experimental, beta, production).
* `1` - Solo se permiten cambios en la configuración de funcionalidades beta y production. Se rechazan los cambios en configuraciones experimentales.
* `2` - Solo se permiten cambios en la configuración de funcionalidades production. Se rechazan los cambios en configuraciones experimentales o beta.

Esto equivale a establecer una restricción readonly en todas las funcionalidades `EXPERIMENTAL` / `BETA`.

<Note>
  Un valor de `0` significa que se puede cambiar toda la configuración.
</Note>

<div id="allow_impersonate_user">
  ## allow_impersonate_user
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Habilita o deshabilita la función IMPERSONATE (EXECUTE AS target&#95;user). Este ajuste está obsoleto.

<div id="allow_implicit_no_password">
  ## allow_implicit_no_password
</div>

Impide crear un usuario sin contraseña, a menos que se especifique explícitamente &#39;IDENTIFIED WITH no&#95;password&#39;.

```xml
<allow_implicit_no_password>1</allow_implicit_no_password>
```

<div id="allow_no_password">
  ## allow_no_password
</div>

Define si se permite o no el tipo de contraseña insegura `no&#95;password`.

```xml
<allow_no_password>1</allow_no_password>
```

<div id="allow_plaintext_password">
  ## allow_plaintext_password
</div>

Establece si se permiten o no las contraseñas de tipo texto plano (inseguras).

```xml
<allow_plaintext_password>1</allow_plaintext_password>
```

<div id="allow_use_jemalloc_memory">
  ## allow_use_jemalloc_memory
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />Permite usar la memoria de jemalloc.

<div id="allowed_disks_for_table_engines">
  ## allowed_disks_for_table_engines
</div>

Lista de discos permitidos para su uso con Iceberg

<div id="async_insert_queue_flush_on_shutdown">
  ## async_insert_queue_flush_on_shutdown
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />Si es true, la cola de inserts asíncronos se vacía durante un apagado ordenado

<div id="async_insert_threads">
  ## async_insert_threads
</div>

<SettingsInfoBlock type="UInt64" default_value="16" changeable_without_restart="No" />Número máximo de hilos para analizar e insertar datos en segundo plano. Cero significa que el modo asíncrono está deshabilitado

<div id="async_load_databases">
  ## async_load_databases
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />

Carga asíncrona de bases de datos y tablas.

* Si es `true`, todas las bases de datos que no sean del sistema con motor `Ordinary`, `Atomic` y `Replicated` se cargarán de forma asíncrona después del arranque del servidor ClickHouse. Consulte la tabla `system.asynchronous_loader` y las configuraciones del servidor `tables_loader_background_pool_size` y `tables_loader_foreground_pool_size`. Cualquier consulta que intente acceder a una tabla que aún no se haya cargado esperará a que se inicie esa tabla en concreto. Si el trabajo de carga falla, la consulta volverá a generar un error (en lugar de apagar todo el servidor, como ocurriría en el caso de `async_load_databases = false`). La tabla por la que esté esperando al menos una consulta se cargará con mayor prioridad. Las consultas DDL sobre una base de datos esperarán a que se inicie esa base de datos en concreto. Considere también establecer un límite `max_waiting_queries` para el número total de consultas en espera.
* Si es `false`, todas las bases de datos se cargan cuando se inicia el servidor.

**Ejemplo**

```xml
<async_load_databases>true</async_load_databases>
```

<div id="async_load_system_database">
  ## async_load_system_database
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />

Carga asíncrona de las tablas del sistema. Útil si hay una gran cantidad de tablas de registros y partes en la base de datos `system`. Independiente de la configuración `async_load_databases`.

* Si se establece en `true`, todas las bases de datos del sistema con motores `Ordinary`, `Atomic` y `Replicated` se cargarán de forma asíncrona después de que se inicie el servidor ClickHouse. Consulte la tabla `system.asynchronous_loader` y las configuraciones del servidor `tables_loader_background_pool_size` y `tables_loader_foreground_pool_size`. Cualquier consulta que intente acceder a una tabla del sistema que aún no se haya cargado esperará a que esa tabla termine de iniciarse. La tabla por la que esté esperando al menos una consulta se cargará con mayor prioridad. Considere también establecer la configuración `max_waiting_queries` para limitar el número total de consultas en espera.
* Si se establece en `false`, la base de datos del sistema se carga antes de que se inicie el servidor.

**Ejemplo**

```xml
<async_load_system_database>true</async_load_system_database>
```

<div id="asynchronous_heavy_metrics_update_period_s">
  ## asynchronous_heavy_metrics_update_period_s
</div>

<SettingsInfoBlock type="UInt32" default_value="120" changeable_without_restart="No" />Período, en segundos, para actualizar las métricas asíncronas pesadas.

<div id="asynchronous_insert_log">
  ## asynchronous_insert_log
</div>

Configuración de la tabla del sistema [asynchronous&#95;insert&#95;log](/es/reference/system-tables/asynchronous_insert_log) para registrar inserciones asíncronas.

<SystemLogParameters />

**Ejemplo**

```xml
<clickhouse>
    <asynchronous_insert_log>
        <database>system</database>
        <table>asynchronous_insert_log</table>
        <flush_interval_milliseconds>7500</flush_interval_milliseconds>
        <partition_by>toYYYYMM(event_date)</partition_by>
        <max_size_rows>1048576</max_size_rows>
        <reserved_size_rows>8192</reserved_size_rows>
        <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
        <flush_on_crash>false</flush_on_crash>
        <!-- <engine>Engine = MergeTree PARTITION BY event_date ORDER BY event_time TTL event_date + INTERVAL 30 day</engine> -->
    </asynchronous_insert_log>
</clickhouse>
```

<div id="asynchronous_metric_log">
  ## asynchronous_metric_log
</div>

Habilitado de forma predeterminada en los despliegues de ClickHouse Cloud.

Si esta configuración no está habilitada de forma predeterminada en su entorno, dependiendo de cómo se haya instalado ClickHouse, puede seguir las instrucciones a continuación para habilitarla o deshabilitarla.

**Activación**

Para habilitar manualmente la recopilación del historial de registros de métricas asíncronas [`system.asynchronous_metric_log`](/es/reference/system-tables/asynchronous_metric_log), cree `/etc/clickhouse-server/config.d/asynchronous_metric_log.xml` con el siguiente contenido:

```xml
<clickhouse>
     <asynchronous_metric_log>
        <database>system</database>
        <table>asynchronous_metric_log</table>
        <flush_interval_milliseconds>7500</flush_interval_milliseconds>
        <collect_interval_milliseconds>1000</collect_interval_milliseconds>
        <max_size_rows>1048576</max_size_rows>
        <reserved_size_rows>8192</reserved_size_rows>
        <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
        <flush_on_crash>false</flush_on_crash>
    </asynchronous_metric_log>
</clickhouse>
```

**Desactivar**

Para deshabilitar la configuración `asynchronous_metric_log`, debe crear el siguiente archivo `/etc/clickhouse-server/config.d/disable_asynchronous_metric_log.xml` con el siguiente contenido:

```xml
<clickhouse><asynchronous_metric_log remove="1" /></clickhouse>
```

<SystemLogParameters />

<div id="asynchronous_metrics_enable_heavy_metrics">
  ## asynchronous_metrics_enable_heavy_metrics
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Activa el cálculo de métricas asíncronas pesadas.

<div id="asynchronous_metrics_keeper_metrics_only">
  ## asynchronous_metrics_keeper_metrics_only
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Hace que las métricas asíncronas calculen únicamente las métricas relacionadas con Keeper.

<div id="asynchronous_metrics_update_period_s">
  ## asynchronous_metrics_update_period_s
</div>

<SettingsInfoBlock type="UInt32" default_value="1" changeable_without_restart="No" />Período en segundos para la actualización de las métricas asíncronas.

<div id="auth_use_forwarded_address">
  ## auth_use_forwarded_address
</div>

Usa la dirección de origen para la autenticación de los clientes conectados a través de un proxy.

<Note>
  Esta configuración debe usarse con especial precaución, ya que las direcciones reenviadas pueden falsificarse fácilmente; no se debe acceder directamente a los servidores que acepten este tipo de autenticación, sino exclusivamente a través de un proxy de confianza.
</Note>

<div id="background_buffer_flush_schedule_pool_size">
  ## background_buffer_flush_schedule_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="16" changeable_without_restart="Increase only" />El número máximo de hilos que se utilizarán para realizar operaciones de vaciado en segundo plano para las [tablas con motor Buffer](/es/reference/engines/table-engines/special/buffer).

<div id="background_common_pool_size">
  ## background_common_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="8" changeable_without_restart="No" />El número máximo de hilos que se usarán para realizar diversas operaciones (principalmente, la recolección de basura) en segundo plano para las tablas [*MergeTree-engine](/es/reference/engines/table-engines/mergetree-family/index).

<div id="background_distributed_schedule_pool_size">
  ## background_distributed_schedule_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="16" changeable_without_restart="Increase only" />El número máximo de hilos que se utilizarán para ejecutar envíos distribuidos.

<div id="background_fetches_pool_size">
  ## background_fetches_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="16" changeable_without_restart="No" />El número máximo de hilos que se usarán para obtener partes de datos de otra réplica para las tablas [*MergeTree-engine](/es/reference/engines/table-engines/mergetree-family/index) en segundo plano.

<div id="background_merges_mutations_concurrency_ratio">
  ## background_merges_mutations_concurrency_ratio
</div>

<SettingsInfoBlock type="Float" default_value="2" changeable_without_restart="No" />

Establece una proporción entre el número de hilos y el número de fusiones y mutaciones en segundo plano que pueden ejecutarse de forma concurrente.

Por ejemplo, si la proporción es 2 y [`background_pool_size`](/es/reference/settings/server-settings/settings#background_pool_size) está establecido en 16, entonces ClickHouse puede ejecutar 32 fusiones en segundo plano de forma concurrente. Esto es posible porque las operaciones en segundo plano pueden suspenderse y posponerse. Esto es necesario para dar mayor prioridad de ejecución a las fusiones pequeñas.

<Note>
  Solo puede aumentar esta proporción en tiempo de ejecución. Para reducirla, debe reiniciar el servidor.

  Al igual que con la configuración [`background_pool_size`](/es/reference/settings/server-settings/settings#background_pool_size), [`background_merges_mutations_concurrency_ratio`](/es/reference/settings/server-settings/settings#background_merges_mutations_concurrency_ratio) puede aplicarse desde el perfil `default` por compatibilidad con versiones anteriores.
</Note>

<div id="background_merges_mutations_scheduling_policy">
  ## background_merges_mutations_scheduling_policy
</div>

<SettingsInfoBlock type="String" default_value="round_robin" changeable_without_restart="No" />

La política que define cómo se programan las fusiones y mutaciones en segundo plano. Los valores posibles son: `round_robin` y `shortest_task_first`.

Algoritmo utilizado para seleccionar la siguiente fusión o mutación que se ejecutará en el grupo de subprocesos en segundo plano. La política puede modificarse en tiempo de ejecución sin reiniciar el servidor.
Puede aplicarse desde el perfil `default` por compatibilidad con versiones anteriores.

Valores posibles:

* `round_robin` — Cada fusión y mutación concurrente se ejecuta en orden round-robin para garantizar un funcionamiento sin inanición. Las fusiones más pequeñas se completan más rápido que las más grandes simplemente porque tienen menos bloques que fusionar.
* `shortest_task_first` — Ejecuta siempre la fusión o mutación más pequeña. A las fusiones y mutaciones se les asignan prioridades en función de su tamaño resultante. Las fusiones de menor tamaño tienen prioridad estricta sobre las más grandes. Esta política garantiza la fusión más rápida posible de partes pequeñas, pero puede provocar la inanición indefinida de las fusiones grandes en particiones muy sobrecargadas por `INSERT`s.

<div id="background_message_broker_schedule_pool_size">
  ## background_message_broker_schedule_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="16" changeable_without_restart="Increase only" />El número máximo de hilos que se utilizarán para ejecutar operaciones en segundo plano para el streaming de mensajes.

<div id="background_move_pool_size">
  ## background_move_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="8" changeable_without_restart="No" />El número máximo de hilos que se utilizarán para mover en segundo plano partes de datos a otro disco o volumen en tablas con motor *MergeTree.

<div id="background_pool_size">
  ## background_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="16" changeable_without_restart="No" />

Establece el número de hilos que realizan las fusiones en segundo plano y las mutaciones de las tablas con motores MergeTree.

<Note>
  * Esta configuración también puede aplicarse durante el inicio del servidor desde la configuración del perfil `default`, por compatibilidad con versiones anteriores, al arrancar el servidor ClickHouse.
  * Solo puede aumentar el número de hilos en tiempo de ejecución.
  * Para reducir el número de hilos, debe reiniciar el servidor.
  * Al ajustar esta configuración, gestiona la carga de CPU y disco.
</Note>

<Danger>
  Un tamaño de grupo más pequeño utiliza menos recursos de CPU y disco, pero los procesos en segundo plano avanzan más lentamente, lo que con el tiempo podría afectar el rendimiento de las consultas.
</Danger>

Antes de cambiarla, revise también los ajustes relacionados de MergeTree, como:

* [`number_of_free_entries_in_pool_to_lower_max_size_of_merge`](/es/reference/settings/merge-tree-settings#number_of_free_entries_in_pool_to_lower_max_size_of_merge).
* [`number_of_free_entries_in_pool_to_execute_mutation`](/es/reference/settings/merge-tree-settings#number_of_free_entries_in_pool_to_execute_mutation).
* [`number_of_free_entries_in_pool_to_execute_optimize_entire_partition`](/es/reference/settings/merge-tree-settings#number_of_free_entries_in_pool_to_execute_optimize_entire_partition)

**Ejemplo**

```xml
<background_pool_size>16</background_pool_size>
```

<div id="background_schedule_pool_log">
  ## background_schedule_pool_log
</div>

Contiene información sobre todas las tareas en segundo plano que se ejecutan mediante varios grupos de procesamiento en segundo plano.

```xml
<background_schedule_pool_log>
    <database>system</database>
    <table>background_schedule_pool_log</table>
    <partition_by>toYYYYMM(event_date)</partition_by>
    <flush_interval_milliseconds>7500</flush_interval_milliseconds>
    <max_size_rows>1048576</max_size_rows>
    <reserved_size_rows>8192</reserved_size_rows>
    <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
    <flush_on_crash>false</flush_on_crash>
    <!-- Solo se registrarán las tareas cuya duración supere duration_threshold_milliseconds. Cero significa registrar todo -->
    <duration_threshold_milliseconds>0</duration_threshold_milliseconds>
</background_schedule_pool_log>
```

<div id="background_schedule_pool_max_parallel_tasks_per_type_ratio">
  ## background_schedule_pool_max_parallel_tasks_per_type_ratio
</div>

<SettingsInfoBlock type="Float" default_value="0.8" changeable_without_restart="No" />La proporción máxima de hilos del pool que pueden ejecutar simultáneamente tareas del mismo tipo.

<div id="background_schedule_pool_size">
  ## background_schedule_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="512" changeable_without_restart="Increase only" />El número máximo de hilos que se utilizarán para ejecutar continuamente algunas operaciones periódicas ligeras de tablas replicadas, streaming de Kafka y actualizaciones de la caché DNS.

<div id="backup_log">
  ## backup_log
</div>

Opciones de configuración de la tabla del sistema [backup&#95;log](/es/reference/system-tables/backup_log) para registrar las operaciones `BACKUP` y `RESTORE`.

<SystemLogParameters />

**Ejemplo**

```xml
<clickhouse>
    <backup_log>
        <database>system</database>
        <table>backup_log</table>
        <flush_interval_milliseconds>1000</flush_interval_milliseconds>
        <partition_by>toYYYYMM(event_date)</partition_by>
        <max_size_rows>1048576</max_size_rows>
        <reserved_size_rows>8192</reserved_size_rows>
        <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
        <flush_on_crash>false</flush_on_crash>
        <!-- <engine>Engine = MergeTree PARTITION BY event_date ORDER BY event_time TTL event_date + INTERVAL 30 day</engine> -->
    </backup_log>
</clickhouse>
```

<div id="backup_threads">
  ## backup_threads
</div>

<SettingsInfoBlock type="NonZeroUInt64" default_value="16" changeable_without_restart="No" />El número máximo de hilos para ejecutar solicitudes `BACKUP`.

<div id="backups">
  ## copias de seguridad
</div>

Configuración de las copias de seguridad utilizada al ejecutar las sentencias [`BACKUP` y `RESTORE`](/es/concepts/features/backup-restore/overview).

Los siguientes ajustes pueden configurarse mediante subetiquetas:

{/* SQL
  WITH settings AS (
  SELECT arrayJoin([
    ('allow_concurrent_backups', 'Bool','Determina si varias operaciones de copia de seguridad pueden ejecutarse simultáneamente en el mismo host.', 'true'),
    ('allow_concurrent_restores', 'Bool', 'Determina si varias operaciones de restauración pueden ejecutarse simultáneamente en el mismo host.', 'true'),
    ('allowed_disk', 'String', 'disco en el que realizar la copia de seguridad al usar `File()`. Esta configuración debe establecerse para poder usar `File`.', ''),
    ('allowed_path', 'String', 'Ruta en la que realizar la copia de seguridad al usar `File()`. Esta configuración debe establecerse para poder usar `File`.', ''),
    ('attempts_to_collect_metadata_before_sleep', 'UInt', 'Número de intentos de recopilar metadatos antes de entrar en espera en caso de inconsistencia tras comparar los metadatos recopilados.', '2'),
    ('collect_metadata_timeout', 'UInt64', 'Tiempo de espera en milisegundos para recopilar metadatos durante la copia de seguridad.', '600000'),
    ('compare_collected_metadata', 'Bool', 'Si es `true`, compara los metadatos recopilados con los metadatos existentes para garantizar que no cambien durante la copia de seguridad.', 'true'),
    ('create_table_timeout', 'UInt64', 'Tiempo de espera en milisegundos para crear tablas durante la restauración.', '300000'),
    ('max_attempts_after_bad_version', 'UInt64', 'Número máximo de reintentos tras encontrar un error de versión no válida durante la copia de seguridad/restauración coordinada.', '3'),
    ('max_sleep_before_next_attempt_to_collect_metadata', 'UInt64', 'Tiempo máximo de espera en milisegundos antes del siguiente intento de recopilar metadatos.', '100'),
    ('min_sleep_before_next_attempt_to_collect_metadata', 'UInt64', 'Tiempo mínimo de espera en milisegundos antes del siguiente intento de recopilar metadatos.', '5000'),
    ('remove_backup_files_after_failure', 'Bool', 'Si el comando `BACKUP` falla, ClickHouse intentará eliminar los archivos ya copiados a la copia de seguridad antes del fallo; de lo contrario, dejará los archivos copiados tal como están.', 'true'),
    ('sync_period_ms', 'UInt64', 'Período de sincronización en milisegundos para la copia de seguridad/restauración coordinada.', '5000'),
    ('test_inject_sleep', 'Bool', 'Espera relacionada con las pruebas', 'false'),
    ('test_randomize_order', 'Bool', 'Si es `true`, aleatoriza el orden de ciertas operaciones con fines de prueba.', 'false'),
    ('zookeeper_path', 'String', 'Ruta en ZooKeeper donde se almacenan los metadatos de copia de seguridad y restauración al usar la cláusula `ON CLUSTER`.', '/clickhouse/backups')
  ]) AS t )
  SELECT concat('`', t.1, '`') AS Configuración, t.2 AS Tipo, t.3 AS Descripción, concat('`', t.4, '`') AS Predeterminado FROM settings FORMAT Markdown
  */}

| Configuración                                       | Tipo   | Descripción                                                                                                                                                                                  | Predeterminado        |
| :-------------------------------------------------- | :----- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :-------------------- |
| `allow_concurrent_backups`                          | Bool   | Determina si varias operaciones de copia de seguridad pueden ejecutarse de forma concurrente en el mismo host.                                                                               | `true`                |
| `allow_concurrent_restores`                         | Bool   | Determina si varias operaciones de restauración pueden ejecutarse de forma concurrente en el mismo host.                                                                                     | `true`                |
| `allowed_disk`                                      | String | Disco en el que realizar la copia de seguridad al usar `File()`. Esta configuración debe establecerse para poder usar `File`.                                                                | &#96;&#96;            |
| `allowed_path`                                      | String | Ruta en la que realizar la copia de seguridad al usar `File()`. Esta configuración debe establecerse para poder usar `File`.                                                                 | &#96;&#96;            |
| `attempts_to_collect_metadata_before_sleep`         | UInt   | Número de intentos para recopilar metadatos antes de esperar en caso de inconsistencia tras comparar los metadatos recopilados.                                                              | `2`                   |
| `collect_metadata_timeout`                          | UInt64 | Tiempo de espera en milisegundos para recopilar metadatos durante la copia de seguridad.                                                                                                     | `600000`              |
| `compare_collected_metadata`                        | Bool   | Si es `true`, compara los metadatos recopilados con los metadatos existentes para asegurarse de que no cambien durante la copia de seguridad.                                                | `true`                |
| `create_table_timeout`                              | UInt64 | Tiempo de espera en milisegundos para crear tablas durante la restauración.                                                                                                                  | `300000`              |
| `max_attempts_after_bad_version`                    | UInt64 | Número máximo de intentos de reintento tras encontrar un error de versión no válida durante una copia de seguridad o restauración coordinada.                                                | `3`                   |
| `max_sleep_before_next_attempt_to_collect_metadata` | UInt64 | Tiempo máximo de espera en milisegundos antes del siguiente intento de recopilar metadatos.                                                                                                  | `100`                 |
| `min_sleep_before_next_attempt_to_collect_metadata` | UInt64 | Tiempo mínimo de espera en milisegundos antes del siguiente intento de recopilar metadatos.                                                                                                  | `5000`                |
| `remove_backup_files_after_failure`                 | Bool   | Si el comando `BACKUP` falla, ClickHouse intentará eliminar los archivos ya copiados en la copia de seguridad antes del fallo; de lo contrario, dejará los archivos copiados tal como están. | `true`                |
| `sync_period_ms`                                    | UInt64 | Período de sincronización en milisegundos para la copia de seguridad o restauración coordinada.                                                                                              | `5000`                |
| `test_inject_sleep`                                 | Bool   | Pausa relacionada con pruebas                                                                                                                                                                | `false`               |
| `test_randomize_order`                              | Bool   | Si es `true`, aleatoriza el orden de ciertas operaciones con fines de prueba.                                                                                                                | `false`               |
| `zookeeper_path`                                    | String | Ruta en ZooKeeper donde se almacenan los metadatos de copia de seguridad y restauración al usar la cláusula `ON CLUSTER`.                                                                    | `/clickhouse/backups` |

Esta configuración se establece de forma predeterminada de la siguiente manera:

```xml
<backups>
    ....
</backups>
```

<div id="backups_io_thread_pool_queue_size">
  ## backups_io_thread_pool_queue_size
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Sí" />

El número máximo de trabajos que se pueden programar en el pool de hilos de E/S de copias de seguridad. Se recomienda mantener esta cola ilimitada debido a la lógica actual de las copias de seguridad en S3.

<Note>
  Un valor de `0` (predeterminado) significa que no hay límite.
</Note>

<div id="bcrypt_workfactor">
  ## bcrypt_workfactor
</div>

Factor de trabajo para el tipo de autenticación `bcrypt_password`, que usa el [algoritmo Bcrypt](https://wildlyinaccurate.com/bcrypt-choosing-a-work-factor/).
El factor de trabajo define la cantidad de cálculos y tiempo necesarios para calcular el hash y verificar la contraseña.

```xml
<bcrypt_workfactor>12</bcrypt_workfactor>
```

<Warning>
  Para aplicaciones con autenticación frecuente,
  considere métodos de autenticación alternativos debido al
  costo computacional de bcrypt con factores de trabajo altos.
</Warning>

<div id="blob_storage_log">
  ## blob_storage_log
</div>

Configuración de la tabla del sistema [`blob_storage_log`](/es/reference/system-tables/blob_storage_log).

<SystemLogParameters />

Ejemplo:

```xml
<blob_storage_log>
    <database>system</database
    <table>blob_storage_log</table
    <partition_by>toYYYYMM(event_date)</partition_by>
    <flush_interval_milliseconds>7500</flush_interval_milliseconds
    <ttl>event_date + INTERVAL 30 DAY</ttl>
</blob_storage_log>
```

<div id="builtin_dictionaries_reload_interval">
  ## builtin_dictionaries_reload_interval
</div>

El intervalo, en segundos, antes de volver a cargar los diccionarios integrados.

ClickHouse vuelve a cargar los diccionarios integrados cada x segundos. Esto permite editar los diccionarios &quot;al vuelo&quot; sin reiniciar el servidor.

**Ejemplo**

```xml
<builtin_dictionaries_reload_interval>3600</builtin_dictionaries_reload_interval>
```

<div id="cache_size_to_ram_max_ratio">
  ## cache_size_to_ram_max_ratio
</div>

<SettingsInfoBlock type="Double" default_value="0.5" changeable_without_restart="No" />Establece la proporción máxima entre el tamaño de la caché y la RAM. Permite reducir el tamaño de la caché en sistemas con poca memoria.

<div id="cannot_allocate_thread_fault_injection_probability">
  ## cannot_allocate_thread_fault_injection_probability
</div>

<SettingsInfoBlock type="Double" default_value="0" changeable_without_restart="No" />Con fines de prueba.

<div id="cgroups_memory_usage_observer_wait_time">
  ## cgroups_memory_usage_observer_wait_time
</div>

<SettingsInfoBlock type="UInt64" default_value="15" changeable_without_restart="No" />

Intervalo en segundos durante el cual el consumo máximo de memoria permitido del servidor se ajusta según el umbral correspondiente en cgroups.

Para desactivar el observador de cgroups, establezca este valor en `0`.

<div id="compiled_expression_cache_elements_size">
  ## compiled_expression_cache_elements_size
</div>

<SettingsInfoBlock type="UInt64" default_value="10000" changeable_without_restart="No" />Establece el tamaño de la caché (en elementos) de las [expresiones compiladas](/es/concepts/features/performance/caches/caches).

<div id="compiled_expression_cache_size">
  ## compiled_expression_cache_size
</div>

<SettingsInfoBlock type="UInt64" default_value="134217728" changeable_without_restart="No" />Define el tamaño de la caché (en bytes) para las [expresiones compiladas](/es/concepts/features/performance/caches/caches).

<div id="compression">
  ## compresión
</div>

Ajustes de compresión de datos para las tablas con motor [MergeTree](/es/reference/engines/table-engines/mergetree-family/mergetree).

<Note>
  Recomendamos no cambiar esto si acabas de empezar con ClickHouse.
</Note>

**Plantilla de configuración**:

```xml
<compression>
    <case>
      <min_part_size>...</min_part_size>
      <min_part_size_ratio>...</min_part_size_ratio>
      <method>...</method>
      <level>...</level>
    </case>
    ...
</compression>
```

**Campos de `<case>`**:

* `min_part_size` – El tamaño mínimo de una parte de datos.
* `min_part_size_ratio` – La proporción entre el tamaño de la parte de datos y el tamaño de la tabla.
* `method` – Método de compresión. Valores aceptables: `lz4`, `lz4hc`, `zstd`,`deflate_qpl`.
* `level` – Nivel de compresión. Consulte [Códecs](/es/reference/statements/create/table#general-purpose-codecs).

<Note>
  Puede configurar varias secciones `<case>`.
</Note>

**Acciones cuando se cumplen las condiciones**:

* Si una parte de datos cumple un conjunto de condiciones, ClickHouse usa el método de compresión especificado.
* Si una parte de datos cumple varios conjuntos de condiciones, ClickHouse usa el primer conjunto de condiciones que coincida.

<Note>
  Si una parte de datos no cumple ninguna condición, ClickHouse usa la compresión `lz4`.
</Note>

**Ejemplo**

```xml
<compression incl="clickhouse_compression">
    <case>
        <min_part_size>10000000000</min_part_size>
        <min_part_size_ratio>0.01</min_part_size_ratio>
        <method>zstd</method>
        <level>1</level>
    </case>
</compression>
```

<div id="concurrent_threads_scheduler">
  ## concurrent_threads_scheduler
</div>

<SettingsInfoBlock type="String" default_value="max_min_fair" changeable_without_restart="Sí" />

La política para planificar los slots de CPU especificados por `concurrent_threads_soft_limit_num` y `concurrent_threads_soft_limit_ratio_to_cores`. Algoritmo utilizado para controlar cómo se distribuye un número limitado de slots de CPU entre consultas concurrentes. El scheduler puede cambiarse en tiempo de ejecución sin reiniciar el servidor.

Valores posibles:

* `round_robin` — Cada consulta con la configuración `use_concurrency_control` = 1 asigna hasta `max_threads` slots de CPU. Un slot por hilo. Cuando hay contención, los slots de CPU se conceden a las consultas mediante round-robin. Tenga en cuenta que el primer slot se concede incondicionalmente, lo que puede provocar una distribución poco equitativa y aumentar la latencia de las consultas con un `max_threads` alto cuando hay un gran número de consultas con `max_threads` = 1.
* `fair_round_robin` — Cada consulta con la configuración `use_concurrency_control` = 1 asigna hasta `max_threads - 1` slots de CPU. Variante de `round_robin` que no requiere un slot de CPU para el primer hilo de cada consulta. De este modo, las consultas con `max_threads` = 1 no requieren ningún slot y no pueden acaparar injustamente todos los slots. No se concede ningún slot incondicionalmente.
* `max_min_fair` — Cada consulta con la configuración `use_concurrency_control` = 1 asigna hasta `max_threads - 1` slots de CPU. Similar a `fair_round_robin`, pero los slots liberados siempre se conceden a la consulta con el menor número de slots asignados en ese momento. Esto proporciona una mayor equidad en situaciones de sobresuscripción elevada, donde muchas consultas compiten por slots de CPU limitados. Las consultas de corta duración no se ven penalizadas por las consultas de larga duración que han acumulado más slots con el tiempo.

<div id="concurrent_threads_soft_limit_num">
  ## concurrent_threads_soft_limit_num
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Sí" />

El número máximo de hilos de procesamiento de consultas, excluidos los hilos usados para recuperar datos de servidores remotos, que pueden utilizarse para ejecutar todas las consultas. No es un límite estricto. Si se alcanza el límite, la consulta seguirá recibiendo al menos un hilo para ejecutarse. La consulta puede ampliarse hasta el número deseado de hilos durante la ejecución si hay más hilos disponibles.

<Note>
  Un valor de `0` (predeterminado) significa que no hay límite.
</Note>

<div id="concurrent_threads_soft_limit_ratio_to_cores">
  ## concurrent_threads_soft_limit_ratio_to_cores
</div>

<SettingsInfoBlock type="UInt64" default_value="2" changeable_without_restart="Yes" />Igual que [`concurrent_threads_soft_limit_num`](#concurrent_threads_soft_limit_num), pero como proporción respecto a los núcleos.

<div id="config-file">
  ## config-file
</div>

<SettingsInfoBlock type="String" default_value="config.xml" changeable_without_restart="No" />Indica la ruta al archivo de configuración del servidor.

<div id="config_reload_interval_ms">
  ## config_reload_interval_ms
</div>

<SettingsInfoBlock type="UInt64" default_value="2000" changeable_without_restart="Sí" />

Frecuencia con la que ClickHouse volverá a cargar la configuración y comprobará si hay nuevos cambios

<div id="core_dump">
  ## core_dump
</div>

Configura el límite blando del tamaño del archivo de volcado de memoria.

<Note>
  El límite duro se configura mediante herramientas del sistema
</Note>

**Ejemplo**

```xml
<core_dump>
     <size_limit>1073741824</size_limit>
</core_dump>
```

<div id="cpu_slot_preemption">
  ## cpu_slot_preemption
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="Sí" />

Define cómo se realiza la planificación de cargas de trabajo para los recursos de CPU (hilo principal y hilo de trabajo).

* Si es `true` (predeterminado), la contabilización se basa en el tiempo de CPU realmente consumido. Se asigna una cantidad equitativa de tiempo de CPU a las cargas de trabajo en competencia. Los slots se asignan por un tiempo limitado y deben volver a solicitarse al expirar. La solicitud de slots puede bloquear la ejecución del hilo en caso de sobrecarga de recursos de CPU; es decir, puede producirse preempción. Esto garantiza la equidad en el uso del tiempo de CPU.
* Si es `false`, la contabilización se basa en el número de slots de CPU asignados. Se asigna una cantidad equitativa de slots de CPU a las cargas de trabajo en competencia. Un slot se asigna cuando se inicia un hilo, se mantiene de forma continua y se libera cuando el hilo finaliza su ejecución. El número de hilos asignados para la ejecución de consultas solo puede aumentar de 1 a `max_threads` y nunca disminuir. Esto favorece más a las consultas de larga duración y puede provocar inanición de CPU en las consultas cortas.

**Ejemplo**

```xml
<cpu_slot_preemption>true</cpu_slot_preemption>
```

**Véase también**

* [Planificación de cargas de trabajo](/es/concepts/features/configuration/server-config/workload-scheduling)

<div id="cpu_slot_preemption_timeout_ms">
  ## cpu_slot_preemption_timeout_ms
</div>

<SettingsInfoBlock type="UInt64" default_value="1000" changeable_without_restart="Yes" />

Define cuántos milisegundos puede esperar un worker thread durante la expulsión preventiva, es decir, mientras espera a que se le asigne otro slot de CPU. Tras este timeout, si el thread no puede adquirir un nuevo slot de CPU, finalizará y la consulta se ajustará dinámicamente a un menor número de hilos que se ejecutan de forma concurrente. Tenga en cuenta que el master thread nunca se reduce, aunque puede quedar desalojado de forma indefinida. Solo tiene sentido cuando `cpu_slot_preemption` está habilitado y el recurso de CPU está definido para hilo de trabajo.

**Ejemplo**

```xml
<cpu_slot_preemption_timeout_ms>1000</cpu_slot_preemption_timeout_ms>
```

**Véase también**

* [Planificación de cargas de trabajo](/es/concepts/features/configuration/server-config/workload-scheduling)

<div id="cpu_slot_quantum_ns">
  ## cpu_slot_quantum_ns
</div>

<SettingsInfoBlock type="UInt64" default_value="10000000" changeable_without_restart="Sí" />

Define cuántos nanosegundos de CPU puede consumir un hilo después de adquirir un slot de CPU y antes de tener que solicitar otro slot de CPU. Solo tiene sentido si `cpu_slot_preemption` está habilitado y el recurso de CPU está definido para HILO PRINCIPAL o HILO DE TRABAJO.

**Ejemplo**

```xml
<cpu_slot_quantum_ns>10000000</cpu_slot_quantum_ns>
```

**Véase también**

* [Planificación de cargas de trabajo](/es/concepts/features/configuration/server-config/workload-scheduling)

<div id="crash_log">
  ## crash_log
</div>

Configuración para el funcionamiento de la tabla del sistema [crash&#95;log](/es/reference/system-tables/crash_log).

La siguiente configuración puede definirse mediante subetiquetas:

| Configuración                      | Descripción                                                                                                                                                                  | Predeterminado      | Nota                                                                                                                                          |
| ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| `buffer_size_rows_flush_threshold` | Umbral para la cantidad de líneas. Si se alcanza este umbral, el volcado de los logs al disco se inicia en segundo plano.                                                    | `max_size_rows / 2` |                                                                                                                                               |
| `database`                         | Nombre de la base de datos.                                                                                                                                                  |                     |                                                                                                                                               |
| `engine`                           | [Definición del motor MergeTree](/es/reference/engines/table-engines/mergetree-family/mergetree#table_engine-mergetree-creating-a-table) para una tabla del sistema.            |                     | No puede usarse si se define `partition_by` u `order_by`. Si no se especifica, se selecciona `MergeTree` de forma predeterminada              |
| `flush_interval_milliseconds`      | Intervalo para volcar los datos del búfer en memoria a la tabla.                                                                                                             | `7500`              |                                                                                                                                               |
| `flush_on_crash`                   | Establece si los logs deben volcarse al disco en caso de fallo.                                                                                                              | `false`             |                                                                                                                                               |
| `max_size_rows`                    | Tamaño máximo en líneas para los logs. Cuando la cantidad de logs no volcados alcanza `max&#95;size`, los logs se vuelcan al disco.                                          | `1024`              |                                                                                                                                               |
| `order_by`                         | [Clave de ordenación personalizada](/es/reference/engines/table-engines/mergetree-family/mergetree#order_by) para una tabla del sistema. No puede usarse si se define `engine`. |                     | Si se especifica `engine` para la tabla del sistema, el parámetro `order_by` debe especificarse directamente dentro de &#39;engine&#39;       |
| `partition_by`                     | [Clave de particionado personalizada](/es/reference/engines/table-engines/mergetree-family/custom-partitioning-key) para una tabla del sistema.                                 |                     | Si se especifica `engine` para la tabla del sistema, el parámetro `partition_by` debe especificarse directamente dentro de &#39;engine&#39;   |
| `reserved_size_rows`               | Tamaño de memoria preasignado en líneas para los logs.                                                                                                                       | `1024`              |                                                                                                                                               |
| `settings`                         | [Parámetros adicionales](/es/reference/engines/table-engines/mergetree-family/mergetree#settings) que controlan el comportamiento de MergeTree (opcional).                      |                     | Si se especifica `engine` para la tabla del sistema, el parámetro `settings` debe especificarse directamente dentro de &#39;engine&#39;       |
| `storage_policy`                   | Nombre de la política de almacenamiento que se usará para la tabla (opcional).                                                                                               |                     | Si se especifica `engine` para la tabla del sistema, el parámetro `storage_policy` debe especificarse directamente dentro de &#39;engine&#39; |
| `table`                            | Nombre de la tabla del sistema.                                                                                                                                              |                     |                                                                                                                                               |
| `ttl`                              | Especifica el [TTL](/es/reference/engines/table-engines/mergetree-family/mergetree#table_engine-mergetree-ttl) de la tabla.                                                     |                     | Si se especifica `engine` para la tabla del sistema, el parámetro `ttl` debe especificarse directamente dentro de &#39;engine&#39;            |

El archivo de configuración del servidor predeterminado `config.xml` contiene la siguiente sección de configuración:

```xml
<crash_log>
    <database>system</database>
    <table>crash_log</table>
    <partition_by>toYYYYMM(event_date)</partition_by>
    <flush_interval_milliseconds>7500</flush_interval_milliseconds>
    <max_size_rows>1024</max_size_rows>
    <reserved_size_rows>1024</reserved_size_rows>
    <buffer_size_rows_flush_threshold>512</buffer_size_rows_flush_threshold>
    <flush_on_crash>false</flush_on_crash>
</crash_log>
```

<div id="custom_cached_disks_base_directory">
  ## custom_cached_disks_base_directory
</div>

Esta configuración especifica la ruta de caché para los discos con caché personalizados (creados desde SQL).
`custom_cached_disks_base_directory` tiene prioridad sobre `filesystem_caches_path` para los discos personalizados (se encuentra en `filesystem_caches_path.xml`),
que se usa si el primero no está presente.
La ruta de configuración de la caché del sistema de archivos debe estar dentro de ese directorio;
de lo contrario, se generará una excepción que impedirá crear el disco.

<Note>
  Esto no afectará a los discos creados en una versión anterior para los que se haya actualizado el servidor.
  En este caso, no se generará ninguna excepción, para permitir que el servidor se inicie correctamente.
</Note>

Ejemplo:

```xml
<custom_cached_disks_base_directory>/var/lib/clickhouse/caches/</custom_cached_disks_base_directory>
```

<div id="custom_settings_prefixes">
  ## custom_settings_prefixes
</div>

Lista de prefijos utilizados para los [ajustes personalizados](/es/concepts/features/configuration/settings/settings-query-level#custom_settings).
Los prefijos múltiples deben separarse con comas.

**Ejemplo**

```xml
<custom_settings_prefixes>SQL_</custom_settings_prefixes>
```

**Véase también**

* [Ajustes personalizados](/es/concepts/features/configuration/settings/settings-query-level#custom_settings)

<div id="database_atomic_delay_before_drop_table_sec">
  ## database_atomic_delay_before_drop_table_sec
</div>

<SettingsInfoBlock type="UInt64" default_value="480" changeable_without_restart="No" />

El tiempo durante el cual una tabla eliminada puede restaurarse mediante la sentencia [`UNDROP`](/es/reference/statements/undrop). Si `DROP TABLE` se ejecutó con el modificador `SYNC`, esta configuración se ignora.
El valor predeterminado de esta configuración es `480` (8 minutos).

<div id="database_catalog_drop_error_cooldown_sec">
  ## database_catalog_drop_error_cooldown_sec
</div>

<SettingsInfoBlock type="UInt64" default_value="5" changeable_without_restart="No" />En caso de que falle la eliminación de una tabla, ClickHouse esperará este intervalo antes de reintentar la operación.

<div id="database_catalog_drop_table_concurrency">
  ## database_catalog_drop_table_concurrency
</div>

<SettingsInfoBlock type="UInt64" default_value="16" changeable_without_restart="No" />El tamaño del pool de hilos utilizado para eliminar tablas.

<div id="database_catalog_unused_dir_cleanup_period_sec">
  ## database_catalog_unused_dir_cleanup_period_sec
</div>

<SettingsInfoBlock type="UInt64" default_value="86400" changeable_without_restart="No" />

Parámetro de una tarea que limpia los residuos del directorio `store/`.
Establece el período de programación de la tarea.

<Note>
  Un valor de `0` significa &quot;nunca&quot;. El valor predeterminado corresponde a 1 día.
</Note>

<div id="database_catalog_unused_dir_hide_timeout_sec">
  ## database_catalog_unused_dir_hide_timeout_sec
</div>

<SettingsInfoBlock type="UInt64" default_value="3600" changeable_without_restart="No" />

Parámetro de una tarea que elimina basura del directorio `store/`.
Si algún subdirectorio no está siendo utilizado por clickhouse-server y este directorio no se ha modificado en los últimos
[`database_catalog_unused_dir_hide_timeout_sec`](/es/reference/settings/server-settings/settings#database_catalog_unused_dir_hide_timeout_sec) segundos, la tarea &quot;ocultará&quot; este directorio
quitando todos los permisos de acceso. Esto también funciona para directorios que clickhouse-server no
espera encontrar dentro de `store/`.

<Note>
  Un valor de `0` significa &quot;inmediatamente&quot;.
</Note>

<div id="database_catalog_unused_dir_rm_timeout_sec">
  ## database_catalog_unused_dir_rm_timeout_sec
</div>

<SettingsInfoBlock type="UInt64" default_value="2592000" changeable_without_restart="No" />

Parámetro de una tarea que limpia los residuos del directorio `store/`.
Si algún subdirectorio no es utilizado por clickhouse-server y anteriormente fue &quot;ocultado&quot;
(consulte [database&#95;catalog&#95;unused&#95;dir&#95;hide&#95;timeout&#95;sec](/es/reference/settings/server-settings/settings#database_catalog_unused_dir_hide_timeout_sec))
y este directorio no se ha modificado durante los últimos
[`database_catalog_unused_dir_rm_timeout_sec`]/operations/server-configuration-parameters/settings#database&#95;catalog&#95;unused&#95;dir&#95;rm&#95;timeout&#95;sec) segundos, la tarea eliminará este directorio.
También funciona para directorios que clickhouse-server no
espera encontrar dentro de `store/`.

<Note>
  Un valor de `0` significa &quot;nunca&quot;. El valor predeterminado equivale a 30 días.
</Note>

<div id="database_replicated_allow_detach_permanently">
  ## database_replicated_allow_detach_permanently
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />Permite desvincular tablas de forma permanente en bases de datos Replicated

<div id="database_replicated_drop_broken_tables">
  ## database_replicated_drop_broken_tables
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Eliminar las tablas inesperadas de las bases de datos Replicated en lugar de moverlas a una base de datos local independiente

<div id="dead_letter_queue">
  ## dead_letter_queue
</div>

Configuración para la tabla del sistema &#39;dead&#95;letter&#95;queue&#39;.

<SystemLogParameters />

La configuración predeterminada es:

```xml
<dead_letter_queue>
    <database>system</database>
    <table>dead_letter</table>
    <partition_by>toYYYYMM(event_date)</partition_by>
    <flush_interval_milliseconds>7500</flush_interval_milliseconds>
</dead_letter_queue>
```

<div id="default_database">
  ## default_database
</div>

<SettingsInfoBlock type="String" default_value="default" changeable_without_restart="No" />El nombre predeterminado de la base de datos.

<div id="default_password_type">
  ## default_password_type
</div>

Establece el tipo de contraseña que se asignará automáticamente en consultas como `CREATE USER u IDENTIFIED BY 'p'`.

Los valores aceptados son:

* `plaintext_password`
* `sha256_password`
* `double_sha1_password`
* `bcrypt_password`

```xml
<default_password_type>sha256_password</default_password_type>
```

<div id="default_profile">
  ## default_profile
</div>

Perfil de configuración predeterminado. Los perfiles de configuración se encuentran en el archivo especificado en la configuración `user_config`.

**Ejemplo**

```xml
<default_profile>default</default_profile>
```

<div id="default_replica_name">
  ## default_replica_name
</div>

<SettingsInfoBlock type="String" default_value="{replica}" changeable_without_restart="No" />

El nombre de la réplica en ZooKeeper.

**Ejemplo**

```xml
<default_replica_name>{replica}</default_replica_name>
```

<div id="default_replica_path">
  ## default_replica_path
</div>

<SettingsInfoBlock type="String" default_value="/clickhouse/tables/{uuid}/{shard}" changeable_without_restart="No" />

La ruta de la tabla en ZooKeeper.

**Ejemplo**

```xml
<default_replica_path>/clickhouse/tables/{uuid}/{shard}</default_replica_path>
```

<div id="default_session_timeout">
  ## default_session_timeout
</div>

Tiempo de espera de sesión predeterminado, en segundos.

```xml
<default_session_timeout>60</default_session_timeout>
```

<div id="dictionaries_config">
  ## dictionaries_config
</div>

Ruta del archivo de configuración de los diccionarios.

Ruta:

* Especifique la ruta absoluta o la ruta relativa al archivo de configuración del servidor.
* La ruta puede contener comodines * y ?.

Véase también:

* &quot;[Diccionarios](/es/reference/statements/create/dictionary)&quot;.

**Ejemplo**

```xml
<dictionaries_config>*_dictionary.xml</dictionaries_config>
```

<div id="dictionaries_lazy_load">
  ## dictionaries_lazy_load
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />

Carga diferida de los diccionarios.

* Si es `true`, cada diccionario se carga cuando se usa por primera vez. Si la carga falla, la función que estaba usando el diccionario lanza una excepción.
* Si es `false`, el servidor carga todos los diccionarios al iniciarse.

<Note>
  El servidor esperará durante el inicio hasta que todos los diccionarios terminen de cargarse antes de aceptar conexiones
  (excepción: si [`wait_dictionaries_load_at_startup`](/es/reference/settings/server-settings/settings#wait_dictionaries_load_at_startup) está establecido en `false`).
</Note>

**Ejemplo**

```xml
<dictionaries_lazy_load>true</dictionaries_lazy_load>
```

<div id="dictionaries_lib_path">
  ## dictionaries_lib_path
</div>

<SettingsInfoBlock type="String" default_value="/var/lib/clickhouse/dictionaries_lib/" changeable_without_restart="No" />

El directorio de la biblioteca de diccionarios.

**Ejemplo**

```xml
<dictionaries_lib_path>/var/lib/clickhouse/dictionaries_lib/</dictionaries_lib_path>
```

<div id="dictionary_background_reconnect_interval">
  ## dictionary_background_reconnect_interval
</div>

<SettingsInfoBlock type="UInt64" default_value="1000" changeable_without_restart="No" />Intervalo en milisegundos entre intentos de reconexión para diccionarios MySQL y Postgres fallidos con `background_reconnect` habilitado.

<div id="disable_insertion_and_mutation">
  ## disable_insertion_and_mutation
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />

Deshabilita las consultas insert/alter/delete. Esta configuración se habilita cuando se necesitan nodos de solo lectura para evitar que las inserciones y las mutaciones afecten al rendimiento de lectura. Las inserciones en motores externos (S3, DataLake, MySQL, PostrgeSQL, Kafka, etc.) se permiten a pesar de esta configuración.

<div id="disable_internal_dns_cache">
  ## disable_internal_dns_cache
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Desactiva la caché DNS interna. Se recomienda para ejecutar ClickHouse en sistemas con infraestructura que cambia con frecuencia, como Kubernetes.

<div id="disable_tunneling_for_https_requests_over_http_proxy">
  ## disable_tunneling_for_https_requests_over_http_proxy
</div>

De forma predeterminada, se usa tunelización (es decir, `HTTP CONNECT`) para realizar solicitudes `HTTPS` a través de un proxy `HTTP`. Esta configuración puede usarse para desactivarla.

**no&#95;proxy**

De forma predeterminada, todas las solicitudes pasarán por el proxy. Para desactivarlo en hosts específicos, se debe establecer la variable `no_proxy`.
Puede establecerse dentro de la cláusula `<proxy>` para los resolvers de lista y remotos, y como variable de entorno para el resolver de entorno.
Admite direcciones IP, dominios, subdominios y el comodín `'*'` para omitirlo por completo. Los puntos iniciales se eliminan, igual que hace curl.

**Ejemplo**

La siguiente configuración omite el proxy para `clickhouse.cloud` y todos sus subdominios (por ejemplo, `auth.clickhouse.cloud`).
Lo mismo se aplica a GitLab, aunque tenga un punto inicial. Tanto `gitlab.com` como `about.gitlab.com` omitirían el proxy.

```xml
<proxy>
    <no_proxy>clickhouse.cloud,.gitlab.com</no_proxy>
    <http>
        <uri>http://proxy1</uri>
        <uri>http://proxy2:3128</uri>
    </http>
    <https>
        <uri>http://proxy1:3128</uri>
    </https>
</proxy>
```

<div id="disk_connections_hard_limit">
  ## disk_connections_hard_limit
</div>

<SettingsInfoBlock type="UInt64" default_value="200000" changeable_without_restart="No" />Se lanza una excepción al intentar crear una conexión cuando se alcanza este límite. Establezca el valor en 0 para desactivar la limitación estricta. El límite se aplica a las conexiones de los discos.

<div id="disk_connections_rcvbuf">
  ## disk_connections_rcvbuf
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Sí" />El tamaño de la opción SO&#95;RCVBUF para las conexiones de disco (S3, Azure, GCS). Si se establece en un valor superior a 0, sobrescribe el autoajuste TCP del kernel para el búfer de recepción. 0 = valor predeterminado del kernel (autoajuste). Nota: al volver a cambiar este ajuste a 0, el autoajuste solo se restablece para las conexiones nuevas; las conexiones existentes del pool conservan tamaños de búfer fijos hasta que se vuelvan a crear.

<div id="disk_connections_sndbuf">
  ## disk_connections_sndbuf
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Yes" />El tamaño de la opción SO&#95;SNDBUF para las conexiones de disco (S3, Azure, GCS). Si se establece en un valor superior a 0, anula el autotuning de TCP del kernel para el búfer de envío. 0 = valor predeterminado del kernel (autotuning). Nota: volver a cambiar esta configuración a 0 restaura el autotuning solo para las conexiones creadas recientemente; las conexiones existentes del pool conservan tamaños de búfer fijos hasta que se vuelven a crear.

<div id="disk_connections_soft_limit">
  ## disk_connections_soft_limit
</div>

<SettingsInfoBlock type="UInt64" default_value="5000" changeable_without_restart="No" />Las conexiones que superen este límite tienen un tiempo de vida significativamente menor. El límite se aplica a las conexiones de disco.

<div id="disk_connections_store_limit">
  ## disk_connections_store_limit
</div>

<SettingsInfoBlock type="UInt64" default_value="10000" changeable_without_restart="No" />Las conexiones que superen este límite se restablecen después de usarse. Establezca 0 para desactivar la caché de conexiones. El límite se aplica a las conexiones de los discos.

<div id="disk_connections_warn_limit">
  ## disk_connections_warn_limit
</div>

<SettingsInfoBlock type="UInt64" default_value="8000" changeable_without_restart="No" />Se escriben mensajes de advertencia en los logs si el número de conexiones en uso supera este límite. El límite se aplica a las conexiones de los discos.

<div id="disk_transaction_wait_for_blob_removal">
  ## disk_transaction_wait_for_blob_removal
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />

Valor predeterminado de la configuración `wait_for_blob_removal` para cada disco.
Cuando está habilitada, el servidor espera a que finalice la eliminación de blobs en segundo plano antes de confirmar la operación.

<div id="display_secrets_in_show_and_select">
  ## display_secrets_in_show_and_select
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />

Habilita o deshabilita la visualización de secretos en las consultas `SHOW` y `SELECT` para tablas, bases de datos, funciones de tabla y diccionarios.

El usuario que desee ver secretos también debe tener habilitada la
[`configuración de formato format_display_secrets_in_show_and_select`](/es/reference/settings/formats#format_display_secrets_in_show_and_select)
y contar con el privilegio
[`displaySecretsInShowAndSelect`](/es/reference/statements/grant#displaysecretsinshowandselect).

Valores posibles:

* `0` — Desactivado.
* `1` — Activado.

<div id="distributed_cache_apply_throttling_settings_from_client">
  ## distributed_cache_apply_throttling_settings_from_client
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />Indica si el servidor de caché debe aplicar la configuración de limitación de velocidad recibida del cliente.

<div id="distributed_cache_keep_up_free_connections_ratio">
  ## distributed_cache_keep_up_free_connections_ratio
</div>

<SettingsInfoBlock type="Float" default_value="0.1" changeable_without_restart="No" />Límite no estricto del número de conexiones activas que Distributed Cache intentará mantener libres. Cuando el número de conexiones libres caiga por debajo de distributed&#95;cache&#95;keep&#95;up&#95;free&#95;connections&#95;ratio * max&#95;connections, se cerrarán las conexiones con la actividad más antigua hasta que el número vuelva a superar el límite.

<div id="distributed_ddl">
  ## distributed_ddl
</div>

Gestiona la ejecución de [consultas DDL distribuidas](/es/reference/statements/distributed-ddl) (`CREATE`, `DROP`, `ALTER`, `RENAME`) en el clúster.
Solo funciona si [ZooKeeper](/es/reference/settings/server-settings/settings#zookeeper) está habilitado.

Los ajustes configurables dentro de `<distributed_ddl>` incluyen:

| Configuración          | Descripción                                                                                                                                        | Valor predeterminado                        |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- |
| `cleanup_delay_period` | la limpieza comienza después de recibir un evento de nodo nuevo si la última limpieza no se realizó hace menos de `cleanup_delay_period` segundos. | `60` segundos                               |
| `max_tasks_in_queue`   | el número máximo de tareas que puede haber en la cola.                                                                                             | `1,000`                                     |
| `path`                 | la ruta en Keeper para la `task_queue` de las consultas DDL                                                                                        |                                             |
| `pool_size`            | cuántas consultas `ON CLUSTER` pueden ejecutarse simultáneamente                                                                                   |                                             |
| `profile`              | el perfil usado para ejecutar las consultas DDL                                                                                                    |                                             |
| `task_max_lifetime`    | elimina el nodo si su antigüedad supera este valor.                                                                                                | `7 * 24 * 60 * 60` (una semana en segundos) |

**Ejemplo**

```xml
<distributed_ddl>
    <!-- Ruta en ZooKeeper hacia la cola con consultas DDL -->
    <path>/clickhouse/task_queue/ddl</path>

    <!-- La configuración de este perfil se usará para ejecutar consultas DDL -->
    <profile>default</profile>

    <!-- Controla cuántas consultas ON CLUSTER pueden ejecutarse simultáneamente. -->
    <pool_size>1</pool_size>

    <!--
         Configuración de limpieza (las tareas activas no se eliminarán)
    -->

    <!-- Controla el TTL de las tareas (por defecto 1 semana) -->
    <task_max_lifetime>604800</task_max_lifetime>

    <!-- Controla con qué frecuencia se realiza la limpieza (en segundos) -->
    <cleanup_delay_period>60</cleanup_delay_period>

    <!-- Controla cuántas tareas pueden estar en la cola -->
    <max_tasks_in_queue>1000</max_tasks_in_queue>
</distributed_ddl>
```

<div id="distributed_ddl.cleanup_delay_period">
  ## distributed_ddl.cleanup_delay_period
</div>

<SettingsInfoBlock type="UInt64" default_value="60" changeable_without_restart="No" />la limpieza comienza después de recibir el evento de un nuevo nodo si la última limpieza no se realizó en los últimos `<cleanup_delay_period>` segundos.

<div id="distributed_ddl.max_tasks_in_queue">
  ## distributed_ddl.max_tasks_in_queue
</div>

<SettingsInfoBlock type="UInt64" default_value="1000" changeable_without_restart="No" />el número máximo de tareas que puede haber en la cola.

<div id="distributed_ddl.path">
  ## distributed_ddl.path
</div>

<SettingsInfoBlock type="String" default_value="/clickhouse/task_queue/ddl/" changeable_without_restart="No" />la ruta en Keeper para la `<task_queue>` de las consultas DDL

<div id="distributed_ddl.pool_size">
  ## distributed_ddl.pool_size
</div>

<SettingsInfoBlock type="Int32" default_value="1" changeable_without_restart="No" />cuántas consultas `<ON CLUSTER>` pueden ejecutarse simultáneamente

<div id="distributed_ddl.profile">
  ## distributed_ddl.profile
</div>

el perfil que se usa para ejecutar las consultas DDL

<div id="distributed_ddl.replicas_path">
  ## distributed_ddl.replicas_path
</div>

<SettingsInfoBlock type="String" default_value="/clickhouse/task_queue/replicas/" changeable_without_restart="No" />la ruta en Keeper de la `<task_queue>` para las réplicas

<div id="distributed_ddl.task_max_lifetime">
  ## distributed_ddl.task_max_lifetime
</div>

<SettingsInfoBlock type="UInt64" default_value="604800" changeable_without_restart="No" />elimina el nodo si su antigüedad supera este valor.

<div id="distributed_ddl_use_initial_user_and_roles">
  ## distributed_ddl_use_initial_user_and_roles
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Si está habilitada, las consultas ON CLUSTER conservarán y usarán el usuario y los roles del iniciador para ejecutarse en segmentos remotos. Esto garantiza un control de acceso uniforme en todo el clúster, pero requiere que el usuario y los roles existan en todos los nodos.

<div id="dns_allow_resolve_names_to_ipv4">
  ## dns_allow_resolve_names_to_ipv4
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="Sí" />Permite resolver nombres a direcciones IPv4.

<div id="dns_allow_resolve_names_to_ipv6">
  ## dns_allow_resolve_names_to_ipv6
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="Yes" />Permite que los nombres se resuelvan en direcciones IPv6.

<div id="dns_cache_max_entries">
  ## dns_cache_max_entries
</div>

<SettingsInfoBlock type="UInt64" default_value="10000" changeable_without_restart="No" />Número máximo de entradas en la caché DNS.

<div id="dns_cache_update_period">
  ## dns_cache_update_period
</div>

<SettingsInfoBlock type="Int32" default_value="15" changeable_without_restart="No" />Período de actualización de la caché interna de DNS en segundos.

<div id="dns_max_consecutive_failures">
  ## dns_max_consecutive_failures
</div>

<SettingsInfoBlock type="UInt32" default_value="5" changeable_without_restart="No" />

Detiene los intentos posteriores de actualizar la caché DNS de un hostname después de este número de fallos consecutivos. La información permanece en la caché DNS. Cero significa ilimitado.

**Véase también**

* [`SYSTEM DROP DNS CACHE`](/es/reference/statements/system#drop-dns-cache)

<div id="drop_distributed_cache_pool_size">
  ## drop_distributed_cache_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="8" changeable_without_restart="No" />El tamaño del pool de hilo utilizado para eliminar la distributed cache.

<div id="drop_distributed_cache_queue_size">
  ## drop_distributed_cache_queue_size
</div>

<SettingsInfoBlock type="UInt64" default_value="1000" changeable_without_restart="No" />El tamaño de la cola del pool de hilo utilizado para eliminar la distributed cache.

<div id="enable_azure_sdk_logging">
  ## enable_azure_sdk_logging
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Habilita el registro del SDK de Azure

<div id="enable_webterminal">
  ## enable_webterminal
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />Habilita la interfaz de terminal web en el endpoint HTTP `/webterminal`. Proporciona una sesión interactiva de `clickhouse-client` en el navegador mediante WebSocket. Cuando es `false`, las solicitudes a `/webterminal` devuelven el estado HTTP `403 Forbidden`.

<div id="encryption">
  ## cifrado
</div>

Configura un comando para obtener una clave que usarán los [códecs de cifrado](/es/reference/statements/create/table#encryption-codecs). La clave (o las claves) debe escribirse en variables de entorno o definirse en el archivo de configuración.

Las claves pueden estar en formato hexadecimal o ser cadenas de 16 bytes de longitud.

**Ejemplo**

Carga desde la configuración:

```xml
<encryption_codecs>
    <aes_128_gcm_siv>
        <key>1234567812345678</key>
    </aes_128_gcm_siv>
</encryption_codecs>
```

<Note>
  No se recomienda almacenar claves en el archivo de configuración. No es una práctica segura. Puede mover las claves a un archivo de configuración independiente en un disco seguro y colocar un enlace simbólico a ese archivo de configuración en la carpeta `config.d/`.
</Note>

Carga desde la configuración cuando la clave está en hexadecimal:

```xml
<encryption_codecs>
    <aes_128_gcm_siv>
        <key_hex>00112233445566778899aabbccddeeff</key_hex>
    </aes_128_gcm_siv>
</encryption_codecs>
```

Cargando la clave desde la variable de entorno:

```xml
<encryption_codecs>
    <aes_128_gcm_siv>
        <key_hex from_env="ENVVAR"></key_hex>
    </aes_128_gcm_siv>
</encryption_codecs>
```

Aquí, `current_key_id` establece la clave actual de cifrado, y todas las claves especificadas pueden usarse para el descifrado.

Cada uno de estos métodos puede aplicarse a varias claves:

```xml
<encryption_codecs>
    <aes_128_gcm_siv>
        <key_hex id="0">00112233445566778899aabbccddeeff</key_hex>
        <key_hex id="1" from_env="ENVVAR"></key_hex>
        <current_key_id>1</current_key_id>
    </aes_128_gcm_siv>
</encryption_codecs>
```

Aquí, `current_key_id` muestra la clave actual de cifrado.

Además, los usuarios pueden añadir un nonce que debe tener una longitud de 12 bytes (de forma predeterminada, los procesos de cifrado y descifrado usan un nonce compuesto por bytes de valor cero):

```xml
<encryption_codecs>
    <aes_128_gcm_siv>
        <nonce>012345678910</nonce>
    </aes_128_gcm_siv>
</encryption_codecs>
```

O puede establecerse en hexadecimal:

```xml
<encryption_codecs>
    <aes_128_gcm_siv>
        <nonce_hex>abcdefabcdef</nonce_hex>
    </aes_128_gcm_siv>
</encryption_codecs>
```

<Note>
  Todo lo mencionado anteriormente puede aplicarse a `aes_256_gcm_siv` (pero la clave debe tener una longitud de 32 bytes).
</Note>

<div id="enforce_keeper_component_tracking">
  ## enforce_keeper_component_tracking
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />

Si está habilitado, cada solicitud a ZooKeeper debe tener un nombre de componente establecido mediante `Coordination::setCurrentComponent`. Lanza una excepción `LOGICAL_ERROR` si falta el componente.

<div id="error_log">
  ## error_log
</div>

Está desactivado de forma predeterminada.

**Activación**

Para activar manualmente la recopilación del historial de errores [`system.error_log`](/es/reference/system-tables/error_log), cree `/etc/clickhouse-server/config.d/error_log.xml` con el siguiente contenido:

```xml
<clickhouse>
    <error_log>
        <database>system</database>
        <table>error_log</table>
        <flush_interval_milliseconds>7500</flush_interval_milliseconds>
        <collect_interval_milliseconds>1000</collect_interval_milliseconds>
        <max_size_rows>1048576</max_size_rows>
        <reserved_size_rows>8192</reserved_size_rows>
        <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
        <flush_on_crash>false</flush_on_crash>
    </error_log>
</clickhouse>
```

**Deshabilitar**

Para deshabilitar la opción `error_log`, debe crear el siguiente archivo `/etc/clickhouse-server/config.d/disable_error_log.xml` con el siguiente contenido:

```xml
<clickhouse>
    <error_log remove="1" />
</clickhouse>
```

<SystemLogParameters />

<div id="filesystem_caches_path">
  ## filesystem_caches_path
</div>

Este ajuste especifica la ruta de la caché.

**Ejemplo**

```xml
<filesystem_caches_path>/var/lib/clickhouse/filesystem_caches/</filesystem_caches_path>
```

<div id="format_parsing_thread_pool_queue_size">
  ## format_parsing_thread_pool_queue_size
</div>

<SettingsInfoBlock type="UInt64" default_value="10000" changeable_without_restart="Sí" />

La cantidad máxima de trabajos que se pueden programar en el thread pool para analizar la entrada.

<Note>
  Un valor de `0` significa ilimitado.
</Note>

<div id="format_schema_path">
  ## format_schema_path
</div>

<SettingsInfoBlock type="String" default_value="/var/lib/clickhouse/format_schemas/" changeable_without_restart="No" />

La ruta al directorio que contiene los esquemas de los datos de entrada, como los esquemas del formato [CapnProto](/es/reference/formats/CapnProto).

**Ejemplo**

```xml
<!-- Directorio que contiene archivos de esquema para varios formatos de entrada. -->
<format_schema_path>/var/lib/clickhouse/format_schemas/</format_schema_path>
```

<div id="global_profiler_cpu_time_period_ns">
  ## global_profiler_cpu_time_period_ns
</div>

<SettingsInfoBlock type="UInt64" default_value="10000000000" changeable_without_restart="No" />Período del temporizador del reloj de la CPU del perfilador global (en nanosegundos). Establezca el valor en 0 para desactivar el perfilador global del reloj de la CPU. El valor recomendado es de al menos 10000000 (100 veces por segundo) para consultas individuales o 1000000000 (una vez por segundo) para el perfilado en todo el clúster.

<div id="global_profiler_real_time_period_ns">
  ## global_profiler_real_time_period_ns
</div>

<SettingsInfoBlock type="UInt64" default_value="10000000000" changeable_without_restart="No" />Período del temporizador de reloj en tiempo real del perfilador global (en nanosegundos). Establezca el valor en 0 para desactivar el perfilador global de reloj en tiempo real. El valor recomendado es de al menos 10000000 (100 veces por segundo) para consultas individuales o 1000000000 (una vez por segundo) para el perfilado de todo el clúster.

<div id="google_protos_path">
  ## google_protos_path
</div>

<SettingsInfoBlock type="String" default_value="/usr/share/clickhouse/protos/" changeable_without_restart="No" />

Define un directorio que contiene archivos .proto para tipos Protobuf.

**Ejemplo**

```xml
<google_protos_path>/usr/share/clickhouse/protos/</google_protos_path>
```

<div id="graphite">
  ## graphite
</div>

Envío de datos a [Graphite](https://github.com/graphite-project).

Configuración:

* `host` – El servidor de Graphite.
* `port` – El puerto del servidor de Graphite.
* `interval` – El intervalo de envío, en segundos.
* `timeout` – El tiempo de espera para el envío de datos, en segundos.
* `root_path` – Prefijo para las claves.
* `metrics` – Envío de datos de la tabla [system.metrics](/es/reference/system-tables/metrics).
* `events` – Envío de datos delta acumulados durante el período de tiempo desde la tabla [system.events](/es/reference/system-tables/events).
* `events_cumulative` – Envío de datos acumulados de la tabla [system.events](/es/reference/system-tables/events).
* `asynchronous_metrics` – Envío de datos de la tabla [system.asynchronous&#95;metrics](/es/reference/system-tables/asynchronous_metrics).

Se pueden configurar varias cláusulas `<graphite>`. Por ejemplo, puede usar esto para enviar distintos datos en intervalos diferentes.

**Ejemplo**

```xml
<graphite>
    <host>localhost</host>
    <port>42000</port>
    <timeout>0.1</timeout>
    <interval>60</interval>
    <root_path>one_min</root_path>
    <metrics>true</metrics>
    <events>true</events>
    <events_cumulative>false</events_cumulative>
    <asynchronous_metrics>true</asynchronous_metrics>
</graphite>
```

<div id="graphite_rollup">
  ## graphite_rollup
</div>

Configuración para reducir la densidad de los datos en Graphite.

Para obtener más detalles, consulte [GraphiteMergeTree](/es/reference/engines/table-engines/mergetree-family/graphitemergetree).

**Ejemplo**

```xml
<graphite_rollup_example>
    <default>
        <function>max</function>
        <retention>
            <age>0</age>
            <precision>60</precision>
        </retention>
        <retention>
            <age>3600</age>
            <precision>300</precision>
        </retention>
        <retention>
            <age>86400</age>
            <precision>3600</precision>
        </retention>
    </default>
</graphite_rollup_example>
```

<div id="handshake_timeout_milliseconds">
  ## handshake_timeout_milliseconds
</div>

<SettingsInfoBlock type="UInt64" default_value="30000" changeable_without_restart="No" />Tiempo de espera de reloj real, en milisegundos, para toda la fase de handshake de TCP (Hello + Addendum). Limita cuánto tiempo una conexión no autenticada puede retener un hilo. Establézcalo en 0 para desactivarlo.

<div id="hdfs.libhdfs3_conf">
  ## hdfs.libhdfs3_conf
</div>

Indica a libhdfs3 la ubicación correcta de su configuración.

<div id="hsts_max_age">
  ## hsts_max_age
</div>

Tiempo de caducidad de HSTS en segundos.

<Note>
  Un valor de `0` significa que ClickHouse desactiva HSTS. Si establece un número positivo, HSTS se activará y `max-age` será el número que haya establecido.
</Note>

**Ejemplo**

```xml
<hsts_max_age>600000</hsts_max_age>
```

<div id="http_connections_hard_limit">
  ## http_connections_hard_limit
</div>

<SettingsInfoBlock type="UInt64" default_value="200000" changeable_without_restart="No" />Se lanza una excepción al intentar crear una conexión cuando se alcanza este límite. Establézcalo en 0 para desactivar la limitación estricta. El límite se aplica a las conexiones HTTP que no están asociadas a ningún disco ni almacenamiento.

<div id="http_connections_rcvbuf">
  ## http_connections_rcvbuf
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Yes" />El tamaño de la opción SO&#95;RCVBUF para las conexiones HTTP en general. Si se establece en un valor superior a 0, anula el autoajuste de TCP del kernel para el búfer de recepción. 0 = valor predeterminado del kernel (autoajuste). Nota: al volver a cambiar esta configuración a 0, el autoajuste se restablece solo para las conexiones creadas recientemente; las conexiones existentes del pool conservan tamaños de búfer fijos hasta que se vuelvan a crear.

<div id="http_connections_sndbuf">
  ## http_connections_sndbuf
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Sí" />El tamaño de la opción SO&#95;SNDBUF para las conexiones HTTP generales. Si se establece en un valor superior a 0, anula el autotuning de TCP del kernel para el búfer de envío. 0 = valor predeterminado del kernel (autotuning). Nota: volver a cambiar esta configuración a 0 restaura el autotuning solo para las conexiones recién creadas; las conexiones existentes del pool conservan tamaños de búfer fijos hasta que se vuelvan a crear.

<div id="http_connections_soft_limit">
  ## http_connections_soft_limit
</div>

<SettingsInfoBlock type="UInt64" default_value="100" changeable_without_restart="No" />Las conexiones que superen este límite tienen una vida útil significativamente más corta. El límite se aplica a las conexiones HTTP que no pertenecen a ningún disco ni backend de almacenamiento.

<div id="http_connections_store_limit">
  ## http_connections_store_limit
</div>

<SettingsInfoBlock type="UInt64" default_value="1000" changeable_without_restart="No" />Las conexiones que superen este límite se reinician después de usarse. Establézcalo en 0 para desactivar la caché de conexiones. El límite se aplica a las conexiones HTTP que no están asociadas a ningún disco ni almacenamiento.

<div id="http_connections_warn_limit">
  ## http_connections_warn_limit
</div>

<SettingsInfoBlock type="UInt64" default_value="500" changeable_without_restart="No" />Se escriben mensajes de advertencia en los logs si el número de conexiones en uso es superior a este límite. El límite se aplica a las conexiones HTTP que no pertenecen a ningún disco ni almacenamiento.

<div id="http_handlers">
  ## http_handlers
</div>

Permite usar controladores HTTP personalizados.
Para añadir un nuevo controlador HTTP, simplemente agregue una nueva `<rule>`.
Las reglas se comprueban de arriba abajo en el orden en que se definen,
y la primera coincidencia ejecutará el controlador.
Una regla sin condiciones de coincidencia (solo `handler`) coincide con cada solicitud; dado que las reglas se comprueban en orden,
una regla de este tipo solo resulta útil como fallback al final.

Los siguientes ajustes se pueden configurar mediante subetiquetas (todas estas subetiquetas son opcionales excepto `handler`):

| Sub-tags             | Definition                                                                                                                                                                                                                                                                                                          |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `url`                | Para hacer coincidir la ruta URL de la solicitud. La query string se ignora al hacer la coincidencia                                                                                                                                                                                                                |
| `url_prefix`         | Para hacer coincidir la ruta URL de la solicitud con una ruta base: la propia ruta o cualquier elemento por debajo de ella en un límite de segmento de ruta (p. ej., &#39;/api/v1&#39; coincide con /api/v1, /api/v1/ y /api/v1/write, pero no con /api/v1beta). La query string se ignora al hacer la coincidencia |
| `url_regexp`         | Para hacer coincidir la ruta URL de la solicitud con una expresión regular. La query string se ignora al hacer la coincidencia                                                                                                                                                                                      |
| `full_url`           | Para hacer coincidir la URL completa de la solicitud `scheme://host:port/path`. La query string se ignora al hacer la coincidencia, y el host es la dirección IP de la conexión (no el encabezado `Host`)                                                                                                           |
| `full_url_prefix`    | Para hacer coincidir la URL completa de la solicitud `scheme://host:port/path` con la base URL `scheme://host:port/base_path`, en un límite de segmento de ruta (consulte `url_prefix`). La query string se ignora al hacer la coincidencia                                                                         |
| `full_url_regexp`    | Para hacer coincidir la URL completa de la solicitud `scheme://host:port/path` con una expresión regular. La query string se ignora al hacer la coincidencia                                                                                                                                                        |
| `methods`            | Para hacer coincidir los métodos de la solicitud, puede usar comas para separar varias coincidencias de métodos                                                                                                                                                                                                     |
| `headers`            | Para hacer coincidir los encabezados de la solicitud, haga coincidir cada elemento hijo (el nombre del elemento hijo es el nombre del encabezado)                                                                                                                                                                   |
| `headers_regexp`     | Igual que `headers`, pero el valor de cada elemento hijo se compara con una expresión regular                                                                                                                                                                                                                       |
| `empty_query_string` | Comprueba que no haya ninguna query string en la URL                                                                                                                                                                                                                                                                |
| `handler`            | El controlador de la solicitud (obligatorio)                                                                                                                                                                                                                                                                        |

<Note>
  En lugar de `url_regexp`, `full_url_regexp` y `headers_regexp`, también puede escribir una expresión regular en `url`, `full_url` o `headers` usando el prefijo `regex:` (por ejemplo, `<url>regex:/api/.*</url>`). Esto sigue siendo compatible por compatibilidad con versiones anteriores, pero es obsoleto: prefiera las subetiquetas específicas `url_regexp`, `full_url_regexp` y `headers_regexp`.
</Note>

`handler` contiene los siguientes ajustes, que se pueden configurar mediante subetiquetas:

| Sub-tags           | Definition                                                                                                                                                                                                     |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `url`              | Una ubicación para la redirección                                                                                                                                                                              |
| `type`             | Tipos compatibles: static, dynamic&#95;query&#95;handler, predefined&#95;query&#95;handler, redirect                                                                                                           |
| `status`           | Se usa con el tipo static; código de estado de la respuesta                                                                                                                                                    |
| `query_param_name` | Se usa con el tipo dynamic&#95;query&#95;handler; extrae y ejecuta el valor correspondiente a `<query_param_name>` en los parámetros de la solicitud HTTP                                                      |
| `query`            | Se usa con el tipo predefined&#95;query&#95;handler; ejecuta la consulta cuando se llama al controlador                                                                                                        |
| `content_type`     | Se usa con el tipo static; tipo de contenido de la respuesta                                                                                                                                                   |
| `response_content` | Se usa con el tipo static; contenido de la respuesta enviado al cliente. Al usar el prefijo &#39;file://&#39; o &#39;config://&#39;, busca el contenido en el archivo o la configuración y lo envía al cliente |

Junto con una lista de reglas, puede especificar `<defaults/>`, que indica que se habiliten todos los controladores predeterminados.

Ejemplo:

```xml
<http_handlers>
    <rule>
        <url>/</url>
        <methods>POST,GET</methods>
        <headers><pragma>no-cache</pragma></headers>
        <handler>
            <type>dynamic_query_handler</type>
            <query_param_name>query</query_param_name>
        </handler>
    </rule>

    <rule>
        <url>/predefined_query</url>
        <methods>POST,GET</methods>
        <handler>
            <type>predefined_query_handler</type>
            <query>SELECT * FROM system.settings</query>
        </handler>
    </rule>

    <rule>
        <handler>
            <type>static</type>
            <status>200</status>
            <content_type>text/plain; charset=UTF-8</content_type>
            <response_content>config://http_server_default_response</response_content>
        </handler>
    </rule>
</http_handlers>
```

<div id="http_options_response">
  ## http_options_response
</div>

Se utiliza para agregar encabezados a la respuesta de una solicitud HTTP `OPTIONS`.
El método `OPTIONS` se utiliza al realizar solicitudes de preflight de CORS.

Para obtener más información, consulta [OPTIONS](https://developer.mozilla.org/en-US/docs/Web/HTTP/Methods/OPTIONS).

Ejemplo:

```xml
<http_options_response>
     <header>
            <name>Access-Control-Allow-Origin</name>
            <value>*</value>
     </header>
     <header>
          <name>Access-Control-Allow-Headers</name>
          <value>origin, x-requested-with, x-clickhouse-format, x-clickhouse-user, x-clickhouse-key, Authorization</value>
     </header>
     <header>
          <name>Access-Control-Allow-Methods</name>
          <value>POST, GET, OPTIONS</value>
     </header>
     <header>
          <name>Access-Control-Max-Age</name>
          <value>86400</value>
     </header>
</http_options_response>
```

<div id="http_server_default_response">
  ## http_server_default_response
</div>

La página que se muestra de forma predeterminada al acceder al servidor HTTP(s) de ClickHouse.
El valor predeterminado es &quot;Ok.&quot; (con un salto de línea al final)

**Ejemplo**

Abre `https://tabix.io/` al acceder a `http://localhost: http_port`.

```xml
<http_server_default_response>
  <![CDATA[<html ng-app="SMI2"><head><base href="http://ui.tabix.io/"></head><body><div ui-view="" class="content-ui"></div><script src="http://loader.tabix.io/master.js"></script></body></html>]]>
</http_server_default_response>
```

<div id="iceberg_background_schedule_pool_size">
  ## iceberg_background_schedule_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="10" changeable_without_restart="No" />Tamaño del pool de hilos para obtener de forma asíncrona los metadatos más recientes de un catálogo Iceberg remoto; el pool se comparte entre todas las tablas activas.

<div id="iceberg_catalog_threadpool_pool_size">
  ## iceberg_catalog_threadpool_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="50" changeable_without_restart="No" />Tamaño del pool en segundo plano del catálogo Iceberg

<div id="iceberg_catalog_threadpool_queue_size">
  ## iceberg_catalog_threadpool_queue_size
</div>

<SettingsInfoBlock type="UInt64" default_value="10000" changeable_without_restart="No" />Número de tareas que se pueden insertar en el pool del catálogo Iceberg

<div id="iceberg_compaction_threadpool_pool_size">
  ## iceberg_compaction_threadpool_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="50" changeable_without_restart="No" />Tamaño del pool en segundo plano para la compactación de Iceberg

<div id="iceberg_compaction_threadpool_queue_size">
  ## iceberg_compaction_threadpool_queue_size
</div>

<SettingsInfoBlock type="UInt64" default_value="1000000" changeable_without_restart="No" />Número de tareas que se pueden introducir en la cola del threadpool de compaction de Iceberg

<div id="iceberg_metadata_files_cache_max_entries">
  ## iceberg_metadata_files_cache_max_entries
</div>

<SettingsInfoBlock type="UInt64" default_value="1000" changeable_without_restart="No" />Tamaño máximo de la caché de archivos de metadatos de Iceberg, en número de entradas. Cero significa que está deshabilitada.

<div id="iceberg_metadata_files_cache_policy">
  ## iceberg_metadata_files_cache_policy
</div>

<SettingsInfoBlock type="String" default_value="SLRU" changeable_without_restart="No" />Nombre de la política de caché para los archivos de metadatos de Iceberg.

<div id="iceberg_metadata_files_cache_size">
  ## iceberg_metadata_files_cache_size
</div>

<SettingsInfoBlock type="UInt64" default_value="134217728" changeable_without_restart="Yes" />Tamaño máximo de la caché de metadatos de Iceberg, en bytes. El valor cero significa que está deshabilitada.

<div id="iceberg_metadata_files_cache_size_ratio">
  ## iceberg_metadata_files_cache_size_ratio
</div>

<SettingsInfoBlock type="Double" default_value="0.5" changeable_without_restart="No" />El tamaño de la cola protegida (en el caso de la política SLRU) en la caché de metadatos de Iceberg, en relación con el tamaño total de la caché.

<div id="iceberg_scheduler_compaction_threadpool_pool_size">
  ## iceberg_scheduler_compaction_threadpool_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="50" changeable_without_restart="No" />Tamaño del pool en segundo plano del planificador de compactación de Iceberg

<div id="iceberg_scheduler_compaction_threadpool_queue_size">
  ## iceberg_scheduler_compaction_threadpool_queue_size
</div>

<SettingsInfoBlock type="UInt64" default_value="1000000" changeable_without_restart="No" />Número de tareas que se pueden añadir al pool del scheduler de compactación de Iceberg

<div id="ignore_empty_sql_security_in_create_view_query">
  ## ignore_empty_sql_security_in_create_view_query
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />

Si es true, ClickHouse no escribe los valores predeterminados para una sentencia SQL security vacía en las consultas `CREATE VIEW`.

<Note>
  Esta configuración solo es necesaria durante el período de migración y quedará obsoleta en la versión 24.4
</Note>

<div id="include_from">
  ## include_from
</div>

<SettingsInfoBlock type="String" default_value="/etc/metrika.xml" changeable_without_restart="No" />

La ruta del archivo con sustituciones. Se admiten los formatos XML y YAML.

Para obtener más información, consulte la sección [Archivos de configuración](/es/concepts/features/configuration/server-config/configuration-files).

**Ejemplo**

```xml
<include_from>/etc/metrica.xml</include_from>
```

<div id="index_mark_cache_policy">
  ## index_mark_cache_policy
</div>

<SettingsInfoBlock type="String" default_value="SLRU" changeable_without_restart="No" />Nombre de la política de caché de marcas para el índice secundario.

<div id="index_mark_cache_prewarm_ratio">
  ## index_mark_cache_prewarm_ratio
</div>

<SettingsInfoBlock type="Double" default_value="0.95" changeable_without_restart="No" />La proporción del tamaño total de la caché de marcas del índice que se llena durante el precalentamiento.

<div id="index_mark_cache_size">
  ## index_mark_cache_size
</div>

<SettingsInfoBlock type="UInt64" default_value="5368709120" changeable_without_restart="Yes" />

Tamaño máximo de la caché de marcas de índice.

<Note>
  Un valor de `0` significa que está deshabilitada.

  Esta configuración puede modificarse en tiempo de ejecución y surtirá efecto de inmediato.
</Note>

<div id="index_mark_cache_size_ratio">
  ## index_mark_cache_size_ratio
</div>

<SettingsInfoBlock type="Double" default_value="0.3" changeable_without_restart="No" />El tamaño de la cola protegida (en el caso de la política SLRU) en la caché de marcas de índices secundarios, en relación con el tamaño total de la caché.

<div id="index_uncompressed_cache_policy">
  ## index_uncompressed_cache_policy
</div>

<SettingsInfoBlock type="String" default_value="SLRU" changeable_without_restart="No" />Nombre de la política de caché no comprimida del índice secundario.

<div id="index_uncompressed_cache_size">
  ## index_uncompressed_cache_size
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Sí" />

Tamaño máximo de la caché para bloques no comprimidos de los índices de `MergeTree`.

<Note>
  Un valor de `0` significa que está deshabilitado.

  Esta configuración se puede modificar en tiempo de ejecución y surtirá efecto de inmediato.
</Note>

<div id="index_uncompressed_cache_size_ratio">
  ## index_uncompressed_cache_size_ratio
</div>

<SettingsInfoBlock type="Double" default_value="0.5" changeable_without_restart="No" />El tamaño de la cola protegida (en el caso de la política SLRU) en la caché no comprimida del índice secundario, en relación con el tamaño total de la caché.

<div id="insert_deduplication_version">
  ## insert_deduplication_version
</div>

<SettingsInfoBlock type="InsertDeduplicationVersions" default_value="compatible_double_hashes" changeable_without_restart="No" />

Esta configuración permite migrar desde una versión del código en la que la deduplicación de inserciones síncronas y asíncronas se realiza de forma totalmente distinta y no transparente, a una versión del código en la que los datos insertados se deduplican entre inserciones síncronas y asíncronas.
El valor `old_separate_hashes` significa que ClickHouse usará distintos hashes de deduplicación para inserciones síncronas y asíncronas (igual que antes).
Este valor debe usarse como valor predeterminado si no se tiene la intención de iniciar la migración.
El valor `compatible_double_hashes` significa que ClickHouse usará dos hashes de deduplicación: el antiguo para inserciones síncronas o asíncronas y otro nuevo para todas las inserciones. Este valor debe usarse para migrar las instancias existentes al nuevo comportamiento de forma segura.
Este valor debe estar habilitado durante cierto tiempo (consulte las configuraciones replicated&#95;deduplication&#95;window y non&#95;replicated&#95;deduplication&#95;window) para asegurarse de que no se pierda ninguna inserción síncrona ni asíncrona durante la migración.
Por último, el valor `new_unified_hash` significa que ClickHouse usará el nuevo hash de deduplicación para inserciones síncronas y asíncronas. Este valor puede habilitarse en instancias nuevas de ClickHouse o en instancias que ya hayan usado el valor `compatible_double_hashes` durante algún tiempo.
El valor predeterminado se establecerá primero en `compatible_double_hashes` y más adelante en `new_unified_hash` en futuras versiones de ClickHouse para completar la migración de forma segura en dos fases.

<div id="interserver_http_credentials">
  ## interserver_http_credentials
</div>

Un nombre de usuario y una contraseña que se usan para conectarse a otros servidores durante la [replicación](/es/reference/engines/table-engines/mergetree-family/replication). Además, el servidor autentica a otras réplicas con estas credenciales.
Por lo tanto, `interserver_http_credentials` debe ser el mismo en todas las réplicas de un clúster.

<Note>
  * De forma predeterminada, si se omite la sección `interserver_http_credentials`, no se usa autenticación durante la replicación.
  * La configuración de `interserver_http_credentials` no está relacionada con la [configuración](/es/concepts/features/interfaces/client#configuration_files) de credenciales de un cliente de ClickHouse.
  * Estas credenciales son comunes para la replicación mediante `HTTP` y `HTTPS`.
</Note>

Las siguientes opciones pueden configurarse mediante subetiquetas:

* `user` — Nombre de usuario.
* `password` — Contraseña.
* `allow_empty` — Si es `true`, se permite que otras réplicas se conecten sin autenticación incluso si se han configurado credenciales. Si es `false`, se rechazan las conexiones sin autenticación. Valor predeterminado: `false`.
* `old` — Contiene los valores anteriores de `user` y `password` usados durante la rotación de credenciales. Se pueden especificar varias secciones `old`.

**Rotación de credenciales**

ClickHouse admite la rotación dinámica de credenciales entre servidores sin detener todas las réplicas al mismo tiempo para actualizar su configuración. Las credenciales pueden cambiarse en varios pasos.

Para habilitar la autenticación, establezca `interserver_http_credentials.allow_empty` en `true` y agregue credenciales. Esto permite conexiones con autenticación y sin ella.

```xml
<interserver_http_credentials>
    <user>admin</user>
    <password>111</password>
    <allow_empty>true</allow_empty>
</interserver_http_credentials>
```

Después de configurar todas las réplicas, establezca `allow_empty` en `false` o elimine esta configuración. Esto hace que la autenticación con las nuevas credenciales sea obligatoria.

Para cambiar las credenciales existentes, mueva el nombre de usuario y la contraseña a la sección `interserver_http_credentials.old` y actualice `user` y `password` con los nuevos valores. En este punto, el servidor usa las nuevas credenciales para conectarse a otras réplicas y acepta conexiones tanto con las credenciales nuevas como con las antiguas.

```xml
<interserver_http_credentials>
    <user>admin</user>
    <password>222</password>
    <old>
        <user>admin</user>
        <password>111</password>
    </old>
    <old>
        <user>temp</user>
        <password>000</password>
    </old>
</interserver_http_credentials>
```

Cuando las nuevas credenciales se hayan aplicado a todas las réplicas, se podrán eliminar las credenciales antiguas.

<div id="interserver_http_host">
  ## interserver_http_host
</div>

El nombre de host que otros servidores pueden usar para acceder a este servidor.

Si se omite, se define de la misma forma que con el comando `<hostname -f>`.

Útil para desvincularse de una interfaz de red específica.

**Ejemplo**

```xml
<interserver_http_host>example.clickhouse.com</interserver_http_host>
```

<div id="interserver_http_port">
  ## interserver_http_port
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />

Puerto para el intercambio de datos entre servidores de ClickHouse.

**Ejemplo**

```xml
<interserver_http_port>9009</interserver_http_port>
```

<div id="interserver_https_host">
  ## interserver_https_host
</div>

Similar a `<interserver_http_host>`, salvo que este nombre de host puede ser utilizado por otros servidores para acceder a este servidor mediante `<HTTPS>`.

**Ejemplo**

```xml
<interserver_https_host>example.clickhouse.com</interserver_https_host>
```

<div id="interserver_https_port">
  ## interserver_https_port
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />

Puerto para el intercambio de datos entre servidores ClickHouse mediante `<HTTPS>`.

**Ejemplo**

```xml
<interserver_https_port>9010</interserver_https_port>
```

<div id="interserver_listen_host">
  ## interserver_listen_host
</div>

Restricción de los hosts que pueden intercambiar datos entre servidores de ClickHouse.
Si se usa Keeper, la misma restricción se aplicará a la comunicación entre distintas instancias de Keeper.

<Note>
  De forma predeterminada, el valor es igual al ajuste [`listen_host`](#listen_host).
</Note>

**Ejemplo**

```xml
<interserver_listen_host>::ffff:a00:1</interserver_listen_host>
<interserver_listen_host>10.0.0.1</interserver_listen_host>
```

Tipo:

Predeterminado:

<div id="io_thread_pool_queue_size">
  ## io_thread_pool_queue_size
</div>

<SettingsInfoBlock type="UInt64" default_value="10000" changeable_without_restart="Sí" />

La cantidad máxima de trabajos que se pueden programar en el pool de hilos de E/S.

<Note>
  Un valor de `0` significa ilimitado.
</Note>

<div id="jemalloc_collect_global_profile_samples_in_trace_log">
  ## jemalloc_collect_global_profile_samples_in_trace_log
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Almacena en system.trace&#95;log las asignaciones de memoria muestreadas de jemalloc

<div id="jemalloc_enable_background_threads">
  ## jemalloc_enable_background_threads
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />Habilita los hilos en segundo plano de jemalloc. Jemalloc usa hilos en segundo plano para liberar las páginas de memoria que no se utilizan. Deshabilitar esta opción podría provocar una degradación del rendimiento.

<div id="jemalloc_enable_global_profiler">
  ## jemalloc_enable_global_profiler
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Habilita el perfilador global de asignaciones de jemalloc para todos los hilos. Jemalloc muestreará las asignaciones y todas las liberaciones de memoria de las asignaciones muestreadas.
Los perfiles pueden volcarse con SYSTEM JEMALLOC FLUSH PROFILE, que puede usarse para analizar las asignaciones.
Las muestras también pueden almacenarse en system.trace&#95;log mediante la configuración jemalloc&#95;collect&#95;global&#95;profile&#95;samples&#95;in&#95;trace&#95;log o con la configuración de consulta jemalloc&#95;collect&#95;profile&#95;samples&#95;in&#95;trace&#95;log.
Consulte [Perfilado de asignaciones](/es/concepts/features/performance/allocation-profiling)

<div id="jemalloc_flush_profile_interval_bytes">
  ## jemalloc_flush_profile_interval_bytes
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />El volcado del perfil de jemalloc se realizará después de que el pico global de uso de memoria aumente en jemalloc&#95;flush&#95;profile&#95;interval&#95;bytes

<div id="jemalloc_flush_profile_on_memory_exceeded">
  ## jemalloc_flush_profile_on_memory_exceeded
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />El volcado del perfil de jemalloc se realizará cuando se produzcan errores por exceso de memoria total

<div id="jemalloc_flush_profile_on_memory_exceeded_interval">
  ## jemalloc_flush_profile_on_memory_exceeded_interval
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />Si no es cero, establece el intervalo mínimo, en segundos, entre los volcados de perfiles de jemalloc cuando se producen errores por exceder la memoria total. Por ejemplo, 5 significa que se realizará como máximo un volcado de perfil cada 5 segundos. Tiene prioridad sobre `jemalloc_flush_profile_on_memory_exceeded`.

<div id="jemalloc_max_background_threads_num">
  ## jemalloc_max_background_threads_num
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />Número máximo de hilos en segundo plano de jemalloc que se pueden crear; establézcalo en 0 para usar el valor predeterminado de jemalloc

<div id="jemalloc_profiler_sampling_rate">
  ## jemalloc_profiler_sampling_rate
</div>

<SettingsInfoBlock type="UInt64" default_value="19" changeable_without_restart="No" />

Controla `lg_prof_sample` de jemalloc: el logaritmo en base 2 del intervalo medio (en bytes) entre las muestras de asignación.
El valor predeterminado de 19 corresponde a 512 KiB. Establecer un valor menor aumenta la frecuencia de muestreo (más sobrecarga, más detalle), mientras que un valor mayor la reduce.
Cambiar este valor llama a `prof.reset`, lo que restablece todas las estadísticas de profiling acumuladas. Requiere que profiling esté habilitado (`MALLOC_CONF=prof:true`).

<div id="keep_alive_timeout">
  ## keep_alive_timeout
</div>

<SettingsInfoBlock type="Segundos" default_value="30" changeable_without_restart="No" />

La cantidad de segundos que ClickHouse espera solicitudes entrantes a través del protocolo HTTP antes de cerrar la conexión.

**Ejemplo**

```xml
<keep_alive_timeout>10</keep_alive_timeout>
```

<div id="keeper_hosts">
  ## keeper_hosts
</div>

Configuración dinámica. Contiene un conjunto de hosts de [Zoo]Keeper a los que ClickHouse puede conectarse. No expone información de `<auxiliary_zookeepers>`

<div id="keeper_multiread_batch_size">
  ## keeper_multiread_batch_size
</div>

<SettingsInfoBlock type="UInt64" default_value="10000" changeable_without_restart="No" />

Tamaño máximo del lote para la solicitud MultiRead a [Zoo]Keeper con compatibilidad con procesamiento por lotes. Si se establece en 0, el procesamiento por lotes se desactiva. Disponible solo en ClickHouse Cloud.

<div id="keeper_server.socket_receive_timeout_sec">
  ## keeper_server.socket_receive_timeout_sec
</div>

<SettingsInfoBlock type="UInt64" default_value="300" changeable_without_restart="No" />Tiempo de espera para la recepción en el socket de Keeper.

<div id="keeper_server.socket_send_timeout_sec">
  ## keeper_server.socket_send_timeout_sec
</div>

<SettingsInfoBlock type="UInt64" default_value="300" changeable_without_restart="No" />Tiempo de espera para el envío en el socket de Keeper.

<div id="ldap_servers">
  ## ldap_servers
</div>

Enumere aquí los servidores LDAP con sus parámetros de conexión para:

* usarlos como autenticadores para usuarios locales dedicados que tienen especificado un mecanismo de autenticación `ldap` en lugar de `password`
* usarlos como directorios remotos de usuarios.

Las siguientes opciones de configuración pueden establecerse mediante subetiquetas:

| Configuración                  | Descripción                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `bind_dn`                      | Plantilla utilizada para construir el DN con el que se realizará el bind. El DN resultante se construirá reemplazando todas las subcadenas `\{user_name\}` de la plantilla por el nombre de usuario real durante cada intento de autenticación.                                                                                                                                                                                                                                                                              |
| `enable_tls`                   | Indicador para activar el uso de una conexión segura con el servidor LDAP. Especifique `no` para el protocolo de texto sin formato (`ldap://`) (no recomendado). Especifique `yes` para LDAP sobre SSL/TLS (`ldaps://`) (recomendado; valor predeterminado). Especifique `starttls` para el protocolo heredado StartTLS (protocolo de texto sin formato (`ldap://`) actualizado a TLS).                                                                                                                                      |
| `host`                         | Nombre de host o IP del servidor LDAP; este parámetro es obligatorio y no puede estar vacío.                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `port`                         | Puerto del servidor LDAP; el valor predeterminado es 636 si `enable_tls` está establecido en true, `389` en caso contrario.                                                                                                                                                                                                                                                                                                                                                                                                  |
| `tls_ca_cert_dir`              | ruta al directorio que contiene certificados de CA.                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `tls_ca_cert_file`             | ruta al archivo del certificado de CA.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `tls_cert_file`                | ruta al archivo del certificado.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `tls_cipher_suite`             | conjunto de cifrado permitido (en notación OpenSSL).                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `tls_key_file`                 | ruta al archivo de la clave del certificado.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `tls_minimum_protocol_version` | La versión mínima del protocolo SSL/TLS. Los valores aceptados son: `ssl2`, `ssl3`, `tls1.0`, `tls1.1`, `tls1.2` (el valor predeterminado).                                                                                                                                                                                                                                                                                                                                                                                  |
| `tls_require_cert`             | Comportamiento de verificación del certificado del par SSL/TLS. Los valores aceptados son: `never`, `allow`, `try`, `demand` (el valor predeterminado).                                                                                                                                                                                                                                                                                                                                                                      |
| `user_dn_detection`            | Sección con parámetros de búsqueda LDAP para detectar el user DN real del usuario autenticado mediante bind. Esto se usa principalmente en filtros de búsqueda para una asignación de roles posterior cuando el servidor es Active Directory. El user DN resultante se usará al reemplazar las subcadenas `\{user_dn\}` dondequiera que estén permitidas. De forma predeterminada, el user DN se establece igual que el bind DN, pero una vez realizada la búsqueda, se actualizará con el valor real detectado del user DN. |
| `verification_cooldown`        | Un periodo de tiempo, en segundos, después de un intento de bind exitoso, durante el cual se asumirá que un usuario se ha autenticado correctamente para todas las solicitudes consecutivas sin contactar al servidor LDAP. Especifique `0` (el valor predeterminado) para deshabilitar el almacenamiento en caché y forzar el contacto con el servidor LDAP para cada solicitud de autenticación.                                                                                                                           |

La configuración `user_dn_detection` puede establecerse con subetiquetas:

| Configuración   | Descripción                                                                                                                                                                                                                                                                                                                                                                              |
| --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `base_dn`       | plantilla utilizada para construir el base DN para la búsqueda LDAP. El DN resultante se construirá reemplazando todas las subcadenas `\{user_name\}` y `\{bind_dn\}` de la plantilla por el nombre de usuario real y el bind DN durante la búsqueda LDAP.                                                                                                                               |
| `scope`         | alcance de la búsqueda LDAP. Los valores aceptados son: `base`, `one_level`, `children`, `subtree` (el valor predeterminado).                                                                                                                                                                                                                                                            |
| `search_filter` | plantilla utilizada para construir el filtro de búsqueda para la búsqueda LDAP. El filtro resultante se construirá reemplazando todas las subcadenas `\{user_name\}`, `\{bind_dn\}` y `\{base_dn\}` de la plantilla por el nombre de usuario real, el bind DN y el base DN durante la búsqueda LDAP. Tenga en cuenta que los caracteres especiales deben escaparse correctamente en XML. |

Ejemplo:

```xml
<my_ldap_server>
    <host>localhost</host>
    <port>636</port>
    <bind_dn>uid={user_name},ou=users,dc=example,dc=com</bind_dn>
    <verification_cooldown>300</verification_cooldown>
    <enable_tls>yes</enable_tls>
    <tls_minimum_protocol_version>tls1.2</tls_minimum_protocol_version>
    <tls_require_cert>demand</tls_require_cert>
    <tls_cert_file>/path/to/tls_cert_file</tls_cert_file>
    <tls_key_file>/path/to/tls_key_file</tls_key_file>
    <tls_ca_cert_file>/path/to/tls_ca_cert_file</tls_ca_cert_file>
    <tls_ca_cert_dir>/path/to/tls_ca_cert_dir</tls_ca_cert_dir>
    <tls_cipher_suite>ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:AES256-GCM-SHA384</tls_cipher_suite>
</my_ldap_server>
```

Ejemplo (Active Directory típico con detección del DN de usuario configurada para una asignación de roles posterior):

```xml
<my_ad_server>
    <host>localhost</host>
    <port>389</port>
    <bind_dn>EXAMPLE\{user_name}</bind_dn>
    <user_dn_detection>
        <base_dn>CN=Users,DC=example,DC=com</base_dn>
        <search_filter>(&amp;(objectClass=user)(sAMAccountName={user_name}))</search_filter>
    </user_dn_detection>
    <enable_tls>no</enable_tls>
</my_ad_server>
```

<div id="license_file">
  ## license_file
</div>

Contenido del archivo de licencia de ClickHouse Enterprise Edition

<div id="license_public_key_for_testing">
  ## license_public_key_for_testing
</div>

Clave de demostración de licencia, solo para uso en CI

<div id="listen_backlog">
  ## listen_backlog
</div>

<SettingsInfoBlock type="UInt32" default_value="4096" changeable_without_restart="No" />

Backlog (tamaño de la cola de conexiones pendientes) del socket de escucha. El valor predeterminado de `<4096>` es el mismo que el de Linux 5.4+).

Normalmente no es necesario cambiar este valor, ya que:

* El valor predeterminado es lo suficientemente grande.
* El servidor tiene un thread independiente para aceptar las conexiones de los clientes.

Por tanto, aunque `<TcpExtListenOverflows>` (de `<nstat>`) sea distinto de cero y este contador aumente en el servidor ClickHouse, eso no significa que sea necesario incrementar este valor, ya que:

* Normalmente, si `<4096>` no es suficiente, esto indica algún problema interno de scaling en ClickHouse, por lo que es mejor reportar un problema.
* Tampoco significa que el servidor vaya a poder gestionar más connections más adelante (e incluso si pudiera, para entonces es posible que los clientes ya no estén o se hayan desconectado).

**Ejemplo**

```xml
<listen_backlog>4096</listen_backlog>
```

<div id="listen_host">
  ## listen_host
</div>

Restringe los hosts desde los que pueden llegar las solicitudes. Si desea que el servidor responda a todos ellos, especifique `::`.

Ejemplos:

```xml
<listen_host>::1</listen_host>
<listen_host>127.0.0.1</listen_host>
```

<div id="listen_reuse_port">
  ## listen_reuse_port
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />

Permite que varios servidores escuchen en la misma dirección:puerto. El sistema operativo dirigirá las solicitudes a un servidor elegido al azar. No se recomienda habilitar esta configuración.

**Ejemplo**

```xml
<listen_reuse_port>0</listen_reuse_port>
```

<div id="listen_try">
  ## listen_try
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />

El servidor no se cerrará si las redes IPv6 o IPv4 no están disponibles al intentar iniciar la escucha.

**Ejemplo**

```xml
<listen_try>0</listen_try>
```

<div id="load_marks_threadpool_pool_size">
  ## load_marks_threadpool_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="50" changeable_without_restart="No" />Tamaño del pool en segundo plano para cargar marks

<div id="load_marks_threadpool_queue_size">
  ## load_marks_threadpool_queue_size
</div>

<SettingsInfoBlock type="UInt64" default_value="10000" changeable_without_restart="No" />Número de tareas que se pueden insertar en el pool de prelecturas

<div id="logger">
  ## logger
</div>

La ubicación y el formato de los mensajes de log.

**Claves**:

| Key                          | Description                                                                                                                                                                                                                                                                                                                                               |
| ---------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `async`                      | Cuando es `true` (predeterminado), el registro se realizará de forma asíncrona (un hilo en segundo plano por canal de salida). En caso contrario, se registrará dentro del hilo que llama a LOG                                                                                                                                                           |
| `async_queue_max_size`       | Al usar registro asíncrono, es la cantidad máxima de mensajes que se conservarán en la cola mientras esperan a vaciarse. Los mensajes adicionales se descartarán                                                                                                                                                                                          |
| `console`                    | Habilita el registro en la consola. Establézcalo en `1` o `true` para activarlo. El valor predeterminado es `1` si ClickHouse no se ejecuta en modo daemon; en caso contrario, `0`.                                                                                                                                                                       |
| `console_log_level`          | Nivel de log para la salida en consola. De forma predeterminada, usa `level`.                                                                                                                                                                                                                                                                             |
| `console_shutdown_log_level` | nivel de apagado se usa para establecer el nivel de log de la consola durante el apagado del servidor.                                                                                                                                                                                                                                                    |
| `console_startup_log_level`  | nivel de inicio se usa para establecer el nivel de log de la consola durante el arranque del servidor. Después del arranque, el nivel de log vuelve a la configuración `console_log_level`                                                                                                                                                                |
| `count`                      | política de rotación: número máximo de archivos de log históricos que conserva ClickHouse.                                                                                                                                                                                                                                                                |
| `errorlog`                   | La ruta al archivo de log de errores.                                                                                                                                                                                                                                                                                                                     |
| `formatting.type`            | Formato de log para la salida en consola. Actualmente, solo se admite `json`                                                                                                                                                                                                                                                                              |
| `level`                      | Nivel de log. Valores aceptables: `none` (desactiva el registro), `fatal`, `critical`, `error`, `warning`, `notice`, `information`,`debug`, `trace`, `test`                                                                                                                                                                                               |
| `log`                        | La ruta al archivo de log.                                                                                                                                                                                                                                                                                                                                |
| `rotation`                   | política de rotación: controla cuándo se rotan los archivos de log. La rotación puede basarse en el tamaño, el tiempo o una combinación de ambos. Ejemplos: 100M, daily, 100M,daily. Una vez que el archivo de log supera el tamaño especificado o se alcanza el intervalo de tiempo indicado, se renombra, se archiva y se crea un nuevo archivo de log. |
| `shutdown_level`             | nivel de apagado se usa para establecer el nivel del logger raíz durante el apagado del servidor.                                                                                                                                                                                                                                                         |
| `size`                       | política de rotación: tamaño máximo de los archivos de log en bytes. Una vez que el tamaño del archivo de log supera este umbral, se renombra, se archiva y se crea un nuevo archivo de log.                                                                                                                                                              |
| `startup_level`              | nivel de inicio se usa para establecer el nivel del logger raíz durante el arranque del servidor. Después del arranque, el nivel de log vuelve a la configuración `level`                                                                                                                                                                                 |
| `stream_compress`            | Comprime los mensajes de log con LZ4. Establézcalo en `1` o `true` para activarlo.                                                                                                                                                                                                                                                                        |
| `syslog_level`               | Nivel de log para el envío a syslog.                                                                                                                                                                                                                                                                                                                      |
| `use_syslog`                 | También reenvía la salida de log a syslog.                                                                                                                                                                                                                                                                                                                |

**Especificadores de formato del log**

Los nombres de archivo de las rutas `log` y `errorLog` admiten los siguientes especificadores de formato para el nombre de archivo resultante (la parte del directorio no los admite).

La columna &quot;Ejemplo&quot; muestra la salida en `2023-07-06 18:32:07`.

| Especificador | Descripción                                                                                                                                                                                    | Ejemplo                    |
| ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- |
| `%%`          | % literal                                                                                                                                                                                      | `%`                        |
| `%n`          | Carácter de nueva línea                                                                                                                                                                        |                            |
| `%t`          | Carácter de tabulación horizontal                                                                                                                                                              |                            |
| `%Y`          | Año como número decimal, p. ej., 2017                                                                                                                                                          | `2023`                     |
| `%y`          | Últimos 2 dígitos del año como número decimal (rango [00,99])                                                                                                                                  | `23`                       |
| `%C`          | Primeros 2 dígitos del año como número decimal (rango [00,99])                                                                                                                                 | `20`                       |
| `%G`          | [Año basado en semanas ISO 8601](https://en.wikipedia.org/wiki/ISO_8601#Week_dates) de cuatro dígitos; es decir, el año que contiene la semana especificada. Normalmente solo es útil con `%V` | `2023`                     |
| `%g`          | Últimos 2 dígitos del [año basado en semanas ISO 8601](https://en.wikipedia.org/wiki/ISO_8601#Week_dates); es decir, el año que contiene la semana especificada                                | `23`                       |
| `%b`          | Nombre abreviado del mes, p. ej., Oct (depende de la configuración regional)                                                                                                                   | `Jul`                      |
| `%h`          | Sinónimo de %b                                                                                                                                                                                 | `Jul`                      |
| `%B`          | Nombre completo del mes, p. ej., October (depende de la configuración regional)                                                                                                                | `July`                     |
| `%m`          | Mes como número decimal (rango [01,12])                                                                                                                                                        | `07`                       |
| `%U`          | Número de la semana del año como número decimal (el domingo es el primer día de la semana) (rango [00,53])                                                                                     | `27`                       |
| `%W`          | Número de la semana del año como número decimal (el lunes es el primer día de la semana) (rango [00,53])                                                                                       | `27`                       |
| `%V`          | Número de semana ISO 8601 (rango [01,53])                                                                                                                                                      | `27`                       |
| `%j`          | Día del año como número decimal (rango [001,366])                                                                                                                                              | `187`                      |
| `%d`          | Día del mes como número decimal con relleno de ceros (rango [01,31]). Si tiene un solo dígito, va precedido de un cero                                                                         | `06`                       |
| `%e`          | Día del mes como número decimal con relleno de espacios (rango [1,31]). Si tiene un solo dígito, va precedido de un espacio                                                                    | `&nbsp; 6`                 |
| `%a`          | Nombre abreviado del día de la semana, p. ej., Fri (depende de la configuración regional)                                                                                                      | `Thu`                      |
| `%A`          | Nombre completo del día de la semana, p. ej., Friday (depende de la configuración regional)                                                                                                    | `Thursday`                 |
| `%w`          | Día de la semana como número entero, donde el domingo es 0 (rango [0-6])                                                                                                                       | `4`                        |
| `%u`          | Día de la semana como número decimal, donde el lunes es 1 (formato ISO 8601) (rango [1-7])                                                                                                     | `4`                        |
| `%H`          | Hora como número decimal, en formato de 24 horas (rango [00-23])                                                                                                                               | `18`                       |
| `%I`          | Hora como número decimal, en formato de 12 horas (rango [01,12])                                                                                                                               | `06`                       |
| `%M`          | Minuto como número decimal (rango [00,59])                                                                                                                                                     | `32`                       |
| `%S`          | Segundo como número decimal (rango [00,60])                                                                                                                                                    | `07`                       |
| `%c`          | Cadena estándar de fecha y hora, p. ej., Sun Oct 17 04:41:13 2010 (depende de la configuración regional)                                                                                       | `Thu Jul  6 18:32:07 2023` |
| `%x`          | Representación localizada de la fecha (depende de la configuración regional)                                                                                                                   | `07/06/23`                 |
| `%X`          | Representación localizada de la hora, p. ej., 18:40:20 o 6:40:20 PM (depende de la configuración regional)                                                                                     | `18:32:07`                 |
| `%D`          | Fecha corta MM/DD/YY, equivalente a %m/%d/%y                                                                                                                                                   | `07/06/23`                 |
| `%F`          | Fecha abreviada YYYY-MM-DD, equivalente a %Y-%m-%d                                                                                                                                             | `2023-07-06`               |
| `%r`          | Hora localizada en formato de 12 horas (según la configuración regional)                                                                                                                       | `06:32:07 PM`              |
| `%R`          | Equivalente a &quot;%H:%M&quot;                                                                                                                                                                | `18:32`                    |
| `%T`          | Equivalente a &quot;%H:%M:%S&quot; (el formato de hora ISO 8601)                                                                                                                               | `18:32:07`                 |
| `%p`          | Indicador localizado de a. m. o p. m. (según la configuración regional)                                                                                                                        | `PM`                       |
| `%z`          | Desplazamiento con respecto a UTC en formato ISO 8601 (p. ej., -0430), o ningún carácter si la información de la zona horaria no está disponible                                               | `+0800`                    |
| `%Z`          | Nombre o abreviatura de la zona horaria según la configuración regional, o ningún carácter si la información de la zona horaria no está disponible                                             | `Z AWST `                  |

**Ejemplo**

```xml
<logger>
    <level>trace</level>
    <log>/var/log/clickhouse-server/clickhouse-server-%F-%T.log</log>
    <errorlog>/var/log/clickhouse-server/clickhouse-server-%F-%T.err.log</errorlog>
    <size>1000M</size>
    <count>10</count>
    <stream_compress>true</stream_compress>
</logger>
```

Para imprimir solo los mensajes de log en la consola:

```xml
<logger>
    <level>information</level>
    <console>true</console>
</logger>
```

**Sobrescrituras por nivel**

Se puede sobrescribir el nivel de log para nombres de log individuales. Por ejemplo, para silenciar todos los mensajes de los loggers &quot;Backup&quot; y &quot;RBAC&quot;.

```xml
<logger>
    <levels>
        <logger>
            <name>Backup</name>
            <level>none</level>
        </logger>
        <logger>
            <name>RBAC</name>
            <level>none</level>
        </logger>
    </levels>
</logger>
```

**syslog**

Para escribir también mensajes de log en syslog:

```xml
<logger>
    <use_syslog>1</use_syslog>
    <syslog>
        <address>syslog.remote:10514</address>
        <hostname>myhost.local</hostname>
        <facility>LOG_LOCAL6</facility>
        <format>syslog</format>
    </syslog>
</logger>
```

Claves para `<syslog>`:

| Key        | Description                                                                                                                                                                                                                                                                                                          |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `address`  | La dirección de syslog en el formato `host\[:port\]`. Si se omite, se usa el demonio local.                                                                                                                                                                                                                          |
| `hostname` | El nombre del host desde el que se envían los logs (opcional).                                                                                                                                                                                                                                                       |
| `facility` | La [palabra clave de facility](https://en.wikipedia.org/wiki/Syslog#Facility) de syslog. Debe especificarse en mayúsculas con el prefijo &quot;LOG&#95;&quot;, por ejemplo, `LOG_USER`, `LOG_DAEMON`, `LOG_LOCAL3`, etc. Valor predeterminado: `LOG_USER` si se especifica `address`; de lo contrario, `LOG_DAEMON`. |
| `format`   | Formato del mensaje de log. Valores posibles: `bsd` y `syslog.`                                                                                                                                                                                                                                                      |

**Formatos de log**

Puede especificar el formato de log que se mostrará en el log de la consola. Actualmente, solo se admite JSON.

**Ejemplo**

Aquí tiene un ejemplo de un log JSON de salida:

```json
{
  "date_time_utc": "2024-11-06T09:06:09Z",
  "date_time": "1650918987.180175",
  "thread_name": "#1",
  "thread_id": "254545",
  "level": "Trace",
  "query_id": "",
  "logger_name": "BaseDaemon",
  "message": "Received signal 2",
  "source_file": "../base/daemon/BaseDaemon.cpp; virtual void SignalListener::run()",
  "source_line": "192"
}
```

Para habilitar la compatibilidad con el registro JSON, use el siguiente fragmento:

```xml
<logger>
    <formatting>
        <type>json</type>
        <!-- Se puede configurar por canal (log, errorlog, console, syslog) o de forma global para todos los canales (en ese caso, simplemente omítalo). -->
        <!-- <channel></channel> -->
        <names>
            <date_time>date_time</date_time>
            <thread_name>thread_name</thread_name>
            <thread_id>thread_id</thread_id>
            <level>level</level>
            <query_id>query_id</query_id>
            <logger_name>logger_name</logger_name>
            <message>message</message>
            <source_file>source_file</source_file>
            <source_line>source_line</source_line>
        </names>
    </formatting>
</logger>
```

**Cambiar el nombre de las claves en logs JSON**

Los nombres de las claves pueden modificarse cambiando los valores de las etiquetas dentro de la etiqueta `<names>`. Por ejemplo, para cambiar `DATE_TIME` por `MY_DATE_TIME`, puede usar `<date_time>MY_DATE_TIME</date_time>`.

**Omitir claves en logs JSON**

Las propiedades del log pueden omitirse comentando la propiedad. Por ejemplo, si no quiere que el log incluya `query_id`, puede comentar la etiqueta `<query_id>`.

<div id="logger.async">
  ## logger.async
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />Cuando `<true>` (valor predeterminado), el registro se realiza de forma asíncrona (un hilo en segundo plano por cada canal de salida). De lo contrario, se registra dentro del hilo que llama a LOG.

<div id="logger.async_queye_max_size">
  ## logger.async_queye_max_size
</div>

<SettingsInfoBlock type="UInt64" default_value="65536" changeable_without_restart="No" />Cuando se usa registro asíncrono, es la cantidad máxima de mensajes que se mantendrán en la cola a la espera de vaciarse. Los mensajes adicionales se descartarán.

<div id="logger.console">
  ## logger.console
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Habilita el registro en la consola. Establézcalo en `<1>` o `<true>` para habilitarlo. El valor predeterminado es `<1>` si ClickHouse no se ejecuta en modo daemon; de lo contrario, es `<0>`.

<div id="logger.console_log_level">
  ## logger.console_log_level
</div>

<SettingsInfoBlock type="String" default_value="trace" changeable_without_restart="No" />Nivel de registro de la salida por consola. El valor predeterminado es `<level>`.

<div id="logger.count">
  ## logger.count
</div>

<SettingsInfoBlock type="UInt64" default_value="1" changeable_without_restart="No" />Política de rotación: cantidad máxima de archivos de log históricos de ClickHouse que se conservan.

<div id="logger.errorlog">
  ## logger.errorlog
</div>

La ruta del archivo de registro de errores.

<div id="logger.formatting.type">
  ## logger.formatting.type
</div>

<SettingsInfoBlock type="String" default_value="json" changeable_without_restart="No" />Formato de registro para la salida por consola. Actualmente, solo se admite `<json>`.

<div id="logger.level">
  ## logger.level
</div>

<SettingsInfoBlock type="String" default_value="trace" changeable_without_restart="No" />Nivel de registro. Valores aceptables: `<none>` (desactiva el registro), `<fatal>`, `<critical>`, `<error>`, `<warning>`, `<notice>`, `<information>`, `<debug>`, `<trace>`, `<test>`.

<div id="logger.log">
  ## logger.log
</div>

La ruta del archivo de log.

<div id="logger.rotation">
  ## logger.rotation
</div>

<SettingsInfoBlock type="String" default_value="100M" changeable_without_restart="No" />Política de rotación: controla cuándo se rotan los archivos de log. La rotación puede basarse en el tamaño, en el tiempo o en una combinación de ambos. Ejemplos: 100M, daily, 100M,daily. Cuando el archivo de log supera el tamaño especificado o se alcanza el intervalo de tiempo indicado, se renombra, se archiva y se crea un nuevo archivo de log.

<div id="logger.shutdown_level">
  ## logger.shutdown_level
</div>

El nivel de apagado se utiliza para establecer el nivel del logger raíz durante el apagado del servidor.

<div id="logger.size">
  ## logger.size
</div>

<SettingsInfoBlock type="String" default_value="100M" changeable_without_restart="No" />Política de rotación: tamaño máximo de los archivos de log en bytes. Cuando el tamaño del archivo de log supera este umbral, se renombra, se archiva y se crea un nuevo archivo de log.

<div id="logger.startup_level">
  ## logger.startup_level
</div>

El nivel de inicio se utiliza para establecer el nivel del logger raíz durante el arranque del servidor. Después del arranque, el nivel de log vuelve a la configuración `<level>`.

<div id="logger.stream_compress">
  ## logger.stream_compress
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Comprime los mensajes de log con LZ4. Establézcalo en `<1>` o `<true>` para activarlo.

<div id="logger.syslog_level">
  ## logger.syslog_level
</div>

<SettingsInfoBlock type="String" default_value="trace" changeable_without_restart="No" />Nivel de log para el registro en syslog.

<div id="logger.use_syslog">
  ## logger.use_syslog
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />También reenvía la salida de logs a syslog.

<div id="macros">
  ## macros
</div>

Sustituciones de parámetros para tablas replicadas.

Puede omitirse si no se usan tablas replicadas.

Para obtener más información, consulte la sección [Creación de tablas replicadas](/es/reference/engines/table-engines/mergetree-family/replication#creating-replicated-tables).

**Ejemplo**

```xml
<macros incl="macros" optional="true" />
```

<div id="mark_cache_policy">
  ## mark_cache_policy
</div>

<SettingsInfoBlock type="String" default_value="SLRU" changeable_without_restart="No" />Nombre de la política de la caché de marcas.

<div id="mark_cache_prewarm_ratio">
  ## mark_cache_prewarm_ratio
</div>

<SettingsInfoBlock type="Double" default_value="0.95" changeable_without_restart="No" />La proporción del tamaño total de la caché de marcas que se llena durante el precalentamiento.

<div id="mark_cache_size">
  ## mark_cache_size
</div>

<SettingsInfoBlock type="UInt64" default_value="5368709120" changeable_without_restart="Sí" />

Tamaño máximo de la caché de marcas (el índice de las tablas de la familia [`MergeTree`](/es/reference/engines/table-engines/mergetree-family/index)).

<Note>
  Esta configuración se puede modificar en tiempo de ejecución y surtirá efecto de inmediato.
</Note>

<div id="mark_cache_size_ratio">
  ## mark_cache_size_ratio
</div>

<SettingsInfoBlock type="Double" default_value="0.5" changeable_without_restart="No" />El tamaño de la cola protegida (en el caso de la política SLRU) en la caché de marcas, en relación con el tamaño total de la caché.

<div id="max_active_parts_loading_thread_pool_size">
  ## max_active_parts_loading_thread_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="64" changeable_without_restart="Yes" />El número de hilos para cargar el conjunto activo de partes de datos (las activas) durante el inicio.

<div id="max_authentication_methods_per_user">
  ## max_authentication_methods_per_user
</div>

<SettingsInfoBlock type="UInt64" default_value="100" changeable_without_restart="No" />

El número máximo de métodos de autenticación con los que puede crearse o modificarse un usuario.
Cambiar esta configuración no afecta a los usuarios existentes. Las consultas CREATE/ALTER relacionadas con la autenticación fallarán si superan el límite especificado en esta configuración.
Las consultas CREATE/ALTER no relacionadas con la autenticación se ejecutarán correctamente.

<Note>
  Un valor de `0` significa que no hay límite.
</Note>

<div id="max_backup_bandwidth_for_server">
  ## max_backup_bandwidth_for_server
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />La velocidad máxima de lectura, en bytes por segundo, para todas las copias de seguridad del servidor. Cero significa que no hay límite.

<div id="max_backups_io_thread_pool_free_size">
  ## max_backups_io_thread_pool_free_size
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Yes" />Si el número de hilos **inactivos** en el pool de hilos de E/S de copias de seguridad supera `max_backup_io_thread_pool_free_size`, ClickHouse liberará los recursos ocupados por los hilos inactivos y reducirá el tamaño del pool. Los hilos pueden volver a crearse si es necesario.

<div id="max_backups_io_thread_pool_size">
  ## max_backups_io_thread_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="1000" changeable_without_restart="Sí" />ClickHouse usa hilos del pool de hilos de E/S de copias de seguridad para realizar operaciones de E/S de copias de seguridad en S3. `max_backups_io_thread_pool_size` limita el número máximo de hilos del pool.

<div id="max_build_vector_similarity_index_thread_pool_size">
  ## max_build_vector_similarity_index_thread_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="16" changeable_without_restart="No" />

El número máximo de hilos que se usarán para crear índices vectoriales.

<Note>
  Un valor de `0` significa usar todos los núcleos.
</Note>

<div id="max_concurrent_insert_queries">
  ## max_concurrent_insert_queries
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Sí" />

Límite del número total de consultas de inserción concurrentes.

<Note>
  Un valor de `0` (predeterminado) significa que no hay límite.

  Esta configuración se puede modificar en tiempo de ejecución y entrará en vigor de inmediato. Las consultas que ya se estén ejecutando no cambiarán.
</Note>

<div id="max_concurrent_queries">
  ## max_concurrent_queries
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Yes" />

Límite del número total de consultas que pueden ejecutarse simultáneamente. Tenga en cuenta que también deben considerarse los límites de las consultas `INSERT` y `SELECT`, así como el número máximo de consultas para los usuarios.

Véase también:

* [`max_concurrent_insert_queries`](/es/reference/settings/server-settings/settings#max_concurrent_insert_queries)
* [`max_concurrent_select_queries`](/es/reference/settings/server-settings/settings#max_concurrent_select_queries)
* [`max_concurrent_queries_for_all_users`](/es/reference/settings/session-settings#max_concurrent_queries_for_all_users)

<Note>
  Un valor de `0` (predeterminado) significa que no hay límite.

  Esta configuración puede modificarse en tiempo de ejecución y tendrá efecto de inmediato. Las consultas que ya estén en ejecución no cambiarán.
</Note>

<div id="max_concurrent_select_queries">
  ## max_concurrent_select_queries
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Yes" />

Límite del número total de consultas `SELECT` simultáneas.

<Note>
  Un valor de `0` (predeterminado) significa ilimitado.

  Esta configuración puede modificarse en tiempo de ejecución y surtirá efecto de inmediato. Las consultas que ya estén en ejecución no se verán afectadas.
</Note>

<div id="max_connections">
  ## max_connections
</div>

<SettingsInfoBlock type="Int32" default_value="4096" changeable_without_restart="No" />Número máximo de conexiones del servidor.

<div id="max_database_num_to_throw">
  ## max_database_num_to_throw
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />Si el número de bases de datos es mayor que este valor, el servidor lanzará una excepción. 0 significa que no hay límite.

<div id="max_database_num_to_warn">
  ## max_database_num_to_warn
</div>

<SettingsInfoBlock type="UInt64" default_value="1000" changeable_without_restart="Yes" />

Si el número de bases de datos adjuntas supera el valor especificado, el servidor de ClickHouse añadirá mensajes de advertencia a la tabla `system.warnings`.

**Ejemplo**

```xml
<max_database_num_to_warn>50</max_database_num_to_warn>
```

<div id="max_database_replicated_create_table_thread_pool_size">
  ## max_database_replicated_create_table_thread_pool_size
</div>

<SettingsInfoBlock type="UInt32" default_value="1" changeable_without_restart="No" />El número de hilos para crear tablas durante la recuperación de réplicas en DatabaseReplicated. Cero significa que el número de hilos es igual al número de núcleos.

<div id="max_dictionary_num_to_throw">
  ## max_dictionary_num_to_throw
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />

Si el número de diccionarios es mayor que este valor, el servidor generará una excepción.

Solo se cuentan las tablas de los motores de base de datos siguientes:

* Atomic
* Ordinary
* Replicated
* Lazy

<Note>
  Un valor de `0` significa que no hay ningún límite.
</Note>

**Ejemplo**

```xml
<max_dictionary_num_to_throw>400</max_dictionary_num_to_throw>
```

<div id="max_dictionary_num_to_warn">
  ## max_dictionary_num_to_warn
</div>

<SettingsInfoBlock type="UInt64" default_value="1000" changeable_without_restart="Yes" />

Si el número de diccionarios adjuntos supera el valor especificado, el servidor de ClickHouse agregará mensajes de advertencia a la tabla `system.warnings`.

**Ejemplo**

```xml
<max_dictionary_num_to_warn>400</max_dictionary_num_to_warn>
```

<div id="max_distributed_cache_read_bandwidth_for_server">
  ## max_distributed_cache_read_bandwidth_for_server
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Sí" />La velocidad máxima total de lectura de la distributed cache en el servidor, en bytes por segundo. Cero significa ilimitado.

<div id="max_distributed_cache_write_bandwidth_for_server">
  ## max_distributed_cache_write_bandwidth_for_server
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Yes" />La velocidad máxima total de escritura en distributed cache del servidor, en bytes por segundo. Cero significa que no hay límite.

<div id="max_entries_for_hash_table_stats">
  ## max_entries_for_hash_table_stats
</div>

<SettingsInfoBlock type="UInt64" default_value="10000" changeable_without_restart="No" />Cuántas entradas se permite que tengan las estadísticas de la tabla hash recopiladas durante la agregación

<div id="max_fetch_partition_thread_pool_size">
  ## max_fetch_partition_thread_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="64" changeable_without_restart="Yes" />Número de hilos para ALTER TABLE FETCH PARTITION.

<div id="max_format_parsing_thread_pool_free_size">
  ## max_format_parsing_thread_pool_free_size
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Sí" />

Número máximo de hilos inactivos en espera que se conservan en el pool de hilos para analizar la entrada.

<div id="max_format_parsing_thread_pool_size">
  ## max_format_parsing_thread_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="100" changeable_without_restart="Sí" />

Número total máximo de hilos que se pueden usar para analizar la entrada.

<div id="max_held_snapshots">
  ## max_held_snapshots
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />Número máximo de instantáneas ligeras que se pueden mantener al mismo tiempo. Cero significa que no hay límite. Si el número de instantáneas alcanza este límite, se lanza una excepción al intentar crear una nueva instantánea.

<div id="max_io_thread_pool_free_size">
  ## max_io_thread_pool_free_size
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Sí" />

Si el número de **hilos** inactivos en el pool de hilos de E/S supera `max_io_thread_pool_free_size`, ClickHouse liberará los recursos ocupados por esos hilos inactivos y reducirá el tamaño del pool. Los hilos pueden volver a crearse si es necesario.

<div id="max_io_thread_pool_size">
  ## max_io_thread_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="100" changeable_without_restart="Sí" />

ClickHouse usa hilos del pool de hilos de E/S para realizar algunas operaciones de E/S (p. ej., para interactuar con S3). `max_io_thread_pool_size` limita la cantidad máxima de hilos del pool.

<div id="max_keep_alive_requests">
  ## max_keep_alive_requests
</div>

<SettingsInfoBlock type="UInt64" default_value="10000" changeable_without_restart="No" />

Número máximo de solicitudes que pueden realizarse a través de una única conexión keep-alive antes de que el servidor ClickHouse la cierre.

**Ejemplo**

```xml
<max_keep_alive_requests>10</max_keep_alive_requests>
```

<div id="max_local_read_bandwidth_for_server">
  ## max_local_read_bandwidth_for_server
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Yes" />

La velocidad máxima de lectura local en bytes por segundo.

<Note>
  Un valor de `0` significa que no hay límite.
</Note>

<div id="max_local_write_bandwidth_for_server">
  ## max_local_write_bandwidth_for_server
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Sí" />

La velocidad máxima de escritura local en bytes por segundo.

<Note>
  Un valor de `0` significa que no hay límite.
</Note>

<div id="max_materialized_views_count_for_table">
  ## max_materialized_views_count_for_table
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />

Límite en el número de vistas materializadas asociadas a una tabla.

<Note>
  Aquí solo se tienen en cuenta las vistas que dependen directamente, y no se considera la creación de una vista sobre otra.
</Note>

<div id="max_merges_bandwidth_for_server">
  ## max_merges_bandwidth_for_server
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />La velocidad máxima de lectura de todas las operaciones de merge en el servidor, en bytes por segundo. Cero significa que no hay límite.

<div id="max_mutations_bandwidth_for_server">
  ## max_mutations_bandwidth_for_server
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />La velocidad máxima de lectura de todas las mutaciones del servidor, en bytes por segundo. Cero significa ilimitado.

<div id="max_named_collection_num_to_throw">
  ## max_named_collection_num_to_throw
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />

Si el número de colecciones con nombre supera este valor, el servidor generará una excepción.

<Note>
  Un valor de `0` significa que no hay límite.
</Note>

**Ejemplo**

```xml
<max_named_collection_num_to_throw>400</max_named_collection_num_to_throw>
```

<div id="max_named_collection_num_to_warn">
  ## max_named_collection_num_to_warn
</div>

<SettingsInfoBlock type="UInt64" default_value="1000" changeable_without_restart="Yes" />

Si el número de colecciones con nombre supera el valor especificado, el servidor de ClickHouse añadirá mensajes de advertencia a la tabla `system.warnings`.

**Ejemplo**

```xml
<max_named_collection_num_to_warn>400</max_named_collection_num_to_warn>
```

<div id="max_open_files">
  ## max_open_files
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />

Número máximo de archivos abiertos.

<Note>
  Recomendamos usar esta opción en macOS, ya que la función getrlimit() devuelve un valor incorrecto.
</Note>

<div id="max_os_cpu_wait_time_ratio_to_drop_connection">
  ## max_os_cpu_wait_time_ratio_to_drop_connection
</div>

<SettingsInfoBlock type="Float" default_value="0" changeable_without_restart="Sí" />

Relación máxima entre los tiempos de espera de la CPU del SO (métrica `OSCPUWaitMicroseconds`) y de actividad (métrica `OSCPUVirtualTimeMicroseconds`) para considerar descartar conexiones. Se utiliza interpolación lineal entre la relación mínima y la máxima para calcular la probabilidad; en este punto, la probabilidad es 1.
Consulta [Control del comportamiento ante la sobrecarga de CPU del servidor](/es/concepts/features/configuration/settings/server-overload) para obtener más detalles.

<div id="max_outdated_parts_loading_thread_pool_size">
  ## max_outdated_parts_loading_thread_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="32" changeable_without_restart="Yes" />El número de hilos para cargar el conjunto inactivo de partes de datos (las obsoletas) al iniciar.

<div id="max_part_num_to_warn">
  ## max_part_num_to_warn
</div>

<SettingsInfoBlock type="UInt64" default_value="100000" changeable_without_restart="Yes" />

Si el número de partes activas supera el valor especificado, el servidor de ClickHouse añadirá mensajes de advertencia a la tabla `system.warnings`.

**Ejemplo**

```xml
<max_part_num_to_warn>400</max_part_num_to_warn>
```

<div id="max_partition_size_to_drop">
  ## max_partition_size_to_drop
</div>

<SettingsInfoBlock type="UInt64" default_value="50000000000" changeable_without_restart="Yes" />

Restricción sobre la eliminación de particiones.

Si el tamaño de una tabla [MergeTree](/es/reference/engines/table-engines/mergetree-family/mergetree) supera [`max_partition_size_to_drop`](#max_partition_size_to_drop) (en bytes), no se puede eliminar una partición mediante una consulta [DROP PARTITION](/es/reference/statements/alter/partition#drop-partitionpart).
Esta configuración no requiere reiniciar el servidor de ClickHouse para que surta efecto. Otra forma de desactivar la restricción es crear el archivo `<clickhouse-path>/flags/force_drop_table`.

<Note>
  El valor `0` significa que puedes eliminar particiones sin ninguna restricción.

  Esta limitación no restringe drop table ni truncate table; consulta [max&#95;table&#95;size&#95;to&#95;drop](/es/reference/settings/session-settings#max_table_size_to_drop)
</Note>

**Ejemplo**

```xml
<max_partition_size_to_drop>0</max_partition_size_to_drop>
```

<div id="max_parts_cleaning_thread_pool_size">
  ## max_parts_cleaning_thread_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="128" changeable_without_restart="Yes" />El número de hilos para la eliminación simultánea de partes de datos inactivas.

<div id="max_pending_mutations_execution_time_to_warn">
  ## max_pending_mutations_execution_time_to_warn
</div>

<SettingsInfoBlock type="UInt64" default_value="86400" changeable_without_restart="Sí" />

Si alguna de las mutaciones pendientes supera el valor especificado en segundos, servidor de ClickHouse añadirá mensajes de advertencia a la tabla `system.warnings`.

**Ejemplo**

```xml
<max_pending_mutations_execution_time_to_warn>10000</max_pending_mutations_execution_time_to_warn>
```

<div id="max_pending_mutations_to_warn">
  ## max_pending_mutations_to_warn
</div>

<SettingsInfoBlock type="UInt64" default_value="500" changeable_without_restart="Yes" />

Si el número de mutaciones pendientes supera el valor especificado, el servidor de ClickHouse añadirá mensajes de advertencia a la tabla `system.warnings`.

**Ejemplo**

```xml
<max_pending_mutations_to_warn>400</max_pending_mutations_to_warn>
```

<div id="max_prefixes_deserialization_thread_pool_free_size">
  ## max_prefixes_deserialization_thread_pool_free_size
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Sí" />

Si el número de **hilos** inactivos en el pool de hilos de deserialización de prefijos supera `max_prefixes_deserialization_thread_pool_free_size`, ClickHouse liberará los recursos ocupados por los hilos inactivos y reducirá el tamaño del grupo. Los hilos pueden volver a crearse si es necesario.

<div id="max_prefixes_deserialization_thread_pool_size">
  ## max_prefixes_deserialization_thread_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="100" changeable_without_restart="Sí" />

ClickHouse usa hilos del pool de hilos de deserialización de prefijos para la lectura paralela de metadatos de columnas y subcolumnas desde prefijos de archivo en partes Wide de MergeTree. `max_prefixes_deserialization_thread_pool_size` limita el número máximo de hilos del grupo.

<div id="max_remote_read_network_bandwidth_for_server">
  ## max_remote_read_network_bandwidth_for_server
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Sí" />

La velocidad máxima de intercambio de datos a través de la red, en bytes por segundo, para la lectura.

<Note>
  Un valor de `0` (predeterminado) significa que no hay límite.
</Note>

<div id="max_remote_write_network_bandwidth_for_server">
  ## max_remote_write_network_bandwidth_for_server
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Yes" />

La velocidad máxima de transferencia de datos a través de la red, en bytes por segundo, para escritura.

<Note>
  Un valor de `0` (predeterminado) significa que no hay límite.
</Note>

<div id="max_replicated_fetches_network_bandwidth_for_server">
  ## max_replicated_fetches_network_bandwidth_for_server
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />La velocidad máxima de intercambio de datos por la red, en bytes por segundo, para los fetches replicados. Cero significa sin límite.

<div id="max_replicated_sends_network_bandwidth_for_server">
  ## max_replicated_sends_network_bandwidth_for_server
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />La velocidad máxima de transferencia de datos por la red, en bytes por segundo, para los envíos replicados. Cero significa sin límite.

<div id="max_replicated_table_num_to_throw">
  ## max_replicated_table_num_to_throw
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />

Si el número de tablas replicadas es mayor que este valor, el servidor lanzará una excepción.

Solo se cuentan las tablas de los siguientes motores de base de datos:

* Atomic
* Ordinary
* Replicated
* Lazy

<Note>
  Un valor de `0` significa que no hay ninguna limitación.
</Note>

**Ejemplo**

```xml
<max_replicated_table_num_to_throw>400</max_replicated_table_num_to_throw>
```

<div id="max_server_memory_usage">
  ## max_server_memory_usage
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Yes" />

La cantidad máxima de memoria que se permite usar al servidor, expresada en bytes.

<Note>
  El consumo máximo de memoria del servidor se restringe aún más mediante la configuración `max_server_memory_usage_to_ram_ratio`.
</Note>

Como caso especial, un valor de `0` (predeterminado) significa que el servidor puede consumir toda la memoria disponible (excepto las restricciones adicionales impuestas por `max_server_memory_usage_to_ram_ratio`).

<div id="max_server_memory_usage_to_ram_ratio">
  ## max_server_memory_usage_to_ram_ratio
</div>

<SettingsInfoBlock type="Double" default_value="0.9" changeable_without_restart="No" />

La cantidad máxima de memoria que se permite usar al servidor, expresada como una proporción de toda la memoria disponible.

Por ejemplo, un valor de `0.9` (predeterminado) significa que el servidor puede consumir el 90 % de la memoria disponible.

Permite reducir el uso de memoria en sistemas con poca memoria.
En hosts con poca RAM y swap, es posible que necesite configurar [`max_server_memory_usage_to_ram_ratio`](#max_server_memory_usage_to_ram_ratio) con un valor superior a 1.

Esta proporción se aplica de dos maneras:

* Al iniciar (y al recargar la configuración), limita el límite estricto de memoria del servidor a esta fracción de la RAM física total, como se ha descrito anteriormente.
* En tiempo de ejecución, el worker de memoria en segundo plano vuelve a calcular periódicamente el límite estricto de memoria como `(resident memory + system available memory) * max_server_memory_usage_to_ram_ratio`, de modo que el servidor deje holgura para otros procesos que se ejecutan en el mismo host. Cuando se ejecuta dentro de un cgroup con un límite de memoria finito, se usa la memoria disponible del cgroup en lugar del valor de todo el host. A medida que otros procesos aumentan y la memoria disponible se reduce, el límite estricto del servidor disminuye con ella, limitando el crecimiento antes de que el host (o el cgroup) se quede sin memoria.

Configurar la proporción en `0` deshabilita tanto el límite al inicio como el ajuste en tiempo de ejecución. El ajuste en tiempo de ejecución tampoco tiene efecto en sistemas que no son Linux ni en `clickhouse-keeper`, que no expone esta configuración. Para mantener el límite estático de inicio/recarga pero deshabilitar el ajuste en tiempo de ejecución (el comportamiento de versiones anteriores), configure `memory_worker_dynamic_hard_limit` en `0`.

<Note>
  El consumo máximo de memoria del servidor está además limitado por la configuración `max_server_memory_usage`.
</Note>

<div id="max_session_timeout">
  ## max_session_timeout
</div>

Tiempo máximo de espera de la sesión, en segundos.

Ejemplo:

```xml
<max_session_timeout>3600</max_session_timeout>
```

<div id="max_snapshot_commit_thread_pool_free_size">
  ## max_snapshot_commit_thread_pool_free_size
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Yes" />Si el número de hilos inactivos en el pool de threads de commit de instantánea supera `max_snapshot_commit_thread_pool_free_size`, ClickHouse liberará los recursos ocupados por esos hilos inactivos y reducirá el tamaño del pool. Los hilos pueden volver a crearse si es necesario.

<div id="max_snapshot_commit_thread_pool_size">
  ## max_snapshot_commit_thread_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="64" changeable_without_restart="Sí" />Número de hilos para el commit de la instantánea.

<div id="max_table_num_to_throw">
  ## max_table_num_to_throw
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />

Si el número de tablas es mayor que este valor, el servidor generará una excepción.

Las siguientes tablas no se tienen en cuenta:

* view
* remote
* dictionary
* system

Solo cuenta las tablas de los siguientes motores de base de datos:

* Atomic
* Ordinary
* Replicated
* Lazy

<Note>
  Un valor de `0` significa que no hay límite.
</Note>

**Ejemplo**

```xml
<max_table_num_to_throw>400</max_table_num_to_throw>
```

<div id="max_table_num_to_warn">
  ## max_table_num_to_warn
</div>

<SettingsInfoBlock type="UInt64" default_value="5000" changeable_without_restart="Yes" />

Si el número de tablas adjuntas supera el valor especificado, el servidor de ClickHouse añadirá mensajes de advertencia a la tabla `system.warnings`.

**Ejemplo**

```xml
<max_table_num_to_warn>400</max_table_num_to_warn>
```

<div id="max_table_size_to_drop">
  ## max_table_size_to_drop
</div>

<SettingsInfoBlock type="UInt64" default_value="50000000000" changeable_without_restart="Yes" />

Restricción para eliminar tablas.

Si el tamaño de una tabla [MergeTree](/es/reference/engines/table-engines/mergetree-family/mergetree) supera `max_table_size_to_drop` (en bytes), no puedes eliminarla mediante una consulta [`DROP`](/es/reference/statements/drop) o [`TRUNCATE`](/es/reference/statements/truncate).

<Note>
  Un valor de `0` significa que puedes eliminar todas las tablas sin ninguna restricción.

  Esta configuración no requiere reiniciar el servidor de ClickHouse para aplicarse. Otra forma de desactivar la restricción es crear el archivo `<clickhouse-path>/flags/force_drop_table`.
</Note>

**Ejemplo**

```xml
<max_table_size_to_drop>0</max_table_size_to_drop>
```

<div id="max_temporary_data_on_disk_size">
  ## max_temporary_data_on_disk_size
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />

La cantidad máxima de almacenamiento que se puede usar para la agregación externa, las operaciones JOIN o la ordenación.
Las consultas que superen este límite fallarán con una excepción.

<Note>
  Un valor de `0` significa que no hay límite.
</Note>

Véase también:

* [`max_temporary_data_on_disk_size_for_user`](/es/reference/settings/session-settings#max_temporary_data_on_disk_size_for_user)
* [`max_temporary_data_on_disk_size_for_query`](/es/reference/settings/session-settings#max_temporary_data_on_disk_size_for_query)

<div id="max_thread_pool_free_size">
  ## max_thread_pool_free_size
</div>

<SettingsInfoBlock type="UInt64" default_value="1000" changeable_without_restart="No" />

Si el número de hilos **inactivos** en el Global pool de hilos es mayor que [`max_thread_pool_free_size`](/es/reference/settings/server-settings/settings#max_thread_pool_free_size), ClickHouse libera parte de los recursos que ocupan algunos hilos y reduce el tamaño del pool. Los hilos pueden volver a crearse si es necesario.

**Ejemplo**

```xml
<max_thread_pool_free_size>1200</max_thread_pool_free_size>
```

<div id="max_thread_pool_size">
  ## max_thread_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="10000" changeable_without_restart="No" />

ClickHouse usa hilos del pool global de hilos para procesar consultas. Si no hay ningún hilo inactivo para procesar una consulta, se crea un hilo nuevo en el pool. `max_thread_pool_size` limita el número máximo de hilos en el pool.

**Ejemplo**

```xml
<max_thread_pool_size>12000</max_thread_pool_size>
```

<div id="max_unexpected_parts_loading_thread_pool_size">
  ## max_unexpected_parts_loading_thread_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="8" changeable_without_restart="No" />El número de hilos para cargar al inicio el conjunto inactivo de partes de datos (las marcadas como Unexpected).

<div id="max_view_num_to_throw">
  ## max_view_num_to_throw
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />

Si el número de vistas supera este valor, el servidor generará una excepción.

Solo contabiliza las tablas de los motores de base de datos:

* Atomic
* Ordinary
* Replicated
* Lazy

<Note>
  Un valor de `0` significa que no hay ningún límite.
</Note>

**Ejemplo**

```xml
<max_view_num_to_throw>400</max_view_num_to_throw>
```

<div id="max_view_num_to_warn">
  ## max_view_num_to_warn
</div>

<SettingsInfoBlock type="UInt64" default_value="10000" changeable_without_restart="Yes" />

Si el número de vistas adjuntas supera el valor especificado, el servidor de ClickHouse añadirá mensajes de advertencia a la tabla `system.warnings`.

**Ejemplo**

```xml
<max_view_num_to_warn>400</max_view_num_to_warn>
```

<div id="max_waiting_queries">
  ## max_waiting_queries
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Yes" />

Límite del número total de consultas en espera simultáneas.
La ejecución de una consulta en espera se bloquea mientras las tablas necesarias se cargan de forma asíncrona (consulte [`async_load_databases`](/es/reference/settings/server-settings/settings#async_load_databases).

<Note>
  Las consultas en espera no se tienen en cuenta al comprobar los límites controlados por las siguientes configuraciones:

  * [`max_concurrent_queries`](/es/reference/settings/server-settings/settings#max_concurrent_queries)
  * [`max_concurrent_insert_queries`](/es/reference/settings/server-settings/settings#max_concurrent_insert_queries)
  * [`max_concurrent_select_queries`](/es/reference/settings/server-settings/settings#max_concurrent_select_queries)
  * [`max_concurrent_queries_for_user`](/es/reference/settings/session-settings#max_concurrent_queries_for_user)
  * [`max_concurrent_queries_for_all_users`](/es/reference/settings/session-settings#max_concurrent_queries_for_all_users)

  Esta corrección se realiza para evitar alcanzar estos límites justo después del inicio del servidor.
</Note>

<Note>
  Un valor de `0` (predeterminado) significa que no hay límite.

  Esta configuración puede modificarse en tiempo de ejecución y surtirá efecto de inmediato. Las consultas que ya estén en ejecución no cambiarán.
</Note>

<div id="max_zookeeper_pooled_connections">
  ## max_zookeeper_pooled_connections
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />

Número máximo de sesiones de ZooKeeper inicializadas de forma diferida por cada clúster de ZooKeeper en el pool compartido.

Un valor de `0` desactiva las conexiones en pool y mantiene el uso de una sola sesión.

<div id="memory_worker_correct_memory_tracker">
  ## memory_worker_correct_memory_tracker
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />

Indica si el worker de memoria en segundo plano debe corregir el rastreador de memoria interno basándose en la información de fuentes externas como jemalloc y cgroups

<div id="memory_worker_decay_adjustment_period_ms">
  ## memory_worker_decay_adjustment_period_ms
</div>

<SettingsInfoBlock type="UInt64" default_value="5000" changeable_without_restart="No" />

Duración, en milisegundos, durante la cual la presión de memoria debe persistir antes de ajustar dinámicamente `dirty_decay_ms` de jemalloc. Cuando el uso de memoria se mantiene por encima del umbral de purga durante este período, se desactiva el decaimiento automático de las páginas sucias (`dirty_decay_ms=0`) para recuperar memoria de forma agresiva. Cuando el uso se mantiene por debajo del umbral durante este período, se restablece el comportamiento de decaimiento predeterminado. Establézcalo en 0 para desactivar el ajuste dinámico y usar la configuración de decaimiento predeterminada de jemalloc.

<div id="memory_worker_dynamic_hard_limit">
  ## memory_worker_dynamic_hard_limit
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />

Indica si el worker de memoria en segundo plano vuelve a calcular periódicamente, en tiempo de ejecución, el límite estricto de memoria del server como `(resident memory + system available memory) * max_server_memory_usage_to_ram_ratio`, para que el server deje holgura para otros procesos que se ejecutan en el mismo host.

Cuando está desactivado, `max_server_memory_usage_to_ram_ratio` solo limita de forma estática el límite estricto de memoria, al inicio y al recargar la configuración, como en versiones anteriores.

No tiene efecto cuando `max_server_memory_usage_to_ram_ratio` es `0`.

<div id="memory_worker_period_ms">
  ## memory_worker_period_ms
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />

Período de activación del worker de memoria en segundo plano que corrige el uso de memoria del rastreador de memoria y limpia las páginas no utilizadas cuando el uso de memoria es elevado. Si se establece en 0, se usará el valor predeterminado según la fuente del uso de memoria.

<div id="memory_worker_purge_dirty_pages_threshold_ratio">
  ## memory_worker_purge_dirty_pages_threshold_ratio
</div>

<SettingsInfoBlock type="Double" default_value="0.2" changeable_without_restart="No" />

La proporción umbral de páginas sucias de jemalloc con respecto a la memoria disponible para el servidor de ClickHouse. Cuando el tamaño de las páginas sucias supera esta proporción, el worker de memoria en segundo plano fuerza la purga de esas páginas. Si se establece en 0, se desactiva la purga forzada basada en la proporción de páginas sucias.

<div id="memory_worker_purge_total_memory_threshold_ratio">
  ## memory_worker_purge_total_memory_threshold_ratio
</div>

<SettingsInfoBlock type="Double" default_value="0.9" changeable_without_restart="No" />

La relación de umbral para purgar `jemalloc` con respecto a la memoria disponible para el servidor ClickHouse. Cuando el uso total de memoria supera esta relación, el worker de memoria en segundo plano fuerza la purga de páginas sucias. Si se establece en 0, se desactiva la purga forzada basada en la memoria total.

<div id="memory_worker_use_cgroup">
  ## memory_worker_use_cgroup
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />Usa la información actual del uso de memoria del cgroup para corregir el seguimiento de memoria.

<div id="merge_tree">
  ## merge_tree
</div>

Ajustes para las tablas de [MergeTree](/es/reference/engines/table-engines/mergetree-family/mergetree).

Para obtener más información, consulte el archivo de encabezado MergeTreeSettings.h.

**Ejemplo**

```xml
<merge_tree>
    <max_suspicious_broken_parts>5</max_suspicious_broken_parts>
</merge_tree>
```

<div id="merge_workload">
  ## merge_workload
</div>

<SettingsInfoBlock type="String" default_value="default" changeable_without_restart="Sí" />

Se utiliza para regular cómo se usan y comparten los recursos entre las fusiones y otras cargas de trabajo. El valor especificado se utiliza como valor de la configuración `workload` para todas las fusiones en segundo plano. Puede sobrescribirse con una configuración de MergeTree.

**Véase también**

* [Planificación de cargas de trabajo](/es/concepts/features/configuration/server-config/workload-scheduling)

<div id="merges_mutations_memory_usage_soft_limit">
  ## merges_mutations_memory_usage_soft_limit
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />

Establece el límite de RAM que se puede usar para realizar operaciones de merge y mutation.
Si ClickHouse alcanza el límite establecido, no programará nuevas operaciones de merge o mutation en segundo plano, pero seguirá ejecutando las tareas ya programadas.

<Note>
  Un valor de `0` significa ilimitado.
</Note>

**Ejemplo**

```xml
<merges_mutations_memory_usage_soft_limit>0</merges_mutations_memory_usage_soft_limit>
```

<div id="merges_mutations_memory_usage_to_ram_ratio">
  ## merges_mutations_memory_usage_to_ram_ratio
</div>

<SettingsInfoBlock type="Double" default_value="0.5" changeable_without_restart="No" />

El valor predeterminado de `merges_mutations_memory_usage_soft_limit` se calcula como `memory_amount * merges_mutations_memory_usage_to_ram_ratio`.

**Véase también:**

* [max&#95;memory&#95;usage](/es/reference/settings/session-settings#max_memory_usage)
* [merges&#95;mutations&#95;memory&#95;usage&#95;soft&#95;limit](/es/reference/settings/server-settings/settings#merges_mutations_memory_usage_soft_limit)

<div id="message_queue_disable_insertion">
  ## message_queue_disable_insertion
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="Yes" />Deshabilita la inserción desde los motores de cola de mensajes (Kafka, RabbitMQ, NATS) hacia las vistas materializadas adjuntas

<div id="metric_log">
  ## metric_log
</div>

Está desactivado de forma predeterminada.

**Activación**

Para activar manualmente la recopilación del historial de métricas [`system.metric_log`](/es/reference/system-tables/metric_log), cree `/etc/clickhouse-server/config.d/metric_log.xml` con el siguiente contenido:

```xml
<clickhouse>
    <metric_log>
        <database>system</database>
        <table>metric_log</table>
        <flush_interval_milliseconds>7500</flush_interval_milliseconds>
        <collect_interval_milliseconds>1000</collect_interval_milliseconds>
        <max_size_rows>1048576</max_size_rows>
        <reserved_size_rows>8192</reserved_size_rows>
        <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
        <flush_on_crash>false</flush_on_crash>
    </metric_log>
</clickhouse>
```

**Desactivación**

Para desactivar la configuración `metric_log`, cree el siguiente archivo `/etc/clickhouse-server/config.d/disable_metric_log.xml` con el siguiente contenido:

```xml
<clickhouse>
    <metric_log remove="1" />
</clickhouse>
```

<SystemLogParameters />

<div id="min_allocation_size_to_throw_on_memory_limit">
  ## min_allocation_size_to_throw_on_memory_limit
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Yes" />

Tamaño mínimo, en bytes, de una asignación genérica de C++ (del tipo que realizan los contenedores estándar, las strings, el crecimiento de `std::vector`, los punteros inteligentes, etc.) que puede provocar `MEMORY_LIMIT_EXCEEDED` una vez alcanzado `max_server_memory_usage`. Las asignaciones genéricas más pequeñas se siguen contabilizando en el rastreador de memoria, pero se les permite completarse incluso después de superar el límite, lo que reduce los fallos espurios durante las rutas de limpieza y manejo de excepciones cerca de OOM.

Las asignaciones realizadas por los propios búferes grandes de ClickHouse — datos de columnas, tablas hash, arenas, pipelines de consulta, búferes de E/S — siempre respetan el límite de memoria y pueden lanzar una excepción independientemente de este valor. Esta configuración solo afecta a las asignaciones implícitas que pasan por `operator new`.

Un valor de `0` (predeterminado) conserva el comportamiento heredado: el rastreador de memoria nunca rechaza las asignaciones implícitas de `operator new`, y solo las asignaciones explícitas de ClickHouse pueden provocar `MEMORY_LIMIT_EXCEEDED`.

Nota: para evitar efectos secundarios, se recomienda establecer un valor mayor que `max_untracked_memory`.

<div id="min_os_cpu_wait_time_ratio_to_drop_connection">
  ## min_os_cpu_wait_time_ratio_to_drop_connection
</div>

<SettingsInfoBlock type="Float" default_value="0" changeable_without_restart="Yes" />

Relación mínima entre los tiempos de espera de CPU del SO (métrica OSCPUWaitMicroseconds) y de actividad (métrica OSCPUVirtualTimeMicroseconds) para considerar el cierre de conexiones. Se utiliza interpolación lineal entre la relación mínima y la máxima para calcular la probabilidad; en este punto, la probabilidad es 0.
Consulta [Control del comportamiento ante la sobrecarga de CPU del servidor](/es/concepts/features/configuration/settings/server-overload) para obtener más información.

<div id="mlock_executable">
  ## mlock_executable
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />

Ejecute `<mlockall>` después del inicio para reducir la latencia de las primeras consultas y evitar que el ejecutable de ClickHouse se intercambie a disco con una carga alta de IO.

<Note>
  Se recomienda habilitar esta opción, pero aumentará el tiempo de inicio en unos segundos. Tenga en cuenta que esta configuración no funcionará sin la capacidad &quot;CAP&#95;IPC&#95;LOCK&quot;.
</Note>

**Ejemplo**

```xml
<mlock_executable>false</mlock_executable>
```

<div id="mlock_executable_min_total_memory_amount_bytes">
  ## mlock_executable_min_total_memory_amount_bytes
</div>

<SettingsInfoBlock type="UInt64" default_value="5000000000" changeable_without_restart="No" />El umbral mínimo de memoria para ejecutar `<mlockall>`

<div id="mmap_cache_size">
  ## mmap_cache_size
</div>

<SettingsInfoBlock type="UInt64" default_value="1024" changeable_without_restart="Sí" />

Esta configuración permite evitar llamadas frecuentes a open/close (que son muy costosas debido a los fallos de página que provocan) y reutilizar mapeos entre varios threads y consultas. El valor de esta configuración es el número de regiones mapeadas (normalmente igual al número de archivos mapeados).

La cantidad de datos en archivos mapeados puede supervisarse en las siguientes system tables mediante las siguientes métricas:

* `MMappedFiles`/`MMappedFileBytes`/`MMapCacheCells` en [`system.metrics`](/es/reference/system-tables/metrics), [`system.metric_log`](/es/reference/system-tables/metric_log)
* `CreatedReadBufferMMap`/`CreatedReadBufferMMapFailed`/`MMappedFileCacheHits`/`MMappedFileCacheMisses` en [`system.events`](/es/reference/system-tables/events), [`system.processes`](/es/reference/system-tables/processes), [`system.query_log`](/es/reference/system-tables/query_log), [`system.query_thread_log`](/es/reference/system-tables/query_thread_log), [`system.query_views_log`](/es/reference/system-tables/query_views_log)

<Note>
  La cantidad de datos en archivos mapeados no consume memoria directamente y no se contabiliza en el uso de memoria de la consulta ni del server, porque esta memoria puede descartarse de forma similar a la caché de páginas del SO. La caché se elimina (los archivos se cierran) automáticamente al eliminar las partes antiguas en tablas de la familia MergeTree; también puede eliminarse manualmente con la consulta `SYSTEM DROP MMAP CACHE`.

  Esta configuración puede modificarse en runtime y surtirá efecto de inmediato.
</Note>

<div id="mutation_workload">
  ## mutation_workload
</div>

<SettingsInfoBlock type="String" default_value="default" changeable_without_restart="Sí" />

Se utiliza para regular cómo se usan y comparten los recursos entre las mutaciones y otras cargas de trabajo. El valor especificado se utiliza como valor de la configuración `workload` para todas las mutaciones en segundo plano. Puede sobrescribirse con una configuración de MergeTree.

**Véase también**

* [Planificación de cargas de trabajo](/es/concepts/features/configuration/server-config/workload-scheduling)

<div id="mysql_port">
  ## mysql_port
</div>

Puerto para comunicarse con los clientes mediante el protocolo MySQL.

<Note>
  * Los enteros positivos especifican el número de puerto en el que se escuchará
  * Los valores vacíos se utilizan para deshabilitar la comunicación con los clientes mediante el protocolo MySQL.
</Note>

**Ejemplo**

```xml
<mysql_port>9004</mysql_port>
```

<div id="mysql_require_secure_transport">
  ## mysql_require_secure_transport
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Si se establece en true, se requiere una comunicación segura con los clientes a través de [mysql&#95;port](/es/reference/settings/server-settings/settings#mysql_port). Se rechazará cualquier conexión con la opción `<--ssl-mode=none>`. Úselo junto con la configuración de [OpenSSL](/es/reference/settings/server-settings/settings#openssl).

<div id="oom_canary_enable">
  ## oom_canary_enable
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />

Experimental. Habilita el canario OOM: un proceso hijo de sacrificio que atrae al OOM killer de Linux
antes que el proceso principal del servidor ClickHouse, lo que le da al servidor la oportunidad de aliviar la carga.
Requiere Linux &gt;= 5.3 (para `pidfd_open`); el canario se desactiva al iniciar en kernels más antiguos.
La respuesta ante OOM requiere evidencia de OOM-kill en `memory.events.local` de cgroup v2 y puede ejecutar la cancelación global de
consultas, la cancelación de merge y escrituras en `system.crash_log`.
El canario no puede proteger al servidor cuando `memory.oom.group` de cgroup v2 está habilitado para el
cgroup del servidor: en ese caso, el kernel mata todo el cgroup como una sola unidad, incluido el servidor,
por lo que la respuesta ante OOM nunca llega a ejecutarse. En este modo, se registra una advertencia al iniciar.
El comportamiento puede cambiar entre versiones de ClickHouse hasta que se complete la validación para producción.

<div id="oom_canary_initial_backoff_seconds">
  ## oom_canary_initial_backoff_seconds
</div>

<SettingsInfoBlock type="UInt64" default_value="1" changeable_without_restart="No" />

Retraso inicial, en segundos, entre relanzamientos consecutivos del canario OOM.
El retraso se duplica con cada relanzamiento hasta `oom_canary_max_backoff_seconds`.

<div id="oom_canary_max_backoff_seconds">
  ## oom_canary_max_backoff_seconds
</div>

<SettingsInfoBlock type="UInt64" default_value="60" changeable_without_restart="No" />

Retraso máximo en segundos entre relanzamientos consecutivos del canario OOM.

<div id="oom_canary_max_rapid_relaunches">
  ## oom_canary_max_rapid_relaunches
</div>

<SettingsInfoBlock type="UInt64" default_value="10" changeable_without_restart="No" />

Número máximo de relanzamientos rápidos consecutivos del canario OOM antes de que se desactive el relanzamiento automático,
para evitar reinicios continuos bajo una presión de memoria sostenida. El contador y el retraso de relanzamiento se restablecen
cuando un canario sobrevive más de `oom_canary_max_backoff_seconds`, por lo que un canario que solo muere
esporádicamente durante un largo tiempo de actividad no termina desactivándose.
Se aplica solo cuando `oom_canary_relaunch` es true.

<div id="oom_canary_relaunch">
  ## oom_canary_relaunch
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />

Cuando es true, el canario OOM se relanza automáticamente después de que el proceso canario finalice por cualquier
motivo distinto de un fallo permanente de configuración o del apagado del server, incluidos los OOM kills, otras
señales y salidas transitorias, sujeto a `oom_canary_max_rapid_relaunches` y a la configuración de retraso.
La propia secuencia de respuesta ante OOM solo se ejecuta cuando cgroup v2 `memory.events.local`
proporciona indicios de OOM-kill.

<div id="oom_canary_size">
  ## oom_canary_size
</div>

<SettingsInfoBlock type="UInt64" default_value="104857600" changeable_without_restart="No" />

Tamaño en bytes de la región de memoria que el proceso hijo del canario OOM asigna y a la que accede.
El bloqueo de la región con `mlock` se realiza en la medida de lo posible: requiere `CAP_IPC_LOCK` o un
`RLIMIT_MEMLOCK` suficiente, y cuando el bloqueo falla el canario registra una advertencia y la memoria permanece
asignada, pero puede paginarse a swap.
El valor predeterminado es 100 MB (104857600). Los valores más grandes hacen que el canario sea un objetivo de OOM más probable.

<div id="oom_score">
  ## oom_score
</div>

<SettingsInfoBlock type="Int32" default_value="0" changeable_without_restart="No" />En sistemas Linux, esto puede controlar el comportamiento del OOM killer.

<div id="openssl">
  ## openSSL
</div>

Configuración SSL de cliente/servidor.

El soporte de SSL lo proporciona la biblioteca `libpoco`. Las opciones de configuración disponibles se explican en [SSLManager.h](https://github.com/ClickHouse-Extras/poco/blob/master/NetSSL_OpenSSL/include/Poco/Net/SSLManager.h). Los valores predeterminados pueden consultarse en [SSLManager.cpp](https://github.com/ClickHouse-Extras/poco/blob/master/NetSSL_OpenSSL/src/SSLManager.cpp).

Claves para la configuración del servidor/cliente:

| Opción                        | Descripción                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Valor predeterminado                                                                       |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------ |
| `cacheSessions`               | Habilita o deshabilita el almacenamiento en caché de las sesiones. Debe usarse en combinación con `sessionIdContext`. Valores aceptables: `true`, `false`.                                                                                                                                                                                                                                                                                                                                                                           | `false`                                                                                    |
| `caConfig`                    | Ruta al archivo o directorio que contiene certificados de CA de confianza. Si apunta a un archivo, debe estar en formato PEM y puede contener varios certificados de CA. Si apunta a un directorio, debe contener un archivo .pem por cada certificado de CA. Los nombres de archivo se buscan mediante el valor hash del nombre del sujeto de la CA. Puede encontrar más detalles en la página man de [SSL&#95;CTX&#95;load&#95;verify&#95;locations](https://www.openssl.org/docs/man3.0/man3/SSL_CTX_load_verify_locations.html). |                                                                                            |
| `certificateFile`             | Ruta del archivo de certificado de cliente/servidor en formato PEM. Puede omitirse si `privateKeyFile` contiene el certificado.                                                                                                                                                                                                                                                                                                                                                                                                      |                                                                                            |
| `cipherList`                  | Cifrados de OpenSSL compatibles.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `ALL:!ADH:!LOW:!EXP:!MD5:!3DES:@STRENGTH`                                                  |
| `disableProtocols`            | Protocolos cuyo uso no está permitido.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |                                                                                            |
| `extendedVerification`        | Si está habilitado, verifica que el CN o SAN del certificado coincida con el hostname del peer.                                                                                                                                                                                                                                                                                                                                                                                                                                      | `false`                                                                                    |
| `fips`                        | Activa el modo FIPS de OpenSSL. Compatible si la versión de OpenSSL de la biblioteca admite FIPS.                                                                                                                                                                                                                                                                                                                                                                                                                                    | `false`                                                                                    |
| `invalidCertificateHandler`   | Clase (una subclase de CertificateHandler) para verificar certificados inválidos. Por ejemplo: `<invalidCertificateHandler> <name>RejectCertificateHandler</name> </invalidCertificateHandler>` .                                                                                                                                                                                                                                                                                                                                    | `RejectCertificateHandler`                                                                 |
| `loadDefaultCAFile`           | Si se utilizarán los certificados de CA integrados de OpenSSL. ClickHouse asume que los certificados de CA integrados se encuentran en el archivo `/etc/ssl/cert.pem` (resp. el directorio `/etc/ssl/certs`) o en el archivo (resp. directorio) especificado por la variable de entorno `SSL_CERT_FILE` (resp. `SSL_CERT_DIR`).                                                                                                                                                                                                      | `true`                                                                                     |
| `preferServerCiphers`         | Suites de cifrado del servidor preferidas por el cliente.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `false`                                                                                    |
| `privateKeyFile`              | Ruta al archivo que contiene la clave privada del certificado PEM. El archivo puede contener la clave y el certificado al mismo tiempo.                                                                                                                                                                                                                                                                                                                                                                                              |                                                                                            |
| `privateKeyPassphraseHandler` | Clase (subclase de PrivateKeyPassphraseHandler) que solicita la frase de paso para acceder a la clave privada. Por ejemplo: `<privateKeyPassphraseHandler>`, `<name>KeyFileHandler</name>`, `<options><password>test</password></options>`, `</privateKeyPassphraseHandler>`.                                                                                                                                                                                                                                                        | `KeyConsoleHandler`                                                                        |
| `requireTLSv1`                | Requiere una conexión TLSv1. Valores aceptables: `true`, `false`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `false`                                                                                    |
| `requireTLSv1_1`              | Requiere una conexión con TLSv1.1. Valores aceptables: `true`, `false`.                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `false`                                                                                    |
| `requireTLSv1_2`              | Requiere una conexión TLSv1.2. Valores aceptables: `true`, `false`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `false`                                                                                    |
| `sessionCacheSize`            | El número máximo de sesiones que el servidor mantiene en caché. Un valor de `0` significa que el número de sesiones es ilimitado.                                                                                                                                                                                                                                                                                                                                                                                                    | [1024*20](https://github.com/ClickHouse/boringssl/blob/master/include/openssl/ssl.h#L1978) |
| `sessionIdContext`            | Un conjunto único de caracteres aleatorios que el servidor añade a cada identificador generado. La longitud de la cadena no debe superar `SSL_MAX_SSL_SESSION_ID_LENGTH`. Este parámetro se recomienda siempre, ya que ayuda a evitar problemas tanto si el servidor almacena la sesión en caché como si el cliente ha solicitado el almacenamiento en caché.                                                                                                                                                                        | `$\{application.name\}`                                                                    |
| `sessionTimeout`              | Tiempo durante el que la sesión se almacena en caché en el servidor, en horas.                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `2`                                                                                        |
| `verificationDepth`           | La longitud máxima de la cadena de verificación. La verificación fallará si la longitud de la cadena de certificados supera el valor establecido.                                                                                                                                                                                                                                                                                                                                                                                    | `9`                                                                                        |
| `verificationMode`            | El método para verificar los certificados del nodo. Los detalles se encuentran en la descripción de la clase [Context](https://github.com/ClickHouse-Extras/poco/blob/master/NetSSL_OpenSSL/include/Poco/Net/Context.h). Posibles valores: `none`, `relaxed`, `strict`, `once`.                                                                                                                                                                                                                                                      | `relaxed`                                                                                  |

**Ejemplo de configuración:**

```xml
<openSSL>
    <server>
        <!-- openssl req -subj "/CN=localhost" -new -newkey rsa:2048 -days 365 -nodes -x509 -keyout /etc/clickhouse-server/server.key -out /etc/clickhouse-server/server.crt -->
        <certificateFile>/etc/clickhouse-server/server.crt</certificateFile>
        <privateKeyFile>/etc/clickhouse-server/server.key</privateKeyFile>
        <!-- openssl dhparam -out /etc/clickhouse-server/dhparam.pem 4096 -->
        <dhParamsFile>/etc/clickhouse-server/dhparam.pem</dhParamsFile>
        <verificationMode>none</verificationMode>
        <loadDefaultCAFile>true</loadDefaultCAFile>
        <cacheSessions>true</cacheSessions>
        <disableProtocols>sslv2,sslv3</disableProtocols>
        <preferServerCiphers>true</preferServerCiphers>
    </server>
    <client>
        <loadDefaultCAFile>true</loadDefaultCAFile>
        <cacheSessions>true</cacheSessions>
        <disableProtocols>sslv2,sslv3</disableProtocols>
        <preferServerCiphers>true</preferServerCiphers>
        <!-- Usar para certificados autofirmados: <verificationMode>none</verificationMode> -->
        <invalidCertificateHandler>
            <!-- Usar para certificados autofirmados: <name>AcceptCertificateHandler</name> -->
            <name>RejectCertificateHandler</name>
        </invalidCertificateHandler>
    </client>
</openSSL>
```

<div id="openssl.client.caconfig">
  ## openSSL.client.caConfig
</div>

Ruta del archivo o directorio que contiene certificados de CA de confianza. Si apunta a un archivo, debe estar en formato PEM y puede contener varios certificados de CA. Si apunta a un directorio, debe contener un archivo `.pem` por cada certificado de CA. Los nombres de archivo se buscan mediante el valor hash del nombre del sujeto de la CA. Puede encontrar más detalles en la página man de [SSL&#95;CTX&#95;load&#95;verify&#95;locations](https://docs.openssl.org/3.0/man3/SSL_CTX_load_verify_locations/).

<div id="openssl.client.cachesessions">
  ## openSSL.client.cacheSessions
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Habilita o deshabilita el almacenamiento en caché de las sesiones. Debe usarse en combinación con `<sessionIdContext>`. Valores aceptables: `<true>`, `<false>`.

<div id="openssl.client.certificatefile">
  ## openSSL.client.certificateFile
</div>

Ruta del archivo de certificado del cliente/servidor en formato PEM. Puede omitirse si `<privateKeyFile>` contiene el certificado.

<div id="openssl.client.cipherlist">
  ## openSSL.client.cipherList
</div>

<SettingsInfoBlock type="String" default_value="ALL:!ADH:!LOW:!EXP:!MD5:!3DES:@STRENGTH" changeable_without_restart="No" />Cifrados de OpenSSL compatibles.

<div id="openssl.client.disableprotocols">
  ## openSSL.client.disableProtocols
</div>

Protocolos que no se permite usar.

<div id="openssl.client.extendedverification">
  ## openSSL.client.extendedVerification
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Si está habilitado, verifica que el CN o SAN del certificado coincida con el hostname del par.

<div id="openssl.client.fips">
  ## openSSL.client.fips
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Activa el modo FIPS de OpenSSL. Está disponible si la versión de OpenSSL de la biblioteca admite FIPS.

<div id="openssl.client.invalidcertificatehandler.name">
  ## openSSL.client.invalidCertificateHandler.name
</div>

<SettingsInfoBlock type="String" default_value="RejectCertificateHandler" changeable_without_restart="No" />Clase (subclase de CertificateHandler) para verificar certificados no válidos. Por ejemplo: `<<invalidCertificateHandler> <name>RejectCertificateHandler</name> </invalidCertificateHandler>>`.

<div id="openssl.client.loaddefaultcafile">
  ## openSSL.client.loadDefaultCAFile
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />Determina si se utilizarán los certificados de CA integrados de OpenSSL. ClickHouse asume que los certificados de CA integrados se encuentran en el archivo `</etc/ssl/cert.pem>` (o bien en el directorio `</etc/ssl/certs>`) o en el archivo (o bien en el directorio) especificado por la variable de entorno `<SSL_CERT_FILE>` (o bien `<SSL_CERT_DIR>`).

<div id="openssl.client.preferserverciphers">
  ## openSSL.client.preferServerCiphers
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Suites de cifrado del servidor preferidas por el cliente.

<div id="openssl.client.privatekeyfile">
  ## openSSL.client.privateKeyFile
</div>

Ruta del archivo que contiene la clave privada del certificado PEM. El archivo puede contener la clave y el certificado al mismo tiempo.

<div id="openssl.client.privatekeypassphrasehandler.name">
  ## openSSL.client.privateKeyPassphraseHandler.name
</div>

<SettingsInfoBlock type="String" default_value="KeyConsoleHandler" changeable_without_restart="No" />Clase (subclase de PrivateKeyPassphraseHandler) que solicita la frase de paso para acceder a la clave privada. Por ejemplo: `<<privateKeyPassphraseHandler>>`, `<<name>KeyFileHandler</name>>`, `<<options><password>test</password></options>>`, `<</privateKeyPassphraseHandler>>`

<div id="openssl.client.requiretlsv1">
  ## openSSL.client.requireTLSv1
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Requiere una conexión TLSv1. Valores aceptables: `<true>`, `<false>`.

<div id="openssl.client.requiretlsv1_1">
  ## openSSL.client.requireTLSv1_1
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Requiere una conexión con TLSv1.1. Valores aceptables: `<true>`, `<false>`.

<div id="openssl.client.requiretlsv1_2">
  ## openSSL.client.requireTLSv1_2
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Requiere una conexión TLSv1.2. Valores aceptables: `<true>`, `<false>`.

<div id="openssl.client.verificationdepth">
  ## openSSL.client.verificationDepth
</div>

<SettingsInfoBlock type="UInt64" default_value="9" changeable_without_restart="No" />La longitud máxima de la cadena de verificación. La verificación fallará si la longitud de la cadena de certificados supera el valor configurado.

<div id="openssl.client.verificationmode">
  ## openSSL.client.verificationMode
</div>

<SettingsInfoBlock type="String" default_value="relaxed" changeable_without_restart="No" />Método para verificar los certificados del nodo. Consulte los detalles en la descripción de la clase [Context](https://github.com/ClickHouse/poco/blob/master/NetSSL_OpenSSL/include/Poco/Net/Context.h). Valores posibles: `<none>`, `<relaxed>`, `<strict>`, `<once>`.

<div id="openssl.server.caconfig">
  ## openSSL.server.caConfig
</div>

Ruta al archivo o directorio que contiene certificados de CA de confianza. Si apunta a un archivo, debe estar en formato PEM y puede contener varios certificados de CA. Si apunta a un directorio, debe contener un archivo .pem por cada certificado de CA. Los nombres de archivo se buscan mediante el valor hash del nombre del subject de la CA. Puede consultar más información en la página man de [SSL&#95;CTX&#95;load&#95;verify&#95;locations](https://docs.openssl.org/3.0/man3/SSL_CTX_load_verify_locations/).

<div id="openssl.server.cachesessions">
  ## openSSL.server.cacheSessions
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Activa o desactiva el almacenamiento en caché de sesiones. Debe usarse en combinación con `<sessionIdContext>`. Valores aceptables: `<true>`, `<false>`.

<div id="openssl.server.certificatefile">
  ## openSSL.server.certificateFile
</div>

Ruta del archivo de certificado de cliente/servidor en formato PEM. Puede omitirse si `<privateKeyFile>` contiene el certificado.

<div id="openssl.server.cipherlist">
  ## openSSL.server.cipherList
</div>

<SettingsInfoBlock type="String" default_value="ALL:!ADH:!LOW:!EXP:!MD5:!3DES:@STRENGTH" changeable_without_restart="No" />Cifrados de OpenSSL admitidos.

<div id="openssl.server.disableprotocols">
  ## openSSL.server.disableProtocols
</div>

Protocolos cuyo uso no se permite.

<div id="openssl.server.extendedverification">
  ## openSSL.server.extendedVerification
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Si está habilitado, verifica que el CN o SAN del certificado coincida con el nombre de host del par.

<div id="openssl.server.fips">
  ## openSSL.server.fips
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Activa el modo FIPS de OpenSSL. Se admite si la versión de OpenSSL de la biblioteca es compatible con FIPS.

<div id="openssl.server.invalidcertificatehandler.name">
  ## openSSL.server.invalidCertificateHandler.name
</div>

<SettingsInfoBlock type="String" default_value="RejectCertificateHandler" changeable_without_restart="No" />Clase (una subclase de CertificateHandler) para verificar certificados inválidos. Por ejemplo: `<<invalidCertificateHandler> <name>RejectCertificateHandler</name> </invalidCertificateHandler>>`.

<div id="openssl.server.loaddefaultcafile">
  ## openSSL.server.loadDefaultCAFile
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />Determina si se utilizarán los certificados de CA integrados de OpenSSL. ClickHouse asume que los certificados de CA integrados se encuentran en el archivo `</etc/ssl/cert.pem>` (respectivamente, en el directorio `</etc/ssl/certs>`) o en el archivo (respectivamente, directorio) especificado por la variable de entorno `<SSL_CERT_FILE>` (respectivamente, `<SSL_CERT_DIR>`).

<div id="openssl.server.preferserverciphers">
  ## openSSL.server.preferServerCiphers
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Cifrados de servidor preferidos por el cliente.

<div id="openssl.server.privatekeyfile">
  ## openSSL.server.privateKeyFile
</div>

Ruta del archivo que contiene la clave privada del certificado PEM. El archivo puede contener la clave y el certificado a la vez.

<div id="openssl.server.privatekeypassphrasehandler.name">
  ## openSSL.server.privateKeyPassphraseHandler.name
</div>

<SettingsInfoBlock type="String" default_value="KeyConsoleHandler" changeable_without_restart="No" />Clase (subclase de PrivateKeyPassphraseHandler) que solicita la frase de paso para acceder a la clave privada. Por ejemplo: `<<privateKeyPassphraseHandler>>`, `<<name>KeyFileHandler</name>>`, `<<options><password>test</password></options>>`, `<</privateKeyPassphraseHandler>>`

<div id="openssl.server.requiretlsv1">
  ## openSSL.server.requireTLSv1
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Requiere una conexión TLSv1. Valores aceptables: `<true>`, `<false>`.

<div id="openssl.server.requiretlsv1_1">
  ## openSSL.server.requireTLSv1_1
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Requiere una conexión TLSv1.1. Valores aceptables: `<true>`, `<false>`.

<div id="openssl.server.requiretlsv1_2">
  ## openSSL.server.requireTLSv1_2
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Requiere una conexión TLSv1.2. Valores permitidos: `<true>`, `<false>`.

<div id="openssl.server.sessioncachesize">
  ## openSSL.server.sessionCacheSize
</div>

<SettingsInfoBlock type="UInt64" default_value="20480" changeable_without_restart="No" />El número máximo de sesiones que el servidor guarda en caché. Un valor de 0 significa que no hay límite de sesiones.

<div id="openssl.server.sessionidcontext">
  ## openSSL.server.sessionIdContext
</div>

<SettingsInfoBlock type="String" default_value="application.name" changeable_without_restart="No" />Un conjunto único de caracteres aleatorios que el servidor añade a cada identificador generado. La longitud de la cadena no debe superar `<SSL_MAX_SSL_SESSION_ID_LENGTH>`. Este parámetro se recomienda siempre, ya que ayuda a evitar problemas tanto si el servidor almacena la sesión en caché como si el cliente solicitó el almacenamiento en caché.

<div id="openssl.server.sessiontimeout">
  ## openSSL.server.sessionTimeout
</div>

<SettingsInfoBlock type="UInt64" default_value="2" changeable_without_restart="No" />Tiempo de almacenamiento en caché de la sesión en el servidor, en horas.

<div id="openssl.server.verificationdepth">
  ## openSSL.server.verificationDepth
</div>

<SettingsInfoBlock type="UInt64" default_value="9" changeable_without_restart="No" />La longitud máxima de la cadena de verificación. La verificación fallará si la longitud de la cadena de certificados supera el valor establecido.

<div id="openssl.server.verificationmode">
  ## openSSL.server.verificationMode
</div>

<SettingsInfoBlock type="String" default_value="relaxed" changeable_without_restart="No" />Método para verificar los certificados del nodo. Encontrará más detalles en la descripción de la clase [Context](https://github.com/ClickHouse/poco/blob/master/NetSSL_OpenSSL/include/Poco/Net/Context.h). Valores posibles: `<none>`, `<relaxed>`, `<strict>`, `<once>`.

<div id="opentelemetry_span_log">
  ## opentelemetry_span_log
</div>

Configuración de la tabla del sistema [`opentelemetry_span_log`](/es/reference/system-tables/opentelemetry_span_log).

<SystemLogParameters />

Ejemplo:

```xml
<opentelemetry_span_log>
    <engine>
        engine MergeTree
        partition by toYYYYMM(finish_date)
        order by (finish_date, finish_time_us, trace_id)
    </engine>
    <database>system</database>
    <table>opentelemetry_span_log</table>
    <flush_interval_milliseconds>7500</flush_interval_milliseconds>
    <max_size_rows>1048576</max_size_rows>
    <reserved_size_rows>8192</reserved_size_rows>
    <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
    <flush_on_crash>false</flush_on_crash>
</opentelemetry_span_log>
```

<div id="os_collect_psi_metrics">
  ## os_collect_psi_metrics
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />Habilita la recopilación de métricas PSI desde los archivos /proc/pressure/.

<div id="os_cpu_busy_time_threshold">
  ## os_cpu_busy_time_threshold
</div>

<SettingsInfoBlock type="UInt64" default_value="1000000" changeable_without_restart="No" />Umbral del tiempo de actividad de la CPU del SO en microsegundos (métrica OSCPUVirtualTimeMicroseconds) para considerar que la CPU está realizando algún trabajo útil; no se considerará que haya sobrecarga de CPU si el tiempo de actividad está por debajo de este valor.

<div id="os_threads_nice_value_distributed_cache_tcp_handler">
  ## os_threads_nice_value_distributed_cache_tcp_handler
</div>

<SettingsInfoBlock type="Int32" default_value="0" changeable_without_restart="No" />

Valor `nice` de Linux para los hilos del handler TCP de la caché distribuida. Los valores más bajos implican una mayor prioridad de CPU.

Requiere la capacidad CAP&#95;SYS&#95;NICE; de lo contrario, no tiene efecto.

Valores posibles: de -20 a 19.

<div id="os_threads_nice_value_merge_mutate">
  ## os_threads_nice_value_merge_mutate
</div>

<SettingsInfoBlock type="Int32" default_value="0" changeable_without_restart="No" />

Valor `nice` de Linux para los hilos de merge y mutación. Los valores más bajos implican una mayor prioridad de CPU.

Requiere la capacidad CAP&#95;SYS&#95;NICE; de lo contrario, no tiene efecto.

Valores posibles: de -20 a 19.

<div id="os_threads_nice_value_zookeeper_client_send_receive">
  ## os_threads_nice_value_zookeeper_client_send_receive
</div>

<SettingsInfoBlock type="Int32" default_value="0" changeable_without_restart="No" />

Valor de nice de Linux para los hilos de envío y recepción del cliente de ZooKeeper. Los valores más bajos implican una mayor prioridad de CPU.

Requiere la capacidad CAP&#95;SYS&#95;NICE; de lo contrario, no tiene efecto.

Valores posibles: de -20 a 19.

<div id="page_cache_free_memory_ratio">
  ## page_cache_free_memory_ratio
</div>

<SettingsInfoBlock type="Double" default_value="0.15" changeable_without_restart="No" />Fracción del límite de memoria que debe mantenerse libre de la caché de páginas en espacio de usuario. Análoga al ajuste `min_free_kbytes` de Linux.

<div id="page_cache_history_window_ms">
  ## page_cache_history_window_ms
</div>

<SettingsInfoBlock type="UInt64" default_value="1000" changeable_without_restart="No" />Retraso antes de que la memoria liberada pueda ser utilizada por la caché de páginas en espacio de usuario.

<div id="page_cache_max_size">
  ## page_cache_max_size
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />Tamaño máximo de la caché de páginas en espacio de usuario. Establézcalo en 0 para desactivar la caché. Si es mayor que page&#95;cache&#95;min&#95;size, el tamaño de la caché se ajustará continuamente dentro de este rango para aprovechar la mayor parte de la memoria disponible, manteniendo el uso total de memoria por debajo del límite (max&#95;server&#95;memory&#95;usage[&#95;to&#95;ram&#95;ratio]).

<div id="page_cache_min_size">
  ## page_cache_min_size
</div>

<SettingsInfoBlock type="UInt64" default_value="104857600" changeable_without_restart="No" />Tamaño mínimo de la caché de páginas en espacio de usuario.

<div id="page_cache_policy">
  ## page_cache_policy
</div>

<SettingsInfoBlock type="String" default_value="SLRU" changeable_without_restart="No" />Nombre de la política de la caché de páginas en espacio de usuario.

<div id="page_cache_shards">
  ## page_cache_shards
</div>

<SettingsInfoBlock type="UInt64" default_value="4" changeable_without_restart="No" />Distribuye la caché de páginas en espacio de usuario en esta cantidad de segmentos para reducir la contención del mutex. Experimental; es poco probable que mejore el rendimiento.

<div id="page_cache_size_ratio">
  ## page_cache_size_ratio
</div>

<SettingsInfoBlock type="Double" default_value="0.5" changeable_without_restart="No" />El tamaño de la cola protegida en la caché de páginas en espacio de usuario respecto al tamaño total de la caché.

<div id="parquet_metadata_cache_max_entries">
  ## parquet_metadata_cache_max_entries
</div>

<SettingsInfoBlock type="UInt64" default_value="5000" changeable_without_restart="No" />Tamaño máximo de la caché de archivos de metadatos de Parquet, en entradas. Cero significa que está deshabilitado.

<div id="parquet_metadata_cache_policy">
  ## parquet_metadata_cache_policy
</div>

<SettingsInfoBlock type="String" default_value="SLRU" changeable_without_restart="No" />Nombre de la política de caché de metadatos de Parquet.

<div id="parquet_metadata_cache_size">
  ## parquet_metadata_cache_size
</div>

<SettingsInfoBlock type="UInt64" default_value="536870912" changeable_without_restart="Yes" />Tamaño máximo de la caché de metadatos de Parquet en bytes. El valor cero significa que está deshabilitada.

<div id="parquet_metadata_cache_size_ratio">
  ## parquet_metadata_cache_size_ratio
</div>

<SettingsInfoBlock type="Double" default_value="0.5" changeable_without_restart="No" />El tamaño de la cola protegida (en caso de usar la política SLRU) en la caché de metadatos de Parquet, en relación con el tamaño total de la caché.

<div id="part_log">
  ## part_log
</div>

Eventos de registro asociados con [MergeTree](/es/reference/engines/table-engines/mergetree-family/mergetree). Por ejemplo, la adición o la fusión de datos. Puede usar el registro para simular algoritmos de fusión y comparar sus características. También puede visualizar el proceso de fusión.

Las consultas se registran en la tabla [system.part&#95;log](/es/reference/system-tables/part_log), no en un archivo independiente. Puede configurar el nombre de esta tabla con el parámetro `table` (consulte más abajo).

<SystemLogParameters />

**Ejemplo**

```xml
<part_log>
    <database>system</database>
    <table>part_log</table>
    <partition_by>toMonday(event_date)</partition_by>
    <flush_interval_milliseconds>7500</flush_interval_milliseconds>
    <max_size_rows>1048576</max_size_rows>
    <reserved_size_rows>8192</reserved_size_rows>
    <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
    <flush_on_crash>false</flush_on_crash>
</part_log>
```

<div id="parts_kill_delay_period">
  ## parts_kill_delay_period
</div>

<SettingsInfoBlock type="UInt64" default_value="30" changeable_without_restart="No" />

Período para eliminar por completo las partes de SharedMergeTree. Solo disponible en ClickHouse Cloud

<div id="parts_kill_delay_period_random_add">
  ## parts_kill_delay_period_random_add
</div>

<SettingsInfoBlock type="UInt64" default_value="10" changeable_without_restart="No" />

Añade un valor con distribución uniforme de 0 a x segundos a `kill_delay_period` para evitar el efecto de avalancha y el posterior DoS de ZooKeeper en caso de que haya un número muy grande de tablas. Solo disponible en ClickHouse Cloud

<div id="parts_killer_max_condemned_parts_per_batch">
  ## parts_killer_max_condemned_parts_per_batch
</div>

<SettingsInfoBlock type="NonZeroUInt64" default_value="100000" changeable_without_restart="No" />

Número máximo de partes marcadas para eliminación que una tabla SharedMergeTree recupera de Keeper en un solo ciclo de limpieza. Limitar el lote mantiene el listado y la operación multi-get dentro del timeout de operación de Keeper, de modo que la limpieza siga avanzando incluso cuando una tabla haya acumulado un volumen muy grande de partes marcadas para eliminación. Debe ser mayor que cero: un valor de `0` haría que Keeper devolviera un listado vacío en cada ciclo, deteniendo permanentemente la limpieza. Solo disponible en ClickHouse Cloud

<div id="parts_killer_pool_size">
  ## parts_killer_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="128" changeable_without_restart="No" />

Hilos para la limpieza de partes de shared merge tree por parte de parts killer. Solo disponible en ClickHouse Cloud

<div id="path">
  ## path
</div>

<SettingsInfoBlock type="String" default_value="/var/lib/clickhouse/" changeable_without_restart="No" />

La ruta del directorio que contiene los datos.

<Note>
  La barra final es obligatoria.
</Note>

**Ejemplo**

```xml
<path>/var/lib/clickhouse/</path>
```

<div id="postgresql_port">
  ## postgresql_port
</div>

Puerto para comunicarse con clientes a través del protocolo PostgreSQL.

<Note>
  * Los enteros positivos especifican el número de puerto en el que se debe escuchar
  * Los valores vacíos se usan para deshabilitar la comunicación con clientes a través del protocolo PostgreSQL.
</Note>

**Ejemplo**

```xml
<postgresql_port>9005</postgresql_port>
```

<div id="postgresql_require_secure_transport">
  ## postgresql_require_secure_transport
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Si se establece en true, se requiere una comunicación segura con los clientes a través de [postgresql&#95;port](/es/reference/settings/server-settings/settings#postgresql_port). Se rechazará cualquier conexión con la opción `<sslmode=disable>`. Úselo junto con la configuración de [OpenSSL](/es/reference/settings/server-settings/settings#openssl).

<div id="prefetch_threadpool_pool_size">
  ## prefetch_threadpool_pool_size
</div>

<SettingsInfoBlock type="NonZeroUInt64" default_value="100" changeable_without_restart="No" />Tamaño del pool en segundo plano para prelecturas en almacenamientos de objetos remotos

<div id="prefetch_threadpool_queue_size">
  ## prefetch_threadpool_queue_size
</div>

<SettingsInfoBlock type="UInt64" default_value="10000" changeable_without_restart="No" />Número de tareas que se pueden insertar en el pool de prefetches

<div id="prefixes_deserialization_thread_pool_thread_pool_queue_size">
  ## prefixes_deserialization_thread_pool_thread_pool_queue_size
</div>

<SettingsInfoBlock type="UInt64" default_value="10000" changeable_without_restart="Yes" />

La cantidad máxima de trabajos que se pueden programar en el Thread pool de deserialización de prefijos.

<Note>
  Un valor de `0` significa que no hay límite.
</Note>

<div id="prepare_system_log_tables_on_startup">
  ## prepare_system_log_tables_on_startup
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />

Si es true, ClickHouse crea todas las tablas `system.*_log` configuradas antes del arranque. Puede resultar útil si algunos scripts de arranque dependen de estas tablas.

<div id="primary_index_cache_policy">
  ## primary_index_cache_policy
</div>

<SettingsInfoBlock type="String" default_value="SLRU" changeable_without_restart="No" />Nombre de la política de caché del índice primario.

<div id="primary_index_cache_prewarm_ratio">
  ## primary_index_cache_prewarm_ratio
</div>

<SettingsInfoBlock type="Double" default_value="0.95" changeable_without_restart="No" />La proporción del tamaño total de la mark cache que se rellenará durante el precalentamiento.

<div id="primary_index_cache_size">
  ## primary_index_cache_size
</div>

<SettingsInfoBlock type="UInt64" default_value="5368709120" changeable_without_restart="Yes" />Tamaño máximo de la caché del índice primario (índice primario de las tablas de la familia MergeTree).

<div id="primary_index_cache_size_ratio">
  ## primary_index_cache_size_ratio
</div>

<SettingsInfoBlock type="Double" default_value="0.5" changeable_without_restart="No" />El tamaño de la cola protegida (en el caso de la política SLRU) en la caché del índice primario en relación con el tamaño total de la caché.

<div id="process_query_plan_packet">
  ## process_query_plan_packet
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />

Esta configuración permite leer el paquete QueryPlan. Este paquete se envía en las consultas distribuidas cuando `serialize_query_plan` está habilitado.
Está deshabilitada de forma predeterminada para evitar posibles problemas de seguridad causados por errores en la deserialización binaria del plan de consulta.

**Ejemplo**

```xml
<process_query_plan_packet>true</process_query_plan_packet>
```

<div id="processors_profile_log">
  ## processors_profile_log
</div>

Configuración de la tabla del sistema [`processors_profile_log`](/es/reference/system-tables/processors_profile_log).

<SystemLogParameters />

La configuración predeterminada es:

```xml
<processors_profile_log>
    <database>system</database>
    <table>processors_profile_log</table>
    <partition_by>toYYYYMM(event_date)</partition_by>
    <flush_interval_milliseconds>7500</flush_interval_milliseconds>
    <max_size_rows>1048576</max_size_rows>
    <reserved_size_rows>8192</reserved_size_rows>
    <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
    <flush_on_crash>false</flush_on_crash>
</processors_profile_log>
```

<div id="prometheus">
  ## prometheus
</div>

Expone datos de métricas para su scraping desde [Prometheus](https://prometheus.io).

Configuración:

* `endpoint` – Endpoint HTTP para que el servidor de Prometheus recopile las métricas. Debe comenzar con &#39;/&#39;.
* `port` – Puerto del `endpoint`.
* `metrics` – Expone métricas de la tabla [system.metrics](/es/reference/system-tables/metrics).
* `events` – Expone métricas de la tabla [system.events](/es/reference/system-tables/events).
* `asynchronous_metrics` – Expone los valores actuales de las métricas de la tabla [system.asynchronous&#95;metrics](/es/reference/system-tables/asynchronous_metrics).
* `errors` - Expone el número de errores, por código de error, ocurridos desde el último reinicio del servidor. Esta información también puede obtenerse de [system.errors](/es/reference/system-tables/errors).

**Ejemplo**

```xml highlight={5-12}
<clickhouse>
    <listen_host>0.0.0.0</listen_host>
    <http_port>8123</http_port>
    <tcp_port>9000</tcp_port>
    <prometheus>
        <endpoint>/metrics</endpoint>
        <port>9363</port>
        <metrics>true</metrics>
        <events>true</events>
        <asynchronous_metrics>true</asynchronous_metrics>
        <errors>true</errors>
    </prometheus>
</clickhouse>
```

Compruebe (sustituya `127.0.0.1` por la dirección IP o el nombre de host de su servidor ClickHouse):

```bash
curl 127.0.0.1:9363/metrics
```

<div id="prometheus.keeper_metrics_only">
  ## prometheus.keeper_metrics_only
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Expone las métricas relacionadas con Keeper

<div id="proxy">
  ## proxy
</div>

Defina servidores proxy para peticiones HTTP y HTTPS, actualmente admitidos por S3 storage, S3 table functions y las funciones URL.

Hay tres formas de definir servidores proxy:

* variables de entorno
* listas de proxy
* resolver remoto de proxy.

También se admite omitir el uso de servidores proxy para hosts específicos mediante `no_proxy`.

**Variables de entorno**

Las variables de entorno `http_proxy` y `https_proxy` le permiten especificar un
servidor proxy para un protocolo determinado. Si están configuradas en su sistema, deberían funcionar sin problemas.

Este es el enfoque más sencillo si un protocolo determinado tiene
solo un servidor proxy y ese servidor proxy no cambia.

**Listas de proxy**

Este enfoque le permite especificar uno o varios
servidores proxy para un protocolo. Si se define más de un servidor proxy,
ClickHouse usa los distintos proxies en un esquema round-robin, equilibrando la
carga entre los servidores. Este es el enfoque más sencillo si hay más de
un servidor proxy para un protocolo y la lista de servidores proxy no cambia.

**Plantilla de configuración**

```xml
<proxy>
    <http>
        <uri>http://proxy1</uri>
        <uri>http://proxy2:3128</uri>
    </http>
    <https>
        <uri>http://proxy1:3128</uri>
    </https>
</proxy>
```

Seleccione un campo padre en las pestañas siguientes para ver sus campos secundarios:

<Tabs>
  <Tab title="<proxy>">
    | Campo     | Descripción                          |
    | --------- | ------------------------------------ |
    | `<http>`  | Una lista de uno o más proxies HTTP  |
    | `<https>` | Una lista de uno o más proxies HTTPS |
  </Tab>

  <Tab title="<http> y <https>">
    | Campo   | Descripción      |
    | ------- | ---------------- |
    | `<uri>` | El URI del proxy |
  </Tab>
</Tabs>

**Resolvers remotos de proxy**

Es posible que los servidores proxy cambien dinámicamente. En ese
caso, puede definir el endpoint de un resolver. ClickHouse envía
una solicitud GET vacía a ese endpoint; el resolver remoto debe devolver el host del proxy.
ClickHouse lo usará para formar el URI del proxy con la siguiente plantilla: `\{proxy_scheme\}://\{proxy_host\}:{proxy_port}`

**Plantilla de configuración**

```xml
<proxy>
    <http>
        <resolver>
            <endpoint>http://resolver:8080/hostname</endpoint>
            <proxy_scheme>http</proxy_scheme>
            <proxy_port>80</proxy_port>
            <proxy_cache_time>10</proxy_cache_time>
        </resolver>
    </http>

    <https>
        <resolver>
            <endpoint>http://resolver:8080/hostname</endpoint>
            <proxy_scheme>http</proxy_scheme>
            <proxy_port>3128</proxy_port>
            <proxy_cache_time>10</proxy_cache_time>
        </resolver>
    </https>

</proxy>
```

Seleccione un campo principal en las pestañas siguientes para ver sus campos secundarios:

<Tabs>
  <Tab title="<proxy>">
    | Campo     | Descripción                       |
    | --------- | --------------------------------- |
    | `<http>`  | Una lista de uno o más resolvers* |
    | `<https>` | Una lista de uno o más resolvers* |
  </Tab>

  <Tab title="<http> y <https>">
    | Campo        | Descripción                                 |
    | ------------ | ------------------------------------------- |
    | `<resolver>` | El endpoint y otros detalles de un resolver |

    <Note>
      Puede tener varios elementos `<resolver>`, pero solo se usa el primero
      para un protocolo determinado. Cualquier otro elemento `<resolver>`
      para ese protocolo se ignora. Esto significa que el balanceo de carga
      (si es necesario) debe implementarse en el resolver remoto.
    </Note>
  </Tab>

  <Tab title="<resolver>">
    | Campo                | Descripción                                                                                                                                                                                                   |
    | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
    | `<endpoint>`         | El URI del resolver de proxy                                                                                                                                                                                  |
    | `<proxy_scheme>`     | El protocolo del URI final del proxy. Puede ser `http` o `https`.                                                                                                                                             |
    | `<proxy_port>`       | El número de puerto del resolver de proxy                                                                                                                                                                     |
    | `<proxy_cache_time>` | El tiempo, en segundos, durante el cual ClickHouse debe almacenar en caché los valores del resolver. Si este valor se establece en `0`, ClickHouse contacta con el resolver para cada solicitud HTTP o HTTPS. |
  </Tab>
</Tabs>

**Precedencia**

La configuración del proxy se determina en el siguiente orden:

| Orden | Configuración              |
| ----- | -------------------------- |
| 1.    | Resolvers de proxy remotos |
| 2.    | Listas de proxy            |
| 3.    | Variables de entorno       |

ClickHouse consultará el tipo de resolver de mayor prioridad para el protocolo de la solicitud. Si no está definido,
consultará el siguiente tipo de resolver con mayor prioridad, hasta llegar al resolver de entorno.
Esto también permite usar una combinación de tipos de resolver.

<div id="query_cache">
  ## query_cache
</div>

Configuración de la [caché de consultas](/es/concepts/features/performance/caches/query-cache).

Los siguientes ajustes están disponibles:

| Ajuste                    | Descripción                                                                                                 | Valor predeterminado |
| ------------------------- | ----------------------------------------------------------------------------------------------------------- | -------------------- |
| `max_entries`             | El número máximo de resultados de consultas `SELECT` almacenados en la caché.                               | `1024`               |
| `max_entry_size_in_bytes` | El tamaño máximo en bytes que pueden tener los resultados de consultas `SELECT` para guardarse en la caché. | `1048576`            |
| `max_entry_size_in_rows`  | El número máximo de filas que pueden tener los resultados de consultas `SELECT` para guardarse en la caché. | `30000000`           |
| `max_size_in_bytes`       | El tamaño máximo de la caché en bytes. `0` significa que la caché de consultas está deshabilitada.          | `1073741824`         |

<Note>
  * Los cambios en la configuración surten efecto inmediatamente.
  * Los datos de la caché de consultas se asignan en DRAM. Si la memoria escasea, asegúrese de establecer un valor pequeño para `max_size_in_bytes` o deshabilitar por completo la caché de consultas.
</Note>

**Ejemplo**

```xml
<query_cache>
    <max_size_in_bytes>1073741824</max_size_in_bytes>
    <max_entries>1024</max_entries>
    <max_entry_size_in_bytes>1048576</max_entry_size_in_bytes>
    <max_entry_size_in_rows>30000000</max_entry_size_in_rows>
</query_cache>
```

<div id="query_cache.max_entries">
  ## query_cache.max_entries
</div>

<SettingsInfoBlock type="UInt64" default_value="1024" changeable_without_restart="No" />El número máximo de resultados de consultas `SELECT` almacenados en la caché.

<div id="query_cache.max_entry_size_in_bytes">
  ## query_cache.max_entry_size_in_bytes
</div>

<SettingsInfoBlock type="UInt64" default_value="1048576" changeable_without_restart="No" />El tamaño máximo, en bytes, que pueden tener los resultados de una consulta SELECT para poder guardarse en la caché.

<div id="query_cache.max_entry_size_in_rows">
  ## query_cache.max_entry_size_in_rows
</div>

<SettingsInfoBlock type="UInt64" default_value="30000000" changeable_without_restart="No" />El número máximo de filas que pueden tener los resultados de una consulta SELECT para poder guardarse en la caché.

<div id="query_cache.max_size_in_bytes">
  ## query_cache.max_size_in_bytes
</div>

<SettingsInfoBlock type="UInt64" default_value="1073741824" changeable_without_restart="Yes" />El tamaño máximo de la caché en bytes. 0 significa que la caché de consultas está desactivada.

<div id="query_condition_cache_policy">
  ## query_condition_cache_policy
</div>

<SettingsInfoBlock type="String" default_value="SLRU" changeable_without_restart="No" />Nombre de la política de la caché de condiciones de consulta.

<div id="query_condition_cache_size">
  ## query_condition_cache_size
</div>

<SettingsInfoBlock type="UInt64" default_value="104857600" changeable_without_restart="Sí" />

Tamaño máximo de la caché de condiciones de consulta.

<Note>
  Esta configuración se puede modificar en tiempo de ejecución y se aplicará de inmediato.
</Note>

<div id="query_condition_cache_size_ratio">
  ## query_condition_cache_size_ratio
</div>

<SettingsInfoBlock type="Double" default_value="0.5" changeable_without_restart="No" />El tamaño de la cola protegida (en el caso de la política SLRU) en la caché de condiciones de consulta, en relación con el tamaño total de la caché.

<div id="query_log">
  ## query_log
</div>

Configuración para registrar las consultas recibidas con la opción [log&#95;queries=1](/es/reference/settings/session-settings).

Las consultas se registran en la tabla [system.query&#95;log](/es/reference/system-tables/query_log), no en un archivo independiente. Puede cambiar el nombre de la tabla mediante el parámetro `table` (consulte a continuación).

<SystemLogParameters />

Si la tabla no existe, ClickHouse la creará. Si la estructura del registro de consultas cambió al actualizarse el servidor ClickHouse, la tabla con la estructura anterior se renombra y se crea una nueva tabla automáticamente.

**Ejemplo**

```xml
<query_log>
    <database>system</database>
    <table>query_log</table>
    <engine>Engine = MergeTree PARTITION BY event_date ORDER BY event_time TTL event_date + INTERVAL 30 day</engine>
    <flush_interval_milliseconds>7500</flush_interval_milliseconds>
    <max_size_rows>1048576</max_size_rows>
    <reserved_size_rows>8192</reserved_size_rows>
    <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
    <flush_on_crash>false</flush_on_crash>
</query_log>
```

<div id="query_masking_rules">
  ## query_masking_rules
</div>

Reglas basadas en expresiones regulares que se aplican a las consultas, así como a todos los mensajes de registro, antes de almacenarlos en los registros del servidor,
las tablas [`system.query_log`](/es/reference/system-tables/query_log), [`system.text_log`](/es/reference/system-tables/text_log), [`system.processes`](/es/reference/system-tables/processes), y en los registros enviados al cliente. Esto permite evitar
la filtración de datos sensibles de las consultas SQL, como nombres, correos electrónicos, identificadores personales o números de tarjeta de crédito, en los registros.

**Ejemplo**

```xml
<query_masking_rules>
    <rule>
        <name>hide SSN</name>
        <regexp>(^|\D)\d{3}-\d{2}-\d{4}($|\D)</regexp>
        <replace>000-00-0000</replace>
    </rule>
</query_masking_rules>
```

**Campos de configuración**:

| Ajuste    | Descripción                                                                         |
| --------- | ----------------------------------------------------------------------------------- |
| `name`    | nombre de la regla (opcional)                                                       |
| `regexp`  | expresión regular compatible con RE2 (obligatoria)                                  |
| `replace` | cadena de sustitución para datos sensibles (opcional; por defecto, seis asteriscos) |

Las reglas de enmascaramiento se aplican a toda la consulta (para evitar filtraciones de datos sensibles procedentes de consultas malformadas o que no se pueden analizar).

La tabla [`system.events`](/es/reference/system-tables/events) tiene el contador `QueryMaskingRulesMatch`, que indica el número total de coincidencias con las reglas de enmascaramiento de consultas.

Para las consultas distribuidas, cada servidor debe configurarse por separado; de lo contrario, las subconsultas enviadas a otros
nodos se almacenarán sin enmascarar.

<div id="query_metric_log">
  ## query_metric_log
</div>

Está deshabilitado de forma predeterminada.

**Activación**

Para activar manualmente la recopilación del historial de métricas [`system.query_metric_log`](/es/reference/system-tables/query_metric_log), cree `/etc/clickhouse-server/config.d/query_metric_log.xml` con el siguiente contenido:

```xml
<clickhouse>
    <query_metric_log>
        <database>system</database>
        <table>query_metric_log</table>
        <flush_interval_milliseconds>7500</flush_interval_milliseconds>
        <collect_interval_milliseconds>1000</collect_interval_milliseconds>
        <max_size_rows>1048576</max_size_rows>
        <reserved_size_rows>8192</reserved_size_rows>
        <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
        <flush_on_crash>false</flush_on_crash>
    </query_metric_log>
</clickhouse>
```

**Desactivación**

Para desactivar el ajuste `query_metric_log`, debe crear el siguiente archivo `/etc/clickhouse-server/config.d/disable_query_metric_log.xml` con el siguiente contenido:

```xml
<clickhouse>
    <query_metric_log remove="1" />
</clickhouse>
```

<SystemLogParameters />

<div id="query_thread_log">
  ## query_thread_log
</div>

Configuración para registrar los hilos de las consultas recibidas con el ajuste [log&#95;query&#95;threads=1](/es/reference/settings/session-settings#log_query_threads).

Las consultas se registran en la tabla [system.query&#95;thread&#95;log](/es/reference/system-tables/query_thread_log), no en un archivo independiente. Puede cambiar el nombre de la tabla mediante el parámetro `table` (consulte a continuación).

<SystemLogParameters />

Si la tabla no existe, ClickHouse la creará. Si la estructura del registro de hilos de consultas cambió al actualizar el servidor ClickHouse, la tabla con la estructura anterior se renombra y se crea automáticamente una nueva.

**Ejemplo**

```xml
<query_thread_log>
    <database>system</database>
    <table>query_thread_log</table>
    <partition_by>toMonday(event_date)</partition_by>
    <flush_interval_milliseconds>7500</flush_interval_milliseconds>
    <max_size_rows>1048576</max_size_rows>
    <reserved_size_rows>8192</reserved_size_rows>
    <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
    <flush_on_crash>false</flush_on_crash>
</query_thread_log>
```

<div id="query_views_log">
  ## query_views_log
</div>

Configuración para registrar vistas (live, materializadas, etc.) que depende de las consultas recibidas con la configuración [log&#95;query&#95;views=1](/es/reference/settings/session-settings#log_query_views).

Las consultas se registran en la tabla [system.query&#95;views&#95;log](/es/reference/system-tables/query_views_log), no en un archivo independiente. Puede cambiar el nombre de la tabla mediante el parámetro `table` (consulte a continuación).

<SystemLogParameters />

Si la tabla no existe, ClickHouse la creará. Si la estructura del registro de vistas de consulta cambió al actualizar el servidor de ClickHouse, la tabla con la estructura anterior se renombra y se crea automáticamente una nueva tabla.

**Ejemplo**

```xml
<query_views_log>
    <database>system</database>
    <table>query_views_log</table>
    <partition_by>toYYYYMM(event_date)</partition_by>
    <flush_interval_milliseconds>7500</flush_interval_milliseconds>
    <max_size_rows>1048576</max_size_rows>
    <reserved_size_rows>8192</reserved_size_rows>
    <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
    <flush_on_crash>false</flush_on_crash>
</query_views_log>
```

<div id="remap_executable">
  ## remap_executable
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />

Configuración para reasignar la memoria del código máquina (&quot;text&quot;) mediante páginas enormes.

<Note>
  Esta funcionalidad es muy experimental.
</Note>

**Ejemplo**

```xml
<remap_executable>false</remap_executable>
```

<div id="remote_servers">
  ## remote_servers
</div>

Configuración de los clústeres utilizados por el motor de tabla [Distributed](/es/reference/engines/table-engines/special/distributed) y por la función de tabla `cluster`.

**Ejemplo**

```xml
<remote_servers incl="clickhouse_remote_servers" />
```

Para el valor del atributo `incl`, consulte la sección &quot;[Archivos de configuración](/es/concepts/features/configuration/server-config/configuration-files)&quot;.

**Véase también**

* [skip&#95;unavailable&#95;shards](/es/reference/settings/session-settings#skip_unavailable_shards)
* [Cluster Discovery](/es/guides/oss/deployment-and-scaling/cluster-discovery)
* [motor de base de datos Replicated](/es/reference/engines/database-engines/replicated)

<div id="remote_url_allow_hosts">
  ## remote_url_allow_hosts
</div>

Lista de hosts que se permite utilizar en motores de almacenamiento y funciones de tabla relacionadas con URL.

Al añadir un host con la etiqueta XML `\<host\>`:

* debe especificarse exactamente como en la URL, ya que el nombre se comprueba antes de la resolución de DNS. Por ejemplo: `<host>clickhouse.com</host>`
* si el puerto se especifica explícitamente en la URL, se comprueba `host:port` en su conjunto. Por ejemplo: `<host>clickhouse.com:80</host>`
* si el host se especifica sin puerto, se permite cualquier puerto del host. Por ejemplo: si se especifica `<host>clickhouse.com</host>`, se permiten `clickhouse.com:20` (FTP), `clickhouse.com:80` (HTTP), `clickhouse.com:443` (HTTPS), etc.
* si el host se especifica como una dirección IP, se comprueba tal como aparece en la URL. Por ejemplo: `[2a02:6b8:a::a]`.
* si hay redirecciones y la compatibilidad con las redirecciones está habilitada, se comprueba cada redirección (el campo `location`).

Por ejemplo:

```sql
<remote_url_allow_hosts>
    <host>clickhouse.com</host>
</remote_url_allow_hosts>
```

<div id="replica_group_name">
  ## replica_group_name
</div>

Nombre del grupo de réplicas de la base de datos Replicated.

El clúster creado por la base de datos Replicated constará de réplicas del mismo grupo.
Las consultas DDL solo esperarán a las réplicas del mismo grupo.

Vacío de forma predeterminada.

**Ejemplo**

```xml
<replica_group_name>backups</replica_group_name>
```

<div id="replicated_fetches_http_connection_timeout">
  ## replicated_fetches_http_connection_timeout
</div>

<SettingsInfoBlock type="Segundos" default_value="0" changeable_without_restart="No" />Tiempo de espera de la conexión HTTP para las solicitudes de recuperación de partes. Se hereda del perfil predeterminado `http_connection_timeout` si no se establece explícitamente.

<div id="replicated_fetches_http_receive_timeout">
  ## replicated_fetches_http_receive_timeout
</div>

<SettingsInfoBlock type="Segundos" default_value="0" changeable_without_restart="No" />Tiempo de espera de recepción HTTP para las solicitudes de recuperación de partes. Se hereda del perfil predeterminado `http_receive_timeout` si no se establece explícitamente.

<div id="replicated_fetches_http_send_timeout">
  ## replicated_fetches_http_send_timeout
</div>

<SettingsInfoBlock type="Segundos" default_value="0" changeable_without_restart="No" />Tiempo de espera de envío HTTP para las solicitudes de recuperación de partes. Se hereda del perfil predeterminado `http_send_timeout` si no se establece explícitamente.

<div id="replicated_merge_tree">
  ## replicated_merge_tree
</div>

Ajuste preciso para tablas en [ReplicatedMergeTree](/es/reference/engines/table-engines/mergetree-family/mergetree). Esta configuración tiene mayor prioridad.

Para obtener más información, consulte el archivo de encabezado MergeTreeSettings.h.

**Ejemplo**

```xml
<replicated_merge_tree>
    <max_suspicious_broken_parts>5</max_suspicious_broken_parts>
</replicated_merge_tree>
```

<div id="restore_threads">
  ## restore_threads
</div>

<SettingsInfoBlock type="NonZeroUInt64" default_value="16" changeable_without_restart="No" />El número máximo de hilos para ejecutar solicitudes RESTORE.

<div id="s3_credentials_provider_max_cache_size">
  ## s3_credentials_provider_max_cache_size
</div>

<SettingsInfoBlock type="UInt64" default_value="100" changeable_without_restart="No" />El número máximo de proveedores de credenciales de S3 que pueden quedar en caché

<div id="s3_max_redirects">
  ## s3_max_redirects
</div>

<SettingsInfoBlock type="UInt64" default_value="10" changeable_without_restart="No" />Número máximo permitido de saltos de redirección de S3.

<div id="s3_retry_attempts">
  ## s3_retry_attempts
</div>

<SettingsInfoBlock type="UInt64" default_value="500" changeable_without_restart="No" />Configuración para Aws::Client::RetryStrategy; Aws::Client realiza los reintentos por sí mismo; 0 significa que no hay reintentos

<div id="s3queue_disable_streaming">
  ## s3queue_disable_streaming
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="Yes" />Deshabilita el procesamiento en streaming en S3Queue incluso si la tabla se ha creado y hay vistas materializadas vinculadas

<div id="s3queue_log">
  ## s3queue_log
</div>

Configuración de la tabla del sistema `s3queue_log`.

<SystemLogParameters />

Los ajustes predeterminados son:

```xml
<s3queue_log>
    <database>system</database>
    <table>s3queue_log</table>
    <partition_by>toYYYYMM(event_date)</partition_by>
    <flush_interval_milliseconds>7500</flush_interval_milliseconds>
</s3queue_log>
```

<div id="send_crash_reports">
  ## send_crash_reports
</div>

Configuración para enviar informes de fallos al equipo principal de desarrolladores de ClickHouse.

Se agradece especialmente habilitarlo en entornos de preproducción.

Claves:

| Clave                 | Descripción                                                                                                                                                    |
| --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `enabled`             | Indicador booleano para habilitar la funcionalidad; `true` de forma predeterminada. Establézcalo en `false` para evitar enviar informes de fallos.             |
| `endpoint`            | Puede sobrescribir la URL del endpoint para el envío de informes de fallos.                                                                                    |
| `send_logical_errors` | `LOGICAL_ERROR` es como un `assert`; es un error en ClickHouse. Este indicador booleano habilita el envío de estas excepciones (valor predeterminado: `true`). |

**Uso recomendado**

```xml
<send_crash_reports>
    <enabled>true</enabled>
</send_crash_reports>
```

<div id="series_keeper_path">
  ## series_keeper_path
</div>

<SettingsInfoBlock type="String" default_value="/clickhouse/series" changeable_without_restart="No" />

Ruta en Keeper con números autoincrementales generados por la función `generateSerialID`. Cada serie será un nodo bajo esta ruta.

<div id="show_addresses_in_stack_traces">
  ## show_addresses_in_stack_traces
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />Si se establece en true, se mostrarán direcciones en las trazas de pila

<div id="shutdown_wait_backups_and_restores">
  ## shutdown_wait_backups_and_restores
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />Si se establece en true, ClickHouse esperará a que las copias de seguridad y las restauraciones en curso finalicen antes de apagarse.

<div id="shutdown_wait_unfinished">
  ## shutdown_wait_unfinished
</div>

<SettingsInfoBlock type="UInt64" default_value="5" changeable_without_restart="No" />Retraso en segundos para esperar a que finalicen las consultas pendientes

<div id="shutdown_wait_unfinished_queries">
  ## shutdown_wait_unfinished_queries
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Si se establece en true, ClickHouse esperará a que finalicen las consultas en ejecución antes de apagarse.

<div id="skip_binary_checksum_checks">
  ## skip_binary_checksum_checks
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Omite las comprobaciones de integridad de la suma de comprobación del ejecutable de ClickHouse

<div id="skip_check_for_incorrect_settings">
  ## skip_check_for_incorrect_settings
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />

Si se establece en true, no se comprobará que la configuración del servidor sea correcta.

**Ejemplo**

```xml
<skip_check_for_incorrect_settings>1</skip_check_for_incorrect_settings>
```

<div id="snapshot_cleaner_period">
  ## snapshot_cleaner_period
</div>

<SettingsInfoBlock type="UInt64" default_value="120" changeable_without_restart="No" />

Período para eliminar por completo las partes de snapshot de SharedMergeTree. Solo está disponible en ClickHouse Cloud

<div id="snapshot_cleaner_pool_size">
  ## snapshot_cleaner_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="128" changeable_without_restart="No" />

Subprocesos para la limpieza de snapshots de shared merge tree. Solo disponible en ClickHouse Cloud

<div id="ssh_server">
  ## ssh_server
</div>

La parte pública de la clave del host se escribirá en el archivo known&#95;hosts
del lado del cliente SSH en la primera conexión.

Las configuraciones de clave de host están desactivadas de forma predeterminada.
Descomente las configuraciones de clave de host y proporcione la ruta a la clave SSH correspondiente para activarlas:

Ejemplo:

```xml
<ssh_server>
    <host_rsa_key>path_to_the_ssh_key</host_rsa_key>
    <host_ecdsa_key>path_to_the_ssh_key</host_ecdsa_key>
    <host_ed25519_key>path_to_the_ssh_key</host_ed25519_key>
</ssh_server>
```

<div id="startup_mv_delay_ms">
  ## startup_mv_delay_ms
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />Parámetro de depuración para simular el retraso en la creación de vistas materializadas

<div id="startup_scripts.throw_on_error">
  ## startup_scripts.throw_on_error
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Si se establece en true, el servidor no se iniciará si se produce un error durante la ejecución del script.

<div id="storage_configuration">
  ## storage_configuration
</div>

Permite configurar el almacenamiento con varios discos.

La configuración de almacenamiento sigue la estructura:

```xml
<storage_configuration>
    <disks>
        <!-- configuración -->
    </disks>
    <policies>
        <!-- configuración -->
    </policies>
</storage_configuration>
```

<div id="configuration-of-disks">
  ### Configuración de `disks`
</div>

La configuración de `disks` sigue la estructura que se muestra a continuación:

```xml
<storage_configuration>
    <disks>
        <disk_name_1>
            <path>/mnt/fast_ssd/clickhouse/</path>
        </disk_name_1>
        <disk_name_2>
            <path>/mnt/hdd1/clickhouse/</path>
            <keep_free_space_bytes>10485760</keep_free_space_bytes>
        </disk_name_2>
        <disk_name_3>
            <path>/mnt/hdd2/clickhouse/</path>
            <keep_free_space_bytes>10485760</keep_free_space_bytes>
        </disk_name_3>
        ...
    </disks>
</storage_configuration>
```

Las subetiquetas anteriores definen los siguientes ajustes para `disks`:

| Setting                 | Description                                                                                               |
| ----------------------- | --------------------------------------------------------------------------------------------------------- |
| `<disk_name_N>`         | El nombre del disco, que debe ser único.                                                                  |
| `path`                  | La ruta donde se almacenarán los datos del servidor (directorios `data` y `shadow`). Debe terminar en `/` |
| `keep_free_space_bytes` | Tamaño del espacio libre reservado en el disco.                                                           |

<Note>
  El orden de los discos no importa.
</Note>

<div id="configuration-of-policies">
  ### Configuración de políticas
</div>

Las subetiquetas anteriores definen la siguiente configuración para `policies`:

| Configuración                | Descripción                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ---------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `policy_name_N`              | Nombre de la política. Los nombres de las políticas deben ser únicos.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `volume_name_N`              | Nombre del volumen. Los nombres de los volúmenes deben ser únicos.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `disk`                       | El disco ubicado dentro del volumen.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `max_data_part_size_bytes`   | El tamaño máximo de un fragmento de datos que puede residir en cualquiera de los discos de este volumen. Si la operación de merge da como resultado un tamaño de fragmento que se prevé que sea mayor que max&#95;data&#95;part&#95;size&#95;bytes, el fragmento se escribirá en el siguiente volumen. Básicamente, esta función le permite almacenar fragmentos nuevos o pequeños en un volumen rápido (SSD) y moverlos a un volumen frío (HDD) cuando alcanzan un tamaño grande. No utilice esta opción si la política tiene un solo volumen.                                                                            |
| `move_factor`                | La proporción de espacio libre disponible en el volumen. Si el espacio queda por debajo de este valor, los datos comenzarán a transferirse al siguiente volumen, si lo hay. Para la transferencia, los fragmentos se ordenan por tamaño de mayor a menor (descendente) y se seleccionan los fragmentos cuyo tamaño total sea suficiente para cumplir la condición de `move_factor`; si el tamaño total de todos los fragmentos es insuficiente, se moverán todos los fragmentos.                                                                                                                                           |
| `perform_ttl_move_on_insert` | Deshabilita el movimiento, en el momento de la inserción, de los datos con TTL vencido. De forma predeterminada (si está habilitada), si insertamos un dato que ya ha vencido según la regla de movimiento por tiempo de vida, se mueve inmediatamente al volumen / disco especificado en la regla de movimiento. Esto puede ralentizar significativamente la inserción si el volumen / disco de destino es lento (por ejemplo, S3). Si está deshabilitada, la parte vencida de los datos se escribe en el volumen predeterminado y luego se mueve inmediatamente al volumen especificado en la regla para el TTL vencido. |
| `load_balancing`             | Política de equilibrio de discos: `round_robin` o `least_used`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `least_used_ttl_ms`          | Establece el tiempo de espera (en milisegundos) para actualizar el espacio disponible en todos los discos (`0` - actualizar siempre, `-1` - no actualizar nunca, el valor predeterminado es `60000`). Tenga en cuenta que, si el disco solo lo usa ClickHouse y no estará sujeto al redimensionamiento dinámico del sistema de archivos, puede usar el valor `-1`. En todos los demás casos, esto no se recomienda, ya que con el tiempo provocará una asignación incorrecta del espacio.                                                                                                                                  |
| `prefer_not_to_merge`        | Deshabilita la fusión de partes de los datos en este volumen. Nota: esto es potencialmente perjudicial y puede causar ralentizaciones. Cuando esta configuración está habilitada (no haga esto), se prohíbe la fusión de datos en este volumen (lo cual es negativo). Esto permite controlar cómo ClickHouse interactúa con discos lentos. Recomendamos no usar esta opción en absoluto.                                                                                                                                                                                                                                   |
| `volume_priority`            | Define la prioridad (orden) en la que se llenan los volúmenes. Cuanto menor sea el valor, mayor será la prioridad. Los valores del parámetro deben ser números naturales y cubrir el rango de 1 a N (N es el mayor valor del parámetro especificado) sin huecos.                                                                                                                                                                                                                                                                                                                                                           |

Para `volume_priority`:

* Si todos los volúmenes tienen este parámetro, se priorizan en el orden especificado.
* Si solo *algunos* volúmenes lo tienen, los volúmenes que no lo tienen tienen la prioridad más baja. Los que sí lo tienen se priorizan según el valor de la etiqueta; la prioridad del resto se determina por el orden en que aparecen en el archivo de configuración.
* Si *ningún* volumen tiene este parámetro, su orden se determina por el orden en que aparecen en el archivo de configuración.
* La prioridad de los volúmenes no puede ser idéntica.

<div id="storage_connections_hard_limit">
  ## storage_connections_hard_limit
</div>

<SettingsInfoBlock type="UInt64" default_value="200000" changeable_without_restart="No" />Se lanza una excepción al intentar crear una conexión cuando se alcanza este límite. Establézcalo en 0 para desactivar el límite estricto. El límite se aplica a las conexiones de almacenamiento.

<div id="storage_connections_rcvbuf">
  ## storage_connections_rcvbuf
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Yes" />El tamaño de la opción SO&#95;RCVBUF para las conexiones de almacenamiento (replicación, consultas distribuidas). Si se establece en un valor superior a 0, sobrescribe el autotuning de TCP del kernel para el búfer de recepción. 0 = valor predeterminado del kernel (autotuning). Nota: al volver a establecer esta configuración en 0, el autotuning solo se restaura para las conexiones recién creadas; las conexiones existentes del pool conservan tamaños de búfer fijos hasta que se vuelven a crear.

<div id="storage_connections_sndbuf">
  ## storage_connections_sndbuf
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Sí" />El tamaño de la opción SO&#95;SNDBUF para las conexiones de almacenamiento (replicación, consultas distribuidas). Si se establece en un valor superior a 0, anula el autotuning TCP del kernel para el búfer de envío. 0 = valor predeterminado del kernel (autotuning). Nota: al volver a cambiar este ajuste a 0, el autotuning solo se restaura para las conexiones nuevas; las conexiones existentes del pool conservan tamaños de búfer fijos hasta que se vuelvan a crear.

<div id="storage_connections_soft_limit">
  ## storage_connections_soft_limit
</div>

<SettingsInfoBlock type="UInt64" default_value="100" changeable_without_restart="No" />Las conexiones que superen este límite tienen una vida útil significativamente más corta. El límite se aplica a las conexiones de almacenamiento.

<div id="storage_connections_store_limit">
  ## storage_connections_store_limit
</div>

<SettingsInfoBlock type="UInt64" default_value="1000" changeable_without_restart="No" />Las conexiones que superen este límite se reinician tras su uso. Establézcalo en 0 para desactivar la caché de conexiones. El límite se aplica a las conexiones de almacenamiento.

<div id="storage_connections_warn_limit">
  ## storage_connections_warn_limit
</div>

<SettingsInfoBlock type="UInt64" default_value="500" changeable_without_restart="No" />Se escriben mensajes de advertencia en los logs si el número de conexiones en uso supera este límite. El límite se aplica a las conexiones de almacenamiento.

<div id="storage_metadata_write_full_object_key">
  ## storage_metadata_write_full_object_key
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />Escribe los archivos de metadatos del disco con el formato VERSION&#95;FULL&#95;OBJECT&#95;KEY. Está habilitada de forma predeterminada. Esta configuración está obsoleta.

<div id="storage_shared_set_join_use_inner_uuid">
  ## storage_shared_set_join_use_inner_uuid
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />Si está habilitada, se genera una UUID interna durante la creación de SharedSet y SharedJoin. Solo en ClickHouse Cloud

<div id="table_engines_require_grant">
  ## table_engines_require_grant
</div>

Si se establece en true, los usuarios necesitan un grant para crear una tabla con un motor específico; por ejemplo, `GRANT TABLE ENGINE ON TinyLog to user`.

<Note>
  De forma predeterminada, para mantener la compatibilidad con versiones anteriores, al crear una tabla con un motor de tabla específico se ignora el grant; sin embargo, puedes cambiar este comportamiento estableciendo este valor en true.
</Note>

<div id="tables_loader_background_pool_size">
  ## tables_loader_background_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />

Establece el número de hilos que realizan trabajos de carga asíncrona en el pool en segundo plano. El pool en segundo plano se utiliza para cargar tablas de forma asíncrona después de que se inicia el servidor, siempre que no haya consultas esperando esa tabla. Puede ser beneficioso mantener un número bajo de hilos en el pool en segundo plano si hay muchas tablas. Esto reservará recursos de CPU para la ejecución concurrente de consultas.

<Note>
  Un valor de `0` significa que se usarán todas las CPU disponibles.
</Note>

<div id="tables_loader_foreground_pool_size">
  ## tables_loader_foreground_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />

Establece el número de hilos que realizan trabajos de carga en el pool en primer plano. El pool en primer plano se usa para cargar tablas de forma síncrona antes de que el servidor empiece a escuchar en un puerto y para cargar tablas cuya carga está en espera. El pool en primer plano tiene mayor prioridad que el pool en segundo plano. Esto significa que no se inicia ningún trabajo en el pool en segundo plano mientras haya trabajos ejecutándose en el pool en primer plano.

<Note>
  Un valor de `0` significa que se usarán todas las CPU disponibles.
</Note>

<div id="tcp_close_connection_after_queries_num">
  ## tcp_close_connection_after_queries_num
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />Número máximo de consultas permitidas por conexión TCP antes de cerrar la conexión. Establezca 0 para permitir consultas ilimitadas.

<div id="tcp_close_connection_after_queries_seconds">
  ## tcp_close_connection_after_queries_seconds
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />Tiempo de vida máximo de una conexión TCP en segundos antes de que se cierre. Establézcalo en 0 para que la duración de la conexión sea ilimitada.

<div id="tcp_port">
  ## tcp_port
</div>

Puerto para comunicarse con los clientes mediante el protocolo TCP.

**Ejemplo**

```xml
<tcp_port>9000</tcp_port>
```

<div id="tcp_port_secure">
  ## tcp_port_secure
</div>

Puerto TCP para la comunicación segura con los clientes. Úselo con la configuración de [OpenSSL](#openssl).

**Valor predeterminado**

```xml
<tcp_port_secure>9440</tcp_port_secure>
```

<div id="tcp_ssh_port">
  ## tcp_ssh_port
</div>

Puerto del servidor SSH que permite al usuario conectarse y ejecutar consultas de forma interactiva mediante el cliente integrado a través de la PTY.

Ejemplo:

```xml
<tcp_ssh_port>9022</tcp_ssh_port>
```

<div id="temporary_data_in_cache">
  ## temporary_data_in_cache
</div>

Con esta opción, los datos temporales se almacenarán en la caché del disco correspondiente.
En esta sección, debe especificar el nombre del disco con el tipo `cache`.
En ese caso, la caché y los datos temporales compartirán el mismo espacio, y la caché del disco puede desalojarse para crear datos temporales.

<Note>
  Solo se puede usar una opción para configurar el almacenamiento de datos temporales: `tmp_path`, `tmp_policy`, `temporary_data_in_cache`.
</Note>

**Ejemplo**

Tanto la caché de `local_disk` como los datos temporales se almacenarán en `/tiny_local_cache` en el sistema de archivos, gestionado por `tiny_local_cache`.

```xml highlight={9-16,20}
<clickhouse>
<storage_configuration>
<disks>
<local_disk>
<type>local</type>
<path>/local_disk/</path>
</local_disk>

<tiny_local_cache>
<type>cache</type>
<disk>local_disk</disk>
<path>/tiny_local_cache/</path>
<max_size_rows>10M</max_size_rows>
<max_file_segment_size>1M</max_file_segment_size>
<cache_on_write_operations>1</cache_on_write_operations>
</tiny_local_cache>
</disks>
</storage_configuration>

<temporary_data_in_cache>tiny_local_cache</temporary_data_in_cache>
</clickhouse>
```

<div id="temporary_data_in_distributed_cache">
  ## temporary_data_in_distributed_cache
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Almacena datos temporales en la memoria caché distribuida.

<div id="text_index_header_cache_max_entries">
  ## text_index_header_cache_max_entries
</div>

<SettingsInfoBlock type="UInt64" default_value="100000" changeable_without_restart="No" />Tamaño de la caché del encabezado del índice de texto, en entradas. Cero significa que está deshabilitado.

<div id="text_index_header_cache_policy">
  ## text_index_header_cache_policy
</div>

<SettingsInfoBlock type="String" default_value="SLRU" changeable_without_restart="No" />Nombre de la política de caché del encabezado del índice de texto.

<div id="text_index_header_cache_size">
  ## text_index_header_cache_size
</div>

<SettingsInfoBlock type="UInt64" default_value="1073741824" changeable_without_restart="Sí" />Tamaño de la caché para las cabeceras del índice de texto. Un valor de cero lo desactiva.

<Note>
  Esta configuración se puede modificar en tiempo de ejecución y surtirá efecto de inmediato.
</Note>

<div id="text_index_header_cache_size_ratio">
  ## text_index_header_cache_size_ratio
</div>

<SettingsInfoBlock type="Double" default_value="0.5" changeable_without_restart="No" />El tamaño de la cola protegida (en el caso de la política SLRU) de la caché del encabezado del índice de texto, en relación con el tamaño total de la caché.

<div id="text_index_postings_cache_max_entries">
  ## text_index_postings_cache_max_entries
</div>

<SettingsInfoBlock type="UInt64" default_value="1000000" changeable_without_restart="No" />Tamaño de la caché de la posting list del índice de texto, en entradas. Cero significa que está deshabilitada.

<div id="text_index_postings_cache_policy">
  ## text_index_postings_cache_policy
</div>

<SettingsInfoBlock type="String" default_value="SLRU" changeable_without_restart="No" />Nombre de la política de caché de la posting list del índice de texto.

<div id="text_index_postings_cache_size">
  ## text_index_postings_cache_size
</div>

<SettingsInfoBlock type="UInt64" default_value="2147483648" changeable_without_restart="Sí" />Tamaño de la caché para las posting lists del índice de texto. Cero significa que está deshabilitada.

<Note>
  Este ajuste puede modificarse en tiempo de ejecución y surtirá efecto de inmediato.
</Note>

<div id="text_index_postings_cache_size_ratio">
  ## text_index_postings_cache_size_ratio
</div>

<SettingsInfoBlock type="Double" default_value="0.5" changeable_without_restart="No" />El tamaño de la cola protegida (en el caso de la política SLRU) en la caché de posting list del índice de texto, en relación con el tamaño total de la caché.

<div id="text_index_tokens_cache_max_entries">
  ## text_index_tokens_cache_max_entries
</div>

<SettingsInfoBlock type="UInt64" default_value="1000000" changeable_without_restart="No" />Tamaño de la caché de tokens del índice de texto, en entradas. Cero significa que está deshabilitada.

<div id="text_index_tokens_cache_policy">
  ## text_index_tokens_cache_policy
</div>

<SettingsInfoBlock type="String" default_value="SLRU" changeable_without_restart="No" />Nombre de la política de caché de tokens del índice de texto.

<div id="text_index_tokens_cache_size">
  ## text_index_tokens_cache_size
</div>

<SettingsInfoBlock type="UInt64" default_value="1073741824" changeable_without_restart="Yes" />Tamaño de la caché de tokens del índice de texto. Cero significa que está deshabilitada.

<Note>
  Esta configuración se puede modificar en tiempo de ejecución y surtirá efecto de inmediato.
</Note>

<div id="text_index_tokens_cache_size_ratio">
  ## text_index_tokens_cache_size_ratio
</div>

<SettingsInfoBlock type="Double" default_value="0.5" changeable_without_restart="No" />El tamaño de la cola protegida (en el caso de la política SLRU) en la caché de tokens del índice de texto, respecto al tamaño total de la caché.

<div id="text_log">
  ## text_log
</div>

Configuración de la [tabla del sistema text&#95;log](/es/reference/system-tables/text_log) para registrar mensajes de texto.

<SystemLogParameters />

Además:

| Configuración | Descripción                                                                               | Valor predeterminado |
| ------------- | ----------------------------------------------------------------------------------------- | -------------------- |
| `level`       | Nivel máximo de mensaje (de forma predeterminada, `Trace`) que se almacenará en la tabla. | `Trace`              |

**Ejemplo**

```xml
<clickhouse>
    <text_log>
        <level>notice</level>
        <database>system</database>
        <table>text_log</table>
        <flush_interval_milliseconds>7500</flush_interval_milliseconds>
        <max_size_rows>1048576</max_size_rows>
        <reserved_size_rows>8192</reserved_size_rows>
        <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
        <flush_on_crash>false</flush_on_crash>
        <!-- <partition_by>event_date</partition_by> -->
        <engine>Engine = MergeTree PARTITION BY event_date ORDER BY event_time TTL event_date + INTERVAL 30 day</engine>
    </text_log>
</clickhouse>
```

<div id="thread_pool_queue_size">
  ## thread_pool_queue_size
</div>

<SettingsInfoBlock type="UInt64" default_value="10000" changeable_without_restart="No" />

La cantidad máxima de trabajos que se pueden programar en el Global Thread pool. Aumentar el tamaño de la cola conlleva un mayor uso de memoria. Se recomienda mantener este valor igual a [`max_thread_pool_size`](/es/reference/settings/server-settings/settings#max_thread_pool_size).

<Note>
  Un valor de `0` significa que no hay límite.
</Note>

**Ejemplo**

```xml
<thread_pool_queue_size>12000</thread_pool_queue_size>
```

<div id="threadpool_local_fs_reader_pool_size">
  ## threadpool_local_fs_reader_pool_size
</div>

<SettingsInfoBlock type="NonZeroUInt64" default_value="100" changeable_without_restart="No" />El número de hilos del pool de hilos para leer desde el sistema de archivos local cuando `local_filesystem_read_method = 'pread_threadpool'`.

<div id="threadpool_local_fs_reader_queue_size">
  ## threadpool_local_fs_reader_queue_size
</div>

<SettingsInfoBlock type="UInt64" default_value="10000" changeable_without_restart="No" />El número máximo de trabajos que se pueden programar en el pool de hilos para leer desde el sistema de archivos local.

<div id="threadpool_remote_fs_reader_pool_size">
  ## threadpool_remote_fs_reader_pool_size
</div>

<SettingsInfoBlock type="NonZeroUInt64" default_value="250" changeable_without_restart="No" />Número de hilos del pool de hilos utilizado para leer desde el sistema de archivos remoto cuando `remote_filesystem_read_method = 'threadpool'`.

<div id="threadpool_remote_fs_reader_queue_size">
  ## threadpool_remote_fs_reader_queue_size
</div>

<SettingsInfoBlock type="UInt64" default_value="10000" changeable_without_restart="No" />El número máximo de trabajos que se pueden programar en el pool de hilos para leer desde un sistema de archivos remoto.

<div id="threadpool_writer_pool_size">
  ## threadpool_writer_pool_size
</div>

<SettingsInfoBlock type="NonZeroUInt64" default_value="100" changeable_without_restart="No" />Tamaño del pool en segundo plano para las solicitudes de escritura a sistemas de almacenamiento de objetos

<div id="threadpool_writer_queue_size">
  ## threadpool_writer_queue_size
</div>

<SettingsInfoBlock type="UInt64" default_value="10000" changeable_without_restart="No" />Número de tareas que se pueden enviar al pool en segundo plano para solicitudes de escritura en almacenamientos de objetos

<div id="throw_on_unknown_workload">
  ## throw_on_unknown_workload
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="Sí" />

Define el comportamiento al acceder a un WORKLOAD desconocido con el ajuste de consulta &#39;workload&#39;.

* Si es `true`, se lanza la excepción RESOURCE&#95;ACCESS&#95;DENIED desde una consulta que intenta acceder a un workload desconocido. Resulta útil para aplicar la planificación de recursos a todas las consultas una vez que la jerarquía de WORKLOAD se haya establecido y contenga WORKLOAD default.
* Si es `false` (predeterminado), se proporciona acceso ilimitado, sin planificación de recursos, a una consulta con el ajuste &#39;workload&#39; que apunta a un WORKLOAD desconocido. Esto es importante mientras se configura la jerarquía de WORKLOAD, antes de añadir WORKLOAD default.

**Ejemplo**

```xml
<throw_on_unknown_workload>true</throw_on_unknown_workload>
```

**Véase también**

* [Planificación de cargas de trabajo](/es/concepts/features/configuration/server-config/workload-scheduling)

<div id="timezone">
  ## timezone
</div>

La zona horaria del servidor.

Se especifica como un identificador IANA de la zona horaria UTC o de una ubicación geográfica (por ejemplo, Africa/Abidjan).

La zona horaria es necesaria para las conversiones entre los formatos String y DateTime cuando los campos DateTime se generan en formato de texto (se imprimen en pantalla o en un archivo) y cuando se obtiene un valor DateTime a partir de una cadena. Además, la zona horaria se usa en las funciones que trabajan con fecha y hora si no la reciben en los parámetros de entrada.

**Ejemplo**

```xml
<timezone>Asia/Istanbul</timezone>
```

**Véase también**

* [session&#95;timezone](/es/reference/settings/session-settings#session_timezone)

<div id="tmp_path">
  ## tmp_path
</div>

<SettingsInfoBlock type="String" default_value="/var/lib/clickhouse/tmp/" changeable_without_restart="No" />

Ruta en el sistema de archivos local para almacenar datos temporales para el procesamiento de consultas grandes.

<Note>
  * Solo se puede usar una opción para configurar el almacenamiento de datos temporales: tmp&#95;path, tmp&#95;policy, temporary&#95;data&#95;in&#95;cache.
  * La barra diagonal al final es obligatoria.
</Note>

**Ejemplo**

```xml
<tmp_path>/var/lib/clickhouse/tmp/</tmp_path>
```

<div id="tmp_policy">
  ## tmp_policy
</div>

Política de almacenamiento para datos temporales. Todos los archivos con el prefijo `tmp` se eliminarán al iniciar.

<Note>
  Recomendaciones para usar almacenamiento de objetos como `tmp_policy`:

  * Use un `bucket:path` independiente en cada servidor
  * Use `metadata_type=plain`
  * También puede que quiera configurar TTL para este bucket
</Note>

<Note>
  * Solo se puede usar una opción para configurar el almacenamiento de datos temporales: `tmp_path`, `tmp_policy`, `temporary_data_in_cache`.
  * `move_factor`, `keep_free_space_bytes`, `max_data_part_size_bytes` se ignoran.
  * La política debe tener exactamente *un volumen*

  Para obtener más información, consulte la documentación de [MergeTree Table Engine](/es/reference/engines/table-engines/mergetree-family/mergetree).
</Note>

**Ejemplo**

Cuando `/disk1` esté lleno, los datos temporales se almacenarán en `/disk2`.

```xml highlight={13-20,24}
<clickhouse>
<storage_configuration>
<disks>
<disk1>
<path>/disk1/</path>
</disk1>
<disk2>
<path>/disk2/</path>
</disk2>
</disks>

<policies>
<tmp_two_disks>
<volumes>
<main>
<disk>disk1</disk>
<disk>disk2</disk>
</main>
</volumes>
</tmp_two_disks>
</policies>
</storage_configuration>

<tmp_policy>tmp_two_disks</tmp_policy>
</clickhouse>
```

<div id="top_level_domains_list">
  ## top_level_domains_list
</div>

Define una lista de dominios de nivel superior personalizados que se añadirán, donde cada entrada tiene el formato `<name>/path/to/file</name>`.

Por ejemplo:

```xml
<top_level_domains_lists>
    <public_suffix_list>/path/to/public_suffix_list.dat</public_suffix_list>
</top_level_domains_lists>
```

Véase también:

* la función [`cutToFirstSignificantSubdomainCustom`](/es/reference/functions/regular-functions/url-functions#cutToFirstSignificantSubdomainCustom) y sus variaciones,
  que acepta el nombre de una lista personalizada de TLD y devuelve la parte del dominio que incluye los subdominios de nivel superior hasta el primer subdominio significativo.

<div id="top_level_domains_path">
  ## top_level_domains_path
</div>

<SettingsInfoBlock type="String" default_value="/var/lib/clickhouse/top_level_domains/" changeable_without_restart="No" />

El directorio que contiene los dominios de nivel superior.

**Ejemplo**

```xml
<top_level_domains_path>/var/lib/clickhouse/top_level_domains/</top_level_domains_path>
```

<div id="total_memory_profiler_sample_max_allocation_size">
  ## total_memory_profiler_sample_max_allocation_size
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />Recopila asignaciones aleatorias de tamaño menor o igual al valor especificado, con una probabilidad igual a `total_memory_profiler_sample_probability`. 0 significa desactivado. Es posible que quiera establecer &#39;max&#95;untracked&#95;memory&#39; en 0 para que este umbral funcione como se espera.

<div id="total_memory_profiler_sample_min_allocation_size">
  ## total_memory_profiler_sample_min_allocation_size
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />Recopila asignaciones aleatorias de tamaño mayor o igual que el valor especificado, con una probabilidad igual a `total_memory_profiler_sample_probability`. 0 significa que está deshabilitado. Puede que quiera establecer `max_untracked_memory` en 0 para que este umbral funcione como se espera.

<div id="total_memory_profiler_step">
  ## total_memory_profiler_step
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="No" />Cada vez que el uso de memoria del servidor supere el siguiente umbral de bytes, el perfilador de memoria recopilará la traza de pila de asignación. Cero significa que el perfilador de memoria está deshabilitado. Los valores inferiores a unos pocos megabytes ralentizarán el servidor.

<div id="total_memory_tracker_sample_probability">
  ## total_memory_tracker_sample_probability
</div>

<SettingsInfoBlock type="Double" default_value="0" changeable_without_restart="No" />

Permite recopilar asignaciones y liberaciones de memoria aleatorias y escribirlas en la tabla del sistema [system.trace&#95;log](/es/reference/system-tables/trace_log) con `trace_type` igual a `MemorySample`, con la probabilidad especificada. La probabilidad se aplica a cada asignación o liberación de memoria, independientemente del tamaño de la asignación. Tenga en cuenta que el muestreo solo se produce cuando la cantidad de memoria no rastreada supera el límite de memoria no rastreada (el valor predeterminado es `4` MiB). Puede reducirse si se reduce [total&#95;memory&#95;profiler&#95;step](/es/reference/settings/server-settings/settings#total_memory_profiler_step). Puede establecer `total_memory_profiler_step` en `1` para obtener un muestreo más detallado.

Posibles valores:

* Número de doble precisión positivo.
* `0` — La escritura de asignaciones y liberaciones de memoria aleatorias en la tabla del sistema `system.trace_log` está deshabilitada.

<div id="trace_log">
  ## trace_log
</div>

Configuración para el funcionamiento de la tabla del sistema [trace&#95;log](/es/reference/system-tables/trace_log).

<SystemLogParameters />

El archivo de configuración del servidor predeterminado `config.xml` contiene la siguiente sección de configuración:

```xml
<trace_log>
    <database>system</database>
    <table>trace_log</table>
    <partition_by>toYYYYMM(event_date)</partition_by>
    <flush_interval_milliseconds>7500</flush_interval_milliseconds>
    <max_size_rows>1048576</max_size_rows>
    <reserved_size_rows>8192</reserved_size_rows>
    <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>
    <flush_on_crash>false</flush_on_crash>
    <symbolize>false</symbolize>
</trace_log>
```

<div id="uncompressed_cache_policy">
  ## uncompressed_cache_policy
</div>

<SettingsInfoBlock type="String" default_value="SLRU" changeable_without_restart="No" />Nombre de la política de caché sin comprimir.

<div id="uncompressed_cache_size">
  ## uncompressed_cache_size
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Sí" />

Tamaño máximo (en bytes) de los datos sin comprimir usados por los motores de tabla de la familia MergeTree.

Hay una única caché compartida para el servidor. La memoria se asigna bajo demanda. La caché se usa si la opción `use_uncompressed_cache` está activada.

La caché sin comprimir puede resultar ventajosa para consultas muy cortas en determinados casos.

<Note>
  Un valor de `0` significa que está deshabilitada.

  Esta configuración puede modificarse en tiempo de ejecución y surtirá efecto de inmediato.
</Note>

<div id="uncompressed_cache_size_ratio">
  ## uncompressed_cache_size_ratio
</div>

<SettingsInfoBlock type="Double" default_value="0.5" changeable_without_restart="No" />El tamaño de la cola protegida (en el caso de la política SLRU) en la caché sin comprimir, en relación con el tamaño total de la caché.

<div id="unique_key_index_cache_policy">
  ## unique_key_index_cache_policy
</div>

<SettingsInfoBlock type="String" default_value="SLRU" changeable_without_restart="No" />Nombre de la política de caché para el índice UNIQUE KEY (SLRU o LRU).

<div id="unique_key_index_cache_size_bytes">
  ## unique_key_index_cache_size_bytes
</div>

<SettingsInfoBlock type="UInt64" default_value="1073741824" changeable_without_restart="No" />Tamaño máximo (en bytes) de la caché en el mismo proceso para los bloques del índice UNIQUE KEY (SST). Establézcalo en 0 para deshabilitar la caché.

<div id="unique_key_index_cache_size_ratio">
  ## unique_key_index_cache_size_ratio
</div>

<SettingsInfoBlock type="Double" default_value="0.5" changeable_without_restart="No" />El tamaño de la cola protegida (en caso de una política SLRU) en la caché del índice UNIQUE KEY, en relación con el tamaño total de la caché.

<div id="url_scheme_mappers">
  ## url_scheme_mappers
</div>

Configuración para convertir prefijos de URL abreviados o simbólicos en URL completas.

Ejemplo:

```xml
<url_scheme_mappers>
    <s3>
        <to>https://{bucket}.s3.amazonaws.com</to>
    </s3>
    <gs>
        <to>https://storage.googleapis.com/{bucket}</to>
    </gs>
    <oss>
        <to>https://{bucket}.oss.aliyuncs.com</to>
    </oss>
</url_scheme_mappers>
```

<div id="use_minimalistic_part_header_in_zookeeper">
  ## use_minimalistic_part_header_in_zookeeper
</div>

Método de almacenamiento de los encabezados de las partes de datos en ZooKeeper. Esta configuración solo se aplica a la familia [`MergeTree`](/es/reference/engines/table-engines/mergetree-family/index). Se puede especificar:

**Globalmente en la sección [merge&#95;tree](#merge_tree) del archivo `config.xml`**

ClickHouse usa esta configuración para todas las tablas del servidor. Puede cambiar la configuración en cualquier momento. Las tablas existentes cambian su comportamiento cuando cambia la configuración.

**Para cada tabla**

Al crear una tabla, especifique la [configuración del motor](/es/reference/engines/table-engines/mergetree-family/mergetree#table_engine-mergetree-creating-a-table) correspondiente. El comportamiento de una tabla existente con esta configuración no cambia, incluso si cambia la configuración global.

**Valores posibles**

* `0` — La funcionalidad está desactivada.
* `1` — La funcionalidad está activada.

Si [`use_minimalistic_part_header_in_zookeeper = 1`](#use_minimalistic_part_header_in_zookeeper), las tablas [replicadas](/es/reference/engines/table-engines/mergetree-family/replication) almacenan de forma compacta los encabezados de las partes de datos usando un único `znode`. Si la tabla contiene muchas columnas, este método de almacenamiento reduce significativamente el volumen de datos almacenados en ZooKeeper.

<Note>
  Después de aplicar `use_minimalistic_part_header_in_zookeeper = 1`, no puede revertir el servidor ClickHouse a una versión anterior que no admita esta configuración. Tenga cuidado al actualizar ClickHouse en los servidores de un clúster. No actualice todos los servidores a la vez. Es más seguro probar las nuevas versiones de ClickHouse en un entorno de prueba o solo en unos pocos servidores del clúster.

  Los encabezados de las partes de datos ya almacenados con esta configuración no pueden restaurarse a su representación anterior (no compacta).
</Note>

<div id="use_separate_cache_arena">
  ## use_separate_cache_arena
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />

Habilita una arena de jemalloc dedicada para las asignaciones de caché (mark cache, uncompressed cache, page cache).
Aísla los datos de la caché de las asignaciones de memoria del procesamiento de consultas, lo que reduce la fragmentación de la memoria.

<div id="use_shared_merge_tree_log_pipeline">
  ## use_shared_merge_tree_log_pipeline
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />

Solo disponible en ClickHouse Cloud. Cuando está habilitado, las tablas `system.*_log` se respaldan con `SharedMergeTree` mediante un pipeline basado en S3.
Para cada log `<log>`, los siguientes objetos se crean automáticamente al iniciar:

* `system.<log>_s3` — una tabla respaldada por `S3` que es el destino directo de vaciado; cada
  llamada a `SYSTEM FLUSH LOGS` escribe aquí un nuevo archivo particionado.
* `system.<log>_s3queue` — una tabla `S3Queue` (modo ordenado) que recoge archivos de
  `<log>_s3` y envía las filas al siguiente paso. Cada nodo procesa solo sus propios archivos mediante
  particionamiento basado en `partition_regex`.
* `system.<log>_mv` — una `MATERIALIZED VIEW` que enruta las filas de `<log>_s3queue` hacia
  la tabla final `SharedMergeTree`.
* `system.<log>` — la tabla final `SharedMergeTree` donde las filas se acumulan y se consultan.

Cuando cambia el esquema o la configuración, la tabla afectada se renombra a `<log>_0`, `<log>_1`, etc.
y se vuelve a crear, siguiendo el comportamiento actual de rotación de `SystemLog`.

Requiere que `<shared_log_pipeline><endpoint>` esté configurado en la configuración del servidor.
Véase también: `shared_log_pipeline.enable_sync_flush`, `shared_log_pipeline.flush_timeout_seconds`.

<div id="user_defined_executable_functions_config">
  ## user_defined_executable_functions_config
</div>

La ruta del archivo de configuración para las funciones ejecutables definidas por el usuario.

Ruta:

* Especifique la ruta absoluta o la ruta relativa al archivo de configuración del servidor.
* La ruta puede contener comodines * y ?.

Véase también:

* &quot;[funciones ejecutables definidas por el usuario](/es/reference/functions/regular-functions/udf#executable-user-defined-functions).&quot;.

**Ejemplo**

```xml
<user_defined_executable_functions_config>*_function.xml</user_defined_executable_functions_config>
```

<div id="user_defined_path">
  ## user_defined_path
</div>

El directorio con archivos definidos por el usuario. Se utiliza para las funciones definidas por el usuario en SQL [Funciones definidas por el usuario en SQL](/es/reference/functions/regular-functions/udf).

**Ejemplo**

```xml
<user_defined_path>/var/lib/clickhouse/user_defined/</user_defined_path>
```

<div id="user_directories">
  ## user_directories
</div>

Sección del archivo de configuración que contiene los siguientes ajustes:

* Ruta al archivo de configuración con los usuarios predefinidos.
* Ruta a la carpeta donde se almacenan los usuarios creados mediante comandos SQL.
* Ruta del nodo de ZooKeeper donde se almacenan y se replican los usuarios creados mediante comandos SQL.

Si se especifica esta sección, no se usarán las rutas de [users&#95;config](/es/reference/settings/server-settings/settings#users_config) y [access&#95;control&#95;path](/es/reference/settings/server-settings/settings#access_control_path).

La sección `user_directories` puede contener cualquier cantidad de elementos; el orden de los elementos determina su precedencia (cuanto más arriba esté un elemento, mayor será su precedencia).

**Ejemplos**

```xml
<user_directories>
    <users_xml>
        <path>/etc/clickhouse-server/users.xml</path>
    </users_xml>
    <local_directory>
        <path>/var/lib/clickhouse/access/</path>
    </local_directory>
</user_directories>
```

Los usuarios, los roles, las políticas de fila, las cuotas y los perfiles también se pueden almacenar en ZooKeeper:

```xml
<user_directories>
    <users_xml>
        <path>/etc/clickhouse-server/users.xml</path>
    </users_xml>
    <replicated>
        <zookeeper_path>/clickhouse/access/</zookeeper_path>
    </replicated>
</user_directories>
```

También puede definir las secciones `memory` — que significa almacenar información solo en memoria, sin escribirla en disco— y `ldap`, que significa almacenar información en un servidor LDAP.

Para añadir un servidor LDAP como directorio remoto para usuarios que no están definidos localmente, defina una única sección `ldap` con la siguiente configuración:

| Setting  | Description                                                                                                                                                                                                                                                                                                                                                                                                            |
| -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `roles`  | sección con una lista de roles definidos localmente que se asignarán a cada usuario recuperado del servidor LDAP. Si no se especifica ningún rol, el usuario no podrá realizar ninguna acción después de la autenticación. Si alguno de los roles de la lista no está definido localmente en el momento de la autenticación, el intento de autenticación fallará como si la contraseña proporcionada fuera incorrecta. |
| `server` | uno de los nombres de servidores LDAP definidos en la sección de configuración `ldap_servers`. Este parámetro es obligatorio y no puede estar vacío.                                                                                                                                                                                                                                                                   |

**Ejemplo**

```xml
<ldap>
    <server>my_ldap_server</server>
        <roles>
            <my_local_role1 />
            <my_local_role2 />
        </roles>
</ldap>
```

<div id="user_files_path">
  ## user_files_path
</div>

<SettingsInfoBlock type="String" default_value="/var/lib/clickhouse/user_files/" changeable_without_restart="No" />

El directorio que contiene los archivos de usuario. Se usa en la función de tabla [file()](/es/reference/functions/table-functions/file), [fileCluster()](/es/reference/functions/table-functions/fileCluster).

**Ejemplo**

```xml
<user_files_path>/var/lib/clickhouse/user_files/</user_files_path>
```

<div id="user_scripts_path">
  ## user_scripts_path
</div>

<SettingsInfoBlock type="String" default_value="/var/lib/clickhouse/user_scripts/" changeable_without_restart="No" />

Directorio que contiene los archivos de scripts de usuario. Se utiliza para las [funciones ejecutables definidas por el usuario](/es/reference/functions/regular-functions/udf#executable-user-defined-functions).

**Ejemplo**

```xml
<user_scripts_path>/var/lib/clickhouse/user_scripts/</user_scripts_path>
```

<div id="users_config">
  ## users_config
</div>

Ruta al archivo que contiene:

* Configuraciones de usuarios.
* Permisos de acceso.
* Perfiles de configuración.
* Configuración de cuotas.

**Ejemplo**

```xml
<users_config>users.xml</users_config>
```

<div id="users_to_ignore_early_memory_limit_check">
  ## users_to_ignore_early_memory_limit_check
</div>

Lista de usuarios separados por comas para los que se ignorará una comprobación temprana del límite de memoria. Si el usuario no está en esta lista, se rechazará una consulta si el uso total de memoria supera el límite.

<div id="validate_tcp_client_information">
  ## validate_tcp_client_information
</div>

<SettingsInfoBlock type="Bool" default_value="0" changeable_without_restart="No" />Determina si la validación de la información del cliente está activada al recibir un paquete de consulta.

De forma predeterminada, es `false`:

```xml
<validate_tcp_client_information>false</validate_tcp_client_information>
```

<div id="vector_similarity_index_cache_max_entries">
  ## vector_similarity_index_cache_max_entries
</div>

<SettingsInfoBlock type="UInt64" default_value="10000000" changeable_without_restart="No" />Tamaño de la caché del índice de similitud vectorial, en entradas. Un valor de cero significa que está desactivada.

<div id="vector_similarity_index_cache_policy">
  ## vector_similarity_index_cache_policy
</div>

<SettingsInfoBlock type="String" default_value="SLRU" changeable_without_restart="No" />Nombre de la política de caché del índice de similitud vectorial.

<div id="vector_similarity_index_cache_size">
  ## vector_similarity_index_cache_size
</div>

<SettingsInfoBlock type="UInt64" default_value="5368709120" changeable_without_restart="Yes" />Tamaño de la caché para los índices de similitud vectorial. Cero significa que está deshabilitado.

<Note>
  Esta configuración puede modificarse en tiempo de ejecución y surtirá efecto de inmediato.
</Note>

<div id="vector_similarity_index_cache_size_ratio">
  ## vector_similarity_index_cache_size_ratio
</div>

<SettingsInfoBlock type="Double" default_value="0.5" changeable_without_restart="No" />El tamaño de la cola protegida (en el caso de la política SLRU) en la caché del índice de similitud vectorial, en relación con el tamaño total de la caché.

<div id="wait_dictionaries_load_at_startup">
  ## wait_dictionaries_load_at_startup
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />

Esta configuración permite especificar el comportamiento cuando `dictionaries_lazy_load` es `false`.
(Si `dictionaries_lazy_load` es `true`, esta configuración no tiene ningún efecto).

Si `wait_dictionaries_load_at_startup` es `false`, el servidor
empezará a cargar todos los diccionarios al iniciarse y aceptará conexiones en paralelo con esa carga.
Cuando se utilice un diccionario en una consulta por primera vez, la consulta esperará hasta que el diccionario se haya cargado, si todavía no lo está.
Establecer `wait_dictionaries_load_at_startup` en `false` puede hacer que ClickHouse se inicie más rápido; sin embargo, algunas consultas pueden ejecutarse más lentamente
(porque tendrán que esperar a que se carguen algunos diccionarios).

Si `wait_dictionaries_load_at_startup` es `true`, el servidor esperará durante el inicio
hasta que todos los diccionarios terminen de cargarse (correctamente o no) antes de aceptar conexiones.

**Ejemplo**

```xml
<wait_dictionaries_load_at_startup>true</wait_dictionaries_load_at_startup>
```

<div id="webassembly_udf_engine">
  ## webassembly_udf_engine
</div>

<SettingsInfoBlock type="String" default_value="wasmtime" changeable_without_restart="No" />El motor utilizado para ejecutar UDFs de WebAssembly. Los valores admitidos son &#39;wasmtime&#39; y &#39;wasmedge&#39;.

<div id="webterminal_allowed_origins">
  ## webterminal_allowed_origins
</div>

Lista separada por comas de orígenes completos (`esquema` + host + puerto opcional) autorizados para abrir sesiones WebSocket de `/webterminal`. Cuando está vacía, se aplica estrictamente la política de mismo origen (`Origin` debe coincidir con el esquema, el host y el puerto de la solicitud). Configure esto para despliegues detrás de un proxy inverso con terminación TLS donde `request.isSecure()` es `false` aunque el navegador use `https`. Ejemplo: `https://example.com,https://app.example.com:8443`.

<div id="workload_path">
  ## workload_path
</div>

El directorio que se usa para almacenar todas las consultas `CREATE WORKLOAD` y `CREATE RESOURCE`. De forma predeterminada, se usa la carpeta `/workload/` dentro del directorio de trabajo del servidor.

**Ejemplo**

```xml
<workload_path>/var/lib/clickhouse/workload/</workload_path>
```

**Véase también**

* [Jerarquía de cargas de trabajo](/es/concepts/features/configuration/server-config/workload-scheduling#workloads)
* [workload&#95;zookeeper&#95;path](#workload_zookeeper_path)

<div id="workload_zookeeper_path">
  ## workload_zookeeper_path
</div>

La ruta a un nodo de ZooKeeper, que se utiliza como almacenamiento para todas las consultas `CREATE WORKLOAD` y `CREATE RESOURCE`. Para mantener la coherencia, todas las definiciones SQL se almacenan como valor de este único znode. De forma predeterminada, ZooKeeper no se utiliza y las definiciones se almacenan en [disco](#workload_path).

**Ejemplo**

```xml
<workload_zookeeper_path>/clickhouse/workload/definitions.sql</workload_zookeeper_path>
```

**Véase también**

* [Jerarquía de cargas de trabajo](/es/concepts/features/configuration/server-config/workload-scheduling#workloads)
* [workload&#95;path](#workload_path)

<div id="zookeeper">
  ## zookeeper
</div>

Contiene ajustes que permiten a ClickHouse interactuar con un [clúster de ZooKeeper](http://zookeeper.apache.org/). ClickHouse usa ZooKeeper para almacenar los metadatos de las réplicas cuando se utilizan tablas replicadas. Si no se usan tablas replicadas, esta sección de parámetros puede omitirse.

Los siguientes ajustes se pueden configurar mediante subetiquetas:

| Setting                                         | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `node`                                          | Endpoint de ZooKeeper. Puede configurar varios endpoints. Por ejemplo, `<node index="1"><host>example_host</host><port>2181</port></node>`. El atributo `index` especifica el orden de los nodos al intentar conectarse al clúster de ZooKeeper.                                                                                                                                                                                                              |
| `operation_timeout_ms`                          | Tiempo de espera máximo para una operación, en milisegundos.                                                                                                                                                                                                                                                                                                                                                                                                  |
| `session_timeout_ms`                            | Tiempo de espera máximo para la sesión del cliente, en milisegundos.                                                                                                                                                                                                                                                                                                                                                                                          |
| `root` (optional)                               | El znode que se usa como raíz para los znodes utilizados por el servidor de ClickHouse.                                                                                                                                                                                                                                                                                                                                                                       |
| `fallback_session_lifetime.min` (optional)      | Límite mínimo para la duración de una sesión de ZooKeeper en el nodo fallback cuando el primario no está disponible (balanceo de carga). Se establece en segundos. Predeterminado: 3 horas.                                                                                                                                                                                                                                                                   |
| `fallback_session_lifetime.max` (optional)      | Límite máximo para la duración de una sesión de ZooKeeper en el nodo fallback cuando el primario no está disponible (balanceo de carga). Se establece en segundos. Predeterminado: 6 horas.                                                                                                                                                                                                                                                                   |
| `identity` (optional)                           | Usuario y contraseña requeridos por ZooKeeper para acceder a los znodes solicitados.                                                                                                                                                                                                                                                                                                                                                                          |
| `use_compression` (optional)                    | Habilita la compresión en el protocolo Keeper si se establece en `true`.                                                                                                                                                                                                                                                                                                                                                                                      |
| `use_xid_64` (optional)                         | Habilita los ID de transacción de 64 bits. Establézcalo en `true` para habilitar el formato extendido de ID de transacción. Predeterminado: `false`.                                                                                                                                                                                                                                                                                                          |
| `pass_opentelemetry_tracing_context` (optional) | Habilita la propagación del contexto de tracing de OpenTelemetry a las solicitudes de Keeper. Cuando está habilitado, se crearán spans de tracing para las operaciones de Keeper, lo que permite realizar distributed tracing entre ClickHouse y Keeper. Consulte [Tracing ClickHouse Keeper Requests](/es/guides/oss/deployment-and-scaling/monitoring/opentelemetry#tracing-clickhouse-keeper-requests) para obtener más información. Predeterminado: `false`. |

También existe el ajuste `zookeeper_load_balancing` (opcional), que permite seleccionar el algoritmo para la selección de nodos de ZooKeeper:

| Algorithm Name                   | Description                                                                                                                                           |
| -------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| `random`                         | selecciona aleatoriamente uno de los nodos de ZooKeeper.                                                                                              |
| `in_order`                       | selecciona el primer nodo de ZooKeeper; si no está disponible, selecciona el segundo, y así sucesivamente.                                            |
| `nearest_hostname`               | selecciona un nodo de ZooKeeper cuyo hostname sea lo más parecido posible al hostname del servidor; el hostname se compara con el prefijo del nombre. |
| `hostname_levenshtein_distance`  | igual que nearest&#95;hostname, pero compara el hostname mediante la distancia de Levenshtein.                                                        |
| `hostname_longest_common_prefix` | igual que nearest&#95;hostname, pero prefiere el nodo cuyo hostname comparte el prefijo común más largo con el hostname del servidor.                 |
| `hostname_longest_common_suffix` | igual que nearest&#95;hostname, pero prefiere el nodo cuyo hostname comparte el sufijo común más largo con el hostname del servidor.                  |
| `first_or_random`                | selecciona el primer nodo de ZooKeeper; si no está disponible, selecciona aleatoriamente uno de los nodos de ZooKeeper restantes.                     |
| `round_robin`                    | selecciona el primer nodo de ZooKeeper; si se produce una reconexión, selecciona el siguiente.                                                        |

**Configuración de ejemplo**

```xml
<zookeeper>
    <node>
        <host>example1</host>
        <port>2181</port>
    </node>
    <node>
        <host>example2</host>
        <port>2181</port>
    </node>
    <session_timeout_ms>30000</session_timeout_ms>
    <operation_timeout_ms>10000</operation_timeout_ms>
    <!-- Optional. Chroot suffix. Should exist. -->
    <root>/path/to/zookeeper/node</root>
    <!-- Optional. Zookeeper digest ACL string. -->
    <identity>user:password</identity>
    <!--<zookeeper_load_balancing>random / in_order / nearest_hostname / hostname_levenshtein_distance / hostname_longest_common_prefix / hostname_longest_common_suffix / first_or_random / round_robin</zookeeper_load_balancing>-->
    <zookeeper_load_balancing>random</zookeeper_load_balancing>
    <!-- Optional. Enable 64-bit transaction IDs. -->
    <use_xid_64>false</use_xid_64>
    <!-- Optional. Enable OpenTelemetry tracing context propagation. -->
    <pass_opentelemetry_tracing_context>false</pass_opentelemetry_tracing_context>
</zookeeper>
```

**Véase también**

* [Replicación](/es/reference/engines/table-engines/mergetree-family/replication)
* [ZooKeeper Programmer&#39;s Guide](http://zookeeper.apache.org/doc/current/zookeeperProgrammers.html)
* [Comunicación segura opcional entre ClickHouse y ZooKeeper](/es/guides/oss/deployment-and-scaling/keeper/ssl-zookeeper)

<div id="zookeeper_log">
  ## zookeeper_log
</div>

Configuración de la tabla del sistema [`zookeeper_log`](/es/reference/system-tables/zookeeper_log).

La siguiente configuración puede definirse mediante subetiquetas:

<SystemLogParameters />

**Ejemplo**

```xml
<clickhouse>
    <zookeeper_log>
        <database>system</database>
        <table>zookeeper_log</table>
        <flush_interval_milliseconds>7500</flush_interval_milliseconds>
        <ttl>event_date + INTERVAL 1 WEEK DELETE</ttl>
    </zookeeper_log>
</clickhouse>
```