---
title: 'ajustes de sesión de http_*'
sidebarTitle: 'http_*'
toc_max_heading_level: 2
description: 'Ajustes de sesión de ClickHouse en el grupo generado http_*.'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/es/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import { VersionHistory } from "/snippets/es/components/VersionHistory/VersionHistory.jsx";

Estas configuraciones están disponibles en [system.settings](/es/reference/system-tables/settings) y se generan automáticamente a partir del [código fuente](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp).

<div id="http_allow_database_as_path">
  ## http_allow_database_as_path
</div>

<SettingsInfoBlock type="Bool" default_value="0" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.8"},{"label": "0"},{"label": "Nueva configuración para reconocer un nombre de base de datos en la ruta URL de las solicitudes HTTP."}]}]} />

Si está habilitada, la interfaz HTTP reconoce un componente `/database/` en la ruta URL y lo utiliza como base de datos actual.

Esta configuración por usuario determina si se interpreta una solicitud enrutada de estilo de ruta. El enrutamiento propiamente dicho se controla globalmente mediante la configuración del servidor `http_allow_path_requests` (desactivada de forma predeterminada), que debe habilitarse para que la interfaz HTTP enrute una solicitud de estilo de ruta (como `/my_db/my_table.csv`) al manejador de consultas; esta decisión de enrutamiento se toma antes de autenticar la solicitud, por lo que no puede depender de una configuración por usuario. Cuando `http_allow_path_requests` está desactivada, las rutas desconocidas devuelven un simple `404`. Tras el enrutamiento, esta configuración se vuelve a comprobar con la configuración efectiva del usuario autenticado, por lo que puede habilitarse selectivamente para cada usuario, rol o perfil.

<div id="http_allow_filters_as_path">
  ## http_allow_filters_as_path
</div>

<SettingsInfoBlock type="Bool" default_value="0" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.8"},{"label": "0"},{"label": "Nueva configuración para reconocer filtros `name=value` de estilo Hive en la ruta URL de las solicitudes HTTP."}]}]} />

Si está habilitada, la interfaz HTTP reconoce componentes `/name=value/` en la ruta (estilo de particionamiento Hive) y los traduce en filtros combinados con AND. También reconoce los operadores `>`, `<`, `>=`, `<=`, `!=`, `<>`.

Al igual que [`http_allow_database_as_path`](/es/reference/settings/session-settings/http#http_allow_database_as_path), esta es una configuración por usuario; el enrutamiento de las solicitudes de estilo de ruta se controla globalmente mediante la configuración de servidor `http_allow_path_requests` (el enrutamiento se realiza antes de la autenticación).

<div id="http_allow_filters_as_unrecognized_url_parameters">
  ## http_allow_filters_as_unrecognized_url_parameters
</div>

<SettingsInfoBlock type="Bool" default_value="0" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.8"},{"label": "0"},{"label": "Nueva configuración para tratar los parámetros URL no reconocidos como expresiones de filtro en solicitudes HTTP."}]}]} />

Si está habilitada, cualquier parámetro URL que no se reconozca como parámetro, configuración o prefijo `param_*` conocido se trata como un filtro y se combina mediante AND. Se admiten dos formas:

* Un `name=value` simple se convierte en la igualdad `` `name` = 'value' `` (el identificador se delimita con comillas invertidas y el valor se entrecomilla como un literal de cadena).
* Un operador de comparación (`!=`, `>`, `<`, `>=`, `<=`, `<>`) lo convierte en una comparación: ya sea separado dentro del parámetro (`?a!=2`, `?a>=2`) o escrito en línea cuando la URL no contiene un `=` por el que separar (`?a<>2`, `?f(x)>3`), en cuyo caso el `name[=value]` reconstruido se analiza como una expresión SQL completa.

<div id="http_allow_table_as_file">
  ## http_allow_table_as_file
</div>

<SettingsInfoBlock type="Bool" default_value="0" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.8"},{"label": "0"},{"label": "Nueva configuración para reconocer un nombre de tabla en la ruta URL de las solicitudes HTTP, con extensiones opcionales de formato\/compresión."}]}]} />

Si está habilitada, la interfaz HTTP reconoce el último componente de la ruta URL como un nombre de tabla con el formato `table`, `table.format` o `table.format.compression`. La ruta se interpreta como `SELECT * FROM table`.

Al igual que [`http_allow_database_as_path`](/es/reference/settings/session-settings/http#http_allow_database_as_path), esta configuración es por usuario; el enrutamiento de las solicitudes de estilo de ruta se controla globalmente mediante la configuración de servidor `http_allow_path_requests` (el enrutamiento se realiza antes de la autenticación).

<div id="http_connection_timeout">
  ## http_connection_timeout
</div>

<SettingsInfoBlock type="Seconds" default_value="1" />

Tiempo de espera de la conexión HTTP (en segundos).

Valores posibles:

* Cualquier número entero positivo.
* 0 - Deshabilitado (tiempo de espera infinito).

<div id="http_make_head_request">
  ## http_make_head_request
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

La configuración `http_make_head_request` permite ejecutar una solicitud `HEAD` al leer datos desde HTTP para obtener información sobre el archivo que se va a leer, como su tamaño. Dado que está habilitada de forma predeterminada, puede ser conveniente desactivar esta configuración cuando el servidor no admita solicitudes `HEAD`.

<div id="http_native_compression_disable_checksumming_on_decompress">
  ## http_native_compression_disable_checksumming_on_decompress
</div>

<SettingsInfoBlock type="Bool" default_value="0" />

Habilita o deshabilita la verificación de la suma de comprobación al descomprimir los datos de una solicitud HTTP POST del client. Se usa solo con el formato de compresión nativo de ClickHouse (no se usa con `gzip` ni `deflate`).

Para obtener más información, lea la [descripción de la interfaz HTTP](/es/concepts/features/interfaces/http).

Valores posibles:

* 0 — Deshabilitado.
* 1 — Habilitado.

<div id="http_receive_timeout">
  ## http_receive_timeout
</div>

<SettingsInfoBlock type="Seconds" default_value="30" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "23.6"},{"label": "30"},{"label": "Consulte http_send_timeout."}]}]} />

Tiempo de espera para la recepción de HTTP (en segundos).

Valores posibles:

* Cualquier número entero positivo (segundos). `0` **no** es un tiempo de espera infinito y puede provocar errores al establecer la conexión (los tiempos de espera de los sockets POSIX requieren un intervalo positivo).

<div id="http_send_timeout">
  ## http_send_timeout
</div>

<SettingsInfoBlock type="Seconds" default_value="30" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "23.6"},{"label": "30"},{"label": "3 minutos parecen exageradamente largos. Ten en cuenta que este es el tiempo de espera para una sola llamada de escritura de red, no para toda la operación de carga."}]}]} />

Tiempo de espera de envío HTTP (en segundos).

Valores posibles:

* Cualquier entero positivo (segundos). `0` **no** es un tiempo de espera infinito y puede provocar errores al establecer la conexión (los tiempos de espera de socket POSIX requieren un intervalo positivo).

<Note>
  Solo se aplica al perfil default. Es necesario reiniciar el servidor para que los cambios surtan efecto.
</Note>

<div id="http_skip_not_found_url_for_globs">
  ## http_skip_not_found_url_for_globs
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

Omitir las URL de globs con error HTTP&#95;NOT&#95;FOUND

<div id="http_wait_end_of_query">
  ## http_wait_end_of_query
</div>

<SettingsInfoBlock type="Bool" default_value="0" />

Habilita el búfer de respuesta HTTP del lado del servidor.

<div id="http_write_exception_in_output_format">
  ## http_write_exception_in_output_format
</div>

<SettingsInfoBlock type="Bool" default_value="0" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "25.11"},{"label": "0"},{"label": "Se cambió para mantener la coherencia entre formatos"}]}, {"id": "row-2","items": [{"label": "23.9"},{"label": "1"},{"label": "Genera JSON/XML válidos cuando se produce una excepción en HTTP streaming."}]}]} />

Escribe la excepción en el formato de salida para generar una salida válida. Funciona con los formatos JSON y XML.

<div id="http_zlib_compression_level">
  ## http_zlib_compression_level
</div>

<SettingsInfoBlock type="Int64" default_value="3" />

Establece el nivel de compresión de datos en la respuesta a una solicitud HTTP si [enable&#95;http&#95;compression = 1](/es/reference/settings/session-settings/enable#enable_http_compression).

Posibles valores: números del 1 al 12. Los niveles superiores a `9` requieren la compilación predeterminada con `libdeflate`; una compilación sin `libdeflate` admite niveles del 1 al 9.

{/*AUTOGENERATED_END*/}