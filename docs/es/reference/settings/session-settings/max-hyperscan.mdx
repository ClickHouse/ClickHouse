---
title: 'ajustes de sesión de max_hyperscan_*'
sidebarTitle: 'max_hyperscan_*'
toc_max_heading_level: 2
description: 'Ajustes de sesión de ClickHouse en el grupo generado max_hyperscan_*.'
doc_type: 'referencia'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/es/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

Estos ajustes están disponibles en [system.settings](/es/reference/system-tables/settings) y se generan automáticamente a partir del [código fuente](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp).

<div id="max_hyperscan_regexp_length">
  ## max_hyperscan_regexp_length
</div>

<SettingsInfoBlock type="UInt64" default_value="0" />

Define la longitud máxima de cada expresión regular en las [funciones hyperscan multi-match](/es/reference/functions/regular-functions/string-search-functions#multiMatchAny).

Valores posibles:

* Un entero positivo.
* 0: la longitud no está limitada.

**Ejemplo**

Consulta:

```sql
SELECT multiMatchAny('abcd', ['ab','bcd','c','d']) SETTINGS max_hyperscan_regexp_length = 3;
```

Resultado:

```text
┌─multiMatchAny('abcd', ['ab', 'bcd', 'c', 'd'])─┐
│                                              1 │
└────────────────────────────────────────────────┘
```

Consulta:

```sql
SELECT multiMatchAny('abcd', ['ab','bcd','c','d']) SETTINGS max_hyperscan_regexp_length = 2;
```

Resultado:

```text
Exception: Regexp length too large.
```

**Véase también**

* [max&#95;hyperscan&#95;regexp&#95;total&#95;length](/es/reference/settings/session-settings/max-hyperscan#max_hyperscan_regexp_total_length)

<div id="max_hyperscan_regexp_total_length">
  ## max_hyperscan_regexp_total_length
</div>

<SettingsInfoBlock type="UInt64" default_value="0" />

Establece la longitud total máxima combinada de todas las expresiones regulares en cada [función hyperscan multi-match](/es/reference/functions/regular-functions/string-search-functions#multiMatchAny).

Posibles valores:

* Entero positivo.
* 0: la longitud no está limitada.

**Ejemplo**

Consulta:

```sql
SELECT multiMatchAny('abcd', ['a','b','c','d']) SETTINGS max_hyperscan_regexp_total_length = 5;
```

Resultado:

```text
┌─multiMatchAny('abcd', ['a', 'b', 'c', 'd'])─┐
│                                           1 │
└─────────────────────────────────────────────┘
```

Consulta:

```sql
SELECT multiMatchAny('abcd', ['ab','bc','c','d']) SETTINGS max_hyperscan_regexp_total_length = 5;
```

Resultado:

```text
Exception: Total regexp lengths too large.
```

**Véase también**

* [max&#95;hyperscan&#95;regexp&#95;length](/es/reference/settings/session-settings/max-hyperscan#max_hyperscan_regexp_length)

{/*AUTOGENERATED_END*/}