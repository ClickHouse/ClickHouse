---
description: 'Tabla del sistema que contiene información sobre el número de eventos que
  se han producido en el sistema.'
keywords: ['tabla del sistema', 'eventos']
slug: /operations/system-tables/events
title: 'system.events'
doc_type: 'reference'
---

import SystemTableCloud from '/snippets/es/_system_table_cloud.mdx';

<SystemTableCloud />

{/*AUTOGENERATED_START*/}

## Descripción

Contiene información sobre la cantidad de eventos que se han producido en el sistema. Por ejemplo, en la tabla se puede ver cuántas consultas `SELECT` se han procesado desde que se inició el servidor de ClickHouse.

## Descripción de eventos

### ACMEAPIRequests

Número de solicitudes emitidas a la API de ACME.

### ACMECertificateOrders

Número de órdenes de certificados ACME emitidas.

### AdaptiveAggregationBucketsRetired

Número de buckets de dos niveles cuya memoria de trabajo (slot de arena, referencias a fragmentos preparados) se retiró inmediatamente después de completarse su fusión y conversión, antes de que terminara la fusión en su totalidad.

### AdaptiveAggregationDrainedRecords

Cuántos registros retrasados volcó la agregación adaptativa en la tabla compartida durante la fusión.

### AdaptiveAggregationGiveUps

Cuántos threads desistieron de la congelación en la agregación adaptativa porque su stream contenía pocas claves distintas.

### AdaptiveAggregationLocalFreezes

Número de tablas hash locales que la agregación adaptativa congeló al alcanzar el threshold de congelación.

### AdaptiveAggregationPressureDrainedRecords

Número de registros preparados que la agregación adaptativa vació de forma anticipada por presión de memoria.

### AdaptiveAggregationPressureStandDowns

Cuántos threads de la agregación adaptativa abandonaron definitivamente la fase de aprendizaje porque la presión de memoria superó el umbral de agregación externa, de modo que sus tablas locales se vuelcan a disco mediante la agregación externa ordinaria.

### AdaptiveAggregationPressureSweeps

Número de veces que la agregación adaptativa vació anticipadamente los registros preparados debido a la presión de memoria.

### AdaptiveAggregationProbeBypasses

Cuántos threads dejaron de sondear su tabla local congelada en la agregación adaptativa porque casi ninguna fila coincidía con ella.

### AdaptiveAggregationResidueReleases

Número de veces que la agregación adaptativa volcó su tabla de drenaje compartida porque un thread que había regresado al algoritmo de referencia estaba a punto de escribir a disco por su causa.

### AdaptiveAggregationSealedChunks

Número de fragmentos fusionados que la agregación adaptativa selló a partir de los lotes de staging almacenados temporalmente en el búfer.

### AdaptiveAggregationSealNormalizations

Cuántas columnas de argumentos preparadas normalizó el sellado de la agregación adaptativa desde una representación envuelta (Const, Replicated, Sparse, LowCardinality) a la forma densa que consume el drenaje.

### AdaptiveAggregationSharedTableSpills

Número de veces que la agregación adaptativa volcó a disco su tabla compartida de vaciado por haber alcanzado el límite de partes bajo presión de memoria.

### AdaptiveAggregationSpillBacklogSheds

Cuántas veces la agregación adaptativa descartó el backlog preparado porque un thread del algoritmo baseline —uno que el deshielo devolvió allí o uno que se retiró por sí mismo— estaba a punto de hacer spill mientras aún quedaban registros preparados residentes en memoria.

### AdaptiveAggregationStagedBytes

Cuántos bytes de claves preparó la agregación adaptativa para el vaciado durante la fusión.

### AdaptiveAggregationStagedChunkPiecesOverBound

Cuántas piezas de un fragmento preparado y recortado publicó la agregación adaptativa que aun así superan el límite que el recorte pretendía respetar. Un fragmento se recorta por los límites de los buckets y, dentro de un bucket que por sí solo supera el límite, registro a registro; por lo tanto, se trata de un único registro cuyos bytes preparados ya superan el límite por sí mismos. Una pieza así se reclama entera, y su vaciado puede rebasar en esa misma medida la parte de presión.

### AdaptiveAggregationStagedChunkSplits

Cuántos fragmentos preparados dividió la agregación adaptativa en los límites de bucket durante la publicación porque se estimó que el drenaje de un fragmento excedía el límite de la parte de presión.

### AdaptiveAggregationStagedClaimsClosedAtBound

Cuántos claims de fragmentos preparados por los drenajes de la agregación adaptativa se cerraron antes de un fragmento cuya incorporación habría llevado el batch al límite de la parte de presión, dejando ese fragmento para el siguiente claim. El límite se aplica al batch tal como se drena, de modo que dos fragmentos que queden justo por debajo de él —dos piezas de un fragmento recortado que superan por un solo registro la mitad de una parte— se drenan en dos tablas en lugar de una; solo un primer fragmento que por sí solo supere el límite se reclama de todos modos.

### AdaptiveAggregationStagedRecords

Cuántos registros diferidos preparó la agregación adaptativa antes de la deduplicación.

### AdaptiveAggregationStagedRecordsMerged

Número de registros preparados que la agregación adaptativa eliminó mediante fusión por tratarse de claves duplicadas, tanto en la publicación como en el sellado.

### AdaptiveAggregationThaws

Número de veces que la agregación adaptativa descongeló las tablas locales porque el stream preparado resultó estar dominado por repeticiones.

### AddressesDiscovered

Número total de nuevas direcciones en los resultados de resolución DNS para conexiones HTTP

### AddressesExpired

Número total de direcciones caducadas que ya no aparecen en los resultados de resolución DNS para las conexiones HTTP

### DireccionesMarcadasComoFallidas

Número total de direcciones marcadas como defectuosas debido a errores de conexión en conexiones HTTP

### AggregatingSortedMilliseconds

Tiempo total empleado durante la agregación de columnas ordenadas

### AggregationBucketTopKConversions

Número de buckets two-level convertidos mediante la selección Top-K local del bucket (la optimization del plan aggregationBucketTopK).

### Agregación convertida a dos niveles

Número de veces que una tabla hash de agregación de un solo nivel se convirtió en una de dos niveles en tiempo de ejecución.

### AggregationHashTablesInitializedAsTwoLevel

Cuántas tablas hash se inicializaron con dos niveles para la agregación.

### AggregationOptimizedEqualRangesOfKeys

Para cuántos bloques se aplicó la optimización de rangos de claves iguales

### AggregationPreallocatedElementsInHashTables

Cantidad de elementos preasignados en las tablas hash para la agregación.

### AggregationTopKHeapsFrozen

Cuántos heaps de agregación top-K se congelaron, recurriendo a la agregación normal. O bien el heap no descartó casi nada dentro de su ventana de observación (por ejemplo, el número de grouping keys distintas no supera el LIMIT), o un conjunto de empates en el boundary del heap —que nunca puede ser expulsado— lo hizo crecer en exceso (véase `enable_group_by_top_k_optimization`).

### AggregationTopKKeysEvicted

Cuántas grouping keys se expulsaron del heap top-K acotado durante la agregación (véase `enable_group_by_top_k_optimization`).

### AggregationTopKKeysPruned

Cuántas grouping keys desalojadas se borraron también de la tabla hash intermedia, destruyendo sus aggregate states (véase `enable_group_by_top_k_optimization`). Es menor que `AggregationTopKKeysEvicted` cuando el método de agregación no puede borrar keys, o cuando solo se clasifica un prefix de la key: en ese caso el heap sigue omitiendo filas, pero la tabla hash conserva todos los grupos admitidos.

### AggregationTopKRowsSkipped

Número de filas omitidas durante la agregación porque su clave de agrupamiento no logró entrar en el resultado top-K (consulte `enable_group_by_top_k_optimization`).

### AIAPICalls

Número de solicitudes HTTP enviadas a proveedores de IA.

### AIInputTokens

Número total de tokens del prompt consumidos en todas las llamadas a funciones de IA de la consulta.

### AIORead

Número de lecturas mediante la interfaz AIO de Linux o FreeBSD

### AIOReadBytes

Número de bytes leídos mediante la interfaz AIO de Linux o FreeBSD

### AIOutputTokens

Número total de tokens de salida consumidos en todas las llamadas a funciones de IA de la consulta.

### AIOWrite

Número de operaciones de escritura con la interfaz AIO de Linux o FreeBSD

### AIOWriteBytes

Número de bytes escritos mediante la interfaz AIO de Linux o FreeBSD

### Filas procesadas por IA

Número de filas que obtuvieron un resultado de IA.

### AIRowsSkipped

Número de filas que recibieron un valor por defecto debido a una cuota o a un error.

### AllUsersThrottlerBytes

Bytes que pasaron por el limitador &#39;max&#95;network&#95;bandwidth&#95;for&#95;all&#95;users&#39;.

### AllUsersThrottlerSleepMicroseconds

Tiempo total durante el que una consulta estuvo en espera para ajustarse a la limitación de ancho de banda de &#39;max&#95;network&#95;bandwidth&#95;for&#95;all&#95;users&#39;.

### AnalyzePatchRangesMicroseconds

Tiempo total empleado en analizar el índice de las partes de parche

### ApplyPatchesMicroseconds

Tiempo total dedicado a aplicar partes de parche a bloques

### ApplyPatchMergeOnKeyMicroseconds

Tiempo total empleado en los bucles de fusión por clave de ordenación de applyPatchesMergeOnKey

### ArenaAllocBytes

Número de bytes asignados para la Arena de memoria (usada para GROUP BY y operaciones similares)

### ArenaAllocChunks

Número de fragmentos asignados al área de memoria Arena (utilizada para GROUP BY y operaciones similares)

### ASTFuzzerOracleChecks

Número de comprobaciones de oráculo intentadas por el AST fuzzer del lado del servidor.

### ASTFuzzerOracleMismatches

Número de discrepancias de oráculo detectadas por el AST fuzzer del lado del servidor.

### ASTFuzzerOracleTLPAggregateChecks

Número de comprobaciones del oráculo TLP Aggregate (State/Merge) intentadas por el AST fuzzer del lado del servidor.

### ASTFuzzerQueries

Número de consultas aleatorias intentadas por el AST fuzzer del lado del servidor.

### ASTFuzzerSkippedBackupRestore

Número de consultas BACKUP/RESTORE generadas por el fuzzer que el AST fuzzer del lado del servidor omitió en lugar de ejecutar.

### ASTFuzzerSkippedReplicatedDDLInternal

Número de veces que el AST fuzzer del lado del servidor omitió el fuzzing porque había en curso, en el contexto, una ejecución interna de DDL de base de datos replicada (una ZooKeeperMetadataTransaction activa).

### AsynchronousReaderIgnoredBytes

Número de bytes ignorados durante la lectura asíncrona

### AsynchronousReadWaitMicroseconds

Tiempo dedicado a esperar las lecturas asíncronas en la lectura local asíncrona.

### AsynchronousRemoteReadWaitMicroseconds

Tiempo empleado en esperar lecturas remotas asíncronas.

### AsyncInsertBytes

Tamaño de los datos, en bytes, de las consultas INSERT asíncronas.

### AsyncInsertCacheHits

Número de veces que se ha encontrado un identificador hash duplicado en la caché de identificadores hash de INSERT asíncronos.

### AsyncInsertFlush

Número de flushes de la cola de INSERT asíncrono. Cada flush inserta un batch de consultas de INSERT asíncrono como una sola parte. Compárelo con AsyncInsertQuery para ver cuántas consultas se agrupan en un batch en promedio.

### AsyncInsertQuery

Igual que InsertQuery, pero solo para consultas INSERT asíncronas.

### AsyncInsertRows

Número de filas insertadas mediante consultas INSERT asíncronas.

### AsyncLoaderSpawnFailures

Número de veces que el cargador asíncrono no pudo crear un worker porque el global thread pool no pudo proporcionar un thread. En ese caso, los jobs en cola los ejecuta un worker que el grupo ya tiene: uno que está en ejecución o uno que se reanuda tras esperar a un job de otro grupo.

### AsyncLoaderWaitMicroseconds

Tiempo total que una consulta estuvo esperando los trabajos del cargador asíncrono.

### AsyncLoggingConsoleDroppedMessages

Cuántos mensajes se descartaron del registro de la consola porque la cola de registro asíncrono estaba llena

### AsyncLoggingConsoleTotalMessages

Cuántos mensajes (aceptados o descartados) se han enviado a la cola asíncrona del log de la consola

### AsyncLoggingErrorFileLogDroppedMessages

Número de mensajes descartados del archivo de registro de errores debido a que la cola de log asíncrono está llena

### AsyncLoggingErrorFileLogTotalMessages

Cuántos mensajes (aceptados o descartados) se han enviado a la cola asíncrona del registro de errores en archivo

### AsyncLoggingFileLogDroppedMessages

Cantidad de mensajes que se han descartado del archivo de registro porque la cola de registros asíncronos estaba llena

### AsyncLoggingFileLogTotalMessages

Cuántos mensajes (aceptados o descartados) se han enviado a la cola asíncrona del registro en archivo

### AsyncLoggingSyslogDroppedMessages

Cuántos mensajes se han descartado en syslog porque la cola de registro asíncrono está llena

### AsyncLoggingSyslogTotalMessages

Cantidad de mensajes (aceptados o descartados) que se han enviado a la cola asíncrona del syslog

### AsyncLoggingTextLogDroppedMessages

Cuántos mensajes se han descartado de text&#95;log porque la cola de logs asíncrona estaba llena

### AsyncLoggingTextLogTotalMessages

Número de mensajes (aceptados o descartados) que se han enviado a la cola asíncrona de text&#95;log

### AzureCommitBlockList

Número de llamadas a CommitBlockList de la API de Azure Blob Storage

### AzureCopyObject

Número de llamadas a CopyObject de la API de Azure Blob Storage

### AzureCreateContainer

Número de llamadas a CreateContainer de la API de Azure Blob Storage.

### AzureDeleteObjects

Número de llamadas a DeleteObject(s) de la API de Azure Blob Storage.

### AzureGetObject

Número de llamadas a GetObject de la API de Azure.

### AzureGetProperties

Número de llamadas a GetProperties de la API de Azure Blob Storage.

### AzureGetRequestThrottlerBlocked

Número de solicitudes GET de Azure bloqueadas por el limitador.

### AzureGetRequestThrottlerCount

Número de solicitudes GET de Azure que pasaron por el limitador: bloqueadas y no bloqueadas.

### AzureGetRequestThrottlerSleepMicroseconds

Tiempo total que una consulta permaneció en espera para ajustarse a la limitación de solicitudes GET de Azure.

### AzureListObjects

Número de llamadas a ListObjects de la API de Azure Blob Storage.

### AzurePutRequestThrottlerBlocked

Número de solicitudes PUT de Azure bloqueadas por el limitador.

### AzurePutRequestThrottlerCount

Número de solicitudes PUT de Azure que pasaron por el limitador: bloqueadas y no bloqueadas.

### AzurePutRequestThrottlerSleepMicroseconds

Tiempo total durante el que una consulta permaneció en espera para ajustarse a la limitación de solicitudes PUT de Azure.

### AzureReadMicroseconds

Tiempo total de espera de las solicitudes de lectura de Azure.

### AzureReadRequestsCount

Número de solicitudes de lectura en Azure.

### AzureReadRequestsErrors

Número de errores en las solicitudes de lectura de Azure.

### AzureReadRequestsRedirects

Número de redirecciones de solicitudes de lectura de Azure.

### AzureReadRequestsThrottling

Número de solicitudes de lectura de Azure limitadas por tasa.

### AzureStageBlock

Número de llamadas a StageBlock de la API de Azure Blob Storage

### AzureUpload

Número de llamadas de tipo Upload a la API de Azure blob storage

### AzureWriteMicroseconds

Tiempo total de espera de las solicitudes de escritura de Azure.

### AzureWriteRequestsCount

Número de solicitudes de escritura en Azure.

### AzureWriteRequestsErrors

Número de errores en las solicitudes de escritura de Azure.

### AzureWriteRequestsRedirects

Número de redirecciones de solicitudes de escritura en Azure.

### AzureWriteRequestsThrottling

Número de solicitudes de escritura de Azure con limitación de tasa.

### BackgroundLoadingMarksTasks

Número de tareas en segundo plano para la carga de marcas

### BackupEntriesCollectorForTablesDataMicroseconds

Tiempo invertido en crear entradas de copia de seguridad para los datos de las tablas

### BackupEntriesCollectorMicroseconds

Tiempo dedicado a crear entradas de copia de seguridad

### BackupEntriesCollectorRunPostTasksMicroseconds

Tiempo dedicado a ejecutar tareas posteriores tras crear entradas de copia de seguridad

### BackupLockFileReads

Cuántas veces se leyó el archivo &#39;.lock&#39; al crear la copia de seguridad

### BackupPreparingFileInfosMicroseconds

Tiempo dedicado a preparar la información de archivos para las entradas de copia de seguridad

### BackupReadLocalBytesToCalculateChecksums

Tamaño total de los archivos leídos localmente para calcular las sumas de verificación de las entradas de copia de seguridad

### BackupReadLocalFilesToCalculateChecksums

Número de archivos leídos localmente para calcular sumas de comprobación de las entradas de la copia de seguridad

### BackupReadMetadataMicroseconds

Tiempo empleado en leer los metadatos de la copia de seguridad desde el archivo .backup

### BackupReadRemoteBytesToCalculateChecksums

Tamaño total de los archivos leídos desde discos remotos para calcular las sumas de comprobación de las entradas de copia de seguridad

### Archivos remotos leídos para calcular sumas de comprobación de la copia de seguridad

Número de archivos leídos desde discos remotos para calcular sumas de comprobación de las entradas de copia de seguridad

### BackupsOpenedForRead

Número de copias de seguridad abiertas para su lectura

### BackupsOpenedForUnlock

Número de copias de seguridad abiertas para desbloquear

### BackupsOpenedForWrite

Número de copias de seguridad abiertas en modo de escritura

### BackupThrottlerBytes

Bytes que pasaron por el limitador de velocidad &#39;max&#95;backup&#95;bandwidth&#95;for&#95;server&#39;.

### BackupThrottlerSleepMicroseconds

Tiempo total que una consulta estuvo en pausa para respetar la limitación de velocidad de `max_backup_bandwidth_for_server`.

### BackupWriteMetadataMicroseconds

Tiempo dedicado a escribir los metadatos de la copia de seguridad en el archivo .backup

### BlobCopierThreadLockBlobsErrors

Número de errores al bloquear blobs durante la ejecución de BlobCopier

### BlobCopierThreadLockedBlobs

Número de blobs devueltos por el almacenamiento de metadatos

### BlobCopierThreadRecordBlobsErrors

Número de errores al registrar blobs ocurridos durante la ejecución de BlobCopier

### BlobCopierThreadRecordedBlobs

Número de blobs cuya replicación mediante BlobCopier se registró en el almacenamiento de metadatos

### BlobCopierThreadReplicateBlobsErrors

Número de errores de replicación de blobs producidos durante la ejecución de BlobCopier

### BlobCopierThreadReplicatedBlobs

Número de blobs replicados por BlobCopier

### BlobCopierThreadRuns

Número de ejecuciones del hilo BlobCopier

### BlobKillerThreadLockBlobsErrors

Número de errores de bloqueo de blobs que se produjeron durante la ejecución de BlobKiller

### BlobKillerThreadLockedBlobs

Número de blobs devueltos por el almacenamiento de metadatos

### BlobKillerThreadRecordBlobsErrors

Número de errores producidos al registrar blobs durante la ejecución de BlobKiller

### BlobKillerThreadRecordedBlobs

Número de blobs cuya eliminación por parte de BlobKiller se registró en el almacenamiento de metadatos

### BlobKillerThreadRemoveBlobsErrors

Número de errores al eliminar blobs durante la ejecución de BlobKiller

### BlobKillerThreadRemovedBlobs

Número de blobs eliminados por BlobKiller

### BlobKillerThreadRemoveTasks

Número de tareas de eliminación creadas por BlobKiller

### BlobKillerThreadRuns

Número de ejecuciones del hilo BlobKiller

### BuildPatchesJoinMicroseconds

Tiempo total invertido en construir índices y tablas hash para aplicar partes de parche en modo Join

### BuildPatchesMergeMicroseconds

Tiempo total dedicado a crear índices para aplicar partes de parche en modo Merge

### CachedReadBufferCacheWriteBytes

Bytes escritos desde la fuente (sistema de archivos remoto, etc.) hacia la caché del sistema de archivos

### CachedReadBufferCacheWriteMicroseconds

Tiempo empleado en escribir datos en la caché del sistema de archivos

### CachedReadBufferCacheWriteStopped

Número de veces que la escritura de un file segment en la caché del sistema de archivos se detuvo a mitad de la descarga (falló la reserva de espacio o la escritura en la caché), tras lo cual la lectura continúa sin pasar por la caché

### CachedReadBufferCreateBufferMicroseconds

Tiempo de preparación del búfer

### CachedReadBufferPredownloadedBytes

Bytes leídos desde el origen de la caché del sistema de archivos. Los segmentos de la caché se leen de izquierda a derecha en su totalidad; es posible que necesitemos predescargar alguna parte del segmento que no sea relevante para la tarea actual solo para llegar a los datos necesarios

### CachedReadBufferPredownloadedFromSourceBytes

Bytes leídos del origen de la caché del sistema de archivos para la predescarga (desde remote fs, etc.)

### CachedReadBufferPredownloadedFromSourceMicroseconds

Tiempo de lectura desde el origen de la caché del sistema de archivos durante la predescarga (desde el sistema de archivos remoto, etc.)

### CachedReadBufferReadFromCacheBytes

Bytes leídos de la caché del sistema de archivos

### CachedReadBufferReadFromCacheHits

Número de veces que una lectura desde la caché del sistema de archivos produjo un acierto de caché.

### CachedReadBufferReadFromCacheMicroseconds

Tiempo de lectura desde la caché del sistema de archivos

### CachedReadBufferReadFromCacheMisses

Número de veces que una lectura desde la caché del sistema de archivos no encontró los datos en la caché.

### CachedReadBufferReadFromSourceBytes

Bytes leídos del origen de la caché del sistema de archivos (del sistema de archivos remoto, etc.)

### CachedReadBufferReadFromSourceMicroseconds

Tiempo de lectura desde la fuente de la caché del sistema de archivos (sistema de archivos remoto, etc.)

### CachedReadBufferWaitReadBufferMicroseconds

Tiempo empleado en esperar al búfer de lectura interno (incluida la espera de la caché)

### CachedWriteBufferCacheWriteBytes

Bytes escritos desde el origen (remote fs, etc.) en la caché del sistema de archivos

### CachedWriteBufferCacheWriteMicroseconds

Tiempo empleado en escribir datos en la caché del sistema de archivos

### CachedWriteBufferCacheWriteStopped

Número de veces que se detuvo el almacenamiento en caché de escritura directa (porque falló la reserva de espacio o la escritura en la cache, o porque se estaba desalojando un segmento que la abarcaba), tras lo cual la escritura continúa sin poblar la cache

### CachedWriteBufferCoveringSegmentShrinkFailed

Número de veces que el almacenamiento en caché de escritura directa (write-through) tuvo que detenerse porque un file segment que abarcaba el rango y quedaba por detrás del offset en el que se estaba escribiendo no pudo reducirse (lo retiene otro proceso, por lo que no es posible resolver el hueco que contiene) o fue expulsado de forma concurrente

### CachedWriteBufferCoveringSegmentShrunk

Número de veces que el almacenamiento en caché de escritura directa (write-through) redujo un file segment que lo abarcaba y que quedaba por detrás del offset en el que se estaba escribiendo (los rangos intermedios se enviaron a otro servidor de distributed cache), con el fin de continuar el almacenamiento en caché en un nuevo file segment

### CacheWarmerBytesDownloaded

Cantidad de datos descargados a la caché del sistema de archivos por hilos dedicados en segundo plano.

### CacheWarmerDataPartsDownloaded

Número de partes de datos que CacheWarmer descargó por completo.

### CannotRemoveEphemeralNode

Número de veces que se produjo un error al intentar eliminar un nodo efímero. Esto no supone ningún problema, ya que nuestra implementación de la biblioteca ZooKeeper garantiza que la sesión expirará y que el nodo se eliminará.

### CannotWriteToWriteBufferDiscard

Número de trazas de pila descartadas por el perfilador de consultas o el manejador de señales porque la tubería está llena o no se puede escribir en ella.

### CoalescingSortedMilliseconds

Tiempo total empleado en fusionar columnas ordenadas

### CollapsingSortedMilliseconds

Tiempo total empleado en el colapso de columnas ordenadas

### CommonBackgroundExecutorTaskCancelMicroseconds

Tiempo dedicado a cancel() en las tareas del ejecutor Common.

### CommonBackgroundExecutorTaskExecuteStepMicroseconds

Tiempo dedicado a executeStep() en las tareas del ejecutor Common.

### CommonBackgroundExecutorTaskResetMicroseconds

Tiempo dedicado a restablecer la tarea del ejecutor Common.

### CommonBackgroundExecutorWaitMicroseconds

Tiempo empleado en esperar a que finalice el ejecutor «Common».

### CompiledFunctionExecute

Número de veces que se ejecutó una función compilada.

### CompileExpressionsBytes

Número de bytes utilizados para compilar expresiones.

### CompileExpressionsMicroseconds

Tiempo total dedicado a compilar expresiones a código LLVM.

### CompileFunction

Número de veces que se inició la compilación del código LLVM generado (para crear una función fusionada para expresiones complejas).

### CompileRegexpFunction

Número de veces que una expresión regular se compiló mediante JIT a código máquina.

### CompressedReadBufferBlocks

Número de bloques comprimidos (bloques de datos que se comprimen de forma independiente entre sí) leídos de fuentes comprimidas (archivos, red).

### CompressedReadBufferBytes

Número de bytes no comprimidos (es decir, el número de bytes después de la descompresión) leídos desde fuentes comprimidas (archivos, red).

### CompressedReadBufferChecksumDoesntMatch

Número de veces que el checksum del bloque comprimido no coincidió.

### CompressedReadBufferChecksumDoesntMatchMicroseconds

Tiempo total dedicado a detectar cambios de bit causados por discrepancias en la suma de comprobación de bloques comprimidos.

### CompressedReadBufferChecksumDoesntMatchSingleBitMismatch

Número de veces que una discrepancia en la suma de comprobación de un bloque comprimido se debió a una diferencia de un solo bit.

### ConcurrencyControlDownscales

Número total de eventos de reducción de CPU

### ConcurrencyControlPreemptedMicroseconds

Tiempo total que una consulta estuvo esperando debido al desalojo de slots de CPU.

### ConcurrencyControlPreemptions

Número total de preempciones de CPU

### ConcurrencyControlQueriesDelayed

Número de consultas retrasadas por la presión sobre la capacidad de los slots de CPU. Indicador de contención: excluye las esperas por aumento diferido de slots en un servidor sin contención.

### ConcurrencyControlSlotsAcquired

Número total de slots de CPU obtenidos

### ConcurrencyControlSlotsAcquiredNonCompeting

Número total de slots de CPU no competitivos adquiridos

### ConcurrencyControlSlotsDelayed

Número de slots de CPU que no se concedieron en el momento de la asignación debido a limitaciones de capacidad. Excluye los slots retenidos por la asignación diferida.

### ConcurrencyControlSlotsGranted

Número de slots de CPU concedidos según la garantía de 1 hilo por consulta y para las consultas con la configuración &#39;use&#95;concurrency&#95;control&#39; = 0

### ConcurrencyControlUpscales

Número total de eventos de escalado al alza de la CPU

### ConcurrencyControlWaitMicroseconds

Tiempo total que una consulta estuvo esperando solicitudes de recursos para obtener CPU slots.

### ConcurrentQuerySlotsAcquired

Número total de slots de consulta adquiridos

### ConcurrentQueryWaitMicroseconds

Tiempo total que una consulta estuvo esperando a que hubiera slots de consulta disponibles

### ConnectionPoolIsFullMicroseconds

Tiempo total empleado en esperar un slot en el pool de conexiones.

### ContextLock

Número de veces que se adquirió el bloqueo de Context o que se intentó adquirirlo. Se trata de un bloqueo global.

### ContextLockWaitMicroseconds

Tiempo de espera del bloqueo de Context en microsegundos

### CoordinatedMergesMergeAssignmentRequest

Número total de solicitudes de asignación de fusiones

### CoordinatedMergesMergeAssignmentRequestMicroseconds

Tiempo total empleado por el cliente de asignación de fusiones

### CoordinatedMergesMergeAssignmentResponse

Número total de solicitudes para la asignación de fusiones

### CoordinatedMergesMergeAssignmentResponseMicroseconds

Tiempo total empleado por el controlador de asignación de fusiones

### CoordinatedMergesMergeCoordinatorFetchMetadataMicroseconds

Tiempo total dedicado a obtener metadatos actualizados dentro del coordinador de fusiones

### CoordinatedMergesMergeCoordinatorFilterMicroseconds

Tiempo total dedicado a filtrar las fusiones preparadas en el coordinador de fusiones

### CoordinatedMergesMergeCoordinatorLockStateExclusivelyCount

Número total de adquisiciones exclusivas del bloqueo del estado del coordinador

### CoordinatedMergesMergeCoordinatorLockStateExclusivelyMicroseconds

Tiempo total dedicado al bloqueo exclusivo del mutex del estado del coordinador

### CoordinatedMergesMergeCoordinatorLockStateForShareCount

Número total de adquisiciones FOR SHARE del bloqueo de estado del coordinador

### CoordinatedMergesMergeCoordinatorLockStateForShareMicroseconds

Tiempo total dedicado al bloqueo del mutex del estado del coordinador en modo compartido

### CoordinatedMergesMergeCoordinatorSelectMergesMicroseconds

Tiempo total empleado en encontrar un merge mediante selectores de merge dentro del coordinador de fusiones

### CoordinatedMergesMergeCoordinatorUpdateCount

Número total de actualizaciones del coordinador de fusiones

### CoordinatedMergesMergeCoordinatorUpdateMicroseconds

Tiempo total dedicado a actualizar el estado del coordinador de fusiones

### CoordinatedMergesMergeWorkerUpdateCount

Número total de actualizaciones del merge worker

### CoordinatedMergesMergeWorkerUpdateMicroseconds

Tiempo total dedicado a actualizar el estado local de las fusiones asignadas al worker

### CreatedLogEntryForMerge

Se creó correctamente una entrada de registro para fusionar partes en ReplicatedMergeTree.

### CreatedLogEntryForMutation

Se creó correctamente una entrada de registro para mutar partes en ReplicatedMergeTree.

### CreatedReadBufferDirectIO

Número de veces que se creó un búfer de lectura con O&#95;DIRECT para la lectura de datos (al elegir entre otros métodos de lectura).

### CreatedReadBufferDirectIOFailed

Número de veces que se intentó crear un búfer de lectura con O&#95;DIRECT para leer datos (al elegir entre otros métodos de lectura), pero el SO no lo permitió (debido a la falta de compatibilidad del filesystem u otras razones) y se volvió al método de lectura habitual.

### CreatedReadBufferMMap

Número de veces que se creó un búfer de lectura con &#39;mmap&#39; para leer datos (al elegir entre otros métodos de lectura).

### CreatedReadBufferMMapFailed

Número de veces que se intentó crear un búfer de lectura con &#39;mmap&#39; para leer datos (al elegir entre otros métodos de lectura), pero el SO no lo permitió (por falta de compatibilidad con el filesystem u otras razones) y se recurrió al método de lectura habitual.

### CreatedReadBufferOrdinary

Número de veces que se creó un búfer de lectura ordinario para leer datos (al escoger entre otros métodos de lectura).

### DataAfterMergeDiffersFromReplica

Número de veces que los datos tras la fusión no son idénticos byte a byte a los datos de otras réplicas. Puede haber varias razones:

1. Uso de una versión más reciente de la biblioteca de compresión tras una actualización del servidor.
2. Uso de otro método de compresión.
3. Algoritmo de compresión no determinista (muy poco probable).
4. Algoritmo de fusión no determinista debido a un error lógico en el código.
5. Corrupción de datos en memoria debido a un error en el código.
6. Corrupción de datos en memoria debido a un problema de hardware.
7. Modificación manual de los datos de origen después del inicio del servidor.
8. Modificación manual de las sumas de verificación almacenadas en ZooKeeper.
9. La configuración relacionada con el formato de las partes, como &#39;enable&#95;mixed&#95;granularity&#95;parts&#39;, es distinta entre réplicas.
   El servidor detectó esta situación correctamente y descargará la parte fusionada desde la réplica para garantizar un resultado idéntico byte a byte.

### DataAfterMutationDiffersFromReplica

Número de veces que los datos tras una mutación no son idénticos byte a byte a los datos de otras réplicas. Además de las razones descritas en &#39;DataAfterMergeDiffersFromReplica&#39;, esto también puede deberse a una mutación no determinista.

### DefaultImplementationForNullsRows

Número de filas procesadas por la implementación predeterminada para valores NULL durante la ejecución de funciones

### DefaultImplementationForNullsRowsWithNulls

Número de filas que contienen valores NULL procesadas por la implementación predeterminada para NULL durante la ejecución de funciones

### DelayedInserts

Número de veces que el INSERT de un bloque en una tabla MergeTree se limitó debido al elevado número de partes de datos activas en la partición.

### DelayedInsertsMilliseconds

Número total de milisegundos empleados mientras el INSERT de un bloque en una tabla MergeTree se vio limitado debido al elevado número de partes de datos activas en la partición.

### DelayedMutations

Número de veces que la mutación de una tabla MergeTree se vio limitada debido al elevado número de mutaciones sin finalizar en la tabla.

### DelayedMutationsMilliseconds

Número total de milisegundos transcurridos mientras la mutación de una tabla MergeTree estuvo limitada debido al elevado número de mutaciones sin finalizar en la tabla.

### DeltaLakeDeltaLogExistenceChecks

Número de enumeraciones en el almacenamiento de objetos realizadas para comprobar si existe un `_delta_log` de DeltaLake (por ejemplo, durante CREATE TABLE).

### DeltaLakePartitionPrunedFiles

Número de archivos omitidos durante la poda de particiones en DeltaLake

### DeltaLakeScannedFiles

Número de archivos escaneados durante los callbacks de escaneo de DeltaLake

### DeltaLakeSnapshotInitializations

Número de veces que se inicializó una instantánea de una tabla de DeltaLake (cargada desde el almacenamiento de objetos)

### DictCacheKeysExpired

Número de claves consultadas en diccionarios de tipo &#39;cache&#39; que se encontraron en la caché, pero estaban obsoletas.

### DictCacheKeysHit

Número de claves buscadas en diccionarios de tipo &#39;cache&#39; y encontradas en la caché.

### DictCacheKeysNotFound

Número de claves buscadas en diccionarios de tipo &#39;cache&#39; que no se encontraron.

### DictCacheKeysRequested

Número de claves solicitadas desde la fuente de datos para los diccionarios de tipo &#39;cache&#39;.

### DictCacheKeysRequestedFound

Número de claves solicitadas a la fuente de datos para diccionarios de tipo &#39;cache&#39; y encontradas en ella.

### DictCacheKeysRequestedMiss

Número de claves solicitadas a la fuente de datos para diccionarios de tipo &#39;cache&#39; que no se encontraron en dicha fuente.

### DictCacheLockReadNs

Número de nanosegundos empleados en esperar el bloqueo de lectura para consultar los datos de los diccionarios de tipo &#39;cache&#39;.

### DictCacheLockWriteNs

Número de nanosegundos dedicados a esperar el bloqueo de escritura para actualizar los datos de los diccionarios de tipo &#39;cache&#39;.

### DictCacheRequests

Número de solicitudes en bloque a las fuentes de datos externas para los diccionarios de tipo &#39;cache&#39;.

### DictCacheRequestTimeNs

Número de nanosegundos empleados en consultar las fuentes de datos externas para los diccionarios de tipo &#39;cache&#39;.

### DirectorySync

Número de veces que se llamó a la función F&#95;FULLFSYNC/fsync/fdatasync en directorios.

### DirectorySyncElapsedMicroseconds

Tiempo total empleado en esperar la llamada al sistema F&#95;FULLFSYNC/fsync/fdatasync para directorios.

### DiskAzureCommitBlockList

Número de llamadas a CommitBlockList en la API de Azure Blob Storage de Disk Azure

### DiskAzureCopyObject

Número de llamadas a CopyObject de la API de Azure Blob Storage de Disk Azure

### DiskAzureCreateContainer

Número de llamadas a CreateContainer en la API de Azure Blob Storage de Disk Azure.

### DiskAzureDeleteObjects

Número de llamadas a DeleteObject(s) de la API de Azure Blob Storage.

### DiskAzureGetObject

Número de llamadas a la API GetObject de Disk Azure.

### DiskAzureGetProperties

Número de llamadas a GetProperties de la API de Azure Blob Storage de Disk Azure.

### DiskAzureGetRequestThrottlerBlocked

Número de solicitudes GET al disco de Azure bloqueadas por el limitador de tasa.

### DiskAzureGetRequestThrottlerCount

Número de solicitudes GET al disco de Azure que pasaron por el limitador de tasa: bloqueadas y no bloqueadas.

### DiskAzureGetRequestThrottlerSleepMicroseconds

Tiempo total que una consulta permaneció en espera para cumplir con la limitación de solicitudes GET del disco de Azure.

### DiskAzureListObjects

Número de llamadas a ListObjects de la API de Azure Blob Storage de Disk Azure.

### DiskAzurePutRequestThrottlerBlocked

Número de solicitudes PUT al disco de Azure bloqueadas por el limitador de tasa.

### DiskAzurePutRequestThrottlerCount

Número de solicitudes PUT al disco de Azure que pasaron por el limitador de tasa: bloqueadas y no bloqueadas.

### DiskAzurePutRequestThrottlerSleepMicroseconds

Tiempo total que una consulta permaneció en espera para ajustarse a la limitación de tasa de solicitudes PUT del disco de Azure.

### DiskAzureReadMicroseconds

Tiempo total de espera de las solicitudes de lectura del disco de Azure.

### DiskAzureReadRequestsCount

Número de solicitudes de lectura del disco de Azure.

### DiskAzureReadRequestsErrors

Número de errores en las solicitudes de lectura del disco de Azure.

### DiskAzureReadRequestsRedirects

Número de redireccionamientos de solicitudes de lectura del disco de Azure.

### DiskAzureReadRequestsThrottling

Número de solicitudes de lectura del disco de Azure limitadas por tasa.

### DiskAzureStageBlock

Número de llamadas a StageBlock de la API de Azure Blob Storage de Disk Azure

### DiskAzureUpload

Número de llamadas de carga a la API de Azure Blob Storage de Disk Azure

### DiskAzureWriteMicroseconds

Tiempo total de espera de las solicitudes de escritura en disco de Azure.

### DiskAzureWriteRequestsCount

Número de solicitudes de escritura de disco de Azure.

### DiskAzureWriteRequestsErrors

Número de errores en las solicitudes de escritura del disco de Azure.

### DiskAzureWriteRequestsRedirects

Número de redireccionamientos de solicitudes de escritura en disco de Azure.

### Limitación de solicitudes de escritura en disco de Azure

Número de solicitudes de escritura en disco de Azure sujetas a limitación de tasa.

### DiskConnectionsCreated

Número de conexiones creadas del disco

### DiskConnectionsElapsedMicroseconds

Tiempo total dedicado a crear conexiones para el disco

### DiskConnectionsErrors

Número de veces que falla la creación de una conexión para el disco

### DiskConnectionsExpired

Número de conexiones expiradas del disco

### DiskConnectionsPreserved

Número de conexiones preservadas del disco

### DiskConnectionsReset

Número de conexiones reiniciadas del disco

### DiskConnectionsReused

Número de conexiones reutilizadas del disco

### DiskObjectStorageWaitBlobRemovalMicroseconds

Tiempo de espera hasta que se complete la eliminación pendiente del blob tras confirmar la transacción de metadatos

### DiskPlainRewritableAzureDirectoryCreated

Número de directorios creados por el almacenamiento de metadatos &#39;plain&#95;rewritable&#39; de AzureObjectStorage.

### DiskPlainRewritableAzureDirectoryRemoved

Número de directorios eliminados por el almacenamiento de metadatos &#39;plain&#95;rewritable&#39; de AzureObjectStorage.

### DiskPlainRewritableLegacyLayoutDiskCount

Número de discos `plain_rewritable` con diseño heredado.

### DiskPlainRewritableLocalDirectoryCreated

Número de directorios creados por el almacenamiento de metadatos &#39;plain&#95;rewritable&#39; para LocalObjectStorage.

### DiskPlainRewritableLocalDirectoryRemoved

Número de directorios eliminados por el almacenamiento de metadatos &#39;plain&#95;rewritable&#39; de LocalObjectStorage.

### DiskPlainRewritableS3DirectoryCreated

Número de directorios creados por el almacenamiento de metadatos &#39;plain&#95;rewritable&#39; para S3ObjectStorage.

### DiskPlainRewritableS3DirectoryRemoved

Número de directorios eliminados por el almacenamiento de metadatos &#39;plain&#95;rewritable&#39; en S3ObjectStorage.

### DiskPlainRewritableUndoStageRetries

Número de veces que hubo que repetir un step de reversión de una transaction de metadato &#39;plain&#95;rewritable&#39; fallida porque el almacenamiento de objetos la rechazó.

### DiskReadElapsedMicroseconds

Tiempo total de espera de la llamada al sistema de lectura. Incluye lecturas desde la caché de páginas.

### DiskS3AbortMultipartUpload

Número de llamadas a AbortMultipartUpload de la API de DiskS3.

### DiskS3CompleteMultipartUpload

Número de llamadas a la API DiskS3 CompleteMultipartUpload.

### DiskS3CopyObject

Número de llamadas a CopyObject de la API de DiskS3.

### DiskS3CreateMultipartUpload

Número de llamadas a la API de DiskS3 CreateMultipartUpload.

### DiskS3DeleteObjects

Número de llamadas a DeleteObject de la API de DiskS3.

### DiskS3GetObject

Número de llamadas a GetObject de la API de DiskS3.

### DiskS3GetObjectTagging

Número de llamadas a la API de DiskS3 GetObjectTagging.

### DiskS3GetRequestThrottlerBlocked

Número de solicitudes GET y SELECT de DiskS3 bloqueadas por el limitador de tasa.

### DiskS3GetRequestThrottlerCount

Número de solicitudes GET y SELECT de DiskS3 que pasaron por el limitador de tasa: bloqueadas y no bloqueadas.

### DiskS3GetRequestThrottlerSleepMicroseconds

Tiempo total durante el que una consulta permaneció en espera para ajustarse a la limitación de tasa de las solicitudes GET y SELECT de DiskS3.

### DiskS3HeadObject

Número de llamadas a HeadObject de la API de DiskS3.

### DiskS3ListObjects

Número de llamadas a ListObjects de la API de DiskS3.

### DiskS3PutObject

Número de llamadas a PutObject de la API de DiskS3.

### DiskS3PutRequestThrottlerBlocked

Número de solicitudes PUT, COPY, POST y LIST de DiskS3 bloqueadas por el limitador de tasa.

### DiskS3PutRequestThrottlerCount

Número de solicitudes PUT, COPY, POST y LIST de DiskS3 que pasaron por el throttler: bloqueadas y no bloqueadas.

### DiskS3PutRequestThrottlerSleepMicroseconds

Tiempo total que una consulta permaneció en espera para ajustarse a la limitación de tasa de las solicitudes PUT, COPY, POST y LIST de DiskS3.

### DiskS3ReadMicroseconds

Tiempo de las solicitudes GET y HEAD al almacenamiento de DiskS3.

### DiskS3ReadRequestAttempts

Número de intentos de solicitudes GET y HEAD al almacenamiento de DiskS3, incluido el intento inicial y cualquier reintento, pero sin contar los reintentos realizados internamente por la estrategia de reintento de S3

### DiskS3ReadRequestRetryableErrors

Número de errores recuperables para solicitudes GET y HEAD al almacenamiento DiskS3, excluidos los reintentos realizados internamente por la estrategia de reintento de S3

### DiskS3ReadRequestsCount

Número de solicitudes GET y HEAD al almacenamiento de DiskS3.

### DiskS3ReadRequestsErrors

Número de errores no debidos a throttling en las solicitudes GET y HEAD al almacenamiento de DiskS3.

### DiskS3ReadRequestsRedirects

Número de redirecciones en las solicitudes GET y HEAD al almacenamiento de DiskS3.

### DiskS3ReadRequestsThrottling

Número de errores 429 y 503 en las solicitudes GET y HEAD al almacenamiento de DiskS3.

### DiskS3UploadPart

Número de llamadas a UploadPart de la API de DiskS3.

### DiskS3UploadPartCopy

Número de llamadas a la API UploadPartCopy de DiskS3.

### DiskS3WriteMicroseconds

Tiempo de las solicitudes POST, DELETE, PUT y PATCH al almacenamiento de DiskS3.

### DiskS3WriteRequestAttempts

Número de intentos de solicitudes POST, DELETE, PUT y PATCH al almacenamiento de DiskS3, incluido el intento inicial y cualquier reintento, pero sin contar los reintentos realizados internamente por la estrategia de reintento

### DiskS3WriteRequestRetryableErrors

Número de errores recuperables para solicitudes POST, DELETE, PUT y PATCH al almacenamiento DiskS3, excluidos los reintentos realizados internamente por la estrategia de reintento

### DiskS3WriteRequestsCount

Número de solicitudes POST, DELETE, PUT y PATCH al almacenamiento de DiskS3.

### DiskS3WriteRequestsErrors

Número de errores no causados por limitación de tasa en solicitudes POST, DELETE, PUT y PATCH al almacenamiento de DiskS3.

### DiskS3WriteRequestsRedirects

Número de redirecciones en las solicitudes POST, DELETE, PUT y PATCH al almacenamiento de DiskS3.

### DiskS3WriteRequestsThrottling

Número de errores 429 y 503 en las solicitudes POST, DELETE, PUT y PATCH al almacenamiento de DiskS3.

### DiskWriteElapsedMicroseconds

Tiempo total empleado en esperar la syscall de escritura. Esto incluye las escrituras en la caché de páginas.

### DistinctTransformsAbandonedDeduplication

Número de transformadas de deduplicación que descartaron su hash table y dejaron de deduplicar porque la entrada observada era casi totalmente única y, de todos modos, un consumer posterior deduplica: el `DISTINCT` preliminar (véase `allow_preliminary_distinct_abandoning`) y la prededuplicación por stream previa al llenado del Set de una subconsulta `IN`.

### DistinctTransformsSwitchedToPassThrough

Cuántas transformaciones preliminares de `DISTINCT` liberaron su hash table y comenzaron a dejar pasar todas las filas porque la memoria de la consulta superó el threshold de `DISTINCT` external, o porque las asignaciones de hash previstas superarían el presupuesto restante del threshold (véase `max_bytes_before_external_distinct`): en ese caso, el `DISTINCT` final, que hace spill a disco, es el que resuelve los duplicados.

### DistrCacheBackgroundWrites

Evento del búfer de escritura de Distributed Cache. Número de escrituras en cache que se ejecutaron en un background thread (se adquirió un slot de escritura en segundo plano)

### DistrCacheConnectAttempts

Evento de conexión de Distributed Cache. Número de intentos de conexión a Distributed Cache

### DistrCacheConnectErrors

Evento del Client de Distributed Cache. Número de fallos al conectarse a un servidor de Distributed Cache antes de realizar una solicitud (se cuenta una vez por solicitud, después de agotar todos los intentos de conexión, a diferencia de DistrCacheUnsuccessfulConnectAttempts, que se cuenta por intento). Un timeout mientras se espera una conexión disponible del grupo también se cuenta aquí. Los fallos al reconectarse durante la creación de la solicitud se contabilizan en DistrCacheMakeRequestErrors

### DistrCacheConnectMicroseconds

Evento de conexión de Distributed Cache. Tiempo empleado en conectarse a Distributed Cache

### DistrCacheEstimatedInflightBytesOnSeek

Evento del búfer de lectura de Distributed Cache. Bytes in-flight estimados en los reposicionamientos en los que se consideró descartar la connection (compárese con DistrCacheUnusedDataPacketsBytesReadRangeIdChanged)

### DistrCacheFallbackReadMicroseconds

Evento del búfer de lectura de Distributed Cache. Tiempo dedicado a leer desde el búfer de respaldo en lugar de usar la distributed cache

### DistrCacheGetClientMicroseconds

Evento de conexión de Distributed Cache. Tiempo dedicado a obtener el cliente de Distributed Cache

### DistrCacheGetResponseMicroseconds

Evento del cliente de Distributed Cache. Tiempo de espera para recibir la respuesta de Distributed Cache

### DistrCacheHashRingRebuilds

Evento del registro de Distributed Cache. Número de reconstrucciones del hash ring de Distributed Cache

### DistrCacheInlineWrites

Evento del búfer de escritura de Distributed Cache. Número de escrituras en caché que se ejecutaron de forma inline en el thread que las invocó (escrituras de temporary data, que no tienen ninguna escritura en almacenamiento de objetos con la que solaparse)

### DistrCacheLockRegistryMicroseconds

Evento del registro de Distributed Cache. Tiempo empleado en adquirir el bloqueo de DistributedCacheRegistry

### DistrCacheMakeRequestErrors

Evento del cliente de Distributed Cache. Número de errores de Distributed Cache al realizar una solicitud

### DistrCacheNextImplMicroseconds

Evento del búfer de lectura de Distributed Cache. Tiempo empleado en ReadBufferFromDistributedCache::nextImpl

### DistrCacheObjectStorageWriteBytes

Evento del búfer de escritura de Distributed Cache. Número de bytes escritos en el almacenamiento de objetos

### DistrCacheObjectStorageWriteMicroseconds

Evento del búfer de escritura de Distributed Cache. Tiempo invertido en escribir en el almacenamiento de objetos

### DistrCacheOpenedConnections

Evento de conexión de Distributed Cache. El número de conexiones abiertas al Distributed Cache

### DistrCacheOpenedConnectionsBypassingPool

Evento de conexión de Distributed Cache. El número de conexiones abiertas a Distributed Cache sin pasar por el pool

### DistrCachePrecomputeRangesMicroseconds

Evento del búfer de lectura de Distributed Cache. Tiempo empleado en precalcular los rangos de lectura

### DistrCacheRangeChange

Evento del búfer de lectura de Distributed Cache. Número de veces que se cambió el rango de lectura debido a un cambio en seek/last&#95;position

### DistrCacheRangeResetBackward

Evento del búfer de lectura de Distributed Cache. Número de veces que se restablece el rango de lectura debido a un cambio en `seek`/`last_position`

### DistrCacheRangeResetForward

Evento del búfer de lectura de Distributed Cache. Número de veces que se restablece el rango de lectura debido a un cambio en seek/last&#95;position

### DistrCacheReadBytesFromFallbackBuffer

Evento del búfer de lectura de Distributed Cache. Bytes leídos del búfer de respaldo

### DistrCacheReadBytesSkippedOnSeek

Evento del búfer de lectura de Distributed Cache. Bytes en tránsito descartados en un reposicionamiento hacia adelante para reutilizar el stream abierto

### DistrCacheReadBytesUnconsumedOnEndRequest

Evento del búfer de lectura de Distributed Cache. Bytes del read range solicitado que quedaron sin consumir cuando el request se finalizó anticipadamente

### DistrCacheReadErrors

Evento de Client de Distributed Cache. Número total de errores de Distributed Cache durante las solicitudes de lectura (equals DistrCacheReadErrorsRetriable + DistrCacheReadErrorsNonRetriable)

### DistrCacheReadErrorsNonRetriable

Evento del Client de Distributed Cache. Número de errores no reintentables de Distributed Cache durante las solicitudes de lectura

### DistrCacheReadErrorsRetriable

Evento del Client de Distributed Cache. Número de errores reintentables de Distributed Cache durante las solicitudes de lectura (se cuentan cada vez que ocurren, incluidas las que se recuperaron mediante un reintento)

### DistrCacheReadMicroseconds

Evento del búfer de lectura de Distributed Cache. Tiempo empleado en leer desde Distributed Cache

### DistrCacheReadRequestRetries

Evento del búfer de lectura de Distributed Cache. Número de reintentos de la solicitud de lectura tras un error reintentable (incluidos los reintentos de la creación inicial de la solicitud)

### DistrCacheReadRequestRetriesExhausted

Evento del búfer de lectura de Distributed Cache. Número de veces que un error de lectura reintentable no se resolvió dentro del límite de reintentos de la solicitud de lectura

### DistrCacheReadThrottlerBytes

Bytes que pasaron por el limitador de tasa &#39;max&#95;distributed&#95;cache&#95;read&#95;bandwidth&#95;for&#95;server&#39;.

### DistrCacheReadThrottlerSleepMicroseconds

Tiempo total durante el que una consulta permaneció en espera para ajustarse a la limitación de velocidad de &#39;max&#95;distributed&#95;cache&#95;read&#95;bandwidth&#95;for&#95;server&#39;.

### DistrCacheReceivedCredentialsRefreshPackets

Evento de cliente de Distributed Cache. Número total de paquetes RefreshCredentials recibidos de Distributed Cache

### DistrCacheReceivedDataPackets

Evento del cliente de Distributed Cache. Número total de paquetes de datos recibidos desde Distributed Cache

### DistrCacheReceivedDataPacketsBytes

Evento del cliente de Distributed Cache. Número de bytes de los paquetes Data recibidos de Distributed Cache

### DistrCacheReceivedErrorPackets

Evento del cliente de Distributed Cache. Número total de paquetes Error recibidos desde Distributed Cache

### DistrCacheReceivedOkPackets

Evento de cliente de Distributed Cache. Número total de paquetes Ok recibidos desde Distributed Cache

### DistrCacheReceivedSlowDownPackets

Distributed Cache client event. Número total de paquetes SlowDown recibidos desde la distributed cache (el servidor rechazó una nueva connection porque este client tiene demasiadas connections abiertas en relación con su peso)

### DistrCacheReceivedStopPackets

Evento de cliente de Distributed Cache. Número total de paquetes Stop recibidos del Distributed Cache

### DistrCacheReceiveResponseErrors

Evento del cliente de Distributed Cache. Número de errores de Distributed Cache al recibir la respuesta a una solicitud

### DistrCacheRegistryUpdateMicroseconds

Evento del registro de Distributed Cache. Tiempo dedicado a actualizar el registro de Distributed Cache

### DistrCacheRegistryUpdates

Evento del registro de Distributed Cache. Número de actualizaciones del registro de Distributed Cache

### DistrCacheRemoveOutdatedMicroseconds

Evento de conexión de Distributed Cache. Tiempo empleado en eliminar las conexiones obsoletas del pool

### DistrCacheReusedConnections

Evento de conexión de Distributed Cache. Número de conexiones reutilizadas con Distributed Cache

### DistrCacheSeekBufferedBytesShortfall

Evento del búfer de lectura de Distributed Cache. El número de bytes que le faltaron a los datos almacenados temporalmente en el búfer local para cubrir el hueco cuando se rechazó un salto

### DistrCacheSeekCancelledRequests

Evento del búfer de lectura de la Distributed Cache. Número de veces que un reposicionamiento canceló la request abierta en lugar de reutilizar el stream

### DistrCacheSeekReRequestFirstPacketMicroseconds

Evento del búfer de lectura de Distributed Cache. Tiempo transcurrido entre la solicitud de un nuevo rango de lectura en el stream abierto debido a un reposicionamiento y la recepción de su primer paquete

### DistrCacheSeekReRequests

Evento del búfer de lectura de Distributed Cache. Número de veces que un reposicionamiento solicitó un nuevo read range en la misma connection

### DistrCacheSeekSkipDeclinedGapNotBuffered

Evento del búfer de lectura de Distributed Cache. Número de reposicionamientos hacia adelante que no pudieron omitirse porque el hueco no estaba almacenado localmente en el búfer

### DistrCacheSeekSkips

Evento del búfer de lectura de Distributed Cache. Número de reposicionamientos hacia adelante que omitieron el hueco en el stream abierto

### DistrCacheSentDataPackets

Evento del cliente de Distributed Cache. Número total de paquetes de datos enviados a Distributed Cache

### DistrCacheSentDataPacketsBytes

Evento del cliente de Distributed Cache. El número de bytes de los paquetes Data enviados a Distributed Cache

### DistrCacheServerAckRequestPackets

Evento del servidor de Distributed Cache. Número de paquetes AckRequest en DistributedCacheServer

### DistrCacheServerCachedReadBufferCacheHits

Evento del servidor de Distributed Cache. El número de veces que Distributed Cache encontró los datos en la caché mientras leía desde la caché del sistema de archivos

### DistrCacheServerCachedReadBufferCacheMisses

Evento del servidor de Distributed Cache. Número de veces que la caché distribuida sufrió un fallo de caché al leer desde la caché del sistema de archivos

### DistrCacheServerCachedReadBufferCachePredownloadBytes

Evento del servidor de Distributed Cache. Número de bytes leídos desde el almacenamiento de objetos para la predescarga en Distributed Cache al leer desde la caché del sistema de archivos

### DistrCacheServerCachedReadBufferCacheReadBytes

Evento del servidor de Distributed Cache. Número de bytes leídos de la caché de Distributed Cache al leer desde la caché del sistema de archivos

### DistrCacheServerCachedReadBufferCacheWrittenBytes

Evento del servidor de Distributed Cache. Número de bytes escritos en la caché de Distributed Cache al leer desde la caché del sistema de archivos

### DistrCacheServerCachedReadBufferObjectStorageReadBytes

Evento del servidor de Distributed Cache. Número de bytes leídos del almacenamiento de objetos en Distributed Cache al leer desde la caché del sistema de archivos

### DistrCacheServerConnectionRegistryClientLockWaitMicroseconds

Evento del Distributed Cache server. Tiempo de espera de un mutex del bucket de connection por client en caso de contention (registro/cancelación de registro/identificación/eviction)

### DistrCacheServerConnectionRegistryLockWaitMicroseconds

Evento del servidor de Distributed Cache. Tiempo de espera del mutex del registro global de connections en caso de contention (identify, setClientWeight, eviction y unregister cuando corresponde un reindexado o la removal de un bucket)

### DistrCacheServerConnectionsDroppedOverLimit

Distributed Cache server event. Número de connections cerradas porque el número de connections superó el límite (cerradas desde el client con el mayor número de connections en relación con su peso)

### DistrCacheServerConnectionsRejectedOverLimit

Distributed Cache server event. El número de nuevas connections rechazadas con un packet SlowDown porque el client que intentaba conectarse superó su cuota de connections

### DistrCacheServerContinueRequestPackets

Evento del servidor Distributed Cache. Número de paquetes ContinueRequest en DistributedCacheServer

### DistrCacheServerCredentialsRefresh

Evento del servidor de Distributed Cache. Se actualizó el número de credenciales expiradas

### DistrCacheServerEndRequestPackets

Evento del servidor de Distributed Cache. Número de paquetes EndRequest en el DistributedCacheServer

### DistrCacheServerNewS3CachedClients

Evento del servidor de Distributed Cache. La cantidad de nuevos clientes de S3 en caché

### DistrCacheServerProcessRequestMicroseconds

Evento del servidor de Distributed Cache. Tiempo dedicado al procesamiento de la solicitud en el lado del servidor de DistributedCache

### DistrCacheServerReceivedCredentialsRefreshPackets

Evento del servidor de Distributed Cache. Número de paquetes RefreshCredentials del cliente en DistributedCacheServer

### DistrCacheServerReusedS3CachedClients

Evento del servidor Distributed Cache. La cantidad de clientes de S3 en caché reutilizados

### DistrCacheServerShutdownConnectionsMicroseconds

Evento del servidor de Distributed Cache. Tiempo dedicado en shutdownAllConnections a esperar a que las conexiones activas finalicen (o se cierren de forma forzada) durante el apagado del servidor

### DistrCacheServerSkipped

Evento del servidor de Distributed Cache. Número de veces que se omitió el servidor de Distributed Cache debido a intentos fallidos previos de conexión.

### DistrCacheServerStartRequestPackets

Evento del servidor de Distributed Cache. Número de paquetes StartRequest en el DistributedCacheServer

### DistrCacheServerSwitches

Evento del búfer de lectura de Distributed Cache. Número de cambios entre servidores de Distributed Cache en la caché con lectura/escritura directa

### DistrCacheServerUpdates

Evento de Distributed Cache. Número de cambios de servidor durante la lectura o escritura porque cambió el servidor seleccionado por hash (p. ej., debido al desregistro del servidor)

### DistrCacheSkippedWrites

Evento del búfer de escritura de Distributed Cache. Número de búferes de escritura que omitieron la escritura en la caché con lectura/escritura directa porque no había ningún slot de escritura en segundo plano disponible (se alcanzó el límite establecido por el server setting `distributed_cache_write_pool_size`) o porque no se pudo iniciar una escritura en segundo plano. Los datos se escriben igualmente en el almacenamiento de objetos

### DistrCacheStaleReconnections

Evento de conexión de Distributed Cache. Número de reconexiones debidas a conexiones obsoletas del pool (cerradas por el extremo remoto)

### DistrCacheStartRangeMicroseconds

Evento del búfer de lectura de Distributed Cache. Tiempo necesario para iniciar un nuevo rango de lectura con Distributed Cache

### DistrCacheSuccessfulConnectAttempts

Evento de conexión de Distributed Cache. Número de intentos de conexión exitosos a Distributed Cache

### DistrCacheSuccessfulRegistryUpdates

Evento del registro de Distributed Cache. Número de actualizaciones exitosas en el registro de servidores

### DistrCacheSystemTablesErrors

Evento de Client de Distributed Cache. Número de errores de distributed cache en las solicitudes a las tablas del sistema de introspección (system.distributed&#95;cache, system.distributed&#95;cache&#95;metrics, system.distributed&#95;cache&#95;events). Estas solicitudes no se reintentan, por lo que los errores reintentables y no reintentables se contabilizan conjuntamente

### DistrCacheTemporaryFilesBytesWritten

Evento de conexión de Distributed Cache. Número de bytes escritos en los archivos temporales creados en Distributed Cache

### DistrCacheTemporaryFilesCreated

Evento de conexión de Distributed Cache. Número de archivos temporales creados en Distributed Cache

### DistrCacheUnsuccessfulConnectAttempts

Evento de conexión de Distributed Cache. Número de intentos fallidos de conexión a Distributed Cache

### DistrCacheUnsuccessfulRegistryUpdates

Evento del registro de Distributed Cache. Número de actualizaciones fallidas del registro de servidores

### DistrCacheUnusedDataPacketsBytes

Evento del cliente de Distributed Cache. El número de bytes de los paquetes Data que se ignoraron

### DistrCacheUnusedDataPacketsBytesReadRangeIdChanged

Distributed Cache Client event. El número de bytes en Data packets ignorados (parte de DistrCacheUnusedDataPacketsBytes) que llegaron para el request rastreado, pero con un id de rango de lectura anterior (se solicitó un rango de lectura más reciente en el mismo request)

### DistrCacheUnusedDataPacketsBytesUnknownRequest

Distributed Cache Client event. El número de bytes en Data packets ignorados (parte de DistrCacheUnusedDataPacketsBytes) que llegaron para un request que el Client ya no rastrea (por ejemplo, la lectura se detuvo mediante EndRequest después de que el servidor ya hubiera enviado más datos)

### DistrCacheUnusedPackets

Evento del cliente de Distributed Cache. Número de paquetes no utilizados omitidos de Distributed Cache

### DistrCacheUnusedPacketsBufferAllocations

Evento del cliente de Distributed Cache. Número de asignaciones adicionales de búfer cuando no se pudo reutilizar un búfer existente

### DistrCacheWriteBytes

Evento del búfer de escritura de Distributed Cache. Número de bytes escritos en Distributed Cache

### DistrCacheWriteErrors

Evento del Client de Distributed Cache. Número total de errores de Distributed Cache durante las solicitudes de escritura (equals DistrCacheWriteErrorsRetriable + DistrCacheWriteErrorsNonRetriable). Un `Stop` de caché write-through iniciado por el server es control de flujo, no un error, y no se cuenta

### DistrCacheWriteErrorsNonRetriable

Evento del Client de Distributed Cache. Número de errores no reintentables en Distributed Cache durante las solicitudes de escritura

### DistrCacheWriteErrorsRetriable

Evento de Client de Distributed Cache. Número de errores de Distributed Cache que admiten reintento durante las solicitudes de escritura (se cuentan en cada ocurrencia, incluidas las recuperadas mediante un reintento)

### DistrCacheWriteMicroseconds

Evento del búfer de escritura de Distributed Cache. Tiempo dedicado a `WriteBufferFromDistributedCache::writeToFileSegment`

### DistrCacheWriteRequestRetries

Evento del búfer de escritura de Distributed Cache. Número de reintentos de solicitud de escritura tras un error reintentable

### DistrCacheWriteRequestRetriesExhausted

Evento del búfer de escritura de Distributed Cache. Número de veces que un error de escritura reintentable no se resolvió dentro del límite de reintentos de la solicitud de escritura

### DistrCacheWriteThrottlerBytes

Bytes que pasaron por el limitador &#39;max&#95;distributed&#95;cache&#95;write&#95;bandwidth&#95;for&#95;server&#39;.

### DistrCacheWriteThrottlerSleepMicroseconds

Tiempo total durante el que una consulta permaneció en espera para cumplir con la limitación de velocidad de &#39;max&#95;distributed&#95;cache&#95;write&#95;bandwidth&#95;for&#95;server&#39;.

### DistributedAsyncInsertionFailures

Número de fallos de la inserción asíncrona en una tabla distribuida (con &#39;distributed&#95;foreground&#95;insert&#39; = 0)

### DistributedConnectionConnectCount

Número de conexiones a otros servidores realizadas durante la ejecución distribuida de consultas. Sucede cuando se establece una conexión nueva en lugar de reutilizar una del pool.

### DistributedConnectionFailAtAll

Recuento total de fallos de una conexión distribuida tras agotarse todos los reintentos.

### DistributedConnectionFailTry

Recuento total cuando falla una conexión distribuida con reintento.

### DistributedConnectionMissingTable

Número de veces que se rechazó una réplica de una consulta distribuida porque no contenía una tabla necesaria para la consulta.

### DistributedConnectionReconnectCount

Número de reconexiones a otros servidores realizadas durante la ejecución distribuida de consultas. Esto puede ocurrir cuando se obtiene una conexión obsoleta del pool de conexiones

### DistributedConnectionSkipReadOnlyReplica

Número de réplicas omitidas durante INSERT en una tabla distribuida porque las réplicas eran de solo lectura

### DistributedConnectionStaleReplica

Número de veces que se rechazó una réplica en una consulta distribuida porque alguna tabla necesaria para la consulta tenía un retraso de replicación superior al umbral configurado.

### DistributedConnectionTries

Número total de intentos de conexión distribuida.

### DistributedConnectionUsable

Recuento total de conexiones distribuidas exitosas a un servidor utilizable (con la tabla requerida, aunque posiblemente obsoleta).

### DistributedDelayedInserts

Número de veces que el INSERT de un bloque en una tabla distribuida se vio limitado debido a un alto número de bytes pendientes.

### DistributedDelayedInsertsMilliseconds

Número total de milisegundos transcurridos mientras el INSERT de un bloque en una tabla distribuida se limitó debido al elevado número de bytes pendientes.

### DistributedIndexAnalysisMicroseconds

Tiempo total empleado durante el análisis distribuido de índices

### DistributedIndexAnalysisMissingParts

Número de partes faltantes durante el análisis distribuido de índices que se resolverán de forma local

### DistributedIndexAnalysisParts

Número de partes enviadas para el análisis distribuido de índices

### DistributedIndexAnalysisReplicaFallback

Número de veces que el análisis distribuido de índices falló en una de las réplicas y se recurrió a la réplica local

### DistributedIndexAnalysisReplicaUnavailable

Número de veces que el análisis distribuido de índices falló en una de las réplicas sin fallback (falló al conectarse)

### DistributedIndexAnalysisScheduledReplicas

Número de réplicas (la réplica local se contabiliza una vez) para las que se ha programado el análisis distribuido de índices

### DistributedPlanHostsUsed

Número de hosts distintos a los que se asignó al menos una tarea al ejecutar una consulta con make&#95;distributed&#95;plan.

### DistributedPlanLocalExecution

Se establece en 1 cuando una consulta `make_distributed_plan` se ejecuta en el mismo proceso mediante el ejecutor local (`distributed_plan_execute_locally`), en lugar de enviarse a workers remotos.

### DistributedPlanRemoteTasks

Número de tareas enviadas a workers remotos al ejecutar una consulta con make&#95;distributed&#95;plan. Un valor distinto de cero indica que la consulta se ejecutó realmente de forma distribuida.

### DistributedPlanWorkerPartsPruned

Número de partes de datos asignadas que el propio análisis de índices del worker descartó, de modo que el worker no las lee. El coordinator selecciona las partes sin realizar ese análisis, por lo que puede asignar una parte con la que no coincida ninguna fila de la consulta. `DistributedPlanWorkerPartsReceived` es la suma de este valor y de `DistributedPlanWorkerPartsScanned`.

### DistributedPlanWorkerPartsReceived

Número de partes de datos que el coordinator asignó a un worker de un distributed query plan, sumado sobre las lecturas por buckets que una consulta ejecuta en ese worker. Una parte asignada a varios buckets se cuenta una vez por cada lectura.

### DistributedPlanWorkerPartsScanned

Número de partes de datos asignadas que el worker conserva para leer.

### DistributedRejectedInserts

Número de veces que se rechazó el INSERT de un bloque en una tabla distribuida con la excepción &#39;Too many bytes&#39; debido al elevado número de bytes pendientes.

### DistributedShardsSkipped

Número de segmentos omitidos de forma silenciosa en una consulta a una tabla Distributed (o a las funciones de tabla remote/cluster) cuando `skip_unavailable_shards` está habilitado, ya sea porque el segmento no estaba disponible o porque lanzó una excepción contemplada por `skip_unavailable_shards_mode`.

### DistributedSyncInsertionTimeoutExceeded

Se ha superado el tiempo de espera mientras se esperaba a los segmentos durante la inserción síncrona en una tabla distribuida (con &#39;distributed&#95;foreground&#95;insert&#39; = 1)

### DNSError

Número total de errores en la resolución DNS

### DuplicatedAsyncInserts

Se deduplicó el número de inserts asíncronos en el bloque insertado en una tabla ReplicatedMergeTree.

### DuplicatedInsertedBlocks

Número de inserciones sincrónicas en una tabla *MergeTree que fueron deduplicadas.

### DuplicationDataHashComputations

Número de cálculos de hash de datos por columna realizados al deduplicar bloques insertados con INSERT en tablas *MergeTree (fallos de caché en el hash de deduplicación unificado).

### DuplicationElapsedMicroseconds

Tiempo total dedicado a comprobar si había duplicados en los bloques insertados en tablas *MergeTree.

### EncryptionHeaderCacheHits

Número de veces que los bytes del encabezado de cifrado se encontraron en el caché de encabezados de cifrado, por lo que se omitió la lectura del encabezado desde la fuente.

### EncryptionHeaderCacheMisses

Número de veces que los bytes del encabezado de cifrado no se encontraron en la caché de encabezados de cifrado, por lo que se leyeron desde la fuente.

### EngineFileLikeReadFiles

Número de archivos leídos en motores de tabla que funcionan con archivos (como File/S3/URL/HDFS).

### ExecutableUserDefinedFunctionElapsedMicroseconds

Tiempo real transcurrido al ejecutar funciones definidas por el usuario de tipo executable y executable&#95;pool, en microsegundos.

### ExecutableUserDefinedFunctionInputBytes

Total de bytes escritos en la stdin de los procesos hijo de las funciones definidas por el usuario de tipo executable y executable&#95;pool.

### ExecutableUserDefinedFunctionInvocations

Número de invocaciones de funciones definidas por el usuario de tipo executable y executable&#95;pool.

### ExecutableUserDefinedFunctionOutputBytes

Total de bytes leídos desde la salida estándar (stdout) de los procesos hijo de las funciones definidas por el usuario de tipo ejecutable y de executable&#95;pool.

### ExecutableUserDefinedFunctionPeakMemoryByteSeconds

Memoria pico por proceso utilizada por los procesos hijos de funciones definidas por el usuario de tipo executable y executable&#95;pool, integrada respecto al tiempo de reloj real, en byte-segundos.

### ExecutableUserDefinedFunctionPoolWaitMicroseconds

Tiempo de espera en tryBorrowObject cuando se agota el grupo de la función definida por el usuario executable&#95;pool, en microsegundos.

### ExecutableUserDefinedFunctionSystemTimeMicroseconds

Tiempo de CPU en modo kernel consumido por los procesos hijo de funciones definidas por el usuario de tipo executable y executable&#95;pool, en microsegundos.

### ExecutableUserDefinedFunctionUserTimeMicroseconds

Tiempo de CPU en modo usuario consumido por los procesos hijo de las funciones definidas por el usuario de tipo executable y executable&#95;pool, en microsegundos.

### ExecuteShellCommand

Número de ejecuciones de comandos de shell.

### ExternalAggregationCompressedBytes

Número de bytes escritos en disco durante la agregación en memoria externa.

### ExternalAggregationMerge

Número de veces que se fusionaron archivos temporales para la agregación en memoria externa.

### ExternalAggregationUncompressedBytes

Cantidad de datos (sin comprimir, antes de la compresión) escritos en disco para la agregación con memoria externa.

### ExternalAggregationWritePart

Número de veces que se escribió un archivo temporal en disco para una agregación con memoria externa.

### ExternalDataSourceLocalCacheReadBytes

Bytes leídos del búfer de caché local de RemoteReadBufferCache

### ExternalDistinctCompressedBytes

Número de bytes comprimidos escritos para DISTINCT en memoria externa.

### ExternalDistinctMerge

Número de veces que se fusionaron archivos temporales para DISTINCT en memoria externa.

### ExternalDistinctUncompressedBytes

Cantidad de datos (sin comprimir, antes de la compresión) escritos para DISTINCT en memoria externa.

### ExternalDistinctWritePart

Número de veces que se escribió un archivo temporal en disco para DISTINCT en memoria externa.

### ExternalJoinCompressedBytes

Número de bytes comprimidos escritos para la operación JOIN en memoria externa.

### ExternalJoinMerge

Número de veces que se fusionaron archivos temporales para operaciones JOIN en memoria externa.

### ExternalJoinUncompressedBytes

Cantidad de datos sin comprimir (antes de la compresión) escritos para JOIN en memoria externa.

### ExternalJoinWritePart

Número de veces que se escribió un archivo temporal en disco para un JOIN con memoria externa.

### ExternalProcessingCompressedBytesTotal

Número de bytes comprimidos escritos por el procesamiento externo (ordenación/agregación/join)

### ExternalProcessingFilesTotal

Número de archivos utilizados por el procesamiento externo (ordenación/agregación/combinación)

### ExternalProcessingUncompressedBytesTotal

Volumen de datos (sin comprimir, antes de la compresión) escrito por el procesamiento externo (ordenación/agregación/join)

### ExternalSortCompressedBytes

Número de bytes comprimidos escritos durante la ordenación en memoria externa.

### ExternalSortMerge

Número de veces que se combinaron archivos temporales para la ordenación en memoria externa.

### ExternalSortUncompressedBytes

Cantidad de datos (sin comprimir, antes de la compresión) escritos durante la ordenación en memoria externa.

### ExternalSortWritePart

Número de veces que se escribió un archivo temporal en disco durante la ordenación en memoria externa.

### FailedAsyncInsertQuery

Número de consultas de ASYNC INSERT fallidas.

### FailedInitialQuery

Número de consultas iniciales fallidas.

### FailedInitialSelectQuery

Igual que FailedInitialQuery, pero solo para consultas de SELECT.

### FailedInsertQuery

Igual que FailedQuery, pero solo para consultas de INSERT.

### FailedInternalInsertQuery

Igual que FailedInternalQuery, pero solo para consultas de INSERT.

### FailedInternalQuery

Número de consultas internas fallidas.

### FailedInternalSelectQuery

Igual que FailedInternalQuery, pero solo para consultas de tipo SELECT.

### FailedQuery

Número total de consultas fallidas, tanto internas como de usuarios.

### FailedSelectQuery

Igual que FailedQuery, pero solo para las consultas SELECT.

### FetchBackgroundExecutorTaskCancelMicroseconds

Tiempo invertido en `cancel()` para las tareas de fetch del ejecutor Fetch.

### FetchBackgroundExecutorTaskExecuteStepMicroseconds

Tiempo empleado en executeStep() por las tareas del ejecutor Fetch.

### FetchBackgroundExecutorTaskResetMicroseconds

Tiempo dedicado a reiniciar la tarea del ejecutor Fetch.

### FetchBackgroundExecutorWaitMicroseconds

Tiempo dedicado a esperar a que finalice el ejecutor Fetch.

### FiberStackAllocBytes

Número de bytes asignados a las pilas de fibras, incluidas las páginas de guarda (las páginas de guarda solo se usan en builds de depuración y con sanitizador).

### FiberStackAllocNanoseconds

Tiempo empleado en asignar pilas de fibras. Se usan nanosegundos porque una sola asignación suele tardar menos de un microsegundo, salvo que haya contención en el allocator o en el kernel.

### FiberStackAllocs

Número de pilas asignadas para fibras (las fibras se utilizan para la comunicación asíncrona con réplicas remotas).

### FiberStackFreeNanoseconds

Cantidad de tiempo empleado en liberar pilas de fibras.

### FileOpen

Número de archivos abiertos.

### FileProgressCallbackInvocations

Número de veces que se invocó `FileProgressCallback` para cada consulta (contabiliza cada evento `FileProgress` por archivo enviado al Client mediante TCP nativo o a través de `clickhouse-local`).

### FileSegmentCompleteMicroseconds

Duración de FileSegment::complete() en la caché del sistema de archivos

### FileSegmentFailToIncreasePriority

Número de veces que no se incrementó la prioridad debido a una alta contención del bloqueo de caché

### FileSegmentHolderCompleteMicroseconds

Tiempo de `complete()` del gestor de segmentos de archivo

### FileSegmentIncreasePriorityMicroseconds

Tiempo de incremento de prioridad del segmento de archivo

### FileSegmentLockMicroseconds

Tiempo de bloqueo del segmento de archivo

### FileSegmentRemoveMicroseconds

Tiempo de remove() del segmento de archivo

### FileSegmentWaitMicroseconds

Espera en estado DOWNLOADING

### FileSegmentWaitTimeouts

Número de veces que la espera de un segmento de archivo en estado DOWNLOADING agotó su tiempo límite (véase `filesystem_cache_wait_for_concurrent_download_timeout_milliseconds`)

### FileSegmentWriteMicroseconds

Tiempo de escritura con write() del segmento de archivo

### FileSync

Número de veces que se llamó a la función F&#95;FULLFSYNC/fsync/fdatasync para los archivos.

### FileSyncElapsedMicroseconds

Tiempo total empleado en esperar la llamada al sistema F&#95;FULLFSYNC/fsync/fdatasync sobre archivos.

### FilesystemCacheBackgroundDownloadQueuePush

Número de segmentos de archivo enviados para su descarga en segundo plano en la caché del sistema de archivos

### FilesystemCacheBackgroundEvictedBytes

Número de bytes expulsados por el hilo en segundo plano

### FilesystemCacheBackgroundEvictedFileSegments

Número de segmentos de archivo desalojados por el hilo en segundo plano

### FilesystemCacheBackgroundRemovedInvalidatedEntries

Número de entradas invalidadas de la cola de prioridad (eliminadas de forma diferida) purgadas por el hilo de limpieza en segundo plano

### FilesystemCacheCheckCorrectness

Número de veces que se ha llamado a FileCache::assertCacheCorrectness

### FilesystemCacheCheckCorrectnessMicroseconds

Cuánto tiempo tarda FileCache::assertCacheCorrectness

### FilesystemCacheClientsMapLockWaitMicroseconds

Tiempo de espera por un lock en contención del segmento del mapa de uso por Client (`user_id`) en la cache policy de overcommit

### FilesystemCacheCreatedKeyDirectories

Número de directorios creados para claves

### FilesystemCacheDowngradedFileSegments

Número de segmentos de archivo descendidos (movidos) de la cola protegida a la cola de prueba en la política de caché SLRU. Se trata de un movimiento interno dentro de la caché, no de una expulsión.

### FilesystemCacheEvictedBytes

Número de bytes desalojados de la caché del sistema de archivos

### FilesystemCacheEvictedFileSegments

Número de segmentos de archivo desalojados de la caché del sistema de archivos

### FilesystemCacheEvictionReusedIterator

Número de veces que se reutiliza el iterador de la caché del sistema de archivos

### FilesystemCacheEvictionSkippedEvictingFileSegments

Número de segmentos de archivo omitidos durante la evicción por estar en estado de evicción

### FilesystemCacheEvictionSkippedFileSegments

Número de segmentos de archivo omitidos durante la evicción por estar en un estado no liberable

### FilesystemCacheEvictionSkippedMovingFileSegments

Número de segmentos de archivo excluidos de la evicción por estar en estado de movimiento

### FilesystemCacheEvictionTries

Número de intentos de evicción de elementos de la caché del sistema de archivos

### FilesystemCacheEvictMicroseconds

Tiempo de evicción de la caché del sistema de archivos

### FilesystemCacheFailedEvictionCandidates

Número de segmentos de archivo cuya evicción falló inesperadamente durante la evicción dinámica de la caché del sistema de archivos

### FilesystemCacheFailedReserveAttempts

Número de intentos fallidos de reservar espacio en la caché del sistema de archivos.

### FilesystemCacheFailToReserveSpaceBecauseOfCacheResize

Número de veces que se omitió la reserva de espacio porque la caché se estaba redimensionando

### FilesystemCacheFailToReserveSpaceBecauseOfLockContention

Número de veces que se omitió la reserva de espacio por una alta contención en el bloqueo de la caché

### FilesystemCacheFreeSpaceKeepingThreadErrors

Número de excepciones capturadas por la tarea en segundo plano encargada de mantener el espacio libre (recopilación, eliminación o finalización)

### FilesystemCacheFreeSpaceKeepingThreadRun

Número de veces que el hilo en segundo plano ejecutó la tarea de mantenimiento de espacio libre

### FilesystemCacheFreeSpaceKeepingThreadWorkMilliseconds

Tiempo durante el cual el hilo en segundo plano ejecutó la tarea de mantenimiento del espacio libre

### FilesystemCacheGetMicroseconds

Tiempo de get() en la caché del sistema de archivos

### FilesystemCacheGetOrSetMicroseconds

Tiempo de getOrSet() en la caché del sistema de archivos

### FilesystemCacheHoldFileSegments

Cantidad de segmentos de archivo retenidos en la caché del sistema de archivos

### FilesystemCacheIdleClientEvictions

Número de veces que se purgaron todas las entradas de caché de un Client de caché distribuida inactivo (`user_id`) porque no se produjo ningún acceso dentro de `idle_client_ttl_sec`.

### FilesystemCacheInvalidatedEntriesCleanupThreadWorkMilliseconds

Tiempo durante el que el hilo en segundo plano ejecutó el trabajo de limpieza de los elementos invalidados de la cola de prioridad

### FilesystemCacheLoadMetadataMicroseconds

Tiempo empleado en cargar los metadatos de la caché del sistema de archivos

### FilesystemCacheLockKeyMicroseconds

Tiempo de bloqueo de la clave de la caché

### FilesystemCacheLockMetadataMicroseconds

Tiempo de bloqueo de los metadatos de la caché del sistema de archivos

### FilesystemCacheLockOriginPoolMicroseconds

Tiempo de bloqueo del grupo de origen compartido de la caché del sistema de archivos

### FilesystemCacheOvercommitCandidatesIterationSteps

Número de iteraciones externas que realizó el bucle de expulsión por overcommit después de su primera pasada para cubrir el déficit de una reserva

### FilesystemCachePriorityReadLockMicroseconds

Tiempo de bloqueo de lectura de la caché del sistema de archivos en la cola de prioridad

### FilesystemCachePriorityWriteLockMicroseconds

Tiempo de bloqueo de la caché del sistema de archivos al escribir en la cola de prioridad

### FilesystemCacheReserveAttempts

Intento de reservar espacio en la caché del sistema de archivos

### FilesystemCacheReserveMicroseconds

Tiempo de reserva de espacio en la caché del sistema de archivos

### Tiempo de bloqueo de la caché del sistema de archivos para el bloqueo de estado, en microsegundos

Tiempo de bloqueo de la caché del sistema de archivos para el bloqueo de estado

### Segmentos de archivo retenidos sin usar de la caché del sistema de archivos

Número de segmentos de archivo de la caché del sistema de archivos que quedaron retenidos, pero no se usaron (debido a operaciones de seek o a LIMIT n, etc.)

### FilteringMarksWithPrimaryKeyMicroseconds

Tiempo empleado en filtrar partes por PK.

### FilteringMarksWithPrimaryKeyProcessedMarks

Número total de marcas procesadas durante el análisis de la PK.

### FilteringMarksWithSecondaryKeysMicroseconds

Tiempo empleado en filtrar partes mediante índices de omisión.

### FilterPartsByVirtualColumnsMicroseconds

Tiempo total dedicado a la función filterPartsByVirtualColumns.

### FilterTransformPassedBytes

Número de bytes que pasaron por el filtro en la consulta

### FilterTransformPassedRows

Número de filas que superaron el filtro en la consulta

### FunctionExecute

Número de llamadas a funciones SQL ordinarias (las funciones SQL se ejecutan por bloque, por lo que este número representa la cantidad de bloques).

### GatheredColumns

Número de columnas recopiladas durante la fase vertical de las fusiones.

### GatheringColumnMilliseconds

Tiempo total invertido en recopilar columnas para la fusión vertical

### GlobalMemoryLimitExceeded

Número de veces que se superó el límite global de memoria.

### GlobalThreadPoolExpansions

Cuenta el número total de veces que se han añadido nuevos hilos al pool global de hilos. Esta métrica indica la frecuencia con la que se expande el pool global de hilos para dar cabida al aumento de las demandas de procesamiento.

### GlobalThreadPoolJobs

Cuenta el número de trabajos que se han enviado al pool global de hilos.

### GlobalThreadPoolJobWaitTimeMicroseconds

Mide el tiempo transcurrido desde que un trabajo se programa en el pool de hilos hasta que un hilo de trabajo lo recoge para ejecutarlo. Esta métrica ayuda a identificar retrasos en el procesamiento de trabajos, lo que indica la capacidad de respuesta del pool de hilos ante nuevas tareas.

### GlobalThreadPoolLockWaitMicroseconds

Tiempo total que los hilos han dedicado a esperar bloqueos en el pool global de hilos.

### GlobalThreadPoolShrinks

Recuento del número total de veces que el pool global de hilos se ha reducido al eliminar hilos. Esto ocurre cuando la cantidad de hilos inactivos supera `max_thread_pool_free_size`, lo que indica ajustes en el tamaño del pool global de hilos en respuesta a una menor utilización de hilos.

### GlobalThreadPoolThreadCreationMicroseconds

Tiempo total de espera para que se inicien nuevos hilos.

### Fallos de página mayores

El número de fallos de página mayores en los hilos de ejecución de consultas. Los valores altos indican que olvidó desactivar la swap en su servidor, el desalojo de páginas de memoria del binario de ClickHouse debido a una presión de memoria muy alta, o el uso correcto del método de lectura &#39;mmap&#39; para los datos de las tablas.

### HashJoinPreallocatedElementsInHashTables

Número de elementos preasignados en las tablas hash para hash join.

### HedgedRequestsChangeReplica

Número total de veces que expiró el timeout para cambiar de réplica en las solicitudes especulativas.

### HTTPConnectionsCreated

Número de conexiones HTTP de cliente que se han creado

### HTTPConnectionsElapsedMicroseconds

Tiempo total invertido en la creación de conexiones HTTP del cliente

### HTTPConnectionsErrors

Número de casos en los que falló la creación de una conexión HTTP del cliente

### HTTPConnectionsExpired

Número de conexiones HTTP de cliente expiradas

### HTTPConnectionsPreserved

Número de conexiones HTTP del cliente conservadas

### HTTPConnectionsReset

Número de conexiones HTTP del cliente reiniciadas

### HTTPConnectionsReused

Número de conexiones HTTP del cliente reutilizadas

### HTTPServerConnectionsClosed

Número de conexiones HTTP del servidor que se han cerrado. No se ha negociado Keep alive

### HTTPServerConnectionsCreated

Número de conexiones HTTP del servidor creadas

### HTTPServerConnectionsExpired

Número de conexiones HTTP del servidor que expiraron.

### HTTPServerConnectionsPreserved

Número de conexiones HTTP del servidor que se conservaron. Conexión mantenida activa correctamente

### HTTPServerConnectionsReset

Número de conexiones HTTP del servidor reiniciadas. El servidor cierra la conexión

### HTTPServerConnectionsReused

Número de conexiones HTTP reutilizadas del servidor

### IcebergIteratorInitializationMicroseconds

Tiempo total empleado en la inicialización síncrona de los iteradores de datos de Iceberg.

### IcebergMetadataFilesCacheHits

Número de veces que los archivos de metadatos de Iceberg se han encontrado en la caché.

### IcebergMetadataFilesCacheMisses

Número de veces que los archivos de metadatos de Iceberg no se encontraron en la caché de metadatos de Iceberg y tuvieron que leerse desde un disco (remoto).

### Fallos de la caché por archivos de metadatos de Iceberg desactualizados

Número de veces que se encontraron archivos de metadatos de Iceberg en la caché, pero se consideraron desactualizados y tuvieron que leerse desde el disco (remoto).

### IcebergMetadataFilesCacheWeightLost

Número aproximado de bytes desalojados de la caché de metadatos de Iceberg.

### IcebergMetadataReadWaitTimeMicroseconds

Tiempo total que los lectores de datos pasan esperando a que se lean y analicen los archivos de metadatos de Iceberg, sumado entre todos los hilos de lectura.

### IcebergMetadataReturnedObjectInfos

Número total de entradas de información de objetos devueltas por el iterador de Iceberg.

### IcebergMetadataUpdateMicroseconds

Tiempo total empleado en la actualización de los metadatos de almacenamiento de Iceberg a partir del estado (buildStorageMetadataFromState).

### IcebergMinMaxIndexPrunedFiles

Número de archivos omitidos mediante el índice MinMax en Iceberg

### IcebergMinMaxNonPrunedDeleteFiles

Número total de pares aceptados de archivos de datos y archivos de eliminación por posición según el análisis minmax, entre los pares aptos según el particionamiento y el número de secuencia.

### IcebergMinMaxPrunedDeleteFiles

Número total de pares aceptados de archivos de datos y archivos de eliminación por posición según el análisis minmax, a partir de los pares aptos por particionamiento y número de secuencia.

### IcebergPartitionPrunedFiles

Número de archivos omitidos durante la poda de particiones en Iceberg

### IcebergPartitionPrunedManifestFiles

Número de manifest files de Iceberg omitidos sin llegar a leerse, a partir de los resúmenes de partición del manifest list

### IcebergTrivialCountOptimizationApplied

Optimización de recuento trivial aplicada al leer desde Iceberg

### IcebergVersionHintUsed

Número de veces que se utilizó version-hint.text.

### IgnoredColdParts

Consulte la configuración `ignore_cold_parts_seconds`. Número de veces que las consultas de lectura ignoraron partes muy recientes que CacheWarmer aún no había incorporado a la caché.

### IndexAnalysisRounds

Número de veces que se realizó el análisis de índices durante la consulta.

### IndexBinarySearchAlgorithm

Número de veces que se utiliza el algoritmo de búsqueda binaria sobre las marcas del índice

### IndexGenericExclusionSearchAlgorithm

Número de veces que se usa el algoritmo de búsqueda de exclusión genérica en las marcas del índice

### IndexGenericExclusionSearchStepLimitReached

Número de veces que la búsqueda de exclusión genérica sobre las marcas del índice alcanzó merge&#95;tree&#95;generic&#95;exclusion&#95;search&#95;max&#95;steps y aceptó los mark ranges restantes sin dividirlos más

### InitialQuery

Igual que Query, pero solo cuenta las consultas iniciales (ver `is_initial_query`).

### InitialSelectQuery

Igual que InitialQuery, pero solo para las consultas SELECT.

### InsertedBytes

Número de bytes (sin comprimir; de las columnas, tal como se almacenan en memoria) insertados en todas las tablas.

### InsertedCompactParts

Número de partes insertadas en formato Compact.

### InsertedRows

Número de filas insertadas en todas las tablas.

### InsertedWideParts

Número de partes insertadas en formato Wide.

### InsertQueriesWithSubqueries

Contar las consultas INSERT con todas las subconsultas

### InsertQuery

Lo mismo que Query, pero solo para consultas INSERT.

### InsertQueryTimeMicroseconds

Tiempo total de las consultas de INSERT.

### InterfaceHTTPReceiveBytes

Número de bytes recibidos a través de interfaces HTTP

### InterfaceHTTPSendBytes

Número de bytes enviados a través de interfaces HTTP

### InterfaceInterserverReceiveBytes

Número de bytes recibidos a través de interfaces entre servidores

### InterfaceInterserverSendBytes

Número de bytes enviados a través de interfaces entre servidores

### InterfaceMySQLReceiveBytes

Número de bytes recibidos a través de las interfaces MySQL

### InterfaceMySQLSendBytes

Número de bytes enviados por las interfaces MySQL

### InterfaceNativeReceiveBytes

Número de bytes recibidos a través de las interfaces nativas

### InterfaceNativeSendBytes

Número de bytes enviados a través de las interfaces nativas

### InterfacePostgreSQLReceiveBytes

Número de bytes recibidos a través de las interfaces de PostgreSQL

### InterfacePostgreSQLSendBytes

Número de bytes enviados a través de las interfaces de PostgreSQL

### InterfacePrometheusReceiveBytes

Número de bytes recibidos a través de las interfaces de Prometheus

### InterfacePrometheusSendBytes

Número de bytes enviados por las interfaces de Prometheus

### IOBufferAllocBytes

Número de bytes asignados a los búferes de E/S (para ReadBuffer/WriteBuffer).

### IOBufferAllocs

Número de asignaciones de memoria de los búferes de E/S (para ReadBuffer/WriteBuffer).

### IOUringCQEsCompleted

Número total de CQE de io&#95;uring completadas con éxito

### IOUringCQEsFailed

Número total de CQE completados de io&#95;uring con errores

### IOUringSQEsResubmitsAsync

Número total de reenvíos asíncronos de SQE en io&#95;uring efectuados

### IOUringSQEsResubmitsSync

Número total de reenvíos síncronos de SQE de io&#95;uring realizados

### IOUringSQEsSubmitted

Número total de SQE enviados a io&#95;uring

### JemallocFailedAllocationSampleTracking

Número total de veces que falló el seguimiento de muestras de asignación de jemalloc

### JemallocFailedDeallocationSampleTracking

Número total de veces que falló el seguimiento de la muestra de liberación de memoria de jemalloc

### JoinBuildPostProcessingMicroseconds

Tiempo transcurrido en los pasos de posprocesamiento tras construir el lado derecho del JOIN.

### JoinBuildRowStoreMicroseconds

Tiempo transcurrido al transformar la carga útil del lado derecho del JOIN a formato orientado a filas (row-major).

### JoinBuildTableRowCount

Número total de filas de la tabla de construcción en una operación JOIN.

### JoinDelayedJoinedTransformBlockCount

Número de bloques emitidos por DelayedJoinedBlocksWorkerTransform.

### JoinDelayedJoinedTransformRowCount

Número de filas emitidas por DelayedJoinedBlocksWorkerTransform.

### JoinNonJoinedTransformBlockCount

Número de bloques emitidos por NonJoinedBlocksTransform.

### JoinNonJoinedTransformRowCount

Número de filas no emparejadas emitidas por NonJoinedBlocksTransform.

### JoinOptimizeMicroseconds

Tiempo total dedicado a ejecutar optimizaciones del plan de JOIN.

### JoinProbeTableRowCount

Número total de filas de la tabla de búsqueda para una operación JOIN.

### JoinReorderMicroseconds

Tiempo total dedicado a ejecutar el algoritmo de reordenación de JOIN.

### Recuento de filas del resultado de JOIN

Número total de filas en el resultado de una operación JOIN.

### JoinSpillingHashJoinSwitchedToGraceJoin

Número de veces que un (Concurrent)HashJoin se sustituyó por GraceHashJoin debido al límite de memoria en SpillingHashJoin.

### KafkaBackgroundReads

Número de lecturas en segundo plano para poblar vistas materializadas desde Kafka desde que se inició el servidor

### Fallos en el commit de Kafka

Número de commits fallidos de los offsets consumidos en Kafka (normalmente, es una señal de cierta duplicación de datos)

### KafkaCommits

Número de commits realizados correctamente de offsets consumidos en Kafka (normalmente debería ser el mismo que KafkaBackgroundReads)

### KafkaConsumerErrors

Número de errores reportados por librdkafka durante los sondeos

### KafkaDirectReads

Número de consultas directas a tablas de Kafka desde que se inició el servidor

### KafkaMessagesFailed

Número de mensajes de Kafka que ClickHouse no pudo analizar

### KafkaMessagesPolled

Número de mensajes de Kafka recuperados de librdkafka para ClickHouse

### KafkaMessagesProduced

Número de mensajes enviados a Kafka

### KafkaMessagesRead

Número de mensajes de Kafka ya procesados por ClickHouse

### KafkaMVNotReady

Número de intentos fallidos de transmitir datos en streaming a una vista materializada que no está lista

### KafkaProducerErrors

Número de errores al producir mensajes en Kafka

### KafkaProducerFlushes

Número de operaciones de flush explícitas del productor de Kafka

### KafkaRebalanceAssignments

Número de asignaciones de particiones (la etapa final del reequilibrio del grupo de consumidores)

### KafkaRebalanceErrors

Número de reequilibrios fallidos del grupo de consumidores

### KafkaRebalanceRevocations

Número de revocaciones de particiones (la primera etapa del rebalanceo del grupo de consumidores)

### KafkaRowsRead

Número de filas analizadas de los mensajes de Kafka

### KafkaRowsRejected

Número de filas analizadas que luego fueron rechazadas (debido a reequilibrios, errores o motivos similares). Esas filas se volverán a consumir después del reequilibrio.

### KafkaRowsWritten

Número de filas insertadas en tablas de Kafka

### KafkaWrites

Número de operaciones de escritura (inserciones) en tablas de Kafka

### KeeperAddWatchRequest

Número de solicitudes para agregar watches

### KeeperBatchMaxCount

Número de lotes que alcanzaron el límite de max&#95;requests&#95;batch&#95;size

### KeeperBatchMaxReadCount

Número de lotes que alcanzaron el límite de max&#95;read&#95;batch&#95;size

### KeeperBatchMaxReadTotalSize

Número de lotes que alcanzaron el límite de max&#95;read&#95;batch&#95;bytes&#95;size

### KeeperBatchMaxTotalSize

Número de lotes que alcanzaron el límite max&#95;requests&#95;batch&#95;bytes&#95;size

### KeeperChangelogFileSyncMicroseconds

Tiempo dedicado a fsync para el changelog de Keeper (solo logs sin comprimir)

### KeeperChangelogLockWaitMicroseconds

Tiempo de espera para adquirir el bloqueo del changelog de Keeper

### KeeperChangelogStartupReadBytes

Número de bytes físicos (suma de comprobación + encabezado + blob) leídos de los archivos changelog por el lector paralelo de arranque de Keeper

### KeeperChangelogStartupReadEntries

Número de registros del changelog validados por el lector paralelo de arranque de Keeper

### KeeperChangelogStartupReadMicroseconds

Wall time empleado en la lectura paralela del changelog durante el inicio de Keeper

### KeeperChangelogStartupStitchMicroseconds

Wall time empleado en combinar los resultados de las lecturas paralelas por archivo realizadas durante el arranque en el estado de LogEntryStorage

### KeeperChangelogWrittenBytes

Número de bytes escritos en el changelog de Keeper

### KeeperCheckRequest

Número de solicitudes de comprobación

### KeeperCheckWatchRequest

Número de solicitudes de remove watches

### KeeperCommits

Número de commits exitosos

### KeeperCommitsFailed

Número de commits fallidos

### KeeperCommitWaitElapsedMicroseconds

Tiempo empleado en esperar a que se confirme un determinado log

### KeeperCreateRequest

Número de solicitudes de creación

### KeeperExistsRequest

Número de solicitudes de comprobación de existencia

### KeeperGetRequest

Número de solicitudes de tipo get

### KeeperLatency

Latencia de Keeper

### KeeperListRecursiveRequest

Número de solicitudes recursivas para obtener nodos hijo

### KeeperListRequest

Número de solicitudes de listado

### Microsegundos de espera para el bloqueo de sesiones activas de Keeper

Tiempo de espera para adquirir el bloqueo de sesiones activas de Keeper

### KeeperLogsEntryReadFromCommitReadAhead

Número de entradas de log entregadas al hilo de commit desde la ventana decodificada del lector de lectura anticipada de commit (extracción por vía rápida o extracción de vaciado del primer elemento de la ventana)

### KeeperLogsEntryReadFromFile

Número de entradas del log en Keeper leídas directamente desde el archivo de changelog

### KeeperLogsEntryReadFromLatestCache

Número de entradas de log de Keeper leídas desde la caché de los logs más recientes

### KeeperLogsReadAheadCursorsInstalled

Número de nuevos cursores de fill encolados en un lector de lectura anticipada del changelog de Keeper (peer o commit) que la tarea de relleno todavía no cubría

### KeeperLogsReadAheadFillDecodedEntries

Número de entradas de log decodificadas por la tarea de relleno de lectura anticipada de Keeper

### KeeperLogsReadAheadFillReopens

Número de veces que el llenado por lectura anticipada de Keeper reabrió o reposicionó un changelog file

### KeeperLogsReadAheadPlanEpochMismatches

Número de veces que se descartó un plan de lectura del changelog de Keeper porque un truncamiento write&#95;at concurrente lo invalidó antes de que pudiera atenderse

### KeeperLogsReadAheadReadersCreated

Número de lectores de lectura anticipada del changelog de Keeper (y de sus tareas de relleno) creados; si el valor se mantiene constante al cruzar los límites de los archivos, indica que se está reutilizando un lector en lugar de destruirlo y volver a crearlo

### KeeperLogsReadAheadScheduleRejected

Número de veces que no se pudo crear un lector de lectura anticipada del changelog de Keeper porque la queue del pool de hilos de lectura anticipada estaba llena; el llamador recurre a una lectura directa

### KeeperLogsReadAheadTimeoutFallbacks

Número de veces que una request de servicio de lectura anticipada del changelog de Keeper superó el timeout mientras esperaba la tarea de relleno y recurrió a una lectura directa del tail restante

### KeeperLSMTCommittedEntryBytes

Bytes serializados de las entries añadidas a la memtable committed del LSMT de Keeper

### KeeperLSMTCommittedMemtablesCreated

Número de memtables con commit creadas por el LSMT de Keeper

### KeeperLSMTFileDeletionExceptions

Número de excepciones al eliminar archivos LSMT de Keeper

### KeeperLSMTFlushes

Número de flushes de memtable LSMT de Keeper completados correctamente

### KeeperLSMTFlushExceptions

Número de excepciones en los flushes de la memtable LSMT de Keeper

### KeeperLSMTFlushWrittenCompressedBytes

Bytes comprimidos escritos en archivos por los flushes de la memtable LSMT de Keeper

### KeeperLSMTFlushWrittenUncompressedBytes

Bytes sin comprimir escritos en archivos por los flushes de la memtable LSMT de Keeper

### KeeperLSMTGetBlockFromCache

Número de accesos a bloques LSMT de Keeper atendidos por la caché de bloques

### KeeperLSMTGetBlockFromWeakPtr

Número de accesos a bloques LSMT de Keeper atendidos por el puntero débil en la información de bloques del archivo

### KeeperLSMTGetBlockLoadedGroup

Número de accesos a bloques LSMT de Keeper que tuvieron que cargar un grupo de bloques desde el disco

### KeeperLSMTGetCommittedNodeFromMemory

Número de lookups de nodos committed de Keeper LSMT que encontraron el bloque del nodo ya en memoria

### KeeperLSMTGetCommittedNodeLoadedBlock

Número de lookups de nodos committed del LSMT de Keeper que tuvieron que cargar el bloque del nodo

### KeeperLSMTGetCommittedNodeNonexistent

Número de lookups de nodos committed en el LSMT de Keeper para nodos que no existen

### KeeperLSMTGetUncommittedNodeHits

Número de lookups de nodos LSMT de Keeper que encontraron el nodo en memtables sin commit

### KeeperLSMTGetUncommittedNodeMisses

Número de lookups de nodos LSMT de Keeper que no encontraron el nodo en las memtables sin commit y recurrieron al state committed

### KeeperLSMTListFilterFalsePositives

Número de escaneos de archivos no omitidos por el filtro de rutas padre del LSMT de Keeper que no encontraron hijos

### KeeperLSMTListFilterSkipped

Número de escaneos de archivos omitidos por las operaciones de listado LSMT de Keeper porque el filtro de rutas padre descartó todos los hijos

### KeeperLSMTListFilterTruePositives

Número de escaneos de archivos no omitidos por el filtro de rutas padre del LSMT de Keeper que encontraron hijos

### KeeperLSMTListNamesFromFiles

Nombres de nodos hijos (incluidas las tombstones) obtenidos de archivos mediante las operaciones de listado LSMT de Keeper

### KeeperLSMTListNamesFromMemtables

Nombres de nodos hijos (incluidas las marcas de eliminación o tombstones) obtenidos de las memtables mediante las operaciones de listado LSMT de Keeper

### KeeperLSMTListScannedBlocks

Bloques escaneados en archivos por las operaciones de listado LSMT de Keeper

### KeeperLSMTListScannedEntries

Entries escaneadas en archivos por las operaciones de listado LSMT de Keeper

### KeeperLSMTLoadedBlocks

Número de bloques cargados desde el disco por el LSMT de Keeper

### KeeperLSMTLoadedUncompressedBytes

Bytes sin comprimir de los bloques cargados desde disco por el LSMT de Keeper

### KeeperLSMTMergeConsumedFiles

Número de archivos de entrada consumidos por completo y eliminados por las fusiones LSMT de Keeper

### KeeperLSMTMergeConsumedUncompressedBytes

Bytes sin comprimir de los archivos de entrada consumidos por completo y eliminados por las fusiones LSMT de Keeper

### KeeperLSMTMergeExceptions

Número de excepciones en las fusiones LSMT de Keeper

### KeeperLSMTMerges

Número de fusiones LSMT de Keeper completadas correctamente

### KeeperLSMTMergeWrittenCompressedBytes

Bytes comprimidos en los archivos de salida publicados por las fusiones LSMT de Keeper

### KeeperLSMTMergeWrittenFiles

Número de archivos de salida publicados por las fusiones LSMT de Keeper

### KeeperLSMTMergeWrittenUncompressedBytes

Bytes sin comprimir en los archivos de salida publicados por las fusiones LSMT de Keeper

### KeeperLSMTNodeCacheEntriesUpdated

Número de entradas de la caché de nodos LSMT de Keeper que se han redirigido a un bloque recién cargado

### KeeperLSMTThrottledWrites

Número de escrituras retrasadas porque los flushes o las fusiones en segundo plano del LSMT de Keeper se quedaron rezagados

### KeeperLSMTUncommittedCreateBytes

Bytes serializados de las entradas Create añadidas a las memtables sin commit del LSMT de Keeper

### KeeperLSMTUncommittedCreates

Número de entradas Create añadidas a las memtables sin commit del LSMT de Keeper

### KeeperLSMTUncommittedMemtablesCreated

Número de memtables sin commit creadas por el LSMT de Keeper

### KeeperLSMTUncommittedRemoveBytes

Bytes serializados de las entradas Remove añadidas a las memtables sin commit del LSMT de Keeper

### KeeperLSMTUncommittedRemoves

Número de entradas Remove añadidas a las memtables sin commit del LSMT de Keeper

### KeeperLSMTUncommittedUpdateBytes

Bytes serializados de las entradas de actualización añadidas a las memtables sin commit del LSMT de Keeper

### KeeperLSMTUncommittedUpdates

Número de entradas de actualización añadidas a las memtables sin commit del LSMT de Keeper

### KeeperMultiReadRequest

Número de solicitudes de lectura múltiple

### KeeperMultiRequest

Número de solicitudes de tipo multi

### KeeperPacketsReceived

Paquetes recibidos por el servidor de Keeper

### KeeperPacketsSent

Paquetes enviados por el servidor de Keeper

### KeeperPreprocessElapsedMicroseconds

Latencia de preprocesamiento en Keeper para una sola solicitud

### KeeperProcessAndResponsesLockWaitMicroseconds

Tiempo dedicado a esperar para adquirir el bloqueo de procesamiento y respuestas de Keeper

### KeeperProcessElapsedMicroseconds

Latencia de commit en Keeper para una única solicitud

### KeeperReadBatchCount

Número de lotes de solicitudes de lectura procesados por Keeper

### KeeperReadBatchTotalRequests

Número total de solicitudes de lectura procesadas por Keeper en lotes

### KeeperReadRequestQueueLockWaitMicroseconds

Tiempo de espera para adquirir el bloqueo de la cola de solicitudes de lectura de Keeper

### KeeperReadSnapshot

Número de lecturas de snapshots completadas

### KeeperReadSnapshotDeferred

Número de lecturas de instantáneas aplazadas porque la instantánea solicitada ya no está disponible localmente; NuRaft reintentará con la instantánea más reciente.

### KeeperReadSnapshotFailed

Número de lecturas fallidas de instantáneas

### KeeperReadSnapshotObject

Número de objetos de instantánea enviados a los nodos seguidores

### KeeperReconfigRequest

Número de solicitudes de reconfiguración

### KeeperRemoveRequest

Número de solicitudes de eliminación

### KeeperRemoveWatchRequest

Número de solicitudes de eliminación de watches

### KeeperRequestRejectedDueToSoftMemoryLimitCount

Número de solicitudes rechazadas por superar el límite de memoria flexible

### KeeperRequestTotal

Número total de solicitudes del servidor Keeper

### KeeperRequestTotalWithSubrequests

Número total de solicitudes en el servidor Keeper, contando cada subsolicitud dentro de una solicitud múltiple

### KeeperSaveSnapshot

Número de operaciones de guardado de instantáneas

### KeeperSaveSnapshotFailed

Número de guardados fallidos de instantáneas

### KeeperSaveSnapshotObject

Número de objetos instantánea recibidos del leader

### KeeperServerWriteLockWaitMicroseconds

Tiempo empleado en esperar para adquirir el bloqueo de escritura del servidor Keeper

### KeeperSessionCallbackLockWaitMicroseconds

Tiempo empleado en esperar a adquirir el bloqueo de callback de la sesión de Keeper

### KeeperSetRequest

Número de solicitudes de tipo set

### KeeperSetWatchesRequest

Número de solicitudes para establecer watches

### KeeperSnapshotApplys

Número de aplicaciones de instantáneas

### KeeperSnapshotApplysFailed

Número de errores al aplicar instantáneas

### KeeperSnapshotCreations

Número de instantáneas creados

### KeeperSnapshotCreationsFailed

Número de instantáneas cuya creación falló

### KeeperSnapshotFileSyncMicroseconds

Tiempo invertido en `fsync` de los archivos de instantánea de Keeper

### Errores del cargador remoto de instantáneas de Keeper

Número de errores de lectura remota en `RemoteSnapshotLoader` al servir una instantánea a un follower

### KeeperSnapshotWrittenBytes

Número de bytes escritos en los archivos de instantánea de Keeper

### KeeperStaleRequestsSkipped

Número de solicitudes de Keeper omitidas porque la sesión ya no sigue activa

### KeeperStorageLockWaitMicroseconds

Tiempo empleado en esperar para adquirir el bloqueo de almacenamiento de Keeper

### KeeperStorageSharedLockWaitMicroseconds

Tiempo dedicado a esperar la adquisición del bloqueo compartido del almacenamiento de Keeper

### KeeperTotalElapsedMicroseconds

Latencia total de Keeper para una solicitud individual

### KeeperTTLRemoveRequestsDropped

Número de solicitudes de eliminación por TTL descartadas porque la cola estaba llena

### KeeperTTLRemoveRequestsEnqueued

Número de solicitudes de eliminación por TTL puestas en cola correctamente

### KeeperWatchesTriggered

Número de activaciones de watches

### KeeperWatchTriggeredNodeChildrenChanged

Número de activaciones de watch por operaciones de cambio en nodos hijo

### KeeperWatchTriggeredNodeCreated

Número de activaciones de watch provocadas por la operación `CREATE`

### KeeperWatchTriggeredNodeDataChanged

Número de activaciones de watch por operaciones de modificación

### KeeperWatchTriggeredNodeDeleted

Número de activaciones de watch por la operación `DELETE`

### KeeperWriteBatchCount

Número de lotes de solicitudes de escritura procesados por Keeper

### KeeperWriteBatchTotalRequests

Número total de solicitudes de escritura procesadas por Keeper en lotes

### LoadedDataParts

Número de partes de datos cargadas por las tablas MergeTree durante la inicialización.

### LoadedDataPartsMicroseconds

Microsegundos empleados por las tablas MergeTree para cargar las partes de datos durante la inicialización.

### LoadedMarksCount

Número de marcas cargadas (total en todas las columnas).

### LoadedMarksFiles

Número de archivos de marcas cargados.

### LoadedMarksMemoryBytes

Tamaño de las representaciones en memoria de las marcas cargadas.

### LoadedPrimaryIndexBytes

Número de filas de la clave primaria que se han cargado.

### LoadedPrimaryIndexFiles

Número de archivos del índice primario cargados.

### LoadedPrimaryIndexRows

Número de filas de la clave primaria que se han cargado.

### LoadedStatisticsMicroseconds

Tiempo transcurrido durante la carga de estadísticas desde las partes

### LoadingMarksTasksCanceled

Número de veces que se cancelaron las tareas en segundo plano de carga de marcas

### LocalReadThrottlerBytes

Bytes que pasaron por el limitador &#39;max&#95;local&#95;read&#95;bandwidth&#95;for&#95;server&#39;/&#39;max&#95;local&#95;read&#95;bandwidth&#39;.

### LocalReadThrottlerSleepMicroseconds

Tiempo total durante el que una consulta estuvo en espera para cumplir con la limitación de velocidad de &#39;max&#95;local&#95;read&#95;bandwidth&#95;for&#95;server&#39;/&#39;max&#95;local&#95;read&#95;bandwidth&#39;.

### LocalThreadPoolBusyMicroseconds

Tiempo total que los hilos han dedicado a ejecutar el trabajo propiamente dicho.

### LocalThreadPoolExpansions

Cuenta el número total de veces que se han tomado prestados hilos del pool global de hilos para ampliar los pools de hilos locales.

### LocalThreadPoolJobs

Cuenta la cantidad de trabajos que se han enviado a los pools de hilos locales.

### LocalThreadPoolJobWaitTimeMicroseconds

Mide el tiempo transcurrido desde que un job se programa en el pool de hilos hasta que un hilo de trabajo lo recoge para ejecutarlo. Esta métrica ayuda a identificar retrasos en el procesamiento de jobs, lo que indica la capacidad de respuesta del pool de hilos ante nuevas tareas.

### LocalThreadPoolLockWaitMicroseconds

Tiempo total que los hilos han pasado esperando bloqueos en los pools de hilos locales.

### LocalThreadPoolShrinks

Cuenta el número total de veces que los hilos se han devuelto al pool global de hilos desde los pools locales de hilos.

### LocalThreadPoolThreadCreationMicroseconds

Tiempo total que los pools locales de hilos han pasado esperando para obtener prestado un hilo del pool global.

### LocalWriteThrottlerBytes

Bytes que pasaron por el limitador &#39;max&#95;local&#95;write&#95;bandwidth&#95;for&#95;server&#39;/&#39;max&#95;local&#95;write&#95;bandwidth&#39;.

### LocalWriteThrottlerSleepMicroseconds

Tiempo total durante el que una consulta permaneció en espera para ajustarse a la limitación de ancho de banda de &#39;max&#95;local&#95;write&#95;bandwidth&#95;for&#95;server&#39;/&#39;max&#95;local&#95;write&#95;bandwidth&#39;.

### LogDebug

Número de mensajes de registro de nivel Debug

### LogError

Número de mensajes de registro de nivel Error

### LogFatal

Número de mensajes de registro de nivel Fatal

### LoggerElapsedNanoseconds

Tiempo acumulado dedicado al logging

### LogInfo

Número de mensajes de registro de nivel Info

### LogTest

Número de mensajes de registro de nivel Test

### LogTrace

Número de mensajes de registro de nivel Trace

### LogWarning

Número de mensajes de registro de nivel Warning

### MainConfigLoads

Número de veces que se recargó la configuración principal.

### MarkCacheEvictedBytes

Número de bytes desalojados de la caché de marcas.

### MarkCacheEvictedFiles

Número de archivos de marcas desalojados de la caché de marcas.

### MarkCacheEvictedMarks

Número de marcas desalojadas de la caché de marcas.

### MarkCacheHits

Número de veces que se encontró una entrada en la caché de marcas, por lo que no fue necesario cargar un archivo de marcas.

### MarkCacheMisses

Número de veces que no se ha encontrado una entrada en la caché de marcas, por lo que ha sido necesario cargar en memoria un archivo de marcas, una operación costosa que aumenta la latencia de la consulta.

### MarksTasksFromCache

Número de veces que las marcas se cargaron de forma síncrona porque ya estaban en la caché.

### MemoryAllocatedWithoutCheck

Número de veces que se ha asignado memoria sin verificar las restricciones de memoria.

### MemoryAllocatedWithoutCheckBytes

Cantidad de bytes asignados sin comprobar las restricciones de memoria.

### MemoryAllocatorPurge

Número total de veces que se solicitó la purga del asignador de memoria

### MemoryAllocatorPurgeTimeMicroseconds

Tiempo total dedicado a la purga del asignador de memoria

### MemoryOvercommitWaitTimeMicroseconds

Tiempo total dedicado a esperar a que se libere memoria en OvercommitTracker.

### MemoryReservationAdmitMicroseconds

Tiempo total que una consulta esperó para que se aprobara la reserva inicial de memoria

### Disminuciones de MemoryReservation

Número total de solicitudes aprobadas para reducir MemoryReservation

### MemoryReservationFailed

Número total de reservas de memoria fallidas

### MemoryReservationIncreaseMicroseconds

Tiempo total que una consulta esperó a que se aprobara un aumento de la reserva de memoria

### Aumentos de MemoryReservation

Número total de solicitudes aprobadas de aumento de MemoryReservation

### MemoryReservationKilled

Número total de señales de terminación de reserva de memoria

### MemoryWorkerRun

Número de ejecuciones de MemoryWorker en segundo plano

### MemoryWorkerRunElapsedMicroseconds

Tiempo total empleado por MemoryWorker en tareas en segundo plano

### Merge

Número de operaciones de fusión en segundo plano iniciadas.

### MergeCommitMilliseconds

Tiempo total dedicado a confirmar los resultados de la fusión (cambio de nombre de partes, verificación de checksum, actualizaciones de ZooKeeper)

### MergedColumns

Número de columnas fusionadas durante la etapa horizontal de las operaciones de fusión.

### MergedIntoCompactParts

Número de partes fusionadas al formato Compact.

### MergedIntoWideParts

Número de partes fusionadas al formato Wide.

### MergedProjections

Número de proyecciones fusionadas (no reconstruidas) durante las operaciones de fusión de MergeTree.

### MergedRows

Filas leídas durante las fusiones en segundo plano. Es el número de filas antes de la fusión.

### MergedUncompressedBytes

Bytes sin comprimir (de las columnas, tal como se almacenan en memoria) que se leyeron para las fusiones en segundo plano. Esta es la cantidad antes de la fusión.

### MergeExecuteMilliseconds

Tiempo total de actividad dedicado a la ejecución de las fusiones en segundo plano

### MergeHorizontalStageExecuteMilliseconds

Tiempo total de actividad dedicado a ejecutar la etapa horizontal de las fusiones en segundo plano

### MergeHorizontalStageTotalMilliseconds

Tiempo total empleado en la etapa horizontal de las fusiones en segundo plano

### MergeMutateBackgroundExecutorTaskCancelMicroseconds

Tiempo dedicado a `cancel()` en las tareas del ejecutor MergeMutate.

### MergeMutateBackgroundExecutorTaskExecuteStepMicroseconds

Tiempo empleado en executeStep() por las tareas del ejecutor en segundo plano MergeMutate.

### MergeMutateBackgroundExecutorTaskResetMicroseconds

Tiempo dedicado a reiniciar la tarea del ejecutor MergeMutate.

### MergeMutateBackgroundExecutorWaitMicroseconds

Tiempo empleado en esperar a que finalice en el ejecutor MergeMutate.

### MergePrewarmStageExecuteMilliseconds

Tiempo total de actividad dedicado a la ejecución de la etapa de precalentamiento de las fusiones en segundo plano

### MergePrewarmStageTotalMilliseconds

Tiempo total dedicado a la etapa de precalentamiento de las fusiones en segundo plano

### MergeProjectionStageExecuteMilliseconds

Tiempo total de ocupación dedicado a la ejecución de la etapa de proyección de las fusiones en segundo plano

### MergeProjectionStageTotalMilliseconds

Tiempo total empleado en la etapa de proyección de las fusiones en segundo plano

### MergerMutatorPartsInRangesForMergeCount

Cantidad de partes candidatas para fusión

### MergerMutatorPrepareRangesForMergeElapsedMicroseconds

Tiempo empleado en preparar los rangos de partes que pueden fusionarse según el predicado de fusión.

### MergerMutatorRangesForMergeCount

Número de rangos candidatos para la fusión

### MergerMutatorSelectPartsForMergeElapsedMicroseconds

Tiempo dedicado a seleccionar partes de los rangos que pueden fusionarse.

### MergerMutatorSelectRangePartsCount

Cantidad de partes en el rango seleccionado para la fusión

### MergerMutatorsGetPartsForMergeElapsedMicroseconds

Tiempo empleado en tomar una instantánea de las partes de datos para construir rangos a partir de ellas.

### MergeSourceParts

Número de partes de origen programadas para fusionarse.

### MergesRejectedByMemoryLimit

Número de fusiones en segundo plano rechazadas debido al límite de memoria

### MergesThrottlerBytes

Bytes que pasaron por el limitador &#39;max&#95;merges&#95;bandwidth&#95;for&#95;server&#39;.

### MergesThrottlerSleepMicroseconds

Tiempo total durante el cual una consulta permaneció en pausa para respetar la limitación de &#39;max&#95;merges&#95;bandwidth&#95;for&#95;server&#39;.

### MergeTextIndexStageExecuteMilliseconds

Tiempo total de actividad dedicado a ejecutar la etapa del índice de texto de las fusiones en segundo plano

### MergeTextIndexStageTotalMilliseconds

Tiempo total dedicado a la etapa del índice de texto en las merges en segundo plano

### MergeTotalMilliseconds

Tiempo total dedicado a las fusiones en segundo plano

### MergeTreeAllRangesAnnouncementsSent

El número de anuncios enviados desde el servidor remoto al servidor iniciador sobre el conjunto de partes de datos (para tablas MergeTree). Se mide en el lado del servidor remoto.

### MergeTreeAllRangesAnnouncementsSentElapsedMicroseconds

Tiempo empleado en enviar el anuncio desde el servidor remoto al servidor iniciador sobre el conjunto de partes de datos (para tablas MergeTree). Se mide en el lado del servidor remoto.

### MergeTreeDataProjectionWriterBlocks

Número de bloques insertados en la proyección de las tablas MergeTree. Cada bloque forma una parte de datos de nivel cero.

### MergeTreeDataProjectionWriterBlocksAlreadySorted

Número de bloques INSERTados en la proyección de tablas MergeTree que ya parecían estar ordenados.

### MergeTreeDataProjectionWriterCompressedBytes

Bytes escritos en el sistema de archivos para los datos INSERTados en la proyección de las tablas MergeTree.

### MergeTreeDataProjectionWriterMergingBlocksMicroseconds

Tiempo empleado en la fusión de bloques

### MergeTreeDataProjectionWriterRows

Número de filas INSERTadas en la proyección de las tablas MergeTree.

### MergeTreeDataProjectionWriterSortingBlocksMicroseconds

Tiempo empleado en ordenar bloques (en la proyección, puede ser una clave distinta de la clave de ordenación de la tabla)

### MergeTreeDataProjectionWriterUncompressedBytes

Bytes sin comprimir (de las columnas, tal como se almacenan en memoria) INSERTados en la proyección de las tablas MergeTree.

### MergeTreeDataWriterBlocks

Número de bloques insertados en tablas MergeTree. Cada bloque forma una parte de datos de nivel cero.

### MergeTreeDataWriterBlocksAlreadySorted

Número de bloques insertados en tablas MergeTree que ya parecían estar ordenados.

### MergeTreeDataWriterCompressedBytes

Bytes escritos en el sistema de archivos para los datos insertados en tablas MergeTree.

### MergeTreeDataWriterMergingBlocksMicroseconds

Tiempo empleado en fusionar los bloques de entrada (para motores MergeTree especiales)

### MergeTreeDataWriterProjectionsCalculationMicroseconds

Tiempo empleado en calcular proyecciones

### MergeTreeDataWriterRows

Número de filas insertadas en tablas MergeTree.

### MergeTreeDataWriterSkipIndicesCalculationMicroseconds

Tiempo dedicado al cálculo de los índices de salto

### MergeTreeDataWriterSortingBlocksMicroseconds

Tiempo empleado en ordenar bloques

### MergeTreeDataWriterStatisticsCalculationMicroseconds

Tiempo empleado en calcular estadísticas

### MergeTreeDataWriterUncompressedBytes

Bytes no comprimidos (de las columnas tal como se almacenan en memoria) INSERTados en tablas MergeTree.

### MergeTreePrefetchedReadPoolInit

Tiempo dedicado a preparar tareas en MergeTreePrefetchedReadPool

### MergeTreeReadTaskRequestsReceived

El número de callbacks solicitados al servidor remoto por el servidor iniciador para seleccionar la tarea de lectura (para tablas MergeTree). Se mide en el servidor iniciador.

### MergeTreeReadTaskRequestsSent

El número de callbacks solicitados desde el servidor remoto al servidor iniciador para elegir la tarea de lectura (para las tablas MergeTree). Se mide del lado del servidor remoto.

### MergeTreeReadTaskRequestsSentElapsedMicroseconds

Tiempo empleado en las callbacks solicitadas por el servidor remoto al servidor iniciador para elegir la tarea de lectura (para tablas MergeTree). Medido en el lado del servidor remoto.

### MergeVerticalStageExecuteMilliseconds

Tiempo total de actividad dedicado a la ejecución de la etapa vertical de las fusiones en segundo plano

### MergeVerticalStageTotalMilliseconds

Tiempo total empleado en la fase vertical de las fusiones en segundo plano

### MergeWrittenRows

Número de filas escritas durante la operación de fusión.

### MergingSortedMilliseconds

Tiempo total invertido en la fusión de columnas ordenadas

### MetadataFromKeeperBackgroundCleanupBlobs

Número de veces que una tarea en segundo plano limpió una parte vacía del layout de blob

### MetadataFromKeeperBackgroundCleanupErrors

Número de veces que se produjo un error en la tarea de limpieza en segundo plano

### MetadataFromKeeperBackgroundCleanupObjects

Número de veces que una tarea en segundo plano realizó la limpieza de un objeto eliminado antiguo

### MetadataFromKeeperBackgroundCleanupTransactions

Número de veces que una tarea en segundo plano eliminó el token de idempotencia de transacción antiguo

### MetadataFromKeeperCacheHit

Número de veces que una solicitud de metadatos de almacenamiento de objetos se atendió desde la caché sin realizar una solicitud a Keeper

### MetadataFromKeeperCacheMiss

Número de veces que una solicitud de metadatos de almacenamiento de objetos tuvo que responderse desde Keeper

### MetadataFromKeeperCacheTooManyInvalidated

Número de veces que la caché del sistema de archivos devolvió demasiadas entradas invalidadas

### MetadataFromKeeperCacheUpdateMicroseconds

Tiempo total dedicado a actualizar la caché, incluida la espera de respuestas de Keeper

### MetadataFromKeeperCleanupTransactionCommit

Número de veces que se intentó completar la transacción de metadatos para la limpieza de objetos eliminados

### MetadataFromKeeperCleanupTransactionCommitRetry

Número de reintentos de la confirmación de la transacción de metadatos para la limpieza de objetos eliminados

### MetadataFromKeeperIndividualOperations

Número de rutas leídas o escritas mediante solicitudes individuales o múltiples a Keeper

### MetadataFromKeeperIndividualOperationsMicroseconds

Tiempo empleado en solicitudes individuales o múltiples a Keeper

### Operaciones de metadatos de Keeper

Número de veces que se realizó una solicitud a Keeper

### MetadataFromKeeperReconnects

Número de veces que se volvió a conectar con Keeper

### MetadataFromKeeperTransactionCommit

Número de veces que se intentó confirmar la transacción de metadatos

### MetadataFromKeeperTransactionCommitRetry

Número de reintentos de la confirmación de la transacción de metadatos

### MetadataFromKeeperUpdateCacheOneLevel

Número de veces que se realizó una actualización de la caché para un nivel del árbol de directorios

### MetadataTransactionRollbacks

Número de transacciones de metadatos que no lograron hacer commit y se revirtieron.

### MetadataTransactionRollbacksFailed

Número de rollbacks de transacciones de metadatos que no se completaron, por lo que los metadatos conservan una parte de una transacción que se reportó como fallida.

### MMappedFileCacheHits

Número de veces que se encontró un archivo en la caché de MMap (para el `read_method` &#39;mmap&#39;), por lo que no fue necesario volver a mapearlo en memoria.

### MMappedFileCacheMisses

Número de veces que no se encontró un archivo en la caché de MMap (para el read&#95;method &#39;mmap&#39;), por lo que hubo que volver a mapearlo en memoria con mmap.

### MoveBackgroundExecutorTaskCancelMicroseconds

Tiempo dedicado a cancel() en las tareas del ejecutor Move.

### MoveBackgroundExecutorTaskExecuteStepMicroseconds

Tiempo empleado en `executeStep()` por las tareas del ejecutor Move.

### MoveBackgroundExecutorTaskResetMicroseconds

Tiempo dedicado a reiniciar la tarea del ejecutor Move.

### MoveBackgroundExecutorWaitMicroseconds

Tiempo de espera hasta la finalización en el ejecutor Move.

### MutatedRows

Filas leídas durante las mutaciones. Este es el número de filas antes de la mutación

### MutatedUncompressedBytes

Bytes sin comprimir (de las columnas tal como estaban almacenadas en memoria) que se leyeron para las mutaciones. Este es el valor antes de la mutación.

### MutateTaskProjectionsCalculationMicroseconds

Tiempo empleado en calcular las proyecciones en las mutaciones

### MutationAffectedRowsUpperBound

El límite superior de la cantidad de filas afectadas por una mutación (p. ej., la cantidad de filas que cumplen el predicado de una mutación `UPDATE` o `DELETE`). La cantidad real puede ser ligeramente menor

### MutationAllPartColumns

Número de veces que se creó una tarea para aplicar una mutación a todas las columnas de una parte

### MutationCommitMilliseconds

Tiempo total empleado en confirmar los resultados de la mutación (renombrado de partes, verificación de checksum, actualizaciones de ZooKeeper)

### MutationCreatedEmptyParts

Número total de partes reemplazadas por partes vacías en lugar de ejecutar la mutación

### MutationExecuteMilliseconds

Tiempo total en actividad dedicado a la ejecución de mutaciones.

### MutationsAppliedOnFlyInAllReadTasks

Número total de mutaciones aplicadas sobre la marcha en todas las tareas de lectura

### MutationSomePartColumns

Número de veces que se creó una tarea para aplicar una mutación a algunas columnas de una parte

### MutationsThrottlerBytes

Bytes que pasaron por el limitador de ancho de banda &#39;max&#95;mutations&#95;bandwidth&#95;for&#95;server&#39;.

### MutationsThrottlerSleepMicroseconds

Tiempo total durante el que una consulta permaneció en espera para cumplir con la limitación de velocidad de `max_mutations_bandwidth_for_server`.

### MutationTotalMilliseconds

Tiempo total invertido en las mutaciones.

### MutationTotalParts

Número total de partes en las que se intentó aplicar mutaciones

### MutationUntouchedParts

Número total de partes en las que se intentó aplicar mutaciones, pero cuya aplicación se omitió por completo según el predicado

### NativeProtocolSend

Número de vaciados no vacíos del búfer de salida del protocolo nativo.

### NetworkReceiveBytes

Número total de bytes recibidos a través de la red. Solo se incluye la interacción de red relacionada con ClickHouse, no la realizada por bibliotecas de terceros.

### NetworkReceiveElapsedMicroseconds

Tiempo total dedicado a esperar la recepción de datos o a recibir datos a través de la red. Solo se incluye la interacción de red relacionada con ClickHouse, no la de bibliotecas de terceros.

### NetworkSendBytes

Número total de bytes enviados a través de la red. Solo se incluye la actividad de red relacionada con ClickHouse, no la realizada por bibliotecas de terceros.

### NetworkSendElapsedMicroseconds

Tiempo total dedicado a esperar a que los datos se envíen por la red o a enviarlos por la red. Solo se incluye la interacción de red relacionada con ClickHouse, no la de bibliotecas de terceros.

### NotCreatedLogEntryForMerge

La entrada del log para fusionar partes en ReplicatedMergeTree no se crea debido a una actualización concurrente del log por parte de otra réplica.

### NotCreatedLogEntryForMutation

No se crea la entrada de registro para aplicar una mutación a las partes en ReplicatedMergeTree debido a una actualización concurrente del registro por otra réplica.

### ObjectStorageGlobFilteredObjects

Objetos que no coincidieron con el patrón glob o regex y se omitieron durante el listado.

### ObjectStorageListedObjects

Número total de objetos devueltos por la API de listado del almacenamiento de objetos antes de aplicar cualquier filtro.

### ObjectStoragePredicateFilteredObjects

Objetos descartados por el filtrado mediante predicados en las columnas virtuales &#95;path/&#95;file.

### ObjectStorageQueueBucketLockLostOwnership

Número de veces que se detectó la pérdida de propiedad de un bucket lock en S3(Azure)Queue. Un valor distinto de cero indica que persistent&#95;processing&#95;node&#95;ttl&#95;seconds es demasiado pequeño o que existe un bug

### ObjectStorageQueueBucketLockRefreshes

Número de refreshes exitosos de bucket locks en S3(Azure)Queue

### ObjectStorageQueueCancelledFiles

Número de archivos cancelados en StorageS3(Azure)Queue

### ObjectStorageQueueCleanupMaxSetSizeOrTTLMicroseconds

Tiempo dedicado a marcar el archivo como Failed

### ObjectStorageQueueCommitRequests

Número de solicitudes a Keeper para marcar archivos como fallidos o procesados

### ObjectStorageQueueExceptionsDuringInsert

Número de excepciones durante la inserción en S3(Azure)Queue

### ObjectStorageQueueExceptionsDuringRead

Número de excepciones durante la lectura en S3(Azure)Queue

### ObjectStorageQueueExclusiveModeProcessingErrors

Número de veces que un archivo en S3(Azure)Queue no finalizó su state debido a un error de processing. Solo para el mode &#39;exclusive&#39;.

### ObjectStorageQueueFailedFiles

Número de archivos cuyo procesamiento falló

### ObjectStorageQueueFailedToBatchSetProcessing

Número de veces que falló la solicitud de procesamiento por lotes de Set

### ObjectStorageQueueFilteredFiles

Número de archivos filtrados en StorageS3(Azure)Queue

### ObjectStorageQueueInsertIterations

Número de iteraciones de inserción

### ObjectStorageQueueListedFiles

Número de archivos enumerados en StorageS3(Azure)Queue

### ObjectStorageQueueMovedObjects

Número de objetos movidos cuando after&#95;processing = move

### ObjectStorageQueueProcessedFiles

Número de archivos procesados

### ObjectStorageQueueProcessedRows

Número de filas procesadas en StorageS3(Azure)Queue

### ObjectStorageQueuePullMicroseconds

Tiempo empleado en leer los datos del archivo

### ObjectStorageQueueReadBytes

Número de bytes leídos (no es igual al número de bytes realmente insertados)

### ObjectStorageQueueReadFiles

Número de archivos leídos (no coincide con el número de archivos realmente insertados)

### ObjectStorageQueueReadRows

Número de filas leídas (no coincide con el número de filas realmente insertadas)

### ObjectStorageQueueRemovedObjects

Número de objetos eliminados cuando after&#95;processing = delete

### ObjectStorageQueueRemoveObjectFailures

Número de objetos que no se pudieron eliminar como parte de after&#95;processing = delete

### ObjectStorageQueueSuccessfulCommits

Número de commits de Keeper exitosos

### ObjectStorageQueueTaggedObjects

Número de objetos marcados como parte de after&#95;processing = tag

### ObjectStorageQueueTrySetProcessingFailed

La cantidad de veces que no pudimos marcar el archivo como processing

### ObjectStorageQueueTrySetProcessingRequests

El número de veces que intentamos realizar una solicitud para establecer el estado de processing

### ObjectStorageQueueTrySetProcessingSucceeded

La cantidad de veces que marcamos correctamente el archivo como processing

### ObjectStorageQueueUnsuccessfulCommits

Número de commits fallidos en Keeper

### ObjectStorageReadObjects

Objetos que la fuente de almacenamiento de objetos abrió realmente para su lectura.

### ObsoleteReplicatedParts

Número de veces que una parte de datos quedó cubierta por otra parte de datos recuperada de una réplica (es decir, marcamos como obsoleta una parte de datos cubierta que ya no era necesaria).

### OneLakeAccessTokenExpirations

Número de veces que el token de acceso de OneLake almacenado en caché se detectó como expirado y se renovó de forma transparente.

### OneLakeAccessTokenRequestFailures

Número de solicitudes fallidas de tokens de acceso emitidas por el catálogo de OneLake mediante el grant de refresh token de Entra ID (incluidos los refresh tokens expirados o revocados).

### OneLakeAccessTokenRequestMicroseconds

Tiempo total empleado en solicitar tokens de acceso mediante el grant de refresh token de Entra ID en el catálogo de OneLake.

### OneLakeAccessTokenRequests

Número de solicitudes de token de acceso emitidas por el catálogo de OneLake mediante el grant de refresh token de Entra ID.

### OpenedFileCacheHits

Número de veces que se encontró un archivo en la caché de archivos abiertos, por lo que no fue necesario volver a abrirlo.

### OpenedFileCacheMicroseconds

Cantidad de tiempo dedicada a la ejecución de métodos de OpenedFileCache.

### Fallos de la caché de archivos abiertos

Número de veces que no se encontró un archivo en la caché de archivos abiertos, por lo que hubo que abrirlo de nuevo.

### OSCPUVirtualTimeMicroseconds

Tiempo de CPU consumido según lo percibe el sistema operativo. No incluye esperas involuntarias debidas a la virtualización.

### OSCPUWaitMicroseconds

Tiempo total durante el cual un hilo estaba listo para ejecutarse, pero esperando a que el SO le asignara tiempo de CPU, desde el punto de vista del SO.

### OSIOWaitMicroseconds

Tiempo total que un hilo pasó esperando el resultado de una operación de IO, desde el punto de vista del sistema operativo. Se trata de IO real, sin incluir la caché de páginas.

### OSReadBytes

Número de bytes leídos de discos o dispositivos de bloque. No incluye los bytes leídos de la caché de páginas. Puede incluir una cantidad excesiva de datos debido al tamaño del bloque, la lectura anticipada, etc.

### OSReadChars

Número de bytes leídos del sistema de archivos, incluida la caché de páginas, así como de la red y de otros archivos.

### OSWriteBytes

Número de bytes escritos en discos o dispositivos de bloque. No incluye los bytes contenidos en páginas sucias de la caché de páginas. Es posible que no incluya los datos escritos de forma asíncrona por el sistema operativo.

### OSWriteChars

Número de bytes escritos en el sistema de archivos, incluida la caché de páginas, así como en la red y en otros archivos.

### OtherQueryTimeMicroseconds

Tiempo total de las consultas que no son ni SELECT ni INSERT.

### OverflowAny

Número de veces que se aplicó el GROUP BY aproximado: cuando la agregación se realizó solo sobre las primeras &#39;max&#95;rows&#95;to&#95;group&#95;by&#39; claves únicas y se ignoraron las demás debido a &#39;group&#95;by&#95;overflow&#95;mode&#39; = &#39;any&#39;.

### OverflowBreak

Número de veces que se canceló el procesamiento de datos debido a la limitación de complejidad de la consulta con la configuración &#39;*&#95;overflow&#95;mode&#39; = &#39;break&#39;, y el resultado quedó incompleto.

### OverflowThrow

Número de veces que se canceló el procesamiento de datos debido a una limitación de complejidad de la consulta con la configuración &#39;*&#95;overflow&#95;mode&#39; = &#39;throw&#39; y se lanzó una excepción.

### PageCacheHits

Número de veces que se encontró un bloque de datos en la caché de páginas en espacio de usuario.

### PageCacheMisses

Número de veces que no se encontró un bloque de datos en la caché de páginas en espacio de usuario.

### PageCacheOvercommitResize

Número de veces que la caché de páginas en espacio de usuario se redimensionó automáticamente para liberar memoria al asignar memoria.

### PageCacheReadBytes

Número de bytes leídos de la caché de páginas en espacio de usuario.

### PageCacheResized

Número de veces que la caché de páginas en espacio de usuario se redimensionó automáticamente (suele ocurrir unas pocas veces por segundo y está controlado por memory&#95;worker&#95;period&#95;ms).

### PageCacheWeightLost

Número de bytes desalojados de la caché de páginas en espacio de usuario

### PaimonMetadataFilesCacheHits

Número de veces que se han encontrado en la caché los archivos de metadatos de paimon.

### PaimonMetadataFilesCacheMisses

Número de veces que los archivos de metadatos de paimon no se encontraron en la caché de metadatos de Paimon y tuvieron que leerse desde el disco (remoto).

### PaimonMetadataFilesCacheWeightLost

Número aproximado de bytes expulsados de la caché de metadatos de paimon.

### ParallelReplicasAnnouncementMicroseconds

Tiempo empleado en enviar un anuncio

### ParallelReplicasAvailableCount

Número de réplicas disponibles para ejecutar una consulta con réplicas paralelas basadas en tareas

### ParallelReplicasCollectingOwnedSegmentsMicroseconds

Tiempo dedicado a recopilar los segmentos asignados por hash

### ParallelReplicasDeniedRequests

Número de solicitudes completamente denegadas al nodo iniciador

### ParallelReplicasHandleAnnouncementMicroseconds

Tiempo dedicado a procesar anuncios de réplicas

### ParallelReplicasHandleRequestMicroseconds

Tiempo dedicado a procesar solicitudes de marcas procedentes de réplicas

### ParallelReplicasNumRequests

Número de solicitudes enviadas al iniciador.

### ParallelReplicasProcessingPartsMicroseconds

Tiempo dedicado a procesar las partes de datos

### ParallelReplicasQueryCount

Número de (sub)consultas ejecutadas con réplicas paralelas durante la ejecución de una consulta

### ParallelReplicasReadAssignedForStealingMarks

Suma de todas las réplicas de cuántas marcas programadas se asignaron para robo de trabajo mediante hash consistente

### ParallelReplicasReadAssignedMarks

Suma, en todas las réplicas, de cuántas marcas programadas se asignaron mediante hash consistente

### ParallelReplicasReadMarks

Cuántas marcas leyó la réplica indicada

### ParallelReplicasReadRequestMicroseconds

Tiempo empleado en las solicitudes de lectura

### ParallelReplicasReadUnassignedMarks

Suma, en todas las réplicas, de cuántas marcas no asignadas se programaron

### ParallelReplicasStealingByHashMicroseconds

Tiempo dedicado a recopilar segmentos destinados al robo por hash

### ParallelReplicasStealingLeftoversMicroseconds

Tiempo dedicado a recoger segmentos huérfanos

### ParallelReplicasUnavailableCount

Número de réplicas que se eligieron, pero que se encontraron no disponibles durante la ejecución de la consulta con réplicas paralelas basadas en tareas

### ParallelReplicasUsedCount

Número de réplicas usadas para ejecutar una consulta con réplicas paralelas basadas en tareas

### ParquetColumnsFilterExpression

El número total de columnas que pasaron por el filtro

### ParquetDecodingTaskBatches

Grupos de tareas que el lector de Parquet envía a un grupo de hilos

### ParquetDecodingTasks

Tareas generadas por el lector de Parquet

### ParquetFetchWaitTimeMicroseconds

Tiempo de espera de las lecturas de archivos Parquet por parte de los hilos de decodificación (no de los hilos de precarga)

### ParquetMetadataCacheHits

Número de veces que se han encontrado metadatos de Parquet en la caché.

### ParquetMetadataCacheMisses

Número de veces que no se encontraron metadatos de Parquet en la caché y fue necesario leerlos del disco.

### ParquetMetadataCacheWeightLost

Número aproximado de bytes eliminados de la caché de metadatos de Parquet.

### ParquetPrefetcherReadEntireFile

El número total de lecturas realizadas con ReadMode::EntireFileIsInMemory por DB::Parquet::Prefetcher

### ParquetPrefetcherReadRandomRead

El número total de lecturas con ReadMode::RandomRead realizadas por DB::Parquet::Prefetcher

### ParquetPrefetcherReadSeekAndRead

El número total de lecturas en modo ReadMode::SeekAndRead realizadas por DB::Parquet::Prefetcher

### ParquetPrunedPages

El número total de pages descartadas de los datos de Parquet mediante el índice de columna

### ParquetPrunedRowGroups

El número total de grupos de filas descartados en los datos Parquet

### ParquetReadPages

Número total de pages de datos Parquet leídas

### ParquetReadRowGroups

El número total de grupos de filas leídos de datos en formato Parquet

### ParquetRowsFilterExpression

El número total de filas que pasaron a través del filtro

### PartsLockHoldMicroseconds

Tiempo total durante el que se mantuvo el bloqueo de las partes de datos en tablas MergeTree

### PartsLocks

Número de veces que se ha adquirido el bloqueo de las partes de datos en tablas MergeTree

### PartsLockWaitMicroseconds

Tiempo total de espera por el bloqueo de las partes de datos en las tablas MergeTree

### PatchesAcquireLockBadVersionRetries

Número total de reintentos al adquirir el bloqueo para actualizaciones ligeras, provocados por una actualización concurrente que hizo commit primero

### PatchesAcquireLockMicroseconds

Número total de microsegundos dedicados a adquirir el bloqueo para ejecutar actualizaciones ligeras

### PatchesAcquireLockTries

Número total de intentos para adquirir el bloqueo al ejecutar actualizaciones ligeras

### PatchesAppliedInAllReadTasks

Número total de partes de parche aplicadas en todas las tareas de lectura

### PatchesJoinAppliedInAllReadTasks

Número total de partes de parche aplicadas en modo Join en todas las tareas de lectura

### Filas añadidas a tablas hash en joins de Patches

Número total de filas añadidas a tablas hash al aplicar partes de parche en modo Join

### PatchesMergeAppliedInAllReadTasks

Número total de partes de parche aplicadas en modo Merge en todas las tareas de lectura

### PatchesMergeOnKeyAppliedInAllReadTasks

Número total de partes de parche aplicadas con el modo MergeOnKey (v2) entre todas las tareas de lectura

### PatchesReadRows

Número total de filas leídas desde partes de parche

### PatchesReadUncompressedBytes

Número total de bytes sin comprimir leídos desde partes de parche

### PerfAlignmentFaults

Número de fallos de alineación. Se producen cuando hay accesos a memoria no alineados; el kernel puede gestionarlos, pero esto reduce el rendimiento. Esto solo ocurre en algunas arquitecturas (nunca en x86).

### PerfBranchInstructions

Instrucciones de bifurcación completadas. Antes de Linux 2.6.35, esto usaba el evento incorrecto en los procesadores AMD.

### PerfBranchMisses

Instrucciones de salto mal predichas.

### Ciclos del bus

Ciclos del bus, que pueden diferir del total de ciclos.

### PerfCacheMisses

Fallos de caché. Normalmente, esto indica fallos en la caché de último nivel; se utiliza junto con el evento PERFCOUNTHWCACHEREFERENCES para calcular las tasas de fallos de caché.

### Referencias de caché de rendimiento

Accesos a la caché. Normalmente, esto indica accesos a la caché de último nivel, aunque puede variar según la CPU. Puede incluir precargas y mensajes de coherencia; de nuevo, esto depende del diseño de la CPU.

### PerfContextSwitches

Número de cambios de contexto

### PerfCPUClock

El reloj de la CPU, un temporizador de alta resolución para cada CPU

### PerfCPUCycles

Ciclos totales. Tenga en cuenta lo que ocurre durante el escalado de frecuencia de la CPU.

### PerfCPUMigrations

Número de veces que el proceso ha migrado a otra CPU

### PerfDataTLBMisses

Fallos de la TLB de datos

### PerfDataTLBReferences

Referencias al TLB de datos

### PerfEmulationFaults

Número de fallos de emulación. El kernel a veces detecta instrucciones no implementadas y las emula para el espacio de usuario. Esto puede afectar negativamente al rendimiento.

### PerfInstructions

Instrucciones obsoletas. Tenga en cuenta que pueden verse afectadas por diversos factores, especialmente por los recuentos de interrupciones de hardware.

### Fallos de TLB de instrucciones

Fallos de TLB de instrucciones

### Referencias del TLB de instrucciones

Referencias del TLB de instrucciones

### PerfLocalMemoryMisses

Fallos de lectura en la memoria del nodo NUMA local

### PerfLocalMemoryReferences

Lecturas de memoria en el nodo NUMA local

### PerfMinEnabledRunningTime

Tiempo de ejecución del evento con el menor tiempo de habilitación. Se utiliza para hacer seguimiento del grado de multiplexación de eventos

### PerfMinEnabledTime

Para todos los eventos, tiempo mínimo en el que un evento estuvo habilitado. Se utiliza para hacer seguimiento de la influencia de la multiplexación de eventos

### PerfRefCPUCycles

Ciclos totales; no se ven afectados por los cambios en la frecuencia de la CPU.

### PerfStalledCyclesBackend

Ciclos bloqueados durante la retirada de instrucciones.

### PerfStalledCyclesFrontend

Ciclos bloqueados durante la emisión.

### PerfTaskClock

Un recuento de reloj específico de la tarea en ejecución

### PreferredWarmedUnmergedParts

Consulte la configuración `prefer_warmed_unmerged_parts_seconds`. Número de veces que las consultas de lectura usaron partes desactualizadas anteriores a la fusión almacenadas en caché, en lugar de la parte fusionada que CacheWarmer aún no había incorporado a la caché.

### PrimaryIndexCacheHits

Número de veces que se encontró una entrada en la caché del índice primario, por lo que no fue necesario cargar un archivo de índice.

### PrimaryIndexCacheMisses

Número de veces que no se encontró una entrada en la caché del índice primario, por lo que fue necesario cargar un archivo de índice en memoria, una operación costosa que aumenta la latencia de la consulta.

### QueriesWithSubqueries

Número de consultas con todas las subconsultas

### Consulta

Número de consultas que se interpretan y que potencialmente pueden ejecutarse. No incluye las consultas cuyo análisis falló ni las que se rechazaron debido a límites de tamaño del AST, límites de cuota o límites en el número de consultas que se ejecutan simultáneamente. Puede incluir consultas internas iniciadas por el propio ClickHouse. No cuenta las subconsultas.

### QueryAnalysisMicroseconds

Tiempo total empleado en construir el árbol de consulta y ejecutar las pasadas del analizador (resuelve identificadores, tipos y expresiones).

### QueryBackupThrottlerBytes

Bytes que pasaron por el limitador `max_backup_bandwidth`.

### QueryBackupThrottlerSleepMicroseconds

Tiempo total que una consulta permaneció en espera para cumplir con la limitación de ancho de banda de &#39;max&#95;backup&#95;bandwidth&#39;.

### QueryCacheAgeSeconds

La suma de la antigüedad de las entradas encontradas en la caché de consultas, en segundos. El valor se establece tanto para aciertos como para fallos.

### QueryCacheHits

Número de veces que se ha encontrado el resultado de una consulta en la caché de consultas (y se ha evitado calcular la consulta). Solo se actualiza para consultas SELECT con SETTING use&#95;query&#95;cache = 1.

### QueryCacheMisses

Número de veces que no se encontró el resultado de una consulta en la caché de consultas (y fue necesario calcular la consulta). Solo se actualiza para consultas SELECT con SETTING use&#95;query&#95;cache = 1.

### QueryCacheReadBytes

La cantidad de bytes (sin comprimir) leídos de la caché de consultas.

### QueryCacheReadRows

El número de filas leídas de la caché de consultas.

### QueryCacheWrittenBytes

La cantidad de bytes (sin comprimir) guardados en la caché de consultas

### QueryCacheWrittenRows

El número de filas almacenadas en la caché de consultas.

### QueryConditionCacheHits

Número de veces que se ha encontrado una entrada en la caché de condiciones de consulta (por lo que puede omitirse la lectura de marcas). Solo se actualiza para consultas SELECT con SETTING use&#95;query&#95;condition&#95;cache = 1.

### QueryConditionCacheMisses

Número de veces que no se ha encontrado una entrada en la caché de condiciones de consulta (y no puede omitirse la lectura de la marca). Solo se actualiza para consultas SELECT con SETTING use&#95;query&#95;condition&#95;cache = 1.

### QueryLocalReadThrottlerBytes

Bytes que pasaron por el limitador &#39;max&#95;local&#95;read&#95;bandwidth&#39;.

### QueryLocalReadThrottlerSleepMicroseconds

Tiempo total que una consulta permaneció en espera para ajustarse a la limitación de tasa de &#39;max&#95;local&#95;read&#95;bandwidth&#39;.

### QueryLocalWriteThrottlerBytes

Bytes que pasaron por el limitador &#39;max&#95;local&#95;write&#95;bandwidth&#39;.

### QueryLocalWriteThrottlerSleepMicroseconds

Tiempo total que una consulta permaneció en espera para ajustarse a la limitación de ancho de banda de escritura local de &#39;max&#95;local&#95;write&#95;bandwidth&#39;.

### QueryMaskingRulesMatch

Número de veces que se encontró una coincidencia con las reglas de enmascaramiento de consultas.

### QueryMemoryLimitExceeded

Número de veces que se superó el límite de memoria de la consulta.

### QueryParseMicroseconds

Tiempo total empleado en el análisis del texto de la consulta para convertirlo en un AST.

### QueryPipelineBuildMicroseconds

Tiempo total empleado en la construcción de QueryPipeline a partir del QueryPlan optimizado.

### QueryPlanBuildMicroseconds

Tiempo total empleado en la construcción de QueryPlan a partir del árbol de consulta analizado.

### QueryPlanOptimizeMicroseconds

Tiempo total dedicado a ejecutar optimizaciones del plan de consulta.

### QueryPreempted

Cuántas veces las tareas quedan en pausa y en espera debido a la configuración &#39;priority&#39;

### QueryProfilerConcurrencyOverruns

Número de veces que se descarta el procesamiento de una señal de QueryProfiler debido a que hay demasiados QueryProfiler concurrentes en otros hilos, lo que puede indicar sobrecarga.

### QueryProfilerErrors

Accesos inválidos a memoria durante el desenrollado asíncrono de la pila.

### QueryProfilerRuns

Número de veces que se ejecutó QueryProfiler.

### QueryProfilerSignalOverruns

Número de veces que se descarta el procesamiento de una señal de QueryProfiler por saturación, más el número de señales que el SO no ha entregado por saturación.

### QueryRemoteReadThrottlerBytes

Bytes que pasaron por el limitador &#39;max&#95;remote&#95;read&#95;network&#95;bandwidth&#39;.

### QueryRemoteReadThrottlerSleepMicroseconds

Tiempo total que una consulta permaneció en espera para cumplir con la limitación de velocidad de &#39;max&#95;remote&#95;read&#95;network&#95;bandwidth&#39;.

### QueryRemoteWriteThrottlerBytes

Bytes que pasaron por el limitador &#39;max&#95;remote&#95;write&#95;network&#95;bandwidth&#39;.

### QueryRemoteWriteThrottlerSleepMicroseconds

Tiempo total que una consulta permaneció en espera para cumplir con la limitación de ancho de banda de &#39;max&#95;remote&#95;write&#95;network&#95;bandwidth&#39;.

### QueryTimeMicroseconds

Tiempo total de todas las consultas.

### QuorumFailedInserts

Número de inserciones fallidas porque no se alcanzó el quórum.

### QuorumParts

Número de partes de datos escritas con cuórum. Cuenta como una parte para un `insert` síncrono y puede llegar hasta el recuento de `async inserts` para un `insert` que realiza el vaciado de `async inserts`.

### QuorumWaitMicroseconds

Tiempo total empleado en esperar el cuórum durante las inserciones.

### QuotaCacheRecalculationMicroseconds

Tiempo total empleado en volver a seleccionar cuotas para todos los conjuntos activos habilitados (mientras se mantiene el mutex de la caché, que también utiliza la ruta de construcción de ContextAccess).

### QuotaCacheRecalculations

Número de veces que la caché de cuotas volvió a seleccionar cuotas para todos los conjuntos activos habilitados. Se consolida en una sola vez por cada lote de notificaciones de entidades de acceso.

### ReadBackoff

Número de veces que se redujo la cantidad de hilos de procesamiento de consultas debido a lecturas lentas.

### ReadBufferFromAzureBytes

Bytes leídos de Azure.

### ReadBufferFromAzureInitMicroseconds

Tiempo empleado en inicializar la conexión con Azure.

### ReadBufferFromAzureMicroseconds

Tiempo dedicado a leer desde Azure.

### ReadBufferFromAzureRequestsErrors

Número de excepciones al leer desde Azure

### ReadBufferFromFileDescriptorRead

Número de operaciones de lectura (read/pread) desde un descriptor de archivo. No incluye sockets.

### ReadBufferFromFileDescriptorReadBytes

Número de bytes leídos desde descriptores de archivo. Si el archivo está comprimido, se mostrará el tamaño de los datos comprimidos.

### ReadBufferFromFileDescriptorReadFailed

Número de veces que han fallado las operaciones de lectura (`read`/`pread`) de un descriptor de archivo.

### ReadBufferFromS3Bytes

Bytes leídos de S3.

### ReadBufferFromS3InitMicroseconds

Tiempo empleado en inicializar la conexión a S3.

### ReadBufferFromS3Microseconds

Tiempo empleado en leer desde S3.

### ReadBufferFromS3RequestsErrors

Número de excepciones al leer de S3.

### ReadBufferSeekCancelConnection

Número de operaciones seek que generan una nueva conexión (S3, HTTP)

### ReadCompressedBytes

Número de bytes (antes de la descompresión) leídos desde fuentes comprimidas (archivos, red).

### ReaderExecutorBytesFromSource

Bytes físicos que ReaderExecutor envió al origen después de no encontrar datos en ningún nivel de caché (precarga en primer plano y en segundo plano, incluidos los bytes de precarga desperdiciados por un descarte posterior); no incluye los bytes servidos por el consumer; consulte ReaderExecutorDeliveredBytes.

### ReaderExecutorCacheGetRequests

Número de llamadas a ICacheHandle::get en ReaderExecutor. Será cero hasta que se introduzcan los niveles de caché de ReaderExecutor.

### Solicitudes de poblado de caché de ReaderExecutor

Número de invocaciones a ICacheHandle::put en ReaderExecutor. Es cero hasta que se introduzcan los niveles de caché de ReaderExecutor.

### ReaderExecutorDecryptMicroseconds

Tiempo que ReaderExecutor dedicó a descifrar in situ la carga útil servida (CTR, direccionable por posición). Cero para lecturas sin cifrar y en builds sin SSL.

### ReaderExecutorDeliveredBytes

Bytes útiles que ReaderExecutor entregó a las solicitudes de lectura (la carga útil de la ventana solicitada, sin incluir la lectura adicional ni la escritura de retorno en caché). Denominador del KPI modelado de costo por byte (ReaderExecutorModeledCostMicroseconds / ReaderExecutorDeliveredBytes).

### ReaderExecutorIncompleteConnections

Número de conexiones de origen que ReaderExecutor descartó antes de drenarlas hasta su límite derecho; no se pueden reutilizar en el grupo, por lo que es necesario restablecerlas. Cero hasta que se introduzca la reutilización en vivo del búfer de origen de ReaderExecutor.

### ReaderExecutorLongConnectionBytes

Total de bytes leídos mediante conexiones de origen largas.

### ReaderExecutorLongConnectionFallbacks

Número de veces que el ReaderExecutor solicitó una conexión de origen larga, pero recurrió a una lectura única porque no había ningún slot disponible.

### ReaderExecutorLongConnectionHits

Número de ventanas que ReaderExecutor atendió leyendo desde una conexión de origen larga ya abierta.

### ReaderExecutorLongConnectionOpened

Número de conexiones de origen largas abiertas por ReaderExecutor para la optimización de lectura secuencial.

### ReaderExecutorModeledCostMicroseconds

Costo de E/S modelado de las lecturas de ReaderExecutor: un KPI sintético a modo de proxy para la optimización de la ruta de lectura, NO es una latencia medida. Suma ponderada de los contadores anteriores con pesos heurísticos de S3: 30 ms por solicitud al origen + 5 ms por conexión incompleta + 20 ms por MiB transferido desde el origen (carga útil más sobrelectura) + 0,1 ms por escritura en caché + 0,05 ms por lectura desde caché. Divídalo por ReaderExecutorDeliveredBytes para obtener un costo por byte independiente de la carga. Experimental, acompaña al ReaderExecutor experimental.

### Solicitudes de origen abiertas por ReaderExecutor

Número de solicitudes abiertas en el origen por ReaderExecutor (excluye las reutilizaciones del búfer activo).

### ReaderExecutorWorkMicroseconds

Tiempo total de reloj empleado dentro de `ReaderExecutor::readNextWindow` (apertura, reposicionamiento y lectura de la ventana procesada). Contribuye directamente a la latencia de lectura de la consulta.

### ReadPatchesMicroseconds

Tiempo total dedicado a leer partes de parche

### ReadPoolRangeRefinerDroppedCuts

Cuántas veces un refinador de rangos descartó por completo los rangos recortados de una parte por un grupo de lectura de MergeTree, de modo que no se creó ninguna tarea de lectura para ellos

### ReadPoolRangeRefinerDroppedMarks

Cuántas marcas se descartaron de las tareas de lectura mediante un refinador de rangos en los grupos de lectura de MergeTree (por ejemplo, marcas completamente filtradas por un projection index)

### ReadTaskRequestsReceived

El número de callbacks solicitados desde el servidor remoto al servidor iniciador para elegir la tarea de lectura (para la table function s3Cluster y otras similares). Se mide del lado del servidor iniciador.

### ReadTaskRequestsSent

El número de callbacks solicitados desde el servidor remoto al servidor iniciador para seleccionar la tarea de lectura (para la función de tabla `s3Cluster` y similares). Se mide en el servidor remoto.

### ReadTaskRequestsSentElapsedMicroseconds

Tiempo invertido en los callbacks solicitados desde el servidor remoto al servidor iniciador para seleccionar la tarea de lectura (para la función de tabla s3Cluster y similares). Se mide en el lado del servidor remoto.

### ReadTasksWithAppliedMutationsOnFly

Número total de tareas de lectura en las que se aplicó alguna mutación al vuelo

### ReadTasksWithAppliedPatches

Número total de tareas de lectura en las que se aplicó alguna parte de parche

### ReadWriteBufferFromHTTPBytes

Tamaño total, en bytes, del payload recibido y enviado por ReadWriteBufferFromHTTP. No incluye las cabeceras HTTP.

### ReadWriteBufferFromHTTPRequestsSent

Número de solicitudes HTTP enviadas por ReadWriteBufferFromHTTP

### RealTimeMicroseconds

Tiempo total transcurrido (de reloj de pared) invertido en los hilos de procesamiento (consultas y otras tareas) (tenga en cuenta que se trata de una suma).

### RebuiltProjections

Número de proyecciones reconstruidas desde cero durante las operaciones de merge de MergeTree.

### RefreshableViewLockTableRetry

Cuántas veces un SELECT de una vista materializada actualizable tuvo que cambiar a una tabla nueva porque se había eliminado la tabla anterior

### RefreshableViewRefreshFailed

Cuántas veces falló la actualización de las vistas materializadas actualizables

### RefreshableViewRefreshSuccess

Número de veces que se actualizaron las vistas materializadas actualizables

### RefreshableViewSyncReplicaRetry

Cuántas veces falló un SELECT de una vista materializada actualizable y se reintentó una SYNC REPLICA implícita

### RefreshableViewSyncReplicaSuccess

Número de veces que un SELECT desde una vista materializada actualizable realizó un SYNC REPLICA implícito

### RegexpLocalCacheHit

Número de veces que se recuperó una expresión regular compilada de una caché local.

### RegexpLocalCacheMiss

Número de veces que no se pudo recuperar una expresión regular compilada de la caché local.

### RegexpWithMultipleNeedlesCreated

Expresiones regulares con múltiples patrones (biblioteca VectorScan) compiladas.

### RegexpWithMultipleNeedlesGlobalCacheHit

Número de veces que se recuperó de la caché global una expresión regular compilada con múltiples patrones (biblioteca VectorScan).

### RegexpWithMultipleNeedlesGlobalCacheMiss

Número de veces que no se pudo recuperar de la caché global una expresión regular compilada con múltiples patrones (biblioteca VectorScan).

### RejectedInserts

Número de veces que se rechazó el INSERT de un bloque en una tabla MergeTree con la excepción «Too many parts», debido al elevado número de partes de datos activas en la partición.

### RejectedLightweightUpdates

Número de veces que se rechazó la actualización ligera debido a un exceso de bytes sin comprimir en patches.

### RejectedMutations

Número de veces que se rechazó la mutación de una tabla MergeTree con la excepción &#39;Too many mutations&#39; debido al elevado número de mutaciones pendientes en la tabla.

### RemoteFSBuffers

Número de búferes creados para la lectura asíncrona desde un sistema de archivos remoto

### RemoteFSCancelledPrefetches

Número de precargas canceladas (por un seek)

### RemoteFSLazySeeks

Número de operaciones seek diferidas

### RemoteFSPrefetchedBytes

Número de bytes del búfer precargado

### RemoteFSPrefetchedReads

Número de lecturas del búfer precargado

### RemoteFSPrefetches

Número de precargas realizadas mediante lectura asíncrona desde el sistema de archivos remoto

### RemoteFSSeeks

Número total de operaciones de seek del búfer asíncrono

### RemoteFSSeeksWithReset

Número de operaciones de seek que provocan una nueva conexión

### RemoteFSUnprefetchedBytes

Número de bytes del búfer no precargado

### RemoteFSUnprefetchedReads

Número de lecturas desde un búfer no precargado

### RemoteFSUnusedPrefetches

Número de precargas pendientes al destruirse el búfer

### RemoteReadThrottlerBytes

Bytes transferidos a través del limitador &#39;max&#95;remote&#95;read&#95;network&#95;bandwidth&#95;for&#95;server&#39;/&#39;max&#95;remote&#95;read&#95;network&#95;bandwidth&#39;.

### RemoteReadThrottlerSleepMicroseconds

Tiempo total durante el que una consulta permaneció en espera para cumplir con la limitación de velocidad de &#39;max&#95;remote&#95;read&#95;network&#95;bandwidth&#95;for&#95;server&#39;/&#39;max&#95;remote&#95;read&#95;network&#95;bandwidth&#39;.

### RemoteWriteThrottlerBytes

Bytes que pasaron por el limitador de ancho de banda &#39;max&#95;remote&#95;write&#95;network&#95;bandwidth&#95;for&#95;server&#39;/&#39;max&#95;remote&#95;write&#95;network&#95;bandwidth&#39;.

### RemoteWriteThrottlerSleepMicroseconds

Tiempo total durante el cual una consulta permaneció en espera para ajustarse a la limitación de ancho de banda de &#39;max&#95;remote&#95;write&#95;network&#95;bandwidth&#95;for&#95;server&#39;/&#39;max&#95;remote&#95;write&#95;network&#95;bandwidth&#39;.

### ReplacingSortedMilliseconds

Tiempo total dedicado al reemplazo de columnas ordenadas

### ReplicaPartialShutdown

Cuántas veces la tabla Replicated debe desinicializar su estado debido al vencimiento de la sesión en ZooKeeper. El estado se reinicializa cada vez que ZooKeeper vuelve a estar disponible.

### ReplicatedCoveredPartsInZooKeeperOnStart

Con fines de depuración. Número de partes en ZooKeeper que tienen una parte que las cubre, pero no existen en el disco. Se comprueba al iniciar el servidor.

### ReplicatedDataLoss

Número de veces que una parte de datos que se quería no existe en ninguna réplica (ni siquiera en réplicas que están fuera de línea en este momento). Esas partes de datos se han perdido definitivamente. Esto es normal debido a la replicación asíncrona (si la inserción con cuórum no estaba habilitada), cuando falló la réplica en la que se escribió la parte de datos y, al volver a estar en línea después del fallo, ya no contiene esa parte de datos.

### ReplicatedPartChecks

Número de veces que fue necesario realizar una búsqueda avanzada de una parte de datos en las réplicas o confirmar la necesidad de una parte de datos existente.

### ReplicatedPartChecksFailed

Número de veces que la búsqueda avanzada de una parte de datos en las réplicas no dio resultado o en que se encontró una parte inesperada y se trasladó.

### ReplicatedPartFailedFetches

Número de veces que falló la descarga de una parte de datos desde una réplica de una tabla ReplicatedMergeTree.

### ReplicatedPartFetches

Número de veces que se descargó una parte de datos desde una réplica de una tabla ReplicatedMergeTree.

### ReplicatedPartFetchesOfMerged

Número de veces que preferimos descargar una parte ya fusionada desde una réplica de una tabla ReplicatedMergeTree, en lugar de realizar la fusión nosotros mismos (normalmente preferimos hacerla nosotros mismos para ahorrar tráfico de red). Esto ocurre cuando no tenemos todas las partes de origen necesarias para realizar la fusión o cuando la parte de datos es lo bastante antigua.

### ReplicatedPartMerges

Número de veces que las partes de datos de las tablas ReplicatedMergeTree se han fusionado correctamente.

### ReplicatedPartMutations

Número de veces que las partes de datos de las tablas ReplicatedMergeTree se mutaron correctamente.

### RestorePartsSkippedBytes

Tamaño total de los archivos omitidos durante la restauración de partes

### RestorePartsSkippedFiles

Número de archivos omitidos al restaurar las partes

### RoleCacheRecalculationMicroseconds

Tiempo total dedicado a recalcular todos los conjuntos activos de roles habilitados (manteniendo el mutex de la caché, que también utiliza la ruta de construcción de ContextAccess).

### Recálculos de la RoleCache

Número de veces que la RoleCache recalculó todos los conjuntos activos habilitados. Se consolida en una sola ejecución por cada lote de notificaciones de entidades de acceso.

### RowPolicyCacheRecalculationMicroseconds

Tiempo total dedicado a volver a combinar los filtros de ROW POLICY para todos los conjuntos activos habilitados, fuera del mutex de RowPolicyCache (de modo que no bloquea la ruta de compilación de ContextAccess).

### RowPolicyCacheRecalculations

Número de veces que la caché de ROW POLICY volvió a combinar los filtros para todos los conjuntos activos habilitados. Se agrupa en una sola vez por lote de notificaciones de entidades de acceso.

### RowsReadByMainReader

Número de filas leídas de las tablas MergeTree por el lector principal (después del paso PREWHERE).

### RowsReadByPrewhereReaders

Número total de filas leídas de tablas MergeTree por lectores `prewhere`.

### RuntimeDataflowStatisticsInputBytes

Estadísticas recopiladas sobre la cantidad de bytes que leerían las réplicas si la consulta se ejecutara con réplicas paralelas

### RuntimeDataflowStatisticsOutputBytes

Estadísticas recopiladas sobre el número de bytes que las réplicas enviarían al iniciador si la consulta se ejecutara con réplicas en paralelo

### RuntimeFilterBlocksProcessed

Número de bloques procesados por los filtros en tiempo de ejecución de JOIN

### RuntimeFilterBlocksSkipped

Número de bloques omitidos por JOIN Runtime Filters sin llegar a procesarse, debido a que el filtro se desactivó dinámicamente por una baja tasa de filtrado

### RuntimeFilterBloomFilterBuildsSkipped

Número de builds de bloom filter del filtro en tiempo de ejecución de JOIN que se omitieron porque el recuento de claves del lado de build, obtenido de las STATISTICS de la tabla hash, predijo que el filter superaría la ratio máxima de set bits

### RuntimeFilterGranulesConsidered

Número de gránulos examinados para el pruning en tiempo de lectura por los JOIN Runtime Filters

### RuntimeFilterGranulesDropped

Número de gránulos descartados en el momento de la lectura por los filtros en tiempo de ejecución de JOIN

### RuntimeFilterRowsChecked

Número de filas evaluadas por JOIN Runtime Filters

### RuntimeFilterRowsPassed

Número de filas que pasaron (es decir, que no fueron filtradas por) los JOIN Runtime Filters

### RuntimeFilterRowsSkipped

Número de filas de los bloques que se omitieron mediante JOIN Runtime Filters

### RuntimeFiltersCreated

Número de JOIN Runtime Filters diferentes creados en una consulta

### RWLockAcquiredReadLocks

Número de veces que se adquirió un bloqueo de lectura (en un RWLock pesado).

### RWLockAcquiredWriteLocks

Número de veces que se adquirió un bloqueo de escritura (en un RWLock pesado).

### RWLockReadersWaitMilliseconds

Tiempo total de espera hasta adquirir un bloqueo de lectura (en un RWLock pesado).

### RWLockWritersWaitMilliseconds

Tiempo total de espera hasta adquirir un bloqueo de escritura (en un RWLock pesado).

### S3AbortMultipartUpload

Número de llamadas a AbortMultipartUpload de la API de S3.

### S3CachedCredentialsProvidersAdded

Número total de nuevos proveedores de credenciales añadidos a la caché

### S3CachedCredentialsProvidersReused

Número total de proveedores de credenciales reutilizados desde la caché

### S3Clients

Número de clientes de S3 creados.

### S3CompleteMultipartUpload

Número de llamadas a CompleteMultipartUpload de la API de S3.

### S3CopyObject

Número de llamadas a la operación CopyObject de la API de S3.

### S3CreateMultipartUpload

Número de llamadas a la operación CreateMultipartUpload de la API de S3.

### S3DeleteObjects

Número de llamadas a DeleteObject(s) de la API de S3.

### S3GetObject

Número de llamadas a GetObject de la API de S3.

### S3GetObjectTagging

Número de llamadas a GetObjectTagging de la API de S3.

### S3GetRequestThrottlerBlocked

Número de solicitudes GET y SELECT de S3 bloqueadas por el limitador.

### S3GetRequestThrottlerCount

Número de solicitudes GET y SELECT de S3 que pasaron por el limitador: bloqueadas y no bloqueadas.

### S3GetRequestThrottlerSleepMicroseconds

Tiempo total durante el que una consulta permaneció en espera para cumplir con la limitación de tasa de las solicitudes GET y SELECT de S3.

### S3HeadObject

Número de llamadas a HeadObject de la API de S3.

### S3ListObjects

Número de llamadas a ListObjects de la API de S3.

### S3PutObject

Número de llamadas a PutObject de la API de S3.

### S3PutRequestThrottlerBlocked

Número de solicitudes PUT, COPY, POST y LIST de S3 bloqueadas por el limitador.

### S3PutRequestThrottlerCount

Número de solicitudes PUT, COPY, POST y LIST de S3 que pasaron por el limitador: bloqueadas y no bloqueadas.

### S3PutRequestThrottlerSleepMicroseconds

Tiempo total durante el cual una consulta permaneció en espera para cumplir con la limitación de tasa de las solicitudes S3 PUT, COPY, POST y LIST.

### S3QueueSetFileFailedMicroseconds

Tiempo empleado en marcar el archivo como fallido

### S3QueueSetFileProcessedMicroseconds

Tiempo empleado en marcar el archivo como procesado

### S3QueueSetFileProcessingMicroseconds

Tiempo empleado para marcar el archivo como en procesamiento

### S3ReadMicroseconds

Tiempo de las solicitudes GET y HEAD al almacenamiento en S3.

### S3ReadRequestAttempts

Número de intentos de las solicitudes GET y HEAD, incluido el intento inicial y cualquier reintento, pero sin contar los reintentos realizados internamente por la estrategia de reintento de S3

### S3ReadRequestRetryableErrors

Número de errores reintentables en solicitudes GET y HEAD, excluidos los reintentos realizados internamente por la estrategia de reintentos de S3

### S3ReadRequestsCount

Número de solicitudes GET y HEAD al almacenamiento en S3.

### Errores en solicitudes de lectura de S3

Número de errores no debidos al throttling en solicitudes GET y HEAD al almacenamiento en S3.

### S3ReadRequestsRedirects

Número de redirecciones en las solicitudes GET y HEAD al almacenamiento en S3.

### Limitación de solicitudes de lectura de S3

Número de errores 429 y 503 en las solicitudes GET y HEAD al almacenamiento en S3.

### S3UploadPart

Número de llamadas a UploadPart de la API de S3.

### S3UploadPartCopy

Número de llamadas a UploadPartCopy de la API de S3.

### S3WriteMicroseconds

Tiempo de las solicitudes POST, DELETE, PUT y PATCH al almacenamiento en S3.

### Intentos de solicitudes de escritura en S3

Número de intentos de solicitudes POST, DELETE, PUT y PATCH, incluido el intento inicial y cualquier reintento, pero sin contar los reintentos realizados internamente por la estrategia de reintento

### S3WriteRequestRetryableErrors

Número de errores que admiten reintento en las solicitudes POST, DELETE, PUT y PATCH, excluidos los reintentos realizados internamente por la estrategia de reintento

### S3WriteRequestsCount

Número de solicitudes POST, DELETE, PUT y PATCH al almacenamiento en S3.

### S3WriteRequestsErrors

Número de errores no relacionados con la limitación de tasa en las solicitudes POST, DELETE, PUT y PATCH al almacenamiento en S3.

### S3WriteRequestsRedirects

Número de redirecciones en las solicitudes POST, DELETE, PUT y PATCH al almacenamiento en S3.

### S3WriteRequestsThrottling

Número de errores 429 y 503 en las solicitudes POST, DELETE, PUT y PATCH al almacenamiento en S3.

### ScalarSubqueriesCacheMiss

Número de veces que una lectura de una subconsulta escalar no se encontró en caché y tuvo que calcularse por completo

### ScalarSubqueriesGlobalCacheHit

Número de veces que una lectura de una subconsulta escalar se realizó mediante la caché global

### ScalarSubqueriesLocalCacheHit

Número de veces que una lectura desde una subconsulta escalar se realizó usando la caché local

### SchedulerIOReadBytes

Bytes procesados por el planificador para lecturas de E/S.

### SchedulerIOReadRequests

Solicitudes de recursos que pasan por el planificador para lecturas de E/S.

### SchedulerIOReadWaitMicroseconds

Tiempo total que una consulta esperó solicitudes de recursos para operaciones de lectura de IO.

### SchedulerIOWriteBytes

Bytes procesados por el planificador para operaciones de escritura de IO.

### SchedulerIOWriteRequests

Solicitudes de recursos que pasan por el planificador para operaciones de escritura de IO.

### SchedulerIOWriteWaitMicroseconds

Tiempo total que una consulta estuvo esperando solicitudes de recursos para operaciones de escritura de IO.

### SchemaInferenceCacheEvictions

Número de veces que se desalojó un esquema de la caché debido a un desbordamiento

### SchemaInferenceCacheHits

Número de veces que la fuente solicitada se encuentra en la caché de esquemas

### SchemaInferenceCacheInvalidations

Número de veces que un esquema almacenado en caché quedó invalidado debido a cambios en los datos

### SchemaInferenceCacheMisses

Número de veces que la fuente solicitada no se encuentra en la caché de inferencia de esquemas

### SchemaInferenceCacheNumRowsHits

Número de veces que se encuentra el número de filas en la caché de esquemas durante el conteo desde archivos

### SchemaInferenceCacheNumRowsMisses

Número de veces que la fuente solicitada está en caché, pero la cantidad de filas no lo está al contar desde archivos

### SchemaInferenceCacheSchemaHits

Número de veces que el esquema se encuentra en la caché de esquemas durante la inferencia de esquemas

### SchemaInferenceCacheSchemaMisses

Número de veces que la fuente solicitada está en caché, pero el esquema no, durante la inferencia del esquema

### Seek

Número de veces que se llamó a la función &#39;lseek&#39;.

### SelectedBytes

Número de bytes (sin comprimir; para las columnas tal y como están almacenadas en memoria) seleccionados de todas las tablas.

### SelectedMarks

Número de marcas (gránulos de índice) seleccionadas para su lectura en una tabla MergeTree.

### Total de marcas seleccionadas

Número total de marcas (gránulos de índice) antes de seleccionar cuáles leer de una tabla MergeTree.

### SelectedParts

Número de partes de datos seleccionadas para la lectura de una tabla MergeTree.

### SelectedPartsTotal

Número total de partes de datos antes de seleccionar cuáles leer de una tabla MergeTree.

### Rangos seleccionados

Número de rangos (no adyacentes) en todas las partes de datos seleccionadas para su lectura desde una tabla MergeTree.

### SelectedRows

Número de filas seleccionadas en todas las tablas.

### SelectivityEstimatorInSetEstimatedFromSize

Número de condiciones `IN` cuya selectividad se estimó a partir del tamaño y los límites del Set en lugar de sus rangos exactos, porque el Set supera `statistics_max_set_size_for_exact_selectivity_estimation`

### SelectivityEstimatorInSetNotBuilt

Cantidad de condiciones `IN` que el estimador de selectividad no pudo analizar porque el Set aún no estaba construido y no debe ejecutar la subconsulta para rellenarlo

### SelectQueriesWithPrimaryKeyUsage

Cuenta las consultas SELECT que usan la clave primaria para evaluar la condición WHERE

### SelectQueriesWithSubqueries

Número de consultas SELECT con todas las subconsultas

### SelectQuery

Igual que Query, pero solo para consultas de tipo SELECT.

### SelectQueryTimeMicroseconds

Tiempo total de las consultas SELECT.

### SelfDuplicatedAsyncInserts

Número de inserciones asíncronas en el bloque insertado en una tabla ReplicatedMergeTree que se deduplicaron automáticamente.

### ServerStartupMilliseconds

Tiempo transcurrido desde el inicio del servidor hasta que comienza a escuchar en los sockets, en milisegundos

### SetsBuiltFromSubquery

Número de Sets `IN`/`JOIN` construidos al ejecutar su subconsulta. No se contabilizan los Sets tomados de la caché de Sets preparados ni los que ya estaban construidos y se reutilizan.

### SettingsProfileCacheRecalculationMicroseconds

Tiempo total dedicado a volver a fusionar la configuración y las restricciones de todos los conjuntos activos habilitados (protegido por el mutex de la caché que también utiliza la ruta de compilación de ContextAccess).

### SettingsProfileCacheRecalculations

Número de veces que la caché del perfil de configuración volvió a combinar configuraciones y restricciones para todos los conjuntos habilitados en uso. Se agrupa en una sola vez por lote de notificaciones de entidades de acceso.

### Segmentos

El número de segmentos involucrados en una consulta, sumado entre todas las tablas distribuidas y las funciones de tabla. Un mismo host se cuenta varias veces si aparece en varias tablas. Este número representa el total esperado de segmentos, incluidos los segmentos omitidos con la configuración `skip_unavailable_shards`.

### SharedCatalogStateFetchFullFallback

Número de veces que el fetch incremental del estado del Shared Catalog falló demasiadas veces consecutivas y, en su lugar, se realizó un fetch completo del estado

### SharedDatabaseCatalogFailedToApplyState

Número de fallos al aplicar un nuevo estado en SharedDatabaseCatalog

### SharedDatabaseCatalogStateApplicationMicroseconds

Tiempo total empleado en aplicar el nuevo estado en SharedDatabaseCatalog

### SharedMergeTreeBlobRefCounterSharedBlobs

Cuántos blobs de datos se compartieron por path entre partes de blob-list de SharedMergeTree (incrementos del conteo de referencias preparados en transacciones de commit, antes de acumularse en una única actualización por nodo de contador)

### SharedMergeTreeBlobsListBlobsWritten

Cuántos blobs de datos se registraron en los mapas blobs.list de SharedMergeTree

### SharedMergeTreeBlobsListInlineFilesWritten

Cuántos archivos pequeños se almacenaron de forma inline en los mapas blobs.list de SharedMergeTree en lugar de como blobs separados

### SharedMergeTreeBlobsListPartsWritten

Cuántas partes blob-list de SharedMergeTree se escribieron (un nodo blobs.list consolidado por cada una)

### SharedMergeTreeCondemnedPartsKillRequest

Cuántas solicitudes de ZooKeeper se utilizaron para eliminar partes marcadas para eliminación

### SharedMergeTreeCondemnedPartsLockConflict

Cuántas veces no se pudo adquirir el bloqueo debido a un conflicto

### SharedMergeTreeCondemnedPartsRemoved

Cuántas partes marcadas para eliminación se eliminaron

### SharedMergeTreeDataPartsFetchAttempt

Cuántas veces intentamos recuperar partes de datos

### SharedMergeTreeDataPartsFetchFromPeer

Cuántas veces obtenemos partes de datos de otra réplica

### SharedMergeTreeDataPartsFetchFromPeerMicroseconds

Microsegundos de obtención de partes de datos desde otra réplica

### SharedMergeTreeDataPartsFetchFromS3

Cuántas veces se recuperan partes de datos de S3

### SharedMergeTreeFetchDeduplicated

Número de fetches de partes deduplicados porque la parte ya estaba siendo obtenida o generada por otro thread

### SharedMergeTreeHandleBlockingParts

Cantidad de partes bloqueantes que se deben gestionar en scheduleDataProcessingJob

### SharedMergeTreeHandleBlockingPartsMicroseconds

Tiempo de gestión de las partes bloqueantes en scheduleDataProcessingJob

### SharedMergeTreeHandleFetchPartsMicroseconds

Tiempo de procesamiento de las partes recuperadas en scheduleDataProcessingJob

### SharedMergeTreeHandleOutdatedParts

Cuántas partes obsoletas se procesan en scheduleDataProcessingJob

### SharedMergeTreeHandleOutdatedPartsMicroseconds

Tiempo de procesamiento de partes obsoletas en scheduleDataProcessingJob

### SharedMergeTreeLoadChecksumAndIndexesMicroseconds

Tiempo de loadColumnsChecksumsIndexes únicamente para SharedMergeTree

### SharedMergeTreeMergeMutationAssignmentAttempt

Cuántas veces se intentó asignar una fusión o una mutación

### SharedMergeTreeMergeMutationAssignmentFailedWithConflict

Cuántas veces se intentó asignar una operación de fusión o mutación y falló debido a un conflicto en Keeper

### SharedMergeTreeMergeMutationAssignmentFailedWithNothingToDo

Número de veces que intentamos asignar una operación de fusión o mutación y fallamos porque no había nada que fusionar

### SharedMergeTreeMergeMutationAssignmentSuccessful

Cuántas veces intentamos asignar una fusión o mutación

### SharedMergeTreeMergePartsMovedToCondemned

Cantidad de partes movidas al directorio marcado para eliminación

### SharedMergeTreeMergePartsMovedToOudated

Número de partes movidas al directorio obsoleto

### SharedMergeTreeMergeSelectingTaskMicroseconds

Microsegundos de la tarea de selección de merges para SMT

### SharedMergeTreeMetadataCacheHintLoadedFromCache

Número de veces que se encontró la indicación de la caché de metadatos sin tener que consultar a Keeper

### SharedMergeTreeOptimizeAsync

Se ejecutaron consultas OPTIMIZE asíncronas

### SharedMergeTreeOptimizeSync

Ejecución de consultas OPTIMIZE sincrónicas

### SharedMergeTreeOutdatedPartsConfirmationInvocations

Cuántas invocaciones se hicieron para confirmar las partes obsoletas

### SharedMergeTreeOutdatedPartsConfirmationRequest

Cuántas solicitudes de ZooKeeper se utilizaron para la configuración de las partes obsoletas

### SharedMergeTreeOutdatedPartsHTTPRequest

Cuántas peticiones HTTP se enviaron para confirmar las partes obsoletas

### SharedMergeTreeOutdatedPartsHTTPResponse

Número de respuestas HTTP enviadas para confirmar las partes obsoletas

### SharedMergeTreePartitionOperationRetries

Cuántas veces una operación a nivel de partición (DROP/MOVE/REPLACE/CLONE/TRUNCATE PARTITION) tuvo que reintentar la escritura múltiple en Keeper debido a un cambio simultáneo

### SharedMergeTreePartsKillerMicroseconds

Cuánto tiempo tarda el hilo principal de parts killer

### SharedMergeTreePartsKillerParts

Cantidad de partes programadas por el killer

### SharedMergeTreePartsKillerPartsMicroseconds

Cuánto tiempo se tarda en eliminar partes (ejecutado por múltiples hilos)

### SharedMergeTreePartsKillerRuns

Número de veces que se ha ejecutado parts killer

### SharedMergeTreeReplicaSetUpdatesFromZooKeeper

Número de veces que hemos actualizado el conjunto de réplicas desde ZooKeeper

### SharedMergeTreeReplicaSetUpdatesFromZooKeeperMicroseconds

Cuánto tiempo se dedica a actualizar el conjunto de réplicas

### SharedMergeTreeReplicaSetUpdatesFromZooKeeperRequests

Número total de solicitudes a ZooKeeper para actualizar el conjunto de réplicas

### SharedMergeTreeReplicaSetUpdateTaskRuns

Número de veces que se ejecutó updateReplicaSetTask

### SharedMergeTreeScheduleDataProcessingJob

Número de veces que se llamó a scheduleDataProcessingJob/

### SharedMergeTreeScheduleDataProcessingJobMicroseconds

tiempo de ejecución de scheduleDataProcessingJob

### SharedMergeTreeScheduleDataProcessingJobNothingToScheduled

Número de veces que se llamó a scheduleDataProcessingJob, pero no había nada que hacer

### SharedMergeTreeSelectPartsForCoordinatedFetchMicroseconds

Tiempo de ejecución de selectPartsForCoordinatedFetch

### SharedMergeTreeSelectPartsForCoordinatedFetchParts

Número de partes seleccionadas por selectPartsForCoordinatedFetch

### SharedMergeTreeSelectPartsForFullFetchMicroseconds

Tiempo de ejecución de selectPartsForFullFetch

### SharedMergeTreeSelectPartsForFullFetchParts

Número de partes seleccionadas mediante selectPartsForFullFetch

### SharedMergeTreeSelectPartsForRendezvousFetchMicroseconds

Duración de selectPartsForRendezvousFetch

### SharedMergeTreeSelectPartsForRendezvousFetchParts

Número de partes seleccionadas para selectPartsForRendezvousFetch

### SharedMergeTreeSnapshotDeadLocksReclaimed

Cuántos nodos de Keeper o subárboles de lightweight snapshots que ya no están activos se recuperaron

### SharedMergeTreeSnapshotPartsCleanerMicroseconds

Cuánto tiempo lleva ejecutándose SnapshotCleanerThread

### SharedMergeTreeSnapshotPartsCleanerParts

Cuánto tiempo tarda SnapshotCleanerThread en intentar limpiar una parte

### SharedMergeTreeSnapshotPartsCleanerPartsMicroseconds

Cuánto tiempo tarda SnapshotCleanerThread en limpiar las partes

### SharedMergeTreeSnapshotPartsCleanerRuns

Número de veces que se ejecuta SnapshotCleanerThread

### SharedMergeTreeSnapshotPartsCleanRequest

Número de veces que SnapshotCleanerThread decide limpiar una parte

### SharedMergeTreeSnapshotPartsRemoved

Cuántas veces SnapshotCleanerThread logró limpiar correctamente una parte

### SharedMergeTreeTryUpdateDiskMetadataCacheForPartMicroseconds

Duración de tryUpdateDiskMetadataCacheForPart en scheduleDataProcessingJob

### SharedMergeTreeVirtualPartsUpdateMicroseconds

Microsegundos de la actualización de partes virtuales

### SharedMergeTreeVirtualPartsUpdates

Número de actualizaciones de partes virtuales

### SharedMergeTreeVirtualPartsUpdatesByLeader

Actualizaciones de partes virtuales por el líder

### SharedMergeTreeVirtualPartsUpdatesForMergesOrStatus

Actualizaciones de partes virtuales desde un trabajo en segundo plano distinto del predeterminado

### SharedMergeTreeVirtualPartsUpdatesFromPeer

Recuento de actualizaciones de partes virtuales desde un nodo par

### SharedMergeTreeVirtualPartsUpdatesFromPeerMicroseconds

Actualizaciones de partes virtuales desde el par, en microsegundos

### SharedMergeTreeVirtualPartsUpdatesFromZooKeeper

Número de actualizaciones de partes virtuales desde ZooKeeper

### SharedMergeTreeVirtualPartsUpdatesFromZooKeeperFullyCached

Número de descubrimientos de partes virtuales en los que todas las particiones se obtuvieron de la caché de descubrimiento y no se recuperó nada de ZooKeeper

### SharedMergeTreeVirtualPartsUpdatesFromZooKeeperMicroseconds

Actualizaciones de partes virtuales desde ZooKeeper, en microsegundos

### SharedMergeTreeVirtualPartsUpdatesFromZooKeeperPartitions

Número de particiones descubiertas en ZooKeeper durante el descubrimiento de partes virtuales porque su estadística no estaba presente o estaba obsoleta en la caché de descubrimiento

### SharedMergeTreeVirtualPartsUpdatesFromZooKeeperPartitionsFromCache

Número de particiones omitidas durante el descubrimiento de partes virtuales porque su información estadística ya estaba actualizada en la caché de descubrimiento

### SharedMergeTreeVirtualPartsUpdatesLeaderFailedElection

Falló la elección del líder de actualizaciones de partes virtuales

### SharedMergeTreeVirtualPartsUpdatesLeaderSuccessfulElection

Elección correcta del líder para las actualizaciones de partes virtuales

### SharedMergeTreeVirtualPartsUpdatesPeerNotFound

Las actualizaciones virtuales del par fallaron porque no se encontró ningún par

### SharedPartsLockHoldMicroseconds

Tiempo total durante el que se mantuvo el bloqueo de partes de datos compartidas en tablas MergeTree

### SharedPartsLocks

Número de veces que se ha adquirido el bloqueo de partes de datos compartidas para tablas MergeTree

### SharedPartsLockWaitMicroseconds

Tiempo total invertido en esperar el bloqueo de partes de datos compartidos en tablas MergeTree

### Llamadas a funciones sleep

Número de veces que se ha llamado a una función sleep (`sleep`, `sleepEachRow`).

### SleepFunctionElapsedMicroseconds

Tiempo empleado en suspensión en una función sleep (`sleep`, `sleepEachRow`).

### SleepFunctionMicroseconds

Tiempo de espera configurado en una función sleep (`sleep`, `sleepEachRow`).

### SlowRead

Número de lecturas lentas de un archivo. Esto indica una sobrecarga del sistema. Los umbrales se controlan mediante los ajustes `read_backoff_*`.

### Fallos de página menores

El número de fallos de página menores en los hilos de ejecución de consultas. Un fallo de página menor normalmente indica un fallo en la caché del asignador de memoria, lo que requiere un nuevo mapeo de memoria por parte del sistema operativo y la posterior asignación de una página de memoria física.

### StatelessWorkerCreateLeaseErrors

Número de solicitudes create&#95;lease fallidas al servicio de discovery de stateless workers (por cualquier error o timeout).

### StatelessWorkerCreateLeaseMicroseconds

Tiempo total empleado en las solicitudes create&#95;lease al servicio de discovery de stateless workers.

### StatelessWorkerCreateLeaseRequests

Número de solicitudes create&#95;lease enviadas al servicio de discovery de stateless workers para adquirir workers.

### StatelessWorkerDiscoveryCreateLeaseErrors

Número de requests create&#95;lease que el servicio de discovery de stateless workers respondió con un error.

### StatelessWorkerDiscoveryCreateLeaseMicroseconds

Tiempo total que el servicio de discovery de stateless workers dedicó a procesar requests de create&#95;lease.

### StatelessWorkerDiscoveryCreateLeaseRequests

Número de requests `create_lease` gestionadas por el servicio de discovery de stateless workers.

### StatelessWorkerDiscoveryCreateWorkerErrors

Número de solicitudes create&#95;worker a las que el servicio de discovery de stateless workers respondió con un error.

### StatelessWorkerDiscoveryCreateWorkerMicroseconds

Tiempo total que el servicio de discovery de stateless worker dedicó a procesar requests de create&#95;worker.

### StatelessWorkerDiscoveryCreateWorkerRequests

Número de requests create&#95;worker atendidas por el servicio de discovery de stateless worker.

### StatelessWorkerDiscoveryDeleteWorkerErrors

Número de solicitudes delete&#95;worker que el servicio de discovery de stateless workers respondió con un error.

### StatelessWorkerDiscoveryDeleteWorkerMicroseconds

Tiempo total que el servicio de discovery de stateless workers dedicó a procesar requests de delete&#95;worker.

### StatelessWorkerDiscoveryDeleteWorkerRequests

Número de requests `delete_worker` atendidos por el servicio de discovery de stateless workers.

### StatelessWorkerDiscoveryGetLeaseErrors

Número de solicitudes get&#95;lease que el servicio de discovery de stateless workers respondió con un error.

### StatelessWorkerDiscoveryGetLeaseMicroseconds

Tiempo total que el servicio de discovery de stateless workers dedicó a procesar requests de get&#95;lease.

### StatelessWorkerDiscoveryGetLeaseRequests

Número de requests get&#95;lease gestionadas por el servicio de discovery de stateless workers.

### StatelessWorkerDiscoveryGetWorkerErrors

Número de solicitudes get&#95;worker que el servicio de discovery de stateless workers respondió con un error.

### StatelessWorkerDiscoveryGetWorkerMicroseconds

Tiempo total que el servicio de discovery de stateless workers dedicó a procesar requests de get&#95;worker.

### StatelessWorkerDiscoveryGetWorkerRequests

Número de solicitudes get&#95;worker atendidas por el servicio de discovery de stateless workers.

### StatelessWorkerDiscoveryHeartbeatErrors

Número de solicitudes de latido que el servicio de discovery de stateless workers respondió con un error.

### StatelessWorkerDiscoveryHeartbeatMicroseconds

Tiempo total que el servicio de discovery de stateless workers dedicó a atender requests de latido.

### StatelessWorkerDiscoveryHeartbeatRequests

Número de solicitudes de latido gestionadas por el servicio de discovery de stateless workers.

### StatelessWorkerDiscoveryHeartbeatsRejected

Número de latidos que el servicio de discovery de stateless workers rechazó porque el worker ya había sido expulsado (evicted).

### StatelessWorkerDiscoveryInternalErrors

Cantidad de solicitudes que el servicio de discovery de stateless workers no pudo procesar debido a un error interno (una excepción o un fallo del almacén de coordinación), en lugar de rechazar una solicitud no válida.

### StatelessWorkerDiscoveryKeeperTransactionRetries

Número de transacciones de escritura que el servicio de discovery de stateless workers reintentó porque el estado de su almacén de coordinación (Keeper) se modificó de forma concurrente.

### StatelessWorkerDiscoveryListWorkersErrors

Número de requests list&#95;workers que el servicio de discovery de stateless workers respondió con un error.

### StatelessWorkerDiscoveryListWorkersMicroseconds

Tiempo total que el servicio de discovery de stateless workers dedicó a atender requests de list&#95;workers.

### StatelessWorkerDiscoveryListWorkersRequests

Número de requests `list_workers` atendidas por el servicio de discovery de stateless workers.

### StatelessWorkerDiscoveryTenantErrors

Número de solicitudes de administración de tenants a las que el servicio de discovery de stateless workers respondió con un error.

### StatelessWorkerDiscoveryTenantMicroseconds

Tiempo total que el servicio de discovery de stateless workers dedicó a atender requests de administración de tenants.

### StatelessWorkerDiscoveryTenantRequests

Número de solicitudes de administración de tenants (crear, actualizar, eliminar, obtener o listar un tenant) atendidas por el servicio de discovery de stateless workers.

### StatelessWorkerDiscoveryUpdateLeaseErrors

Número de solicitudes `update_lease` que el servicio de discovery de stateless workers respondió con un error.

### StatelessWorkerDiscoveryUpdateLeaseMicroseconds

Tiempo total que el servicio de discovery de stateless workers dedicó a procesar requests de update&#95;lease.

### StatelessWorkerDiscoveryUpdateLeaseRequests

Número de requests update&#95;lease gestionadas por el servicio de discovery de stateless workers.

### StatelessWorkerDiscoveryWorkersHeartbeatExpired

Número de stateless workers reclamados por el servicio de discovery de stateless workers porque dejaron de enviar latidos.

### StatelessWorkerDiscoveryWorkersLeased

Número de stateless workers recién asignados a los tenants por el servicio de discovery de stateless workers.

### StatelessWorkerDiscoveryWorkersLeaseExpired

Número de stateless workers recuperados por el servicio de discovery de stateless workers debido a que su lease expiró.

### StatelessWorkerDiscoveryWorkersUnavailable

Número de stateless workers solicitados por los tenants que el servicio de discovery de stateless workers no pudo proporcionar porque no había workers libres disponibles o porque se alcanzó el límite de workers del tenant.

### StatelessWorkerGetLeaseErrors

Número de solicitudes get&#95;lease fallidas al servicio de discovery de stateless workers (cualquier error o timeout).

### StatelessWorkerGetLeaseMicroseconds

Tiempo total empleado en las requests get&#95;lease al servicio de discovery de stateless workers.

### StatelessWorkerGetLeaseRequests

Cantidad de requests de get&#95;lease enviados al servicio de discovery de stateless workers para actualizar la lista de workers en lease.

### StatelessWorkerNotAccessible

Número de veces que un stateless worker arrendado resultó aún no accesible y se excluyó de la asignación de consultas hasta que supere la comprobación de accesibilidad.

### StatelessWorkerProvided

Número de stateless workers proporcionados a las consultas para la ejecución de consultas distribuidas.

### StatelessWorkerProvisioningMicroseconds

Tiempo total que las queries pasaron esperando el aprovisionamiento de stateless workers.

### StatelessWorkerProvisioningWaits

Número de veces que una consulta tuvo que esperar a que el servicio de discovery de stateless workers proporcionara workers.

### StatelessWorkerRequested

Número de stateless workers solicitados por las consultas para la ejecución de consultas distribuidas.

### StatelessWorkerServerHeartbeatErrors

Número de latidos fallidos de los stateless workers hacia el servicio de discovery de stateless workers (cualquier error o timeout).

### StatelessWorkerServerHeartbeatMicroseconds

Tiempo total que los stateless workers dedicaron a enviar latidos al servicio de discovery de stateless workers.

### StatelessWorkerServerHeartbeatRequests

Número de latidos que un stateless worker envió al servicio de discovery de stateless workers.

### StatelessWorkerServerLeasesLost

Número de veces que finalizó el lease de un stateless worker y este pasó a esperar el shutdown.

### StatelessWorkerServerRegisterErrors

Número de solicitudes de registro fallidas de los stateless workers al servicio de discovery de stateless workers (cualquier error o timeout).

### StatelessWorkerServerRegisterMicroseconds

Tiempo total que los stateless workers dedicaron a registrarse en el servicio de discovery de stateless workers.

### StatelessWorkerServerRegisterRequests

Número de solicitudes de registro que un stateless worker envió al servicio de discovery de stateless workers.

### StatelessWorkerServerTenantBindings

Número de veces que se asignó un stateless worker a un tenant para atender sus queries.

### StatelessWorkerTimeToAvailableMicroseconds

Tiempo total (sumado entre todos los workers) transcurrido desde que un stateless worker arrendado aparece por primera vez en el servicio de discovery hasta que supera la comprobación de accesibilidad y queda disponible para la ejecución de consultas.

### StatelessWorkerUpdateLeaseErrors

Número de solicitudes `update_lease` fallidas al servicio de discovery de stateless workers (cualquier error o timeout).

### StatelessWorkerUpdateLeaseMicroseconds

Tiempo total empleado en las solicitudes update&#95;lease al servicio de discovery de stateless workers.

### StatelessWorkerUpdateLeaseRequests

Número de solicitudes update&#95;lease enviadas al servicio de discovery de stateless workers para renovar los leases de los workers.

### StorageBufferErrorOnFlush

Número de veces que un búfer de la tabla &#39;Buffer&#39; no ha podido vaciarse debido a un error al escribir en la tabla de destino.

### StorageBufferFlush

Número de veces que se volcó un búfer de una tabla &#39;Buffer&#39;.

### StorageBufferLayerLockReadersWaitMilliseconds

Tiempo de espera de la capa de búfer durante la lectura.

### StorageBufferLayerLockWritersWaitMilliseconds

Tiempo de espera hasta que quede libre una capa Buffer para poder escribir en ella (puede usarse para ajustar las capas Buffer).

### StorageBufferPassedAllMinThresholds

Número de veces que se han cumplido los criterios de umbrales mínimos para volcar un búfer en una tabla &#39;Buffer&#39;.

### StorageBufferPassedBytesFlushThreshold

Número de veces que se ha alcanzado el umbral de vaciado en segundo plano basado únicamente en bytes para vaciar un búfer en una tabla &#39;Buffer&#39;. Esta métrica es solo para expertos. Si está leyendo esto y no es un experto, deje de leer.

### StorageBufferPassedBytesMaxThreshold

Número de veces que se ha alcanzado el umbral máximo de bytes para vaciar un búfer en una tabla &#39;Buffer&#39;.

### StorageBufferPassedRowsFlushThreshold

Número de veces que se ha alcanzado el umbral de vaciado en segundo plano basado únicamente en el número de filas para vaciar un búfer en una tabla &#39;Buffer&#39;. Esta es una métrica solo para expertos. Si está leyendo esto y no es un experto, deje de leer.

### StorageBufferPassedRowsMaxThreshold

Número de veces que se ha alcanzado el criterio del umbral máximo de filas para vaciar un búfer en una tabla &#39;Buffer&#39;.

### StorageBufferPassedTimeFlushThreshold

Número de veces que se ha alcanzado, en segundo plano, el umbral temporal de flush para vaciar un búfer en una tabla &#39;Buffer&#39;. Esta métrica es solo para expertos. Si está leyendo esto y no es un experto, deje de leer.

### StorageBufferPassedTimeMaxThreshold

Número de veces que se ha alcanzado el umbral de tiempo máximo para vaciar un búfer en una tabla &#39;Buffer&#39;.

### StorageConnectionsCreated

Número de conexiones creadas para los sistemas de almacenamiento

### StorageConnectionsElapsedMicroseconds

Tiempo total dedicado a establecer conexiones con los sistemas de almacenamiento

### StorageConnectionsErrors

Número de veces que falla la creación de una conexión de almacenamiento

### StorageConnectionsExpired

Número de conexiones expiradas de los sistemas de almacenamiento

### StorageConnectionsPreserved

Número de conexiones preservadas para los sistemas de almacenamiento

### StorageConnectionsReset

Número de conexiones reiniciadas para los sistemas de almacenamiento

### StorageConnectionsReused

Número de conexiones reutilizadas para los backends de almacenamiento

### StreamingExchangeConnectionWaitMicroseconds

Tiempo que un sink de streaming exchange esperó a que se conectara la task receptora.

### StreamingExchangeDeserializeMicroseconds

Tiempo empleado en descomprimir y deserializar los paquetes de streaming exchange en fragmentos.

### StreamingExchangeEarlyCloses

Número de veces que una fuente de streaming exchange indicó a su emisor que se detuviera antes de que el stream se leyera hasta el final, por ejemplo porque se cumplió un LIMIT. No se contabilizan los emisores que ya habían finalizado.

### StreamingExchangePacketsReceived

Paquetes de datos leídos de los streaming exchanges: uno por cada fragmento, más un paquete de fin de stream por cada stream.

### StreamingExchangePacketsSent

Paquetes de datos escritos por completo en los sockets de los streaming exchanges, uno por cada fragmento más un paquete de fin de stream por cada stream. Un paquete se contabiliza cuando se escribe por completo; un paquete truncado porque el receiver ya no necesitaba más datos no se contabiliza.

### StreamingExchangeReceiveBytes

Bytes leídos de los sockets de los streaming exchanges de un plan de consulta distribuido. `NetworkReceiveBytes` no los contabiliza.

### StreamingExchangeReceiveWaitMicroseconds

Tiempo que una fuente de streaming exchange estuvo esperando los siguientes bytes de su emisor.

### StreamingExchangeSendBytes

Bytes escritos en los sockets de los streaming exchanges de un plan de consulta distribuido. `NetworkSendBytes` no los contabiliza.

### StreamingExchangeSendQueueFullMicroseconds

Tiempo durante el cual un sink de streaming exchange no aceptó fragmentos porque su cola de paquetes a la espera del socket estaba llena.

### StreamingExchangeSerializedBytes

Bytes de los bloques Native en los paquetes de streaming exchange antes de la compresión, contabilizados una sola vez por cada paquete en el que se serializan. En comparación con `StreamingExchangeSendBytes`, esto proporciona la relación de compresión de un shuffle o un gather; un broadcast envía cada paquete a todos los destinos, por lo que en ese caso `StreamingExchangeSendBytes` crece con el número de destinos.

### StreamingExchangeSerializeMicroseconds

Tiempo empleado en serializar y comprimir fragmentos en paquetes de streaming exchange.

### SummingSortedMilliseconds

Tiempo total invertido en sumar columnas ordenadas

### SuspendSendingQueryToShard

Número total de veces que se suspendió el envío de una consulta a un segmento cuando `async_query_sending_for_remote` está habilitado.

### SynchronousReadWaitMicroseconds

Tiempo empleado en esperar las lecturas síncronas durante la lectura local asíncrona.

### SynchronousRemoteReadWaitMicroseconds

Tiempo dedicado a esperar las lecturas remotas síncronas.

### SystemLogErrorOnFlush

Número de veces que alguno de los logs del sistema no ha podido vaciarse en la tabla del sistema correspondiente. Los intentos de vaciado se repiten.

### SystemTimeMicroseconds

Tiempo total que los hilos de procesamiento (consultas y otras tareas) dedican a ejecutar instrucciones de CPU en modo kernel del sistema operativo. Este es el tiempo dedicado a las llamadas al sistema, excluido el tiempo de espera durante las llamadas al sistema bloqueantes.

### TableFunctionExecute

Número de llamadas a funciones de tabla.

### TextIndexDiscardHint

Número de gránulos de índice en los que se añadió como pista una lectura directa desde el índice de texto y que se descartó por baja selectividad.

### TextIndexDiscardPatternScan

Número de veces que se descartó un escaneo de diccionario basado en patrones en un índice de texto porque el número de listas de postings que debían leerse superó el umbral.

### TextIndexGenericExclusionSearchAlgorithm

Número de veces que se emplea el generic exclusion search algorithm sobre el índice de texto.

### TextIndexGenericExclusionSearchStepLimitReached

Número de veces que la generic exclusion search sobre el índice de texto alcanzó merge&#95;tree&#95;generic&#95;exclusion&#95;search&#95;max&#95;steps y aceptó los mark ranges restantes sin seguir dividiéndolos.

### TextIndexHeaderCacheHits

Número de veces que se ha encontrado una cabecera en la caché.

### TextIndexHeaderCacheMisses

Número de veces que no se encontró una cabecera en la caché.

### TextIndexLazyAdvanceCount

Número de operaciones de avance realizadas en el modo diferido de posting list.

### TextIndexLazyBlocksSkippedResolved

Número de bloques empaquetados omitidos porque la región de salida ya se había resuelto (todo a unos para OR, todo a ceros para AND) en el modo diferido de posting list.

### TextIndexLazyBruteForceIntersections

Número de intersecciones por fuerza bruta realizadas en el modo diferido de la posting list.

### TextIndexLazyLeapfrogIntersections

Número de intersecciones leapfrog realizadas en el modo diferido de posting list.

### TextIndexLazyPackedBlocksDecoded

Número de bloques empaquetados decodificados en modo diferido de posting list.

### TextIndexLazySegmentsBuilt

Número de segmentos realmente leídos y decodificados (fallos de caché) en modo diferido de posting list.

### TextIndexLazySegmentsPrepared

Número de segmentos preparados (leídos del disco o en caché) en el modo diferido de posting list.

### TextIndexLazySegmentsSkippedDense

Número de segmentos totalmente densos rellenados de una sola vez (memset para OR, incremento para AND) en lugar de decodificar bloques, en el modo diferido de posting list.

### TextIndexLazySegmentsSkippedResolved

Número de segmentos omitidos porque la región de salida ya se había resuelto (todos unos para OR, todos ceros para AND) en el modo diferido de posting list.

### TextIndexPhraseCandidates

Filas candidatas (intersección de postings) examinadas por la búsqueda de frase basada en candidatos.

### TextIndexPhraseFallbacks

Número de veces que una consulta de frase omitió el índice de texto porque se estimó que la frase coincidiría con más filas de las indicadas por &#39;text&#95;index&#95;hint&#95;max&#95;selectivity&#39; y, en su lugar, se evaluó sobre la columna.

### TextIndexPhraseMatchMicroseconds

Tiempo total empleado en la coincidencia de posiciones de frases sobre las filas candidatas.

### TextIndexPhraseSearches

Número de búsquedas de frases ejecutadas sobre un índice de texto. Las búsquedas repetidas de la misma frase en la misma parte se resuelven desde la cache y no se contabilizan.

### TextIndexPositionsBlocksRead

Número de bloques de posiciones leídos para resolver consultas de frase sobre un índice de texto. Un bloque que abarca dos ventanas candidatas se cuenta una sola vez por lectura.

### TextIndexPositionsBlocksTotal

Número total de bloques de posiciones de los tokens de la frase antes de seleccionar cuáles leer.

### TextIndexPositionsBytesRead

Bytes de bloques de posiciones leídos para resolver consultas de frase sobre un índice de texto.

### TextIndexPositionsDecodeMicroseconds

Tiempo total empleado en la decodificación de listas de posiciones (.pos) del índice de texto para la búsqueda de frases.

### TextIndexPostingsCacheHits

Número de veces que se encontró en la caché una posting list de un índice de texto.

### TextIndexPostingsCacheMisses

Número de veces que no se encontró en la caché la posting list de un índice de texto.

### TextIndexReadDictionaryBlocks

Número de veces que se ha leído desde disco un bloque del diccionario de un índice de texto.

### TextIndexReaderTotalMicroseconds

Tiempo total empleado en leer el índice de texto.

### TextIndexReadGranulesMicroseconds

Tiempo total empleado en leer y analizar gránulos del índice de texto.

### TextIndexReadPostings

Número de veces que se ha leído una posting list del índice de texto.

### TextIndexReadSparseIndexBlocks

Número de veces que se leyó un bloque del índice disperso desde el índice de texto.

### TextIndexTemporarySegmentsWritten

Número de segmentos temporales escritos durante la construcción de índices de texto.

### TextIndexTokensCacheHits

Número de veces que se ha encontrado en la caché la información de un token de un índice de texto.

### TextIndexTokensCacheMisses

Número de veces que no se encontró en la caché la información de un token de un índice de texto.

### TextIndexTokensCacheNegativeHits

Número de veces que se ha encontrado en la cache un token ausente de un índice de texto.

### TextIndexTokensCacheNegativeMisses

Número de tokens de índice de texto ausentes añadidos a la caché tras un lookup en el diccionario.

### TextIndexUsedEmbeddedPostings

Número de veces que se ha usado una posting list integrada en el diccionario.

### TextIndexUseHint

Número de gránulos de índice en los que se añadió y utilizó una lectura directa desde el índice de texto como sugerencia.

### ThreadPoolReaderPageCacheHit

Número de veces que la lectura en ThreadPoolReader se realizó desde la caché de páginas.

### ThreadPoolReaderPageCacheHitBytes

Número de bytes leídos dentro de ThreadPoolReader cuando la lectura se realizó desde la caché de páginas.

### ThreadPoolReaderPageCacheHitElapsedMicroseconds

Tiempo empleado en leer datos de la caché de páginas en ThreadPoolReader.

### ThreadPoolReaderPageCacheMiss

Número de veces que la lectura dentro de ThreadPoolReader no se realizó desde la caché de página y se delegó al pool de hilos.

### ThreadPoolReaderPageCacheMissBytes

Número de bytes leídos en ThreadPoolReader cuando la lectura no se realizó desde la caché de página y se delegó al pool de hilo.

### ThreadPoolReaderPageCacheMissElapsedMicroseconds

Tiempo empleado en leer datos dentro del trabajo asíncrono de ThreadPoolReader cuando la lectura no se realizó desde la caché de páginas.

### ThreadpoolReaderPrepareMicroseconds

Tiempo dedicado a la preparación (p. ej., una llamada al método `seek()` del lector)

### ThreadpoolReaderReadBytes

Bytes leídos por una tarea del threadpool durante la lectura asíncrona

### ThreadpoolReaderSubmit

Bytes leídos por una tarea del threadpool durante la lectura asíncrona

### ThreadpoolReaderSubmitLookupInCacheMicroseconds

Cuánto tiempo se dedica a comprobar si el contenido está en caché

### ThreadpoolReaderSubmitReadSynchronously

Número de veces que no se ha programado una tarea en el pool de hilos y, en su lugar, se ha leído de forma síncrona

### ThreadpoolReaderSubmitReadSynchronouslyBytes

Cantidad de bytes leídos de forma síncrona

### ThreadpoolReaderSubmitReadSynchronouslyMicroseconds

Cuánto tiempo se dedicó a la lectura síncrona

### ThreadpoolReaderTaskMicroseconds

Tiempo dedicado a obtener los datos durante la lectura asíncrona

### ThrottlerSleepMicroseconds

Tiempo total que una consulta permaneció en espera para ajustarse a toda la configuración de limitación de velocidad.

### TinyS3Clients

Número de copias de clientes de S3 que reutilizan un proveedor de autenticación existente de otro cliente.

### UncompressedCacheHits

Número de veces que se encontró un bloque de datos en la caché sin comprimir (evitando así la descompresión).

### UncompressedCacheMisses

Número de veces que no se encontró un bloque de datos en la caché sin comprimir (y fue necesario descomprimirlo).

### UncompressedCacheWeightLost

Número de bytes desalojados de la caché sin comprimir.

### UniqueKeyBitmapLoadMicroseconds

Tiempo dedicado a leer y deserializar archivos sidecar de bitmap de eliminación de UNIQUE KEY cuando se produce un fallo de caché en DeleteBitmapCache.

### Actualizaciones de bitmap de UNIQUE KEY

Número de archivos sidecar del bitmap de eliminación de UNIQUE KEY escritos (uno por sustitución / commit de DELETE).

### UniqueKeyIndexCacheHits

Número de veces que se encontró una entrada en la caché del índice UNIQUE KEY, por lo que no fue necesario cargar un bloque SST.

### UniqueKeyIndexCacheLookupMicroseconds

Tiempo según el reloj del sistema dentro de `UniqueKeyIndexCache::Lookup` + `UniqueKeyIndexCache::Insert` (adaptador `CacheBase` del lado de ClickHouse para la caché de bloques de RocksDB).

### UniqueKeyIndexCacheMisses

Número de veces que no se encontró una entrada en la caché del índice UNIQUE KEY, por lo que fue necesario cargar un bloque SST desde disco.

### UniqueKeyLoadTimeSSTRebuildCount

Número de partes UNIQUE KEY cuyo archivo `unique_key_index.sst` se reconstruyó durante la carga tras la ventana de crash-before-flush.

### UniqueKeyLoadTimeSSTRebuildMicroseconds

Tiempo total dedicado a reconstruir `unique_key_index.sst` durante la carga (lectura secuencial de las columnas UK + escritura del SST).

### UniqueKeySSTWriteMicroseconds

Tiempo total de reloj transcurrido durante el ciclo de vida de un `SSTIndexWriter`: incluye `Open` de SST, cada operación Put de `addEncoded` y `Finish` + la finalización del `WriteBuffer` (+ fsync) en `finish` (los bytes de SST se transmiten directamente al `WriteBuffer` del almacenamiento de la parte). Excluye el trabajo que realizan las funciones auxiliares estáticas antes de construir el writer (codificación + ordenación de rutas sin prefijo). Se emite una vez por cada writer.

### USearchAddComputedDistances

Número de veces que se calculó la distancia al agregar vectores a los índices de usearch.

### USearchAddCount

Número de vectores añadidos a los índices de USearch.

### USearchAddVisitedMembers

Número de nodos visitados al añadir vectores a los índices de USearch.

### USearchSearchComputedDistances

Número de veces que se calculó la distancia al buscar en los índices de usearch.

### USearchSearchCount

Número de operaciones de búsqueda realizadas en índices de usearch.

### USearchSearchVisitedMembers

Número de nodos visitados durante la búsqueda en índices usearch.

### UserThrottlerBytes

Bytes que pasaron por el limitador &#39;max&#95;network&#95;bandwidth&#95;for&#95;user&#39;.

### UserThrottlerSleepMicroseconds

Tiempo total que una consulta permaneció en espera para cumplir con la limitación de ancho de banda de `max_network_bandwidth_for_user`.

### UserTimeMicroseconds

Tiempo total que los hilos de procesamiento (consultas y otras tareas) dedican a ejecutar instrucciones de CPU en modo de usuario. Esto incluye el tiempo en que la canalización de la CPU se vio bloqueada debido al acceso a la memoria principal, fallos de caché, errores de predicción de saltos, hyper-threading, etc.

### VectorSimilarityIndexCacheHits

Número de veces que se ha encontrado un gránulo del índice en la caché del índice vectorial.

### VectorSimilarityIndexCacheMisses

Número de veces que no se encontró un gránulo de índice en la caché del índice vectorial y tuvo que leerse del disco.

### VectorSimilarityIndexCacheWeightLost

Número aproximado de bytes desalojados de la caché del índice vectorial.

### VersionedCollapsingSortedMilliseconds

Tiempo total invertido en el colapsado por versión de columnas ordenadas

### WaitMarksLoadMicroseconds

Tiempo dedicado a cargar los archivos de marcas

### WaitPrefetchTaskMicroseconds

Tiempo empleado en esperar al lector precargado

### WasmDeserializationMicroseconds

Tiempo empleado en ejecutar código WebAssembly

### WasmGuestExecuteMicroseconds

Tiempo empleado en ejecutar código WebAssembly

### WasmMemoryAllocated

Memoria total asignada a los compartimentos de WebAssembly

### WasmModuleInstatiate

Número de compartimentos de WebAssembly creados

### WasmSerializationMicroseconds

Tiempo empleado en ejecutar código WebAssembly

### WasmTotalExecuteMicroseconds

Tiempo invertido en ejecutar código WebAssembly

### WriteBufferFromFileDescriptorWrite

Número de escrituras (write/pwrite) en un descriptor de archivo. No incluye sockets.

### WriteBufferFromFileDescriptorWriteBytes

Número de bytes escritos en los descriptores de archivo. Si el archivo está comprimido, se mostrará el tamaño de los datos comprimidos.

### WriteBufferFromFileDescriptorWriteFailed

Número de veces que ha fallado la escritura (`write`/`pwrite`) en un descriptor de archivo.

### WriteBufferFromHTTPBytes

Tamaño total en bytes del payload recibido y enviado por WriteBufferFromHTTP. No incluye las cabeceras HTTP.

### WriteBufferFromHTTPRequestsSent

Número de peticiones HTTP enviadas por WriteBufferFromHTTP

### WriteBufferFromS3Bytes

Bytes escritos en S3.

### WriteBufferFromS3Microseconds

Tiempo dedicado a escribir en S3.

### WriteBufferFromS3RequestsErrors

Número de excepciones al escribir en S3.

### WriteBufferFromS3WaitInflightLimitMicroseconds

Tiempo dedicado a esperar a que se completen algunas de las solicitudes actuales cuando su número alcanza el límite definido por s3&#95;max&#95;inflight&#95;parts&#95;for&#95;one&#95;file.

### ZooKeeperBytesReceived

Número de bytes recibidos por la red al comunicarse con ZooKeeper.

### ZooKeeperBytesSent

Cantidad de bytes enviados a través de la red durante la comunicación con ZooKeeper.

### ZooKeeperCheck

Número de solicitudes &#39;check&#39; a ZooKeeper. Por lo general, no tienen sentido por sí solas, sino solo como parte de una transacción compleja.

### ZooKeeperClose

Número de veces que la conexión con ZooKeeper se ha cerrado de forma voluntaria.

### ZooKeeperCreate

Número de solicitudes &#39;create&#39; enviadas a ZooKeeper.

### ZooKeeperExists

Número de solicitudes `exists` a ZooKeeper.

### ZooKeeperGet

Número de solicitudes &quot;get&quot; a ZooKeeper.

### ZooKeeperGetACL

Número de solicitudes `getACL` a ZooKeeper.

### Excepciones de hardware en ZooKeeper

Número de excepciones relacionadas con la red (pérdida de conexión o similares) al trabajar con ZooKeeper.

### ZooKeeperInit

Número de veces que se ha establecido la conexión con ZooKeeper.

### ZooKeeperList

Número de solicitudes de tipo &#39;list&#39; (getChildren) a ZooKeeper.

### ZooKeeperListRecursive

Número de solicitudes a ZooKeeper de tipo &#39;listRecursive&#39;.

### ZooKeeperMulti

Número de solicitudes &#39;multi&#39; a ZooKeeper (transacciones compuestas).

### ZooKeeperMultiRead

Número de solicitudes de lectura «multi» a ZooKeeper (transacciones compuestas).

### ZooKeeperMultiWrite

Número de solicitudes de escritura múltiples a ZooKeeper (transacciones compuestas).

### ZooKeeperOtherExceptions

Número de excepciones al trabajar con ZooKeeper, distintas de ZooKeeperUserExceptions y ZooKeeperHardwareExceptions.

### ZooKeeperReconfig

Número de solicitudes de &#39;reconfig&#39; a ZooKeeper.

### ZooKeeperRemove

Número de solicitudes `remove` a ZooKeeper.

### ZooKeeperSet

Número de solicitudes de &#39;set&#39; a ZooKeeper.

### ZooKeeperSync

Número de solicitudes de &#39;sync&#39; a ZooKeeper. Estas solicitudes rara vez son necesarias o útiles.

### ZooKeeperTransactions

Número de operaciones de ZooKeeper, incluidas tanto las operaciones de lectura como las de escritura, así como las multitransacciones.

### Excepciones de usuario en ZooKeeper

Número de excepciones relacionadas con los datos al trabajar con ZooKeeper (nodo inexistente, versión incorrecta o similares).

### ZooKeeperWaitMicroseconds

Número de microsegundos dedicados a esperar respuestas de ZooKeeper tras crear una solicitud, sumado en todos los hilos que la realizan.

### ZooKeeperWatchCallbackDurationMicroseconds

Tiempo total invertido en los callbacks `watch` de ZooKeeper (hilo de red).

### Errores en los callbacks watch de ZooKeeper

Número de excepciones generadas por los callbacks watch de ZooKeeper.

### ZooKeeperWatchResponse

Número de veces que se ha recibido una notificación de watch de ZooKeeper.

### ZooKeeperWatchTriggeredBackupCoordination

Número de notificaciones de watch enviadas a la coordinación de copia de seguridad/restauración.

### ZooKeeperWatchTriggeredClusterDiscovery

Número de notificaciones de watch enviadas a ClusterDiscovery.

### ZooKeeperWatchTriggeredDistributedDDL

Número de notificaciones de watch enviadas a la cola de DDLWorker.

### ZooKeeperWatchTriggeredMaterializedViewRefresh

Número de notificaciones de watch despachadas a la coordinación de actualización de MaterializedView.

### ZooKeeperWatchTriggeredObjectStorageQueue

Número de notificaciones de watch enviadas a ObjectStorageQueue / S3Queue.

### ZooKeeperWatchTriggeredOther

Número de notificaciones de watch enviadas a callbacks no categorizados.

### ZooKeeperWatchTriggeredReplicatedAccessControl

Número de notificaciones de watch despachadas a ReplicatedAccessStorage.

### ZooKeeperWatchTriggeredReplicatedMergeTreeLeaderElection

Número de notificaciones de watch enviadas a la elección de líder de ReplicatedMergeTree / a los bloqueos de réplica.

### ZooKeeperWatchTriggeredReplicatedMergeTreeLog

Número de notificaciones de watch despachadas para las entradas de log de ReplicatedMergeTree.

### ZooKeeperWatchTriggeredReplicatedMergeTreeMutations

Número de notificaciones de watch enviadas a las mutaciones de ReplicatedMergeTree.

### ZooKeeperWatchTriggeredReplicatedMergeTreeReplicaSync

Número de notificaciones de watch enviadas mientras se espera a que las réplicas procesen las entradas de log (SYNC REPLICA, ALTER, mutations).

### ZooKeeperWatchTriggeredUserDefinedSQLObjects

Número de notificaciones de watch enviadas a UserDefinedSQLObjectsZooKeeperStorage.

### ZooKeeperWatchTriggeredWorkloadEntity

Número de notificaciones de watch despachadas a WorkloadEntityKeeperStorage.

### KeeperLogsEntryReadFromCommitCache

Evento retirado que contabilizaba las entradas de log leídas desde la caché de logs de commit de Keeper.

### KeeperLogsPrefetchedEntries

Evento retirado que contaba las entradas de log de Keeper precargadas desde el archivo de changelog.

### NaiveBayesClassifierModelsAllocatedBytes

Evento retirado que contaba los bytes asignados a los modelos del clasificador Naive Bayes.

### NaiveBayesClassifierModelsLoaded

Evento retirado que contaba los modelos del clasificador Naive Bayes cargados.

### ReaderExecutorRequestedBytes

Evento retirado que contabilizaba los bytes útiles entregados por las solicitudes de lectura de `ReaderExecutor`.

## Columnas

* `event` ([String](/es/reference/data-types/string)) — Nombre del evento.
* `value` ([UInt64](/es/reference/data-types/int-uint)) — Cantidad de eventos ocurridos.
* `description` ([String](/es/reference/data-types/string)) — Descripción del evento.

**Alias:**

* `name` — Alias de `event`.

## Ejemplo

```sql
SELECT * FROM system.events LIMIT 5
```

```text
┌─event─────────────────────────────────┬─value─┬─description────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ Query                                 │    12 │ Número de consultas que se interpretarán y potencialmente se ejecutarán. No incluye consultas que no se pudieron analizar o que fueron rechazadas por límites de tamaño de AST, límites de cuota o límites en el número de consultas en ejecución simultánea. Puede incluir consultas internas iniciadas por el propio ClickHouse. No cuenta las subconsultas.                  │
│ SelectQuery                           │     8 │ Igual que Query, pero solo para consultas SELECT.                                                                                                                                                                                                                │
│ FileOpen                              │    73 │ Número de archivos abiertos.                                                                                                                                                                                                                                    │
│ ReadBufferFromFileDescriptorRead      │   155 │ Número de lecturas (read/pread) desde un descriptor de archivo. No incluye sockets.                                                                                                                                                                             │
│ ReadBufferFromFileDescriptorReadBytes │  9931 │ Número de bytes leídos desde descriptores de archivo. Si el archivo está comprimido, se mostrará el tamaño de los datos comprimidos.                                                                                                                                              │
└───────────────────────────────────────┴───────┴────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```

## Véase también

* [system.asynchronous&#95;metrics](/es/reference/system-tables/asynchronous_metrics) — Contiene métricas calculadas periódicamente.
* [system.metrics](/es/reference/system-tables/metrics) — Contiene métricas calculadas al instante.
* [system.metric&#95;log](/es/reference/system-tables/metric_log) — Contiene un historial de los valores de las métricas de las tablas `system.metrics` y `system.events`.
* [Monitorización](/es/guides/oss/deployment-and-scaling/monitoring/monitoring) — Conceptos básicos de la monitorización en ClickHouse.

{/*AUTOGENERATED_END*/}