---
description: 'Tabla del sistema que contiene información sobre las tareas de los pools de programación en segundo plano.'
keywords: ['tabla del sistema', 'background_schedule_pool']
slug: /operations/system-tables/background_schedule_pool
title: 'system.background_schedule_pool'
doc_type: 'reference'
---

import SystemTableCloud from '/snippets/es/_system_table_cloud.mdx';

<SystemTableCloud />

<div id="description">
  ## Descripción
</div>

Contiene información sobre las tareas de los pools de programación en segundo plano. Los pools de programación en segundo plano se utilizan para ejecutar tareas periódicas, como envíos distribuidos, vaciados del búfer y operaciones del broker de mensajes.

<div id="columns">
  ## Columnas
</div>

{/*AUTOGENERATED_START*/}

* `pool` ([String](/es/reference/data-types/index)) — Nombre del pool. Valores posibles: `schedule` — pool de programación de uso general, `buffer_flush` — pool para el vaciado de datos de la tabla Buffer, `distributed` — pool para operaciones de la tabla distribuida, `message_broker` — pool para operaciones del broker de mensajes.
* `database` ([String](/es/reference/data-types/index)) — Nombre de la base de datos.
* `table` ([String](/es/reference/data-types/index)) — Nombre de la tabla.
* `table_uuid` ([UUID](/es/reference/data-types/index)) — UUID de la tabla.
* `query_id` ([String](/es/reference/data-types/index)) — ID de consulta (si se está ejecutando ahora). Nota: no es una consulta real, sino un ID generado aleatoriamente para correlacionar los logs en `system.text_log`.
* `elapsed_ms` ([UInt64](/es/reference/data-types/index)) — Tiempo de ejecución de la tarea (si se está ejecutando ahora).
* `log_name` ([String](/es/reference/data-types/index)) — Nombre del log de la tarea.
* `deactivated` ([UInt8](/es/reference/data-types/index)) — Indica si la tarea está desactivada (siempre es falso, ya que las tareas desactivadas se eliminan del pool).
* `scheduled` ([UInt8](/es/reference/data-types/index)) — Indica si la tarea está programada para ejecutarse.
* `delayed` ([UInt8](/es/reference/data-types/index)) — Indica si la tarea está programada con retraso.
* `executing` ([UInt8](/es/reference/data-types/index)) — Indica si la tarea se está ejecutando actualmente.

{/*AUTOGENERATED_END*/}

<div id="example">
  ## Ejemplo
</div>

```sql title="Query"
SELECT * FROM system.background_schedule_pool LIMIT 5 FORMAT Vertical;
```

```text title="Response"
Row 1:
──────
pool:        distributed
database:    default
table:       data
table_uuid:  00000000-0000-0000-0000-000000000000
query_id:
elapsed_ms:  0
log_name:    BackgroundJobsAssignee:DataProcessing
deactivated: 0
scheduled:   1
delayed:     0
executing:   0
```

<div id="see-also">
  ## Véase también
</div>

* [system.background&#95;schedule&#95;pool&#95;log](/es/reference/system-tables/background_schedule_pool_log) — Contiene el historial de ejecución de las tareas del pool de programación en segundo plano.