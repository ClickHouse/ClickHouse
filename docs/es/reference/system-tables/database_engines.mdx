---
description: 'Tabla del sistema que contiene una lista de los motores de base de datos compatibles con el
  servidor.'
keywords: ['system table', 'database_engines']
slug: /operations/system-tables/database_engines
title: 'system.database_engines'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

## Descripción {#description}

Contiene la lista de motores de base de datos admitidos por el servidor.

## Columnas {#columns}

* `name` ([String](/es/reference/data-types/string)) — El nombre del motor de base de datos.
* `description` ([String](/es/reference/data-types/string)) — Una descripción general de la función del motor de base de datos.
* `syntax` ([String](/es/reference/data-types/string)) — Cómo se especifica el motor de base de datos en la cláusula ENGINE de una consulta CREATE DATABASE.
* `examples` ([String](/es/reference/data-types/string)) — Ejemplos de uso.
* `introduced_in` ([String](/es/reference/data-types/string)) — La versión de ClickHouse en la que se introdujo por primera vez el motor de base de datos, en el formato major.minor.
* `related` ([Array(String)](/es/reference/data-types/array)) — Los nombres de los motores de base de datos relacionados.

## Ejemplo {#example}

```sql title="Query"
SELECT name
FROM system.database_engines
WHERE name IN ('Atomic', 'Ordinary')
ORDER BY name
```

```text title="Response"
┌─name─────┐
│ Atomic   │
│ Ordinary │
└──────────┘
```

{/*AUTOGENERATED_END*/}