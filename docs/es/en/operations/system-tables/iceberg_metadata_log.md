---
description: 'Tabla del sistema que contiene información sobre los archivos de metadatos leídos desde tablas Iceberg. Cada entrada
  representa un archivo de metadatos raíz, metadatos extraídos de un archivo Avro o una entrada de un archivo Avro.'
keywords: ['tabla del sistema', 'iceberg_metadata_log']
slug: /operations/system-tables/iceberg_metadata_log
title: 'system.iceberg_metadata_log'
doc_type: 'reference'
---

import SystemTableCloud from '@site/docs/_snippets/_system_table_cloud.md';

<SystemTableCloud />

<div id="description">
  ## Descripción
</div>

La tabla `system.iceberg_metadata_log` registra eventos de acceso y análisis de metadatos de las tablas Iceberg leídas por ClickHouse. Proporciona información detallada sobre cada archivo o entrada de metadatos procesados, lo que resulta útil para depuración, auditoría y para comprender la evolución de la estructura de las tablas Iceberg.

Esta tabla registra cada archivo y entrada de metadatos leídos de tablas Iceberg, incluidos archivos de metadatos raíz, listas de manifiestos y entradas de manifiesto. Ayuda a los usuarios a seguir cómo ClickHouse interpreta los metadatos de las tablas Iceberg y a diagnosticar problemas relacionados con la evolución del esquema, la resolución de archivos o la planificación de consultas.

:::note
Esta tabla se utiliza principalmente con fines de depuración.
:::

<div id="controlling-log-verbosity">
  ### Control de la verbosidad del log
</div>

Puede controlar qué eventos de metadatos se registran con la configuración [`iceberg_metadata_log_level`](../../operations/settings/settings.md#iceberg_metadata_log_level).

Para registrar todos los metadatos usados en la consulta actual:

```sql
SELECT * FROM my_iceberg_table SETTINGS iceberg_metadata_log_level = 'manifest_file_entry';

SYSTEM FLUSH LOGS iceberg_metadata_log;

SELECT content_type, file_path, row_in_file
FROM system.iceberg_metadata_log
WHERE query_id = '{previous_query_id}';
```

Para registrar solo el archivo JSON raíz de metadatos que se usa en la consulta actual:

```sql
SELECT * FROM my_iceberg_table SETTINGS iceberg_metadata_log_level = 'metadata';

SYSTEM FLUSH LOGS iceberg_metadata_log;

SELECT content_type, file_path, row_in_file
FROM system.iceberg_metadata_log
WHERE query_id = '{previous_query_id}';
```

Consulte la descripción de la configuración [`iceberg_metadata_log_level`](../../operations/settings/settings.md#iceberg_metadata_log_level) para obtener más información.

<div id="good-to-know">
  ### Conviene saberlo
</div>

* Use `iceberg_metadata_log_level` solo a nivel de consulta cuando necesite investigar en detalle su tabla Iceberg. De lo contrario, puede poblar la tabla de registros con una cantidad excesiva de metadatos y provocar una degradación del rendimiento.
* La tabla contiene entradas duplicadas, ya que está pensada principalmente para depuración y no garantiza la unicidad por entidad. El contenido y el estado de poda se almacenan en filas distintas porque se recopilan en momentos diferentes del programa. El contenido se recopila cuando se leen los metadatos; el estado de poda se recopila cuando se comprueban los metadatos para la poda. **Nunca confíe en la propia tabla para la deduplicación.**
* Si usa un `content_type` más detallado que `ManifestListMetadata`, la caché de metadatos de Iceberg se desactiva para las listas de manifiestos.
* Del mismo modo, si usa un `content_type` más detallado que `ManifestFileMetadata`, la caché de metadatos de Iceberg se desactiva para los archivos de manifiesto.
* Si la consulta SELECT se canceló o falló, la tabla de registros puede seguir conteniendo entradas de metadatos procesados antes del fallo, pero no contendrá información sobre las entidades de metadatos que no se procesaron.

<div id="columns">
  ## Columnas
</div>

{/*AUTOGENERATED_START*/ }

* `hostname` ([LowCardinality(String)](/es/sql-reference/data-types/lowcardinality)) — Hostname del servidor que ejecuta la consulta.
* `event_date` ([Date](/es/sql-reference/data-types/date)) — Fecha del registro.
* `event_time` ([DateTime](/es/sql-reference/data-types/datetime)) — Hora del evento.
* `query_id` ([String](/es/sql-reference/data-types/string)) — ID de la consulta.
* `content_type` ([Enum8(&#39;None&#39; = 0, &#39;Metadata&#39; = 1, &#39;ManifestListMetadata&#39; = 2, &#39;ManifestListEntry&#39; = 3, &#39;ManifestFileMetadata&#39; = 4, &#39;ManifestFileEntry&#39; = 5)](/es/sql-reference/data-types/enum)) — Tipo de contenido.
* `table_path` ([String](/es/sql-reference/data-types/string)) — Ruta de la tabla.
* `file_path` ([String](/es/sql-reference/data-types/string)) — Ruta del archivo.
* `content` ([String](/es/sql-reference/data-types/string)) — Contenido en formato JSON (contenido del archivo JSON, metadatos de Avro o entrada de Avro).
* `row_in_file` ([Nullable(UInt64)](/es/sql-reference/data-types/nullable)) — Fila en el archivo.
* `pruning_status` ([Nullable(Enum8(&#39;NotPruned&#39; = 0, &#39;PartitionPruned&#39; = 1, &#39;MinMaxIndexPruned&#39; = 2))](/es/sql-reference/data-types/nullable)) — Estado de la poda de particiones o de la poda del índice min-max para el archivo.

{/*AUTOGENERATED_END*/ }

<div id="content-type-values">
  ### Valores de `content_type`
</div>

* `None`: Sin contenido.
* `Metadata`: Archivo de metadatos raíz.
* `ManifestListMetadata`: Metadatos de la lista de manifiestos.
* `ManifestListEntry`: Entrada de una lista de manifiestos.
* `ManifestFileMetadata`: Metadatos del archivo de manifiesto.
* `ManifestFileEntry`: Entrada de un archivo de manifiesto.

<div id="see-also">
  ## Véase también
</div>

* [Iceberg Table Engine](../../engines/table-engines/integrations/iceberg.md)
* [Iceberg Table Function](../../sql-reference/table-functions/iceberg.md)
* [system.iceberg&#95;history](./iceberg_history.md)