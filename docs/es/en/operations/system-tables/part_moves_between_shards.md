---
description: 'Contiene información sobre las partes que actualmente se están moviendo entre segmentos y su progreso.'
keywords: ['tabla del sistema', 'part_moves_between_shards']
slug: /operations/system-tables/part_moves_between_shards
title: 'system.part_moves_between_shards'
doc_type: 'reference'
---

<div id="description">
  ## Descripción
</div>

Contiene información sobre las partes que actualmente se están moviendo entre segmentos y su progreso.

<div id="columns">
  ## Columnas
</div>

{/*AUTOGENERATED_START*/ }

* `database` ([String](/es/sql-reference/data-types/string)) — El nombre de la base de datos donde se realiza el movimiento.
* `table` ([String](/es/sql-reference/data-types/string)) — El nombre de la tabla donde se realiza el movimiento.
* `task_name` ([String](/es/sql-reference/data-types/string)) — El nombre de la tarea de movimiento.
* `task_uuid` ([UUID](/es/sql-reference/data-types/uuid)) — El identificador de la tarea de movimiento.
* `create_time` ([DateTime](/es/sql-reference/data-types/datetime)) — La hora a la que se creó la tarea.
* `part_name` ([String](/es/sql-reference/data-types/string)) — El nombre de la parte que está en proceso de movimiento.
* `part_uuid` ([UUID](/es/sql-reference/data-types/uuid)) — El UUID de la parte que está en proceso de movimiento.
* `to_shard` ([String](/es/sql-reference/data-types/string)) — El nombre del segmento de destino.
* `dst_part_name` ([String](/es/sql-reference/data-types/string)) — El nombre de la parte resultante.
* `update_time` ([DateTime](/es/sql-reference/data-types/datetime)) — La última vez que se actualizó.
* `state` ([String](/es/sql-reference/data-types/string)) — El estado actual del movimiento.
* `rollback` ([UInt8](/es/sql-reference/data-types/int-uint)) — El indicador que señala si la operación se revirtió.
* `num_tries` ([UInt32](/es/sql-reference/data-types/int-uint)) — El número de intentos para completar la operación.
* `last_exception` ([String](/es/sql-reference/data-types/string)) — El nombre de la última excepción, si la hubo.

{/*AUTOGENERATED_END*/ }