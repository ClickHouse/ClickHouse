---
description: 'Esta tabla contiene métricas de histograma que se pueden calcular al instante
  y exportar en formato Prometheus. Siempre está actualizada.'
keywords: ['tabla del sistema', 'histogram_metrics']
slug: /operations/system-tables/histogram_metrics
title: 'system.histogram_metrics'
doc_type: 'reference'
---

import SystemTableCloud from '@site/docs/_snippets/_system_table_cloud.md';

<SystemTableCloud />

<div id="description">
  ## Descripción
</div>

Esta tabla contiene métricas de histograma que se pueden calcular al instante y exportar en formato Prometheus. Siempre está actualizada. Sustituye a `system.latency_log`, que ha quedado obsoleto.

<div id="columns">
  ## Columnas
</div>

{/*AUTOGENERATED_START*/ }

* `metric` ([String](/es/sql-reference/data-types/string)) — Nombre de la métrica.
* `value` ([Float64](/es/sql-reference/data-types/float)) — Valor de la métrica.
* `description` ([String](/es/sql-reference/data-types/string)) — Descripción de la métrica.
* `labels` ([Map(String, String)](/es/sql-reference/data-types/map)) — Etiquetas de la métrica.

**Alias:**

* `name` — Alias de `metric`.

{/*AUTOGENERATED_END*/ }

<div id="example">
  ## Ejemplo
</div>

Puede usar una consulta como esta para exportar todas las métricas de histograma en formato Prometheus.

```sql
SELECT
  metric AS name,
  toFloat64(value) AS value,
  description AS help,
  labels,
  'histogram' AS type
FROM system.histogram_metrics
FORMAT Prometheus
```

<div id="metric_descriptions">
  ## Descripción de métricas
</div>

| Métrica                                                      | Descripción                                                                                                                                          |
| ------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| `keeper_response_time_ms_bucket`                             | Tiempo de respuesta de Keeper, en milisegundos.                                                                                                      |
| `keeper_client_queue_duration_milliseconds_bucket`           | Tiempo que las solicitudes pasan esperando para ser encoladas y en la cola antes de ser procesadas por el client de Keeper, en milisegundos.         |
| `keeper_receive_request_time_milliseconds_bucket`            | Tiempo necesario para recibir y analizar una solicitud del client en el handler TCP de Keeper, en milisegundos.                                      |
| `keeper_dispatcher_requests_queue_time_milliseconds_bucket`  | Tiempo que una solicitud pasa en la cola de solicitudes del despachador de Keeper, en milisegundos.                                                  |
| `keeper_write_pre_commit_time_milliseconds_bucket`           | Tiempo necesario para preprocesar una solicitud de escritura antes del commit de Raft, en milisegundos.                                              |
| `keeper_write_commit_time_milliseconds_bucket`               | Tiempo necesario para procesar una solicitud de escritura después del commit de Raft, en milisegundos.                                               |
| `keeper_dispatcher_responses_queue_time_milliseconds_bucket` | Tiempo que una respuesta pasa en la cola de respuestas del despachador de Keeper, en milisegundos.                                                   |
| `keeper_send_response_time_milliseconds_bucket`              | Tiempo necesario para enviar una respuesta al client en el handler TCP de Keeper (incluye el encolado y la escritura en el socket), en milisegundos. |
| `keeper_read_wait_for_write_time_milliseconds_bucket`        | Tiempo que una solicitud de lectura espera a que se complete la solicitud de escritura de la que depende, en milisegundos.                           |
| `keeper_read_process_time_milliseconds_bucket`               | Tiempo necesario para procesar una solicitud de lectura en Keeper, en milisegundos.                                                                  |
| `keeper_batch_size_elements_bucket`                          | Tamaño del Batch enviado a Raft, en elementos.                                                                                                       |
| `keeper_batch_size_bytes_bucket`                             | Tamaño del Batch enviado a Raft, en bytes.                                                                                                           |
| `filesystem_cache_evicted_segment_hits_bucket`               | Distribución de los recuentos de aciertos de caché en los segmentos de archivo en el momento de su evicción, etiquetada por nombre de caché.         |
| `filesystem_cache_evicted_segment_size_bytes_bucket`         | Distribución de los tamaños en bytes de los segmentos de archivo desalojados, etiquetada por nombre de caché.                                        |
| `filesystem_cache_evicted_segment_hits_by_user_bucket`       | Distribución de los recuentos de aciertos de caché en los segmentos de archivo desalojados, etiquetada por nombre de caché e ID de usuario.          |
| `filesystem_cache_evicted_segment_size_bytes_by_user_bucket` | Distribución de los tamaños en bytes de los segmentos de archivo desalojados, etiquetada por nombre de caché e ID de usuario.                        |

<div id="see-also">
  ## Véase también
</div>

* [system.asynchronous&#95;metrics](/es/operations/system-tables/asynchronous_metrics) — Contiene métricas calculadas periódicamente.
* [system.events](/es/operations/system-tables/events) — Contiene diversos eventos ocurridos.
* [system.metric&#95;log](/es/operations/system-tables/metric_log) — Contiene un historial de los valores de las métricas de las tablas `system.metrics` y `system.events`.
* [Monitorización](../../operations/monitoring.md) — Conceptos básicos de la monitorización de ClickHouse.