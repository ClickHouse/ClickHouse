---
description: 'Documentación sobre las funciones de comparación'
sidebar_label: 'Comparación'
slug: /sql-reference/functions/comparison-functions
title: 'Funciones de comparación'
doc_type: 'reference'
---

<div id="comparison-rules">
  ## Reglas de comparación
</div>

Las funciones de comparación que se muestran a continuación devuelven `0` o `1` con el tipo [UInt8](/es/sql-reference/data-types/int-uint). Solo se pueden
comparar valores dentro del mismo grupo (por ejemplo, `UInt16` y `UInt64`), pero no entre grupos distintos (por ejemplo, `UInt16` y `DateTime`).
Es posible comparar números y cadenas, así como cadenas con fechas y fechas con horas.
En el caso de las tuplas y los arrays, la comparación es lexicográfica, lo que significa que se compara cada
elemento correspondiente de la tupla/array del lado izquierdo con el del lado derecho.

Se pueden comparar los siguientes tipos:

* números y decimales
* cadenas y cadenas de longitud fija
* fechas
* fechas con horas
* tuplas (comparación lexicográfica)
* arrays (comparación lexicográfica)

:::note
Las cadenas se comparan byte a byte. Esto puede dar lugar a resultados inesperados si una de las cadenas contiene caracteres de varios bytes codificados en UTF-8.
Una cadena S1 que tiene como prefijo otra cadena S2 se considera más larga que S2.
:::

{/* 
  El contenido interno de las etiquetas siguientes se reemplaza, en tiempo de compilación del marco de documentación, con 
  la documentación generada a partir de system.functions. No modifique ni elimine las etiquetas.
  Consulte: https://github.com/ClickHouse/clickhouse-docs/blob/main/contribute/autogenerated-documentation-from-source.md
  */ }

{/*AUTOGENERATED_START*/ }

{/*AUTOGENERATED_END*/ }