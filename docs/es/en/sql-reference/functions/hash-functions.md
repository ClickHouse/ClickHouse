---
description: 'Documentación sobre funciones hash'
sidebar_label: 'Hash'
slug: /sql-reference/functions/hash-functions
title: 'Funciones hash'
doc_type: 'reference'
---

Las funciones hash pueden utilizarse para la reordenación pseudoaleatoria determinista de elementos.

Simhash es una función hash que devuelve valores hash similares para argumentos cercanos (similares).

La mayoría de las funciones hash aceptan cualquier número de argumentos de cualquier tipo.

:::note
El hash de NULL es NULL. Para obtener un hash no NULL de una columna Nullable, envuélvala en una tupla:

```sql
SELECT cityHash64(tuple(NULL))
```

:::

:::note
Para calcular el hash de todo el contenido de una tabla, use `sum(cityHash64(tuple(*)))` (u otra función hash). `tuple` garantiza que no se omitan las filas con valores NULL. `sum` garantiza que el orden de las filas no importe.
:::

{/* 
  El contenido interno de las etiquetas siguientes se sustituye durante el proceso de build del framework de documentación por 
  la documentación generada a partir de system.functions. No modifique ni elimine las etiquetas.
  Consulte: https://github.com/ClickHouse/clickhouse-docs/blob/main/contribute/autogenerated-documentation-from-source.md
  */ }

{/*AUTOGENERATED_START*/ }

{/*AUTOGENERATED_END*/ }