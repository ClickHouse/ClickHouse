---
title: Función Template
---



## functionName {#functionname-in-lower-case}

Descripción breve.

**Sintaxis** (sin SELECT)

```sql
<function syntax>
```

Alias: `<alias name>`. (Opcional)

Más texto (Opcional).

**Argumentos** (Opcional)

-   `x` — Descripción. Opcional (solo para argumentos opcionales). Valores posibles: <values list>. Valor predeterminado: <value>. [Type name](relative/path/to/type/dscr.md#type).
-   `y` — Descripción. Opcional (solo para argumentos opcionales). Valores posibles: <values list>. Valor predeterminado: <value>. [Type name](relative/path/to/type/dscr.md#type).

**Parámetros** (Opcional, solo para funciones de agregación paramétricas)

-   `z` — Descripción. Opcional (solo para parámetros opcionales). Valores posibles: <values list>. Valor predeterminado: <value>. [Type name](relative/path/to/type/dscr.md#type).

**Valor(es) devuelto(s)**

-   Lista de valores devueltos.

Type: [Type name](relative/path/to/type/dscr.md#type).

**Ejemplo**

El ejemplo debe mostrar el uso y/o casos de uso. El siguiente texto contiene las partes recomendadas de un ejemplo.

Tabla de entrada (Opcional):

```text
```

```sql title="Query"
```

```text title="Response"
```

**Véase también** (Opcional)

-   [link](#)