---
slug: /use-cases/observability/clickstack/sdks/deno
description: 'SDK de Deno para ClickStack - la pila de observabilidad de ClickHouse'
title: 'Deno'
doc_type: 'guide'
keywords: ['SDK de Deno para ClickStack', 'Deno OpenTelemetry', 'integración de Deno con ClickStack', 'observabilidad en Deno', 'SDK de logging para Deno']
---

import { ClickStackIntegrates } from '/snippets/es/components/ClickStackIntegrates/ClickStackIntegrates.jsx'

<ClickStackIntegrates signals={['logs']} />

<Note>
  Actualmente, solo admite OpenTelemetry Logging. Para obtener compatibilidad con tracing, [consulta la siguiente guía](https://dev.to/grunet/leveraging-opentelemetry-in-deno-45bj#a-minimal-interesting-example).
</Note>

## Logging {#logging}

Se admite el Logging exportando un registrador personalizado para el módulo `std/log`.

**Ejemplo de uso:**

```typescript
import * as log from 'https://deno.land/std@0.203.0/log/mod.ts';
import { OpenTelemetryHandler } from 'npm:@hyperdx/deno';

log.setup({
  handlers: {
    otel: new OpenTelemetryHandler('DEBUG'),
  },

  loggers: {
    'my-otel-logger': {
      level: 'DEBUG',
      handlers: ['otel'],
    },
  },
});

log.getLogger('my-otel-logger').info('Hello from Deno!');
```

### Ejecuta la aplicación {#run-the-application}

<Tabs>
  <Tab title="Managed ClickStack">
    ```shell
    OTEL_EXPORTER_OTLP_ENDPOINT=<YOUR_OTEL_COLLECTOR_HTTP_ENDPOINT> \
    OTEL_SERVICE_NAME="<YOUR_SERVICE_NAME>" \
    deno run --allow-net --allow-env --allow-read --allow-sys --allow-run app.ts
    ```
  </Tab>

  <Tab title="ClickStack Open Source">
    ```shell
    OTEL_EXPORTER_OTLP_HEADERS="authorization=<YOUR_INGESTION_API_KEY>" \
    OTEL_EXPORTER_OTLP_ENDPOINT=<YOUR_OTEL_COLLECTOR_HTTP_ENDPOINT> \
    OTEL_SERVICE_NAME="<YOUR_SERVICE_NAME>" \
    deno run --allow-net --allow-env --allow-read --allow-sys --allow-run app.ts
    ```
  </Tab>
</Tabs>