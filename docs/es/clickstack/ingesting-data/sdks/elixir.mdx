---
slug: /use-cases/observability/clickstack/sdks/elixir
description: 'SDK de Elixir para ClickStack - El stack de observabilidad de ClickHouse'
title: 'Elixir'
doc_type: 'guide'
keywords: ['SDK de ClickStack para Elixir', 'observabilidad en Elixir', 'HyperDX Elixir', 'SDK de logging para Elixir', 'integración de ClickStack con Elixir']
---

import { ClickStackIntegrates } from '/snippets/es/components/ClickStackIntegrates/ClickStackIntegrates.jsx'

<ClickStackIntegrates signals={['logs']} />

*🚧 ¡La instrumentación de métricas y trazas de OpenTelemetry estará disponible próximamente!*

## Primeros pasos {#getting-started}

### Instala el paquete backend del logger de ClickStack {#install-hyperdx-logger-backend-package}

El paquete se puede instalar agregando `hyperdx` a tu lista de dependencias en
`mix.exs`:

```elixir
def deps do
  [
    {:hyperdx, "~> 0.1.6"}
  ]
end
```

### Configurar Logger {#configure-logger}

Añade lo siguiente a tu archivo `config.exs`:

```elixir
# config/releases.exs

config :logger,
  level: :info,
  backends: [:console, {Hyperdx.Backend, :hyperdx}]
```

### Configurar variables de entorno {#configure-environment-variables}

Después, tendrás que configurar las siguientes variables de entorno en tu
shell para enviar telemetría a ClickStack a través del collector de OpenTelemetry:

<Tabs>
  <Tab title="Managed ClickStack">
    ```shell
    OTEL_SERVICE_NAME='<YOUR_SERVICE_NAME>'
    ```
  </Tab>

  <Tab title="ClickStack Open Source">
    ```shell
    export HYPERDX_API_KEY='<YOUR_INGESTION_API_KEY>' \
    OTEL_SERVICE_NAME='<YOUR_SERVICE_NAME>'
    ```
  </Tab>
</Tabs>

*La variable de entorno `OTEL_SERVICE_NAME` se utiliza para identificar tu servicio
en la aplicación HyperDX; puede tener cualquier nombre que quieras.*