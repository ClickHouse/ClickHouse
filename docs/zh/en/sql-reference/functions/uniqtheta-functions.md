---
description: 'uniqTheta 函数文档'
sidebar_label: 'uniqTheta'
slug: /sql-reference/functions/uniqtheta-functions
title: 'uniqTheta 函数'
doc_type: 'reference'
---

uniqTheta 函数用于对两个 uniqThetaSketch 对象执行集合运算，例如 ∪ / ∩ / × (union/intersect/not) ，并返回一个包含结果的新 uniqThetaSketch 对象。

uniqThetaSketch 对象通过带有 -State 后缀的聚合函数 uniqTheta 构造。

UniqThetaSketch 是一种用于存储近似值集合的数据结构。
更多信息，请参阅：[Theta Sketch Framework](https://datasketches.apache.org/docs/Theta/ThetaSketches.html#theta-sketch-framework)。

<div id="uniqthetaunion">
  ## uniqThetaUnion
</div>

两个 uniqThetaSketch 对象用于执行并集计算 (集合运算 ∪) ，结果是一个新的 uniqThetaSketch。

```sql
uniqThetaUnion(uniqThetaSketch,uniqThetaSketch)
```

**参数**

* `uniqThetaSketch` – uniqThetaSketch 对象。

**示例**

```sql
SELECT finalizeAggregation(uniqThetaUnion(a, b)) AS a_union_b, finalizeAggregation(a) AS a_cardinality, finalizeAggregation(b) AS b_cardinality
FROM
(SELECT arrayReduce('uniqThetaState',[1,2]) AS a, arrayReduce('uniqThetaState',[2,3,4]) AS b );
```

```text
┌─a_union_b─┬─a_cardinality─┬─b_cardinality─┐
│         4 │             2 │             3 │
└───────────┴───────────────┴───────────────┘
```

<div id="uniqthetaintersect">
  ## uniqThetaIntersect
</div>

两个 uniqThetaSketch 对象 进行 intersect 计算 (集合运算 ∩) ，结果会得到一个新的 uniqThetaSketch。

```sql
uniqThetaIntersect(uniqThetaSketch,uniqThetaSketch)
```

**参数**

* `uniqThetaSketch` – uniqThetaSketch 对象。

**示例**

```sql
SELECT finalizeAggregation(uniqThetaIntersect(a, b)) AS a_intersect_b, finalizeAggregation(a) AS a_cardinality, finalizeAggregation(b) AS b_cardinality
FROM
(SELECT arrayReduce('uniqThetaState',[1,2]) AS a, arrayReduce('uniqThetaState',[2,3,4]) AS b );
```

```text
┌─a_intersect_b─┬─a_cardinality─┬─b_cardinality─┐
│             1 │             2 │             3 │
└───────────────┴───────────────┴───────────────┘
```

<div id="uniqthetanot">
  ## uniqThetaNot
</div>

两个 uniqThetaSketch 对象进行 a&#95;not&#95;b 计算 (集合运算 ×) ，结果会生成一个新的 uniqThetaSketch。

```sql
uniqThetaNot(uniqThetaSketch,uniqThetaSketch)
```

**参数**

* `uniqThetaSketch` – uniqThetaSketch 对象。

**示例**

```sql
SELECT finalizeAggregation(uniqThetaNot(a, b)) AS a_not_b, finalizeAggregation(a) AS a_cardinality, finalizeAggregation(b) AS b_cardinality
FROM
(SELECT arrayReduce('uniqThetaState',[2,3,4]) AS a, arrayReduce('uniqThetaState',[1,2]) AS b );
```

```text
┌─a_not_b─┬─a_cardinality─┬─b_cardinality─┐
│       2 │             3 │             2 │
└─────────┴───────────────┴───────────────┘
```

**另请参阅**

* [uniqThetaSketch](/zh/sql-reference/aggregate-functions/reference/uniqthetasketch)

{/* 
  以下标签中的内容会在文档框架构建时
  替换为根据 system.functions 生成的文档。请勿修改或删除这些标签。
  参见：https://github.com/ClickHouse/clickhouse-docs/blob/main/contribute/autogenerated-documentation-from-source.md
  */ }

{/*AUTOGENERATED_START*/ }

{/*AUTOGENERATED_END*/ }