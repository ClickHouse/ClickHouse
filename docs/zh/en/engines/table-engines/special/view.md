---
description: '用于实现视图（更多信息，请参见 `CREATE VIEW
  query`）。它不存储数据，只存储指定的 `SELECT` 查询。从表中读取时，会运行该查询（并从查询中去除所有不必要的列）。'
sidebar_label: '视图'
sidebar_position: 90
slug: /engines/table-engines/special/view
title: '视图表引擎'
doc_type: '参考'
---

用于实现视图 (更多信息，请参见 `CREATE VIEW query`) 。它不存储数据，只存储指定的 `SELECT` 查询。从表中读取时，会运行该查询 (并从查询中去除所有不必要的列) 。