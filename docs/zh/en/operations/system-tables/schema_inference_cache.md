---
description: '包含所有已缓存文件 schema 信息的系统表。'
keywords: ['系统表', 'schema_inference_cache']
slug: /operations/system-tables/schema_inference_cache
title: 'system.schema_inference_cache'
doc_type: 'reference'
---

import SystemTableCloud from '@site/docs/_snippets/_system_table_cloud.md';

<SystemTableCloud />

<div id="description">
  ## 描述
</div>

包含所有缓存的文件 schema 信息。

<div id="columns">
  ## 列
</div>

{/*AUTOGENERATED_START*/ }

* `storage` ([String](/zh/sql-reference/data-types/string)) — 存储名称：File、URL、S3 或 HDFS。
* `source` ([String](/zh/sql-reference/data-types/string)) — 文件来源。
* `format` ([String](/zh/sql-reference/data-types/string)) — 格式名称。
* `additional_format_info` ([String](/zh/sql-reference/data-types/string)) — 用于识别 schema 所需的附加信息。例如，格式特定的设置。
* `registration_time` ([DateTime](/zh/sql-reference/data-types/datetime)) — schema 加入缓存时的时间戳。
* `schema` ([Nullable(String)](/zh/sql-reference/data-types/nullable)) — 缓存的 schema。
* `number_of_rows` ([Nullable(UInt64)](/zh/sql-reference/data-types/nullable)) — 给定格式下文件中的行数。它用于缓存数据文件中的简单 `count()` 结果，以及在 schema 推断期间缓存元数据中的行数。
* `schema_inference_mode` ([Nullable(String)](/zh/sql-reference/data-types/nullable)) — schema 推断模式。

{/*AUTOGENERATED_END*/ }

<div id="example">
  ## 示例
</div>

假设我们有一个名为 `data.jsonl` 的文件，内容如下：

```json
{"id" :  1, "age" :  25, "name" :  "Josh", "hobbies" :  ["football", "cooking", "music"]}
{"id" :  2, "age" :  19, "name" :  "Alan", "hobbies" :  ["tennis", "art"]}
{"id" :  3, "age" :  32, "name" :  "Lana", "hobbies" :  ["fitness", "reading", "shopping"]}
{"id" :  4, "age" :  47, "name" :  "Brayan", "hobbies" :  ["movies", "skydiving"]}
```

:::tip
将 `data.jsonl` 放到 `user_files_path` 目录中。可在 ClickHouse 配置文件中查看该路径。默认值为：

```sql
<user_files_path>/var/lib/clickhouse/user_files/</user_files_path>
```

:::

打开 `clickhouse-client`，然后运行 `DESCRIBE` 查询：

```sql
DESCRIBE file('data.jsonl') SETTINGS input_format_try_infer_integers=0;
```

```response
┌─name────┬─type────────────────────┬─default_type─┬─default_expression─┬─comment─┬─codec_expression─┬─ttl_expression─┐
│ id      │ Nullable(Float64)       │              │                    │         │                  │                │
│ age     │ Nullable(Float64)       │              │                    │         │                  │                │
│ name    │ Nullable(String)        │              │                    │         │                  │                │
│ hobbies │ Array(Nullable(String)) │              │                    │         │                  │                │
└─────────┴─────────────────────────┴──────────────┴────────────────────┴─────────┴──────────────────┴────────────────┘
```

我们来查看一下 `system.schema_inference_cache` 表的内容：

```sql
SELECT *
FROM system.schema_inference_cache
FORMAT Vertical
```

```response
Row 1:
──────
storage:                File
source:                 /home/droscigno/user_files/data.jsonl
format:                 JSONEachRow
additional_format_info: schema_inference_hints=, max_rows_to_read_for_schema_inference=25000, schema_inference_make_columns_nullable=true, try_infer_integers=false, try_infer_dates=true, try_infer_datetimes=true, try_infer_numbers_from_strings=true, read_bools_as_numbers=true, try_infer_objects=false
registration_time:      2022-12-29 17:49:52
schema:                 id Nullable(Float64), age Nullable(Float64), name Nullable(String), hobbies Array(Nullable(String))
```

<div id="see-also">
  ## 另请参阅
</div>

* [从输入数据自动推断 schema](/zh/interfaces/schema-inference.md)