---
noindex: true
title: "ClickHouse Private 文档"
sidebarTitle: "概览"
icon: "house"
description: "在隔离网络和自管理环境中部署、运维并深入了解 ClickHouse Private。"
mode: "custom"
---

export const HeroCard = ({ filename, darkFilename: darkFilenameProp, title, description, body, href, links, icon, showArrow, arrowBottom, extraStyle, extraClass, onClick }) => {
    const assetBase = typeof window !== 'undefined' && window.location.pathname.startsWith('/docs') ? '/docs' : '';
    const withBase = (p) => p && p.startsWith('/') ? assetBase + p : p;
    const darkFilename = darkFilenameProp || (filename ? filename.replace('-light.', '-dark.') : null);
    const cardClassName = `ch-hero-card ${arrowBottom ? 'flex flex-col' : 'block'} p-4 rounded-lg no-underline bg-[#f9f9f9] border border-[#e5e7eb] text-inherit ${extraClass || ''}`;

    const content = (
        <>
            {icon ? (
                <div className="mb-6">
                    {icon}
                </div>
            ) : filename ? (
                <>
                    <img
                        src={`${assetBase}/images/${filename}`}
                        alt={title}
                        className="block dark:hidden w-full h-48 object-cover rounded-lg border border-[#d1d5db] pointer-events-none"
                    />
                    <img
                        src={`${assetBase}/images/${darkFilename}`}
                        alt={title}
                        className="hidden dark:block w-full h-48 object-cover rounded-lg pointer-events-none"
                    />
                </>
            ) : null}
            <h3 className={`text-[#111827] dark:text-white text-lg font-semibold leading-[1.3] ${(icon || !filename) ? 'mt-0' : 'mt-5'}`}>
                {title}
            </h3>
            {body ? body : (
                <span className="block text-[#6b7280] dark:text-[#a1a1aa] mt-2 text-sm font-normal leading-normal min-h-[42px]">{description}</span>
            )}
            {links && links.length > 0 && (
                <ul className="mt-6 list-none p-0">
                    {links.map((link, index) => (
                        <li key={index} className={index > 0 ? 'mt-2' : 'mt-0'}>
                            <a href={link.href} onClick={(e) => { e.preventDefault(); window.location.href = withBase(link.href); }} className="goal-card-link flex items-center justify-between text-[#374151] dark:text-[#FCFF74] text-sm font-medium no-underline gap-1">
                                <span className="truncate min-w-0">{link.label}</span>
                                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" className="shrink-0">
                                    <path d="M5 12h14M12 5l7 7-7 7"/>
                                </svg>
                            </a>
                        </li>
                    ))}
                </ul>
            )}
            {showArrow && (
                <>
                    {arrowBottom && <div className="flex-1" />}
                    <div className="ch-usecase-arrow flex justify-end mt-4">
                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
                            <path d="M5 12h14M12 5l7 7-7 7"/>
                        </svg>
                    </div>
                </>
            )}
        </>
    );

    if (links && links.length > 0) {
        return <div className={cardClassName} style={extraStyle}>{content}</div>;
    }

    return <a className={cardClassName} href={href || '#'} style={extraStyle} onClick={(e) => { e.preventDefault(); if (onClick) { onClick(); } else { window.location.href = withBase(href); } }}>{content}</a>;
};

export const SectionExplorer = () => {
    const [view, setViewState] = useState('home');
    const [entered, setEntered] = useState(false);

    const setView = (v) => {
        setEntered(false);
        setViewState(v);
    };

    useEffect(() => {
        let raf2;
        const raf1 = requestAnimationFrame(() => {
            raf2 = requestAnimationFrame(() => setEntered(true));
        });
        return () => {
            cancelAnimationFrame(raf1);
            if (raf2) cancelAnimationFrame(raf2);
        };
    }, [view]);

    const cardAnim = (index) => entered
        ? `transition-all duration-300 ease-out ${['delay-0', 'delay-75', 'delay-150', 'delay-200'][index]} opacity-100 translate-y-0`
        : 'transition-none opacity-0 translate-y-2';

    if (view === 'tutorials') {
        return (
            <div className="mt-16">
                <button
                    type="button"
                    onClick={() => setView('home')}
                    className="inline-flex items-center gap-1.5 mb-4 bg-transparent border-0 p-0 cursor-pointer text-xs text-gray-500 dark:text-zinc-500 hover:text-gray-900 dark:hover:text-[#fdff75] transition-colors"
                >
                    <svg xmlns="http://www.w3.org/2000/svg" width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" className="shrink-0">
                        <path d="M19 12H5M12 19l-7-7 7-7"/>
                    </svg>
                    Back
                </button>
                <div className="grid sm:grid-cols-2 min-[1300px]:grid-cols-4 gap-x-6 gap-y-4">
                    <HeroCard
                        title="AWS"
                        description="Deploy ClickHouse Private on AWS EKS, from IAM roles and VPC to a running cluster."
                        href="/zh/products/clickhouse-private/tutorials/deploy-clickhouse-on-aws-eks"
                        showArrow
                        arrowBottom
                        extraClass={cardAnim(0)}
                    />
                    <HeroCard
                        title="GCP"
                        description="Deploy ClickHouse Private on Google Kubernetes Engine with Workload Identity and GCS."
                        href="/zh/products/clickhouse-private/tutorials/deploy-clickhouse-on-gcp-gke"
                        showArrow
                        arrowBottom
                        extraClass={cardAnim(1)}
                    />
                    <HeroCard
                        title="Bare metal"
                        description="Deploy ClickHouse on bare-metal nodes with Terraform, Ansible, and RKE2."
                        href="/zh/products/clickhouse-private/tutorials/deploy-clickhouse-on-bare-metal"
                        showArrow
                        arrowBottom
                        extraClass={cardAnim(2)}
                    />
                    <HeroCard
                        title="Government (FIPS)"
                        description="Deploy ClickHouse Private with FIPS 140-2 compliance for government environments."
                        href="/zh/products/clickhouse-private/tutorials/deploy-clickhouse-with-fips-compliance-government"
                        showArrow
                        arrowBottom
                        extraClass={cardAnim(3)}
                    />
                </div>
            </div>
        );
    }

    return (
        <div className="grid sm:grid-cols-2 min-[1300px]:grid-cols-4 gap-x-6 gap-y-4 mt-16">
            <HeroCard
                title="Tutorials"
                description="Step-by-step deployment walkthroughs for AWS EKS, GCP GKE, bare metal, and FIPS-compliant government environments."
                onClick={() => setView('tutorials')}
                showArrow
                arrowBottom
                extraClass={cardAnim(0)}
            />
            <HeroCard
                title="How-To Guides"
                description="Task-oriented guides for backups, scaling, upgrades, certificates, monitoring, and troubleshooting tooling."
                href="/zh/products/clickhouse-private/guides/back-up-and-restore-clickhouse-manual"
                showArrow
                arrowBottom
                extraClass={cardAnim(1)}
            />
            <HeroCard
                title="Reference"
                description="Breaking changes, changelog, component versions, API reference, infrastructure requirements, and metrics."
                href="/zh/products/clickhouse-private/reference/breaking-changes"
                showArrow
                arrowBottom
                extraClass={cardAnim(2)}
            />
            <HeroCard
                title="Explanation"
                description="Architecture, compute separation, PKI and mTLS, FIPS compliance, and backup strategy concepts."
                href="/zh/products/clickhouse-private/explanation/clickhouse-operator-architecture"
                showArrow
                arrowBottom
                extraClass={cardAnim(3)}
            />
        </div>
    );
};

<div className="relative z-10 px-4 pt-16 pb-20 lg:pt-20 max-w-6xl mx-auto">
  <h1 className="block text-[34px] font-semibold text-center text-gray-900 dark:text-zinc-50 tracking-tight">
    ClickHouse Private
  </h1>

  <div className="mx-auto px-4 mt-3 text-center text-sm text-gray-500 dark:text-zinc-500">
    在隔离网络和自管理环境中部署、运维并深入了解 ClickHouse Private。
  </div>

  <SectionExplorer />
</div>