---
title: 'iceberg_data_* 会话设置'
sidebarTitle: 'iceberg_data_*'
toc_max_heading_level: 2
description: 'iceberg_data_* 生成分组中的 ClickHouse 会话设置。'
doc_type: 'reference'
---

{/*自动生成开始*/}

import SettingsInfoBlock from "/snippets/zh/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import { VersionHistory } from "/snippets/zh/components/VersionHistory/VersionHistory.jsx";

这些设置可在 [system.settings](/zh/reference/system-tables/settings) 中查看，并由 [源代码](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp) 自动生成。

## iceberg_data_file_size_lower_threshold_compaction {#iceberg_data_file_size_lower_threshold_compaction}

<SettingsInfoBlock type="UInt64" default_value="402653184" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.9"},{"label": "402653184"},{"label": "与 Iceberg `rewrite_data_files` 存储过程推导 `min-file-size-bytes` 的方式保持一致：取目标文件大小（512 MiB）的 0.75 倍。合并整理现在选取小于 384 MiB 的文件，而非小于 10 MiB 的文件。"}]}, {"id": "row-2","items": [{"label": "26.5"},{"label": "10485760"},{"label": "新增设置"}]}]} />

小于该值的数据文件会被选中进行合并整理。

默认值为 Iceberg 表属性 `write.target-file-size-bytes` 文档默认值 (512 MiB) 的 `0.75` 倍，这也是 `rewrite_data_files` 存储过程推导其 `min-file-size-bytes` 选项的方式，
参见 https://iceberg.apache.org/docs/1.5.2/configuration/。

## iceberg_data_file_size_upper_threshold_compaction {#iceberg_data_file_size_upper_threshold_compaction}

<SettingsInfoBlock type="UInt64" default_value="966367641" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.9"},{"label": "966367641"},{"label": "与 Iceberg `rewrite_data_files` 存储过程推导 `max-file-size-bytes` 的方式保持一致：目标文件大小（512 MiB）的 1.8 倍。"}]}, {"id": "row-2","items": [{"label": "26.5"},{"label": "10737418240"},{"label": "新增设置"}]}]} />

超过该大小的数据文件会被选中进行合并整理。

默认值为 Iceberg 表属性 `write.target-file-size-bytes` 文档中所载默认值 (512 MiB) 的 `1.8` 倍，`rewrite_data_files` 存储过程也是以这种方式推导其 `max-file-size-bytes` 选项的，
参见 https://iceberg.apache.org/docs/1.5.2/configuration/。

{/*AUTOGENERATED_END*/}