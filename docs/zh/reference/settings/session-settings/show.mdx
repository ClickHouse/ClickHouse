---
title: 'show_* 会话设置'
sidebarTitle: 'show_*'
toc_max_heading_level: 2
description: 'show_* 生成的组中的 ClickHouse 会话设置。'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/zh/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import VersionHistory from "/snippets/zh/components/VersionHistory/VersionHistory.jsx";

这些设置可在 [system.settings](/zh/reference/system-tables/settings) 中查看，并由 [源代码](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp) 自动生成。

<div id="show_data_lake_catalogs_in_system_tables">
  ## show_data_lake_catalogs_in_system_tables
</div>

<SettingsInfoBlock type="Bool" default_value="0" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "25.8"},{"label": "1"},{"label": "新增设置"}]}, {"id": "row-2","items": [{"label": "25.10"},{"label": "0"},{"label": "默认不在系统表中显示目录"}]}]} />

启用在系统表中显示数据湖目录的功能。

<div id="show_processlist_include_internal">
  ## show_processlist_include_internal
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "25.11"},{"label": "1"},{"label": "新设置。"}]}]} />

在 `SHOW PROCESSLIST` 查询输出中显示内部辅助进程。

内部进程包括字典重新加载、可刷新materialized view 重新加载、在 `SHOW ...` 查询中执行的辅助 `SELECT`，以及为处理损坏表而在内部执行的辅助 `CREATE DATABASE ...` 查询等。

<div id="show_remote_databases_in_system_tables">
  ## show_remote_databases_in_system_tables
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.7"},{"label": "1"},{"label": "新增设置，用于控制是否在系统表中显示 `MySQL` 和 `PostgreSQL` 数据库，包括 `system.tables`、`system.columns` 和 `system.completions`。"}]}, {"id": "row-2","items": [{"label": "26.2"},{"label": "1"},{"label": "新增设置，用于控制是否在系统表中显示 `MySQL` 和 `PostgreSQL` 数据库，包括 `system.tables`、`system.columns` 和 `system.completions`。"}]}]} />

启用后，会在系统表中显示 `MySQL` 和 `PostgreSQL` 数据库。

<div id="show_table_uuid_in_table_create_query_if_not_nil">
  ## show_table_uuid_in_table_create_query_if_not_nil
</div>

<SettingsInfoBlock type="Bool" default_value="0" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "20.7"},{"label": "0"},{"label": "停止在 Engine=Atomic 表的 CREATE 查询中显示表 UUID"}]}]} />

设置 `SHOW TABLE` 查询的显示方式。

可能的值：

* 0 — 显示的查询中不包含表 UUID。
* 1 — 显示的查询中包含表 UUID。

{/*AUTOGENERATED_END*/}