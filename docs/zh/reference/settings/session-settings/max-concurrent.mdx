---
title: 'max_concurrent_* 会话设置'
sidebarTitle: 'max_concurrent_*'
toc_max_heading_level: 2
description: 'ClickHouse 在 max_concurrent_* 自动生成分组中的会话设置。'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/zh/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

这些设置可在 [system.settings](/zh/reference/system-tables/settings) 中查看，并由 [源代码](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp) 自动生成。

<div id="max_concurrent_queries_for_all_users">
  ## max_concurrent_queries_for_all_users
</div>

<SettingsInfoBlock type="UInt64" default_value="0" />

如果此设置的值小于或等于当前正在同时处理的查询数量，则抛出异常。

示例：可将所有用户的 `max_concurrent_queries_for_all_users` 设置为 99，而数据库管理员可将自己的值设为 100，这样即使在 server 过载时，仍可运行查询进行调查。

修改某个查询或用户的此设置，不会影响其他查询。

可能的值：

* 正整数。
* 0 — 不限制。

**示例**

```xml
<max_concurrent_queries_for_all_users>99</max_concurrent_queries_for_all_users>
```

**另请参见**

* [max&#95;concurrent&#95;queries](/zh/reference/settings/server-settings/settings#max_concurrent_queries)

Cloud 默认值：`1000`。

<div id="max_concurrent_queries_for_user">
  ## max_concurrent_queries_for_user
</div>

<SettingsInfoBlock type="UInt64" default_value="0" />

每个用户可同时执行的查询最大数量。

可能的值：

* 正整数。
* 0 — 不限制。

**示例**

```xml
<max_concurrent_queries_for_user>5</max_concurrent_queries_for_user>
```

{/*AUTOGENERATED_END*/}