---
title: 'optimize_injective_* 会话设置'
sidebarTitle: 'optimize_injective_*'
toc_max_heading_level: 2
description: 'ClickHouse 中 optimize_injective_* 自动生成分组下的会话设置。'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/zh/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import VersionHistory from "/snippets/zh/components/VersionHistory/VersionHistory.jsx";

这些设置可在 [system.settings](/zh/reference/system-tables/settings) 中找到，并由 [源代码](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp) 自动生成。

<div id="optimize_injective_functions_in_group_by">
  ## optimize_injective_functions_in_group_by
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "24.1"},{"label": "1"},{"label": "在 analyzer 中将 GROUP BY 部分中的单射函数替换为其参数"}]}]} />

将 GROUP BY 部分中的单射函数替换为其参数

<div id="optimize_injective_functions_in_limit_by">
  ## optimize_injective_functions_in_limit_by
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.6"},{"label": "1"},{"label": "新增设置：在 LIMIT BY 键中将单射函数替换为其参数。"}]}]} />

在 LIMIT BY 部分中，将单射函数替换为其参数。

示例：`LIMIT 5 BY toString(x)` 会变成 `LIMIT 5 BY x`。

<div id="optimize_injective_functions_inside_uniq">
  ## optimize_injective_functions_inside_uniq
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

移除 `uniq*()` 函数内部的单参数单射函数。

{/*AUTOGENERATED_END*/}