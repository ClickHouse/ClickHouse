---
title: 'unique_key_* 会话设置'
sidebarTitle: 'unique_key_*'
toc_max_heading_level: 2
description: 'unique_key_* 自动生成组中的 ClickHouse 会话设置。'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/zh/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import { VersionHistory } from "/snippets/zh/components/VersionHistory/VersionHistory.jsx";
import { ExperimentalBadge } from "/snippets/zh/components/ExperimentalBadge/ExperimentalBadge.jsx";

这些设置可在 [system.settings](/zh/reference/system-tables/settings) 中查看，并根据[源代码](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp)自动生成。

<div id="unique_key_max_encoded_size">
  ## unique_key_max_encoded_size
</div>

<ExperimentalBadge />

<SettingsInfoBlock type="UInt64" default_value="256" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.6"},{"label": "256"},{"label": "新增设置：单个 UNIQUE KEY 行的保序二进制编码的最大大小（字节）"}]}]} />

单个 `UNIQUE KEY` 行的保序二进制编码的最大大小 (字节) 。

<div id="unique_key_probe_implementation">
  ## unique_key_probe_implementation
</div>

<ExperimentalBadge />

<SettingsInfoBlock type="UniqueKeyProbeImplementation" default_value="auto" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.8"},{"label": "auto"},{"label": "新设置：选择 UNIQUE KEY 探测实现（目前仅提供简单基线实现）"}]}]} />

选择 UNIQUE KEY 写入路径中使用的探测实现。目前仅提供单线程的
基线实现，因此 `auto` 和 `simple` 都会使用该实现。

预留：此设置暂不生效——在构建探测的 UNIQUE KEY INSERT
写入路径完成接入前，不会读取该值。现先声明此设置，以便该开关随实现一同发布。

{/*AUTOGENERATED_END*/}