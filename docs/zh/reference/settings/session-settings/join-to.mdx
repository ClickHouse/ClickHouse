---
title: 'join_to_* 会话设置'
sidebarTitle: 'join_to_*'
toc_max_heading_level: 2
description: 'ClickHouse 在 join_to_* 分组中的会话设置。'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import ExperimentalBadge from "/snippets/zh/components/ExperimentalBadge/ExperimentalBadge.jsx";
import SettingsInfoBlock from "/snippets/zh/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import VersionHistory from "/snippets/zh/components/VersionHistory/VersionHistory.jsx";

这些设置可在 [system.settings](/zh/reference/system-tables/settings) 中找到，并由 [源码](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp) 自动生成。

<div id="join_to_sort_maximum_table_rows">
  ## join_to_sort_maximum_table_rows
</div>

<ExperimentalBadge />

<SettingsInfoBlock type="UInt64" default_value="10000" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "24.9"},{"label": "10000"},{"label": "在 LEFT JOIN 或 INNER JOIN 中，用于判断是否按键对右表重新排序的右表最大行数"}]}]} />

在 LEFT JOIN 或 INNER JOIN 中，用于判断是否按键对右表重新排序的右表最大行数。

<div id="join_to_sort_minimum_perkey_rows">
  ## join_to_sort_minimum_perkey_rows
</div>

<ExperimentalBadge />

<SettingsInfoBlock type="UInt64" default_value="40" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "24.9"},{"label": "40"},{"label": "用于确定在 LEFT JOIN 或 INNER JOIN 中，是否需要按键对右表重新排序的右表每个键平均行数下限。此设置可确保该优化不会应用于键分布稀疏的表"}]}]} />

用于确定在 LEFT JOIN 或 INNER JOIN 中，是否需要按键对右表重新排序的右表每个键平均行数下限。此设置可确保该优化不会应用于键分布稀疏的表

{/*AUTOGENERATED_END*/}