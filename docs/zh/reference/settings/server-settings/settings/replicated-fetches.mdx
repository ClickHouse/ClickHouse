---
title: 'replicated_fetches_* 服务器设置'
sidebarTitle: 'replicated_fetches_*'
toc_max_heading_level: 2
description: '自动生成的 replicated_fetches_* 分组中的 ClickHouse 服务器设置。'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/zh/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

这些设置用于配置 ClickHouse server，内容由 ClickHouse 源代码自动生成。

<div id="replicated_fetches_http_connection_timeout">
  ## replicated_fetches_http_connection_timeout
</div>

<SettingsInfoBlock type="Seconds" default_value="0" changeable_without_restart="No" />分片拉取请求的 HTTP 连接超时时间。如果未显式设置，则继承自默认 profile `http_connection_timeout`。

<div id="replicated_fetches_http_receive_timeout">
  ## replicated_fetches_http_receive_timeout
</div>

<SettingsInfoBlock type="秒" default_value="0" changeable_without_restart="否" />拉取分片请求的 HTTP 接收超时时间。如果未显式设置，则继承自默认 profile `http_receive_timeout`。

<div id="replicated_fetches_http_send_timeout">
  ## replicated_fetches_http_send_timeout
</div>

<SettingsInfoBlock type="秒" default_value="0" changeable_without_restart="否" />分片拉取请求的 HTTP 发送超时时间。若未显式设置，则继承默认 profile `http_send_timeout` 的值。

{/*AUTOGENERATED_END*/}