---
title: 'storage_* 服务器设置'
sidebarTitle: 'storage_*'
toc_max_heading_level: 2
description: '属于 storage_* 自动生成组的 ClickHouse 服务器设置。'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/zh/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

这些设置用于配置 ClickHouse 服务器，由 ClickHouse 源代码自动生成。

<div id="storage_configuration">
  ## storage_configuration
</div>

支持存储的多磁盘配置。

存储配置的结构如下：

```xml
<storage_configuration>
    <disks>
        <!-- configuration -->
    </disks>
    <policies>
        <!-- configuration -->
    </policies>
</storage_configuration>
```

<div id="configuration-of-disks">
  ### `disks` 的配置
</div>

`disks` 的配置结构如下：

```xml
<storage_configuration>
    <disks>
        <disk_name_1>
            <path>/mnt/fast_ssd/clickhouse/</path>
        </disk_name_1>
        <disk_name_2>
            <path>/mnt/hdd1/clickhouse/</path>
            <keep_free_space_bytes>10485760</keep_free_space_bytes>
        </disk_name_2>
        <disk_name_3>
            <path>/mnt/hdd2/clickhouse/</path>
            <keep_free_space_bytes>10485760</keep_free_space_bytes>
        </disk_name_3>
        ...
    </disks>
</storage_configuration>
```

上述子标签为 `disks` 定义了以下设置：

| Setting                 | Description                                       |
| ----------------------- | ------------------------------------------------- |
| `<disk_name_N>`         | 磁盘名称，必须是唯一的。                                      |
| `path`                  | 服务器 数据的存储路径 (`data` 和 `shadow` 目录) 。必须以 `/` 结尾 |
| `keep_free_space_bytes` | 磁盘上预留的可用空间大小。                                     |

<Note>
  `disks` 的顺序无关紧要。
</Note>

<div id="configuration-of-policies">
  ### 策略配置
</div>

上述子标签为 `policies` 定义了以下设置：

| Setting                      | Description                                                                                                                                                               |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `policy_name_N`              | 策略名称。策略名称必须唯一。                                                                                                                                                            |
| `volume_name_N`              | 卷名称。卷名称必须唯一。                                                                                                                                                              |
| `disk`                       | 卷内的磁盘。                                                                                                                                                                    |
| `max_data_part_size_bytes`   | 此卷中任一磁盘上可存放的数据块的最大大小。如果合并后产生的数据块预计会大于 max&#95;data&#95;part&#95;size&#95;bytes，则该数据块会写入下一个卷。该功能的本质是：将新的 / 较小的数据块存储在热 (SSD) 卷上，当其变大后再移动到冷 (HDD) 卷上。如果策略中只有一个卷，请不要使用此选项。    |
| `move_factor`                | 卷上可用空间的占比。如果可用空间低于该值，数据将开始转移到下一个卷 (如果有) 。转移时，数据块会按大小从大到小排序 (降序) ，并选取总大小足以满足 `move_factor` 条件的数据块；如果所有数据块的总大小仍然不足，则会移动所有数据块。                                               |
| `perform_ttl_move_on_insert` | 禁用在插入时移动生存时间 (TTL) 已过期的数据。默认情况下 (即启用时) ，如果插入的一部分数据根据 move TTL 规则已过期，它会立即被移动到该规则指定的卷 / 磁盘。如果目标卷 / 磁盘较慢 (例如 S3) ，这可能会显著降低插入速度。如果禁用，则已过期的数据部分会先写入默认卷，然后立即移动到规则中为过期 TTL 指定的卷。 |
| `load_balancing`             | 磁盘均衡策略，`round_robin` 或 `least_used`。                                                                                                                                      |
| `least_used_ttl_ms`          | 设置更新所有磁盘可用空间的超时时间 (以毫秒为单位)  (`0` 表示始终更新，`-1` 表示从不更新，默认值为 `60000`) 。注意，如果磁盘仅由 ClickHouse 使用，且文件系统不会动态扩容，可以使用 `-1`。其他情况下不建议这样做，因为最终会导致空间分配不正确。                              |
| `prefer_not_to_merge`        | 禁止在此卷上合并数据 parts。注意：这可能有害，并会导致性能下降。启用此设置时 (不要这样做) ，将禁止在此卷上合并数据 (这并不是好事) 。该选项可用于控制 ClickHouse 与慢速磁盘的交互方式。我们建议完全不要使用它。                                                      |
| `volume_priority`            | 定义卷被写满时的优先级 (顺序) 。值越小，优先级越高。参数值必须是自然数，并且必须无空缺地覆盖从 1 到 N 的范围 (N 为指定的最大参数值) 。                                                                                               |

对于 `volume_priority`：

* 如果所有卷都设置了此参数，则按指定顺序确定优先级。
* 如果只有&#95;部分&#95;卷设置了此参数，则未设置该参数的卷优先级最低。设置了该参数的卷按标签值确定优先级，其余卷之间的优先级则由它们在配置文件中的描述顺序决定。
* 如果&#95;没有&#95;任何卷设置此参数，则它们的顺序由其在配置文件中的描述顺序决定。
* 各卷的优先级不能相同。

<div id="storage_metadata_write_full_object_key">
  ## storage_metadata_write_full_object_key
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />将磁盘元数据文件按 VERSION&#95;FULL&#95;OBJECT&#95;KEY 格式写入。此设置默认启用。该设置已弃用。

<div id="storage_shared_set_join_use_inner_uuid">
  ## storage_shared_set_join_use_inner_uuid
</div>

<SettingsInfoBlock type="Bool" default_value="1" changeable_without_restart="No" />启用后，在创建 SharedSet 和 SharedJoin 时会生成内部 UUID。仅 ClickHouse Cloud 提供

{/*AUTOGENERATED_END*/}