---
title: 'max_remote_* 服务器设置'
sidebarTitle: 'max_remote_*'
toc_max_heading_level: 2
description: 'ClickHouse 在 max_remote_* 生成分组中的服务器设置。'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/zh/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

这些设置用于配置 ClickHouse server，由 ClickHouse 源代码自动生成。

<div id="max_remote_read_connections">
  ## max_remote_read_connections
</div>

<SettingsInfoBlock type="UInt64" default_value="1000" changeable_without_restart="否" />`ReaderExecutor` 为优化顺序读取而保持存活的远程读取连接的最大打开数量。设置为 0 时将禁用连接复用。

<div id="max_remote_read_network_bandwidth_for_server">
  ## max_remote_read_network_bandwidth_for_server
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Yes" />

读取时通过网络进行数据交换的最大速度，以每秒字节数为单位。

<Note>
  值为 `0` (默认值) 表示不受限制。
</Note>

<div id="max_remote_write_network_bandwidth_for_server">
  ## max_remote_write_network_bandwidth_for_server
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Yes" />

写入时通过网络进行数据交换的最大速率，单位为字节/秒。

<Note>
  值为 `0` (默认值) 表示无限制。
</Note>

{/*AUTOGENERATED_END*/}