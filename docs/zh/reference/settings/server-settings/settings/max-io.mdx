---
title: 'max_io_* 服务器设置'
sidebarTitle: 'max_io_*'
toc_max_heading_level: 2
description: 'max_io_* 自动生成分组中的 ClickHouse 服务器设置。'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/zh/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

这些设置用于配置 ClickHouse 服务器，由 ClickHouse 源代码自动生成。

<div id="max_io_thread_pool_free_size">
  ## max_io_thread_pool_free_size
</div>

<SettingsInfoBlock type="UInt64" default_value="0" changeable_without_restart="Yes" />

如果 IO Thread pool 中的空闲线程数超过 `max_io_thread_pool_free_size`，ClickHouse 将释放这些空闲线程占用的资源，并缩减线程池大小。必要时，仍可再次创建线程。

<div id="max_io_thread_pool_size">
  ## max_io_thread_pool_size
</div>

<SettingsInfoBlock type="UInt64" default_value="100" changeable_without_restart="Yes" />

ClickHouse 使用 IO Thread pool 中的线程来执行某些 IO 操作 (例如与 S3 交互) 。`max_io_thread_pool_size` 用于限制池中的线程最大数量。

{/*AUTOGENERATED_END*/}