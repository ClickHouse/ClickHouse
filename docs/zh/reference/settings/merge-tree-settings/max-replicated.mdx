---
title: 'max_replicated_* MergeTree 表设置'
sidebarTitle: 'max_replicated_*'
toc_max_heading_level: 2
description: 'ClickHouse 中 max_replicated_* 自动生成分组下的 MergeTree 表设置。'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/zh/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

这些设置可在 [system.merge&#95;tree&#95;settings](/zh/reference/system-tables/merge_tree_settings) 中查看，且由 ClickHouse 源码自动生成。

<div id="max_replicated_fetches_network_bandwidth">
  ## max_replicated_fetches_network_bandwidth
</div>

<SettingsInfoBlock type="UInt64" default_value="0" />

限制 [replicated](/zh/reference/engines/table-engines/mergetree-family/replication)
拉取在网络上进行数据交换时的最大速度，单位为每秒字节。与应用于服务器的
[`max_replicated_fetches_network_bandwidth_for_server`](/zh/reference/settings/merge-tree-settings/max-replicated#max_replicated_fetches_network_bandwidth)
设置不同，此设置适用于特定表。

你可以同时限制服务器网络和特定表的网络，但在这种情况下，
表级设置的值应小于服务器级
设置的值。否则，服务器只会考虑
`max_replicated_fetches_network_bandwidth_for_server` 设置。

该设置无法被完全精确地遵循。

可能的值：

* 正整数。
* `0` — 不限制。

默认值：`0`。

**用法**

可用于在复制数据以添加或替换新
节点时对速度进行节流。

<div id="max_replicated_logs_to_keep">
  ## max_replicated_logs_to_keep
</div>

<SettingsInfoBlock type="UInt64" default_value="1000" />

如果存在不活跃的副本，ClickHouse Keeper 日志中最多可保留多少条记录。超过该数量时，不活跃的副本将被视为丢失。

可能的值：

* 任意正整数。

<div id="max_replicated_merges_in_queue">
  ## max_replicated_merges_in_queue
</div>

<SettingsInfoBlock type="UInt64" default_value="1000" />

ReplicatedMergeTree 队列中同时允许执行的 parts 合并和变更任务数量。

<div id="max_replicated_merges_with_ttl_in_queue">
  ## max_replicated_merges_with_ttl_in_queue
</div>

<SettingsInfoBlock type="UInt64" default_value="1" />

ReplicatedMergeTree 队列中允许同时进行的带有生存时间 (TTL) 的 parts 合并任务数量。

<div id="max_replicated_mutations_in_queue">
  ## max_replicated_mutations_in_queue
</div>

<SettingsInfoBlock type="UInt64" default_value="8" />

ReplicatedMergeTree 队列中允许同时进行的
正在变更的 parts 任务数。

<div id="max_replicated_sends_network_bandwidth">
  ## max_replicated_sends_network_bandwidth
</div>

<SettingsInfoBlock type="UInt64" default_value="0" />

限制 [replicated](/zh/reference/engines/table-engines/mergetree-family/replacingmergetree)
发送的数据交换最大速率，单位为每秒字节数。与应用于
服务器的
[`max_replicated_sends_network_bandwidth_for_server`](/zh/reference/settings/merge-tree-settings/max-replicated#max_replicated_sends_network_bandwidth)
设置不同，此设置作用于特定表。

你可以同时限制服务器网络和特定表的网络速率，但在这种情况下，表级设置的值必须小于
服务器级设置的值。否则，服务器只会采用
`max_replicated_sends_network_bandwidth_for_server` 设置。

此设置无法做到绝对精确。

可能的值：

* 正整数。
* `0` — 不限。

**用法**

可用于在复制数据以添加或替换新节点时限制传输速率。

{/*AUTOGENERATED_END*/}