---
title: 'map_buckets_* MergeTree 表设置'
sidebarTitle: 'map_buckets_*'
toc_max_heading_level: 2
description: 'ClickHouse 在 map_buckets_* 自动生成组中的 MergeTree 表设置。'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/zh/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import VersionHistory from "/snippets/zh/components/VersionHistory/VersionHistory.jsx";

这些设置可在 [system.merge&#95;tree&#95;settings](/zh/reference/system-tables/merge_tree_settings) 中查看，且由 ClickHouse 源代码自动生成。

<div id="map_buckets_coefficient">
  ## map_buckets_coefficient
</div>

<SettingsInfoBlock type="Float" default_value="1" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.3"},{"label": "1"},{"label": "新增一个设置，用于控制在 'with_buckets' Map 序列化中，使用 `sqrt` 和 `linear` 策略计算桶数量时的系数"}]}]} />

在 `sqrt` 和 `linear` [map&#95;buckets&#95;strategy](/zh/reference/settings/merge-tree-settings/map-buckets#map_buckets_strategy) 中，用于根据平均 Map 大小计算桶数量的系数。
对于 `sqrt` 策略：`round(map_buckets_coefficient * sqrt(avg_map_size))`。
对于 `linear` 策略：`round(map_buckets_coefficient * avg_map_size)`。
当 `map_buckets_strategy` 为 `constant` 时，此设置将被忽略。

<div id="map_buckets_min_avg_size">
  ## map_buckets_min_avg_size
</div>

<SettingsInfoBlock type="UInt64" default_value="32" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.3"},{"label": "32"},{"label": "新增一个设置，用于控制应用 `with_buckets` 序列化所需的最小平均 Map 大小（每行的键数）"}]}]} />

应用 `with_buckets` 序列化所需的最小平均 Map 大小 (每行的键数) 。
如果平均 Map 大小小于该值，则无论其他桶设置如何，都会使用单个桶。
值为 `0` 时，将禁用该阈值，并始终应用分桶策略。
此设置可用于避免对小型 Map 使用分桶序列化所带来的额外开销，因为其收益微乎其微。

<div id="map_buckets_strategy">
  ## map_buckets_strategy
</div>

<SettingsInfoBlock type="MergeTreeMapBucketsStrategy" default_value="sqrt" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.3"},{"label": "sqrt"},{"label": "新增了一个设置，用于控制在 `with_buckets` `Map` 序列化中选择桶数量的策略"}]}]} />

根据平均 Map 大小，控制在 `with_buckets` `Map` 序列化中选择桶数量的策略。

可能的值：

* constant — 始终使用 [max&#95;buckets&#95;in&#95;map](/zh/reference/settings/merge-tree-settings/max#max_buckets_in_map) 作为桶数量，不受平均 Map 大小影响。
* sqrt — 使用 `round(map_buckets_coefficient * sqrt(avg_map_size))` 作为桶数量，并将结果限制在 `[1, max_buckets_in_map]` 范围内。
* linear — 使用 `round(map_buckets_coefficient * avg_map_size)` 作为桶数量，并将结果限制在 `[1, max_buckets_in_map]` 范围内。

{/*AUTOGENERATED_END*/}