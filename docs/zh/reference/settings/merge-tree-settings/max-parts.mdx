---
title: 'max_parts_* MergeTree 表设置'
sidebarTitle: 'max_parts_*'
toc_max_heading_level: 2
description: 'ClickHouse MergeTree 表设置，属于自动生成的 max_parts_* 组。'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/zh/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

这些设置位于 [system.merge&#95;tree&#95;settings](/zh/reference/system-tables/merge_tree_settings) 中，由 ClickHouse 源代码自动生成。

<div id="max_parts_in_total">
  ## max_parts_in_total
</div>

<SettingsInfoBlock type="UInt64" default_value="100000" />

如果某个表所有分区中的活跃 parts 总数超过
`max_parts_in_total` 的值，`INSERT` 将被中断，并抛出 `Too many parts
(N)` 异常。

可能的值：

* 任意正整数。

表中的 parts 数量过多会降低 ClickHouse 查询性能，
并增加 ClickHouse 启动时间。这通常是由于
设计不当造成的 (例如分区策略选择错误——分区过小) 。

<div id="max_parts_to_merge_at_once">
  ## max_parts_to_merge_at_once
</div>

<SettingsInfoBlock type="UInt64" default_value="100" />

一次最多可合并的 parts 数量 (0 - 禁用) 。不会影响
OPTIMIZE FINAL 查询。

{/*AUTOGENERATED_END*/}