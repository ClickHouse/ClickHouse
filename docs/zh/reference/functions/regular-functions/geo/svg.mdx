---
description: 'Svg 函数文档'
sidebarTitle: 'SVG'
slug: /sql-reference/functions/geo/svg
title: '从 Geo 数据生成 SVG 图像的函数'
doc_type: 'reference'
---

<div id="svg">
  ## Svg
</div>

返回由 Geo 数据生成的选定 SVG 元素标签字符串。

**语法**

```sql
Svg(geometry,[style])
```

别名：`SVG`, `svg`

**参数**

* `geometry` — Geo 数据。[Geo](/zh/reference/data-types/geo)。
* `style` — 可选的样式名称。[String](/zh/reference/data-types/string)。

**返回值**

* 几何对象的 SVG 表示形式。[String](/zh/reference/data-types/string)。
  * SVG 圆形
  * SVG 多边形
  * SVG 路径

**示例**

**圆形**

```sql title="Query"
SELECT SVG((0., 0.))
```

```response title="Response"
<circle cx="0" cy="0" r="5" style=""/>
```

**Polygon**

```sql title="Query"
SELECT SVG([(0., 0.), (10, 0), (10, 10), (0, 10)])
```

```response title="Response"
<polygon points="0,0 0,10 10,10 10,0 0,0" style=""/>
```

**Path**

```sql title="Query"
SELECT SVG([[(0., 0.), (10, 0), (10, 10), (0, 10)], [(4., 4.), (5, 4), (5, 5), (4, 5)]])
```

```response title="Response"
<g fill-rule="evenodd"><path d="M 0,0 L 0,10 L 10,10 L 10,0 L 0,0M 4,4 L 5,4 L 5,5 L 4,5 L 4,4 z " style=""/></g>
```