---
description: '包含 `/dashboard` 页面（可通过 HTTP 接口访问）使用的查询，可用于监控和故障排查。'
keywords: ['系统表', '仪表盘', '监控', '故障排查']
slug: /operations/system-tables/dashboards
title: 'system.dashboards'
doc_type: 'reference'
---

## 描述 {#description}

包含可通过 [HTTP 接口](/zh/concepts/features/interfaces/http) 访问的 `/dashboard` 页面所使用的查询。
该表可用于监控和故障排查。仪表盘中的每个图表在该表中都对应一行。

<Note>
  `/dashboard` 页面不仅可以渲染来自 `system.dashboards` 的查询，还可以渲染来自任何具有相同 schema 的表的查询。
  这可用于创建自定义仪表盘。
</Note>

内置仪表盘图表从 `system.metric_log` 和 `system.asynchronous_metric_log` 读取数据。请在服务器配置中通过 [`metric_log`](/zh/reference/settings/server-settings/settings/other#metric_log) 和 [`asynchronous_metric_log`](/zh/reference/settings/server-settings/settings/asynchronous#asynchronous_metric_log) 启用这两个日志，以填充所有这些图表。

## 列 {#columns}

{/*AUTOGENERATED_START*/}

* `dashboard` ([String](/zh/reference/data-types/string)) — 仪表盘名称。
* `title` ([String](/zh/reference/data-types/string)) — 图表标题。
* `query` ([String](/zh/reference/data-types/string)) — 用于获取要显示数据的查询。

{/*AUTOGENERATED_END*/}

## 示例 {#example}

```sql
SELECT *
FROM system.dashboards
WHERE title ILIKE '%CPU%'
```

```text
Row 1:
──────
dashboard: overview
title:     CPU Usage (cores)
query:     SELECT toStartOfInterval(event_time, INTERVAL {rounding:UInt32} SECOND)::INT AS t, avg(ProfileEvent_OSCPUVirtualTimeMicroseconds) / 1000000
FROM system.metric_log
WHERE event_date >= toDate(now() - {seconds:UInt32}) AND event_time >= now() - {seconds:UInt32}
GROUP BY t
ORDER BY t WITH FILL STEP {rounding:UInt32}

Row 2:
──────
dashboard: overview
title:     CPU Wait
query:     SELECT toStartOfInterval(event_time, INTERVAL {rounding:UInt32} SECOND)::INT AS t, avg(ProfileEvent_OSCPUWaitMicroseconds) / 1000000
FROM system.metric_log
WHERE event_date >= toDate(now() - {seconds:UInt32}) AND event_time >= now() - {seconds:UInt32}
GROUP BY t
ORDER BY t WITH FILL STEP {rounding:UInt32}

Row 3:
──────
dashboard: overview
title:     OS CPU Usage (Userspace)
query:     SELECT toStartOfInterval(event_time, INTERVAL {rounding:UInt32} SECOND)::INT AS t, avg(value)
FROM system.asynchronous_metric_log
WHERE event_date >= toDate(now() - {seconds:UInt32}) AND event_time >= now() - {seconds:UInt32} AND metric = 'OSUserTimeNormalized'
GROUP BY t
ORDER BY t WITH FILL STEP {rounding:UInt32}

Row 4:
──────
dashboard: overview
title:     OS CPU Usage (Kernel)
query:     SELECT toStartOfInterval(event_time, INTERVAL {rounding:UInt32} SECOND)::INT AS t, avg(value)
FROM system.asynchronous_metric_log
WHERE event_date >= toDate(now() - {seconds:UInt32}) AND event_time >= now() - {seconds:UInt32} AND metric = 'OSSystemTimeNormalized'
GROUP BY t
ORDER BY t WITH FILL STEP {rounding:UInt32}
```