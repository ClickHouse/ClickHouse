---
title: Template 函数
---



## functionName {#functionname-in-lower-case}

简短描述。

**语法**（不含 SELECT）

```sql
<function syntax>
```

别名：`<alias name>`。（可选）

更多说明（可选）。

**参数**（可选）

-   `x` — 描述。可选（仅适用于可选参数）。可能的值：<values list>。默认值：<value>。[类型名称](relative/path/to/type/dscr.md#type)。
-   `y` — 描述。可选（仅适用于可选参数）。可能的值：<values list>。默认值：<value>。[类型名称](relative/path/to/type/dscr.md#type)。

**参数**（可选，仅适用于参数化聚合函数）

-   `z` — 描述。可选（仅适用于可选参数）。可能的值：<values list>。默认值：<value>。[类型名称](relative/path/to/type/dscr.md#type)。

**返回值**

-   返回值列表。

类型：[类型名称](relative/path/to/type/dscr.md#type)。

**示例**

示例须展示用法和/或用例。以下文本包含示例的推荐组成部分。

输入表（可选）：

```text
```

```sql title="Query"
```

```text title="Response"
```

**另请参阅**（可选）

-   [link](#)