---
description: 'جدول نظام يحتوي على معلومات حول العروض التابعة التي تُنفَّذ عند تشغيل استعلام، مثل نوع العرض أو وقت التنفيذ.'
keywords: ['جدول نظام', 'query_views_log']
slug: /operations/system-tables/query_views_log
title: 'system.query_views_log'
doc_type: 'reference'
---

import SystemTableCloud from '@site/docs/_snippets/_system_table_cloud.md';

<SystemTableCloud />

<div id="description">
  ## الوصف
</div>

يتضمن معلومات حول العروض التابعة التي تُنفَّذ عند تشغيل استعلام، مثل نوع العرض أو وقت التنفيذ.

لبدء تسجيل السجلات:

1. اضبط المَعلمات في قسم [query&#95;views&#95;log](../../operations/server-configuration-parameters/settings.md#query_views_log).
2. عيّن [log&#95;query&#95;views](/ar/operations/settings/settings#log_query_views) إلى 1.

تُحدَّد فترة تفريغ البيانات في المَعلمة `flush_interval_milliseconds` ضمن قسم إعدادات الخادم [query&#95;views&#95;log](../../operations/server-configuration-parameters/settings.md#query_views_log). وللفرض الفوري للتفريغ، استخدم الاستعلام [SYSTEM FLUSH LOGS](/ar/sql-reference/statements/system#flush-logs).

لا يحذف ClickHouse البيانات من الجدول تلقائيًا. راجع [المقدمة](/ar/operations/system-tables/overview#system-tables-introduction) لمزيد من التفاصيل.

يمكنك استخدام الإعداد [log&#95;queries&#95;probability](/ar/operations/settings/settings#log_queries_probability)) لتقليل عدد الاستعلامات المسجَّلة في جدول `query_views_log`.

<div id="columns">
  ## الأعمدة
</div>

{/*AUTOGENERATED_START*/ }

* `hostname` ([LowCardinality(String)](/ar/sql-reference/data-types/lowcardinality)) — اسم مضيف الخادم الذي ينفّذ الاستعلام.
* `event_date` ([Date](/ar/sql-reference/data-types/date)) — التاريخ الذي وقع فيه آخر حدث متعلق بالعرض.
* `event_time` ([DateTime](/ar/sql-reference/data-types/datetime)) — التاريخ والوقت اللذان انتهى فيهما تنفيذ العرض.
* `event_time_microseconds` ([DateTime64(6)](/ar/sql-reference/data-types/datetime64)) — التاريخ والوقت اللذان انتهى فيهما تنفيذ العرض بدقة الميكروثانية.
* `view_duration_ms` ([UInt64](/ar/sql-reference/data-types/int-uint)) — مدة تنفيذ العرض (مجموع مراحله) بالملي ثانية.
* `initial_query_id` ([String](/ar/sql-reference/data-types/string)) — معرّف الاستعلام الأولي (لتنفيذ الاستعلام الموزّع).
* `view_name` ([String](/ar/sql-reference/data-types/string)) — اسم العرض.
* `view_uuid` ([UUID](/ar/sql-reference/data-types/uuid)) — معرّف UUID للعرض.
* `view_type` ([Enum8(&#39;Default&#39; = 1, &#39;Materialized&#39; = 2, &#39;Live&#39; = 3, &#39;Window&#39; = 4)](/ar/sql-reference/data-types/enum)) — نوع العرض. القيم: &#39;Default&#39; = 1 — العروض الافتراضية. يجب ألا يظهر في هذا السجل، &#39;Materialized&#39; = 2 — العروض المُجسَّدة، &#39;Live&#39; = 3 — العروض المباشرة.
* `view_query` ([String](/ar/sql-reference/data-types/string)) — الاستعلام الذي نفّذه العرض.
* `view_target` ([String](/ar/sql-reference/data-types/string)) — اسم الجدول الهدف للعرض.
* `read_rows` ([UInt64](/ar/sql-reference/data-types/int-uint)) — عدد الصفوف المقروءة.
* `read_bytes` ([UInt64](/ar/sql-reference/data-types/int-uint)) — عدد البايتات المقروءة.
* `written_rows` ([UInt64](/ar/sql-reference/data-types/int-uint)) — عدد الصفوف المكتوبة.
* `written_bytes` ([UInt64](/ar/sql-reference/data-types/int-uint)) — عدد البايتات المكتوبة.
* `peak_memory_usage` ([Int64](/ar/sql-reference/data-types/int-uint)) — أكبر فرق بين مقدار الذاكرة المخصّصة والمحرّرة في سياق هذا العرض.
* `ProfileEvents` ([Map(LowCardinality(String), UInt64)](/ar/sql-reference/data-types/map)) — `ProfileEvents` التي تقيس مقاييس مختلفة. يمكن العثور على وصفها في الجدول system.events.
* `status` ([Enum8(&#39;QueryStart&#39; = 1, &#39;QueryFinish&#39; = 2, &#39;ExceptionBeforeStart&#39; = 3, &#39;ExceptionWhileProcessing&#39; = 4)](/ar/sql-reference/data-types/enum)) — حالة العرض. القيم: &#39;QueryStart&#39; = 1 — بداية ناجحة لتنفيذ العرض. يجب ألا يظهر، &#39;QueryFinish&#39; = 2 — نهاية ناجحة لتنفيذ العرض، &#39;ExceptionBeforeStart&#39; = 3 — استثناء قبل بدء تنفيذ العرض، &#39;ExceptionWhileProcessing&#39; = 4 — استثناء أثناء تنفيذ العرض.
* `exception_code` ([Int32](/ar/sql-reference/data-types/int-uint)) — رمز الاستثناء.
* `exception` ([String](/ar/sql-reference/data-types/string)) — رسالة الاستثناء.
* `stack_trace` ([String](/ar/sql-reference/data-types/string)) — تتبّع المكدس. تكون سلسلة فارغة إذا اكتمل الاستعلام بنجاح.

**الأسماء البديلة:**

* `ProfileEvents.Names` — اسم بديل لـ `mapKeys(ProfileEvents)`.
* `ProfileEvents.Values` — اسم بديل لـ `mapValues(ProfileEvents)`.

{/*AUTOGENERATED_END*/ }

<div id="example">
  ## مثال
</div>

```sql title="Query"
SELECT * FROM system.query_views_log LIMIT 1 \G;
```

```text title="Response"
Row 1:
──────
hostname:                clickhouse.eu-central1.internal
event_date:              2021-06-22
event_time:              2021-06-22 13:23:07
event_time_microseconds: 2021-06-22 13:23:07.738221
view_duration_ms:        0
initial_query_id:        c3a1ac02-9cad-479b-af54-9e9c0a7afd70
view_name:               default.matview_inner
view_uuid:               00000000-0000-0000-0000-000000000000
view_type:               Materialized
view_query:              SELECT * FROM default.table_b
view_target:             default.`.inner.matview_inner`
read_rows:               4
read_bytes:              64
written_rows:            2
written_bytes:           32
peak_memory_usage:       4196188
ProfileEvents:           {'FileOpen':2,'WriteBufferFromFileDescriptorWrite':2,'WriteBufferFromFileDescriptorWriteBytes':187,'IOBufferAllocs':3,'IOBufferAllocBytes':3145773,'FunctionExecute':3,'DiskWriteElapsedMicroseconds':13,'InsertedRows':2,'InsertedBytes':16,'SelectedRows':4,'SelectedBytes':48,'ContextLock':16,'RWLockAcquiredReadLocks':1,'RealTimeMicroseconds':698,'SoftPageFaults':4,'OSReadChars':463}
status:                  QueryFinish
exception_code:          0
exception:
stack_trace:
```

<div id="see-also">
  ## راجع أيضًا
</div>

{/*AUTOGENERATED_START*/ }

{/*AUTOGENERATED_END*/ }