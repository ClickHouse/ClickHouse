---
description: 'جدول النظام يحتوي على معلومات عن القواميس'
keywords: ['جدول النظام', 'القواميس']
slug: /operations/system-tables/dictionaries
title: 'system.dictionaries'
doc_type: 'reference'
---

import SystemTableCloud from '@site/docs/_snippets/_system_table_cloud.md';

<SystemTableCloud />

<div id="description">
  ## الوصف
</div>

يتضمن معلومات حول [القواميس](../../sql-reference/statements/create/dictionary/overview.md).

<div id="columns">
  ## الأعمدة
</div>

{/*AUTOGENERATED_START*/ }

* `database` ([String](/ar/sql-reference/data-types/string)) — اسم قاعدة البيانات التي تحتوي على القاموس الذي أُنشئ بواسطة استعلام DDL. وتكون سلسلة فارغة للقواميس الأخرى.
* `name` ([String](/ar/sql-reference/data-types/string)) — اسم القاموس.
* `uuid` ([UUID](/ar/sql-reference/data-types/uuid)) — معرّف UUID للقاموس.
* `status` ([Enum8(&#39;NOT&#95;LOADED&#39; = 0, &#39;LOADED&#39; = 1, &#39;FAILED&#39; = 2, &#39;LOADING&#39; = 3, &#39;FAILED&#95;AND&#95;RELOADING&#39; = 4, &#39;LOADED&#95;AND&#95;RELOADING&#39; = 5, &#39;NOT&#95;EXIST&#39; = 6)](/ar/sql-reference/data-types/enum)) — حالة القاموس. القيم الممكنة: NOT&#95;LOADED — لم يُحمَّل القاموس لأنه لم يُستخدم، LOADED — تم تحميل القاموس بنجاح، FAILED — تعذّر تحميل القاموس نتيجة خطأ، LOADING — يجري تحميل القاموس الآن، LOADED&#95;AND&#95;RELOADING — تم تحميل القاموس بنجاح ويجري الآن إعادة تحميله (من الأسباب الشائعة: استعلام SYSTEM RELOAD DICTIONARY، وtimeout، وتغيّر config القاموس)، FAILED&#95;AND&#95;RELOADING — تعذّر تحميل القاموس بسبب خطأ ويجري الآن تحميله.
* `origin` ([String](/ar/sql-reference/data-types/string)) — المسار إلى ملف الإعدادات الذي يصف القاموس.
* `type` ([String](/ar/sql-reference/data-types/string)) — نوع تخصيص القاموس. تخزين القواميس في الذاكرة.
* `key.names` ([Array(String)](/ar/sql-reference/data-types/array)) — مصفوفة بأسماء المفاتيح التي يوفّرها القاموس.
* `key.types` ([Array(String)](/ar/sql-reference/data-types/array)) — المصفوفة المقابلة لأنواع المفاتيح التي يوفّرها القاموس.
* `attribute.names` ([Array(String)](/ar/sql-reference/data-types/array)) — مصفوفة بأسماء السمات التي يوفّرها القاموس.
* `attribute.types` ([Array(String)](/ar/sql-reference/data-types/array)) — المصفوفة المقابلة لأنواع السمات التي يوفّرها القاموس.
* `bytes_allocated` ([UInt64](/ar/sql-reference/data-types/int-uint)) — مقدار RAM المخصّص للقاموس.
* `hierarchical_index_bytes_allocated` ([UInt64](/ar/sql-reference/data-types/int-uint)) — مقدار RAM المخصّص للفهرس الهرمي.
* `query_count` ([UInt64](/ar/sql-reference/data-types/int-uint)) — عدد الاستعلامات منذ تحميل القاموس أو منذ آخر إعادة تشغيل ناجحة.
* `hit_rate` ([Float64](/ar/sql-reference/data-types/float)) — بالنسبة إلى قواميس cache، النسبة المئوية للاستخدامات التي كانت القيمة فيها موجودة في cache.
* `found_rate` ([Float64](/ar/sql-reference/data-types/float)) — النسبة المئوية للاستخدامات التي عُثر فيها على القيمة.
* `element_count` ([UInt64](/ar/sql-reference/data-types/int-uint)) — عدد العناصر المخزّنة في القاموس.
* `load_factor` ([Float64](/ar/sql-reference/data-types/float)) — نسبة الامتلاء في القاموس (وبالنسبة إلى قاموس hashed، فهي نسبة الامتلاء في hash table).
* `source` ([String](/ar/sql-reference/data-types/string)) — نص يصف مصدر بيانات القاموس.
* `lifetime_min` ([UInt64](/ar/sql-reference/data-types/int-uint)) — الحد الأدنى لعمر القاموس في الذاكرة، وبعده يحاول ClickHouse إعادة تحميل القاموس (إذا كان `invalidate_query` مضبوطًا، فلا تتم إعادة التحميل إلا إذا كان قد تغيّر). ويُحدَّد بالثواني.
* `lifetime_max` ([UInt64](/ar/sql-reference/data-types/int-uint)) — الحد الأقصى لعمر القاموس في الذاكرة، وبعده يحاول ClickHouse إعادة تحميل القاموس (إذا كان `invalidate_query` مضبوطًا، فلا تتم إعادة التحميل إلا إذا كان قد تغيّر). ويُحدَّد بالثواني.
* `loading_start_time` ([DateTime](/ar/sql-reference/data-types/datetime)) — وقت بدء تحميل القاموس.
* `last_successful_update_time` ([DateTime](/ar/sql-reference/data-types/datetime)) — وقت انتهاء تحميل القاموس أو تحديثه. يساعد ذلك على مراقبة بعض المشكلات المتعلقة بمصادر القواميس والتحقيق في أسبابها.
* `error_count` ([UInt64](/ar/sql-reference/data-types/int-uint)) — عدد الأخطاء منذ آخر تحميل ناجح. يساعد ذلك على مراقبة بعض المشكلات المتعلقة بمصادر القواميس والتحقيق في أسبابها.
* `loading_duration` ([Float32](/ar/sql-reference/data-types/float)) — مدة تحميل القاموس.
* `last_exception` ([String](/ar/sql-reference/data-types/string)) — نص الخطأ الذي يظهر عند إنشاء القاموس أو إعادة تحميله إذا تعذّر إنشاء القاموس.
* `comment` ([String](/ar/sql-reference/data-types/string)) — نص التعليق الخاص بالقاموس.

{/*AUTOGENERATED_END*/ }

<div id="example">
  ## مثال
</div>

هيّئ القاموس:

```sql
CREATE DICTIONARY dictionary_with_comment
(
    id UInt64,
    value String
)
PRIMARY KEY id
SOURCE(CLICKHOUSE(HOST 'localhost' PORT tcpPort() TABLE 'source_table'))
LAYOUT(FLAT())
LIFETIME(MIN 0 MAX 1000)
COMMENT 'The temporary dictionary';
```

تأكد من تحميل القاموس.

```sql
SELECT * FROM system.dictionaries LIMIT 1 FORMAT Vertical;
```

```text
Row 1:
──────
database:                    default
name:                        dictionary_with_comment
uuid:                        4654d460-0d03-433a-8654-d4600d03d33a
status:                      NOT_LOADED
origin:                      4654d460-0d03-433a-8654-d4600d03d33a
type:
key.names:                   ['id']
key.types:                   ['UInt64']
attribute.names:             ['value']
attribute.types:             ['String']
bytes_allocated:             0
query_count:                 0
hit_rate:                    0
found_rate:                  0
element_count:               0
load_factor:                 0
source:
lifetime_min:                0
lifetime_max:                0
loading_start_time:          1970-01-01 00:00:00
last_successful_update_time: 1970-01-01 00:00:00
loading_duration:            0
last_exception:
comment:                     The temporary dictionary
```