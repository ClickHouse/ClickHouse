---
description: 'جدول نظامي يحتوي على معلومات عن ملفات البيانات الوصفية المقروءة من جداول Iceberg. يمثّل كل إدخال
  إما ملف البيانات الوصفية الجذر، أو بيانات وصفية مستخرجة من ملف Avro، أو إدخالًا من أحد ملفات Avro.'
keywords: ['جدول نظام', 'iceberg_metadata_log']
slug: /operations/system-tables/iceberg_metadata_log
title: 'system.iceberg_metadata_log'
doc_type: 'مرجع'
---

import SystemTableCloud from '@site/docs/_snippets/_system_table_cloud.md';

<SystemTableCloud />

<div id="description">
  ## الوصف
</div>

يسجّل الجدول `system.iceberg_metadata_log` أحداث الوصول إلى البيانات الوصفية وتحليلها لجداول Iceberg التي يقرأها ClickHouse. ويوفّر معلومات تفصيلية عن كل ملف البيانات الوصفية أو إدخال تمت معالجته، مما يفيد في استكشاف الأخطاء وإصلاحها، والتدقيق، وفهم تطوّر بنية جدول Iceberg.

يسجّل هذا الجدول كل ملف البيانات الوصفية وكل إدخال تتم قراءته من جداول Iceberg، بما في ذلك ملفات البيانات الوصفية الجذر، ومانيفست، وإدخالات مانيفست. كما يساعد المستخدمين على تتبّع كيفية تفسير ClickHouse للبيانات الوصفية لجدول Iceberg وتشخيص المشكلات المرتبطة بتطوّر المخطط، أو تحديد الملفات، أو تخطيط الاستعلامات.

:::note
هذا الجدول مخصّص أساسًا لأغراض استكشاف الأخطاء وإصلاحها.
:::

<div id="controlling-log-verbosity">
  ### التحكم في مستوى تفصيل السجل
</div>

يمكنك التحكم في أحداث البيانات الوصفية التي تُسجَّل باستخدام الإعداد [`iceberg_metadata_log_level`](../../operations/settings/settings.md#iceberg_metadata_log_level).

لتسجيل جميع البيانات الوصفية المستخدمة في الاستعلام الحالي:

```sql
SELECT * FROM my_iceberg_table SETTINGS iceberg_metadata_log_level = 'manifest_file_entry';

SYSTEM FLUSH LOGS iceberg_metadata_log;

SELECT content_type, file_path, row_in_file
FROM system.iceberg_metadata_log
WHERE query_id = '{previous_query_id}';
```

لتسجيل ملف JSON للبيانات الوصفية الجذر المستخدم في الاستعلام الحالي فقط:

```sql
SELECT * FROM my_iceberg_table SETTINGS iceberg_metadata_log_level = 'metadata';

SYSTEM FLUSH LOGS iceberg_metadata_log;

SELECT content_type, file_path, row_in_file
FROM system.iceberg_metadata_log
WHERE query_id = '{previous_query_id}';
```

اطّلع على مزيد من المعلومات في وصف الإعداد [`iceberg_metadata_log_level`](../../operations/settings/settings.md#iceberg_metadata_log_level).

<div id="good-to-know">
  ### من الجيد أن تعرف
</div>

* استخدم `iceberg_metadata_log_level` على مستوى الاستعلام فقط عندما تحتاج إلى فحص جدول Iceberg بالتفصيل. وإلا، فقد تمتلئ جدول السجل بكمية مفرطة من البيانات الوصفية وقد يتراجع الأداء.
* يحتوي الجدول على إدخالات مكررة، لأنه مخصص أساسًا لتصحيح الأخطاء ولا يضمن التفرد لكل كيان. وتُخزَّن المحتويات وحالة التقليم في صفوف منفصلة لأن جمعهما يتم في أوقات مختلفة داخل البرنامج. يُجمع المحتوى عند قراءة البيانات الوصفية، وتُجمع حالة التقليم عند فحص البيانات الوصفية بغرض التقليم. **لا تعتمد مطلقًا على الجدول نفسه في إزالة التكرار.**
* إذا استخدمت `content_type` أكثر تفصيلاً من `ManifestListMetadata`، فسيتم تعطيل ذاكرة التخزين المؤقت للبيانات الوصفية في Iceberg للمانيفست.
* وبالمثل، إذا استخدمت `content_type` أكثر تفصيلاً من `ManifestFileMetadata`، فسيتم تعطيل ذاكرة التخزين المؤقت للبيانات الوصفية في Iceberg للمانيفست.
* إذا أُلغي استعلام SELECT أو فشل، فقد يظل جدول السجل يحتوي على إدخالات للبيانات الوصفية التي تمت معالجتها قبل الفشل، لكنه لن يحتوي على معلومات عن كيانات البيانات الوصفية التي لم تتم معالجتها.

<div id="columns">
  ## الأعمدة
</div>

{/*AUTOGENERATED_START*/ }

* `hostname` ([LowCardinality(String)](/ar/sql-reference/data-types/lowcardinality)) — اسم مضيف الخادم الذي ينفّذ الاستعلام.
* `event_date` ([Date](/ar/sql-reference/data-types/date)) — تاريخ السجل.
* `event_time` ([DateTime](/ar/sql-reference/data-types/datetime)) — وقت الحدث.
* `query_id` ([String](/ar/sql-reference/data-types/string)) — معرّف الاستعلام.
* `content_type` ([Enum8(&#39;None&#39; = 0, &#39;Metadata&#39; = 1, &#39;ManifestListMetadata&#39; = 2, &#39;ManifestListEntry&#39; = 3, &#39;ManifestFileMetadata&#39; = 4, &#39;ManifestFileEntry&#39; = 5)](/ar/sql-reference/data-types/enum)) — نوع المحتوى.
* `table_path` ([String](/ar/sql-reference/data-types/string)) — مسار الجدول.
* `file_path` ([String](/ar/sql-reference/data-types/string)) — مسار الملف.
* `content` ([String](/ar/sql-reference/data-types/string)) — المحتوى بتنسيق JSON (محتوى ملف JSON، أو البيانات الوصفية لـ Avro، أو إدخال Avro).
* `row_in_file` ([Nullable(UInt64)](/ar/sql-reference/data-types/nullable)) — الصف في الملف.
* `pruning_status` ([Nullable(Enum8(&#39;NotPruned&#39; = 0, &#39;PartitionPruned&#39; = 1, &#39;MinMaxIndexPruned&#39; = 2))](/ar/sql-reference/data-types/nullable)) — حالة تشذيب الأقسام أو تشذيب فهرس min-max للملف.

{/*AUTOGENERATED_END*/ }

<div id="content-type-values">
  ### قيم `content_type`
</div>

* `None`: لا محتوى.
* `Metadata`: ملف البيانات الوصفية الجذر.
* `ManifestListMetadata`: البيانات الوصفية لقائمة المانيفست.
* `ManifestListEntry`: مدخل في قائمة المانيفست.
* `ManifestFileMetadata`: البيانات الوصفية لملف المانيفست.
* `ManifestFileEntry`: مدخل في ملف المانيفست.

<div id="see-also">
  ## انظر أيضًا
</div>

* [Iceberg Table Engine](../../engines/table-engines/integrations/iceberg.md)
* [Iceberg Table Function](../../sql-reference/table-functions/iceberg.md)
* [system.iceberg&#95;history](./iceberg_history.md)