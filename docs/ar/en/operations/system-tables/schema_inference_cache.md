---
description: 'جدول نظامي يحتوي على معلومات عن جميع مخططات الملفات المخزنة مؤقتًا.'
keywords: ['جدول نظامي', 'schema_inference_cache']
slug: /operations/system-tables/schema_inference_cache
title: 'system.schema_inference_cache'
doc_type: 'مرجع'
---

import SystemTableCloud from '@site/docs/_snippets/_system_table_cloud.md';

<SystemTableCloud />

<div id="description">
  ## الوصف
</div>

يتضمن معلومات عن جميع مخططات الملفات المخزنة مؤقتًا.

<div id="columns">
  ## الأعمدة
</div>

{/*AUTOGENERATED_START*/ }

* `storage` ([String](/ar/sql-reference/data-types/string)) — اسم التخزين: File أو URL أو S3 أو HDFS.
* `source` ([String](/ar/sql-reference/data-types/string)) — مصدر الملف.
* `format` ([String](/ar/sql-reference/data-types/string)) — اسم التنسيق.
* `additional_format_info` ([String](/ar/sql-reference/data-types/string)) — معلومات إضافية مطلوبة لتحديد المخطط، مثل الإعدادات الخاصة بالتنسيق.
* `registration_time` ([DateTime](/ar/sql-reference/data-types/datetime)) — الطابع الزمني لإضافة المخطط إلى الذاكرة المؤقتة.
* `schema` ([Nullable(String)](/ar/sql-reference/data-types/nullable)) — المخطط المخزَّن مؤقتًا.
* `number_of_rows` ([Nullable(UInt64)](/ar/sql-reference/data-types/nullable)) — عدد الصفوف في الملف بالتنسيق المحدد. ويُستخدم للتخزين المؤقت لنتيجة `count()` البسيطة من data files، وكذلك للتخزين المؤقت لعدد الصفوف من البيانات الوصفية أثناء استدلال المخطط.
* `schema_inference_mode` ([Nullable(String)](/ar/sql-reference/data-types/nullable)) — وضع استدلال المخطط.

{/*AUTOGENERATED_END*/ }

<div id="example">
  ## مثال
</div>

لنفترض أن لدينا ملفًا باسم `data.jsonl` يحتوي على المحتوى التالي:

```json
{"id" :  1, "age" :  25, "name" :  "Josh", "hobbies" :  ["football", "cooking", "music"]}
{"id" :  2, "age" :  19, "name" :  "Alan", "hobbies" :  ["tennis", "art"]}
{"id" :  3, "age" :  32, "name" :  "Lana", "hobbies" :  ["fitness", "reading", "shopping"]}
{"id" :  4, "age" :  47, "name" :  "Brayan", "hobbies" :  ["movies", "skydiving"]}
```

:::tip
ضع `data.jsonl` في الدليل `user_files_path`. يمكنك العثور على هذا المسار بالنظر
في ملفات إعداد ClickHouse. القيمة الافتراضية هي:

```sql
<user_files_path>/var/lib/clickhouse/user_files/</user_files_path>
```

:::

افتح `clickhouse-client` ونفّذ استعلام `DESCRIBE`:

```sql
DESCRIBE file('data.jsonl') SETTINGS input_format_try_infer_integers=0;
```

```response
┌─name────┬─type────────────────────┬─default_type─┬─default_expression─┬─comment─┬─codec_expression─┬─ttl_expression─┐
│ id      │ Nullable(Float64)       │              │                    │         │                  │                │
│ age     │ Nullable(Float64)       │              │                    │         │                  │                │
│ name    │ Nullable(String)        │              │                    │         │                  │                │
│ hobbies │ Array(Nullable(String)) │              │                    │         │                  │                │
└─────────┴─────────────────────────┴──────────────┴────────────────────┴─────────┴──────────────────┴────────────────┘
```

لنرَ محتوى الجدول `system.schema_inference_cache`:

```sql
SELECT *
FROM system.schema_inference_cache
FORMAT Vertical
```

```response
Row 1:
──────
storage:                File
source:                 /home/droscigno/user_files/data.jsonl
format:                 JSONEachRow
additional_format_info: schema_inference_hints=, max_rows_to_read_for_schema_inference=25000, schema_inference_make_columns_nullable=true, try_infer_integers=false, try_infer_dates=true, try_infer_datetimes=true, try_infer_numbers_from_strings=true, read_bools_as_numbers=true, try_infer_objects=false
registration_time:      2022-12-29 17:49:52
schema:                 id Nullable(Float64), age Nullable(Float64), name Nullable(String), hobbies Array(Nullable(String))
```

<div id="see-also">
  ## انظر أيضًا
</div>

* [استدلال المخطط تلقائيًا من بيانات الإدخال](/ar/interfaces/schema-inference.md)