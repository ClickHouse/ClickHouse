---
description: 'جدول نظامي يحتوي على معلومات عن المهام في مجمعات الجدولة الخلفية.'
keywords: ['جدول نظامي', 'background_schedule_pool']
slug: /operations/system-tables/background_schedule_pool
title: 'system.background_schedule_pool'
doc_type: 'reference'
---

import SystemTableCloud from '/snippets/ar/_system_table_cloud.mdx';

<SystemTableCloud />

<div id="description">
  ## الوصف
</div>

تتضمن معلومات عن المهام في مجمعات الجدولة الخلفية. تُستخدم مجمعات الجدولة الخلفية لتنفيذ المهام الدورية، مثل عمليات الإرسال الموزعة، وتفريغ المخزن المؤقت، وعمليات وسيط الرسائل، والمهام الخلفية للاستعلامات المتدفقة، وتحديث البيانات الوصفية لجدول Iceberg.

<div id="columns">
  ## الأعمدة
</div>

{/*AUTOGENERATED_START*/}

* `pool` ([String](/ar/reference/data-types/index)) — اسم المجمّع. القيم الممكنة: `schedule` (مجمّع جدولة للأغراض العامة)، `buffer_flush` (مجمّع لتفريغ بيانات جدول Buffer)، `distributed` (مجمّع لعمليات الجدول الموزّع)، `message_broker` (مجمّع لعمليات وسيط الرسائل)، `streaming` (مجمّع للمهام الخلفية للاستعلامات المتدفقة)، `iceberg` (مجمّع لتحديث البيانات الوصفية لجدول Iceberg).
* `database` ([String](/ar/reference/data-types/index)) — اسم قاعدة البيانات.
* `table` ([String](/ar/reference/data-types/index)) — اسم الجدول.
* `table_uuid` ([UUID](/ar/reference/data-types/index)) — UUID الجدول.
* `query_id` ([String](/ar/reference/data-types/index)) — معرّف الاستعلام (إذا كان يُنفَّذ الآن). ملاحظة: هذا ليس استعلامًا فعليًا، بل معرّفًا مُولَّدًا عشوائيًا لمطابقة السجلات في `system.text_log`.
* `elapsed_ms` ([UInt64](/ar/reference/data-types/index)) — مدة تنفيذ المهمة (إذا كانت تُنفَّذ الآن).
* `log_name` ([String](/ar/reference/data-types/index)) — اسم السجل الخاص بالمهمة.
* `deactivated` ([UInt8](/ar/reference/data-types/index)) — ما إذا كانت المهمة معطّلة (تكون دائمًا false، لأن المهام المعطّلة تُزال من المجمّع).
* `scheduled` ([UInt8](/ar/reference/data-types/index)) — ما إذا كانت المهمة مجدولة للتنفيذ.
* `delayed` ([UInt8](/ar/reference/data-types/index)) — ما إذا كانت المهمة مجدولة مع تأخير.
* `executing` ([UInt8](/ar/reference/data-types/index)) — ما إذا كانت المهمة تُنفَّذ حاليًا.

{/*AUTOGENERATED_END*/}

<div id="example">
  ## مثال
</div>

```sql title="Query"
SELECT * FROM system.background_schedule_pool LIMIT 5 FORMAT Vertical;
```

```text title="Response"
Row 1:
──────
pool:        distributed
database:    default
table:       data
table_uuid:  00000000-0000-0000-0000-000000000000
query_id:
elapsed_ms:  0
log_name:    BackgroundJobsAssignee:DataProcessing
deactivated: 0
scheduled:   1
delayed:     0
executing:   0
```

<div id="see-also">
  ## انظر أيضًا
</div>

* [system.background&#95;schedule&#95;pool&#95;log](/ar/reference/system-tables/background_schedule_pool_log) — يحتوي على سجلّ بتاريخ تنفيذ مهام مجمع الجدولة الخلفية.