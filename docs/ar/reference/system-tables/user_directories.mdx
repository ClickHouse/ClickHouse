---
description: 'يتضمن معلومات حول أدلة المستخدمين المُعَدّة — وهي الأدلة الموجودة على نظام الملفات التي يُسمح لخادم ClickHouse بقراءة البيانات التي يوفّرها المستخدم منها.'
keywords: ['جدول النظام', 'user_directories']
slug: /operations/system-tables/user_directories
title: 'system.user_directories'
doc_type: 'reference'
---

<div id="description">
  ## الوصف
</div>

تتضمن معلومات عن أدلة المستخدمين المُهيأة — وهي الأدلة الموجودة في نظام الملفات التي يُسمح لخادم ClickHouse بقراءة البيانات التي يوفّرها المستخدم منها.

<div id="columns">
  ## الأعمدة
</div>

{/*AUTOGENERATED_START*/}

* `name` ([String](/ar/reference/data-types/string)) — اسم الدليل.
* `type` ([String](/ar/reference/data-types/string)) — نوع تخزين بيانات الوصول، مثل users.xml أو replicated أو memory وما إلى ذلك.
* `params` ([String](/ar/reference/data-types/string)) — JSON يتضمن معلمات تخزين بيانات الوصول.
* `precedence` ([UInt64](/ar/reference/data-types/int-uint)) — الترتيب الذي يُصرَّح فيه بهذا الدليل ضمن config. ويُستخدَم الترتيب نفسه عندما يحاول ClickHouse العثور على مستخدم أو دور.

{/*AUTOGENERATED_END*/}