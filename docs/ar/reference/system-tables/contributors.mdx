---
description: 'جدول نظامي يحتوي على معلومات عن المساهمين.'
keywords: ['جدول نظامي', 'المساهمون']
slug: /operations/system-tables/contributors
title: 'system.contributors'
doc_type: 'مرجع'
---

{/*AUTOGENERATED_START*/}

## الوصف {#description}

يتضمن معلومات عن المساهمين. يكون الترتيب عشوائيًا عند تنفيذ الاستعلام.

## الأعمدة {#columns}

* `name` ([String](/ar/reference/data-types/string)) — اسم المساهم (المؤلف) في سجل Git.

## مثال {#example}

```sql
SELECT * FROM system.contributors LIMIT 10
```

```text
┌─name─────────────┐
│ Olga Khvostikova │
│ Max Vetrov       │
│ LiuYangkuan      │
│ svladykin        │
│ zamulla          │
│ Šimon Podlipský  │
│ BayoNet          │
│ Ilya Khomutov    │
│ Amy Krishnevsky  │
│ Loud_Scream      │
└──────────────────┘
```

للعثور بنفسك على بياناتك في الجدول، استخدم الاستعلام التالي:

```sql
SELECT * FROM system.contributors WHERE name = 'Olga Khvostikova'
```

```text
┌─name─────────────┐
│ Olga Khvostikova │
└──────────────────┘
```

{/*AUTOGENERATED_END*/}