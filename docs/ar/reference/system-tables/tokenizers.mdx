---
description: 'جدول نظامي يعرض جميع مُقسِّمات الرموز المتاحة.'
keywords: ['جدول نظامي', 'مُقسِّمات الرموز']
slug: /operations/system-tables/tokenizers
title: 'system.tokenizers'
doc_type: 'مرجع'
---

import SystemTableCloud from '/snippets/ar/_system_table_cloud.mdx';

<SystemTableCloud />

{/*AUTOGENERATED_START*/}

## الوصف {#description}

يعرض جميع مُقسِّمات الرموز المتاحة.
يمكن استخدام هذه الأدوات في الدوال [tokens](/ar/reference/functions/regular-functions/splitting-merging-functions#tokens)، و[hasAllTokens](/ar/reference/functions/regular-functions/string-search-functions#hasAllTokens)، و[hasAnyTokens](/ar/reference/functions/regular-functions/string-search-functions#hasAnyTokens)، وفي [الفهرس النصي](/ar/reference/engines/table-engines/mergetree-family/textindexes).

## الأعمدة {#columns}

* `name` ([String](/ar/reference/data-types/string)) — اسم مُقسِّم الرموز

## مثال {#example}

```sql
SELECT * FROM system.tokenizers;
```

```text
┌─name────────────┐
│ ngrams          │
│ splitByNonAlpha │
│ sparseGrams     │
│ tokenbf_v1      │
│ ngrambf_v1      │
│ array           │
│ splitByString   │
│ sparse_grams    │
│ asciiCJK        │
│ icu             │
│ japanese        │
└─────────────────┘
```

{/*AUTOGENERATED_END*/}