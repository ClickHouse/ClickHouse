---
description: 'جدول نظامي يحتوي على معلومات حول إعدادات جداول AzureQueue.
  متاح ابتداءً من إصدار الخادم `24.10`.'
keywords: ['جدول نظامي', 'azure_queue_settings']
slug: /operations/system-tables/azure_queue_settings
title: 'system.azure_queue_settings'
doc_type: 'reference'
---

<div id="description">
  ## الوصف
</div>

يتضمن معلومات حول إعدادات جداول [AzureQueue](/ar/reference/engines/table-engines/integrations/azure-queue).
متاح بدءًا من إصدار الخادم `24.10`.

<div id="columns">
  ## الأعمدة
</div>

{/*AUTOGENERATED_START*/}

* `database` ([String](/ar/reference/data-types/string)) — قاعدة بيانات الجدول ذي المحرك AzureQueue.
* `table` ([String](/ar/reference/data-types/string)) — اسم الجدول ذي المحرك AzureQueue.
* `name` ([String](/ar/reference/data-types/string)) — اسم الإعداد.
* `value` ([String](/ar/reference/data-types/string)) — قيمة الإعداد.
* `type` ([String](/ar/reference/data-types/string)) — نوع الإعداد (قيمة نصية خاصة بالتنفيذ).
* `changed` ([UInt8](/ar/reference/data-types/int-uint)) — 1 إذا كان الإعداد محددًا صراحةً في config أو تم تغييره صراحةً.
* `description` ([String](/ar/reference/data-types/string)) — وصف الإعداد.
* `alterable` ([UInt8](/ar/reference/data-types/int-uint)) — يوضّح ما إذا كان بإمكان المستخدم الحالي تغيير الإعداد عبر ALTER TABLE MODIFY SETTING: ‏0 — لا يمكن للمستخدم الحالي تغيير الإعداد، ‏1 — يمكن للمستخدم الحالي تغيير الإعداد.

{/*AUTOGENERATED_END*/}