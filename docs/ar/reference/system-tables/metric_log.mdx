---
description: 'جدول نظامي يحتوي على سجلٍّ لقيم المقاييس من الجدولين `system.metrics`
  و`system.events`، ويُفرَّغ دوريًا على القرص.'
keywords: ['جدول نظامي', 'metric_log']
slug: /operations/system-tables/metric_log
title: 'system.metric_log'
doc_type: 'مرجع'
---

import SystemTableCloud from '/snippets/ar/_system_table_cloud.mdx';

<SystemTableCloud />

<div id="description">
  ## الوصف
</div>

يحتوي على سجلٍّ تاريخي لقيم المقاييس من الجدولين `system.metrics` و`system.events`، وتُفرَّغ دوريًا إلى القرص.

<div id="columns">
  ## الأعمدة
</div>

{/*AUTOGENERATED_START*/}

* `hostname` ([LowCardinality(String)](/ar/reference/data-types/lowcardinality)) — اسم مضيف الخادم الذي ينفّذ الاستعلام.
* `event_date` ([Date](/ar/reference/data-types/date)) — تاريخ الحدث.
* `event_time` ([DateTime](/ar/reference/data-types/datetime)) — وقت الحدث.
* `event_time_microseconds` ([DateTime64(6)](/ar/reference/data-types/datetime64)) — وقت الحدث بدقة الميكروثانية.
* `ProfileEvent_Query` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الاستعلامات التي يجب تفسيرها وقد تُنفَّذ. لا يشمل الاستعلامات التي فشل تحليلها أو التي رُفضت بسبب حدود حجم AST أو حدود الحصة أو الحدود المفروضة على عدد الاستعلامات التي تعمل بالتزامن. وقد يشمل استعلامات داخلية يبدأها ClickHouse نفسه. ولا يحتسب الاستعلامات الفرعية.
* `ProfileEvent_SelectQuery` ([UInt64](/ar/reference/data-types/int-uint)) — مثل Query، ولكن لاستعلامات SELECT فقط.
* `ProfileEvent_InsertQuery` ([UInt64](/ar/reference/data-types/int-uint)) — مثل Query، ولكن لاستعلامات INSERT فقط.
* `ProfileEvent_InitialQuery` ([UInt64](/ar/reference/data-types/int-uint)) — مثل Query، ولكنه يحتسب الاستعلامات الأولية فقط (راجع is&#95;initial&#95;query).
* `ProfileEvent_InitialSelectQuery` ([UInt64](/ar/reference/data-types/int-uint)) — مثل InitialQuery، ولكن لاستعلامات SELECT فقط.
* `ProfileEvent_QueriesWithSubqueries` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الاستعلامات بما في ذلك جميع الاستعلامات الفرعية
* `ProfileEvent_SelectQueriesWithSubqueries` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استعلامات SELECT بما في ذلك جميع الاستعلامات الفرعية
* `ProfileEvent_InsertQueriesWithSubqueries` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استعلامات INSERT بما في ذلك جميع الاستعلامات الفرعية
* `ProfileEvent_SelectQueriesWithPrimaryKeyUsage` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استعلامات SELECT التي تستخدم المفتاح الأساسي لتقييم شرط WHERE
* `ProfileEvent_AsyncInsertQuery` ([UInt64](/ar/reference/data-types/int-uint)) — مثل InsertQuery، ولكن لاستعلامات INSERT غير المتزامنة فقط.
* `ProfileEvent_AsyncInsertBytes` ([UInt64](/ar/reference/data-types/int-uint)) — حجم البيانات بالبايت لاستعلامات INSERT غير المتزامنة.
* `ProfileEvent_AsyncInsertRows` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الصفوف المُدرجة بواسطة استعلامات INSERT غير المتزامنة.
* `ProfileEvent_AsyncInsertCacheHits` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي عُثر فيها على معرّف hash مكرّر في ذاكرة التخزين المؤقت لمعرّفات hash الخاصة بـ INSERT غير المتزامن.
* `ProfileEvent_FailedInternalQuery` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الاستعلامات الداخلية الفاشلة.
* `ProfileEvent_FailedInternalSelectQuery` ([UInt64](/ar/reference/data-types/int-uint)) — مثل FailedInternalQuery، ولكن لاستعلامات SELECT فقط.
* `ProfileEvent_FailedInternalInsertQuery` ([UInt64](/ar/reference/data-types/int-uint)) — مثل FailedInternalQuery، ولكن لاستعلامات INSERT فقط.
* `ProfileEvent_FailedInitialQuery` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الاستعلامات الأولية الفاشلة.
* `ProfileEvent_FailedInitialSelectQuery` ([UInt64](/ar/reference/data-types/int-uint)) — مثل FailedInitialQuery، ولكن لاستعلامات SELECT فقط.
* `ProfileEvent_FailedQuery` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للاستعلامات الفاشلة، سواء كانت استعلامات داخلية أم استعلامات مستخدمين.
* `ProfileEvent_FailedSelectQuery` ([UInt64](/ar/reference/data-types/int-uint)) — مثل FailedQuery، ولكن لاستعلامات SELECT فقط.
* `ProfileEvent_FailedInsertQuery` ([UInt64](/ar/reference/data-types/int-uint)) — مثل FailedQuery، ولكن لاستعلامات INSERT فقط.
* `ProfileEvent_FailedAsyncInsertQuery` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استعلامات ASYNC INSERT الفاشلة.
* `ProfileEvent_ASTFuzzerQueries` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الاستعلامات المشوَّشة التي حاولها server-side AST fuzzer.
* `ProfileEvent_QueryTimeMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت الإجمالي لجميع الاستعلامات.
* `ProfileEvent_SelectQueryTimeMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت الإجمالي لاستعلامات SELECT.
* `ProfileEvent_InsertQueryTimeMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت الإجمالي لاستعلامات INSERT.
* `ProfileEvent_OtherQueryTimeMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت الإجمالي للاستعلامات التي ليست من نوع SELECT أو INSERT.
* `ProfileEvent_FileOpen` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الملفات المفتوحة.
* `ProfileEvent_Seek` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استدعاء الدالة &#39;lseek&#39;.
* `ProfileEvent_ReadBufferFromFileDescriptorRead` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات القراءة (read/pread) من واصف ملف. لا يشمل المقابس.
* `ProfileEvent_ReadBufferFromFileDescriptorReadFailed` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي فشلت فيها القراءة (read/pread) من واصف ملف.
* `ProfileEvent_ReadBufferFromFileDescriptorReadBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المقروءة من واصفات الملفات. إذا كان الملف مضغوطًا، فسيعرض هذا حجم البيانات المضغوطة.
* `ProfileEvent_WriteBufferFromFileDescriptorWrite` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات الكتابة (write/pwrite) إلى واصف ملف. لا يشمل المقابس.
* `ProfileEvent_WriteBufferFromFileDescriptorWriteFailed` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي فشلت فيها الكتابة (write/pwrite) إلى واصف ملف.
* `ProfileEvent_WriteBufferFromFileDescriptorWriteBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المكتوبة إلى واصفات الملفات. إذا كان الملف مضغوطًا، فسيعرض هذا حجم البيانات المضغوطة.
* `ProfileEvent_FileSync` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استدعاء الدالة F&#95;FULLFSYNC/fsync/fdatasync للملفات.
* `ProfileEvent_DirectorySync` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استدعاء الدالة F&#95;FULLFSYNC/fsync/fdatasync للدلائل.
* `ProfileEvent_FileSyncElapsedMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في انتظار استدعاء النظام F&#95;FULLFSYNC/fsync/fdatasync للملفات.
* `ProfileEvent_DirectorySyncElapsedMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في انتظار استدعاء النظام F&#95;FULLFSYNC/fsync/fdatasync للدلائل.
* `ProfileEvent_ReadCompressedBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات (أي عدد البايتات قبل فك الضغط) المقروءة من المصادر المضغوطة (الملفات، الشبكة).
* `ProfileEvent_CompressedReadBufferBlocks` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الكتل المضغوطة (كتل البيانات التي تُضغط كلٌّ منها بشكل مستقل) المقروءة من المصادر المضغوطة (الملفات، الشبكة).
* `ProfileEvent_CompressedReadBufferBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات غير المضغوطة (أي عدد البايتات بعد فك الضغط) المقروءة من المصادر المضغوطة (الملفات، الشبكة).
* `ProfileEvent_CompressedReadBufferChecksumDoesntMatch` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي لم يتطابق فيها المجموع الاختباري للكتلة المضغوطة.
* `ProfileEvent_CompressedReadBufferChecksumDoesntMatchSingleBitMismatch` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي كان فيها عدم تطابق المجموع الاختباري للكتلة المضغوطة ناتجًا عن اختلاف بِتّ واحد.
* `ProfileEvent_CompressedReadBufferChecksumDoesntMatchMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في اكتشاف انقلابات البِتّات الناتجة عن عدم تطابق المجموع الاختباري للكتلة المضغوطة.
* `ProfileEvent_UncompressedCacheHits` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي جرى فيها العثور على كتلة بيانات في التخزين المؤقت غير المضغوط (مع تجنّب فك الضغط).
* `ProfileEvent_UncompressedCacheMisses` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي لم يُعثر فيها على كتلة بيانات في الذاكرة المؤقتة غير المضغوطة (ما استلزم فك الضغط).
* `ProfileEvent_UncompressedCacheWeightLost` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات التي أُخرجت من الذاكرة المؤقتة غير المضغوطة.
* `ProfileEvent_PageCacheHits` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي عُثر فيها على كتلة بيانات في ذاكرة تخزين الصفحات في فضاء المستخدم.
* `ProfileEvent_PageCacheMisses` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي لم يُعثر فيها على كتلة بيانات في ذاكرة تخزين الصفحات في فضاء المستخدم.
* `ProfileEvent_PageCacheWeightLost` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات التي أُخرجت من ذاكرة تخزين الصفحات في فضاء المستخدم
* `ProfileEvent_PageCacheResized` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي جرى فيها تغيير حجم ذاكرة تخزين الصفحات في فضاء المستخدم تلقائيًا (يحدث ذلك عادةً بضع مرات في الثانية، ويتحكم فيه memory&#95;worker&#95;period&#95;ms).
* `ProfileEvent_PageCacheOvercommitResize` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي جرى فيها تغيير حجم ذاكرة تخزين الصفحات في فضاء المستخدم تلقائيًا لتحرير الذاكرة أثناء تخصيص الذاكرة.
* `ProfileEvent_PageCacheReadBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المقروءة من ذاكرة تخزين الصفحات في فضاء المستخدم.
* `ProfileEvent_MMappedFileCacheHits` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي عُثر فيها على ملف في ذاكرة التخزين المؤقت MMap (لـ read&#95;method &#39;mmap&#39;)، لذلك لم نحتج إلى تنفيذ mmap له مرة أخرى.
* `ProfileEvent_MMappedFileCacheMisses` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي لم يُعثر فيها على ملف في ذاكرة التخزين المؤقت MMap (لـ read&#95;method &#39;mmap&#39;)، لذلك اضطررنا إلى تنفيذ mmap له مرة أخرى.
* `ProfileEvent_OpenedFileCacheHits` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي عُثر فيها على ملف في opened file cache، لذلك لم نحتج إلى فتحه مرة أخرى.
* `ProfileEvent_OpenedFileCacheMisses` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي لم يُعثر فيها على ملف في opened file cache، لذلك اضطررنا إلى فتحه مرة أخرى.
* `ProfileEvent_OpenedFileCacheMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — مقدار الوقت المستغرَق في تنفيذ أساليب OpenedFileCache.
* `ProfileEvent_AIOWrite` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات الكتابة باستخدام واجهة AIO في Linux أو FreeBSD
* `ProfileEvent_AIOWriteBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المكتوبة باستخدام واجهة AIO في Linux أو FreeBSD
* `ProfileEvent_AIORead` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات القراءة باستخدام واجهة AIO في Linux أو FreeBSD
* `ProfileEvent_AIOReadBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المقروءة باستخدام واجهة AIO في Linux أو FreeBSD
* `ProfileEvent_IOBufferAllocs` ([UInt64](/ar/reference/data-types/int-uint)) — عدد تخصيصات مخازن IO المؤقتة (لـ ReadBuffer/WriteBuffer).
* `ProfileEvent_IOBufferAllocBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المخصصة لمخازن IO المؤقتة (لـ ReadBuffer/WriteBuffer).
* `ProfileEvent_ArenaAllocChunks` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المقاطع المخصصة لذاكرة Arena (المستخدمة في GROUP BY والعمليات المشابهة)
* `ProfileEvent_ArenaAllocBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المخصصة لذاكرة Arena (المستخدمة في GROUP BY والعمليات المشابهة)
* `ProfileEvent_FunctionExecute` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استدعاءات دوال SQL العادية (تُستدعى دوال SQL على مستوى كل كتلة، لذا يمثّل هذا الرقم عدد الكتل).
* `ProfileEvent_TableFunctionExecute` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استدعاءات دوال الجدول.
* `ProfileEvent_DefaultImplementationForNullsRows` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الصفوف التي عالجها التنفيذ الافتراضي لقيم NULL أثناء تنفيذ الدالة
* `ProfileEvent_DefaultImplementationForNullsRowsWithNulls` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الصفوف التي تحتوي على قيم NULL والتي عالجها التنفيذ الافتراضي لقيم NULL أثناء تنفيذ الدالة
* `ProfileEvent_MarkCacheHits` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي عُثر فيها على مُدخل في ذاكرة التخزين المؤقت للعلامات، لذا لم تكن هناك حاجة إلى تحميل ملف العلامات.
* `ProfileEvent_MarkCacheMisses` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي لم يُعثر فيها على مُدخل في ذاكرة التخزين المؤقت للعلامات، لذا كان لا بد من تحميل ملف العلامات إلى الذاكرة، وهي عملية مكلفة تزيد من زمن استجابة الاستعلام.
* `ProfileEvent_PrimaryIndexCacheHits` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي عُثر فيها على مُدخل في ذاكرة التخزين المؤقت للفهرس الأساسي، لذا لم تكن هناك حاجة إلى تحميل ملف الفهرس.
* `ProfileEvent_PrimaryIndexCacheMisses` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي لم يُعثر فيها على مُدخل في ذاكرة التخزين المؤقت للفهرس الأساسي، لذا كان لا بد من تحميل ملف الفهرس إلى الذاكرة، وهي عملية مكلفة تزيد من زمن استجابة الاستعلام.
* `ProfileEvent_IcebergMetadataFilesCacheHits` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي عُثر فيها على ملفات بيانات Iceberg الوصفية في ذاكرة التخزين المؤقت.
* `ProfileEvent_IcebergMetadataFilesCacheMisses` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي لم يُعثر فيها على ملفات بيانات Iceberg الوصفية في ذاكرة التخزين المؤقت لبيانات Iceberg الوصفية، وكان لا بد من قراءتها من القرص (البعيد).
* `ProfileEvent_IcebergMetadataFilesCacheStaleMisses` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي عُثر فيها على ملفات بيانات Iceberg الوصفية في ذاكرة التخزين المؤقت، لكنها اعتُبرت قديمة وكان لا بد من قراءتها من القرص (البعيد).
* `ProfileEvent_IcebergMetadataFilesCacheWeightLost` ([UInt64](/ar/reference/data-types/int-uint)) — العدد التقريبي للبايتات التي أُخرجت من ذاكرة التخزين المؤقت لبيانات Iceberg الوصفية.
* `ProfileEvent_IcebergMetadataReadWaitTimeMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي تقضيه قارئات البيانات في انتظار قراءة ملفات بيانات Iceberg الوصفية وتحليلها، محسوبًا عبر جميع خيوط القراءة.
* `ProfileEvent_ParquetMetadataCacheHits` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي عُثر فيها على بيانات Parquet الوصفية في ذاكرة التخزين المؤقت.
* `ProfileEvent_ParquetMetadataCacheMisses` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي لم يُعثر فيها على بيانات Parquet الوصفية في ذاكرة التخزين المؤقت وكان لا بد من قراءتها من القرص.
* `ProfileEvent_ParquetMetadataCacheWeightLost` ([UInt64](/ar/reference/data-types/int-uint)) — العدد التقريبي للبايتات التي أُخرجت من ذاكرة التخزين المؤقت لبيانات Parquet الوصفية.
* `ProfileEvent_IcebergIteratorInitializationMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في التهيئة المتزامنة لمكرّرات بيانات Iceberg.
* `ProfileEvent_IcebergMetadataUpdateMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في تحديث بيانات التخزين الوصفية لـ Iceberg انطلاقًا من الحالة (`buildStorageMetadataFromState`).
* `ProfileEvent_IcebergMetadataReturnedObjectInfos` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لمعلومات الكائنات المُعادة من مكرّر Iceberg.
* `ProfileEvent_IcebergMinMaxNonPrunedDeleteFiles` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لأزواج ملفات البيانات وملفات الحذف الموضعي المقبولة بواسطة تحليل minmax من بين الأزواج المناسبة بحسب التقسيم ورقم التسلسل.
* `ProfileEvent_IcebergMinMaxPrunedDeleteFiles` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لأزواج ملفات البيانات وملفات الحذف الموضعي المقبولة بواسطة تحليل minmax من بين الأزواج المناسبة بحسب التقسيم ورقم التسلسل.
* `ProfileEvent_VectorSimilarityIndexCacheHits` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي عُثر فيها على حبيبة فهرس في ذاكرة التخزين المؤقت للفهرس المتجهي.
* `ProfileEvent_VectorSimilarityIndexCacheMisses` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي لم يُعثر فيها على حبيبة فهرس في ذاكرة التخزين المؤقت للفهرس المتجهي وكان لا بد من قراءتها من القرص.
* `ProfileEvent_VectorSimilarityIndexCacheWeightLost` ([UInt64](/ar/reference/data-types/int-uint)) — العدد التقريبي للبايتات التي أُخرجت من ذاكرة التخزين المؤقت للفهرس المتجهي.
* `ProfileEvent_TextIndexReadDictionaryBlocks` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي جرت فيها قراءة block القاموس الخاص بالفهرس النصي من القرص.
* `ProfileEvent_TextIndexTokensCacheHits` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي عُثر فيها على معلومات الرمز المميّز الخاصة بالفهرس النصي في ذاكرة التخزين المؤقت.
* `ProfileEvent_TextIndexTokensCacheMisses` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي لم يُعثر فيها على معلومات الرمز المميّز الخاصة بالفهرس النصي في ذاكرة التخزين المؤقت.
* `ProfileEvent_TextIndexHeaderCacheHits` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي جرى فيها العثور على ترويسة في الذاكرة المؤقتة.
* `ProfileEvent_TextIndexHeaderCacheMisses` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي لم يُعثر فيها على ترويسة في الذاكرة المؤقتة.
* `ProfileEvent_TextIndexPostingsCacheHits` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي جرى فيها العثور على قائمة ترحيلات لفهرس نصي في الذاكرة المؤقتة.
* `ProfileEvent_TextIndexPostingsCacheMisses` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي لم يُعثر فيها على قائمة ترحيلات لفهرس نصي في الذاكرة المؤقتة.
* `ProfileEvent_TextIndexReadSparseIndexBlocks` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي تمت فيها قراءة كتلة من الفهرس المتناثر من فهرس النص.
* `ProfileEvent_TextIndexReaderTotalMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في قراءة فهرس النص.
* `ProfileEvent_TextIndexReadGranulesMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في قراءة حبيبات فهرس النص وتحليلها.
* `ProfileEvent_TextIndexReadPostings` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي تمت فيها قراءة قائمة ترحيلات من فهرس النص.
* `ProfileEvent_TextIndexUsedEmbeddedPostings` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي استُخدمت فيها قائمة ترحيلات مضمنة في القاموس.
* `ProfileEvent_TextIndexUseHint` ([UInt64](/ar/reference/data-types/int-uint)) — عدد حبيبات الفهرس التي أُضيفت إليها قراءة مباشرة من فهرس النص كتلميح ثم استُخدمت.
* `ProfileEvent_TextIndexDiscardHint` ([UInt64](/ar/reference/data-types/int-uint)) — عدد حبيبات الفهرس التي أُضيفت إليها قراءة مباشرة من فهرس النص كتلميح ثم استُبعدت بسبب انخفاض الانتقائية.
* `ProfileEvent_TextIndexDiscardPatternScan` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي جرى فيها استبعاد فحص القاموس المستند إلى النمط في فهرس نصي لأن عدد قوائم الترحيلات المطلوب قراءتها تجاوز العتبة.
* `ProfileEvent_QueryConditionCacheHits` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي جرى فيها العثور على إدخال في QueryConditionCache (وبالتالي يمكن تخطي قراءة العلامات). يُحدَّث فقط لاستعلامات SELECT التي تتضمن SETTING use&#95;query&#95;condition&#95;cache = 1.
* `ProfileEvent_QueryConditionCacheMisses` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي لم يُعثر فيها على إدخال في QueryConditionCache (وبالتالي لا يمكن تخطي قراءة العلامة). يُحدَّث فقط لاستعلامات SELECT التي تتضمن SETTING use&#95;query&#95;condition&#95;cache = 1.
* `ProfileEvent_QueryCacheHits` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي جرى فيها العثور على نتيجة استعلام في query cache (وبالتالي تم تجنّب حساب الاستعلام). يُحدَّث فقط لاستعلامات SELECT التي تتضمن SETTING use&#95;query&#95;cache = 1.
* `ProfileEvent_QueryCacheMisses` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي لم يُعثر فيها على نتيجة استعلام في query cache (مما استلزم حساب الاستعلام). يُحدَّث فقط لاستعلامات SELECT التي تتضمن SETTING use&#95;query&#95;cache = 1.
* `ProfileEvent_QueryCacheAgeSeconds` ([UInt64](/ar/reference/data-types/int-uint)) — مجموع أعمار إدخالات query cache التي جرى العثور عليها، بالثواني. تُسجَّل القيمة لكلٍّ من حالات الإصابة والإخفاق.
* `ProfileEvent_QueryCacheReadRows` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الصفوف المقروءة من query cache.
* `ProfileEvent_QueryCacheReadBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات (غير المضغوطة) المقروءة من query cache.
* `ProfileEvent_QueryCacheWrittenRows` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الصفوف المحفوظة في query cache.
* `ProfileEvent_QueryCacheWrittenBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات (غير المضغوطة) المحفوظة في query cache
* `ProfileEvent_CreatedReadBufferOrdinary` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي تم فيها إنشاء مخزن قراءة مؤقت عادي لقراءة البيانات (أثناء الاختيار بين طرق القراءة الأخرى).
* `ProfileEvent_CreatedReadBufferDirectIO` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي تم فيها إنشاء مخزن قراءة مؤقت مع O&#95;DIRECT لقراءة البيانات (أثناء الاختيار بين طرق القراءة الأخرى).
* `ProfileEvent_CreatedReadBufferDirectIOFailed` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي جرت فيها محاولة إنشاء مخزن قراءة مؤقت باستخدام O&#95;DIRECT لقراءة البيانات (أثناء المفاضلة بين طرائق قراءة أخرى)، لكن نظام التشغيل لم يسمح بذلك (بسبب عدم دعم نظام الملفات أو لأسباب أخرى)، فتمت العودة إلى طريقة القراءة العادية.
* `ProfileEvent_CreatedReadBufferMMap` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي أُنشئ فيها مخزن قراءة مؤقت باستخدام &#39;mmap&#39; لقراءة البيانات (أثناء المفاضلة بين طرائق قراءة أخرى).
* `ProfileEvent_CreatedReadBufferMMapFailed` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي جرت فيها محاولة إنشاء مخزن قراءة مؤقت باستخدام &#39;mmap&#39; لقراءة البيانات (أثناء المفاضلة بين طرائق قراءة أخرى)، لكن نظام التشغيل لم يسمح بذلك (بسبب عدم دعم نظام الملفات أو لأسباب أخرى)، فتمت العودة إلى طريقة القراءة العادية.
* `ProfileEvent_DiskReadElapsedMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرق في انتظار استدعاء نظام القراءة. ويشمل ذلك القراءات من ذاكرة التخزين المؤقت للصفحات.
* `ProfileEvent_DiskWriteElapsedMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرق في انتظار استدعاء نظام الكتابة. ويشمل ذلك الكتابات إلى ذاكرة التخزين المؤقت للصفحات.
* `ProfileEvent_NetworkReceiveElapsedMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرق في انتظار استلام البيانات أو في استلامها من الشبكة. وتُحتسب فقط تفاعلات الشبكة المرتبطة بـ ClickHouse، لا تلك الخاصة بمكتبات الجهات الخارجية.
* `ProfileEvent_NetworkSendElapsedMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرق في انتظار إرسال البيانات إلى الشبكة أو في إرسالها عبرها. وتُحتسب فقط تفاعلات الشبكة المرتبطة بـ ClickHouse، لا تلك الخاصة بمكتبات الجهات الخارجية.
* `ProfileEvent_NetworkReceiveBytes` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي عدد البايتات المستلمة من الشبكة. وتُحتسب فقط تفاعلات الشبكة المرتبطة بـ ClickHouse، لا تلك الخاصة بمكتبات الجهات الخارجية.
* `ProfileEvent_NetworkSendBytes` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي عدد البايتات المرسلة عبر الشبكة. وتُحتسب فقط تفاعلات الشبكة المرتبطة بـ ClickHouse، لا تلك الخاصة بمكتبات الجهات الخارجية.
* `ProfileEvent_FilterPartsByVirtualColumnsMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرق في الدالة filterPartsByVirtualColumns.
* `ProfileEvent_GlobalThreadPoolExpansions` ([UInt64](/ar/reference/data-types/int-uint)) — يحسب العدد الإجمالي للمرات التي أُضيفت فيها خيوط جديدة إلى مجمّع الخيوط العام. ويُظهر هذا المقياس مدى تكرار توسعة مجمّع الخيوط العام لاستيعاب زيادة متطلبات المعالجة.
* `ProfileEvent_GlobalThreadPoolShrinks` ([UInt64](/ar/reference/data-types/int-uint)) — يحسب العدد الإجمالي للمرات التي تقلّص فيها مجمّع الخيوط العام عبر إزالة خيوط. ويحدث ذلك عندما يتجاوز عدد الخيوط الخاملة القيمة max&#95;thread&#95;pool&#95;free&#95;size، مما يشير إلى تعديلات في حجم مجمّع الخيوط العام استجابةً لانخفاض استخدام الخيوط.
* `ProfileEvent_GlobalThreadPoolThreadCreationMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرق في انتظار بدء تشغيل خيوط جديدة.
* `ProfileEvent_GlobalThreadPoolLockWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضته الخيوط في انتظار الأقفال داخل مجمّع الخيوط العام.
* `ProfileEvent_GlobalThreadPoolJobs` ([UInt64](/ar/reference/data-types/int-uint)) — يحسب عدد المهام التي دُفعت إلى مجمّع الخيوط العام.
* `ProfileEvent_GlobalThreadPoolJobWaitTimeMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — يقيس الزمن المنقضي من وقت جدولة مهمة في مجمّع الخيوط إلى أن يلتقطها خيط عامل لتنفيذها. ويساعد هذا المقياس في تحديد التأخيرات في معالجة المهام، بما يوضح مدى استجابة مجمّع الخيوط للمهام الجديدة.
* `ProfileEvent_LocalThreadPoolExpansions` ([UInt64](/ar/reference/data-types/int-uint)) — يحسب العدد الإجمالي للمرات التي جرت فيها استعارة خيوط من مجمّع الخيوط العام لتوسيع مجمّعات الخيوط المحلية.
* `ProfileEvent_LocalThreadPoolShrinks` ([UInt64](/ar/reference/data-types/int-uint)) — يحسب العدد الإجمالي للمرات التي أُعيدت فيها خيوط من مجمّعات الخيوط المحلية إلى مجمّع الخيوط العام.
* `ProfileEvent_LocalThreadPoolThreadCreationMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضته مجمّعات الخيوط المحلية في انتظار استعارة خيط من المجمّع العام.
* `ProfileEvent_LocalThreadPoolLockWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضته خيوط التنفيذ في انتظار الأقفال ضمن تجمعات خيوط التنفيذ المحلية.
* `ProfileEvent_LocalThreadPoolJobs` ([UInt64](/ar/reference/data-types/int-uint)) — يحسب عدد المهام التي دُفعت إلى تجمعات خيوط التنفيذ المحلية.
* `ProfileEvent_LocalThreadPoolBusyMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضته خيوط التنفيذ في تنفيذ العمل الفعلي.
* `ProfileEvent_LocalThreadPoolJobWaitTimeMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — يقيس الزمن المنقضي منذ جدولة مهمة في تجمع خيوط التنفيذ وحتى يلتقطها خيط تنفيذ عامل للتنفيذ. يساعد هذا المقياس على تحديد حالات التأخير في معالجة المهام، مما يوضح مدى استجابة تجمع خيوط التنفيذ للمهام الجديدة.
* `ProfileEvent_DiskS3GetRequestThrottlerCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات GET وSELECT الخاصة بـ DiskS3 التي مرّت عبر المُقيِّد، سواء المحظورة أو غير المحظورة.
* `ProfileEvent_DiskS3GetRequestThrottlerBlocked` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات GET وSELECT الخاصة بـ DiskS3 التي حظرها المُقيِّد.
* `ProfileEvent_DiskS3GetRequestThrottlerSleepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي كان فيه الاستعلام في حالة انتظار للامتثال لتقييد طلبات GET وSELECT الخاصة بـ DiskS3.
* `ProfileEvent_DiskS3PutRequestThrottlerCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات PUT وCOPY وPOST وLIST الخاصة بـ DiskS3 التي مرّت عبر المُقيِّد، سواء المحظورة أو غير المحظورة.
* `ProfileEvent_DiskS3PutRequestThrottlerBlocked` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات PUT وCOPY وPOST وLIST الخاصة بـ DiskS3 التي حظرها المُقيِّد.
* `ProfileEvent_DiskS3PutRequestThrottlerSleepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي كان فيه الاستعلام في حالة انتظار للامتثال لتقييد طلبات PUT وCOPY وPOST وLIST الخاصة بـ DiskS3.
* `ProfileEvent_S3GetRequestThrottlerCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات GET وSELECT الخاصة بـ S3 التي مرّت عبر المُقيِّد، سواء المحظورة أو غير المحظورة.
* `ProfileEvent_S3GetRequestThrottlerBlocked` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات GET وSELECT الخاصة بـ S3 التي حظرها المُقيِّد.
* `ProfileEvent_S3GetRequestThrottlerSleepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي كان فيه الاستعلام في حالة انتظار للامتثال لتقييد طلبات GET وSELECT الخاصة بـ S3.
* `ProfileEvent_S3PutRequestThrottlerCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات PUT وCOPY وPOST وLIST الخاصة بـ S3 التي مرّت عبر المُقيِّد، سواء المحظورة أو غير المحظورة.
* `ProfileEvent_S3PutRequestThrottlerBlocked` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات PUT وCOPY وPOST وLIST الخاصة بـ S3 التي حظرها المُقيِّد.
* `ProfileEvent_S3PutRequestThrottlerSleepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي كان فيه الاستعلام في حالة انتظار للامتثال لتقييد طلبات PUT وCOPY وPOST وLIST الخاصة بـ S3.
* `ProfileEvent_ACMEAPIRequests` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات واجهة برمجة تطبيقات ACME التي أُرسلت.
* `ProfileEvent_ACMECertificateOrders` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات شهادات ACME التي أُرسلت.
* `ProfileEvent_DiskAzureReadMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في انتظار طلبات القراءة من قرص Azure.
* `ProfileEvent_DiskAzureReadRequestsCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات القراءة من قرص Azure.
* `ProfileEvent_DiskAzureReadRequestsErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد أخطاء طلبات القراءة من قرص Azure.
* `ProfileEvent_DiskAzureReadRequestsThrottling` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات القراءة من قرص Azure التي خضعت لتقييد المعدل.
* `ProfileEvent_DiskAzureReadRequestsRedirects` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات إعادة توجيه طلبات القراءة من قرص Azure.
* `ProfileEvent_DiskAzureWriteMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في انتظار طلبات الكتابة إلى قرص Azure.
* `ProfileEvent_DiskAzureWriteRequestsCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات الكتابة على قرص Azure.
* `ProfileEvent_DiskAzureWriteRequestsErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد أخطاء طلبات الكتابة على قرص Azure.
* `ProfileEvent_DiskAzureWriteRequestsThrottling` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات الكتابة على قرص Azure التي تم تقييد معدلها.
* `ProfileEvent_DiskAzureWriteRequestsRedirects` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات إعادة توجيه طلبات الكتابة على قرص Azure.
* `ProfileEvent_AzureReadMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في انتظار طلبات القراءة من Azure.
* `ProfileEvent_AzureReadRequestsCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات القراءة من Azure.
* `ProfileEvent_AzureReadRequestsErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد أخطاء طلبات القراءة من Azure.
* `ProfileEvent_AzureReadRequestsThrottling` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات القراءة من Azure التي تم تقييد معدلها.
* `ProfileEvent_AzureReadRequestsRedirects` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات إعادة توجيه طلبات القراءة من Azure.
* `ProfileEvent_AzureWriteMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في انتظار طلبات الكتابة إلى Azure.
* `ProfileEvent_AzureWriteRequestsCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات الكتابة إلى Azure.
* `ProfileEvent_AzureWriteRequestsErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد أخطاء طلبات الكتابة إلى Azure.
* `ProfileEvent_AzureWriteRequestsThrottling` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات الكتابة إلى Azure التي تم تقييد معدلها.
* `ProfileEvent_AzureWriteRequestsRedirects` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات إعادة توجيه طلبات الكتابة إلى Azure.
* `ProfileEvent_AzureGetRequestThrottlerCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات Azure GET التي مرّت عبر مقيِّد المعدل: المحجوبة وغير المحجوبة.
* `ProfileEvent_AzureGetRequestThrottlerBlocked` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات Azure GET التي حجبها مقيِّد المعدل.
* `ProfileEvent_AzureGetRequestThrottlerSleepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضاه الاستعلام في الانتظار للالتزام بتقييد معدل طلبات Azure GET.
* `ProfileEvent_DiskAzureGetRequestThrottlerCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات Azure GET لقرص Azure التي مرّت عبر مقيِّد المعدل: المحجوبة وغير المحجوبة.
* `ProfileEvent_DiskAzureGetRequestThrottlerBlocked` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات Azure GET لقرص Azure التي حجبها مقيِّد المعدل.
* `ProfileEvent_DiskAzureGetRequestThrottlerSleepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضاه الاستعلام في الانتظار للالتزام بتقييد معدل طلبات Azure GET لقرص Azure.
* `ProfileEvent_AzurePutRequestThrottlerCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات Azure PUT التي مرّت عبر مقيِّد المعدل: المحجوبة وغير المحجوبة.
* `ProfileEvent_AzurePutRequestThrottlerBlocked` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات Azure PUT التي حجبها مقيِّد المعدل.
* `ProfileEvent_AzurePutRequestThrottlerSleepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضاه الاستعلام في الانتظار للالتزام بتقييد معدل طلبات Azure PUT.
* `ProfileEvent_DiskAzurePutRequestThrottlerCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات Azure PUT لقرص Azure التي مرّت عبر مقيِّد المعدل: المحجوبة وغير المحجوبة.
* `ProfileEvent_DiskAzurePutRequestThrottlerBlocked` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات Azure PUT لقرص Azure التي حجبها مقيِّد المعدل.
* `ProfileEvent_DiskAzurePutRequestThrottlerSleepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضاه الاستعلام في الانتظار للامتثال لتقييد معدل طلبات PUT لقرص Azure.
* `ProfileEvent_RemoteReadThrottlerBytes` ([UInt64](/ar/reference/data-types/int-uint)) — البايتات التي مرّت عبر مُقيِّد &#39;max&#95;remote&#95;read&#95;network&#95;bandwidth&#95;for&#95;server&#39;/&#39;max&#95;remote&#95;read&#95;network&#95;bandwidth&#39;.
* `ProfileEvent_RemoteReadThrottlerSleepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضاه الاستعلام في الانتظار للامتثال لتقييد &#39;max&#95;remote&#95;read&#95;network&#95;bandwidth&#95;for&#95;server&#39;/&#39;max&#95;remote&#95;read&#95;network&#95;bandwidth&#39;.
* `ProfileEvent_RemoteWriteThrottlerBytes` ([UInt64](/ar/reference/data-types/int-uint)) — البايتات التي مرّت عبر مُقيِّد &#39;max&#95;remote&#95;write&#95;network&#95;bandwidth&#95;for&#95;server&#39;/&#39;max&#95;remote&#95;write&#95;network&#95;bandwidth&#39;.
* `ProfileEvent_RemoteWriteThrottlerSleepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضاه الاستعلام في الانتظار للامتثال لتقييد &#39;max&#95;remote&#95;write&#95;network&#95;bandwidth&#95;for&#95;server&#39;/&#39;max&#95;remote&#95;write&#95;network&#95;bandwidth&#39;.
* `ProfileEvent_LocalReadThrottlerBytes` ([UInt64](/ar/reference/data-types/int-uint)) — البايتات التي مرّت عبر مُقيِّد &#39;max&#95;local&#95;read&#95;bandwidth&#95;for&#95;server&#39;/&#39;max&#95;local&#95;read&#95;bandwidth&#39;.
* `ProfileEvent_LocalReadThrottlerSleepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضاه الاستعلام في الانتظار للامتثال لتقييد &#39;max&#95;local&#95;read&#95;bandwidth&#95;for&#95;server&#39;/&#39;max&#95;local&#95;read&#95;bandwidth&#39;.
* `ProfileEvent_LocalWriteThrottlerBytes` ([UInt64](/ar/reference/data-types/int-uint)) — البايتات التي مرّت عبر مُقيِّد &#39;max&#95;local&#95;write&#95;bandwidth&#95;for&#95;server&#39;/&#39;max&#95;local&#95;write&#95;bandwidth&#39;.
* `ProfileEvent_LocalWriteThrottlerSleepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضاه الاستعلام في الانتظار للامتثال لتقييد &#39;max&#95;local&#95;write&#95;bandwidth&#95;for&#95;server&#39;/&#39;max&#95;local&#95;write&#95;bandwidth&#39;.
* `ProfileEvent_BackupThrottlerBytes` ([UInt64](/ar/reference/data-types/int-uint)) — البايتات التي مرّت عبر مُقيِّد &#39;max&#95;backup&#95;bandwidth&#95;for&#95;server&#39;.
* `ProfileEvent_BackupThrottlerSleepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضاه الاستعلام في الانتظار للامتثال لتقييد &#39;max&#95;backup&#95;bandwidth&#95;for&#95;server&#39;.
* `ProfileEvent_MergesThrottlerBytes` ([UInt64](/ar/reference/data-types/int-uint)) — البايتات التي مرّت عبر مُقيِّد &#39;max&#95;merges&#95;bandwidth&#95;for&#95;server&#39;.
* `ProfileEvent_MergesThrottlerSleepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضاه الاستعلام في الانتظار للامتثال لتقييد &#39;max&#95;merges&#95;bandwidth&#95;for&#95;server&#39;.
* `ProfileEvent_MutationsThrottlerBytes` ([UInt64](/ar/reference/data-types/int-uint)) — البايتات التي مرّت عبر مُقيِّد &#39;max&#95;mutations&#95;bandwidth&#95;for&#95;server&#39;.
* `ProfileEvent_MutationsThrottlerSleepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضاه الاستعلام في الانتظار للامتثال لآلية throttling الخاصة بـ `max_mutations_bandwidth_for_server`.
* `ProfileEvent_UserThrottlerBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات التي مرّت عبر throttler الخاص بـ `max_network_bandwidth_for_user`.
* `ProfileEvent_UserThrottlerSleepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضاه الاستعلام في الانتظار للامتثال لآلية throttling الخاصة بـ `max_network_bandwidth_for_user`.
* `ProfileEvent_AllUsersThrottlerBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات التي مرّت عبر throttler الخاص بـ `max_network_bandwidth_for_all_users`.
* `ProfileEvent_AllUsersThrottlerSleepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضاه الاستعلام في الانتظار للامتثال لآلية throttling الخاصة بـ `max_network_bandwidth_for_all_users`.
* `ProfileEvent_QueryRemoteReadThrottlerBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات التي مرّت عبر throttler الخاص بـ `max_remote_read_network_bandwidth`.
* `ProfileEvent_QueryRemoteReadThrottlerSleepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضاه الاستعلام في الانتظار للامتثال لآلية throttling الخاصة بـ `max_remote_read_network_bandwidth`.
* `ProfileEvent_QueryRemoteWriteThrottlerBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات التي مرّت عبر throttler الخاص بـ `max_remote_write_network_bandwidth`.
* `ProfileEvent_QueryRemoteWriteThrottlerSleepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضاه الاستعلام في الانتظار للامتثال لآلية throttling الخاصة بـ `max_remote_write_network_bandwidth`.
* `ProfileEvent_QueryLocalReadThrottlerBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات التي مرّت عبر throttler الخاص بـ `max_local_read_bandwidth`.
* `ProfileEvent_QueryLocalReadThrottlerSleepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضاه الاستعلام في الانتظار للامتثال لآلية throttling الخاصة بـ `max_local_read_bandwidth`.
* `ProfileEvent_QueryLocalWriteThrottlerBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات التي مرّت عبر throttler الخاص بـ `max_local_write_bandwidth`.
* `ProfileEvent_QueryLocalWriteThrottlerSleepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضاه الاستعلام في الانتظار للامتثال لآلية throttling الخاصة بـ `max_local_write_bandwidth`.
* `ProfileEvent_QueryBackupThrottlerBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات التي مرّت عبر throttler الخاص بـ `max_backup_bandwidth`.
* `ProfileEvent_QueryBackupThrottlerSleepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضاه الاستعلام في الانتظار للامتثال لآلية throttling الخاصة بـ `max_backup_bandwidth`.
* `ProfileEvent_DistrCacheReadThrottlerBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات التي مرّت عبر throttler الخاص بـ `max_distributed_cache_read_bandwidth_for_server`.
* `ProfileEvent_DistrCacheReadThrottlerSleepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضاه الاستعلام في الانتظار للامتثال لآلية throttling الخاصة بـ `max_distributed_cache_read_bandwidth_for_server`.
* `ProfileEvent_DistrCacheWriteThrottlerBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات التي مرّت عبر محدِّد المعدل &#39;max&#95;distributed&#95;cache&#95;write&#95;bandwidth&#95;for&#95;server&#39;.
* `ProfileEvent_DistrCacheWriteThrottlerSleepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضاه الاستعلام في الانتظار للامتثال لتقييد &#39;max&#95;distributed&#95;cache&#95;write&#95;bandwidth&#95;for&#95;server&#39;.
* `ProfileEvent_ThrottlerSleepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضاه الاستعلام في الانتظار للامتثال لجميع إعدادات تقييد المعدل.
* `ProfileEvent_ReadTasksWithAppliedPatches` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لمهام القراءة التي طُبِّق عليها أي جزء تصحيح
* `ProfileEvent_PatchesAppliedInAllReadTasks` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لأجزاء التصحيح المطبَّقة عبر جميع مهام القراءة
* `ProfileEvent_PatchesMergeAppliedInAllReadTasks` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لأجزاء التصحيح المطبَّقة بوضع Merge عبر جميع مهام القراءة
* `ProfileEvent_PatchesJoinAppliedInAllReadTasks` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لأجزاء التصحيح المطبَّقة بوضع Join عبر جميع مهام القراءة
* `ProfileEvent_PatchesReadRows` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للصفوف المقروءة من أجزاء التصحيح
* `ProfileEvent_PatchesReadUncompressedBytes` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للبايتات غير المضغوطة المقروءة من أجزاء التصحيح
* `ProfileEvent_PatchesJoinRowsAddedToHashTable` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للصفوف المضافة إلى جداول التجزئة عند تطبيق أجزاء التصحيح بوضع Join
* `ProfileEvent_ApplyPatchesMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في تطبيق أجزاء التصحيح على الكتل
* `ProfileEvent_ReadPatchesMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في قراءة أجزاء التصحيح
* `ProfileEvent_BuildPatchesMergeMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في بناء الفهارس لتطبيق أجزاء التصحيح بوضع Merge
* `ProfileEvent_BuildPatchesJoinMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في بناء الفهارس وجداول التجزئة لتطبيق أجزاء التصحيح بوضع Join
* `ProfileEvent_AnalyzePatchRangesMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في تحليل فهرس أجزاء التصحيح
* `ProfileEvent_ReadTasksWithAppliedMutationsOnFly` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لمهام القراءة التي طُبِّقت عليها أي عملية تعديل أثناء التنفيذ
* `ProfileEvent_MutationsAppliedOnFlyInAllReadTasks` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لعمليات التعديل المطبَّقة أثناء التنفيذ عبر جميع مهام القراءة
* `ProfileEvent_PatchesAcquireLockTries` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لمحاولات الحصول على قفل لتنفيذ التحديثات خفيفة الوزن
* `ProfileEvent_PatchesAcquireLockMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للميكروثواني المستغرَقة للحصول على قفل لتنفيذ التحديثات خفيفة الوزن
* `ProfileEvent_DiskObjectStorageWaitBlobRemovalMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في انتظار إزالة الـ blob المعلّقة بعد تنفيذ commit لمعاملة البيانات الوصفية
* `ProfileEvent_SchedulerIOReadRequests` ([UInt64](/ar/reference/data-types/int-uint)) — طلبات الموارد التي مرّت عبر المجدول لعمليات IO reads.
* `ProfileEvent_SchedulerIOReadBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات التي مرّت عبر المجدول لعمليات IO reads.
* `ProfileEvent_SchedulerIOReadWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضاه الاستعلام في انتظار طلبات الموارد لعمليات IO reads.
* `ProfileEvent_SchedulerIOWriteRequests` ([UInt64](/ar/reference/data-types/int-uint)) — طلبات الموارد التي مرّت عبر المجدول لعمليات IO writes.
* `ProfileEvent_SchedulerIOWriteBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات التي مرّت عبر المجدول لعمليات IO writes.
* `ProfileEvent_SchedulerIOWriteWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضاه الاستعلام في انتظار طلبات الموارد لعمليات كتابة IO.
* `ProfileEvent_QueryMaskingRulesMatch` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات مطابقة Query masking rules بنجاح.
* `ProfileEvent_ReplicatedPartFetches` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات تنزيل جزء بيانات من replica لجدول ReplicatedMergeTree.
* `ProfileEvent_ReplicatedPartFailedFetches` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات فشل تنزيل جزء بيانات من replica لجدول ReplicatedMergeTree.
* `ProfileEvent_ObsoleteReplicatedParts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي كان فيها جزء بيانات مغطّى بجزء بيانات آخر تم جلبه من replica (ولذلك وُضع عليه وسم Obsolete ولم تعد هناك حاجة إليه).
* `ProfileEvent_ReplicatedPartMerges` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات دمج أجزاء البيانات الخاصة بجداول ReplicatedMergeTree بنجاح.
* `ProfileEvent_ReplicatedPartFetchesOfMerged` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي نفضّل فيها تنزيل merged part جاهز من replica لجدول ReplicatedMergeTree بدلاً من إجراء merge بأنفسنا (وعادةً نفضّل إجراء merge بأنفسنا لتقليل Network traffic). يحدث ذلك عندما لا تكون لدينا كل source parts اللازمة لإجراء merge أو عندما يكون جزء البيانات قديماً بما يكفي.
* `ProfileEvent_ReplicatedPartMutations` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي طُبّقت فيها mutations على أجزاء البيانات الخاصة بجداول ReplicatedMergeTree بنجاح.
* `ProfileEvent_ReplicatedPartChecks` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي اضطررنا فيها إلى إجراء advanced search عن جزء بيانات على replicas أو للتحقق من الحاجة إلى جزء بيانات موجود.
* `ProfileEvent_ReplicatedPartChecksFailed` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي لم يُسفر فيها advanced search عن جزء بيانات على replicas عن أي نتيجة، أو التي عُثر فيها على جزء Unexpected وتم نقله بعيداً.
* `ProfileEvent_ReplicatedDataLoss` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي لا يكون فيها جزء البيانات المطلوب موجوداً على أي replica (حتى على replicas غير المتصلة حالياً). وتُعد أجزاء البيانات هذه مفقودة نهائياً. وهذا أمر طبيعي في حالة asynchronous replication (إذا لم تكن quorum inserts مفعّلة)، عندما تتعطل replica التي كُتب إليها جزء البيانات، ثم بعد عودتها إلى العمل لا تحتوي على ذلك الجزء من البيانات.
* `ProfileEvent_ReplicatedCoveredPartsInZooKeeperOnStart` ([UInt64](/ar/reference/data-types/int-uint)) — لأغراض debugging. عدد الأجزاء الموجودة في ZooKeeper التي لها جزء يغطيها، لكنها غير موجودة على disk. ويُتحقق من ذلك عند بدء تشغيل server.
* `ProfileEvent_QuorumParts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد أجزاء البيانات المكتوبة باستخدام quorum. ويُحتسب كجزء واحد في insert المتزامن، وقد يصل إلى عدد async inserts في عملية insert التي تنفّذ flush للـ async inserts.
* `ProfileEvent_QuorumWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في انتظار quorum أثناء عمليات inserts.
* `ProfileEvent_QuorumFailedInserts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات inserts التي فشلت بسبب عدم اكتمال quorum.
* `ProfileEvent_InsertedRows` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الصفوف التي تم INSERTها في جميع الجداول.
* `ProfileEvent_InsertedBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات (غير المضغوطة؛ للأعمدة كما تكون مخزّنة في الذاكرة) التي تم INSERTها في جميع الجداول.
* `ProfileEvent_DelayedInserts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي تم فيها تقييد INSERT لكتلة إلى جدول MergeTree بسبب الارتفاع الكبير في عدد أجزاء البيانات النشطة في partition.
* `ProfileEvent_RejectedInserts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي رُفض فيها INSERT لكتلة إلى جدول MergeTree مع الاستثناء &#39;Too many parts&#39; بسبب الارتفاع الكبير في عدد أجزاء البيانات النشطة في partition.
* `ProfileEvent_DelayedInsertsMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي عدد المللي ثواني المستغرَقة أثناء تقييد INSERT لكتلة إلى جدول MergeTree بسبب الارتفاع الكبير في عدد أجزاء البيانات النشطة في partition.
* `ProfileEvent_DelayedMutations` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي جرى فيها تقييد mutation لجدول MergeTree بسبب الارتفاع الكبير في عدد mutations غير المكتملة للجدول.
* `ProfileEvent_RejectedMutations` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي رُفضت فيها mutation لجدول MergeTree مع الاستثناء &#39;Too many mutations&#39; بسبب الارتفاع الكبير في عدد mutations غير المكتملة للجدول.
* `ProfileEvent_DelayedMutationsMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي عدد المللي ثانية المستغرقة أثناء تقييد mutation لجدول MergeTree بسبب الارتفاع الكبير في عدد mutations غير المكتملة للجدول.
* `ProfileEvent_RejectedLightweightUpdates` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي رُفض فيها lightweight update بسبب وجود عدد كبير جدًا من البايتات غير المضغوطة في Patches.
* `ProfileEvent_DistributedDelayedInserts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي جرى فيها تقييد INSERT لكتلة إلى جدول Distributed بسبب الارتفاع الكبير في عدد البايتات المعلّقة.
* `ProfileEvent_DistributedRejectedInserts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي رُفض فيها INSERT لكتلة إلى جدول Distributed مع الاستثناء &#39;Too many bytes&#39; بسبب الارتفاع الكبير في عدد البايتات المعلّقة.
* `ProfileEvent_DistributedDelayedInsertsMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي عدد المللي ثانية المستغرقة أثناء تقييد INSERT لكتلة إلى جدول Distributed بسبب الارتفاع الكبير في عدد البايتات المعلّقة.
* `ProfileEvent_DuplicatedInsertedBlocks` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات Synchronous inserts إلى جدول *MergeTree التي أُزيل تكرارها.
* `ProfileEvent_SelfDuplicatedAsyncInserts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات async insert في الكتلة المُدخلة عبر INSERT إلى جدول ReplicatedMergeTree التي أُزيل تكرارها ذاتيًا.
* `ProfileEvent_DuplicatedAsyncInserts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات async insert في الكتلة المُدخلة عبر INSERT إلى جدول ReplicatedMergeTree التي أُزيل تكرارها.
* `ProfileEvent_DuplicationElapsedMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق للتحقق من تكرار الكتل المُدخلة عبر INSERT إلى جداول *MergeTree.
* `ProfileEvent_ZooKeeperInit` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي أُنشئ فيها connection مع ZooKeeper.
* `ProfileEvent_ZooKeeperTransactions` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات ZooKeeper، وتشمل عمليات القراءة والكتابة، بالإضافة إلى المعاملات المتعددة.
* `ProfileEvent_ZooKeeperList` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات &#39;list&#39; ‏(getChildren) إلى ZooKeeper.
* `ProfileEvent_ZooKeeperListRecursive` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات &#39;listRecursive&#39; إلى ZooKeeper.
* `ProfileEvent_ZooKeeperCreate` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات &#39;create&#39; إلى ZooKeeper.
* `ProfileEvent_ZooKeeperRemove` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات &#39;remove&#39; إلى ZooKeeper.
* `ProfileEvent_ZooKeeperExists` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات &#39;exists&#39; إلى ZooKeeper.
* `ProfileEvent_ZooKeeperGet` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات &#39;get&#39; إلى ZooKeeper.
* `ProfileEvent_ZooKeeperSet` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات &#39;set&#39; إلى ZooKeeper.
* `ProfileEvent_ZooKeeperMulti` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات &#39;multi&#39; إلى ZooKeeper (compound transactions).
* `ProfileEvent_ZooKeeperMultiRead` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات &#39;multi&#39; للقراءة إلى ZooKeeper (compound transactions).
* `ProfileEvent_ZooKeeperMultiWrite` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات &#39;multi&#39; للكتابة إلى ZooKeeper (compound transactions).
* `ProfileEvent_ZooKeeperCheck` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات &#39;check&#39; إلى ZooKeeper. وعادةً لا يكون لها معنى عند النظر إليها بشكل منفصل، وإنما فقط كجزء من transaction معقّدة.
* `ProfileEvent_ZooKeeperSync` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات &#39;sync&#39; إلى ZooKeeper. نادرًا ما تكون هذه الطلبات مطلوبة أو مفيدة.
* `ProfileEvent_ZooKeeperReconfig` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات &#39;reconfig&#39; إلى ZooKeeper.
* `ProfileEvent_ZooKeeperClose` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي أُغلق فيها الاتصال مع ZooKeeper طوعًا.
* `ProfileEvent_ZooKeeperGetACL` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات &#39;getACL&#39; إلى ZooKeeper.
* `ProfileEvent_ZooKeeperWatchResponse` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات تلقي إشعار watch من ZooKeeper.
* `ProfileEvent_ZooKeeperUserExceptions` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الاستثناءات أثناء العمل مع ZooKeeper المرتبطة بالبيانات (مثل عدم وجود عقدة، أو إصدار غير صالح، أو ما شابه ذلك).
* `ProfileEvent_ZooKeeperHardwareExceptions` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الاستثناءات أثناء العمل مع ZooKeeper المرتبطة بالشبكة (مثل فقدان الاتصال أو ما شابه ذلك).
* `ProfileEvent_ZooKeeperOtherExceptions` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الاستثناءات أثناء العمل مع ZooKeeper بخلاف ZooKeeperUserExceptions و ZooKeeperHardwareExceptions.
* `ProfileEvent_ZooKeeperWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الميكروثواني المُستغرقة في انتظار الاستجابات من ZooKeeper بعد إنشاء طلب، مجمّعة عبر جميع سلاسل التنفيذ الطالبة.
* `ProfileEvent_ZooKeeperBytesSent` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المُرسلة عبر الشبكة أثناء التواصل مع ZooKeeper.
* `ProfileEvent_ZooKeeperBytesReceived` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المُستلمة عبر الشبكة أثناء التواصل مع ZooKeeper.
* `ProfileEvent_DistributedConnectionTries` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لمحاولات الاتصال الموزّع.
* `ProfileEvent_DistributedConnectionUsable` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للاتصالات الموزّعة الناجحة إلى خادم قابل للاستخدام (يحتوي على الجدول المطلوب، لكنه قد يكون غير متزامن).
* `ProfileEvent_DistributedConnectionFailTry` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للحالات التي يفشل فيها الاتصال الموزّع مع إعادة المحاولة.
* `ProfileEvent_DistributedConnectionMissingTable` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي رُفضت فيها نسخة متماثلة من استعلام موزّع لأنها لم تحتوِ على جدول مطلوب للاستعلام.
* `ProfileEvent_DistributedConnectionStaleReplica` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي رُفضت فيها نسخة متماثلة من استعلام موزّع لأن أحد الجداول المطلوبة للاستعلام كان لديه تأخر في النسخ المتماثل أعلى من العتبة المُعدّة.
* `ProfileEvent_DistributedConnectionSkipReadOnlyReplica` ([UInt64](/ar/reference/data-types/int-uint)) — عدد النسخ المتماثلة التي تم تخطيها أثناء `INSERT` إلى جدول Distributed بسبب كونها للقراءة فقط
* `ProfileEvent_DistributedConnectionFailAtAll` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للحالات التي يفشل فيها الاتصال الموزّع بعد انتهاء جميع محاولات إعادة المحاولة.
* `ProfileEvent_Shards` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المقاطع المشاركة في الاستعلام، مجمّعًا عبر جميع جداول Distributed ودوال الجدول. يُحتسب المضيف الواحد عدة مرات إذا ظهر في عدة جداول. ويمثل هذا العدد إجمالي العدد المتوقع للمقاطع، بما في ذلك المقاطع المتخطاة باستخدام الإعداد `skip_unavailable_shards`.
* `ProfileEvent_HedgedRequestsChangeReplica` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للحالات التي انتهت فيها مهلة تغيير النسخة المتماثلة في الطلبات المحوّطة.
* `ProfileEvent_SuspendSendingQueryToShard` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للحالات التي عُلّق فيها إرسال استعلام إلى مقطع عندما يكون async&#95;query&#95;sending&#95;for&#95;remote مفعّلًا.
* `ProfileEvent_CompileFunction` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي بدأ فيها تجميع تعليمات LLVM البرمجية المُولَّدة (لإنشاء دالة مدمجة للتعبيرات المعقدة).
* `ProfileEvent_CompiledFunctionExecute` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي نُفِّذت فيها دالة مُجمَّعة.
* `ProfileEvent_CompileExpressionsMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في تجميع التعبيرات إلى تعليمات LLVM البرمجية.
* `ProfileEvent_CompileExpressionsBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المستخدمة في تجميع التعبيرات.
* `ProfileEvent_ExecuteShellCommand` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات تنفيذ أوامر الصدفة.
* `ProfileEvent_ExternalProcessingCompressedBytesTotal` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المضغوطة التي كتبتها المعالجة الخارجية (الفرز/التجميع/الربط)
* `ProfileEvent_ExternalProcessingUncompressedBytesTotal` ([UInt64](/ar/reference/data-types/int-uint)) — كمية البيانات (غير المضغوطة، قبل الضغط) التي كتبتها المعالجة الخارجية (الفرز/التجميع/الربط)
* `ProfileEvent_ExternalProcessingFilesTotal` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الملفات المستخدمة في المعالجة الخارجية (الفرز/التجميع/الربط)
* `ProfileEvent_ExternalSortWritePart` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي كُتب فيها ملف مؤقت إلى القرص لأغراض الفرز في الذاكرة الخارجية.
* `ProfileEvent_ExternalSortMerge` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي دُمجت فيها ملفات مؤقتة لأغراض الفرز في الذاكرة الخارجية.
* `ProfileEvent_ExternalSortCompressedBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المضغوطة المكتوبة لأغراض الفرز في الذاكرة الخارجية.
* `ProfileEvent_ExternalSortUncompressedBytes` ([UInt64](/ar/reference/data-types/int-uint)) — كمية البيانات (غير المضغوطة، قبل الضغط) المكتوبة لأغراض الفرز في الذاكرة الخارجية.
* `ProfileEvent_ExternalAggregationWritePart` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي كُتب فيها ملف مؤقت إلى القرص لأغراض التجميع في الذاكرة الخارجية.
* `ProfileEvent_ExternalAggregationMerge` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي دُمجت فيها ملفات مؤقتة لأغراض التجميع في الذاكرة الخارجية.
* `ProfileEvent_ExternalAggregationCompressedBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المكتوبة إلى القرص لأغراض التجميع في الذاكرة الخارجية.
* `ProfileEvent_ExternalAggregationUncompressedBytes` ([UInt64](/ar/reference/data-types/int-uint)) — كمية البيانات (غير المضغوطة، قبل الضغط) المكتوبة إلى القرص لأغراض التجميع في الذاكرة الخارجية.
* `ProfileEvent_ExternalJoinWritePart` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي كُتب فيها ملف مؤقت إلى القرص لأغراض JOIN في الذاكرة الخارجية.
* `ProfileEvent_ExternalJoinMerge` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي دُمجت فيها ملفات مؤقتة لأغراض JOIN في الذاكرة الخارجية.
* `ProfileEvent_ExternalJoinCompressedBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المضغوطة المكتوبة لأغراض JOIN في الذاكرة الخارجية.
* `ProfileEvent_ExternalJoinUncompressedBytes` ([UInt64](/ar/reference/data-types/int-uint)) — كمية البيانات (غير المضغوطة، قبل الضغط) المكتوبة لأغراض JOIN في الذاكرة الخارجية.
* `ProfileEvent_IcebergPartitionPrunedFiles` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الملفات التي جرى تخطيها أثناء استبعاد الأقسام في Iceberg
* `ProfileEvent_IcebergTrivialCountOptimizationApplied` ([UInt64](/ar/reference/data-types/int-uint)) — طُبّق تحسين العدّ البسيط أثناء القراءة من Iceberg
* `ProfileEvent_IcebergVersionHintUsed` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استخدام version-hint.text.
* `ProfileEvent_IcebergMinMaxIndexPrunedFiles` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الملفات التي جرى تخطيها باستخدام فهرس MinMax في Iceberg
* `ProfileEvent_JoinBuildTableRowCount` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للصفوف في جدول البناء لعملية JOIN.
* `ProfileEvent_JoinProbeTableRowCount` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للصفوف في جدول الفحص لعملية JOIN.
* `ProfileEvent_JoinResultRowCount` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للصفوف في ناتج عملية JOIN.
* `ProfileEvent_JoinNonJoinedTransformBlockCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الكتل الصادرة بواسطة NonJoinedBlocksTransform.
* `ProfileEvent_JoinNonJoinedTransformRowCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الصفوف غير المنضمّة الصادرة بواسطة NonJoinedBlocksTransform.
* `ProfileEvent_JoinDelayedJoinedTransformBlockCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الـ blocks التي أخرجها DelayedJoinedBlocksWorkerTransform.
* `ProfileEvent_JoinDelayedJoinedTransformRowCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الصفوف التي أخرجها DelayedJoinedBlocksWorkerTransform.
* `ProfileEvent_JoinSpillingHashJoinSwitchedToGraceJoin` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي جرى فيها تحويل (Concurrent)HashJoin إلى GraceHashJoin بسبب تجاوز حد الذاكرة في SpillingHashJoin.
* `ProfileEvent_JoinReorderMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في تنفيذ خوارزمية إعادة ترتيب JOIN.
* `ProfileEvent_JoinOptimizeMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في تنفيذ تحسينات خطة JOIN.
* `ProfileEvent_QueryPlanOptimizeMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في تنفيذ تحسينات خطة الاستعلام.
* `ProfileEvent_DeltaLakePartitionPrunedFiles` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الملفات التي تم تخطيها أثناء تقليم partitions في DeltaLake
* `ProfileEvent_DeltaLakeSnapshotInitializations` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي جرى فيها تهيئة snapshot لجدول DeltaLake (بتحميلها من object storage)
* `ProfileEvent_DeltaLakeScannedFiles` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الملفات التي جرى فحصها أثناء scan callbacks في DeltaLake
* `ProfileEvent_SlowRead` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات القراءة البطيئة من ملف. يشير ذلك إلى زيادة الحمل على النظام. تتحكم إعدادات read&#95;backoff&#95;* في thresholds.
* `ProfileEvent_ReadBackoff` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي خُفِّض فيها عدد threads الخاصة بمعالجة الاستعلام بسبب بطء reads.
* `ProfileEvent_ReplicaPartialShutdown` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي يضطر فيها جدول Replicated إلى إلغاء تهيئة حالته بسبب انتهاء session في ZooKeeper. وتُعاد تهيئة الحالة كلما أصبح ZooKeeper متاحًا مرة أخرى.
* `ProfileEvent_IndexAnalysisRounds` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي أُجري فيها index analysis ضمن الاستعلام.
* `ProfileEvent_SelectedParts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد data parts التي تم اختيار القراءة منها من جدول MergeTree.
* `ProfileEvent_SelectedPartsTotal` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي عدد data parts قبل تحديد ما سيُقرأ منها من جدول MergeTree.
* `ProfileEvent_SelectedRanges` ([UInt64](/ar/reference/data-types/int-uint)) — عدد ranges (غير المتجاورة) في جميع data parts التي تم اختيار القراءة منها من جدول MergeTree.
* `ProfileEvent_SelectedMarks` ([UInt64](/ar/reference/data-types/int-uint)) — عدد marks (index granules) التي تم اختيار قراءتها من جدول MergeTree.
* `ProfileEvent_SelectedMarksTotal` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي عدد marks (index granules) قبل تحديد ما سيُقرأ منها من جدول MergeTree.
* `ProfileEvent_SelectedRows` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الصفوف التي جرى SELECTها من جميع الجداول.
* `ProfileEvent_SelectedBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات (غير المضغوطة؛ للأعمدة كما هي مخزنة في الذاكرة) التي جرى SELECTها من جميع الجداول.
* `ProfileEvent_RowsReadByMainReader` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الصفوف المقروءة من جداول MergeTree بواسطة القارئ الرئيسي (بعد خطوة PREWHERE).
* `ProfileEvent_RowsReadByPrewhereReaders` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي عدد الصفوف المقروءة من جداول MergeTree بواسطة قرّاء prewhere.
* `ProfileEvent_LoadedDataParts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد data parts التي حمّلتها جداول MergeTree أثناء التهيئة.
* `ProfileEvent_LoadedDataPartsMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الميكروثواني التي استغرقتها جداول MergeTree في تحميل data parts أثناء التهيئة.
* `ProfileEvent_FilteringMarksWithPrimaryKeyProcessedMarks` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي marks التي تمت معالجتها أثناء تحليل PK.
* `ProfileEvent_FilteringMarksWithPrimaryKeyMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرق في تصفية الأجزاء حسب المفتاح الأساسي.
* `ProfileEvent_FilteringMarksWithSecondaryKeysMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرق في تصفية الأجزاء باستخدام فهارس التخطي.
* `ProfileEvent_DistributedIndexAnalysisMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرق أثناء تحليل الفهرس الموزع
* `ProfileEvent_DistributedIndexAnalysisScheduledReplicas` ([UInt64](/ar/reference/data-types/int-uint)) — عدد النسخ المتماثلة التي جرت جدولة تحليل الفهرس الموزع لها (تُحتسب النسخة المتماثلة المحلية مرة واحدة)
* `ProfileEvent_DistributedIndexAnalysisReplicaUnavailable` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي فشل فيها تحليل الفهرس الموزع على إحدى النسخ المتماثلة دون رجوع احتياطي (فشل أثناء الاتصال)
* `ProfileEvent_DistributedIndexAnalysisReplicaFallback` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي فشل فيها تحليل الفهرس الموزع على إحدى النسخ المتماثلة مع الرجوع احتياطيًا إلى النسخة المتماثلة المحلية
* `ProfileEvent_DistributedIndexAnalysisParts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأجزاء المُرسلة لتحليل الفهرس الموزع
* `ProfileEvent_DistributedIndexAnalysisMissingParts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأجزاء المفقودة أثناء تحليل الفهرس الموزع التي ستُعالَج محليًا
* `ProfileEvent_WaitMarksLoadMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرق في انتظار تحميل العلامات
* `ProfileEvent_BackgroundLoadingMarksTasks` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المهام الخلفية لتحميل العلامات
* `ProfileEvent_MarksTasksFromCache` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي حُمّلت فيها العلامات بشكل متزامن لأنها كانت موجودة بالفعل في ذاكرة التخزين المؤقت.
* `ProfileEvent_LoadingMarksTasksCanceled` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي أُلغيت فيها المهام الخلفية لتحميل العلامات
* `ProfileEvent_LoadedMarksFiles` ([UInt64](/ar/reference/data-types/int-uint)) — عدد ملفات العلامات التي تم تحميلها.
* `ProfileEvent_LoadedMarksCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد العلامات التي تم تحميلها (الإجمالي عبر الأعمدة).
* `ProfileEvent_LoadedMarksMemoryBytes` ([UInt64](/ar/reference/data-types/int-uint)) — حجم تمثيلات العلامات المحمّلة في الذاكرة.
* `ProfileEvent_MarkCacheEvictedBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات التي أُخرجت من ذاكرة التخزين المؤقت للعلامات.
* `ProfileEvent_MarkCacheEvictedMarks` ([UInt64](/ar/reference/data-types/int-uint)) — عدد العلامات التي أُخرجت من ذاكرة التخزين المؤقت للعلامات.
* `ProfileEvent_MarkCacheEvictedFiles` ([UInt64](/ar/reference/data-types/int-uint)) — عدد ملفات العلامات التي أُخرجت من ذاكرة التخزين المؤقت للعلامات.
* `ProfileEvent_LoadedPrimaryIndexFiles` ([UInt64](/ar/reference/data-types/int-uint)) — عدد ملفات الفهرس الأساسي التي تم تحميلها.
* `ProfileEvent_LoadedPrimaryIndexRows` ([UInt64](/ar/reference/data-types/int-uint)) — عدد صفوف المفتاح الأساسي التي تم تحميلها.
* `ProfileEvent_LoadedPrimaryIndexBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد صفوف المفتاح الأساسي التي تم تحميلها.
* `ProfileEvent_Merge` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات الدمج الخلفية التي تم تشغيلها.
* `ProfileEvent_MergeSourceParts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأجزاء المصدر التي جرت جدولتها لعمليات الدمج.
* `ProfileEvent_MergedRows` ([UInt64](/ar/reference/data-types/int-uint)) — الصفوف المقروءة لعمليات الدمج الخلفية. هذا هو عدد الصفوف قبل الدمج.
* `ProfileEvent_MergedColumns` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأعمدة التي دُمجت أثناء المرحلة الأفقية من عمليات الدمج.
* `ProfileEvent_GatheredColumns` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأعمدة التي جُمعت أثناء المرحلة الرأسية من عمليات الدمج.
* `ProfileEvent_MergedProjections` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الإسقاطات التي دُمجت (ولم يُعَد بناؤها) أثناء عمليات دمج MergeTree.
* `ProfileEvent_RebuiltProjections` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الإسقاطات التي أُعيد بناؤها من الصفر أثناء عمليات دمج MergeTree.
* `ProfileEvent_MergedUncompressedBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات غير المضغوطة (للأعمدة كما هي مخزنة في الذاكرة) التي قُرئت لعمليات الدمج في الخلفية. وهذا هو العدد قبل الدمج.
* `ProfileEvent_MergeWrittenRows` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الصفوف التي كُتبت أثناء الدمج.
* `ProfileEvent_MergeTotalMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في عمليات الدمج في الخلفية
* `ProfileEvent_MergeExecuteMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي وقت التشغيل الفعلي المستغرَق في تنفيذ عمليات الدمج في الخلفية
* `ProfileEvent_MergeCommitMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في تثبيت نتائج الدمج (إعادة تسمية الأجزاء، والتحقق من قيمة checksum، وتحديثات ZooKeeper)
* `ProfileEvent_MergeHorizontalStageTotalMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في المرحلة الأفقية من عمليات الدمج في الخلفية
* `ProfileEvent_MergeHorizontalStageExecuteMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي وقت التشغيل الفعلي المستغرَق في تنفيذ المرحلة الأفقية من عمليات الدمج في الخلفية
* `ProfileEvent_MergeVerticalStageTotalMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في المرحلة الرأسية من عمليات الدمج في الخلفية
* `ProfileEvent_MergeVerticalStageExecuteMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي وقت التشغيل الفعلي المستغرَق في تنفيذ المرحلة الرأسية من عمليات الدمج في الخلفية
* `ProfileEvent_MergeTextIndexStageTotalMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في مرحلة فهرس النص من عمليات الدمج في الخلفية
* `ProfileEvent_MergeTextIndexStageExecuteMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي وقت التشغيل الفعلي المستغرَق في تنفيذ مرحلة فهرس النص من عمليات الدمج في الخلفية
* `ProfileEvent_MergeProjectionStageTotalMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في مرحلة الإسقاطات من عمليات الدمج في الخلفية
* `ProfileEvent_MergeProjectionStageExecuteMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي وقت التشغيل الفعلي المستغرَق في تنفيذ مرحلة الإسقاطات من عمليات الدمج في الخلفية
* `ProfileEvent_MergePrewarmStageTotalMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في مرحلة التهيئة المسبقة من عمليات الدمج في الخلفية
* `ProfileEvent_MergePrewarmStageExecuteMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي وقت التشغيل الفعلي المستغرَق في تنفيذ مرحلة التهيئة المسبقة من عمليات الدمج في الخلفية
* `ProfileEvent_MergesRejectedByMemoryLimit` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات الدمج في الخلفية التي رُفضت بسبب حد الذاكرة
* `ProfileEvent_MergingSortedMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق أثناء دمج الأعمدة المرتبة
* `ProfileEvent_AggregatingSortedMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق أثناء تجميع الأعمدة المرتبة
* `ProfileEvent_CoalescingSortedMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق أثناء دمج الأعمدة المرتبة معًا
* `ProfileEvent_CollapsingSortedMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق أثناء طيّ الأعمدة المرتبة
* `ProfileEvent_ReplacingSortedMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق أثناء استبدال الأعمدة المرتبة
* `ProfileEvent_SummingSortedMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق أثناء جمع الأعمدة المرتبة
* `ProfileEvent_VersionedCollapsingSortedMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق أثناء الطيّ بالإصدارات للأعمدة المرتّبة
* `ProfileEvent_GatheringColumnMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق أثناء جمع الأعمدة من أجل vertical merge
* `ProfileEvent_MutationTotalParts` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي عدد الأجزاء التي جرت محاولة تطبيق mutations عليها
* `ProfileEvent_MutationUntouchedParts` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي عدد الأجزاء التي جرت محاولة تطبيق mutations عليها، ولكن تم تخطيها بالكامل وفقًا لـ predicate
* `ProfileEvent_MutationCreatedEmptyParts` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي عدد الأجزاء التي استُبدلت بأجزاء فارغة بدلًا من تنفيذ mutation
* `ProfileEvent_MutatedRows` ([UInt64](/ar/reference/data-types/int-uint)) — الصفوف المقروءة من أجل mutations. هذا هو عدد الصفوف قبل mutation
* `ProfileEvent_MutatedUncompressedBytes` ([UInt64](/ar/reference/data-types/int-uint)) — البايتات غير المضغوطة (للأعمدة كما هي مخزّنة في الذاكرة) التي قُرئت من أجل mutations. وهذا هو العدد قبل mutation.
* `ProfileEvent_MutationAffectedRowsUpperBound` ([UInt64](/ar/reference/data-types/int-uint)) — الحد الأعلى لعدد الصفوف التي تأثرت بـ mutation (مثل عدد الصفوف التي تستوفي predicate الخاصة بـ UPDATE أو Delete mutation). وقد يكون العدد الفعلي أقل بقليل
* `ProfileEvent_MutationTotalMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في mutations.
* `ProfileEvent_MutationExecuteMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي busy time المستغرَق في تنفيذ mutations.
* `ProfileEvent_MutationCommitMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في commit نتائج mutation (إعادة تسمية part، والتحقق من checksum، وتحديثات ZooKeeper)
* `ProfileEvent_MutationAllPartColumns` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي أُنشئت فيها task لتطبيق mutation على جميع الأعمدة في part
* `ProfileEvent_MutationSomePartColumns` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي أُنشئت فيها task لتطبيق mutation على بعض الأعمدة في part
* `ProfileEvent_MutateTaskProjectionsCalculationMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في حساب projections ضمن mutations
* `ProfileEvent_MergeTreeDataWriterRows` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الصفوف التي تم INSERTها إلى MergeTree tables.
* `ProfileEvent_MergeTreeDataWriterUncompressedBytes` ([UInt64](/ar/reference/data-types/int-uint)) — البايتات غير المضغوطة (للأعمدة كما هي مخزّنة في الذاكرة) التي تم INSERTها إلى MergeTree tables.
* `ProfileEvent_MergeTreeDataWriterCompressedBytes` ([UInt64](/ar/reference/data-types/int-uint)) — البايتات المكتوبة إلى filesystem للبيانات التي تم INSERTها إلى MergeTree tables.
* `ProfileEvent_MergeTreeDataWriterBlocks` ([UInt64](/ar/reference/data-types/int-uint)) — عدد blocks التي تم INSERTها إلى MergeTree tables. يشكّل كل block data part من المستوى صفر.
* `ProfileEvent_MergeTreeDataWriterBlocksAlreadySorted` ([UInt64](/ar/reference/data-types/int-uint)) — عدد blocks التي تم INSERTها إلى MergeTree tables واتضح أنها كانت مرتّبة بالفعل.
* `ProfileEvent_MergeMutateBackgroundExecutorTaskExecuteStepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في executeStep() لمهام MergeMutate executor.
* `ProfileEvent_MergeMutateBackgroundExecutorTaskCancelMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في cancel() لمهام MergeMutate executor.
* `ProfileEvent_MergeMutateBackgroundExecutorTaskResetMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في إعادة ضبط task الخاصة بـ MergeMutate executor.
* `ProfileEvent_MergeMutateBackgroundExecutorWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في انتظار الاكتمال ضمن MergeMutate executor.
* `ProfileEvent_MoveBackgroundExecutorTaskExecuteStepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في executeStep() لمهام Move executor.
* `ProfileEvent_MoveBackgroundExecutorTaskCancelMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في cancel() لمهام Move executor.
* `ProfileEvent_MoveBackgroundExecutorTaskResetMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في إعادة تعيين المهمة لمُنفِّذ Move.
* `ProfileEvent_MoveBackgroundExecutorWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في انتظار الاكتمال في مُنفِّذ Move.
* `ProfileEvent_FetchBackgroundExecutorTaskExecuteStepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في executeStep() لمهام مُنفِّذ Fetch.
* `ProfileEvent_FetchBackgroundExecutorTaskCancelMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في cancel() لمهام مُنفِّذ Fetch.
* `ProfileEvent_FetchBackgroundExecutorTaskResetMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في إعادة تعيين المهمة لمُنفِّذ Fetch.
* `ProfileEvent_FetchBackgroundExecutorWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في انتظار الاكتمال في مُنفِّذ Fetch.
* `ProfileEvent_CommonBackgroundExecutorTaskExecuteStepMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في executeStep() لمهام مُنفِّذ Common.
* `ProfileEvent_CommonBackgroundExecutorTaskCancelMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في cancel() لمهام مُنفِّذ Common.
* `ProfileEvent_CommonBackgroundExecutorTaskResetMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في إعادة تعيين المهمة لمُنفِّذ Common.
* `ProfileEvent_CommonBackgroundExecutorWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في انتظار الاكتمال في مُنفِّذ Common.
* `ProfileEvent_MergeTreeDataWriterSkipIndicesCalculationMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في حساب فهارس التخطي
* `ProfileEvent_MergeTreeDataWriterStatisticsCalculationMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في حساب الإحصاءات
* `ProfileEvent_MergeTreeDataWriterSortingBlocksMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في فرز الكتل
* `ProfileEvent_MergeTreeDataWriterMergingBlocksMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في دمج كتل الإدخال (لمحركات MergeTree الخاصة)
* `ProfileEvent_MergeTreeDataWriterProjectionsCalculationMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في حساب الإسقاطات
* `ProfileEvent_MergeTreeDataProjectionWriterSortingBlocksMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في فرز الكتل (بالنسبة إلى الإسقاط، قد يكون المفتاح مختلفًا عن مفتاح فرز الجدول)
* `ProfileEvent_MergeTreeDataProjectionWriterMergingBlocksMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في دمج الكتل
* `ProfileEvent_InsertedWideParts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأجزاء المُدرجة بتنسيق Wide.
* `ProfileEvent_InsertedCompactParts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأجزاء المُدرجة بتنسيق Compact.
* `ProfileEvent_MergedIntoWideParts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأجزاء المدمجة إلى تنسيق Wide.
* `ProfileEvent_MergedIntoCompactParts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأجزاء المدمجة إلى تنسيق Compact.
* `ProfileEvent_MergeTreeDataProjectionWriterRows` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الصفوف التي جرى INSERTها إلى إسقاط جداول MergeTree.
* `ProfileEvent_MergeTreeDataProjectionWriterUncompressedBytes` ([UInt64](/ar/reference/data-types/int-uint)) — البايتات غير المضغوطة (للأعمدة كما هي مخزنة في الذاكرة) التي جرى INSERTها إلى إسقاط جداول MergeTree.
* `ProfileEvent_MergeTreeDataProjectionWriterCompressedBytes` ([UInt64](/ar/reference/data-types/int-uint)) — البايتات المكتوبة إلى نظام الملفات للبيانات التي جرى INSERTها إلى إسقاط جداول MergeTree.
* `ProfileEvent_MergeTreeDataProjectionWriterBlocks` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الكتل التي جرى INSERTها إلى إسقاط جداول MergeTree. تشكّل كل كتلة data part من المستوى صفر.
* `ProfileEvent_MergeTreeDataProjectionWriterBlocksAlreadySorted` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الكتل التي أُدرجت بأمر INSERT في إسقاطات جداول MergeTree وبدا أنها مرتبة مسبقًا.
* `ProfileEvent_CannotRemoveEphemeralNode` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي حدث فيها خطأ أثناء محاولة إزالة عقدة مؤقتة. لا يُعد هذا مشكلة، لأن تنفيذنا لمكتبة ZooKeeper يضمن انتهاء صلاحية الجلسة وإزالة العقدة.
* `ProfileEvent_RegexpWithMultipleNeedlesCreated` ([UInt64](/ar/reference/data-types/int-uint)) — عدد التعبيرات النمطية المُجمَّعة التي تحتوي على multiple needles (مكتبة VectorScan).
* `ProfileEvent_RegexpWithMultipleNeedlesGlobalCacheHit` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي جلبنا فيها تعبيرًا نمطيًا مُجمَّعًا يحتوي على multiple needles (مكتبة VectorScan) من الذاكرة المؤقتة العامة.
* `ProfileEvent_RegexpWithMultipleNeedlesGlobalCacheMiss` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي فشلنا فيها في جلب تعبير نمطي مُجمَّع يحتوي على multiple needles (مكتبة VectorScan) من الذاكرة المؤقتة العامة.
* `ProfileEvent_RegexpLocalCacheHit` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي جلبنا فيها تعبيرًا نمطيًا مُجمَّعًا من ذاكرة مؤقتة محلية.
* `ProfileEvent_RegexpLocalCacheMiss` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي فشلنا فيها في جلب تعبير نمطي مُجمَّع من ذاكرة مؤقتة محلية.
* `ProfileEvent_ContextLock` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي تم فيها الحصول على قفل Context أو محاولة الحصول عليه. هذا قفل عام.
* `ProfileEvent_ContextLockWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — وقت انتظار قفل Context بالميكروثانية
* `ProfileEvent_StorageBufferFlush` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي تم فيها تفريغ مخزن مؤقت في جدول &#39;Buffer&#39;.
* `ProfileEvent_StorageBufferErrorOnFlush` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي تعذر فيها تفريغ مخزن مؤقت في جدول &#39;Buffer&#39; بسبب خطأ عند الكتابة إلى destination table.
* `ProfileEvent_StorageBufferPassedAllMinThresholds` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي استوفت فيها معايير thresholds الدنيا لتفريغ مخزن مؤقت في جدول &#39;Buffer&#39;.
* `ProfileEvent_StorageBufferPassedTimeMaxThreshold` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي تم فيها بلوغ threshold الحد الأقصى للوقت لتفريغ مخزن مؤقت في جدول &#39;Buffer&#39;.
* `ProfileEvent_StorageBufferPassedRowsMaxThreshold` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي تم فيها بلوغ threshold الحد الأقصى للصفوف لتفريغ مخزن مؤقت في جدول &#39;Buffer&#39;.
* `ProfileEvent_StorageBufferPassedBytesMaxThreshold` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي تم فيها بلوغ threshold الحد الأقصى للبايتات لتفريغ مخزن مؤقت في جدول &#39;Buffer&#39;.
* `ProfileEvent_StorageBufferPassedTimeFlushThreshold` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي تم فيها بلوغ flush threshold الزمني الخاص بعمليات Background فقط لتفريغ مخزن مؤقت في جدول &#39;Buffer&#39;. هذا metric مخصص للخبراء فقط. إذا كنت تقرأ هذا ولست خبيرًا، فتوقف عن القراءة.
* `ProfileEvent_StorageBufferPassedRowsFlushThreshold` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي تم فيها بلوغ flush threshold الخاص بالصفوف لعمليات Background فقط لتفريغ مخزن مؤقت في جدول &#39;Buffer&#39;. هذا metric مخصص للخبراء فقط. إذا كنت تقرأ هذا ولست خبيرًا، فتوقف عن القراءة.
* `ProfileEvent_StorageBufferPassedBytesFlushThreshold` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي تم فيها بلوغ flush threshold الخاص بالبايتات لعمليات Background فقط لتفريغ مخزن مؤقت في جدول &#39;Buffer&#39;. هذا metric مخصص للخبراء فقط. إذا كنت تقرأ هذا ولست خبيرًا، فتوقف عن القراءة.
* `ProfileEvent_StorageBufferLayerLockReadersWaitMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — وقت انتظار Buffer layer أثناء القراءة.
* `ProfileEvent_StorageBufferLayerLockWritersWaitMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — وقت انتظار توفر Buffer layer للكتابة إليها (يمكن استخدامه لضبط Buffer layers).
* `ProfileEvent_SystemLogErrorOnFlush` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي فشل فيها أيٌّ من سجلات النظام في التفريغ إلى جدول النظام المقابل. وتُعاد محاولات التفريغ.
* `ProfileEvent_DictCacheKeysRequested` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المفاتيح المطلوبة من مصدر البيانات للقواميس من النوع &#39;cache&#39;.
* `ProfileEvent_DictCacheKeysRequestedMiss` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المفاتيح المطلوبة من مصدر البيانات للقواميس من النوع &#39;cache&#39;، ولكن لم يُعثر عليها في مصدر البيانات.
* `ProfileEvent_DictCacheKeysRequestedFound` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المفاتيح المطلوبة من مصدر البيانات للقواميس من النوع &#39;cache&#39;، والتي عُثر عليها في مصدر البيانات.
* `ProfileEvent_DictCacheKeysExpired` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المفاتيح التي جرى البحث عنها في القواميس من النوع &#39;cache&#39;، وعُثر عليها في ذاكرة التخزين المؤقت، لكنها كانت منتهية الصلاحية.
* `ProfileEvent_DictCacheKeysNotFound` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المفاتيح التي جرى البحث عنها في القواميس من النوع &#39;cache&#39; ولم يُعثر عليها.
* `ProfileEvent_DictCacheKeysHit` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المفاتيح التي جرى البحث عنها في القواميس من النوع &#39;cache&#39; وعُثر عليها في ذاكرة التخزين المؤقت.
* `ProfileEvent_DictCacheRequestTimeNs` ([UInt64](/ar/reference/data-types/int-uint)) — عدد النانوثواني المستغرقة في الاستعلام عن مصادر البيانات الخارجية للقواميس من النوع &#39;cache&#39;.
* `ProfileEvent_DictCacheRequests` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الطلبات المجمّعة إلى مصادر البيانات الخارجية للقواميس من النوع &#39;cache&#39;.
* `ProfileEvent_DictCacheLockWriteNs` ([UInt64](/ar/reference/data-types/int-uint)) — عدد النانوثواني المستغرقة في انتظار قفل الكتابة لتحديث بيانات القواميس من النوع &#39;cache&#39;.
* `ProfileEvent_DictCacheLockReadNs` ([UInt64](/ar/reference/data-types/int-uint)) — عدد النانوثواني المستغرقة في انتظار قفل القراءة للبحث في بيانات القواميس من النوع &#39;cache&#39;.
* `ProfileEvent_DistributedSyncInsertionTimeoutExceeded` ([UInt64](/ar/reference/data-types/int-uint)) — تم تجاوز المهلة أثناء انتظار الشظايا خلال الإدراج المتزامن في جدول Distributed (مع &#39;distributed&#95;foreground&#95;insert&#39; = 1)
* `ProfileEvent_DistributedAsyncInsertionFailures` ([UInt64](/ar/reference/data-types/int-uint)) — عدد حالات الفشل في الإدراج غير المتزامن في جدول Distributed (مع &#39;distributed&#95;foreground&#95;insert&#39; = 0)
* `ProfileEvent_DataAfterMergeDiffersFromReplica` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي لا تكون فيها البيانات بعد الدمج مطابقة تمامًا على مستوى البايتات للبيانات الموجودة على النسخ المتماثلة الأخرى. وقد تكون هناك عدة أسباب لذلك:
  1. استخدام إصدار أحدث من مكتبة الضغط بعد تحديث الخادم.
  2. استخدام أسلوب ضغط آخر.
  3. خوارزمية ضغط غير حتمية (وهو أمر غير مرجح جدًا).
  4. خوارزمية دمج غير حتمية بسبب خطأ منطقي في الشيفرة.
  5. تلف البيانات في الذاكرة بسبب خلل في الشيفرة.
  6. تلف البيانات في الذاكرة بسبب مشكلة في العتاد.
  7. تعديل البيانات الأصلية يدويًا بعد بدء تشغيل الخادم.
  8. تعديل قيم التحقق المخزنة في ZooKeeper يدويًا.
  9. تختلف الإعدادات المرتبطة بتنسيق الأجزاء، مثل &#39;enable&#95;mixed&#95;granularity&#95;parts&#39;، بين النسخ المتماثلة المختلفة.
     اكتشف الخادم هذه الحالة بنجاح، وسيقوم بتنزيل الجزء المدمج من النسخة المتماثلة لفرض نتيجة متطابقة على مستوى البايت.
* `ProfileEvent_DataAfterMutationDiffersFromReplica` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي لا تكون فيها البيانات بعد الـ mutation مطابقة على مستوى البايتات للبيانات الموجودة على النسخ المتماثلة الأخرى. بالإضافة إلى الأسباب الموضحة في &#39;DataAfterMergeDiffersFromReplica&#39;، قد يحدث هذا أيضًا بسبب mutation غير حتمية.
* `ProfileEvent_NaiveBayesClassifierModelsLoaded` ([UInt64](/ar/reference/data-types/int-uint)) — عدد نماذج مصنف Naive Bayes التي جرى تحميلها.
* `ProfileEvent_NaiveBayesClassifierModelsAllocatedBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المخصصة لنماذج مصنف Naive Bayes.
* `ProfileEvent_USearchAddCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المتجهات التي أُضيفت إلى indexes الخاصة بـ usearch.
* `ProfileEvent_USearchAddVisitedMembers` ([UInt64](/ar/reference/data-types/int-uint)) — عدد العُقد التي جرت زيارتها عند إضافة المتجهات إلى indexes الخاصة بـ usearch.
* `ProfileEvent_USearchAddComputedDistances` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات حساب المسافة عند إضافة المتجهات إلى indexes الخاصة بـ usearch.
* `ProfileEvent_USearchSearchCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات البحث المنفذة في indexes الخاصة بـ usearch.
* `ProfileEvent_USearchSearchVisitedMembers` ([UInt64](/ar/reference/data-types/int-uint)) — عدد العُقد التي جرت زيارتها عند البحث في indexes الخاصة بـ usearch.
* `ProfileEvent_USearchSearchComputedDistances` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات حساب المسافة عند البحث في indexes الخاصة بـ usearch.
* `ProfileEvent_RWLockAcquiredReadLocks` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات الحصول على قفل قراءة (في heavy RWLock).
* `ProfileEvent_RWLockAcquiredWriteLocks` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات الحصول على قفل كتابة (في heavy RWLock).
* `ProfileEvent_RWLockReadersWaitMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في انتظار الحصول على قفل قراءة (في heavy RWLock).
* `ProfileEvent_RWLockWritersWaitMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في انتظار الحصول على قفل كتابة (في heavy RWLock).
* `ProfileEvent_DNSError` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للأخطاء في تحليل DNS
* `ProfileEvent_PartsLockHoldMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في الاحتفاظ بقفل أجزاء البيانات في جداول MergeTree
* `ProfileEvent_PartsLockWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في انتظار قفل أجزاء البيانات في جداول MergeTree
* `ProfileEvent_PartsLocks` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات الحصول على قفل أجزاء البيانات في جداول MergeTree
* `ProfileEvent_SharedPartsLockHoldMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في الاحتفاظ بقفل أجزاء البيانات المشتركة في جداول MergeTree
* `ProfileEvent_SharedPartsLockWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في انتظار قفل أجزاء البيانات المشتركة في جداول MergeTree
* `ProfileEvent_SharedPartsLocks` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات الحصول على قفل أجزاء البيانات المشتركة في جداول MergeTree
* `ProfileEvent_RealTimeMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت (وقت الساعة الفعلي) المستغرَق في خيوط المعالجة (الاستعلامات والمهام الأخرى) (لاحظ أن هذا مجموع).
* `ProfileEvent_UserTimeMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في خيوط المعالجة (الاستعلامات والمهام الأخرى) التي تنفذ تعليمات CPU في وضع المستخدم. ويشمل ذلك الوقت الذي تعطلت فيه pipeline الخاصة بـ CPU بسبب الوصول إلى الذاكرة الرئيسية، وحالات cache misses، وأخطاء التنبؤ بالتفرعات، وhyper-threading، وما إلى ذلك.
* `ProfileEvent_SystemTimeMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضته سلاسل التنفيذ الخاصة بالمعالجة (الاستعلامات والمهام الأخرى) في تنفيذ تعليمات CPU في وضع نواة نظام التشغيل. وهذا هو الوقت المستغرَق في استدعاءات النظام، باستثناء وقت الانتظار أثناء استدعاءات النظام الحاجبة.
* `ProfileEvent_MemoryOvercommitWaitTimeMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في انتظار تحرير الذاكرة في OvercommitTracker.
* `ProfileEvent_MemoryAllocatorPurge` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي عدد مرات طلب purge لمخصّص الذاكرة
* `ProfileEvent_MemoryAllocatorPurgeTimeMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في purge لمخصّص الذاكرة
* `ProfileEvent_SoftPageFaults` ([UInt64](/ar/reference/data-types/int-uint)) — عدد أخطاء الصفحات اللينة في سلاسل تنفيذ الاستعلامات. ويعني خطأ الصفحة اللين عادةً حدوث إخفاق في cache مخصّص الذاكرة، مما يتطلب تعيينًا جديدًا للذاكرة من نظام التشغيل ثم تخصيص صفحة من الذاكرة الفعلية.
* `ProfileEvent_HardPageFaults` ([UInt64](/ar/reference/data-types/int-uint)) — عدد أخطاء الصفحات الصلبة في سلاسل تنفيذ الاستعلامات. وتشير القيم المرتفعة إما إلى أنك نسيت تعطيل swap على الخادم، أو إلى eviction لصفحات ذاكرة binary الخاص بـ ClickHouse أثناء ضغط ذاكرة مرتفع جدًا، أو إلى استخدام ناجح لأسلوب القراءة &#39;mmap&#39; لبيانات الجداول.
* `ProfileEvent_OSIOWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضاه thread في انتظار نتيجة عملية IO، من منظور نظام التشغيل. هذا IO فعلي ولا يتضمن page cache.
* `ProfileEvent_OSCPUWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي كان فيه thread جاهزًا للتنفيذ لكنه كان ينتظر أن يجدوله نظام التشغيل، من منظور نظام التشغيل.
* `ProfileEvent_OSCPUVirtualTimeMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — وقت CPU المستغرَق كما يراه نظام التشغيل. لا يتضمن أوقات الانتظار غير الطوعية بسبب البيئة الافتراضية.
* `ProfileEvent_OSReadBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المقروءة من disks أو block devices. لا يتضمن البايتات المقروءة من page cache. وقد يتضمن بيانات زائدة بسبب حجم block وعمليات readahead وما إلى ذلك.
* `ProfileEvent_OSWriteBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المكتوبة إلى disks أو block devices. لا يتضمن البايتات الموجودة في dirty pages الخاصة بـ page cache. وقد لا يتضمن البيانات التي كتبها نظام التشغيل بشكل Asynchronous.
* `ProfileEvent_OSReadChars` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المقروءة من filesystem، بما في ذلك page cache، وكذلك من الشبكة والملفات الأخرى.
* `ProfileEvent_OSWriteChars` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المكتوبة إلى filesystem، بما في ذلك page cache، وكذلك إلى الشبكة والملفات الأخرى.
* `ProfileEvent_ParallelReplicasHandleRequestMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في معالجة requests الخاصة بـ marks من replicas
* `ProfileEvent_ParallelReplicasHandleAnnouncementMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في معالجة announcements الخاصة بـ replicas
* `ProfileEvent_ParallelReplicasAnnouncementMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في إرسال announcement
* `ProfileEvent_ParallelReplicasReadRequestMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في read requests
* `ProfileEvent_ParallelReplicasReadAssignedMarks` ([UInt64](/ar/reference/data-types/int-uint)) — المجموع عبر جميع replicas لعدد scheduled marks التي أُسنِدت بواسطة consistent hash
* `ProfileEvent_ParallelReplicasReadUnassignedMarks` ([UInt64](/ar/reference/data-types/int-uint)) — المجموع عبر جميع replicas لعدد marks غير المعيّنة التي تمت جدولتها
* `ProfileEvent_ParallelReplicasReadAssignedForStealingMarks` ([UInt64](/ar/reference/data-types/int-uint)) — المجموع عبر جميع replicas لعدد scheduled marks التي أُسنِدت للـ stealing بواسطة consistent hash
* `ProfileEvent_ParallelReplicasReadMarks` ([UInt64](/ar/reference/data-types/int-uint)) — عدد marks التي قرأها الـ replica المحدد
* `ProfileEvent_ParallelReplicasStealingByHashMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في جمع segments المخصّصة للـ stealing حسب hash
* `ProfileEvent_ParallelReplicasProcessingPartsMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في معالجة data parts
* `ProfileEvent_ParallelReplicasStealingLeftoversMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في جمع المقاطع المتروكة
* `ProfileEvent_ParallelReplicasCollectingOwnedSegmentsMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في جمع المقاطع المخصّصة وفقًا لـ `hash`
* `ProfileEvent_ParallelReplicasNumRequests` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الطلبات المرسلة إلى العقدة البادئة.
* `ProfileEvent_ParallelReplicasDeniedRequests` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الطلبات التي رُفضت بالكامل من قِبل العقدة البادئة
* `ProfileEvent_CacheWarmerBytesDownloaded` ([UInt64](/ar/reference/data-types/int-uint)) — حجم البيانات التي جرى جلبها إلى ذاكرة التخزين المؤقت لنظام الملفات بواسطة خيوط خلفية مخصّصة.
* `ProfileEvent_CacheWarmerDataPartsDownloaded` ([UInt64](/ar/reference/data-types/int-uint)) — عدد أجزاء البيانات التي جرى جلبها بالكامل بواسطة CacheWarmer.
* `ProfileEvent_IgnoredColdParts` ([UInt64](/ar/reference/data-types/int-uint)) — راجع الإعداد ignore&#95;cold&#95;parts&#95;seconds. عدد المرات التي تجاهلت فيها استعلامات القراءة الأجزاء الحديثة جدًا التي لم يكن CacheWarmer قد جلبها إلى ذاكرة التخزين المؤقت بعد.
* `ProfileEvent_PreferredWarmedUnmergedParts` ([UInt64](/ar/reference/data-types/int-uint)) — راجع الإعداد prefer&#95;warmed&#95;unmerged&#95;parts&#95;seconds. عدد المرات التي استخدمت فيها استعلامات القراءة أجزاء قديمة قبل الدمج موجودة في ذاكرة التخزين المؤقت بدلًا من الجزء المدمج الذي لم يكن CacheWarmer قد جلبه إلى ذاكرة التخزين المؤقت بعد.
* `ProfileEvent_PerfCPUCycles` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الدورات. انتبه إلى ما يحدث أثناء تغيّر تردد CPU.
* `ProfileEvent_PerfInstructions` ([UInt64](/ar/reference/data-types/int-uint)) — التعليمات المنجزة. انتبه، فقد تتأثر هذه بعوامل متعددة، وأبرزها عدد مقاطعات العتاد.
* `ProfileEvent_PerfCacheReferences` ([UInt64](/ar/reference/data-types/int-uint)) — عمليات الوصول إلى ذاكرة التخزين المؤقت. يشير هذا عادةً إلى عمليات الوصول إلى ذاكرة التخزين المؤقت من المستوى الأخير، لكن ذلك قد يختلف بحسب CPU لديك. وقد يشمل هذا عمليات `prefetch` ورسائل الاتساق؛ ومرة أخرى يعتمد ذلك على تصميم CPU لديك.
* `ProfileEvent_PerfCacheMisses` ([UInt64](/ar/reference/data-types/int-uint)) — إخفاقات ذاكرة التخزين المؤقت. يشير هذا عادةً إلى إخفاقات ذاكرة التخزين المؤقت من المستوى الأخير؛ والمقصود استخدامه بالاقتران مع الحدث PERFCOUNTHWCACHEREFERENCES لحساب معدلات الإخفاق في ذاكرة التخزين المؤقت.
* `ProfileEvent_PerfBranchInstructions` ([UInt64](/ar/reference/data-types/int-uint)) — تعليمات التفرّع المنجزة. قبل Linux 2.6.35، كان هذا يستخدم الحدث الخاطئ على معالجات AMD.
* `ProfileEvent_PerfBranchMisses` ([UInt64](/ar/reference/data-types/int-uint)) — تعليمات التفرّع التي أُخطئ في التنبؤ بها.
* `ProfileEvent_PerfBusCycles` ([UInt64](/ar/reference/data-types/int-uint)) — دورات الناقل، وقد تختلف عن إجمالي الدورات.
* `ProfileEvent_PerfStalledCyclesFrontend` ([UInt64](/ar/reference/data-types/int-uint)) — الدورات المتوقفة أثناء الإصدار.
* `ProfileEvent_PerfStalledCyclesBackend` ([UInt64](/ar/reference/data-types/int-uint)) — الدورات المتوقفة أثناء الإنهاء.
* `ProfileEvent_PerfRefCPUCycles` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الدورات؛ لا يتأثر بتغيّر تردد CPU.
* `ProfileEvent_PerfCPUClock` ([UInt64](/ar/reference/data-types/int-uint)) — ساعة CPU، وهي مؤقّت عالي الدقة لكل CPU
* `ProfileEvent_PerfTaskClock` ([UInt64](/ar/reference/data-types/int-uint)) — عدد نبضات ساعة خاص بالمهمة قيد التشغيل
* `ProfileEvent_PerfContextSwitches` ([UInt64](/ar/reference/data-types/int-uint)) — عدد تبديلات السياق
* `ProfileEvent_PerfCPUMigrations` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات انتقال العملية إلى CPU جديد
* `ProfileEvent_PerfAlignmentFaults` ([UInt64](/ar/reference/data-types/int-uint)) — عدد أخطاء المحاذاة. تحدث هذه عند وقوع عمليات وصول غير مصطفّة إلى الذاكرة؛ ويمكن للنواة التعامل معها، لكنها تقلل الأداء. ويحدث هذا فقط في بعض المعماريات (ولا يحدث أبدًا على x86).
* `ProfileEvent_PerfEmulationFaults` ([UInt64](/ar/reference/data-types/int-uint)) — عدد أخطاء المحاكاة. أحيانًا تعترض النواة تعليمات غير مدعومة وتقوم بمحاكاتها في حيز المستخدم. وقد يؤثر هذا سلبًا في الأداء.
* `ProfileEvent_PerfMinEnabledTime` ([UInt64](/ar/reference/data-types/int-uint)) — بالنسبة إلى جميع الأحداث، الحد الأدنى للوقت الذي كان فيه الحدث مفعّلًا. يُستخدم لتتبّع تأثير تعدد إرسال الأحداث
* `ProfileEvent_PerfMinEnabledRunningTime` ([UInt64](/ar/reference/data-types/int-uint)) — وقت تشغيل الحدث ذي أقل وقت تفعيل. يُستخدم لتتبّع مقدار تعدد إرسال الأحداث
* `ProfileEvent_PerfDataTLBReferences` ([UInt64](/ar/reference/data-types/int-uint)) — مراجع Data TLB
* `ProfileEvent_PerfDataTLBMisses` ([UInt64](/ar/reference/data-types/int-uint)) — حالات إخفاق Data TLB
* `ProfileEvent_PerfInstructionTLBReferences` ([UInt64](/ar/reference/data-types/int-uint)) — مراجع Instruction TLB
* `ProfileEvent_PerfInstructionTLBMisses` ([UInt64](/ar/reference/data-types/int-uint)) — حالات إخفاق Instruction TLB
* `ProfileEvent_PerfLocalMemoryReferences` ([UInt64](/ar/reference/data-types/int-uint)) — قراءات الذاكرة المحلية لعقدة NUMA
* `ProfileEvent_PerfLocalMemoryMisses` ([UInt64](/ar/reference/data-types/int-uint)) — حالات إخفاق قراءة الذاكرة المحلية لعقدة NUMA
* `ProfileEvent_CannotWriteToWriteBufferDiscard` ([UInt64](/ar/reference/data-types/int-uint)) — عدد تتبعات المكدس التي أسقطها query profiler أو معالج الإشارة لأن القناة ممتلئة أو يتعذر الكتابة إليها.
* `ProfileEvent_QueryProfilerSignalOverruns` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي أُسقطت فيها معالجة إشارة query profiler بسبب التجاوز، بالإضافة إلى عدد الإشارات التي لم يسلّمها نظام التشغيل بسبب التجاوز.
* `ProfileEvent_QueryProfilerConcurrencyOverruns` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي أُسقطت فيها معالجة إشارة query profiler بسبب وجود عدد كبير جدًا من query profiler المتزامنة في سلاسل تنفيذ أخرى، ما قد يشير إلى حمل زائد.
* `ProfileEvent_QueryProfilerRuns` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات تشغيل QueryProfiler.
* `ProfileEvent_QueryProfilerErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عمليات وصول غير صالحة إلى الذاكرة أثناء فك تتبع المكدس غير المتزامن.
* `ProfileEvent_CreatedLogEntryForMerge` ([UInt64](/ar/reference/data-types/int-uint)) — تم إنشاء إدخال سجل بنجاح لدمج الأجزاء في ReplicatedMergeTree.
* `ProfileEvent_NotCreatedLogEntryForMerge` ([UInt64](/ar/reference/data-types/int-uint)) — لم يتم إنشاء إدخال سجل لدمج الأجزاء في ReplicatedMergeTree بسبب تحديث متزامن للسجل من نسخة متماثلة أخرى.
* `ProfileEvent_CreatedLogEntryForMutation` ([UInt64](/ar/reference/data-types/int-uint)) — تم إنشاء إدخال سجل بنجاح لإجراء mutation على الأجزاء في ReplicatedMergeTree.
* `ProfileEvent_NotCreatedLogEntryForMutation` ([UInt64](/ar/reference/data-types/int-uint)) — لم يتم إنشاء إدخال سجل لإجراء mutation على الأجزاء في ReplicatedMergeTree بسبب تحديث متزامن للسجل من نسخة متماثلة أخرى.
* `ProfileEvent_S3ReadMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — زمن طلبات GET وHEAD إلى S3 storage.
* `ProfileEvent_S3ReadRequestsCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات GET وHEAD إلى S3 storage.
* `ProfileEvent_S3ReadRequestsErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأخطاء غير المرتبطة بتقييد المعدل في طلبات GET وHEAD إلى S3 storage.
* `ProfileEvent_S3ReadRequestsThrottling` ([UInt64](/ar/reference/data-types/int-uint)) — عدد أخطاء 429 و503 في طلبات GET وHEAD إلى S3 storage.
* `ProfileEvent_S3ReadRequestsRedirects` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات إعادة التوجيه في طلبات GET وHEAD إلى S3 storage.
* `ProfileEvent_S3ReadRequestAttempts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد محاولات طلبات GET وHEAD، بما في ذلك المحاولة الأولى وأي محاولات إعادة، باستثناء المحاولات التي تُنفَّذ داخليًا بواسطة retry strategy الخاصة بـ S3
* `ProfileEvent_S3ReadRequestRetryableErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأخطاء القابلة لإعادة المحاولة لطلبات GET وHEAD، باستثناء المحاولات التي تُنفَّذ داخليًا بواسطة retry strategy الخاصة بـ S3
* `ProfileEvent_S3WriteMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — زمن طلبات POST وDELETE وPUT وPATCH إلى S3 storage.
* `ProfileEvent_S3WriteRequestsCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات POST وDELETE وPUT وPATCH في تخزين S3.
* `ProfileEvent_S3WriteRequestsErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأخطاء غير الناتجة عن تقييد المعدل في طلبات POST وDELETE وPUT وPATCH في تخزين S3.
* `ProfileEvent_S3WriteRequestsThrottling` ([UInt64](/ar/reference/data-types/int-uint)) — عدد أخطاء 429 و503 في طلبات POST وDELETE وPUT وPATCH في تخزين S3.
* `ProfileEvent_S3WriteRequestsRedirects` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات إعادة التوجيه في طلبات POST وDELETE وPUT وPATCH في تخزين S3.
* `ProfileEvent_S3WriteRequestAttempts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد محاولات طلبات POST وDELETE وPUT وPATCH، بما في ذلك المحاولة الأولية وأي عمليات إعادة محاولة، مع استبعاد عمليات إعادة المحاولة التي تُنفَّذ داخليًا بواسطة استراتيجية إعادة المحاولة
* `ProfileEvent_S3WriteRequestRetryableErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأخطاء القابلة لإعادة المحاولة لطلبات POST وDELETE وPUT وPATCH، مع استبعاد عمليات إعادة المحاولة التي تُنفَّذ داخليًا بواسطة استراتيجية إعادة المحاولة
* `ProfileEvent_DiskS3ReadMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق لطلبات GET وHEAD في تخزين DiskS3.
* `ProfileEvent_DiskS3ReadRequestsCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات GET وHEAD في تخزين DiskS3.
* `ProfileEvent_DiskS3ReadRequestsErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأخطاء غير الناتجة عن تقييد المعدل في طلبات GET وHEAD في تخزين DiskS3.
* `ProfileEvent_DiskS3ReadRequestsThrottling` ([UInt64](/ar/reference/data-types/int-uint)) — عدد أخطاء 429 و503 في طلبات GET وHEAD في تخزين DiskS3.
* `ProfileEvent_DiskS3ReadRequestsRedirects` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات إعادة التوجيه في طلبات GET وHEAD في تخزين DiskS3.
* `ProfileEvent_DiskS3ReadRequestAttempts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد محاولات طلبات GET وHEAD في تخزين DiskS3، بما في ذلك المحاولة الأولية وأي عمليات إعادة محاولة، مع استبعاد عمليات إعادة المحاولة التي تُنفَّذ داخليًا بواسطة استراتيجية إعادة المحاولة في S3
* `ProfileEvent_DiskS3ReadRequestRetryableErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأخطاء القابلة لإعادة المحاولة لطلبات GET وHEAD في تخزين DiskS3، مع استبعاد عمليات إعادة المحاولة التي تُنفَّذ داخليًا بواسطة استراتيجية إعادة المحاولة في S3
* `ProfileEvent_DiskS3WriteMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق لطلبات POST وDELETE وPUT وPATCH في تخزين DiskS3.
* `ProfileEvent_DiskS3WriteRequestsCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات POST وDELETE وPUT وPATCH في تخزين DiskS3.
* `ProfileEvent_DiskS3WriteRequestsErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأخطاء غير الناتجة عن تقييد المعدل في طلبات POST وDELETE وPUT وPATCH في تخزين DiskS3.
* `ProfileEvent_DiskS3WriteRequestsThrottling` ([UInt64](/ar/reference/data-types/int-uint)) — عدد أخطاء 429 و503 في طلبات POST وDELETE وPUT وPATCH في تخزين DiskS3.
* `ProfileEvent_DiskS3WriteRequestsRedirects` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات إعادة التوجيه في طلبات POST وDELETE وPUT وPATCH في تخزين DiskS3.
* `ProfileEvent_DiskS3WriteRequestAttempts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد محاولات طلبات POST وDELETE وPUT وPATCH في تخزين DiskS3، بما في ذلك المحاولة الأولية وأي عمليات إعادة محاولة، مع استبعاد عمليات إعادة المحاولة التي تُنفَّذ داخليًا بواسطة استراتيجية إعادة المحاولة
* `ProfileEvent_DiskS3WriteRequestRetryableErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأخطاء القابلة لإعادة المحاولة لطلبات POST وDELETE وPUT وPATCH في تخزين DiskS3، مع استبعاد عمليات إعادة المحاولة التي تُنفَّذ داخليًا بواسطة استراتيجية إعادة المحاولة
* `ProfileEvent_S3DeleteObjects` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استدعاءات DeleteObject(s) في واجهة برمجة تطبيقات S3.
* `ProfileEvent_S3CopyObject` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استدعاءات CopyObject في واجهة برمجة تطبيقات S3.
* `ProfileEvent_S3ListObjects` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استدعاءات ListObjects في واجهة برمجة تطبيقات S3.
* `ProfileEvent_S3HeadObject` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استدعاء HeadObject في واجهة برمجة تطبيقات S3.
* `ProfileEvent_S3GetObjectTagging` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استدعاء GetObjectTagging في واجهة برمجة تطبيقات S3.
* `ProfileEvent_S3CreateMultipartUpload` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استدعاء CreateMultipartUpload في واجهة برمجة تطبيقات S3.
* `ProfileEvent_S3UploadPartCopy` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استدعاء UploadPartCopy في واجهة برمجة تطبيقات S3.
* `ProfileEvent_S3UploadPart` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استدعاء UploadPart في واجهة برمجة تطبيقات S3.
* `ProfileEvent_S3AbortMultipartUpload` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استدعاء AbortMultipartUpload في واجهة برمجة تطبيقات S3.
* `ProfileEvent_S3CompleteMultipartUpload` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استدعاء CompleteMultipartUpload في واجهة برمجة تطبيقات S3.
* `ProfileEvent_S3PutObject` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استدعاء PutObject في واجهة برمجة تطبيقات S3.
* `ProfileEvent_S3GetObject` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استدعاء GetObject في واجهة برمجة تطبيقات S3.
* `ProfileEvent_DiskS3DeleteObjects` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استدعاء DeleteObject(s) في واجهة برمجة تطبيقات DiskS3.
* `ProfileEvent_DiskS3CopyObject` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استدعاء CopyObject في واجهة برمجة تطبيقات DiskS3.
* `ProfileEvent_DiskS3ListObjects` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استدعاء ListObjects في واجهة برمجة تطبيقات DiskS3.
* `ProfileEvent_DiskS3HeadObject` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استدعاء HeadObject في واجهة برمجة تطبيقات DiskS3.
* `ProfileEvent_DiskS3GetObjectTagging` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استدعاء GetObjectTagging في واجهة برمجة تطبيقات DiskS3.
* `ProfileEvent_DiskS3CreateMultipartUpload` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استدعاء CreateMultipartUpload في واجهة برمجة تطبيقات DiskS3.
* `ProfileEvent_DiskS3UploadPartCopy` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استدعاء UploadPartCopy في واجهة برمجة تطبيقات DiskS3.
* `ProfileEvent_DiskS3UploadPart` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استدعاء UploadPart في واجهة برمجة تطبيقات DiskS3.
* `ProfileEvent_DiskS3AbortMultipartUpload` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استدعاء AbortMultipartUpload في واجهة برمجة تطبيقات DiskS3.
* `ProfileEvent_DiskS3CompleteMultipartUpload` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استدعاء CompleteMultipartUpload في واجهة برمجة تطبيقات DiskS3.
* `ProfileEvent_DiskS3PutObject` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استدعاء PutObject في واجهة برمجة تطبيقات DiskS3.
* `ProfileEvent_DiskS3GetObject` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استدعاء GetObject في واجهة برمجة تطبيقات DiskS3.
* `ProfileEvent_DiskPlainRewritableAzureDirectoryCreated` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الدلائل التي أنشأتها طبقة تخزين البيانات الوصفية &#39;plain&#95;rewritable&#39; لـ AzureObjectStorage.
* `ProfileEvent_DiskPlainRewritableAzureDirectoryRemoved` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الدلائل التي أزالتها طبقة تخزين البيانات الوصفية &#39;plain&#95;rewritable&#39; لـ AzureObjectStorage.
* `ProfileEvent_DiskPlainRewritableLocalDirectoryCreated` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الدلائل التي أنشأتها طبقة تخزين البيانات الوصفية &#39;plain&#95;rewritable&#39; لـ LocalObjectStorage.
* `ProfileEvent_DiskPlainRewritableLocalDirectoryRemoved` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الدلائل التي أزالتها طبقة تخزين البيانات الوصفية &#39;plain&#95;rewritable&#39; لـ LocalObjectStorage.
* `ProfileEvent_DiskPlainRewritableS3DirectoryCreated` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الدلائل التي أنشأها تخزين البيانات الوصفية `plain_rewritable` لـ S3ObjectStorage.
* `ProfileEvent_DiskPlainRewritableS3DirectoryRemoved` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الدلائل التي أزالها تخزين البيانات الوصفية `plain_rewritable` لـ S3ObjectStorage.
* `ProfileEvent_DiskPlainRewritableLegacyLayoutDiskCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد أقراص `plain_rewritable` ذات التخطيط القديم.
* `ProfileEvent_S3Clients` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عملاء S3 الذين أُنشئوا.
* `ProfileEvent_TinyS3Clients` ([UInt64](/ar/reference/data-types/int-uint)) — عدد نُسخ عملاء S3 التي تعيد استخدام موفّر مصادقة موجود من عميل آخر.
* `ProfileEvent_EngineFileLikeReadFiles` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الملفات المقروءة في محركات الجداول التي تعمل مع الملفات (مثل File/S3/URL/HDFS).
* `ProfileEvent_ReadBufferFromS3Microseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في القراءة من S3.
* `ProfileEvent_ReadBufferFromS3InitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في تهيئة الاتصال بـ S3.
* `ProfileEvent_ReadBufferFromS3Bytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المقروءة من S3.
* `ProfileEvent_ReadBufferFromS3RequestsErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الاستثناءات أثناء القراءة من S3.
* `ProfileEvent_WriteBufferFromS3Microseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في الكتابة إلى S3.
* `ProfileEvent_WriteBufferFromS3Bytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المكتوبة إلى S3.
* `ProfileEvent_WriteBufferFromS3RequestsErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الاستثناءات أثناء الكتابة إلى S3.
* `ProfileEvent_WriteBufferFromS3WaitInflightLimitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في الانتظار حتى تكتمل بعض الطلبات الحالية عندما يصل عددها إلى الحد المحدد بواسطة s3&#95;max&#95;inflight&#95;parts&#95;for&#95;one&#95;file.
* `ProfileEvent_QueryMemoryLimitExceeded` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي تم فيها تجاوز حد الذاكرة للاستعلام.
* `ProfileEvent_MemoryAllocatedWithoutCheck` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي جرى فيها تخصيص الذاكرة دون التحقق من قيود الذاكرة.
* `ProfileEvent_MemoryAllocatedWithoutCheckBytes` ([UInt64](/ar/reference/data-types/int-uint)) — مقدار البايتات التي جرى تخصيصها دون التحقق من قيود الذاكرة.
* `ProfileEvent_AzureGetObject` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استدعاءات GetObject في واجهة برمجة تطبيقات Azure.
* `ProfileEvent_AzureUpload` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استدعاءات Upload في واجهة برمجة تطبيقات Azure Blob Storage
* `ProfileEvent_AzureStageBlock` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استدعاءات StageBlock في واجهة برمجة تطبيقات Azure Blob Storage
* `ProfileEvent_AzureCommitBlockList` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استدعاءات CommitBlockList في واجهة برمجة تطبيقات Azure Blob Storage
* `ProfileEvent_AzureCopyObject` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استدعاءات CopyObject في واجهة برمجة تطبيقات Azure Blob Storage
* `ProfileEvent_AzureDeleteObjects` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استدعاءات DeleteObject(s) في واجهة برمجة تطبيقات Azure Blob Storage.
* `ProfileEvent_AzureListObjects` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استدعاءات ListObjects في واجهة برمجة تطبيقات Azure Blob Storage.
* `ProfileEvent_AzureGetProperties` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استدعاءات GetProperties في واجهة برمجة تطبيقات Azure Blob Storage.
* `ProfileEvent_AzureCreateContainer` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استدعاءات CreateContainer لواجهة برمجة تطبيقات Azure blob storage.
* `ProfileEvent_DiskAzureGetObject` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استدعاءات GetObject لواجهة برمجة تطبيقات Disk Azure.
* `ProfileEvent_DiskAzureUpload` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استدعاءات Upload لواجهة برمجة تطبيقات Azure blob storage الخاصة بـ Disk Azure
* `ProfileEvent_DiskAzureStageBlock` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استدعاءات StageBlock لواجهة برمجة تطبيقات Azure blob storage الخاصة بـ Disk Azure
* `ProfileEvent_DiskAzureCommitBlockList` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استدعاءات CommitBlockList لواجهة برمجة تطبيقات Azure blob storage الخاصة بـ Disk Azure
* `ProfileEvent_DiskAzureCopyObject` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استدعاءات CopyObject لواجهة برمجة تطبيقات Azure blob storage الخاصة بـ Disk Azure
* `ProfileEvent_DiskAzureListObjects` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استدعاءات ListObjects لواجهة برمجة تطبيقات Azure blob storage الخاصة بـ Disk Azure.
* `ProfileEvent_DiskAzureDeleteObjects` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استدعاءات DeleteObject(s) لواجهة برمجة تطبيقات Azure blob storage.
* `ProfileEvent_DiskAzureGetProperties` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استدعاءات GetProperties لواجهة برمجة تطبيقات Azure blob storage الخاصة بـ Disk Azure.
* `ProfileEvent_DiskAzureCreateContainer` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استدعاءات CreateContainer لواجهة برمجة تطبيقات Azure blob storage الخاصة بـ Disk Azure.
* `ProfileEvent_ReadBufferFromAzureMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في القراءة من Azure.
* `ProfileEvent_ReadBufferFromAzureInitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في تهيئة الاتصال بـ Azure.
* `ProfileEvent_ReadBufferFromAzureBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المقروءة من Azure.
* `ProfileEvent_ReadBufferFromAzureRequestsErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الاستثناءات أثناء القراءة من Azure
* `ProfileEvent_CachedReadBufferReadFromCacheHits` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات وصول القراءة من ذاكرة التخزين المؤقت لنظام الملفات إلى بيانات موجودة في ذاكرة التخزين المؤقت.
* `ProfileEvent_CachedReadBufferReadFromCacheMisses` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات عدم عثور القراءة من ذاكرة التخزين المؤقت لنظام الملفات على البيانات في ذاكرة التخزين المؤقت.
* `ProfileEvent_CachedReadBufferReadFromSourceMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — وقت القراءة من مصدر ذاكرة التخزين المؤقت لنظام الملفات (من نظام الملفات البعيد، وما إلى ذلك)
* `ProfileEvent_CachedReadBufferWaitReadBufferMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في انتظار read buffer الداخلي (بما في ذلك انتظار ذاكرة التخزين المؤقت)
* `ProfileEvent_CachedReadBufferPredownloadedFromSourceMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — وقت القراءة من مصدر ذاكرة التخزين المؤقت لنظام الملفات لأجل التنزيل المسبق (من نظام الملفات البعيد، وما إلى ذلك)
* `ProfileEvent_CachedReadBufferReadFromCacheMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — وقت القراءة من ذاكرة التخزين المؤقت لنظام الملفات
* `ProfileEvent_CachedReadBufferReadFromSourceBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المقروءة من مصدر ذاكرة التخزين المؤقت لنظام الملفات (من نظام الملفات البعيد، وما إلى ذلك)
* `ProfileEvent_CachedReadBufferPredownloadedFromSourceBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المقروءة من مصدر ذاكرة التخزين المؤقت لنظام الملفات لأجل التنزيل المسبق (من نظام الملفات البعيد، وما إلى ذلك)
* `ProfileEvent_CachedReadBufferReadFromCacheBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المقروءة من ذاكرة التخزين المؤقت لنظام الملفات
* `ProfileEvent_CachedReadBufferPredownloadedBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المقروءة من مصدر ذاكرة التخزين المؤقت لنظام الملفات. تُقرأ مقاطع ذاكرة التخزين المؤقت كاملةً من اليسار إلى اليمين، وقد نحتاج إلى تنزيل جزء من المقطع مسبقًا رغم أنه غير مرتبط بالمهمة الحالية، فقط للوصول إلى البيانات المطلوبة
* `ProfileEvent_CachedReadBufferCacheWriteBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المكتوبة من المصدر (نظام الملفات البعيد، وما إلى ذلك) إلى ذاكرة التخزين المؤقت لنظام الملفات
* `ProfileEvent_CachedReadBufferCacheWriteMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرق في كتابة البيانات إلى ذاكرة التخزين المؤقت لنظام الملفات
* `ProfileEvent_CachedReadBufferCreateBufferMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — وقت إعداد المخزن المؤقت
* `ProfileEvent_CachedWriteBufferCacheWriteBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المكتوبة من المصدر (remote fs، وما إلى ذلك) إلى ذاكرة التخزين المؤقت لنظام الملفات
* `ProfileEvent_CachedWriteBufferCacheWriteMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرق في كتابة البيانات إلى ذاكرة التخزين المؤقت لنظام الملفات
* `ProfileEvent_FilesystemCacheLoadMetadataMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرق في تحميل بيانات التعريف الخاصة بذاكرة التخزين المؤقت لنظام الملفات
* `ProfileEvent_FilesystemCacheEvictedBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات التي أُزيلت من ذاكرة التخزين المؤقت لنظام الملفات
* `ProfileEvent_FilesystemCacheCreatedKeyDirectories` ([UInt64](/ar/reference/data-types/int-uint)) — عدد أدلة المفاتيح التي تم إنشاؤها
* `ProfileEvent_FilesystemCacheEvictedFileSegments` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مقاطع الملفات التي أُزيلت من ذاكرة التخزين المؤقت لنظام الملفات
* `ProfileEvent_FilesystemCacheDowngradedFileSegments` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مقاطع الملفات التي خُفِّضت (نُقلت) من قائمة الانتظار المحمية إلى قائمة الانتظار الاختبارية في سياسة التخزين المؤقت SLRU. هذه حركة داخلية داخل ذاكرة التخزين المؤقت وليست عملية إزالة.
* `ProfileEvent_FilesystemCacheBackgroundDownloadQueuePush` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مقاطع الملفات المرسلة للتنزيل في الخلفية في ذاكرة التخزين المؤقت لنظام الملفات
* `ProfileEvent_FilesystemCacheEvictionSkippedFileSegments` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مقاطع الملفات التي تم تخطيها عند الإزالة لأنها في حالة غير قابلة للتحرير
* `ProfileEvent_FilesystemCacheEvictionSkippedEvictingFileSegments` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مقاطع الملفات التي تم تخطيها عند الإزالة لأنها في حالة إزالة
* `ProfileEvent_FilesystemCacheEvictionSkippedMovingFileSegments` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مقاطع الملفات التي تم تخطيها عند الإزالة لأنها في حالة نقل
* `ProfileEvent_FilesystemCacheEvictionTries` ([UInt64](/ar/reference/data-types/int-uint)) — عدد محاولات إزالة عناصر ذاكرة التخزين المؤقت لنظام الملفات
* `ProfileEvent_FilesystemCacheEvictionReusedIterator` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات إعادة استخدام المكرِّر لذاكرة التخزين المؤقت لنظام الملفات
* `ProfileEvent_FilesystemCacheLockKeyMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — زمن قفل مفتاح ذاكرة التخزين المؤقت
* `ProfileEvent_FilesystemCacheLockMetadataMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — زمن قفل بيانات التعريف الخاصة بذاكرة التخزين المؤقت لنظام الملفات
* `ProfileEvent_FilesystemCachePriorityWriteLockMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — زمن قفل ذاكرة التخزين المؤقت لنظام الملفات للكتابة في قائمة انتظار الأولوية
* `ProfileEvent_FilesystemCachePriorityReadLockMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — زمن قفل ذاكرة التخزين المؤقت لنظام الملفات للقراءة في قائمة انتظار الأولوية
* `ProfileEvent_FilesystemCacheStateLockMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — زمن قفل حالة ذاكرة التخزين المؤقت لنظام الملفات
* `ProfileEvent_FilesystemCacheReserveMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — زمن حجز المساحة في ذاكرة التخزين المؤقت لنظام الملفات
* `ProfileEvent_FilesystemCacheReserveAttempts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد محاولات حجز المساحة في ذاكرة التخزين المؤقت لنظام الملفات
* `ProfileEvent_FilesystemCacheEvictMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — زمن إزالة عناصر ذاكرة التخزين المؤقت لنظام الملفات
* `ProfileEvent_FilesystemCacheGetOrSetMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — زمن getOrSet() لذاكرة التخزين المؤقت لنظام الملفات
* `ProfileEvent_FilesystemCacheGetMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — زمن get() لذاكرة التخزين المؤقت لنظام الملفات
* `ProfileEvent_FilesystemCacheBackgroundEvictedFileSegments` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مقاطع الملفات التي طُردت بواسطة خيط الخلفية
* `ProfileEvent_FilesystemCacheBackgroundEvictedBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات التي طُردت بواسطة خيط الخلفية
* `ProfileEvent_FilesystemCacheCheckCorrectness` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استدعاء `FileCache::assertCacheCorrectness`
* `ProfileEvent_FilesystemCacheCheckCorrectnessMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — مقدار الوقت الذي تستغرقه `FileCache::assertCacheCorrectness`
* `ProfileEvent_FileSegmentWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — وقت الانتظار في الحالة DOWNLOADING
* `ProfileEvent_FileSegmentCompleteMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — مدة `FileSegment::complete()` في ذاكرة التخزين المؤقت لنظام الملفات
* `ProfileEvent_FileSegmentLockMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — وقت قفل مقطع الملف
* `ProfileEvent_FileSegmentWriteMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — وقت `write()` لمقطع الملف
* `ProfileEvent_FileSegmentIncreasePriorityMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — وقت زيادة أولوية مقطع الملف
* `ProfileEvent_FileSegmentRemoveMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — وقت `remove()` لمقطع الملف
* `ProfileEvent_FileSegmentHolderCompleteMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — وقت `complete()` لحامل مقاطع الملفات
* `ProfileEvent_FileSegmentFailToIncreasePriority` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي لم تزد فيها الأولوية بسبب ارتفاع التنازع على قفل ذاكرة التخزين المؤقت
* `ProfileEvent_FilesystemCacheFailToReserveSpaceBecauseOfLockContention` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي جرى فيها تخطي حجز المساحة بسبب ارتفاع التنازع على قفل ذاكرة التخزين المؤقت
* `ProfileEvent_FilesystemCacheFailToReserveSpaceBecauseOfCacheResize` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي جرى فيها تخطي حجز المساحة بسبب تغيير حجم ذاكرة التخزين المؤقت
* `ProfileEvent_FilesystemCacheHoldFileSegments` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مقاطع الملفات المحتفَظ بها في ذاكرة التخزين المؤقت لنظام الملفات
* `ProfileEvent_FilesystemCacheUnusedHoldFileSegments` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مقاطع الملفات المحتفَظ بها في ذاكرة التخزين المؤقت لنظام الملفات ولكن لم تُستخدم (بسبب seek أو LIMIT n، إلخ)
* `ProfileEvent_FilesystemCacheFreeSpaceKeepingThreadRun` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي نفّذ فيها خيط الخلفية مهمة الحفاظ على المساحة الحرة
* `ProfileEvent_FilesystemCacheFreeSpaceKeepingThreadWorkMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — المدة التي نفّذ خلالها خيط الخلفية مهمة الحفاظ على المساحة الحرة
* `ProfileEvent_FilesystemCacheFailedEvictionCandidates` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مقاطع الملفات التي فشل طردها بشكل غير متوقع أثناء طرد ذاكرة التخزين المؤقت الديناميكية لنظام الملفات
* `ProfileEvent_RemoteFSSeeks` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لعمليات seek للمخزن المؤقت غير المتزامن
* `ProfileEvent_RemoteFSPrefetches` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات الجلب المسبق التي أُجريت مع القراءة غير المتزامنة من نظام الملفات البعيد
* `ProfileEvent_RemoteFSCancelledPrefetches` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات الجلب المسبق الملغاة (بسبب seek)
* `ProfileEvent_RemoteFSUnusedPrefetches` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات الجلب المسبق المعلقة عند إتلاف المخزن المؤقت
* `ProfileEvent_RemoteFSPrefetchedReads` ([UInt64](/ar/reference/data-types/int-uint)) — عدد القراءات من المخزن المؤقت المُجلَب مسبقًا
* `ProfileEvent_RemoteFSPrefetchedBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات من المخزن المؤقت المُجلَب مسبقًا
* `ProfileEvent_RemoteFSUnprefetchedReads` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات القراءة من المخزن المؤقت الذي لم يُجرَ له جلب مسبق
* `ProfileEvent_RemoteFSUnprefetchedBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات من المخزن المؤقت الذي لم يُجرَ له جلب مسبق
* `ProfileEvent_RemoteFSLazySeeks` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات seek الكسولة
* `ProfileEvent_RemoteFSSeeksWithReset` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات seek التي تؤدي إلى اتصال جديد
* `ProfileEvent_RemoteFSBuffers` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المخازن المؤقتة المُنشأة للقراءة غير المتزامنة من نظام الملفات البعيد
* `ProfileEvent_MergeTreePrefetchedReadPoolInit` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في تجهيز المهام في MergeTreePrefetchedReadPool
* `ProfileEvent_WaitPrefetchTaskMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في انتظار القارئ ذي الجلب المسبق
* `ProfileEvent_ThreadpoolReaderTaskMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في جلب البيانات أثناء القراءة غير المتزامنة
* `ProfileEvent_ThreadpoolReaderPrepareMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في التحضير (مثل استدعاء الطريقة `seek()` الخاصة بالقارئ)
* `ProfileEvent_ThreadpoolReaderReadBytes` ([UInt64](/ar/reference/data-types/int-uint)) — البايتات المقروءة من مهمة في مجمّع الخيوط أثناء القراءة غير المتزامنة
* `ProfileEvent_ThreadpoolReaderSubmit` ([UInt64](/ar/reference/data-types/int-uint)) — البايتات المقروءة من مهمة في مجمّع الخيوط أثناء القراءة غير المتزامنة
* `ProfileEvent_ThreadpoolReaderSubmitReadSynchronously` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي لم تُجدول فيها مهمة على مجمّع الخيوط، وتمت القراءة بشكل متزامن بدلًا من ذلك
* `ProfileEvent_ThreadpoolReaderSubmitReadSynchronouslyBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات التي تمت قراءتها بشكل متزامن
* `ProfileEvent_ThreadpoolReaderSubmitReadSynchronouslyMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — مقدار الوقت المستغرَق في القراءة المتزامنة
* `ProfileEvent_ThreadpoolReaderSubmitLookupInCacheMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — مقدار الوقت المستغرَق في التحقق مما إذا كان المحتوى مخزنًا مؤقتًا
* `ProfileEvent_AsynchronousReaderIgnoredBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات التي تم تجاهلها أثناء القراءة غير المتزامنة
* `ProfileEvent_ReadBufferSeekCancelConnection` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات seek التي تؤدي إلى اتصال جديد (S3, HTTP)
* `ProfileEvent_SleepFunctionCalls` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استدعاء دالة sleep ‏(`sleep`, `sleepEachRow`).
* `ProfileEvent_SleepFunctionMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المضبوط للنوم في دالة sleep ‏(`sleep`, `sleepEachRow`).
* `ProfileEvent_SleepFunctionElapsedMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في النوم داخل دالة sleep ‏(`sleep`, `sleepEachRow`).
* `ProfileEvent_ThreadPoolReaderPageCacheHit` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي تمت فيها القراءة داخل ThreadPoolReader من page cache.
* `ProfileEvent_ThreadPoolReaderPageCacheHitBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المقروءة داخل ThreadPoolReader عندما تمت القراءة من page cache.
* `ProfileEvent_ThreadPoolReaderPageCacheHitElapsedMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في قراءة البيانات من page cache داخل ThreadPoolReader.
* `ProfileEvent_ThreadPoolReaderPageCacheMiss` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي لم تتم فيها القراءة داخل ThreadPoolReader من page cache، وتم تمريرها إلى مجمّع الخيوط.
* `ProfileEvent_ThreadPoolReaderPageCacheMissBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المقروءة داخل ThreadPoolReader عندما لم تتم القراءة من page cache وتم تمريرها إلى مجمّع الخيوط.
* `ProfileEvent_ThreadPoolReaderPageCacheMissElapsedMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في قراءة البيانات داخل المهمة غير المتزامنة في ThreadPoolReader عندما لا تتم القراءة من ذاكرة تخزين الصفحات المؤقتة.
* `ProfileEvent_AsynchronousReadWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في انتظار القراءات غير المتزامنة ضمن القراءة المحلية غير المتزامنة.
* `ProfileEvent_SynchronousReadWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في انتظار القراءات المتزامنة ضمن القراءة المحلية غير المتزامنة.
* `ProfileEvent_AsynchronousRemoteReadWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في انتظار القراءات البعيدة غير المتزامنة.
* `ProfileEvent_SynchronousRemoteReadWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في انتظار القراءات البعيدة المتزامنة.
* `ProfileEvent_ExternalDataSourceLocalCacheReadBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المقروءة من المخزن المؤقت المحلي في RemoteReadBufferCache
* `ProfileEvent_MainConfigLoads` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات إعادة تحميل التهيئة الرئيسية.
* `ProfileEvent_AggregationPreallocatedElementsInHashTables` ([UInt64](/ar/reference/data-types/int-uint)) — عدد العناصر التي خُصصت مسبقًا في جداول التجزئة لأغراض التجميع.
* `ProfileEvent_AggregationHashTablesInitializedAsTwoLevel` ([UInt64](/ar/reference/data-types/int-uint)) — عدد جداول التجزئة التي هُيئت بمستويين لأغراض التجميع.
* `ProfileEvent_AggregationOptimizedEqualRangesOfKeys` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الكتل التي طُبّق عليها تحسين النطاقات المتساوية للمفاتيح
* `ProfileEvent_HashJoinPreallocatedElementsInHashTables` ([UInt64](/ar/reference/data-types/int-uint)) — عدد العناصر التي خُصصت مسبقًا في جداول التجزئة لعملية hash join.
* `ProfileEvent_MetadataFromKeeperCacheHit` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي جرى فيها الرد على طلب بيانات وصفية لتخزين الكائنات من ذاكرة التخزين المؤقت دون إرسال طلب إلى Keeper
* `ProfileEvent_MetadataFromKeeperCacheMiss` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي كان لا بد فيها من الرد على طلب بيانات وصفية لتخزين الكائنات من Keeper
* `ProfileEvent_MetadataFromKeeperCacheTooManyInvalidated` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي أعادت فيها ذاكرة التخزين المؤقت لنظام الملفات عددًا كبيرًا جدًا من الإدخالات غير الصالحة
* `ProfileEvent_MetadataFromKeeperCacheUpdateMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في تحديث ذاكرة التخزين المؤقت، بما في ذلك انتظار الردود من Keeper
* `ProfileEvent_MetadataFromKeeperUpdateCacheOneLevel` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي نُفِّذ فيها تحديث لذاكرة التخزين المؤقت لمستوى واحد من شجرة الدليل
* `ProfileEvent_MetadataFromKeeperTransactionCommit` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات محاولة اعتماد معاملة البيانات الوصفية
* `ProfileEvent_MetadataFromKeeperTransactionCommitRetry` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات إعادة محاولة اعتماد معاملة البيانات الوصفية
* `ProfileEvent_MetadataFromKeeperCleanupTransactionCommit` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات محاولة اعتماد معاملة البيانات الوصفية الخاصة بتنظيف الكائنات المحذوفة
* `ProfileEvent_MetadataFromKeeperCleanupTransactionCommitRetry` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات إعادة محاولة اعتماد معاملة البيانات الوصفية الخاصة بتنظيف الكائنات المحذوفة
* `ProfileEvent_MetadataFromKeeperOperations` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي أُرسل فيها طلب إلى Keeper
* `ProfileEvent_MetadataFromKeeperIndividualOperations` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المسارات التي تمت قراءتها أو كتابتها عبر طلبات مفردة أو متعددة إلى Keeper
* `ProfileEvent_MetadataFromKeeperIndividualOperationsMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق أثناء الطلبات المفردة أو المتعددة إلى Keeper
* `ProfileEvent_MetadataFromKeeperReconnects` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات إعادة الاتصال بـ Keeper
* `ProfileEvent_MetadataFromKeeperBackgroundCleanupObjects` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي جرى فيها تنظيف كائن محذوف قديم بواسطة مهمة في الخلفية
* `ProfileEvent_MetadataFromKeeperBackgroundCleanupTransactions` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي نُظِّف فيها token idempotency القديم للمعاملة بواسطة مهمة تعمل في الخلفية
* `ProfileEvent_MetadataFromKeeperBackgroundCleanupBlobs` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي نُظِّف فيها جزء فارغ من تخطيط blob بواسطة مهمة تعمل في الخلفية
* `ProfileEvent_MetadataFromKeeperBackgroundCleanupErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي حدث فيها error أثناء مهمة التنظيف في الخلفية
* `ProfileEvent_BlobKillerThreadRuns` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات تنفيذ thread الخاص بـ BlobKiller
* `ProfileEvent_BlobKillerThreadLockedBlobs` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الـ blobs المُعادة من تخزين البيانات الوصفية
* `ProfileEvent_BlobKillerThreadRemoveTasks` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مهام الإزالة التي أنشأها BlobKiller
* `ProfileEvent_BlobKillerThreadRemovedBlobs` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الـ blobs التي أزالها BlobKiller
* `ProfileEvent_BlobKillerThreadRecordedBlobs` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الـ blobs التي سُجِّلت إزالتها بواسطة BlobKiller في تخزين البيانات الوصفية
* `ProfileEvent_BlobKillerThreadLockBlobsErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد أخطاء قفل الـ blobs التي حدثت أثناء تنفيذ BlobKiller
* `ProfileEvent_BlobKillerThreadRemoveBlobsErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد أخطاء إزالة الـ blobs التي حدثت أثناء تنفيذ BlobKiller
* `ProfileEvent_BlobKillerThreadRecordBlobsErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد أخطاء تسجيل الـ blobs التي حدثت أثناء تنفيذ BlobKiller
* `ProfileEvent_BlobCopierThreadRuns` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات تنفيذ thread الخاص بـ BlobCopier
* `ProfileEvent_BlobCopierThreadLockedBlobs` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الـ blobs المُعادة من تخزين البيانات الوصفية
* `ProfileEvent_BlobCopierThreadReplicatedBlobs` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الـ blobs التي نسخها BlobCopier
* `ProfileEvent_BlobCopierThreadRecordedBlobs` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الـ blobs التي سُجِّلت replication الخاصة بها بواسطة BlobCopier في تخزين البيانات الوصفية
* `ProfileEvent_BlobCopierThreadLockBlobsErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد أخطاء قفل الـ blobs التي حدثت أثناء تنفيذ BlobCopier
* `ProfileEvent_BlobCopierThreadReplicateBlobsErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد أخطاء replication للـ blobs التي حدثت أثناء تنفيذ BlobCopier
* `ProfileEvent_BlobCopierThreadRecordBlobsErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد أخطاء تسجيل الـ blobs التي حدثت أثناء تنفيذ BlobCopier
* `ProfileEvent_SharedMergeTreeMetadataCacheHintLoadedFromCache` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي عُثر فيها على تلميح metadata cache من دون الرجوع إلى Keeper
* `ProfileEvent_KafkaRebalanceRevocations` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات سحب الأقسام (المرحلة الأولى من إعادة موازنة consumer group)
* `ProfileEvent_KafkaRebalanceAssignments` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات إسناد الأقسام (المرحلة النهائية من إعادة موازنة consumer group)
* `ProfileEvent_KafkaRebalanceErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات فشل إعادة موازنة consumer group
* `ProfileEvent_KafkaMessagesPolled` ([UInt64](/ar/reference/data-types/int-uint)) — عدد رسائل Kafka التي جرى جلبها من librdkafka إلى ClickHouse
* `ProfileEvent_KafkaMessagesRead` ([UInt64](/ar/reference/data-types/int-uint)) — عدد رسائل Kafka التي عالجها ClickHouse بالفعل
* `ProfileEvent_KafkaMessagesFailed` ([UInt64](/ar/reference/data-types/int-uint)) — عدد رسائل Kafka التي أخفق ClickHouse في parse لها
* `ProfileEvent_KafkaRowsRead` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الصفوف التي جرى تحليلها من رسائل Kafka
* `ProfileEvent_KafkaRowsRejected` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الصفوف التي جرى تحليلها ثم رُفضت لاحقًا (بسبب عمليات إعادة التوازن / الأخطاء أو أسباب مشابهة). ستُستهلك هذه الصفوف مرة أخرى بعد إعادة التوازن.
* `ProfileEvent_KafkaDirectReads` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات SELECT المباشرة من جداول Kafka منذ بدء تشغيل الخادم
* `ProfileEvent_KafkaBackgroundReads` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات القراءة الخلفية التي تُغذّي العروض المادية من Kafka منذ بدء تشغيل الخادم
* `ProfileEvent_KafkaCommits` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات commit الناجحة للإزاحات المستهلكة إلى Kafka (ومن المفترض عادةً أن يكون مساويًا لـ KafkaBackgroundReads)
* `ProfileEvent_KafkaCommitFailures` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات commit الفاشلة للإزاحات المستهلكة إلى Kafka (ويكون هذا عادةً مؤشرًا على وجود ازدواجية في البيانات)
* `ProfileEvent_KafkaConsumerErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأخطاء التي أبلغت عنها librdkafka أثناء عمليات poll
* `ProfileEvent_KafkaMVNotReady` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المحاولات الفاشلة لبث البيانات إلى عرض مادي غير جاهز
* `ProfileEvent_KafkaWrites` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات الكتابة (الإدراج) إلى جداول Kafka
* `ProfileEvent_KafkaRowsWritten` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الصفوف المُدرجة في جداول Kafka
* `ProfileEvent_KafkaProducerFlushes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات flush الصريحة إلى مُنتِج Kafka
* `ProfileEvent_KafkaMessagesProduced` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الرسائل المُنتَجة إلى Kafka
* `ProfileEvent_KafkaProducerErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأخطاء أثناء إنتاج الرسائل إلى Kafka
* `ProfileEvent_ScalarSubqueriesGlobalCacheHit` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي نُفِّذت فيها قراءة من استعلام فرعي scalar باستخدام ذاكرة التخزين المؤقت العامة
* `ProfileEvent_ScalarSubqueriesLocalCacheHit` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي نُفِّذت فيها قراءة من استعلام فرعي scalar باستخدام ذاكرة التخزين المؤقت المحلية
* `ProfileEvent_ScalarSubqueriesCacheMiss` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي لم تكن فيها القراءة من استعلام فرعي scalar مخزنة مؤقتًا واضطرت إلى أن تُحسب بالكامل
* `ProfileEvent_SchemaInferenceCacheHits` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي يُعثر فيها على المصدر المطلوب في ذاكرة التخزين المؤقت للمخطط
* `ProfileEvent_SchemaInferenceCacheSchemaHits` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي يُعثر فيها على المخطط في ذاكرة التخزين المؤقت للمخطط أثناء استنتاج المخطط
* `ProfileEvent_SchemaInferenceCacheNumRowsHits` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي يُعثر فيها على عدد الصفوف في ذاكرة التخزين المؤقت للمخطط أثناء العد من الملفات
* `ProfileEvent_SchemaInferenceCacheMisses` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي لا يكون فيها المصدر المطلوب موجودًا في ذاكرة التخزين المؤقت للمخطط
* `ProfileEvent_SchemaInferenceCacheSchemaMisses` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي يكون فيها المصدر المطلوب موجودًا في ذاكرة التخزين المؤقت لكن المخطط غير موجود فيها أثناء استنتاج المخطط
* `ProfileEvent_SchemaInferenceCacheNumRowsMisses` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي يكون فيها المصدر المطلوب موجودًا في ذاكرة التخزين المؤقت لكن عدد الصفوف غير موجود فيها أثناء العد من الملفات
* `ProfileEvent_SchemaInferenceCacheEvictions` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي أُزيل فيها مخطط من ذاكرة التخزين المؤقت بسبب تجاوز السعة
* `ProfileEvent_SchemaInferenceCacheInvalidations` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي أصبح فيها مخطط في ذاكرة التخزين المؤقت غير صالح بسبب تغيّرات في البيانات
* `ProfileEvent_KeeperPacketsSent` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الحزم المرسلة بواسطة خادم Keeper
* `ProfileEvent_KeeperPacketsReceived` ([UInt64](/ar/reference/data-types/int-uint)) — الحزم التي استلمها خادم Keeper
* `ProfileEvent_KeeperRequestTotal` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للطلبات على خادم Keeper
* `ProfileEvent_KeeperRequestTotalWithSubrequests` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للطلبات على خادم Keeper، مع احتساب كل طلب فرعي داخل طلب متعدد
* `ProfileEvent_KeeperLatency` ([UInt64](/ar/reference/data-types/int-uint)) — زمن الاستجابة في Keeper
* `ProfileEvent_KeeperTotalElapsedMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي زمن الاستجابة في Keeper لطلب واحد
* `ProfileEvent_KeeperProcessElapsedMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — زمن استجابة commit في Keeper لطلب واحد
* `ProfileEvent_KeeperPreprocessElapsedMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — زمن استجابة المعالجة التمهيدية في Keeper لطلب واحد
* `ProfileEvent_KeeperStorageLockWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في انتظار الحصول على قفل تخزين Keeper
* `ProfileEvent_KeeperStorageSharedLockWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في انتظار الحصول على القفل المشترك لتخزين Keeper
* `ProfileEvent_KeeperChangelogLockWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في انتظار الحصول على قفل سجل التغييرات في Keeper
* `ProfileEvent_KeeperServerWriteLockWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في انتظار الحصول على قفل الكتابة لخادم Keeper
* `ProfileEvent_KeeperSessionCallbackLockWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في انتظار الحصول على قفل استدعاء الجلسة في Keeper
* `ProfileEvent_KeeperReadRequestQueueLockWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في انتظار الحصول على قفل قائمة انتظار طلبات القراءة في Keeper
* `ProfileEvent_KeeperProcessAndResponsesLockWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في انتظار الحصول على قفل المعالجة والاستجابات في Keeper
* `ProfileEvent_KeeperCommitWaitElapsedMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في انتظار اعتماد سجل معيّن
* `ProfileEvent_KeeperBatchMaxCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي كان فيها حجم الدفعة مقيَّدًا بالعدد
* `ProfileEvent_KeeperBatchMaxTotalSize` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي كان فيها حجم الدفعة مقيَّدًا بإجمالي حجم البايتات
* `ProfileEvent_KeeperReadBatchCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد دفعات طلبات القراءة التي عالجها Keeper
* `ProfileEvent_KeeperReadBatchTotalRequests` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لطلبات القراءة التي عالجها Keeper على شكل دفعات
* `ProfileEvent_KeeperCommits` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات commit الناجحة
* `ProfileEvent_KeeperCommitsFailed` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات commit الفاشلة
* `ProfileEvent_KeeperSnapshotCreations` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات إنشاء اللقطات
* `ProfileEvent_KeeperSnapshotCreationsFailed` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات إنشاء اللقطات الفاشلة
* `ProfileEvent_KeeperSnapshotApplys` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات تطبيق اللقطات
* `ProfileEvent_KeeperSnapshotApplysFailed` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات تطبيق اللقطات الفاشلة
* `ProfileEvent_KeeperReadSnapshot` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات قراءة اللقطات المكتملة
* `ProfileEvent_KeeperReadSnapshotObject` ([UInt64](/ar/reference/data-types/int-uint)) — عدد كائنات اللقطات المُرسلة إلى العقد التابعة
* `ProfileEvent_KeeperReadSnapshotFailed` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات قراءة اللقطات الفاشلة
* `ProfileEvent_KeeperSnapshotRemoteLoaderErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد أخطاء القراءة عن بُعد في RemoteSnapshotLoader أثناء تقديم لقطة إلى عقدة تابعة
* `ProfileEvent_KeeperSaveSnapshotObject` ([UInt64](/ar/reference/data-types/int-uint)) — عدد كائنات اللقطات المستلمة من القائد
* `ProfileEvent_KeeperSaveSnapshotFailed` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات حفظ اللقطات الفاشلة
* `ProfileEvent_KeeperSaveSnapshot` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات حفظ اللقطات
* `ProfileEvent_KeeperCreateRequest` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات الإنشاء
* `ProfileEvent_KeeperRemoveRequest` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات الإزالة
* `ProfileEvent_KeeperSetRequest` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات التعيين
* `ProfileEvent_KeeperReconfigRequest` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات إعادة التهيئة
* `ProfileEvent_KeeperCheckRequest` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات التحقق
* `ProfileEvent_KeeperMultiRequest` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الطلبات المتعددة
* `ProfileEvent_KeeperMultiReadRequest` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات القراءة المتعددة
* `ProfileEvent_KeeperGetRequest` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات الجلب
* `ProfileEvent_KeeperListRequest` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات الإدراج
* `ProfileEvent_KeeperListRecursiveRequest` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات جلب العقد الفرعية بشكل متكرر
* `ProfileEvent_KeeperExistsRequest` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات التحقق من الوجود
* `ProfileEvent_KeeperSetWatchesRequest` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات تعيين المراقبات
* `ProfileEvent_KeeperAddWatchRequest` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات إضافة المراقبات
* `ProfileEvent_KeeperRemoveWatchRequest` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات إزالة المراقبات
* `ProfileEvent_KeeperCheckWatchRequest` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات إزالة المراقبات
* `ProfileEvent_KeeperRequestRejectedDueToSoftMemoryLimitCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الطلبات التي رُفضت بسبب تجاوز حد الذاكرة المرن
* `ProfileEvent_KeeperStaleRequestsSkipped` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات Keeper التي تم تخطيها لأن الجلسة لم تعد نشطة
* `ProfileEvent_KeeperLiveSessionsLockWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في انتظار الحصول على قفل الجلسات النشطة في Keeper
* `ProfileEvent_OverflowBreak` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي أُلغيت فيها معالجة البيانات بسبب حدّ تعقيد الاستعلام مع الإعداد &#39;*&#95;overflow&#95;mode&#39; = &#39;break&#39; وكانت النتيجة غير مكتملة.
* `ProfileEvent_OverflowThrow` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي أُلغيت فيها معالجة البيانات بسبب حدّ تعقيد الاستعلام مع الإعداد &#39;*&#95;overflow&#95;mode&#39; = &#39;throw&#39; وتم طرح استثناء.
* `ProfileEvent_OverflowAny` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي كان فيها GROUP BY التقريبي مفعّلًا: عندما أُجري التجميع فقط على أول المفاتيح الفريدة بعدد &#39;max&#95;rows&#95;to&#95;group&#95;by&#39;، وتجاهُل المفاتيح الأخرى بسبب &#39;group&#95;by&#95;overflow&#95;mode&#39; = &#39;any&#39;.
* `ProfileEvent_S3QueueSetFileProcessingMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرق لتعيين الملف على أنه قيد المعالجة
* `ProfileEvent_S3QueueSetFileProcessedMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرق لتعيين الملف على أنه تمت معالجته
* `ProfileEvent_S3QueueSetFileFailedMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرق لتعيين الملف على أنه فشل
* `ProfileEvent_ObjectStorageQueueFailedFiles` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الملفات التي تعذّرت معالجتها
* `ProfileEvent_ObjectStorageQueueProcessedFiles` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الملفات التي تمت معالجتها
* `ProfileEvent_ObjectStorageQueueCleanupMaxSetSizeOrTTLMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرق لتعيين الملف على أنه فشل
* `ProfileEvent_ObjectStorageQueuePullMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرق لقراءة بيانات الملف
* `ProfileEvent_ObjectStorageQueueFailedToBatchSetProcessing` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي فشل فيها طلب set processing request المجمّع
* `ProfileEvent_ObjectStorageQueueTrySetProcessingRequests` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي حاولنا فيها تنفيذ طلب set processing request
* `ProfileEvent_ObjectStorageQueueTrySetProcessingSucceeded` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي نجحنا فيها في تعيين الملف على أنه قيد المعالجة
* `ProfileEvent_ObjectStorageQueueTrySetProcessingFailed` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي أخفقنا فيها في تعيين الملف على أنه قيد المعالجة
* `ProfileEvent_ObjectStorageQueueListedFiles` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الملفات المُدرجة في StorageS3(Azure)Queue
* `ProfileEvent_ObjectStorageQueueFilteredFiles` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الملفات المُرشَّحة في StorageS3(Azure)Queue
* `ProfileEvent_ObjectStorageQueueReadFiles` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الملفات المقروءة (لا يساوي عدد الملفات المُدرجة فعليًا)
* `ProfileEvent_ObjectStorageQueueReadRows` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الصفوف المقروءة (لا يساوي عدد الصفوف المُدرجة فعليًا)
* `ProfileEvent_ObjectStorageQueueReadBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المقروءة (لا يساوي عدد البايتات المُدرجة فعليًا)
* `ProfileEvent_ObjectStorageQueueExceptionsDuringRead` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الاستثناءات أثناء القراءة في S3(Azure)Queue
* `ProfileEvent_ObjectStorageQueueExceptionsDuringInsert` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الاستثناءات أثناء الإدراج في S3(Azure)Queue
* `ProfileEvent_ObjectStorageQueueMovedObjects` ([UInt64](/ar/reference/data-types/int-uint)) — عدد العناصر التي نُقلت كجزء من after&#95;processing = move
* `ProfileEvent_ObjectStorageQueueRemovedObjects` ([UInt64](/ar/reference/data-types/int-uint)) — عدد العناصر التي أُزيلت كجزء من after&#95;processing = delete
* `ProfileEvent_ObjectStorageQueueTaggedObjects` ([UInt64](/ar/reference/data-types/int-uint)) — عدد العناصر التي وُسِمت كجزء من after&#95;processing = tag
* `ProfileEvent_ObjectStorageQueueInsertIterations` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات تكرار الإدراج
* `ProfileEvent_ObjectStorageQueueCommitRequests` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات Keeper لتنفيذ commit للملفات باعتبارها فاشلة أو مُعالجة
* `ProfileEvent_ObjectStorageQueueSuccessfulCommits` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات commit الناجحة في Keeper
* `ProfileEvent_ObjectStorageQueueUnsuccessfulCommits` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عمليات commit غير الناجحة في Keeper
* `ProfileEvent_ObjectStorageQueueCancelledFiles` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الملفات التي أُلغيت في StorageS3(Azure)Queue
* `ProfileEvent_ObjectStorageQueueProcessedRows` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الصفوف المُعالجة في StorageS3(Azure)Queue
* `ProfileEvent_ObjectStorageListedObjects` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الكائنات التي أعادتها واجهة برمجة تطبيقات سرد تخزين الكائنات قبل أي تصفية.
* `ProfileEvent_ObjectStorageGlobFilteredObjects` ([UInt64](/ar/reference/data-types/int-uint)) — الكائنات التي لم تطابق نمط glob أو Regex وتم تخطيها أثناء السرد.
* `ProfileEvent_ObjectStoragePredicateFilteredObjects` ([UInt64](/ar/reference/data-types/int-uint)) — الكائنات التي أُزيلت بواسطة تصفية الشرط على العمود الافتراضي &#95;path/&#95;file.
* `ProfileEvent_ObjectStorageReadObjects` ([UInt64](/ar/reference/data-types/int-uint)) — الكائنات التي فُتحت فعليًا للقراءة بواسطة مصدر تخزين الكائنات.
* `ProfileEvent_ServerStartupMilliseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المنقضي من بدء تشغيل الخادم حتى بدء الاستماع على المقابس، بالمللي ثانية
* `ProfileEvent_IOUringSQEsSubmitted` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لعناصر SQE الخاصة بـ io&#95;uring التي تم إرسالها
* `ProfileEvent_IOUringSQEsResubmitsAsync` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لعمليات إعادة إرسال SQE غير المتزامنة الخاصة بـ io&#95;uring التي تم تنفيذها
* `ProfileEvent_IOUringSQEsResubmitsSync` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لعمليات إعادة إرسال SQE المتزامنة الخاصة بـ io&#95;uring التي تم تنفيذها
* `ProfileEvent_IOUringCQEsCompleted` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لعناصر CQE الخاصة بـ io&#95;uring التي اكتملت بنجاح
* `ProfileEvent_IOUringCQEsFailed` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لعناصر CQE الخاصة بـ io&#95;uring التي اكتملت مع إخفاقات
* `ProfileEvent_BackupsOpenedForRead` ([UInt64](/ar/reference/data-types/int-uint)) — عدد النسخ الاحتياطية المفتوحة للقراءة
* `ProfileEvent_BackupsOpenedForWrite` ([UInt64](/ar/reference/data-types/int-uint)) — عدد النسخ الاحتياطية المفتوحة للكتابة
* `ProfileEvent_BackupsOpenedForUnlock` ([UInt64](/ar/reference/data-types/int-uint)) — عدد النسخ الاحتياطية المفتوحة لإلغاء القفل
* `ProfileEvent_BackupReadMetadataMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرق في قراءة البيانات الوصفية للنسخة الاحتياطية من ملف .backup
* `ProfileEvent_BackupWriteMetadataMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرق في كتابة البيانات الوصفية للنسخة الاحتياطية إلى ملف .backup
* `ProfileEvent_BackupEntriesCollectorMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرق في إنشاء إدخالات النسخة الاحتياطية
* `ProfileEvent_BackupEntriesCollectorForTablesDataMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرق في إنشاء إدخالات النسخة الاحتياطية لبيانات الجداول
* `ProfileEvent_BackupEntriesCollectorRunPostTasksMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرق في تشغيل المهام اللاحقة بعد إنشاء إدخالات النسخة الاحتياطية
* `ProfileEvent_BackupPreparingFileInfosMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرق في إعداد معلومات الملفات لإدخالات النسخة الاحتياطية
* `ProfileEvent_BackupReadLocalFilesToCalculateChecksums` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الملفات المقروءة محليًا لحساب checksums لإدخالات النسخة الاحتياطية
* `ProfileEvent_BackupReadLocalBytesToCalculateChecksums` ([UInt64](/ar/reference/data-types/int-uint)) — الحجم الإجمالي للملفات المقروءة محليًا لحساب قيم التحقق لإدخالات النسخ الاحتياطي
* `ProfileEvent_BackupReadRemoteFilesToCalculateChecksums` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الملفات المقروءة من الأقراص البعيدة لحساب قيم التحقق لإدخالات النسخ الاحتياطي
* `ProfileEvent_BackupReadRemoteBytesToCalculateChecksums` ([UInt64](/ar/reference/data-types/int-uint)) — الحجم الإجمالي للملفات المقروءة من الأقراص البعيدة لحساب قيم التحقق لإدخالات النسخ الاحتياطي
* `ProfileEvent_BackupLockFileReads` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات قراءة الملف &#39;.lock&#39; أثناء إنشاء النسخة الاحتياطية
* `ProfileEvent_RestorePartsSkippedFiles` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الملفات التي تم تخطيها أثناء استعادة الأجزاء
* `ProfileEvent_RestorePartsSkippedBytes` ([UInt64](/ar/reference/data-types/int-uint)) — الحجم الإجمالي للملفات التي تم تخطيها أثناء استعادة الأجزاء
* `ProfileEvent_ReadTaskRequestsReceived` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات الاستدعاء العكسي المطلوبة من الخادم البعيد إلى الخادم المُبادِر لاختيار مهمة القراءة (لدالة الجدول s3Cluster وما شابهها). يُقاس من جهة الخادم المُبادِر.
* `ProfileEvent_MergeTreeReadTaskRequestsReceived` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات الاستدعاء العكسي المطلوبة من الخادم البعيد إلى الخادم المُبادِر لاختيار مهمة القراءة (لجداول MergeTree). يُقاس من جهة الخادم المُبادِر.
* `ProfileEvent_ReadTaskRequestsSent` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات الاستدعاء العكسي المطلوبة من الخادم البعيد إلى الخادم المُبادِر لاختيار مهمة القراءة (لدالة الجدول s3Cluster وما شابهها). يُقاس من جهة الخادم البعيد.
* `ProfileEvent_MergeTreeReadTaskRequestsSent` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات الاستدعاء العكسي المطلوبة من الخادم البعيد إلى الخادم المُبادِر لاختيار مهمة القراءة (لجداول MergeTree). يُقاس من جهة الخادم البعيد.
* `ProfileEvent_MergeTreeAllRangesAnnouncementsSent` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الإعلانات المرسلة من الخادم البعيد إلى الخادم المُبادِر بشأن مجموعة أجزاء البيانات (لجداول MergeTree). يُقاس من جهة الخادم البعيد.
* `ProfileEvent_ReadTaskRequestsSentElapsedMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في طلبات الاستدعاء العكسي المطلوبة من الخادم البعيد إلى الخادم المُبادِر لاختيار مهمة القراءة (لدالة الجدول s3Cluster وما شابهها). يُقاس من جهة الخادم البعيد.
* `ProfileEvent_MergeTreeReadTaskRequestsSentElapsedMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في طلبات الاستدعاء العكسي المطلوبة من الخادم البعيد إلى الخادم المُبادِر لاختيار مهمة القراءة (لجداول MergeTree). يُقاس من جهة الخادم البعيد.
* `ProfileEvent_MergeTreeAllRangesAnnouncementsSentElapsedMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في إرسال الإعلان من الخادم البعيد إلى الخادم المُبادِر بشأن مجموعة أجزاء البيانات (لجداول MergeTree). يُقاس من جهة الخادم البعيد.
* `ProfileEvent_MergerMutatorsGetPartsForMergeElapsedMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق لالتقاط snapshot لأجزاء البيانات وبناء النطاقات منها.
* `ProfileEvent_MergerMutatorPrepareRangesForMergeElapsedMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في إعداد نطاقات الأجزاء التي يمكن دمجها وفقًا لشرط الدمج.
* `ProfileEvent_MergerMutatorSelectPartsForMergeElapsedMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في اختيار الأجزاء من النطاقات التي يمكن دمجها.
* `ProfileEvent_MergerMutatorRangesForMergeCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد النطاقات المرشحة للدمج
* `ProfileEvent_MergerMutatorPartsInRangesForMergeCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأجزاء المرشحة للدمج
* `ProfileEvent_MergerMutatorSelectRangePartsCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأجزاء في النطاق المحدد للدمج
* `ProfileEvent_ConnectionPoolIsFullMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في انتظار فتحة شاغرة في مجمع الاتصالات.
* `ProfileEvent_AsyncLoaderWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي قضاه الاستعلام في انتظار jobs الخاصة بالمُحمِّل غير المتزامن.
* `ProfileEvent_DistrCacheServerSwitches` ([UInt64](/ar/reference/data-types/int-uint)) — حدث مخزن القراءة لـ Distributed Cache. عدد مرات التبديل بين خوادم Distributed Cache في ذاكرة التخزين المؤقت للقراءة/الكتابة المباشرة
* `ProfileEvent_DistrCacheReadMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — حدث مخزن القراءة لـ Distributed Cache. الوقت المستغرَق في القراءة من Distributed Cache
* `ProfileEvent_DistrCacheFallbackReadMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — حدث مخزن القراءة لـ Distributed Cache. الوقت المستغرَق في القراءة من المخزن المؤقت البديل بدلًا من Distributed Cache
* `ProfileEvent_DistrCachePrecomputeRangesMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — حدث مخزن القراءة لـ Distributed Cache. الوقت المستغرَق في الحساب المسبق لنطاقات القراءة
* `ProfileEvent_DistrCacheNextImplMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — حدث مخزن القراءة لـ Distributed Cache. الوقت المستغرَق في ReadBufferFromDistributedCache::nextImpl
* `ProfileEvent_DistrCacheStartRangeMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — حدث مخزن القراءة لـ Distributed Cache. الوقت المستغرَق لبدء نطاق قراءة جديد باستخدام Distributed Cache
* `ProfileEvent_DistrCacheRangeChange` ([UInt64](/ar/reference/data-types/int-uint)) — حدث مخزن القراءة لـ Distributed Cache. عدد المرات التي غُيِّر فيها نطاق القراءة بسبب تغيّر seek/last&#95;position
* `ProfileEvent_DistrCacheRangeResetBackward` ([UInt64](/ar/reference/data-types/int-uint)) — حدث مخزن القراءة لـ Distributed Cache. عدد المرات التي أُعيد فيها ضبط نطاق القراءة بسبب تغيّر seek/last&#95;position
* `ProfileEvent_DistrCacheRangeResetForward` ([UInt64](/ar/reference/data-types/int-uint)) — حدث مخزن القراءة لـ Distributed Cache. عدد المرات التي أُعيد فيها ضبط نطاق القراءة بسبب تغيّر seek/last&#95;position
* `ProfileEvent_DistrCacheReconnectsAfterTimeout` ([UInt64](/ar/reference/data-types/int-uint)) — حدث مخزن القراءة لـ Distributed Cache. عدد محاولات إعادة الاتصال بعد انتهاء المهلة
* `ProfileEvent_DistrCacheServerUpdates` ([UInt64](/ar/reference/data-types/int-uint)) — حدث Distributed Cache. عدد مرات تبديل الخادم أثناء القراءة أو الكتابة لأن الخادم الذي اختاره hash قد تغيّر (على سبيل المثال، بسبب إلغاء تسجيل الخادم)
* `ProfileEvent_DistrCacheReadErrors` ([UInt64](/ar/reference/data-types/int-uint)) — حدث مخزن القراءة لـ Distributed Cache. عدد أخطاء Distributed Cache أثناء القراءة
* `ProfileEvent_DistrCacheWriteErrors` ([UInt64](/ar/reference/data-types/int-uint)) — حدث مخزن الكتابة لـ Distributed Cache. عدد أخطاء Distributed Cache أثناء الكتابة
* `ProfileEvent_DistrCacheWriteReconnectsAfterTimeout` ([UInt64](/ar/reference/data-types/int-uint)) — حدث مخزن الكتابة لـ Distributed Cache. عدد محاولات إعادة الاتصال بعد انتهاء المهلة
* `ProfileEvent_DistrCacheWriteMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — حدث مخزن الكتابة لـ Distributed Cache. الوقت المستغرَق في WriteBufferFromDistributedCache::writeToFileSegment
* `ProfileEvent_DistrCacheWriteBytes` ([UInt64](/ar/reference/data-types/int-uint)) — حدث مخزن الكتابة لـ Distributed Cache. عدد البايتات المكتوبة إلى Distributed Cache
* `ProfileEvent_DistrCacheObjectStorageWriteMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — حدث مخزن الكتابة لـ Distributed Cache. الوقت المستغرَق في الكتابة إلى تخزين الكائنات
* `ProfileEvent_DistrCacheObjectStorageWriteBytes` ([UInt64](/ar/reference/data-types/int-uint)) — حدث مخزن الكتابة لـ Distributed Cache. عدد البايتات المكتوبة إلى تخزين الكائنات
* `ProfileEvent_DistrCacheGetResponseMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — حدث عميل Distributed Cache. الوقت المستغرَق في انتظار الاستجابة من Distributed Cache
* `ProfileEvent_DistrCacheMakeRequestErrors` ([UInt64](/ar/reference/data-types/int-uint)) — حدث عميل Distributed Cache. عدد أخطاء Distributed Cache عند إرسال طلب
* `ProfileEvent_DistrCacheReceiveResponseErrors` ([UInt64](/ar/reference/data-types/int-uint)) — حدث عميل Distributed Cache. عدد أخطاء Distributed Cache عند تلقّي استجابة لطلب
* `ProfileEvent_DistrCacheReceivedDataPackets` ([UInt64](/ar/reference/data-types/int-uint)) — حدث عميل Distributed Cache. العدد الإجمالي لحزم البيانات المستلَمة من Distributed Cache
* `ProfileEvent_DistrCacheReceivedDataPacketsBytes` ([UInt64](/ar/reference/data-types/int-uint)) — حدث عميل Distributed Cache. عدد البايتات في حزم Data المستلَمة من Distributed Cache
* `ProfileEvent_DistrCacheReceivedOkPackets` ([UInt64](/ar/reference/data-types/int-uint)) — حدث عميل Distributed Cache. العدد الإجمالي لحزم Ok المستلَمة من Distributed Cache
* `ProfileEvent_DistrCacheReceivedErrorPackets` ([UInt64](/ar/reference/data-types/int-uint)) — حدث عميل لـ Distributed Cache. العدد الإجمالي لحزم Error المستلمة من Distributed Cache
* `ProfileEvent_DistrCacheReceivedCredentialsRefreshPackets` ([UInt64](/ar/reference/data-types/int-uint)) — حدث عميل لـ Distributed Cache. العدد الإجمالي لحزم RefreshCredentials المستلمة من Distributed Cache
* `ProfileEvent_DistrCacheReceivedStopPackets` ([UInt64](/ar/reference/data-types/int-uint)) — حدث عميل لـ Distributed Cache. العدد الإجمالي لحزم Stop المستلمة من Distributed Cache
* `ProfileEvent_DistrCacheSentDataPackets` ([UInt64](/ar/reference/data-types/int-uint)) — حدث عميل لـ Distributed Cache. العدد الإجمالي لحزم Data المرسلة إلى Distributed Cache
* `ProfileEvent_DistrCacheSentDataPacketsBytes` ([UInt64](/ar/reference/data-types/int-uint)) — حدث عميل لـ Distributed Cache. عدد البايتات في حزم Data المرسلة إلى Distributed Cache
* `ProfileEvent_DistrCacheUnusedPackets` ([UInt64](/ar/reference/data-types/int-uint)) — حدث عميل لـ Distributed Cache. عدد الحزم غير المستخدمة التي تم تخطيها من Distributed Cache
* `ProfileEvent_DistrCacheUnusedDataPacketsBytes` ([UInt64](/ar/reference/data-types/int-uint)) — حدث عميل لـ Distributed Cache. عدد البايتات في حزم Data التي تم تجاهلها
* `ProfileEvent_DistrCacheUnusedPacketsBufferAllocations` ([UInt64](/ar/reference/data-types/int-uint)) — حدث عميل لـ Distributed Cache. عدد عمليات التخصيص الإضافية للمخزن المؤقت عند تعذر إعادة استخدام المخزن المؤقت الحالي
* `ProfileEvent_DistrCacheLockRegistryMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — حدث سجل Distributed Cache. الوقت المستغرَق للحصول على قفل DistributedCacheRegistry
* `ProfileEvent_DistrCacheRegistryUpdateMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — حدث سجل Distributed Cache. الوقت المستغرَق في تحديث سجل Distributed Cache
* `ProfileEvent_DistrCacheRegistryUpdates` ([UInt64](/ar/reference/data-types/int-uint)) — حدث سجل Distributed Cache. عدد تحديثات سجل Distributed Cache
* `ProfileEvent_DistrCacheHashRingRebuilds` ([UInt64](/ar/reference/data-types/int-uint)) — حدث سجل Distributed Cache. عدد مرات إعادة بناء hash ring الخاص بـ Distributed Cache
* `ProfileEvent_DistrCacheSuccessfulRegistryUpdates` ([UInt64](/ar/reference/data-types/int-uint)) — حدث سجل Distributed Cache. عدد تحديثات سجل الخوادم الناجحة
* `ProfileEvent_DistrCacheUnsuccessfulRegistryUpdates` ([UInt64](/ar/reference/data-types/int-uint)) — حدث سجل Distributed Cache. عدد تحديثات سجل الخوادم غير الناجحة
* `ProfileEvent_DistrCacheReadBytesFromFallbackBuffer` ([UInt64](/ar/reference/data-types/int-uint)) — حدث read buffer لـ Distributed Cache. البايتات المقروءة من fallback buffer
* `ProfileEvent_DistrCacheOpenedConnections` ([UInt64](/ar/reference/data-types/int-uint)) — حدث connection لـ Distributed Cache. عدد الاتصالات المفتوحة إلى Distributed Cache
* `ProfileEvent_DistrCacheReusedConnections` ([UInt64](/ar/reference/data-types/int-uint)) — حدث connection لـ Distributed Cache. عدد الاتصالات المعاد استخدامها مع Distributed Cache
* `ProfileEvent_DistrCacheStaleReconnections` ([UInt64](/ar/reference/data-types/int-uint)) — حدث connection لـ Distributed Cache. عدد عمليات إعادة الاتصال بسبب الاتصالات المجمّعة stale (التي أغلقها النظير)
* `ProfileEvent_DistrCacheRemoveOutdatedMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — حدث connection لـ Distributed Cache. الوقت المستغرَق لإزالة الاتصالات outdated من الـ pool
* `ProfileEvent_DistrCacheOpenedConnectionsBypassingPool` ([UInt64](/ar/reference/data-types/int-uint)) — حدث connection لـ Distributed Cache. عدد الاتصالات المفتوحة إلى Distributed Cache مع تجاوز الـ pool
* `ProfileEvent_DistrCacheConnectMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — حدث connection لـ Distributed Cache. الوقت المستغرَق للاتصال بـ Distributed Cache
* `ProfileEvent_DistrCacheConnectAttempts` ([UInt64](/ar/reference/data-types/int-uint)) — حدث connection لـ Distributed Cache. عدد محاولات الاتصال بـ Distributed Cache
* `ProfileEvent_DistrCacheSuccessfulConnectAttempts` ([UInt64](/ar/reference/data-types/int-uint)) — حدث connection لـ Distributed Cache. عدد محاولات الاتصال الناجحة بـ Distributed Cache
* `ProfileEvent_DistrCacheUnsuccessfulConnectAttempts` ([UInt64](/ar/reference/data-types/int-uint)) — حدث connection لـ Distributed Cache. عدد محاولات الاتصال غير الناجحة بـ Distributed Cache
* `ProfileEvent_DistrCacheGetClientMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — حدث connection لـ Distributed Cache. الوقت المستغرَق للحصول على client لـ Distributed Cache
* `ProfileEvent_DistrCacheTemporaryFilesCreated` ([UInt64](/ar/reference/data-types/int-uint)) — حدث اتصال Distributed Cache. عدد الملفات المؤقتة التي أُنشئت في Distributed Cache
* `ProfileEvent_DistrCacheTemporaryFilesBytesWritten` ([UInt64](/ar/reference/data-types/int-uint)) — حدث اتصال Distributed Cache. عدد البايتات المكتوبة إلى الملفات المؤقتة التي أُنشئت في Distributed Cache
* `ProfileEvent_DistrCacheServerProcessRequestMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — حدث خادم Distributed Cache. الوقت المستغرَق في معالجة الطلب على جانب خادم DistributedCache
* `ProfileEvent_DistrCacheServerStartRequestPackets` ([UInt64](/ar/reference/data-types/int-uint)) — حدث خادم Distributed Cache. عدد حزم StartRequest في DistributedCacheServer
* `ProfileEvent_DistrCacheServerContinueRequestPackets` ([UInt64](/ar/reference/data-types/int-uint)) — حدث خادم Distributed Cache. عدد حزم ContinueRequest في DistributedCacheServer
* `ProfileEvent_DistrCacheServerEndRequestPackets` ([UInt64](/ar/reference/data-types/int-uint)) — حدث خادم Distributed Cache. عدد حزم EndRequest في DistributedCacheServer
* `ProfileEvent_DistrCacheServerReceivedCredentialsRefreshPackets` ([UInt64](/ar/reference/data-types/int-uint)) — حدث خادم Distributed Cache. عدد حزم العميل RefreshCredentials في DistributedCacheServer
* `ProfileEvent_DistrCacheServerAckRequestPackets` ([UInt64](/ar/reference/data-types/int-uint)) — حدث خادم Distributed Cache. عدد حزم AckRequest في DistributedCacheServer
* `ProfileEvent_DistrCacheServerNewS3CachedClients` ([UInt64](/ar/reference/data-types/int-uint)) — حدث خادم Distributed Cache. عدد عملاء S3 الجدد المخزَّنين مؤقتًا
* `ProfileEvent_DistrCacheServerReusedS3CachedClients` ([UInt64](/ar/reference/data-types/int-uint)) — حدث خادم Distributed Cache. عدد عملاء S3 المخزَّنين مؤقتًا الذين أُعيد استخدامهم
* `ProfileEvent_DistrCacheServerCredentialsRefresh` ([UInt64](/ar/reference/data-types/int-uint)) — حدث خادم Distributed Cache. عدد مرات تحديث بيانات الاعتماد منتهية الصلاحية
* `ProfileEvent_DistrCacheServerCachedReadBufferCacheHits` ([UInt64](/ar/reference/data-types/int-uint)) — حدث خادم Distributed Cache. عدد مرات العثور على البيانات في الذاكرة المؤقتة في Distributed Cache أثناء القراءة من filesystem cache
* `ProfileEvent_DistrCacheServerCachedReadBufferCacheMisses` ([UInt64](/ar/reference/data-types/int-uint)) — حدث خادم Distributed Cache. عدد مرات عدم العثور على البيانات في الذاكرة المؤقتة في Distributed Cache أثناء القراءة من filesystem cache
* `ProfileEvent_DistrCacheServerCachedReadBufferCacheWrittenBytes` ([UInt64](/ar/reference/data-types/int-uint)) — حدث خادم Distributed Cache. عدد البايتات المكتوبة إلى الذاكرة المؤقتة في Distributed Cache أثناء القراءة من filesystem cache
* `ProfileEvent_DistrCacheServerCachedReadBufferCacheReadBytes` ([UInt64](/ar/reference/data-types/int-uint)) — حدث خادم Distributed Cache. عدد البايتات المقروءة من الذاكرة المؤقتة في Distributed Cache أثناء القراءة من filesystem cache
* `ProfileEvent_DistrCacheServerCachedReadBufferObjectStorageReadBytes` ([UInt64](/ar/reference/data-types/int-uint)) — حدث خادم Distributed Cache. عدد البايتات المقروءة من object storage في Distributed Cache أثناء القراءة من filesystem cache
* `ProfileEvent_DistrCacheServerCachedReadBufferCachePredownloadBytes` ([UInt64](/ar/reference/data-types/int-uint)) — حدث خادم Distributed Cache. عدد البايتات المقروءة من object storage للتنزيل المسبق في Distributed Cache أثناء القراءة من filesystem cache
* `ProfileEvent_DistrCacheServerSkipped` ([UInt64](/ar/reference/data-types/int-uint)) — حدث خادم Distributed Cache. عدد المرات التي تم فيها تخطي خادم Distributed Cache بسبب محاولات اتصال فاشلة سابقة
* `ProfileEvent_LogTest` ([UInt64](/ar/reference/data-types/int-uint)) — عدد رسائل السجل ذات المستوى Test
* `ProfileEvent_LogTrace` ([UInt64](/ar/reference/data-types/int-uint)) — عدد رسائل السجل ذات المستوى Trace
* `ProfileEvent_LogDebug` ([UInt64](/ar/reference/data-types/int-uint)) — عدد رسائل السجل ذات المستوى Debug
* `ProfileEvent_LogInfo` ([UInt64](/ar/reference/data-types/int-uint)) — عدد رسائل السجل ذات المستوى Info
* `ProfileEvent_LogWarning` ([UInt64](/ar/reference/data-types/int-uint)) — عدد رسائل السجل ذات المستوى Warning
* `ProfileEvent_LogError` ([UInt64](/ar/reference/data-types/int-uint)) — عدد رسائل السجل ذات المستوى Error
* `ProfileEvent_LogFatal` ([UInt64](/ar/reference/data-types/int-uint)) — عدد رسائل السجل ذات المستوى Fatal
* `ProfileEvent_LoggerElapsedNanoseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت التراكمي المستغرَق في التسجيل
* `ProfileEvent_InterfaceHTTPSendBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المُرسلة عبر واجهات HTTP
* `ProfileEvent_InterfaceHTTPReceiveBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المُستلمة عبر واجهات HTTP
* `ProfileEvent_InterfaceNativeSendBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المُرسلة عبر الواجهات الأصلية
* `ProfileEvent_InterfaceNativeReceiveBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المُستلمة عبر الواجهات الأصلية
* `ProfileEvent_InterfacePrometheusSendBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المُرسلة عبر واجهات Prometheus
* `ProfileEvent_InterfacePrometheusReceiveBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المُستلمة عبر واجهات Prometheus
* `ProfileEvent_InterfaceInterserverSendBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المُرسلة عبر الواجهات بين الخوادم
* `ProfileEvent_InterfaceInterserverReceiveBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المُستلمة عبر الواجهات بين الخوادم
* `ProfileEvent_InterfaceMySQLSendBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المُرسلة عبر واجهات MySQL
* `ProfileEvent_InterfaceMySQLReceiveBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المُستلمة عبر واجهات MySQL
* `ProfileEvent_InterfacePostgreSQLSendBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المُرسلة عبر واجهات PostgreSQL
* `ProfileEvent_InterfacePostgreSQLReceiveBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المُستلمة عبر واجهات PostgreSQL
* `ProfileEvent_ParallelReplicasUsedCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد النسخ المتماثلة المستخدمة لتنفيذ استعلام باستخدام النسخ المتماثلة المتوازية المستندة إلى المهام
* `ProfileEvent_ParallelReplicasAvailableCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد النسخ المتماثلة المتاحة لتنفيذ استعلام باستخدام النسخ المتماثلة المتوازية المستندة إلى المهام
* `ProfileEvent_ParallelReplicasUnavailableCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد النسخ المتماثلة التي اختيرت، لكن تبيّن عدم توفرها أثناء تنفيذ الاستعلام باستخدام النسخ المتماثلة المتوازية المستندة إلى المهام
* `ProfileEvent_SharedMergeTreeVirtualPartsUpdates` ([UInt64](/ar/reference/data-types/int-uint)) — عدد تحديثات الأجزاء الافتراضية
* `ProfileEvent_SharedMergeTreeVirtualPartsUpdatesByLeader` ([UInt64](/ar/reference/data-types/int-uint)) — تحديثات الأجزاء الافتراضية بواسطة القائد
* `ProfileEvent_SharedMergeTreeVirtualPartsUpdateMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت بالميكروثانية لتحديث الأجزاء الافتراضية
* `ProfileEvent_SharedMergeTreeVirtualPartsUpdatesFromZooKeeper` ([UInt64](/ar/reference/data-types/int-uint)) — عدد تحديثات الأجزاء الافتراضية من ZooKeeper
* `ProfileEvent_SharedMergeTreeVirtualPartsUpdatesFromZooKeeperMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت بالميكروثانية لتحديثات الأجزاء الافتراضية من ZooKeeper
* `ProfileEvent_SharedMergeTreeVirtualPartsUpdatesPeerNotFound` ([UInt64](/ar/reference/data-types/int-uint)) — فشلت تحديثات الأجزاء الافتراضية من النظير بسبب عدم العثور على أي نظير
* `ProfileEvent_SharedMergeTreeVirtualPartsUpdatesFromPeer` ([UInt64](/ar/reference/data-types/int-uint)) — عدد تحديثات الأجزاء الافتراضية من النظير
* `ProfileEvent_SharedMergeTreeVirtualPartsUpdatesFromPeerMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت بالميكروثانية لتحديثات الأجزاء الافتراضية من النظير
* `ProfileEvent_SharedMergeTreeVirtualPartsUpdatesForMergesOrStatus` ([UInt64](/ar/reference/data-types/int-uint)) — تحديثات الأجزاء الافتراضية من مهمة في الخلفية غير default
* `ProfileEvent_SharedMergeTreeVirtualPartsUpdatesLeaderFailedElection` ([UInt64](/ar/reference/data-types/int-uint)) — فشل انتخاب قائد تحديثات الأجزاء الافتراضية
* `ProfileEvent_SharedMergeTreeVirtualPartsUpdatesLeaderSuccessfulElection` ([UInt64](/ar/reference/data-types/int-uint)) — نجح انتخاب قائد تحديثات الأجزاء الافتراضية
* `ProfileEvent_SharedMergeTreeMergeMutationAssignmentAttempt` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات محاولة إسناد merge أو mutation
* `ProfileEvent_SharedMergeTreeMergeMutationAssignmentFailedWithNothingToDo` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات محاولة إسناد merge أو mutation التي فشلت لعدم وجود ما يمكن دمجه
* `ProfileEvent_SharedMergeTreeMergeMutationAssignmentFailedWithConflict` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات محاولة إسناد merge أو mutation التي فشلت بسبب تعارض في Keeper
* `ProfileEvent_SharedMergeTreeMergeMutationAssignmentSuccessful` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات نجاح إسناد merge أو mutation
* `ProfileEvent_SharedMergeTreeMergePartsMovedToOudated` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأجزاء التي نُقلت إلى مجلد الأجزاء المتقادمة
* `ProfileEvent_SharedMergeTreeMergePartsMovedToCondemned` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأجزاء التي نُقلت إلى مجلد الأجزاء المحكوم بإزالتها
* `ProfileEvent_SharedMergeTreeOutdatedPartsConfirmationRequest` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات ZooKeeper المستخدمة لتأكيد الأجزاء المتقادمة
* `ProfileEvent_SharedMergeTreeOutdatedPartsConfirmationInvocations` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات الاستدعاء التي أُجريت لتأكيد الأجزاء المتقادمة
* `ProfileEvent_SharedMergeTreeOutdatedPartsHTTPRequest` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات HTTP التي أُرسلت لتأكيد الأجزاء المتقادمة
* `ProfileEvent_SharedMergeTreeOutdatedPartsHTTPResponse` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استجابات HTTP التي أُرسلت لتأكيد الأجزاء المتقادمة
* `ProfileEvent_SharedMergeTreeCondemnedPartsKillRequest` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات ZooKeeper المستخدمة لإزالة الأجزاء المحكوم بإزالتها
* `ProfileEvent_SharedMergeTreeCondemnedPartsLockConflict` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات فشل الحصول على القفل بسبب تعارض
* `ProfileEvent_SharedMergeTreeCondemnedPartsRemoved` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأجزاء المحكوم بإزالتها التي أُزيلت
* `ProfileEvent_SharedMergeTreePartsKillerRuns` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات تشغيل parts killer
* `ProfileEvent_SharedMergeTreePartsKillerMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — مقدار الوقت الذي يستغرقه الخيط الرئيسي لـ parts killer، بالميكروثانية
* `ProfileEvent_SharedMergeTreePartsKillerParts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأجزاء التي جدولت لإزالتها بواسطة killer
* `ProfileEvent_SharedMergeTreePartsKillerPartsMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — مقدار الوقت الذي تستغرقه إزالة الأجزاء، بالميكروثانية (تُنفَّذ عبر عدة threads)
* `ProfileEvent_SharedMergeTreeMergeSelectingTaskMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الميكروثواني لمهمة اختيار الدمج في SMT
* `ProfileEvent_SharedMergeTreeReplicaSetUpdateTaskRuns` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات تشغيل updateReplicaSetTask
* `ProfileEvent_SharedMergeTreeOptimizeAsync` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استعلامات OPTIMIZE غير المتزامنة التي نُفِّذت
* `ProfileEvent_SharedMergeTreeOptimizeSync` ([UInt64](/ar/reference/data-types/int-uint)) — عدد استعلامات OPTIMIZE المتزامنة التي نُفِّذت
* `ProfileEvent_SharedMergeTreeScheduleDataProcessingJob` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استدعاء scheduleDataProcessingJob/
* `ProfileEvent_SharedMergeTreeScheduleDataProcessingJobNothingToScheduled` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استدعاء scheduleDataProcessingJob دون وجود أي عمل للتنفيذ
* `ProfileEvent_SharedMergeTreeScheduleDataProcessingJobMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — وقت تنفيذ scheduleDataProcessingJob
* `ProfileEvent_SharedMergeTreeHandleBlockingParts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأجزاء المُعيقة المطلوب التعامل معها في scheduleDataProcessingJob
* `ProfileEvent_SharedMergeTreeHandleBlockingPartsMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — وقت التعامل مع الأجزاء المُعيقة في scheduleDataProcessingJob
* `ProfileEvent_SharedMergeTreeHandleFetchPartsMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — وقت التعامل مع الأجزاء التي تم جلبها في scheduleDataProcessingJob
* `ProfileEvent_SharedMergeTreeHandleOutdatedParts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأجزاء المتقادمة المطلوب التعامل معها في scheduleDataProcessingJob
* `ProfileEvent_SharedMergeTreeHandleOutdatedPartsMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — وقت التعامل مع الأجزاء المتقادمة في scheduleDataProcessingJob
* `ProfileEvent_SharedMergeTreeSelectPartsForRendezvousFetchMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — وقت selectPartsForRendezvousFetch
* `ProfileEvent_SharedMergeTreeSelectPartsForRendezvousFetchParts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأجزاء التي حدّدها selectPartsForRendezvousFetch
* `ProfileEvent_SharedMergeTreeSelectPartsForCoordinatedFetchMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — وقت selectPartsForCoordinatedFetch
* `ProfileEvent_SharedMergeTreeSelectPartsForCoordinatedFetchParts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأجزاء التي حدّدها selectPartsForCoordinatedFetch
* `ProfileEvent_SharedMergeTreeSelectPartsForFullFetchMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — وقت selectPartsForFullFetch
* `ProfileEvent_SharedMergeTreeSelectPartsForFullFetchParts` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الأجزاء التي حدّدها selectPartsForFullFetch
* `ProfileEvent_SharedMergeTreeTryUpdateDiskMetadataCacheForPartMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — وقت tryUpdateDiskMetadataCacheForPart في scheduleDataProcessingJob
* `ProfileEvent_SharedMergeTreeLoadChecksumAndIndexesMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — وقت loadColumnsChecksumsIndexes في SharedMergeTree فقط
* `ProfileEvent_SharedMergeTreeSnapshotPartsCleanRequest` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي يقرّر فيها SnapshotCleanerThread تنظيف جزء
* `ProfileEvent_SharedMergeTreeSnapshotPartsCleanerParts` ([UInt64](/ar/reference/data-types/int-uint)) — المدة التي يحاول خلالها SnapshotCleanerThread تنظيف جزء
* `ProfileEvent_SharedMergeTreeSnapshotPartsRemoved` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي ينجح فيها SnapshotCleanerThread في تنظيف جزء
* `ProfileEvent_SharedMergeTreeSnapshotPartsCleanerRuns` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات تشغيل SnapshotCleanerThread
* `ProfileEvent_SharedMergeTreeSnapshotPartsCleanerMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — مدة تشغيل SnapshotCleanerThread
* `ProfileEvent_SharedMergeTreeSnapshotPartsCleanerPartsMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — المدة التي يستغرقها SnapshotCleanerThread لتنظيف الأجزاء
* `ProfileEvent_SharedMergeTreeDataPartsFetchAttempt` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات محاولة جلب أجزاء البيانات
* `ProfileEvent_SharedMergeTreeDataPartsFetchFromPeer` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي جُلبت فيها أجزاء البيانات من النظير
* `ProfileEvent_SharedMergeTreeDataPartsFetchFromPeerMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — زمن جلب أجزاء البيانات من النظير بالميكروثانية
* `ProfileEvent_SharedMergeTreeDataPartsFetchFromS3` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي جُلبت فيها أجزاء البيانات من S3
* `ProfileEvent_SharedMergeTreeReplicaSetUpdatesFromZooKeeper` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي جرى فيها تحديث مجموعة النسخ المتماثلة من ZooKeeper
* `ProfileEvent_SharedMergeTreeReplicaSetUpdatesFromZooKeeperRequests` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي عدد طلبات ZooKeeper التي أُجريت لتحديث مجموعة النسخ المتماثلة
* `ProfileEvent_SharedMergeTreeReplicaSetUpdatesFromZooKeeperMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — مقدار الوقت المستغرَق لتحديث مجموعة النسخ المتماثلة
* `ProfileEvent_KeeperLogsEntryReadFromLatestCache` ([UInt64](/ar/reference/data-types/int-uint)) — عدد إدخالات السجل في Keeper التي تمت قراءتها من ذاكرة التخزين المؤقت لأحدث السجلات
* `ProfileEvent_KeeperLogsEntryReadFromCommitCache` ([UInt64](/ar/reference/data-types/int-uint)) — عدد إدخالات السجل في Keeper التي تمت قراءتها من ذاكرة التخزين المؤقت لسجلات commit
* `ProfileEvent_KeeperLogsEntryReadFromFile` ([UInt64](/ar/reference/data-types/int-uint)) — عدد إدخالات السجل في Keeper التي تمت قراءتها مباشرةً من ملف changelog
* `ProfileEvent_KeeperLogsPrefetchedEntries` ([UInt64](/ar/reference/data-types/int-uint)) — عدد إدخالات السجل في Keeper التي جرى prefetch لها من ملف changelog
* `ProfileEvent_KeeperChangelogWrittenBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المكتوبة إلى changelog في Keeper
* `ProfileEvent_KeeperChangelogFileSyncMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في `fsync` لـ changelog الخاص بـ Keeper (السجلات غير المضغوطة فقط)
* `ProfileEvent_KeeperSnapshotWrittenBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات المكتوبة إلى ملفات snapshot في Keeper
* `ProfileEvent_KeeperSnapshotFileSyncMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرَق في `fsync` لملفات snapshot الخاصة بـ Keeper
* `ProfileEvent_StorageConnectionsCreated` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الاتصالات المُنشأة لوحدات التخزين
* `ProfileEvent_StorageConnectionsReused` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الاتصالات المُعاد استخدامها لوحدات التخزين
* `ProfileEvent_StorageConnectionsReset` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الاتصالات التي أُعيد تعيينها لوحدات التخزين
* `ProfileEvent_StorageConnectionsPreserved` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الاتصالات التي تم الحفاظ عليها لوحدات التخزين
* `ProfileEvent_StorageConnectionsExpired` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الاتصالات منتهية الصلاحية لوحدات التخزين
* `ProfileEvent_StorageConnectionsErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الحالات التي فشل فيها إنشاء اتصال لوحدة تخزين
* `ProfileEvent_StorageConnectionsElapsedMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في إنشاء الاتصالات لوحدات التخزين
* `ProfileEvent_DiskConnectionsCreated` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الاتصالات المُنشأة للقرص
* `ProfileEvent_DiskConnectionsReused` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الاتصالات المُعاد استخدامها للقرص
* `ProfileEvent_DiskConnectionsReset` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الاتصالات التي أُعيد تعيينها للقرص
* `ProfileEvent_DiskConnectionsPreserved` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الاتصالات التي تم الحفاظ عليها للقرص
* `ProfileEvent_DiskConnectionsExpired` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الاتصالات منتهية الصلاحية للقرص
* `ProfileEvent_DiskConnectionsErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الحالات التي فشل فيها إنشاء اتصال للقرص
* `ProfileEvent_DiskConnectionsElapsedMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في إنشاء الاتصالات للقرص
* `ProfileEvent_HTTPConnectionsCreated` ([UInt64](/ar/reference/data-types/int-uint)) — عدد اتصالات HTTP الخاصة بالعميل التي تم إنشاؤها
* `ProfileEvent_HTTPConnectionsReused` ([UInt64](/ar/reference/data-types/int-uint)) — عدد اتصالات HTTP من جهة العميل التي أُعيد استخدامها
* `ProfileEvent_HTTPConnectionsReset` ([UInt64](/ar/reference/data-types/int-uint)) — عدد اتصالات HTTP من جهة العميل التي أُعيد ضبطها
* `ProfileEvent_HTTPConnectionsPreserved` ([UInt64](/ar/reference/data-types/int-uint)) — عدد اتصالات HTTP من جهة العميل التي جرى الحفاظ عليها
* `ProfileEvent_HTTPConnectionsExpired` ([UInt64](/ar/reference/data-types/int-uint)) — عدد اتصالات HTTP من جهة العميل التي انتهت صلاحيتها
* `ProfileEvent_HTTPConnectionsErrors` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الحالات التي فشل فيها إنشاء اتصال HTTP من جهة العميل
* `ProfileEvent_HTTPConnectionsElapsedMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرَق في إنشاء اتصالات HTTP من جهة العميل
* `ProfileEvent_HTTPServerConnectionsCreated` ([UInt64](/ar/reference/data-types/int-uint)) — عدد اتصالات HTTP من جهة الخادم التي أُنشئت
* `ProfileEvent_HTTPServerConnectionsReused` ([UInt64](/ar/reference/data-types/int-uint)) — عدد اتصالات HTTP من جهة الخادم التي أُعيد استخدامها
* `ProfileEvent_HTTPServerConnectionsPreserved` ([UInt64](/ar/reference/data-types/int-uint)) — عدد اتصالات HTTP من جهة الخادم التي جرى الحفاظ عليها. جرى الإبقاء على الاتصال حيًا بنجاح
* `ProfileEvent_HTTPServerConnectionsExpired` ([UInt64](/ar/reference/data-types/int-uint)) — عدد اتصالات HTTP من جهة الخادم التي انتهت صلاحيتها.
* `ProfileEvent_HTTPServerConnectionsClosed` ([UInt64](/ar/reference/data-types/int-uint)) — عدد اتصالات HTTP من جهة الخادم التي أُغلقت. لم يتم التفاوض على Keep alive
* `ProfileEvent_HTTPServerConnectionsReset` ([UInt64](/ar/reference/data-types/int-uint)) — عدد اتصالات HTTP من جهة الخادم التي أُعيد ضبطها. الخادم يغلق الاتصال
* `ProfileEvent_AddressesDiscovered` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للعناوين الجديدة في نتائج تحليل DNS لاتصالات HTTP
* `ProfileEvent_AddressesExpired` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للعناوين منتهية الصلاحية التي لم تعد تظهر في نتائج تحليل DNS لاتصالات HTTP
* `ProfileEvent_AddressesMarkedAsFailed` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للعناوين التي وُسِمت على أنها معطوبة بسبب أخطاء الاتصال لاتصالات HTTP
* `ProfileEvent_ReadWriteBufferFromHTTPRequestsSent` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات HTTP التي أرسلها ReadWriteBufferFromHTTP
* `ProfileEvent_ReadWriteBufferFromHTTPBytes` ([UInt64](/ar/reference/data-types/int-uint)) — الحجم الإجمالي لبايتات payload المستلمة والمرسلة بواسطة ReadWriteBufferFromHTTP. لا يشمل ترويسات HTTP.
* `ProfileEvent_WriteBufferFromHTTPRequestsSent` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات HTTP التي أرسلها WriteBufferFromHTTP
* `ProfileEvent_WriteBufferFromHTTPBytes` ([UInt64](/ar/reference/data-types/int-uint)) — الحجم الإجمالي لبايتات payload المستلمة والمرسلة بواسطة WriteBufferFromHTTP. لا يشمل ترويسات HTTP.
* `ProfileEvent_ConcurrencyControlSlotsGranted` ([UInt64](/ar/reference/data-types/int-uint)) — عدد CPU slot الممنوحة وفقًا لضمان thread واحدة لكل query، وللاستعلامات التي يكون فيها الإعداد &#39;use&#95;concurrency&#95;control&#39; = 0
* `ProfileEvent_ConcurrencyControlSlotsDelayed` ([UInt64](/ar/reference/data-types/int-uint)) — عدد CPU slot التي لم تُمنح مبدئيًا واضطرت إلى انتظار CPU slot شاغرة
* `ProfileEvent_ConcurrencyControlSlotsAcquired` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لـ CPU slots المكتسَبة
* `ProfileEvent_ConcurrencyControlSlotsAcquiredNonCompeting` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لـ CPU slot غير المتنافس عليها التي تم اكتسابها
* `ProfileEvent_ConcurrencyControlQueriesDelayed` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لتخصيصات CPU slot (queries) التي اضطرت إلى انتظار slots للتوسّع
* `ProfileEvent_ConcurrencyControlWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي كانت فيه query تنتظر طلبات الموارد الخاصة بـ CPU slots.
* `ProfileEvent_ConcurrencyControlPreemptedMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي انتظر فيه الاستعلام بسبب استباق فتحات CPU.
* `ProfileEvent_ConcurrencyControlPreemptions` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لعمليات استباق CPU
* `ProfileEvent_ConcurrencyControlUpscales` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لأحداث التوسيع التصاعدي لـ CPU
* `ProfileEvent_ConcurrencyControlDownscales` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لأحداث التوسيع التنازلي لـ CPU
* `ProfileEvent_ConcurrentQuerySlotsAcquired` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لفتحات الاستعلام التي تم الحصول عليها
* `ProfileEvent_ConcurrentQueryWaitMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي انتظره الاستعلام للحصول على فتحات الاستعلام
* `ProfileEvent_CoordinatedMergesMergeCoordinatorUpdateCount` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لتحديثات منسق الدمج
* `ProfileEvent_CoordinatedMergesMergeCoordinatorUpdateMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرق في تحديث حالة منسق الدمج
* `ProfileEvent_CoordinatedMergesMergeCoordinatorFetchMetadataMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرق في جلب البيانات الوصفية الأحدث داخل منسق الدمج
* `ProfileEvent_CoordinatedMergesMergeCoordinatorFilterMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرق في تصفية عمليات الدمج المُحضَّرة داخل منسق الدمج
* `ProfileEvent_CoordinatedMergesMergeCoordinatorSelectMergesMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرق في العثور على عملية دمج باستخدام محددات الدمج داخل منسق الدمج
* `ProfileEvent_CoordinatedMergesMergeCoordinatorLockStateForShareCount` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لمرات أخذ قفل حالة المنسق بوضع المشاركة
* `ProfileEvent_CoordinatedMergesMergeCoordinatorLockStateExclusivelyCount` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لمرات أخذ قفل حالة المنسق حصريًا
* `ProfileEvent_CoordinatedMergesMergeCoordinatorLockStateForShareMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرق في قفل mutex حالة المنسق بوضع المشاركة
* `ProfileEvent_CoordinatedMergesMergeCoordinatorLockStateExclusivelyMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرق في قفل mutex حالة المنسق حصريًا
* `ProfileEvent_CoordinatedMergesMergeWorkerUpdateCount` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لتحديثات عامل الدمج
* `ProfileEvent_CoordinatedMergesMergeWorkerUpdateMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرق في تحديث الحالة المحلية لعمليات الدمج المسندة إلى العامل
* `ProfileEvent_CoordinatedMergesMergeAssignmentRequest` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لطلبات إسناد الدمج
* `ProfileEvent_CoordinatedMergesMergeAssignmentResponse` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لاستجابات إسناد الدمج
* `ProfileEvent_CoordinatedMergesMergeAssignmentRequestMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرق في عميل إسناد الدمج
* `ProfileEvent_CoordinatedMergesMergeAssignmentResponseMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرق في معالج إسناد الدمج
* `ProfileEvent_SharedDatabaseCatalogFailedToApplyState` ([UInt64](/ar/reference/data-types/int-uint)) — عدد حالات الفشل في تطبيق الحالة الجديدة في SharedDatabaseCatalog
* `ProfileEvent_SharedDatabaseCatalogStateApplicationMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت المستغرق في تطبيق الحالة الجديدة في SharedDatabaseCatalog
* `ProfileEvent_MemoryWorkerRun` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات التشغيل التي نفذها MemoryWorker في الخلفية
* `ProfileEvent_MemoryWorkerRunElapsedMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الوقت الذي استغرقه MemoryWorker في العمل في الخلفية
* `ProfileEvent_ParquetFetchWaitTimeMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — وقت انتظار قراءات ملف Parquet من خيوط فك الترميز (وليس خيوط الجلب المسبق)
* `ProfileEvent_WasmSerializationMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرق في تنفيذ شيفرة WebAssembly
* `ProfileEvent_WasmDeserializationMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرق في تنفيذ شيفرة WebAssembly
* `ProfileEvent_WasmGuestExecuteMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرق في تنفيذ شيفرة WebAssembly
* `ProfileEvent_WasmTotalExecuteMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المستغرق في تنفيذ شيفرة WebAssembly
* `ProfileEvent_WasmModuleInstatiate` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مقصورات WebAssembly التي تم إنشاؤها
* `ProfileEvent_WasmMemoryAllocated` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي الذاكرة المخصصة لمقصورات WebAssembly
* `ProfileEvent_ParquetReadRowGroups` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لمجموعات الصفوف المقروءة من بيانات Parquet
* `ProfileEvent_ParquetPrunedRowGroups` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لمجموعات الصفوف التي تم استبعادها من بيانات Parquet
* `ProfileEvent_ParquetDecodingTasks` ([UInt64](/ar/reference/data-types/int-uint)) — المهام التي أصدرها قارئ Parquet
* `ProfileEvent_ParquetDecodingTaskBatches` ([UInt64](/ar/reference/data-types/int-uint)) — مجموعات المهام التي أرسلها قارئ Parquet إلى مجمّع خيوط
* `ProfileEvent_ParquetPrefetcherReadRandomRead` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لعمليات القراءة باستخدام ReadMode::RandomRead بواسطة DB::Parquet::Prefetcher
* `ProfileEvent_ParquetPrefetcherReadSeekAndRead` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لعمليات القراءة باستخدام ReadMode::SeekAndRead بواسطة DB::Parquet::Prefetcher
* `ProfileEvent_ParquetPrefetcherReadEntireFile` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لعمليات القراءة باستخدام ReadMode::EntireFileIsInMemory بواسطة DB::Parquet::Prefetcher
* `ProfileEvent_ParquetRowsFilterExpression` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للصفوف التي مرّت عبر عامل التصفية
* `ProfileEvent_ParquetColumnsFilterExpression` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للأعمدة التي مرّت عبر عامل التصفية
* `ProfileEvent_FilterTransformPassedRows` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الصفوف التي اجتازت عامل التصفية في الاستعلام
* `ProfileEvent_FilterTransformPassedBytes` ([UInt64](/ar/reference/data-types/int-uint)) — عدد البايتات التي اجتازت عامل التصفية في الاستعلام
* `ProfileEvent_QueryPreempted` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي تُوقَف فيها المهام مؤقتًا وتنتظر بسبب الإعداد &#39;priority&#39;
* `ProfileEvent_IndexBinarySearchAlgorithm` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استخدام خوارزمية البحث الثنائي على علامات الفهرس
* `ProfileEvent_IndexGenericExclusionSearchAlgorithm` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات استخدام خوارزمية البحث العامة بالاستبعاد على علامات الفهرس
* `ProfileEvent_ParallelReplicasQueryCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الاستعلامات (الفرعية) المنفذة باستخدام النسخ المتماثلة المتوازية أثناء تنفيذ الاستعلام
* `ProfileEvent_DistributedConnectionReconnectCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات إعادة الاتصال بالخوادم الأخرى أثناء تنفيذ الاستعلامات الموزعة. وقد يحدث ذلك عند الحصول على اتصال غير محدّث من مجمّع الاتصالات
* `ProfileEvent_DistributedConnectionConnectCount` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات الاتصال بالخوادم الأخرى أثناء تنفيذ الاستعلامات الموزعة. ويحدث ذلك عند إنشاء اتصال جديد بدلًا من استخدام اتصال موجود من المجمّع.
* `ProfileEvent_RefreshableViewRefreshSuccess` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات تحديث العروض المادية القابلة للتحديث
* `ProfileEvent_RefreshableViewRefreshFailed` ([UInt64](/ar/reference/data-types/int-uint)) — عدد مرات فشل تحديث العروض المادية القابلة للتحديث
* `ProfileEvent_RefreshableViewSyncReplicaSuccess` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي نفّذت فيها عبارة `SELECT` من عرض مادي قابل للتحديث عملية `SYNC REPLICA` ضمنية
* `ProfileEvent_RefreshableViewSyncReplicaRetry` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي فشلت فيها عبارة `SELECT` من عرض مادي قابل للتحديث ثم أعادت محاولة `SYNC REPLICA` ضمنية
* `ProfileEvent_RefreshableViewLockTableRetry` ([UInt64](/ar/reference/data-types/int-uint)) — عدد المرات التي اضطرت فيها عبارة `SELECT` من عرض مادي قابل للتحديث إلى التبديل إلى جدول جديد لأن الجدول القديم أُزيل
* `ProfileEvent_AsyncLoggingConsoleTotalMessages` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الرسائل (المقبولة أو المُسقطة) التي أُرسلت إلى الطابور غير المتزامن لسجل وحدة التحكم
* `ProfileEvent_AsyncLoggingFileLogTotalMessages` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الرسائل (المقبولة أو المُسقطة) التي أُرسلت إلى الطابور غير المتزامن لسجل الملف
* `ProfileEvent_AsyncLoggingErrorFileLogTotalMessages` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الرسائل (المقبولة أو المُسقطة) التي أُرسلت إلى الطابور غير المتزامن لسجل ملف الأخطاء
* `ProfileEvent_AsyncLoggingSyslogTotalMessages` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الرسائل (المقبولة أو المُسقطة) التي أُرسلت إلى الطابور غير المتزامن لـ syslog
* `ProfileEvent_AsyncLoggingTextLogTotalMessages` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الرسائل (المقبولة أو المُسقطة) التي أُرسلت إلى الطابور غير المتزامن لـ text&#95;log
* `ProfileEvent_AsyncLoggingConsoleDroppedMessages` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الرسائل التي أُسقطت من سجل وحدة التحكم بسبب امتلاء طابور السجل غير المتزامن
* `ProfileEvent_AsyncLoggingFileLogDroppedMessages` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الرسائل التي أُسقطت من سجل الملف بسبب امتلاء طابور السجل غير المتزامن
* `ProfileEvent_AsyncLoggingErrorFileLogDroppedMessages` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الرسائل التي أُسقطت من سجل ملف الأخطاء بسبب امتلاء طابور السجل غير المتزامن
* `ProfileEvent_AsyncLoggingSyslogDroppedMessages` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الرسائل التي أُسقطت من syslog بسبب امتلاء طابور السجل غير المتزامن
* `ProfileEvent_AsyncLoggingTextLogDroppedMessages` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الرسائل التي أُسقطت من text&#95;log بسبب امتلاء طابور السجل غير المتزامن
* `ProfileEvent_JemallocFailedAllocationSampleTracking` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لمرات فشل تتبّع عيّنة تخصيص jemalloc
* `ProfileEvent_JemallocFailedDeallocationSampleTracking` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لمرات فشل تتبّع عيّنة إلغاء تخصيص jemalloc
* `ProfileEvent_LoadedStatisticsMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المنقضي في تحميل الإحصاءات من الأجزاء
* `ProfileEvent_RuntimeDataflowStatisticsInputBytes` ([UInt64](/ar/reference/data-types/int-uint)) — إحصاءات مجمّعة عن عدد البايتات التي كانت النسخ المتماثلة ستقرأها إذا نُفِّذ الاستعلام باستخدام النسخ المتماثلة المتوازية
* `ProfileEvent_RuntimeDataflowStatisticsOutputBytes` ([UInt64](/ar/reference/data-types/int-uint)) — إحصاءات مجمّعة عن عدد البايتات التي كانت النسخ المتماثلة سترسلها إلى العقدة البادئة إذا نُفِّذ الاستعلام باستخدام النسخ المتماثلة المتوازية
* `ProfileEvent_S3CachedCredentialsProvidersReused` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لموفري بيانات الاعتماد الذين أُعيد استخدامهم من الذاكرة المؤقتة
* `ProfileEvent_S3CachedCredentialsProvidersAdded` ([UInt64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لموفري بيانات الاعتماد المضافين حديثًا إلى الذاكرة المؤقتة
* `ProfileEvent_RuntimeFiltersCreated` ([UInt64](/ar/reference/data-types/int-uint)) — عدد عوامل التصفية المختلفة من JOIN Runtime Filters التي أُنشئت ضمن استعلام
* `ProfileEvent_RuntimeFilterBlocksProcessed` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الكتل التي عالجتها JOIN Runtime Filters
* `ProfileEvent_RuntimeFilterBlocksSkipped` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الكتل التي تخطّتها JOIN Runtime Filters دون معالجة بسبب تعطيل عامل التصفية ديناميكيًا نتيجة انخفاض نسبة التصفية
* `ProfileEvent_RuntimeFilterRowsChecked` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الصفوف التي فحصتها مرشحات JOIN وقت التشغيل
* `ProfileEvent_RuntimeFilterRowsPassed` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الصفوف التي اجتازت مرشحات JOIN وقت التشغيل (أي لم تُستبعَد بواسطتها)
* `ProfileEvent_RuntimeFilterRowsSkipped` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الصفوف داخل الكتل التي تخطّتها مرشحات JOIN وقت التشغيل
* `ProfileEvent_JoinBuildPostProcessingMicroseconds` ([UInt64](/ar/reference/data-types/int-uint)) — الوقت المنقضي في خطوات المعالجة اللاحقة بعد بناء الجانب الأيمن من JOIN.
* `ProfileEvent_AIInputTokens` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي رموز prompt المستهلَكة عبر جميع استدعاءات AI function في الاستعلام.
* `ProfileEvent_AIOutputTokens` ([UInt64](/ar/reference/data-types/int-uint)) — إجمالي رموز الإكمال المستهلَكة عبر جميع استدعاءات AI function في الاستعلام.
* `ProfileEvent_AIAPICalls` ([UInt64](/ar/reference/data-types/int-uint)) — عدد طلبات HTTP المُرسَلة إلى موفري AI.
* `ProfileEvent_AIRowsProcessed` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الصفوف التي تلقت نتيجة من AI.
* `ProfileEvent_AIRowsSkipped` ([UInt64](/ar/reference/data-types/int-uint)) — عدد الصفوف التي تلقت قيمة افتراضية بسبب حصة استخدام أو خطأ.
* `CurrentMetric_Query` ([Int64](/ar/reference/data-types/int-uint)) — عدد الاستعلامات الجاري تنفيذها
* `CurrentMetric_ASTFuzzerAccumulatedFragments` ([Int64](/ar/reference/data-types/int-uint)) — عدد أجزاء AST التي راكمها AST fuzzer على جانب الخادم لاستخدامها في mutations مستقبلية.
* `CurrentMetric_QueryNonInternal` ([Int64](/ar/reference/data-types/int-uint)) — عدد الاستعلامات غير الداخلية الجاري تنفيذها (الاستعلامات التي بدأها المستخدمون، باستثناء الاستعلامات الداخلية من ClickHouse)
* `CurrentMetric_Merge` ([Int64](/ar/reference/data-types/int-uint)) — عدد عمليات الدمج في الخلفية الجاري تنفيذها
* `CurrentMetric_MergeParts` ([Int64](/ar/reference/data-types/int-uint)) — عدد الأجزاء المصدر المشاركة في عمليات الدمج الحالية في الخلفية
* `CurrentMetric_Move` ([Int64](/ar/reference/data-types/int-uint)) — عدد عمليات النقل الجاري تنفيذها حاليًا
* `CurrentMetric_PartMutation` ([Int64](/ar/reference/data-types/int-uint)) — عدد عمليات mutation ‏(ALTER DELETE/UPDATE)
* `CurrentMetric_ReplicatedFetch` ([Int64](/ar/reference/data-types/int-uint)) — عدد أجزاء البيانات الجاري جلبها من replica
* `CurrentMetric_ReplicatedSend` ([Int64](/ar/reference/data-types/int-uint)) — عدد أجزاء البيانات الجاري إرسالها إلى replicas
* `CurrentMetric_ReplicatedChecks` ([Int64](/ar/reference/data-types/int-uint)) — عدد أجزاء البيانات الجاري التحقق من اتساقها
* `CurrentMetric_BackgroundMergesAndMutationsPoolTask` ([Int64](/ar/reference/data-types/int-uint)) — عدد عمليات الدمج وعمليات mutation النشطة في مجمّع الخلفية المرتبط
* `CurrentMetric_BackgroundMergesAndMutationsPoolSize` ([Int64](/ar/reference/data-types/int-uint)) — الحد الأقصى لعدد عمليات الدمج وعمليات mutation النشطة في مجمّع الخلفية المرتبط
* `CurrentMetric_BackgroundFetchesPoolTask` ([Int64](/ar/reference/data-types/int-uint)) — عدد عمليات الجلب النشطة في مجمّع الخلفية المرتبط
* `CurrentMetric_BackgroundFetchesPoolSize` ([Int64](/ar/reference/data-types/int-uint)) — الحد الأقصى لعدد عمليات الجلب المتزامنة في مجمّع الخلفية المرتبط
* `CurrentMetric_BackgroundCommonPoolTask` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة في مجمّع الخلفية المرتبط
* `CurrentMetric_BackgroundCommonPoolSize` ([Int64](/ar/reference/data-types/int-uint)) — الحد الأقصى لعدد المهام في مجمّع الخلفية المرتبط
* `CurrentMetric_BackgroundMovePoolTask` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة في BackgroundProcessingPool لعمليات النقل
* `CurrentMetric_BackgroundMovePoolSize` ([Int64](/ar/reference/data-types/int-uint)) — الحد الأقصى لعدد المهام في BackgroundProcessingPool لعمليات النقل
* `CurrentMetric_AzureRequests` ([Int64](/ar/reference/data-types/int-uint)) — عدد طلبات Azure التي يجري تنفيذها حاليًا
* `CurrentMetric_BackgroundSchedulePoolTask` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة في BackgroundSchedulePool. يُستخدم هذا المجمع للمهام الدورية الخاصة بـ ReplicatedMergeTree، مثل تنظيف أجزاء البيانات القديمة، وتعديل أجزاء البيانات، وإعادة تهيئة النسخة المتماثلة، وما إلى ذلك.
* `CurrentMetric_BackgroundSchedulePoolSize` ([Int64](/ar/reference/data-types/int-uint)) — الحد الأقصى لعدد المهام في BackgroundSchedulePool. يُستخدم هذا المجمع للمهام الدورية الخاصة بـ ReplicatedMergeTree، مثل تنظيف أجزاء البيانات القديمة، وتعديل أجزاء البيانات، وإعادة تهيئة النسخة المتماثلة، وما إلى ذلك.
* `CurrentMetric_BackgroundBufferFlushSchedulePoolTask` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة في BackgroundBufferFlushSchedulePool. يُستخدم هذا المجمع لعمليات flush الدورية الخاصة بـ Buffer
* `CurrentMetric_BackgroundBufferFlushSchedulePoolSize` ([Int64](/ar/reference/data-types/int-uint)) — الحد الأقصى لعدد المهام في BackgroundBufferFlushSchedulePool
* `CurrentMetric_BackgroundDistributedSchedulePoolTask` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة في BackgroundDistributedSchedulePool. يُستخدم هذا المجمع لعمليات الإرسال الموزعة التي تُنفَّذ في الخلفية.
* `CurrentMetric_BackgroundDistributedSchedulePoolSize` ([Int64](/ar/reference/data-types/int-uint)) — الحد الأقصى لعدد المهام في BackgroundDistributedSchedulePool
* `CurrentMetric_BackgroundMessageBrokerSchedulePoolTask` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة في BackgroundMessageBrokerSchedulePool لبث الرسائل
* `CurrentMetric_BackgroundMessageBrokerSchedulePoolSize` ([Int64](/ar/reference/data-types/int-uint)) — الحد الأقصى لعدد المهام في BackgroundMessageBrokerSchedulePool لبث الرسائل
* `CurrentMetric_CacheDictionaryUpdateQueueBatches` ([Int64](/ar/reference/data-types/int-uint)) — عدد &#39;الدفعات&#39; (مجموعة من المفاتيح) في قائمة انتظار التحديث في CacheDictionaries.
* `CurrentMetric_CacheDictionaryUpdateQueueKeys` ([Int64](/ar/reference/data-types/int-uint)) — العدد الدقيق للمفاتيح في قائمة انتظار التحديث في CacheDictionaries.
* `CurrentMetric_DiskSpaceReservedForMerge` ([Int64](/ar/reference/data-types/int-uint)) — مساحة القرص المحجوزة لعمليات الدمج في الخلفية التي تعمل حاليًا. وهي أكبر قليلًا من الحجم الإجمالي للأجزاء التي يجري دمجها حاليًا.
* `CurrentMetric_DistributedSend` ([Int64](/ar/reference/data-types/int-uint)) — عدد الاتصالات بالخوادم البعيدة التي ترسل البيانات التي أُدرجت في جداول Distributed. ويشمل ذلك النمطين المتزامن وغير المتزامن.
* `CurrentMetric_QueryPreempted` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام قيد التشغيل المتوقفة مؤقتًا والتي تنتظر بسبب إعداد &#39;priority&#39;.
* `CurrentMetric_TCPConnection` ([Int64](/ar/reference/data-types/int-uint)) — عدد الاتصالات بخادم TCP (العملاء الذين يستخدمون native interface)، ويشمل أيضًا اتصالات الاستعلامات الموزعة بين الخوادم
* `CurrentMetric_MySQLConnection` ([Int64](/ar/reference/data-types/int-uint)) — عدد اتصالات العميل التي تستخدم بروتوكول MySQL
* `CurrentMetric_HTTPConnection` ([Int64](/ar/reference/data-types/int-uint)) — عدد الاتصالات بخادم HTTP
* `CurrentMetric_InterserverConnection` ([Int64](/ar/reference/data-types/int-uint)) — عدد الاتصالات الواردة من النسخ المتماثلة الأخرى لجلب الأجزاء
* `CurrentMetric_PostgreSQLConnection` ([Int64](/ar/reference/data-types/int-uint)) — عدد اتصالات العميل التي تستخدم بروتوكول PostgreSQL
* `CurrentMetric_OpenFileForRead` ([Int64](/ar/reference/data-types/int-uint)) — عدد الملفات المفتوحة للقراءة
* `CurrentMetric_OpenFileForWrite` ([Int64](/ar/reference/data-types/int-uint)) — عدد الملفات المفتوحة للكتابة
* `CurrentMetric_Compressing` ([Int64](/ar/reference/data-types/int-uint)) — عدد عمليات الضغط التي تستخدم ترميزات الضغط الداخلية
* `CurrentMetric_Decompressing` ([Int64](/ar/reference/data-types/int-uint)) — عدد عمليات فك الضغط التي تستخدم ترميزات الضغط الداخلية
* `CurrentMetric_ParallelCompressedWriteBufferThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في جميع مثيلات ParallelCompressedWriteBuffer — تنفّذ هذه الخيوط الضغط والكتابة بالتوازي
* `CurrentMetric_ParallelCompressedWriteBufferWait` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في جميع مثيلات ParallelCompressedWriteBuffer التي تنتظر حاليًا حتى يصبح المخزن المؤقت متاحًا للكتابة
* `CurrentMetric_TotalTemporaryFiles` ([Int64](/ar/reference/data-types/int-uint)) — عدد الملفات المؤقتة المُنشأة
* `CurrentMetric_TemporaryFilesForSort` ([Int64](/ar/reference/data-types/int-uint)) — عدد الملفات المؤقتة المُنشأة للفرز الخارجي
* `CurrentMetric_TemporaryFilesForAggregation` ([Int64](/ar/reference/data-types/int-uint)) — عدد الملفات المؤقتة المُنشأة للتجميع الخارجي
* `CurrentMetric_TemporaryFilesForJoin` ([Int64](/ar/reference/data-types/int-uint)) — عدد الملفات المؤقتة المُنشأة من أجل JOIN
* `CurrentMetric_TemporaryFilesForMerge` ([Int64](/ar/reference/data-types/int-uint)) — عدد الملفات المؤقتة الخاصة بـ vertical merge
* `CurrentMetric_TemporaryFilesUnknown` ([Int64](/ar/reference/data-types/int-uint)) — عدد الملفات المؤقتة المُنشأة دون غرض معروف
* `CurrentMetric_Read` ([Int64](/ar/reference/data-types/int-uint)) — عدد استدعاءات النظام للقراءة (read و pread و io&#95;getevents وغيرها) الجارية حاليًا
* `CurrentMetric_RemoteRead` ([Int64](/ar/reference/data-types/int-uint)) — عدد عمليات القراءة الجارية باستخدام قارئ بعيد
* `CurrentMetric_Write` ([Int64](/ar/reference/data-types/int-uint)) — عدد استدعاءات النظام للكتابة (write و pwrite و io&#95;getevents وغيرها) الجارية حاليًا
* `CurrentMetric_NetworkReceive` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط التي تتلقى البيانات من الشبكة. لا تُحتسب إلا تفاعلات الشبكة المرتبطة بـ ClickHouse، وليس تلك الخاصة بمكتبات 3rd party.
* `CurrentMetric_NetworkSend` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط التي ترسل البيانات عبر الشبكة. لا تُحتسب إلا تفاعلات الشبكة المرتبطة بـ ClickHouse، وليس تلك الخاصة بمكتبات 3rd party.
* `CurrentMetric_SendScalars` ([Int64](/ar/reference/data-types/int-uint)) — عدد الاتصالات التي ترسل بيانات القيم القياسية إلى الخوادم البعيدة.
* `CurrentMetric_SendExternalTables` ([Int64](/ar/reference/data-types/int-uint)) — عدد الاتصالات التي ترسل بيانات الجداول الخارجية إلى الخوادم البعيدة. تُستخدم الجداول الخارجية لتنفيذ معاملي GLOBAL IN و GLOBAL JOIN مع الاستعلامات الفرعية الموزعة.
* `CurrentMetric_QueryThread` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط معالجة الاستعلامات
* `CurrentMetric_ReadonlyReplica` ([Int64](/ar/reference/data-types/int-uint)) — عدد جداول Replicated التي تكون حاليًا في حالة للقراءة فقط بسبب إعادة التهيئة بعد فقدان جلسة ZooKeeper أو بسبب بدء التشغيل دون تهيئة ZooKeeper.
* `CurrentMetric_ReplicaReady` ([Int64](/ar/reference/data-types/int-uint)) — يشير إلى ما إذا كانت النسخة المتماثلة جاهزة للاستعلامات: 0 = لا، 1 = نعم
* `CurrentMetric_MemoryTracking` ([Int64](/ar/reference/data-types/int-uint)) — إجمالي مقدار الذاكرة (بالبايت) المخصصة بواسطة الخادم.
* `CurrentMetric_MemoryTrackingUncorrected` ([Int64](/ar/reference/data-types/int-uint)) — إجمالي مقدار الذاكرة (بالبايت) المخصصة بواسطة الخادم من دون تصحيح بواسطة RSS.
* `CurrentMetric_MergesMutationsMemoryTracking` ([Int64](/ar/reference/data-types/int-uint)) — إجمالي مقدار الذاكرة (بالبايت) المخصصة بواسطة المهام الخلفية (merges و mutations).
* `CurrentMetric_EphemeralNode` ([Int64](/ar/reference/data-types/int-uint)) — عدد العقد المؤقتة الموجودة في ZooKeeper.
* `CurrentMetric_MaxAllocatedEphemeralLockSequentialNumber` ([Int64](/ar/reference/data-types/int-uint)) — أكبر رقم تسلسلي مُخصّص لعُقد القفل المؤقتة من نوع znode في ZooKeeper. ويتأثر هذا أساسًا بأرقام block.
* `CurrentMetric_ZooKeeperSession` ([Int64](/ar/reference/data-types/int-uint)) — عدد الجلسات (الاتصالات) مع ZooKeeper. يجب ألا يزيد عن واحدة، لأن استخدام أكثر من اتصال واحد بـ ZooKeeper قد يؤدي إلى أخطاء بسبب غياب خاصية linearizability (قراءات قديمة) التي يسمح بها نموذج الاتساق في ZooKeeper.
* `CurrentMetric_ZooKeeperSessionExpired` ([Int64](/ar/reference/data-types/int-uint)) — عدد جلسات ZooKeeper العامة منتهية الصلاحية.
* `CurrentMetric_ZooKeeperConnectionLossStartedTimestampSeconds` ([Int64](/ar/reference/data-types/int-uint)) — طابع زمني Unix بالثواني يبيّن وقت فقدان الاتصال بـ ZooKeeper، أو 0 إذا كان الاتصال ناجحًا.
* `CurrentMetric_ZooKeeperWatch` ([Int64](/ar/reference/data-types/int-uint)) — عدد عمليات watch (اشتراكات الأحداث) في ZooKeeper.
* `CurrentMetric_ZooKeeperRequest` ([Int64](/ar/reference/data-types/int-uint)) — عدد الطلبات المرسلة إلى ZooKeeper والقيد التنفيذ.
* `CurrentMetric_DelayedInserts` ([Int64](/ar/reference/data-types/int-uint)) — عدد استعلامات INSERT التي جرى تقييد معدلها بسبب الارتفاع الكبير في عدد أجزاء البيانات النشطة ضمن partition في جدول MergeTree.
* `CurrentMetric_ContextLockWait` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط التي تنتظر القفل في Context. هذا قفل عام.
* `CurrentMetric_StorageBufferRows` ([Int64](/ar/reference/data-types/int-uint)) — عدد الصفوف في المخازن المؤقتة لجداول Buffer
* `CurrentMetric_StorageBufferBytes` ([Int64](/ar/reference/data-types/int-uint)) — عدد البايتات في المخازن المؤقتة لجداول Buffer
* `CurrentMetric_DictCacheRequests` ([Int64](/ar/reference/data-types/int-uint)) — عدد الطلبات القيد التنفيذ إلى مصادر البيانات الخاصة بالقواميس من نوع cache.
* `CurrentMetric_Revision` ([Int64](/ar/reference/data-types/int-uint)) — رقم Revision الخاص بالخادم. وهو رقم يُزاد مع كل إصدار أو إصدار مرشح، باستثناء إصدارات التصحيح.
* `CurrentMetric_VersionInteger` ([Int64](/ar/reference/data-types/int-uint)) — إصدار الخادم على هيئة عدد صحيح واحد بنظام الأساس 1000. على سبيل المثال، يُحوَّل الإصدار 11.22.33 إلى 11022033.
* `CurrentMetric_RWLockWaitingReaders` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط التي تنتظر القراءة على RWLock الخاص بالجدول.
* `CurrentMetric_RWLockWaitingWriters` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط التي تنتظر الكتابة على RWLock الخاص بالجدول.
* `CurrentMetric_RWLockActiveReaders` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط التي تحتفظ بقفل قراءة في RWLock الخاص بالجدول.
* `CurrentMetric_RWLockActiveWriters` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط التي تحتفظ بقفل كتابة في RWLock الخاص بالجدول.
* `CurrentMetric_GlobalThread` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في التجمّع العام للخيوط.
* `CurrentMetric_GlobalThreadActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في التجمّع العام للخيوط التي تنفّذ مهمة.
* `CurrentMetric_GlobalThreadScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في التجمّع العام للخيوط.
* `CurrentMetric_LocalThread` ([Int64](/ar/reference/data-types/int-uint)) — متقادم. عدد الخيوط في تجمّعات الخيوط المحلية. تُؤخذ الخيوط في تجمّعات الخيوط المحلية من التجمّع العام للخيوط.
* `CurrentMetric_LocalThreadActive` ([Int64](/ar/reference/data-types/int-uint)) — متقادم. عدد الخيوط في تجمّعات الخيوط المحلية التي تنفّذ مهمة.
* `CurrentMetric_LocalThreadScheduled` ([Int64](/ar/reference/data-types/int-uint)) — متقادم. عدد المهام الموجودة في قائمة الانتظار أو النشطة في تجمّعات الخيوط المحلية.
* `CurrentMetric_MergeTreeDataSelectExecutorThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في تجمّع الخيوط الخاص بـ MergeTreeDataSelectExecutor.
* `CurrentMetric_MergeTreeDataSelectExecutorThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في تجمّع الخيوط الخاص بـ MergeTreeDataSelectExecutor التي تنفّذ مهمة.
* `CurrentMetric_MergeTreeDataSelectExecutorThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في تجمّع الخيوط الخاص بـ MergeTreeDataSelectExecutor.
* `CurrentMetric_BackupsThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في تجمّع الخيوط الخاص بـ BACKUP.
* `CurrentMetric_BackupsThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في تجمّع الخيوط الخاص بـ BACKUP التي تنفّذ مهمة.
* `CurrentMetric_BackupsThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة لـ BACKUP.
* `CurrentMetric_RestoreThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في مجمّع الخيوط لـ RESTORE.
* `CurrentMetric_RestoreThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في مجمّع الخيوط لـ RESTORE التي تنفّذ مهمة.
* `CurrentMetric_RestoreThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة لـ RESTORE.
* `CurrentMetric_MarksLoaderThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في مجمّع الخيوط المخصّص لتحميل العلامات.
* `CurrentMetric_MarksLoaderThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في مجمّع الخيوط المخصّص لتحميل العلامات التي تنفّذ مهمة.
* `CurrentMetric_MarksLoaderThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في مجمّع الخيوط المخصّص لتحميل العلامات.
* `CurrentMetric_IOPrefetchThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في مجمّع خيوط الجلب المسبق لـ IO.
* `CurrentMetric_IOPrefetchThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في مجمّع خيوط الجلب المسبق لـ IO التي تنفّذ مهمة.
* `CurrentMetric_IOPrefetchThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في مجمّع خيوط الجلب المسبق لـ IO.
* `CurrentMetric_IOWriterThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في مجمّع خيوط الكتابة لـ IO.
* `CurrentMetric_IOWriterThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في مجمّع خيوط الكتابة لـ IO التي تنفّذ مهمة.
* `CurrentMetric_IOWriterThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في مجمّع خيوط الكتابة لـ IO.
* `CurrentMetric_IOThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في مجمّع خيوط IO.
* `CurrentMetric_IOThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في مجمّع خيوط IO التي تنفّذ مهمة.
* `CurrentMetric_IOThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في مجمّع خيوط IO.
* `CurrentMetric_CompressionThread` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في مجمّعات خيوط الضغط.
* `CurrentMetric_CompressionThreadActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في مجمّعات خيوط الضغط التي تنفّذ مهمة.
* `CurrentMetric_CompressionThreadScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في مجمّعات خيوط الضغط.
* `CurrentMetric_ThreadPoolRemoteFSReaderThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في مجمّع الخيوط لـ remote&#95;filesystem&#95;read&#95;method=threadpool.
* `CurrentMetric_ThreadPoolRemoteFSReaderThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في مجمّع الخيوط لـ remote&#95;filesystem&#95;read&#95;method=threadpool التي تنفّذ مهمة.
* `CurrentMetric_ThreadPoolRemoteFSReaderThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في مجمّع الخيوط لـ remote&#95;filesystem&#95;read&#95;method=threadpool.
* `CurrentMetric_ThreadPoolFSReaderThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في مجمّع الخيوط لـ local&#95;filesystem&#95;read&#95;method=threadpool.
* `CurrentMetric_ThreadPoolFSReaderThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في مجمّع الخيوط لـ local&#95;filesystem&#95;read&#95;method=threadpool التي تنفّذ مهمة.
* `CurrentMetric_ThreadPoolFSReaderThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في مجمّع الخيوط لـ local&#95;filesystem&#95;read&#95;method=threadpool.
* `CurrentMetric_ObjectStorageQueueShutdownThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في مجمّع خيوط إيقاف تشغيل قائمة انتظار تخزين الكائنات.
* `CurrentMetric_ObjectStorageQueueShutdownThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط التي تشغّل مهمة في مجمّع خيوط إيقاف تشغيل قائمة انتظار تخزين الكائنات.
* `CurrentMetric_ObjectStorageQueueShutdownThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في مجمّع خيوط إيقاف تشغيل قائمة انتظار تخزين الكائنات.
* `CurrentMetric_ObjectStorageQueueMetadataCacheSizeBytes` ([Int64](/ar/reference/data-types/int-uint)) — حجم ذاكرة التخزين المؤقت للبيانات الوصفية الخاصة بـ ObjectStorageQueue بالبايت.
* `CurrentMetric_ObjectStorageQueueMetadataCacheSizeElements` ([Int64](/ar/reference/data-types/int-uint)) — حجم ذاكرة التخزين المؤقت للبيانات الوصفية الخاصة بـ ObjectStorageQueue بالعناصر.
* `CurrentMetric_DeltaLakeSnapshotCacheSizeElements` ([Int64](/ar/reference/data-types/int-uint)) — حجم ذاكرة التخزين المؤقت للّقطة الخاصة بـ DeltaLake بالعناصر.
* `CurrentMetric_BackupsIOThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في مجمّع خيوط BackupsIO.
* `CurrentMetric_BackupsIOThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط التي تشغّل مهمة في مجمّع خيوط BackupsIO.
* `CurrentMetric_BackupsIOThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في مجمّع خيوط BackupsIO.
* `CurrentMetric_DiskObjectStorageAsyncThreads` ([Int64](/ar/reference/data-types/int-uint)) — مقياس متقادم، لا يعرض شيئًا.
* `CurrentMetric_DiskObjectStorageAsyncThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — مقياس متقادم، لا يعرض شيئًا.
* `CurrentMetric_StorageHiveThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في مجمّع خيوط StorageHive.
* `CurrentMetric_StorageHiveThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط التي تشغّل مهمة في مجمّع خيوط StorageHive.
* `CurrentMetric_StorageHiveThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في مجمّع خيوط StorageHive.
* `CurrentMetric_TablesLoaderBackgroundThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في مجمّع الخيوط الخلفي لمحمّل الجداول.
* `CurrentMetric_TablesLoaderBackgroundThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط التي تشغّل مهمة في مجمّع الخيوط الخلفي لمحمّل الجداول.
* `CurrentMetric_TablesLoaderBackgroundThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في مجمّع الخيوط الخلفي لمحمّل الجداول.
* `CurrentMetric_TablesLoaderForegroundThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في مجمّع الخيوط الأمامي لمحمّل الجداول.
* `CurrentMetric_TablesLoaderForegroundThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط التي تشغّل مهمة في مجمّع الخيوط الأمامي لمحمّل الجداول.
* `CurrentMetric_TablesLoaderForegroundThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في مجمّع الخيوط الأمامي لمحمّل الجداول.
* `CurrentMetric_DatabaseOnDiskThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في مجمّع خيوط DatabaseOnDisk.
* `CurrentMetric_DatabaseOnDiskThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط التي تشغّل مهمة في مجمّع خيوط DatabaseOnDisk.
* `CurrentMetric_DatabaseOnDiskThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في مجمّع خيوط DatabaseOnDisk.
* `CurrentMetric_DatabaseBackupThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في مجمّع خيوط DatabaseBackup.
* `CurrentMetric_DatabaseBackupThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط التي تشغّل مهمة في مجمّع خيوط DatabaseBackup.
* `CurrentMetric_DatabaseBackupThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة أو الموجودة في قائمة الانتظار في تجمّع مؤشرات الترابط DatabaseBackup.
* `CurrentMetric_DatabaseCatalogThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط في تجمّع مؤشرات الترابط DatabaseCatalog.
* `CurrentMetric_DatabaseCatalogThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط في تجمّع مؤشرات الترابط DatabaseCatalog التي تشغّل مهمة.
* `CurrentMetric_DatabaseCatalogThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة أو الموجودة في قائمة الانتظار في تجمّع مؤشرات الترابط DatabaseCatalog.
* `CurrentMetric_DestroyAggregatesThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط في تجمّع مؤشرات الترابط المخصّص لتدمير حالات التجميع.
* `CurrentMetric_DestroyAggregatesThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط في تجمّع مؤشرات الترابط المخصّص لتدمير حالات التجميع التي تشغّل مهمة.
* `CurrentMetric_DestroyAggregatesThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة أو الموجودة في قائمة الانتظار في تجمّع مؤشرات الترابط المخصّص لتدمير حالات التجميع.
* `CurrentMetric_ConcurrentHashJoinPoolThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط في تجمّع مؤشرات الترابط المخصّص لعملية الربط التجزئي المتزامن.
* `CurrentMetric_ConcurrentHashJoinPoolThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط في تجمّع مؤشرات الترابط المخصّص لعملية الربط التجزئي المتزامن التي تشغّل مهمة.
* `CurrentMetric_ConcurrentHashJoinPoolThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة أو الموجودة في قائمة الانتظار في تجمّع مؤشرات الترابط المخصّص لعملية الربط التجزئي المتزامن.
* `CurrentMetric_HashedDictionaryThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط في تجمّع مؤشرات الترابط HashedDictionary.
* `CurrentMetric_HashedDictionaryThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط في تجمّع مؤشرات الترابط HashedDictionary التي تشغّل مهمة.
* `CurrentMetric_HashedDictionaryThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة أو الموجودة في قائمة الانتظار في تجمّع مؤشرات الترابط HashedDictionary.
* `CurrentMetric_CacheDictionaryThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط في تجمّع مؤشرات الترابط CacheDictionary.
* `CurrentMetric_CacheDictionaryThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط في تجمّع مؤشرات الترابط CacheDictionary التي تشغّل مهمة.
* `CurrentMetric_CacheDictionaryThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة أو الموجودة في قائمة الانتظار في تجمّع مؤشرات الترابط CacheDictionary.
* `CurrentMetric_ParallelFormattingOutputFormatThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط في تجمّع مؤشرات الترابط ParallelFormattingOutputFormatThreads.
* `CurrentMetric_ParallelFormattingOutputFormatThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط في تجمّع مؤشرات الترابط ParallelFormattingOutputFormatThreads التي تشغّل مهمة.
* `CurrentMetric_ParallelFormattingOutputFormatThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة أو الموجودة في قائمة الانتظار في تجمّع مؤشرات الترابط ParallelFormattingOutputFormatThreads.
* `CurrentMetric_MergeTreeBackgroundExecutorThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط في تجمّع مؤشرات الترابط MergeTreeBackgroundExecutor.
* `CurrentMetric_MergeTreeBackgroundExecutorThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط في تجمّع مؤشرات الترابط MergeTreeBackgroundExecutor التي تشغّل مهمة.
* `CurrentMetric_MergeTreeBackgroundExecutorThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة أو الموجودة في قائمة الانتظار في تجمّع مؤشرات الترابط MergeTreeBackgroundExecutor.
* `CurrentMetric_AsynchronousInsertThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط في تجمّع مؤشرات الترابط AsynchronousInsert.
* `CurrentMetric_AsynchronousInsertThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط في تجمّع مؤشرات الترابط AsynchronousInsert التي تشغّل مهمة.
* `CurrentMetric_AsynchronousInsertThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة أو الموجودة في قائمة الانتظار في تجمّع مؤشرات الترابط AsynchronousInsert.
* `CurrentMetric_AsynchronousInsertQueueSize` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام المعلّقة في قائمة انتظار AsynchronousInsert.
* `CurrentMetric_AsynchronousInsertQueueBytes` ([Int64](/ar/reference/data-types/int-uint)) — عدد البايتات المعلّقة في قائمة انتظار AsynchronousInsert.
* `CurrentMetric_StartupSystemTablesThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ في تجمع خيوط التنفيذ StartupSystemTables.
* `CurrentMetric_StartupSystemTablesThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ في تجمع خيوط التنفيذ StartupSystemTables التي تُشغّل مهمة.
* `CurrentMetric_StartupSystemTablesThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في تجمع خيوط التنفيذ StartupSystemTables.
* `CurrentMetric_AggregatorThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ في تجمع خيوط التنفيذ Aggregator.
* `CurrentMetric_AggregatorThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ في تجمع خيوط التنفيذ Aggregator التي تُشغّل مهمة.
* `CurrentMetric_AggregatorThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في تجمع خيوط التنفيذ Aggregator.
* `CurrentMetric_DDLWorkerThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ في تجمع خيوط التنفيذ DDLWorker لاستعلامات ON CLUSTER.
* `CurrentMetric_DDLWorkerThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ في تجمع خيوط التنفيذ DDLWORKER لاستعلامات ON CLUSTER التي تُشغّل مهمة.
* `CurrentMetric_DDLWorkerThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في تجمع خيوط التنفيذ DDLWORKER لاستعلامات ON CLUSTER.
* `CurrentMetric_StorageDistributedThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ في تجمع خيوط التنفيذ StorageDistributed.
* `CurrentMetric_StorageDistributedThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ في تجمع خيوط التنفيذ StorageDistributed التي تُشغّل مهمة.
* `CurrentMetric_StorageDistributedThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في تجمع خيوط التنفيذ StorageDistributed.
* `CurrentMetric_DistributedInsertThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ المستخدمة لعملية INSERT إلى Distributed.
* `CurrentMetric_DistributedInsertThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ المستخدمة لعملية INSERT إلى Distributed التي تُشغّل مهمة.
* `CurrentMetric_DistributedInsertThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة المستخدمة لعملية INSERT إلى Distributed.
* `CurrentMetric_StorageS3Threads` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ في تجمع خيوط التنفيذ StorageS3.
* `CurrentMetric_StorageS3ThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ في تجمع خيوط التنفيذ StorageS3 التي تُشغّل مهمة.
* `CurrentMetric_StorageS3ThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في تجمع خيوط التنفيذ StorageS3.
* `CurrentMetric_ObjectStorageS3Threads` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ في تجمع خيوط التنفيذ S3ObjectStorage.
* `CurrentMetric_ObjectStorageS3ThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ في تجمع خيوط التنفيذ S3ObjectStorage التي تُشغّل مهمة.
* `CurrentMetric_ObjectStorageS3ThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في تجمع خيوط التنفيذ S3ObjectStorage.
* `CurrentMetric_StorageObjectStorageThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ في تجمعات خيوط تنفيذ محركات الجداول البعيدة.
* `CurrentMetric_StorageObjectStorageThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ في تجمع خيوط تنفيذ محركات الجداول البعيدة التي تُشغّل مهمة.
* `CurrentMetric_StorageObjectStorageThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة أو الموجودة في قائمة الانتظار في مجمّع مؤشرات الترابط لمحركات الجداول البعيدة.
* `CurrentMetric_ObjectStorageAzureThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط في مجمّع مؤشرات الترابط AzureObjectStorage.
* `CurrentMetric_ObjectStorageAzureThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط في مجمّع مؤشرات الترابط AzureObjectStorage التي تُشغّل مهمة.
* `CurrentMetric_ObjectStorageAzureThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة أو الموجودة في قائمة الانتظار في مجمّع مؤشرات الترابط AzureObjectStorage.
* `CurrentMetric_BuildVectorSimilarityIndexThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط في مجمّع مؤشرات الترابط الخاص ببناء فهرس تشابه المتجهات.
* `CurrentMetric_BuildVectorSimilarityIndexThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط في مجمّع مؤشرات الترابط الخاص ببناء فهرس تشابه المتجهات التي تُشغّل مهمة.
* `CurrentMetric_BuildVectorSimilarityIndexThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة أو الموجودة في قائمة الانتظار في مجمّع مؤشرات الترابط الخاص ببناء فهرس تشابه المتجهات.
* `CurrentMetric_DistributedIndexAnalysisThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط في مجمّع مؤشرات الترابط الخاص بتحليل الفهرس الموزّع.
* `CurrentMetric_DistributedIndexAnalysisThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط في مجمّع مؤشرات الترابط الخاص بتحليل الفهرس الموزّع التي تُشغّل مهمة.
* `CurrentMetric_DistributedIndexAnalysisThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة أو الموجودة في قائمة الانتظار في مجمّع مؤشرات الترابط الخاص بتحليل الفهرس الموزّع.
* `CurrentMetric_ObjectStorageQueueRegisteredServers` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخوادم المسجّلة في StorageS3(Azure)Queue
* `CurrentMetric_IcebergCatalogThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط في مجمّع مؤشرات الترابط IcebergCatalog.
* `CurrentMetric_IcebergCatalogThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط في مجمّع مؤشرات الترابط IcebergCatalog التي تُشغّل مهمة.
* `CurrentMetric_IcebergCatalogThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة أو الموجودة في قائمة الانتظار في مجمّع مؤشرات الترابط IcebergCatalog.
* `CurrentMetric_IcebergSchedulePoolTask` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام في مجمّع الجدولة الخلفية لجداول Iceberg.
* `CurrentMetric_IcebergSchedulePoolSize` ([Int64](/ar/reference/data-types/int-uint)) — الحد الأقصى لعدد المهام في مجمّع الجدولة الخلفية لجداول Iceberg.
* `CurrentMetric_ParallelWithQueryThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط في مجمّع مؤشرات الترابط لمعالجة استعلامات PARALLEL WITH.
* `CurrentMetric_ParallelWithQueryActiveThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط النشطة في مجمّع مؤشرات الترابط لمعالجة استعلامات PARALLEL WITH.
* `CurrentMetric_ParallelWithQueryScheduledThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة أو الموجودة في قائمة الانتظار في مجمّع مؤشرات الترابط لمعالجة استعلامات PARALLEL WITH.
* `CurrentMetric_DiskPlainRewritableAzureDirectoryMapSize` ([Int64](/ar/reference/data-types/int-uint)) — عدد إدخالات المسار من المحلي إلى البعيد في الخريطة داخل الذاكرة &#39;plain&#95;rewritable&#39; الخاصة بـ AzureObjectStorage.
* `CurrentMetric_DiskPlainRewritableAzureFileCount` ([Int64](/ar/reference/data-types/int-uint)) — عدد إدخالات الملفات في الخريطة داخل الذاكرة &#39;plain&#95;rewritable&#39; الخاصة بـ AzureObjectStorage.
* `CurrentMetric_DiskPlainRewritableLocalDirectoryMapSize` ([Int64](/ar/reference/data-types/int-uint)) — عدد إدخالات المسار من المحلي إلى البعيد في الخريطة داخل الذاكرة &#39;plain&#95;rewritable&#39; الخاصة بـ LocalObjectStorage.
* `CurrentMetric_DiskPlainRewritableLocalFileCount` ([Int64](/ar/reference/data-types/int-uint)) — عدد إدخالات الملفات في الخريطة داخل الذاكرة &#39;plain&#95;rewritable&#39; الخاصة بـ LocalObjectStorage.
* `CurrentMetric_DiskPlainRewritableS3DirectoryMapSize` ([Int64](/ar/reference/data-types/int-uint)) — عدد إدخالات المسار من المحلي إلى البعيد في الخريطة داخل الذاكرة &#39;plain&#95;rewritable&#39; الخاصة بـ S3ObjectStorage.
* `CurrentMetric_DiskPlainRewritableS3FileCount` ([Int64](/ar/reference/data-types/int-uint)) — عدد إدخالات الملفات في الخريطة داخل الذاكرة `plain_rewritable` الخاصة بـ S3ObjectStorage.
* `CurrentMetric_MergeTreeFetchPartitionThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ لأمر ALTER TABLE FETCH PARTITION
* `CurrentMetric_MergeTreeFetchPartitionThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ في أمر ALTER TABLE FETCH PARTITION التي تنفّذ جلب جزء
* `CurrentMetric_MergeTreeFetchPartitionThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد عمليات جلب الأجزاء النشطة أو الموجودة في قائمة الانتظار في ALTER TABLE FETCH PARTITION
* `CurrentMetric_MergeTreePartsLoaderThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ في مجمّع سلاسل تنفيذ محمّل أجزاء MergeTree.
* `CurrentMetric_MergeTreePartsLoaderThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ في مجمّع سلاسل تنفيذ محمّل أجزاء MergeTree التي تشغّل مهمة.
* `CurrentMetric_MergeTreePartsLoaderThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة أو الموجودة في قائمة الانتظار في مجمّع سلاسل تنفيذ محمّل أجزاء MergeTree.
* `CurrentMetric_MergeTreeOutdatedPartsLoaderThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ في مجمّع سلاسل التنفيذ لتحميل أجزاء البيانات القديمة.
* `CurrentMetric_MergeTreeOutdatedPartsLoaderThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ النشطة في مجمّع سلاسل التنفيذ لتحميل أجزاء البيانات القديمة.
* `CurrentMetric_MergeTreeOutdatedPartsLoaderThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة أو الموجودة في قائمة الانتظار في مجمّع سلاسل التنفيذ لتحميل أجزاء البيانات القديمة.
* `CurrentMetric_MergeTreeUnexpectedPartsLoaderThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ في مجمّع سلاسل التنفيذ لتحميل أجزاء البيانات غير المتوقعة.
* `CurrentMetric_MergeTreeUnexpectedPartsLoaderThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ النشطة في مجمّع سلاسل التنفيذ لتحميل أجزاء البيانات غير المتوقعة.
* `CurrentMetric_MergeTreeUnexpectedPartsLoaderThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة أو الموجودة في قائمة الانتظار في مجمّع سلاسل التنفيذ لتحميل أجزاء البيانات غير المتوقعة.
* `CurrentMetric_MergeTreePartsCleanerThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ في مجمّع سلاسل تنفيذ منظّف أجزاء MergeTree.
* `CurrentMetric_MergeTreePartsCleanerThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ في مجمّع سلاسل تنفيذ منظّف أجزاء MergeTree التي تشغّل مهمة.
* `CurrentMetric_MergeTreePartsCleanerThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة أو الموجودة في قائمة الانتظار في مجمّع سلاسل تنفيذ منظّف أجزاء MergeTree.
* `CurrentMetric_DatabaseReplicatedCreateTablesThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ في مجمّع سلاسل التنفيذ المخصّص لإنشاء الجداول في DatabaseReplicated.
* `CurrentMetric_DatabaseReplicatedCreateTablesThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ النشطة في مجمّع سلاسل التنفيذ المخصّص لإنشاء الجداول في DatabaseReplicated.
* `CurrentMetric_DatabaseReplicatedCreateTablesThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة أو الموجودة في قائمة الانتظار في مجمّع سلاسل التنفيذ المخصّص لإنشاء الجداول في DatabaseReplicated.
* `CurrentMetric_IDiskCopierThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ لنسخ البيانات بين أقراص من أنواع مختلفة.
* `CurrentMetric_IDiskCopierThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ لنسخ البيانات بين أقراص من أنواع مختلفة التي تشغّل مهمة.
* `CurrentMetric_IDiskCopierThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة أو الموجودة في قائمة الانتظار لنسخ البيانات بين أقراص من أنواع مختلفة.
* `CurrentMetric_SystemReplicasThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ في مجمّع سلاسل التنفيذ system.replicas.
* `CurrentMetric_SystemDatabaseReplicasThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ في مجمّع سلاسل التنفيذ system.database&#95;replicas.
* `CurrentMetric_SystemReplicasThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ في مجمّع سلاسل التنفيذ system.replicas التي تشغّل مهمة.
* `CurrentMetric_SystemDatabaseReplicasThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ في تجمّع خيوط `system.database&#95;replicas` التي تنفّذ مهمة.
* `CurrentMetric_SystemReplicasThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في تجمّع خيوط `system.replicas`.
* `CurrentMetric_SystemDatabaseReplicasThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في تجمّع خيوط `system.database&#95;replicas`.
* `CurrentMetric_RestartReplicaThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ في تجمّع خيوط `RESTART REPLICA`.
* `CurrentMetric_RestartReplicaThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ في تجمّع خيوط `RESTART REPLICA` التي تنفّذ مهمة.
* `CurrentMetric_RestartReplicaThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في تجمّع خيوط `RESTART REPLICA`.
* `CurrentMetric_QueryPipelineExecutorThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ في تجمّع خيوط `PipelineExecutor`.
* `CurrentMetric_QueryPipelineExecutorThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ في تجمّع خيوط `PipelineExecutor` التي تنفّذ مهمة.
* `CurrentMetric_QueryPipelineExecutorThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في تجمّع خيوط `PipelineExecutor`.
* `CurrentMetric_ParquetEncoderThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ في تجمّع خيوط `ParquetBlockOutputFormat`.
* `CurrentMetric_ParquetEncoderThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ في تجمّع خيوط `ParquetBlockOutputFormat` التي تنفّذ مهمة.
* `CurrentMetric_ParquetEncoderThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في تجمّع خيوط `ParquetBlockOutputFormat`.
* `CurrentMetric_MergeTreeSubcolumnsReaderThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ في تجمّع الخيوط المستخدم لقراءة الأعمدة الفرعية في `MergeTree`.
* `CurrentMetric_MergeTreeSubcolumnsReaderThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ في تجمّع الخيوط المستخدم لقراءة الأعمدة الفرعية في `MergeTree` التي تنفّذ مهمة.
* `CurrentMetric_MergeTreeSubcolumnsReaderThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في تجمّع الخيوط المستخدم لقراءة الأعمدة الفرعية في `MergeTree`.
* `CurrentMetric_FormatParsingThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ في تجمّع الخيوط المستخدم لتحليل المدخلات.
* `CurrentMetric_FormatParsingThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ في تجمّع الخيوط المستخدم لتحليل المدخلات التي تنفّذ مهمة.
* `CurrentMetric_FormatParsingThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في تجمّع الخيوط المستخدم لتحليل المدخلات.
* `CurrentMetric_OutdatedPartsLoadingThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ في تجمّع الخيوط المخصّص لتحميل أجزاء البيانات القديمة.
* `CurrentMetric_OutdatedPartsLoadingThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ النشطة في تجمّع الخيوط المخصّص لتحميل أجزاء البيانات القديمة.
* `CurrentMetric_OutdatedPartsLoadingThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في تجمّع الخيوط المخصّص لتحميل أجزاء البيانات القديمة.
* `CurrentMetric_FreezePartThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ في تجمّع الخيوط المخصّص لتجميد أجزاء البيانات.
* `CurrentMetric_FreezePartThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ النشطة في تجمّع الخيوط المخصّص لتجميد أجزاء البيانات.
* `CurrentMetric_FreezePartThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في تجمّع الخيوط المخصّص لتجميد أجزاء البيانات.
* `CurrentMetric_PolygonDictionaryThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط التنفيذ في تجمّع الخيوط المخصّص لقواميس `Polygon`.
* `CurrentMetric_PolygonDictionaryThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط النشطة في مجمع الخيوط لقواميس المضلعات.
* `CurrentMetric_PolygonDictionaryThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في مجمع الخيوط لقواميس المضلعات.
* `CurrentMetric_KeeperReadThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في مجمع الخيوط لعمليات القراءة في خادم Keeper.
* `CurrentMetric_KeeperReadThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط النشطة في مجمع الخيوط لعمليات القراءة في خادم Keeper.
* `CurrentMetric_KeeperReadThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في مجمع الخيوط لعمليات القراءة في خادم Keeper. هذا المقياس غير ذي معنى، لأن مهام القراءة الفعلية في مجمع الخيوط هذا تُجدول عبر آلية مختلفة.
* `CurrentMetric_DistributedBytesToInsert` ([Int64](/ar/reference/data-types/int-uint)) — عدد البايتات المعلّقة المطلوب معالجتها للإدراج غير المتزامن في جداول Distributed. ويُجمع عدد البايتات لكل shard.
* `CurrentMetric_BrokenDistributedBytesToInsert` ([Int64](/ar/reference/data-types/int-uint)) — عدد البايتات الخاصة بالإدراج غير المتزامن في جداول Distributed التي وُسمت بأنها تالفة. ويُجمع عدد البايتات لكل shard.
* `CurrentMetric_DistributedFilesToInsert` ([Int64](/ar/reference/data-types/int-uint)) — عدد الملفات المعلّقة المطلوب معالجتها للإدراج غير المتزامن في جداول Distributed. ويُجمع عدد الملفات لكل shard.
* `CurrentMetric_BrokenDistributedFilesToInsert` ([Int64](/ar/reference/data-types/int-uint)) — عدد الملفات الخاصة بالإدراج غير المتزامن في جداول Distributed التي وُسمت بأنها تالفة. ويُجمع عدد الملفات لكل shard.
* `CurrentMetric_TablesToDropQueueSize` ([Int64](/ar/reference/data-types/int-uint)) — عدد الجداول التي أُسقطت وتنتظر إزالة البيانات في الخلفية.
* `CurrentMetric_MaxDDLEntryID` ([Int64](/ar/reference/data-types/int-uint)) — أكبر إدخال DDL عالجه DDLWorker.
* `CurrentMetric_MaxPushedDDLEntryID` ([Int64](/ar/reference/data-types/int-uint)) — أكبر إدخال DDL دفعه DDLWorker إلى ZooKeeper.
* `CurrentMetric_PartsTemporary` ([Int64](/ar/reference/data-types/int-uint)) — يجري إنشاء هذا الجزء الآن، وهو غير موجود في قائمة data&#95;parts.
* `CurrentMetric_PartsPreCommitted` ([Int64](/ar/reference/data-types/int-uint)) — مهمل. انظر PartsPreActive.
* `CurrentMetric_PartsCommitted` ([Int64](/ar/reference/data-types/int-uint)) — مهمل. انظر PartsActive.
* `CurrentMetric_PartsPreActive` ([Int64](/ar/reference/data-types/int-uint)) — الجزء موجود في data&#95;parts، لكنه غير مستخدم في عمليات SELECT.
* `CurrentMetric_PartsActive` ([Int64](/ar/reference/data-types/int-uint)) — جزء بيانات نشط، تستخدمه عمليات SELECT الحالية والقادمة.
* `CurrentMetric_AttachedDatabase` ([Int64](/ar/reference/data-types/int-uint)) — قواعد البيانات النشطة.
* `CurrentMetric_AttachedTable` ([Int64](/ar/reference/data-types/int-uint)) — الجداول النشطة.
* `CurrentMetric_AttachedReplicatedTable` ([Int64](/ar/reference/data-types/int-uint)) — الجداول Replicated النشطة.
* `CurrentMetric_AttachedView` ([Int64](/ar/reference/data-types/int-uint)) — طرق العرض النشطة.
* `CurrentMetric_AttachedDictionary` ([Int64](/ar/reference/data-types/int-uint)) — القواميس النشطة.
* `CurrentMetric_PartsOutdated` ([Int64](/ar/reference/data-types/int-uint)) — جزء بيانات غير نشط، لكنه قد يُستخدم فقط بواسطة عمليات SELECT الحالية، ويمكن حذفه بعد انتهاء هذه العمليات.
* `CurrentMetric_PartsDeleting` ([Int64](/ar/reference/data-types/int-uint)) — جزء بيانات غير نشط له عدّاد مراجع خاص به، ويجري حذفه الآن بواسطة أداة التنظيف.
* `CurrentMetric_PartsDeleteOnDestroy` ([Int64](/ar/reference/data-types/int-uint)) — نُقل الجزء إلى disk آخر، ويجب حذفه داخل destructor الخاص به.
* `CurrentMetric_PartsWide` ([Int64](/ar/reference/data-types/int-uint)) — الأجزاء الواسعة.
* `CurrentMetric_PartsCompact` ([Int64](/ar/reference/data-types/int-uint)) — الأجزاء المدمجة.
* `CurrentMetric_MMappedFiles` ([Int64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للملفات المعيّنة بالذاكرة.
* `CurrentMetric_MMappedFileBytes` ([Int64](/ar/reference/data-types/int-uint)) — إجمالي حجم مناطق الملفات المعيّنة بالذاكرة.
* `CurrentMetric_AsynchronousReadWait` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط التي تنتظر القراءة غير المتزامنة.
* `CurrentMetric_PendingAsyncInsert` ([Int64](/ar/reference/data-types/int-uint)) — عدد عمليات الإدراج غير المتزامنة التي تنتظر التفريغ.
* `CurrentMetric_KafkaConsumers` ([Int64](/ar/reference/data-types/int-uint)) — عدد مستهلكي Kafka النشطين
* `CurrentMetric_KafkaConsumersWithAssignment` ([Int64](/ar/reference/data-types/int-uint)) — عدد مستهلكي Kafka النشطين الذين أُسنِدت إليهم بعض التقسيمات.
* `CurrentMetric_KafkaProducers` ([Int64](/ar/reference/data-types/int-uint)) — عدد منتجي Kafka النشطين الذين أُنشِئوا
* `CurrentMetric_KafkaLibrdkafkaThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد خيوط librdkafka النشطة
* `CurrentMetric_KafkaBackgroundReads` ([Int64](/ar/reference/data-types/int-uint)) — عدد عمليات القراءة في الخلفية العاملة حاليًا (لملء العروض المادية من Kafka)
* `CurrentMetric_KafkaConsumersInUse` ([Int64](/ar/reference/data-types/int-uint)) — عدد المستهلكين المستخدمين حاليًا بواسطة عمليات القراءة المباشرة أو في الخلفية
* `CurrentMetric_KafkaWrites` ([Int64](/ar/reference/data-types/int-uint)) — عدد عمليات الإدراج الجارية حاليًا إلى Kafka
* `CurrentMetric_KafkaAssignedPartitions` ([Int64](/ar/reference/data-types/int-uint)) — عدد التقسيمات المسندة حاليًا إلى جداول Kafka
* `CurrentMetric_FilesystemCacheReadBuffers` ([Int64](/ar/reference/data-types/int-uint)) — عدد مخازن قراءة ذاكرة التخزين المؤقت لنظام الملفات النشطة
* `CurrentMetric_CacheFileSegments` ([Int64](/ar/reference/data-types/int-uint)) — عدد مقاطع الملفات الموجودة في ذاكرة التخزين المؤقت
* `CurrentMetric_CacheDetachedFileSegments` ([Int64](/ar/reference/data-types/int-uint)) — عدد مقاطع الملفات المنفصلة الموجودة في ذاكرة التخزين المؤقت
* `CurrentMetric_FilesystemCacheSize` ([Int64](/ar/reference/data-types/int-uint)) — حجم ذاكرة التخزين المؤقت لنظام الملفات بالبايت
* `CurrentMetric_FilesystemCacheSizeLimit` ([Int64](/ar/reference/data-types/int-uint)) — الحد الأقصى لحجم ذاكرة التخزين المؤقت لنظام الملفات بالبايت
* `CurrentMetric_FilesystemCacheElements` ([Int64](/ar/reference/data-types/int-uint)) — عناصر ذاكرة التخزين المؤقت لنظام الملفات (مقاطع الملفات)
* `CurrentMetric_FilesystemCachePriorityQueueElements` ([Int64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للعناصر في طابور الأولوية الخاص بذاكرة التخزين المؤقت لنظام الملفات، بما في ذلك العناصر المُبطلة المعلّقة للإزالة
* `CurrentMetric_FilesystemCacheInvalidatedElements` ([Int64](/ar/reference/data-types/int-uint)) — عدد العناصر المُبطلة (ذات الحجم الصفري والمعلّقة للإزالة) في طابور الأولوية الخاص بذاكرة التخزين المؤقت لنظام الملفات
* `CurrentMetric_FilesystemCacheDownloadQueueElements` ([Int64](/ar/reference/data-types/int-uint)) — عناصر ذاكرة التخزين المؤقت لنظام الملفات الموجودة في طابور التنزيل
* `CurrentMetric_FilesystemCacheDelayedCleanupElements` ([Int64](/ar/reference/data-types/int-uint)) — عناصر ذاكرة التخزين المؤقت لنظام الملفات الموجودة في طابور التنظيف في الخلفية
* `CurrentMetric_FilesystemCacheHoldFileSegments` ([Int64](/ar/reference/data-types/int-uint)) — مقاطع الملفات في ذاكرة التخزين المؤقت لنظام الملفات المحتفَظ بها حاليًا بحيث لا يمكن تحريرها
* `CurrentMetric_FilesystemCacheKeys` ([Int64](/ar/reference/data-types/int-uint)) — عدد المفاتيح في ذاكرة التخزين المؤقت لنظام الملفات
* `CurrentMetric_FilesystemCacheReserveThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط التي تحاول حجز مساحة في ذاكرة التخزين المؤقت
* `CurrentMetric_AsyncInsertCacheSize` ([Int64](/ar/reference/data-types/int-uint)) — عدد معرّفات التجزئة للإدراج غير المتزامن في ذاكرة التخزين المؤقت
* `CurrentMetric_IcebergMetadataFilesCacheBytes` ([Int64](/ar/reference/data-types/int-uint)) — حجم ذاكرة التخزين المؤقت لملفات البيانات الوصفية لـ Iceberg بالبايت
* `CurrentMetric_IcebergMetadataFilesCacheFiles` ([Int64](/ar/reference/data-types/int-uint)) — عدد الملفات المخزنة مؤقتًا في ذاكرة التخزين المؤقت لملفات البيانات الوصفية لـ Iceberg
* `CurrentMetric_ParquetMetadataCacheBytes` ([Int64](/ar/reference/data-types/int-uint)) — حجم ذاكرة التخزين المؤقت للبيانات الوصفية لـ Parquet بالبايت
* `CurrentMetric_ParquetMetadataCacheFiles` ([Int64](/ar/reference/data-types/int-uint)) — عدد الملفات المخزنة مؤقتًا في ذاكرة التخزين المؤقت للبيانات الوصفية لـ Parquet
* `CurrentMetric_AvroSchemaCacheBytes` ([Int64](/ar/reference/data-types/int-uint)) — حجم ذاكرة التخزين المؤقت لمخطط Avro بالبايت
* `CurrentMetric_AvroSchemaCacheCells` ([Int64](/ar/reference/data-types/int-uint)) — عدد مخططات Avro المخزنة مؤقتًا
* `CurrentMetric_AvroSchemaRegistryCacheBytes` ([Int64](/ar/reference/data-types/int-uint)) — حجم ذاكرة التخزين المؤقت لسجل مخططات Avro بالبايت
* `CurrentMetric_AvroSchemaRegistryCacheCells` ([Int64](/ar/reference/data-types/int-uint)) — عدد الإدخالات في ذاكرة التخزين المؤقت لسجل مخططات Avro
* `CurrentMetric_HiveFilesCacheBytes` ([Int64](/ar/reference/data-types/int-uint)) — حجم ذاكرة التخزين المؤقت لملفات Hive بالبايت
* `CurrentMetric_HiveFilesCacheFiles` ([Int64](/ar/reference/data-types/int-uint)) — عدد الملفات المخزنة مؤقتًا في ذاكرة التخزين المؤقت لملفات Hive
* `CurrentMetric_HiveMetadataFilesCacheBytes` ([Int64](/ar/reference/data-types/int-uint)) — حجم ذاكرة التخزين المؤقت لملفات البيانات الوصفية لـ Hive بالبايت
* `CurrentMetric_HiveMetadataFilesCacheFiles` ([Int64](/ar/reference/data-types/int-uint)) — عدد الملفات المخزنة مؤقتًا في ذاكرة التخزين المؤقت لملفات البيانات الوصفية لـ Hive
* `CurrentMetric_VectorSimilarityIndexCacheBytes` ([Int64](/ar/reference/data-types/int-uint)) — حجم ذاكرة التخزين المؤقت لفهرس تشابه المتجهات بالبايت
* `CurrentMetric_VectorSimilarityIndexCacheCells` ([Int64](/ar/reference/data-types/int-uint)) — عدد الإدخالات في ذاكرة التخزين المؤقت لفهرس تشابه المتجهات
* `CurrentMetric_TextIndexTokensCacheBytes` ([Int64](/ar/reference/data-types/int-uint)) — حجم ذاكرة التخزين المؤقت لرموز الفهرس النصي بالبايت
* `CurrentMetric_TextIndexTokensCacheCells` ([Int64](/ar/reference/data-types/int-uint)) — عدد الإدخالات في ذاكرة التخزين المؤقت لرموز الفهرس النصي
* `CurrentMetric_TextIndexHeaderCacheBytes` ([Int64](/ar/reference/data-types/int-uint)) — حجم ذاكرة التخزين المؤقت لترويسة الفهرس النصي بالبايت
* `CurrentMetric_TextIndexHeaderCacheCells` ([Int64](/ar/reference/data-types/int-uint)) — عدد الإدخالات في ذاكرة التخزين المؤقت لترويسة الفهرس النصي
* `CurrentMetric_TextIndexPostingsCacheBytes` ([Int64](/ar/reference/data-types/int-uint)) — حجم ذاكرة التخزين المؤقت لقوائم الإحالة في الفهرس النصي بالبايت
* `CurrentMetric_TextIndexPostingsCacheCells` ([Int64](/ar/reference/data-types/int-uint)) — عدد الإدخالات في ذاكرة التخزين المؤقت لقوائم الإحالة في الفهرس النصي
* `CurrentMetric_DNSHostsCacheBytes` ([Int64](/ar/reference/data-types/int-uint)) — حجم ذاكرة التخزين المؤقت لمضيفات DNS بالبايت
* `CurrentMetric_DNSHostsCacheSize` ([Int64](/ar/reference/data-types/int-uint)) — عدد مضيفات DNS المخزنة مؤقتًا
* `CurrentMetric_DNSAddressesCacheBytes` ([Int64](/ar/reference/data-types/int-uint)) — حجم ذاكرة التخزين المؤقت لعناوين DNS بالبايت
* `CurrentMetric_DNSAddressesCacheSize` ([Int64](/ar/reference/data-types/int-uint)) — عدد عناوين DNS المخزنة مؤقتًا
* `CurrentMetric_MarkCacheBytes` ([Int64](/ar/reference/data-types/int-uint)) — الحجم الإجمالي لذاكرة التخزين المؤقت للعلامات بالبايت
* `CurrentMetric_MarkCacheFiles` ([Int64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لملفات العلامات المخزنة مؤقتًا في ذاكرة التخزين المؤقت للعلامات
* `CurrentMetric_NamedCollection` ([Int64](/ar/reference/data-types/int-uint)) — عدد المجموعات المسماة
* `CurrentMetric_PrimaryIndexCacheBytes` ([Int64](/ar/reference/data-types/int-uint)) — الحجم الإجمالي لذاكرة التخزين المؤقت للفهرس الأساسي بالبايت
* `CurrentMetric_PrimaryIndexCacheFiles` ([Int64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لملفات الفهرس المخزنة مؤقتًا في ذاكرة التخزين المؤقت للفهرس الأساسي
* `CurrentMetric_PageCacheBytes` ([Int64](/ar/reference/data-types/int-uint)) — الحجم الإجمالي لذاكرة تخزين الصفحات المؤقتة في مساحة المستخدم بالبايت
* `CurrentMetric_PageCacheCells` ([Int64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للإدخالات في ذاكرة تخزين الصفحات المؤقتة في مساحة المستخدم
* `CurrentMetric_UncompressedCacheBytes` ([Int64](/ar/reference/data-types/int-uint)) — الحجم الإجمالي لذاكرة التخزين المؤقت غير المضغوطة بالبايت. لا تؤدي ذاكرة التخزين المؤقت غير المضغوطة عادةً إلى تحسين الأداء، لذا ينبغي تجنبها في معظم الحالات
* `CurrentMetric_UncompressedCacheCells` ([Int64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للإدخالات في ذاكرة التخزين المؤقت غير المضغوطة. يمثّل كل إدخال كتلة بيانات مفكوكة الضغط. لا تؤدي ذاكرة التخزين المؤقت غير المضغوطة عادةً إلى تحسين الأداء، لذا ينبغي تجنبها في معظم الحالات
* `CurrentMetric_IndexMarkCacheBytes` ([Int64](/ar/reference/data-types/int-uint)) — الحجم الإجمالي لذاكرة التخزين المؤقت للعلامات الخاصة بالفهارس الثانوية بالبايت
* `CurrentMetric_IndexMarkCacheFiles` ([Int64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لملفات العلامات المخزنة مؤقتًا في ذاكرة التخزين المؤقت للعلامات الخاصة بالفهارس الثانوية
* `CurrentMetric_IndexUncompressedCacheBytes` ([Int64](/ar/reference/data-types/int-uint)) — الحجم الإجمالي لذاكرة التخزين المؤقت غير المضغوطة للفهارس الثانوية بالبايت. لا تؤدي ذاكرة التخزين المؤقت غير المضغوطة عادةً إلى تحسين الأداء، لذا ينبغي تجنبها في معظم الحالات
* `CurrentMetric_IndexUncompressedCacheCells` ([Int64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للإدخالات في ذاكرة التخزين المؤقت غير المضغوطة للفهارس الثانوية. يمثّل كل إدخال كتلة بيانات مفكوكة الضغط. لا تؤدي ذاكرة التخزين المؤقت غير المضغوطة عادةً إلى تحسين الأداء، لذا ينبغي تجنبها في معظم الحالات
* `CurrentMetric_MMapCacheCells` ([Int64](/ar/reference/data-types/int-uint)) — عدد الملفات المفتوحة باستخدام `mmap` (المعيّنة في الذاكرة). يُستخدم هذا للاستعلامات التي يكون فيها الإعداد `local_filesystem_read_method` مضبوطًا على `mmap`. وتُحتفظ الملفات المفتوحة باستخدام `mmap` في ذاكرة التخزين المؤقت لتجنب عمليات تفريغ TLB المكلفة.
* `CurrentMetric_QueryCacheBytes` ([Int64](/ar/reference/data-types/int-uint)) — الحجم الإجمالي لذاكرة التخزين المؤقت للاستعلامات بالبايت
* `CurrentMetric_QueryCacheEntries` ([Int64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للإدخالات في ذاكرة التخزين المؤقت للاستعلامات
* `CurrentMetric_QueryConditionCacheBytes` ([Int64](/ar/reference/data-types/int-uint)) — الحجم الإجمالي لذاكرة التخزين المؤقت لشروط الاستعلام بالبايت
* `CurrentMetric_QueryConditionCacheEntries` ([Int64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للإدخالات في ذاكرة التخزين المؤقت لشروط الاستعلام
* `CurrentMetric_CompiledExpressionCacheBytes` ([Int64](/ar/reference/data-types/int-uint)) — إجمالي البايتات المستخدمة لذاكرة التخزين المؤقت للشيفرة المترجمة فوريًا بواسطة JIT
* `CurrentMetric_CompiledExpressionCacheCount` ([Int64](/ar/reference/data-types/int-uint)) — إجمالي الإدخالات في ذاكرة التخزين المؤقت للشيفرة المترجمة فوريًا بواسطة JIT
* `CurrentMetric_SerializationCacheBytesInMemoryAllocated` ([Int64](/ar/reference/data-types/int-uint)) — الحجم الإجمالي لذاكرة التخزين المؤقت للتسلسل بالبايت، بما في ذلك المفاتيح والكلفة الإضافية الناتجة عن الخانات الفارغة
* `CurrentMetric_SerializationCacheBytesInMemory` ([Int64](/ar/reference/data-types/int-uint)) — الحجم الإجمالي لذاكرة التخزين المؤقت للتسلسل بالبايت، بما يشمل القيم فقط
* `CurrentMetric_SerializationCacheCount` ([Int64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للإدخالات في ذاكرة التخزين المؤقت للتسلسل
* `CurrentMetric_MergeJoinBlocksCacheBytes` ([Int64](/ar/reference/data-types/int-uint)) — إجمالي عدد البايتات المستخدمة للكتل المخزنة مؤقتًا في MergeJoin
* `CurrentMetric_MergeJoinBlocksCacheCount` ([Int64](/ar/reference/data-types/int-uint)) — إجمالي عدد الكتل المخزنة مؤقتًا في MergeJoin
* `CurrentMetric_BcryptCacheBytes` ([Int64](/ar/reference/data-types/int-uint)) — الحجم الإجمالي لذاكرة التخزين المؤقت لمصادقة bcrypt بالبايت
* `CurrentMetric_BcryptCacheSize` ([Int64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للإدخالات في ذاكرة التخزين المؤقت لمصادقة bcrypt
* `CurrentMetric_ColumnsDescriptionsCacheSize` ([Int64](/ar/reference/data-types/int-uint)) — حجم ذاكرة التخزين المؤقت لـ ColumnsDescriptions (ذاكرة تخزين مؤقت لكل جدول)
* `CurrentMetric_S3Requests` ([Int64](/ar/reference/data-types/int-uint)) — عدد طلبات S3
* `CurrentMetric_KeeperAliveConnections` ([Int64](/ar/reference/data-types/int-uint)) — عدد الاتصالات النشطة
* `CurrentMetric_KeeperOutstandingRequests` ([Int64](/ar/reference/data-types/int-uint)) — عدد الطلبات المعلّقة
* `CurrentMetric_ThreadsInOvercommitTracker` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ المنتظرة داخل OvercommitTracker
* `CurrentMetric_IOUringPendingEvents` ([Int64](/ar/reference/data-types/int-uint)) — عدد عناصر SQEs الخاصة بـ io&#95;uring المنتظرة للإرسال
* `CurrentMetric_IOUringInFlightEvents` ([Int64](/ar/reference/data-types/int-uint)) — عدد عناصر SQEs الخاصة بـ io&#95;uring قيد التنفيذ
* `CurrentMetric_ReadTaskRequestsSent` ([Int64](/ar/reference/data-types/int-uint)) — العدد الحالي لطلبات callback قيد التنفيذ من الخادم البعيد إلى الخادم initiator لاختيار مهمة القراءة (لدالة table function ‏s3Cluster وما شابهها). يُقاس هذا على جانب الخادم البعيد.
* `CurrentMetric_MergeTreeReadTaskRequestsSent` ([Int64](/ar/reference/data-types/int-uint)) — العدد الحالي لطلبات callback قيد التنفيذ من الخادم البعيد إلى الخادم initiator لاختيار مهمة القراءة (لجداول MergeTree). يُقاس هذا على جانب الخادم البعيد.
* `CurrentMetric_MergeTreeAllRangesAnnouncementsSent` ([Int64](/ar/reference/data-types/int-uint)) — العدد الحالي لإعلانات announcements الجاري إرسالها من الخادم البعيد إلى الخادم initiator بشأن مجموعة data parts (لجداول MergeTree). يُقاس هذا على جانب الخادم البعيد.
* `CurrentMetric_CreatedTimersInQueryProfiler` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤقتات thread local التي تم إنشاؤها في QueryProfiler
* `CurrentMetric_ActiveTimersInQueryProfiler` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤقتات thread local النشطة في QueryProfiler
* `CurrentMetric_RefreshableViews` ([Int64](/ar/reference/data-types/int-uint)) — عدد materialized views ذات التحديث الدوري (REFRESH)
* `CurrentMetric_RefreshingViews` ([Int64](/ar/reference/data-types/int-uint)) — عدد materialized views التي تنفّذ عملية refresh حاليًا
* `CurrentMetric_StorageBufferFlushThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ الخاصة بعمليات flushes الخلفية في StorageBuffer
* `CurrentMetric_StorageBufferFlushThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ الخاصة بعمليات flushes الخلفية في StorageBuffer التي تنفّذ مهمة
* `CurrentMetric_StorageBufferFlushThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ النشطة أو الموجودة في queue لعمليات flushes الخلفية في StorageBuffer
* `CurrentMetric_SharedMergeTreeThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ في thread pools ضمن المكوّنات الداخلية لـ SharedMergeTree
* `CurrentMetric_SharedMergeTreeThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ في thread pools ضمن المكوّنات الداخلية لـ SharedMergeTree التي تنفّذ مهمة
* `CurrentMetric_SharedMergeTreeThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ النشطة أو الموجودة في queue ضمن thread pools في المكوّنات الداخلية لـ SharedMergeTree
* `CurrentMetric_SharedMergeTreeFetch` ([Int64](/ar/reference/data-types/int-uint)) — عدد عمليات fetch الجارية
* `CurrentMetric_SharedMergeTreeAssignedCurrentParts` ([Int64](/ar/reference/data-types/int-uint)) — عدد الأجزاء المقفلة بسبب عمليات الدمج أو التعديل
* `CurrentMetric_SharedMergeTreeOutdatedPartsInKeeper` ([Int64](/ar/reference/data-types/int-uint)) — عدد سجلات الأجزاء القديمة المخزنة في Keeper
* `CurrentMetric_SharedMergeTreeCondemnedPartsInKeeper` ([Int64](/ar/reference/data-types/int-uint)) — عدد سجلات الأجزاء الموسومة للإزالة المخزنة في Keeper
* `CurrentMetric_SharedMergeTreeBrokenCondemnedPartsInKeeper` ([Int64](/ar/reference/data-types/int-uint)) — عدد سجلات الأجزاء المعطلة والموسومة للإزالة المخزنة في Keeper
* `CurrentMetric_SharedMergeTreeMaxActiveReplicas` ([Int64](/ar/reference/data-types/int-uint)) — الحد الأقصى لعدد النسخ المتماثلة النشطة المسجلة في Keeper
* `CurrentMetric_SharedMergeTreeMaxInactiveReplicas` ([Int64](/ar/reference/data-types/int-uint)) — الحد الأقصى لعدد النسخ المتماثلة غير النشطة المسجلة في Keeper
* `CurrentMetric_SharedMergeTreeMaxReplicas` ([Int64](/ar/reference/data-types/int-uint)) — الحد الأقصى لعدد النسخ المتماثلة المسجلة في Keeper عبر جميع الجداول. لاحظ أن هذا العدد قد لا يساوي مجموع SharedMergeTreeMaxActiveReplicas وSharedMergeTreeMaxInactiveReplicas
* `CurrentMetric_SharedMergeTreeMinActiveReplicas` ([Int64](/ar/reference/data-types/int-uint)) — الحد الأدنى لعدد النسخ المتماثلة النشطة المسجلة في Keeper
* `CurrentMetric_SharedMergeTreeMinInactiveReplicas` ([Int64](/ar/reference/data-types/int-uint)) — الحد الأدنى لعدد النسخ المتماثلة غير النشطة المسجلة في Keeper
* `CurrentMetric_SharedMergeTreeMinReplicas` ([Int64](/ar/reference/data-types/int-uint)) — الحد الأدنى لعدد النسخ المتماثلة المسجلة في Keeper عبر جميع الجداول. لاحظ أن هذا العدد قد لا يساوي مجموع SharedMergeTreeMinActiveReplicas وSharedMergeTreeMinInactiveReplicas
* `CurrentMetric_SharedMergeTreeMinPartitions` ([Int64](/ar/reference/data-types/int-uint)) — الحد الأدنى لعدد الأقسام المسجلة في Keeper عبر جميع جداول SharedMergeTree
* `CurrentMetric_SharedMergeTreeMaxPartitions` ([Int64](/ar/reference/data-types/int-uint)) — الحد الأقصى لعدد الأقسام المسجلة في Keeper عبر جميع جداول SharedMergeTree
* `CurrentMetric_CacheWarmerBytesInProgress` ([Int64](/ar/reference/data-types/int-uint)) — الحجم الإجمالي لمقاطع الملفات البعيدة التي تنتظر تحميلها بشكل غير متزامن إلى ذاكرة التخزين المؤقت لنظام الملفات.
* `CurrentMetric_DistrCacheOpenedConnections` ([Int64](/ar/reference/data-types/int-uint)) — عدد الاتصالات المفتوحة مع Distributed Cache
* `CurrentMetric_DistrCacheSharedLimitCount` ([Int64](/ar/reference/data-types/int-uint)) — عدد الاتصالات المفتوحة وفقًا لـ DistributedCache::ConnectionPool::SharedLimit
* `CurrentMetric_DistrCacheUsedConnections` ([Int64](/ar/reference/data-types/int-uint)) — عدد الاتصالات المستخدمة حاليًا مع Distributed Cache
* `CurrentMetric_DistrCacheAllocatedConnections` ([Int64](/ar/reference/data-types/int-uint)) — عدد الاتصالات المخصصة حاليًا في مجموعة اتصالات Distributed Cache
* `CurrentMetric_DistrCacheBorrowedConnections` ([Int64](/ar/reference/data-types/int-uint)) — عدد الاتصالات المستعارة حاليًا من مجموعة اتصالات Distributed Cache
* `CurrentMetric_DistrCacheReadRequests` ([Int64](/ar/reference/data-types/int-uint)) — عدد طلبات القراءة المنفذة إلى Distributed Cache
* `CurrentMetric_DistrCacheWriteRequests` ([Int64](/ar/reference/data-types/int-uint)) — عدد طلبات الكتابة المنفذة إلى Distributed Cache
* `CurrentMetric_DistrCacheWriteBuffers` ([Int64](/ar/reference/data-types/int-uint)) — عدد مخازن الكتابة المؤقتة الخاصة بـ Distributed Cache
* `CurrentMetric_DistrCacheReadBuffers` ([Int64](/ar/reference/data-types/int-uint)) — عدد مخازن القراءة المؤقتة الخاصة بـ Distributed Cache
* `CurrentMetric_DistrCacheServerConnections` ([Int64](/ar/reference/data-types/int-uint)) — عدد الاتصالات المفتوحة مع خادم ClickHouse من Distributed Cache
* `CurrentMetric_DistrCacheRegisteredServers` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخوادم المسجلة في Distributed Cache
* `CurrentMetric_DistrCacheRegisteredServersCurrentAZ` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخوادم المسجلة في Distributed Cache ضمن منطقة التوفر الحالية
* `CurrentMetric_DistrCacheServerS3CachedClients` ([Int64](/ar/reference/data-types/int-uint)) — عدد عملاء S3 المخزّنين مؤقتًا في Distributed Cache
* `CurrentMetric_DistrCacheServerRegistryConnections` ([Int64](/ar/reference/data-types/int-uint)) — عدد الاتصالات النشطة بخادم ClickHouse من Distributed Cache (غير المعلَّمة على أنها مُلغاة بسبب الحد)
* `CurrentMetric_SchedulerIOReadScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد عمليات قراءة IO الجاري جدولتها حاليًا
* `CurrentMetric_SchedulerIOWriteScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد عمليات كتابة IO الجاري جدولتها حاليًا
* `CurrentMetric_StorageConnectionsStored` ([Int64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للجلسات المخزنة في مجمّع الجلسات لوحدات التخزين
* `CurrentMetric_StorageConnectionsTotal` ([Int64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لجميع الجلسات: المخزنة في المجمّع والمستخدَمة حاليًا لوحدات التخزين
* `CurrentMetric_DiskConnectionsStored` ([Int64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للجلسات المخزنة في مجمّع الجلسات للأقراص
* `CurrentMetric_DiskConnectionsTotal` ([Int64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لجميع الجلسات: المخزنة في المجمّع والمستخدَمة حاليًا للأقراص
* `CurrentMetric_BlobKillerThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في مجمّع الخيوط لعملية الإزالة في الخلفية لقرص تخزين الكائنات
* `CurrentMetric_BlobKillerThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في مجمّع الخيوط لعملية الإزالة في الخلفية لقرص تخزين الكائنات التي تنفّذ مهمة
* `CurrentMetric_BlobKillerThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة أو الموجودة في قائمة الانتظار في مجمّع الخيوط لعملية الإزالة في الخلفية لقرص تخزين الكائنات
* `CurrentMetric_BlobCopierThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في مجمّع الخيوط لعملية النسخ المتماثل في الخلفية لقرص تخزين الكائنات
* `CurrentMetric_BlobCopierThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط في مجمّع الخيوط لعملية النسخ المتماثل في الخلفية لقرص تخزين الكائنات التي تنفّذ مهمة
* `CurrentMetric_BlobCopierThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام النشطة أو الموجودة في قائمة الانتظار في مجمّع الخيوط لعملية النسخ المتماثل في الخلفية لقرص تخزين الكائنات
* `CurrentMetric_HTTPConnectionsStored` ([Int64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للجلسات المخزنة في مجمّع الجلسات لمضيفي HTTP
* `CurrentMetric_HTTPConnectionsTotal` ([Int64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لجميع الجلسات: المخزنة في المجمّع والمستخدَمة حاليًا لمضيفي HTTP
* `CurrentMetric_AddressesActive` ([Int64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للعناوين المستخدَمة لإنشاء اتصالات مع مجمّعات الاتصالات
* `CurrentMetric_AddressesBanned` ([Int64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للعناوين المحظورة باعتبارها معيبة عند إنشاء اتصالات مع مجمّعات الاتصالات
* `CurrentMetric_FilteringMarksWithPrimaryKey` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط التي تُجري حاليًا تصفية نطاقات العلامات باستخدام المفتاح الأساسي
* `CurrentMetric_FilteringMarksWithSecondaryKeys` ([Int64](/ar/reference/data-types/int-uint)) — عدد الخيوط التي تُجري حاليًا تصفية نطاقات العلامات باستخدام المفاتيح الثانوية
* `CurrentMetric_ConcurrencyControlScheduled` ([Int64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لطلبات CPU slot الجاري جدولتها حاليًا
* `CurrentMetric_ConcurrencyControlAcquired` ([Int64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لـ CPU slots التي تم الحصول عليها
* `CurrentMetric_ConcurrencyControlAcquiredNonCompeting` ([Int64](/ar/reference/data-types/int-uint)) — العدد الإجمالي لـ CPU slots التي تم الحصول عليها ولا تُعدّ متنافسة (الخيط الأول إذا كان المجدول fair&#95;round&#95;robin مستخدمًا)
* `CurrentMetric_ConcurrencyControlSoftLimit` ([Int64](/ar/reference/data-types/int-uint)) — قيمة الحد المرن لعدد CPU slots
* `CurrentMetric_ConcurrencyControlPreempted` ([Int64](/ar/reference/data-types/int-uint)) — العدد الإجمالي للخيوط التي جرى استباقها وتنتظر CPU slot
* `CurrentMetric_ConcurrentQueryScheduled` ([Int64](/ar/reference/data-types/int-uint)) — إجمالي عدد طلبات فتحات الاستعلام التي تتم جدولتها حاليًا
* `CurrentMetric_ConcurrentQueryAcquired` ([Int64](/ar/reference/data-types/int-uint)) — إجمالي عدد فتحات الاستعلام التي تم الحصول عليها
* `CurrentMetric_DiskS3NoSuchKeyErrors` ([Int64](/ar/reference/data-types/int-uint)) — عدد أخطاء `NoSuchKey` التي تحدث عند قراءة البيانات من cloud storage عبر S3 من خلال الأقراص في ClickHouse.
* `CurrentMetric_SharedCatalogStateApplicationThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ في تجمع سلاسل التنفيذ المخصّص لتطبيق الحالة في Shared Catalog.
* `CurrentMetric_SharedCatalogStateApplicationThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ النشطة في تجمع سلاسل التنفيذ المخصّص لتطبيق الحالة في Shared Catalog.
* `CurrentMetric_SharedCatalogStateApplicationThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في تجمع سلاسل التنفيذ المخصّص لتطبيق الحالة في Shared Catalog.
* `CurrentMetric_SharedCatalogDropLocalThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ في تجمع سلاسل التنفيذ المخصّص لحذف الجداول المحلية في Shared Catalog.
* `CurrentMetric_SharedCatalogDropLocalThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ النشطة في تجمع سلاسل التنفيذ المخصّص لحذف الجداول المحلية في Shared Catalog.
* `CurrentMetric_SharedCatalogDropLocalThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في تجمع سلاسل التنفيذ المخصّص لحذف الجداول المحلية في Shared Catalog.
* `CurrentMetric_SharedCatalogDropZooKeeperThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ في تجمع سلاسل التنفيذ المخصّص لحذف الكائنات في ZooKeeper ضمن Shared Catalog.
* `CurrentMetric_SharedCatalogDropZooKeeperThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ النشطة في تجمع سلاسل التنفيذ المخصّص لحذف الكائنات في ZooKeeper ضمن Shared Catalog.
* `CurrentMetric_SharedCatalogDropZooKeeperThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في تجمع سلاسل التنفيذ المخصّص لحذف الكائنات في ZooKeeper ضمن Shared Catalog.
* `CurrentMetric_CoordinatedMergesCoordinatorRunningMerges` ([Int64](/ar/reference/data-types/int-uint)) — عدد عمليات الدمج الجارية المرئية لمنسّق الدمج.
* `CurrentMetric_CoordinatedMergesCoordinatorAssignedMerges` ([Int64](/ar/reference/data-types/int-uint)) — عدد عمليات الدمج المعيّنة في حالة منسّق الدمج.
* `CurrentMetric_CoordinatedMergesWorkerAssignedMerges` ([Int64](/ar/reference/data-types/int-uint)) — عدد عمليات الدمج المعيّنة في حالة عامل الدمج.
* `CurrentMetric_SharedDatabaseCatalogTablesInLocalDropDetachQueue` ([Int64](/ar/reference/data-types/int-uint)) — عدد الجداول الموجودة في قائمة الانتظار الخاصة بالحذف أو الفصل المحلي في Shared Catalog.
* `CurrentMetric_SharedCatalogDropDetachLocalTablesErrors` ([Int64](/ar/reference/data-types/int-uint)) — عدد الأخطاء التي حدثت عند محاولة حذف الجداول المحلية أو فصلها في Shared Catalog.
* `CurrentMetric_SharedCatalogNumberOfObjectsInState` ([Int64](/ar/reference/data-types/int-uint)) — عدد الكائنات في الحالة الحالية لـ Shared Catalog.
* `CurrentMetric_MetadataFromKeeperCacheObjects` ([Int64](/ar/reference/data-types/int-uint)) — عدد الكائنات في ذاكرة التخزين المؤقت للبيانات الوصفية الخاصة بالقرص.
* `CurrentMetric_LicenseRemainingSeconds` ([Int64](/ar/reference/data-types/int-uint)) — الثواني المتبقية من مدة صلاحية الترخيص.
* `CurrentMetric_StartupScriptsExecutionState` ([Int64](/ar/reference/data-types/int-uint)) — حالة تنفيذ نصوص بدء التشغيل: 0 = لم يكتمل، 1 = نجاح، 2 = فشل.
* `CurrentMetric_IsServerShuttingDown` ([Int64](/ar/reference/data-types/int-uint)) — يشير إلى ما إذا كان الخادم قيد إيقاف التشغيل: 0 = لا، 1 = نعم
* `CurrentMetric_StatelessWorkerThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ في تجمع سلاسل تنفيذ العامل عديم الحالة.
* `CurrentMetric_StatelessWorkerThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد سلاسل التنفيذ في تجمع سلاسل تنفيذ العامل عديم الحالة التي تُشغّل مهمة.
* `CurrentMetric_StatelessWorkerThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد المهام الموجودة في قائمة الانتظار أو النشطة في تجمع سلاسل تنفيذ العامل عديم الحالة.
* `CurrentMetric_ReadonlyDisks` ([Int64](/ar/reference/data-types/int-uint)) — عدد الأقراص التي وُسِمت بأنها للقراءة فقط أثناء فحص القرص.
* `CurrentMetric_BrokenDisks` ([Int64](/ar/reference/data-types/int-uint)) — عدد الأقراص التي وُسِمت بأنها تالفة أثناء فحص القرص.
* `CurrentMetric_TaskTrackerThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط التي يستخدمها متعقّب المهام البعيدة للاستعلام الموزّع.
* `CurrentMetric_TaskTrackerThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط في مجموعة مؤشرات الترابط الخاصة بمتعقّب المهام البعيدة للاستعلام الموزّع التي تنفّذ مهمة.
* `CurrentMetric_TaskTrackerThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد الوظائف الموجودة في قائمة الانتظار أو النشطة في مجموعة مؤشرات الترابط الخاصة بمتعقّب المهام البعيدة للاستعلام الموزّع.
* `CurrentMetric_DropDistributedCacheThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط في مجموعة مؤشرات الترابط الخاصة باستعلام drop distributed cache.
* `CurrentMetric_DropDistributedCacheThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط النشطة في مجموعة مؤشرات الترابط الخاصة باستعلام drop distributed cache.
* `CurrentMetric_DropDistributedCacheThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد الوظائف الموجودة في قائمة الانتظار أو النشطة في مجموعة مؤشرات الترابط الخاصة بـ drop distributed cache.
* `CurrentMetric_S3CachedCredentialsProviders` ([Int64](/ar/reference/data-types/int-uint)) — إجمالي عدد موفري بيانات الاعتماد المخزّنة مؤقتًا.
* `CurrentMetric_MergeTreeSnapshotCommitThreads` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط المستخدمة لتثبيت اللقطة.
* `CurrentMetric_MergeTreeSnapshotCommitThreadsActive` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط النشطة المستخدمة لتثبيت اللقطة.
* `CurrentMetric_MergeTreeSnapshotCommitThreadsScheduled` ([Int64](/ar/reference/data-types/int-uint)) — عدد مؤشرات الترابط المجدولة المستخدمة لتثبيت اللقطة.

{/*AUTOGENERATED_END*/}

<div id="example">
  ## مثال
</div>

```sql
SELECT * FROM system.metric_log LIMIT 1 FORMAT Vertical;
```

```text
Row 1:
──────
hostname:                                                        clickhouse.eu-central1.internal
event_date:                                                      2020-09-05
event_time:                                                      2020-09-05 16:22:33
event_time_microseconds:                                         2020-09-05 16:22:33.196807
milliseconds:                                                    196
ProfileEvent_Query:                                              0
ProfileEvent_SelectQuery:                                        0
ProfileEvent_InsertQuery:                                        0
ProfileEvent_FailedQuery:                                        0
ProfileEvent_FailedSelectQuery:                                  0
...
...
CurrentMetric_Revision:                                          54439
CurrentMetric_VersionInteger:                                    20009001
CurrentMetric_RWLockWaitingReaders:                              0
CurrentMetric_RWLockWaitingWriters:                              0
CurrentMetric_RWLockActiveReaders:                               0
CurrentMetric_RWLockActiveWriters:                               0
CurrentMetric_GlobalThread:                                      74
CurrentMetric_GlobalThreadActive:                                26
CurrentMetric_LocalThread:                                       0
CurrentMetric_LocalThreadActive:                                 0
CurrentMetric_DistributedFilesToInsert:                          0
```

**المخطط**
يمكن تهيئة هذا الجدول بأنواع مختلفة من المخططات باستخدام وسم XML `<schema_type>`. نوع المخطط الافتراضي هو `wide`، حيث يُخزَّن كل مقياس أو حدث profile في عمود منفصل. هذا المخطط هو الأفضل أداءً والأكثر كفاءة لعمليات القراءة من عمود واحد.

يخزّن المخطط `transposed` البيانات بتنسيق مشابه لـ `system.asynchronous_metric_log`، حيث تُخزَّن المقاييس والأحداث على شكل صفوف. ويكون هذا المخطط مفيدًا في البيئات محدودة الموارد لأنه يقلّل استهلاك الموارد أثناء عمليات الدمج.

**المدرجات التكرارية**

يحمل كل صف أيضًا لقطة لكل مقياس مدرج تكراري مسجَّل في عمود `histograms` من النوع `Nested`، ويتضمن الحقول `metric` و`labels` و`histogram` و`count` و`sum`. تكون أعداد الـ bucket تراكمية منذ بدء تشغيل الخادم. افتراضيًا، لا يتم إخراج المدرجات التكرارية التي يكون فيها إجمالي `count` صفرًا، كما تُحذف الـ bucket ذات العداد الصفري داخل المدرج التكراري المُخرَج من map ‏`histogram`؛ اضبط `system_metric_log_show_zero_values_in_histograms = 1` (في profile المستخدم `default`) للاحتفاظ بجميع المدرجات التكرارية وجميع الـ bucket.

مثال على استعلام:

```sql
SELECT h.metric, h.labels, h.histogram, h.count, h.sum
FROM system.metric_log
ARRAY JOIN histograms AS h
WHERE h.metric = 'keeper_response_time_ms' AND h.labels['operation_type'] = 'readonly'
ORDER BY event_time DESC
LIMIT 1;
```

<div id="see-also">
  ## انظر أيضًا
</div>

* [إعداد metric&#95;log](/ar/reference/settings/server-settings/settings#metric_log) — تمكين الإعداد وتعطيله.
* [system.asynchronous&#95;metrics](/ar/reference/system-tables/asynchronous_metrics) — يحتوي على مقاييس تُحتسب دوريًا.
* [system.events](/ar/reference/system-tables/events) — يحتوي على عدد من الأحداث التي حدثت.
* [system.metrics](/ar/reference/system-tables/metrics) — يحتوي على مقاييس تُحتسب لحظيًا.
* [المراقبة](/ar/guides/oss/deployment-and-scaling/monitoring/monitoring) — المفاهيم الأساسية لمراقبة ClickHouse.