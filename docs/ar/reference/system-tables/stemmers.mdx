---
description: 'جدول نظامي يعرض جميع خوارزميات التجذير المتاحة.'
keywords: ['جدول نظامي', 'خوارزميات التجذير']
sidebarTitle: 'system.stemmers'
slug: /operations/system-tables/stemmers
title: 'system.stemmers'
doc_type: 'مرجع'
---

<div id="description">
  ## الوصف
</div>

يعرض جميع خوارزميات التجذير المتاحة.
يمكن استخدامها في الدالة [stem](/ar/reference/functions/regular-functions/nlp-functions).

<Info>
  **التوفر**

  لا يتوفر `system.stemmers` إلا في إصدارات ClickHouse المُجمَّعة مع الاعتمادية `libstemmer` (`USE_LIBSTEMMER`). وفي الإصدارات التي لا تتضمنها، لا يكون الجدول موجودًا، وتفشل الاستعلامات الموجَّهة إليه بالخطأ `UNKNOWN_TABLE`. يمكنك التحقق مما إذا كانت مفعّلة في إصدارك باستخدام:

  ```sql
  SELECT value FROM system.build_options WHERE name = 'USE_LIBSTEMMER';
  ```
</Info>

الأعمدة:

{/*AUTOGENERATED_START*/}

* `name` ([String](/ar/reference/data-types/index)) — معرّف خوارزمية التجذير Snowball (اللغة/الخوارزمية)

{/*AUTOGENERATED_END*/}

<div id="example">
  ## مثال
</div>

```sql
SELECT * FROM system.stemmers;
```

```text
 ┌─name───────┐
 │ arabic     │
 │ armenian   │
 │ basque     │
 │ catalan    │
 │ danish     │
 │ dutch      │
 │ english    │
 │ [...]      │
 └────────────┘
```