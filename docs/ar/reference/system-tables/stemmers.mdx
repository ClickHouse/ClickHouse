---
description: 'جدول نظامي يعرض جميع خوارزميات التجذير المتاحة.'
keywords: ['جدول نظامي', 'خوارزميات التجذير']
sidebarTitle: 'system.stemmers'
slug: /operations/system-tables/stemmers
title: 'system.stemmers'
doc_type: 'مرجع'
---

{/*AUTOGENERATED_START*/}

## الوصف {#description}

يعرض جميع خوارزميات التجذير المتاحة.
يمكن استخدامها في الدالة [stem](/ar/reference/functions/regular-functions/nlp-functions).

**التوفر**

لا يتوفر `system.stemmers` إلا في إصدارات ClickHouse المُجمَّعة مع الاعتمادية `libstemmer` (`USE_LIBSTEMMER`). وفي الإصدارات التي لا تتضمنها، لا يكون الجدول موجودًا، وتفشل الاستعلامات الموجَّهة إليه بالخطأ `UNKNOWN_TABLE`. يمكنك التحقق مما إذا كانت مفعّلة في إصدارك باستخدام:

```sql
SELECT value FROM system.build_options WHERE name = 'USE_LIBSTEMMER';
```

## الأعمدة {#columns}

* `name` ([String](/ar/reference/data-types/string)) — معرّف خوارزمية التجذير Snowball (اللغة/الخوارزمية)

## مثال {#example}

```sql
SELECT * FROM system.stemmers;
```

```text
 ┌─name───────┐
 │ arabic     │
 │ armenian   │
 │ basque     │
 │ catalan    │
 │ danish     │
 │ dutch      │
 │ english    │
 │ [...]      │
 └────────────┘
```

{/*AUTOGENERATED_END*/}