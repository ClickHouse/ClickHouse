---
description: 'يتيح إجراء استعلامات على البيانات المخزنة في قاعدة بيانات SQLite.'
sidebarTitle: 'sqlite'
slug: /sql-reference/table-functions/sqlite
title: 'sqlite'
doc_type: 'reference'
---

يتيح إجراء استعلامات على البيانات المخزنة في قاعدة بيانات [SQLite](/ar/reference/engines/database-engines/sqlite).

<div id="syntax">
  ## البنية
</div>

```sql
sqlite('db_path', 'table_name')
```

<div id="arguments">
  ## الوسيطات
</div>

* `db_path` — مسار ملف يحتوي على قاعدة بيانات SQLite. [String](/ar/reference/data-types/string).
* `table_name` — اسم جدول في قاعدة بيانات SQLite. [String](/ar/reference/data-types/string).

<div id="returned_value">
  ## القيمة المعادة
</div>

* كائن من نوع جدول يحتوي على الأعمدة نفسها الموجودة في جدول `SQLite` الأصلي.

<div id="example">
  ## مثال
</div>

```sql title="Query"
SELECT * FROM sqlite('sqlite.db', 'table1') ORDER BY col2;
```

```text title="Response"
┌─col1──┬─col2─┐
│ line1 │    1 │
│ line2 │    2 │
│ line3 │    3 │
└───────┴──────┘
```

<div id="related">
  ## ذات صلة
</div>

* محرك جدول [SQLite](/ar/reference/engines/table-engines/integrations/sqlite)
* [محرك قاعدة البيانات SQLite](/ar/reference/engines/database-engines/sqlite) — قسم دعم أنواع البيانات