---
description: 'يحسب عدة كوانتيلات من مُدرَّج تكراري في Prometheus عند مستويات مختلفة في آنٍ واحد.'
slug: /sql-reference/aggregate-functions/reference/quantilesPrometheusHistogram
title: 'quantilesPrometheusHistogram'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="quantilesPrometheusHistogram">
  ## quantilesPrometheusHistogram
</div>

أُضيفت في: v25.10.0

تحسب هذه الدالة عدة [كوانتيل](https://en.wikipedia.org/wiki/Quantile) لمُدرَّج تكراري باستخدام الاستيفاء الخطي عند مستويات مختلفة بالتزامن، مع أخذ القيمة التراكمية والحدود العليا لكل سلة في المُدرَّج التكراري في الاعتبار.

تعادل هذه الدالة [`quantilePrometheusHistogram`](/ar/reference/functions/aggregate-functions/quantilePrometheusHistogram)، لكنها تتيح حساب عدة مستويات quantile في تمريرة واحدة، ما يجعلها أكثر كفاءة من استدعاء دوال quantile الفردية.

**الصيغة**

```sql
quantilesPrometheusHistogram(level1, level2, ...)(bucket_upper_bound, cumulative_bucket_value)
```

**المعلمات**

* `level` — مستويات كوانتيل. رقم ثابت واحد أو أكثر من الأرقام ذات الفاصلة العائمة من 0 إلى 1. نوصي باستخدام قيم `level` ضمن النطاق `[0.01, 0.99]`. [`Float64`](/ar/reference/data-types/float)

**الوسائط**

* `bucket_upper_bound` — الحدود العليا لحاويات المُدرَّج التكراري. يجب أن تكون أعلى حاوية ذات حد علوي `+Inf`. [`Float*`](/ar/reference/data-types/float)
* `cumulative_bucket_value` — القيم التراكمية لحاويات المُدرَّج التكراري. يجب أن تكون القيم متزايدة باطراد كلما زاد الحد العلوي للحاوية. [`UInt*`](/ar/reference/data-types/int-uint) أو [`Float*`](/ar/reference/data-types/float)

**القيمة المعادة**

مصفوفة من كوانتيل للمستويات المحددة، وبالترتيب نفسه الذي حُدِّدت به هذه المستويات. يطابق نوع الفاصلة العائمة في النتيجة نوع `bucket_upper_bound`. [`Array(Float32)`](/ar/reference/data-types/array) أو [`Array(Float64)`](/ar/reference/data-types/array)

**أمثلة**

**مثال على الاستخدام**

```sql title=Query
SELECT quantilesPrometheusHistogram(0.25, 0.5, 0.75)(bucket_upper_bound, cumulative_bucket_value)
FROM VALUES('bucket_upper_bound Float64, cumulative_bucket_value UInt64', (0, 6), (0.5, 11), (1, 14), (inf, 19));
```

```response title=Response
┌─quantilesPrometheusHistogram(0.25, 0.5, 0.75)(bucket_upper_bound, cumulative_bucket_value)─┐
│ [0,0.35,1]                                                                                 │
└────────────────────────────────────────────────────────────────────────────────────────────┘
```

{/*AUTOGENERATED_END*/}