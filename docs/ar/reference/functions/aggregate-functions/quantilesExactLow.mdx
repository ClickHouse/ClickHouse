---
description: 'تحسب بدقة عدة كوانتايلات عند مستويات مختلفة في آنٍ واحد، وتُرجع القيمة الوسطى الأدنى عند وجود عدد زوجي من العناصر.'
slug: /sql-reference/aggregate-functions/reference/quantilesExactLow
title: 'quantilesExactLow'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="quantilesExactLow">
  ## quantilesExactLow
</div>

أُضيفت في: v20.8.0

تحسب هذه الدالة بدقة عدة [كوانتايلات](https://en.wikipedia.org/wiki/Quantile) لتسلسل من البيانات الرقمية عند مستويات مختلفة في الوقت نفسه. وإذا كان عدد العناصر زوجيًا عند المستوى `0.5`، فإنها تُرجع أصغر القيمتين الواقعتين في الوسط.

تعادل هذه الدالة [`quantileExactLow`](/ar/reference/functions/aggregate-functions/quantileExactLow)، لكنها تتيح حساب عدة مستويات كوانتايل في تمريرة واحدة، مما يجعلها أكثر كفاءة من استدعاء كل دالة كوانتايل على حدة.

**الصياغة**

```sql
quantilesExactLow(level1, level2, ...)(expr)
```

**المعلمات**

* `level` — مستويات الكوانتايل. عدد واحد أو أكثر من الأعداد الثابتة ذات الفاصلة العائمة من 0 إلى 1. نوصي باستخدام قيم `level` ضمن النطاق `[0.01, 0.99]`. [`Float*`](/ar/reference/data-types/float)

**الوسائط**

* `expr` — تعبير يُطبَّق على قيم العمود وينتج أنواع بيانات رقمية، أو `Date` أو `DateTime` أو `DateTime64`. [`(U)Int*`](/ar/reference/data-types/int-uint) أو [`Float*`](/ar/reference/data-types/float) أو [`Decimal*`](/ar/reference/data-types/decimal) أو [`Date`](/ar/reference/data-types/date) أو [`DateTime`](/ar/reference/data-types/datetime) أو [`DateTime64`](/ar/reference/data-types/datetime64)

**القيمة المعادة**

مصفوفة من قيم الكوانتايل عند المستويات المحددة، وبالترتيب نفسه الذي حُدِّدت به هذه المستويات. بالنسبة إلى أنواع البيانات الرقمية، يتطابق format الإخراج مع input format. [`Array((U)Int*)`](/ar/reference/data-types/array) أو [`Array(Float*)`](/ar/reference/data-types/array) أو [`Array(Decimal*)`](/ar/reference/data-types/array) أو [`Array(Date)`](/ar/reference/data-types/array) أو [`Array(DateTime)`](/ar/reference/data-types/array) أو [`Array(DateTime64)`](/ar/reference/data-types/array)

**أمثلة**

**حساب عدة قيم كوانتايل منخفضة دقيقة**

```sql title=Query
SELECT quantilesExactLow(0.25, 0.5, 0.75)(number) FROM numbers(10)
```

```response title=Response
┌─quantilesExactLow(0.25, 0.5, 0.75)(number)─┐
│ [2,4,7]                                    │
└────────────────────────────────────────────┘
```

{/*AUTOGENERATED_END*/}