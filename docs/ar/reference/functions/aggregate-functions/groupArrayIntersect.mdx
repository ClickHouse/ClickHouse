---
description: 'إرجاع تقاطع المصفوفات المحددة (إرجاع جميع عناصر المصفوفات
  الموجودة في كل المصفوفات المحددة).'
slug: /sql-reference/aggregate-functions/reference/grouparrayintersect
title: 'groupArrayIntersect'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="groupArrayIntersect">
  ## groupArrayIntersect
</div>

قُدِّمت في: v24.2.0

ترجع تقاطع المصفوفات المحددة (أي جميع عناصر المصفوفات المشتركة بين جميع المصفوفات المحددة).

**الصيغة**

```sql
groupArrayIntersect(x)
```

**الوسيطات**

* `x` — وسيطة (اسم العمود أو تعبير). [`Any`](/ar/reference/data-types/index)

**القيمة المُعادة**

تعيد مصفوفةً تحتوي على العناصر الموجودة في جميع المصفوفات. [`Array`](/ar/reference/data-types/array)

**أمثلة**

**مثال على الاستخدام**

```sql title=Query
-- Create table with Memory engine
CREATE TABLE numbers (
    a Array(Int32)
) ENGINE = Memory;

-- Insert sample data
INSERT INTO numbers VALUES
    ([1,2,4]),
    ([1,5,2,8,-1,0]),
    ([1,5,7,5,8,2]);

SELECT groupArrayIntersect(a) AS intersection FROM numbers;
```

```response title=Response
┌─intersection──────┐
│ [1, 2]            │
└───────────────────┘
```

{/*AUTOGENERATED_END*/}