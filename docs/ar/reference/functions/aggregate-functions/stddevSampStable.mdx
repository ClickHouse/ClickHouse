---
description: 'النتيجة تساوي الجذر التربيعي لـ varSamp. وعلى خلاف ذلك، تستخدم هذه الدالة
  خوارزمية مستقرة عدديًا.'
slug: /sql-reference/aggregate-functions/reference/stddevsampstable
title: 'stddevSampStable'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="stddevSampStable">
  ## stddevSampStable
</div>

أُضيفت في: v1.1.0

النتيجة تساوي الجذر التربيعي لـ [varSamp](/ar/reference/functions/aggregate-functions/varSamp). وعلى خلاف [stddevSamp](/ar/reference/functions/aggregate-functions/stddevSamp)، تستخدم هذه الدالة خوارزمية مستقرة عدديًا. وهي أبطأ، لكنها توفر خطأً حسابيًا أقل.

**البنية**

```sql
stddevSampStable(x)
```

**الوسائط**

* `x` — القيم التي يُراد إيجاد الجذر التربيعي لتباين العينة لها. [`(U)Int*`](/ar/reference/data-types/int-uint) أو [`Float*`](/ar/reference/data-types/float) أو [`Decimal*`](/ar/reference/data-types/decimal)

**القيمة المُعادة**

يعيد الجذر التربيعي لتباين العينة لـ `x`. [`Float64`](/ar/reference/data-types/float)

**أمثلة**

**الاستخدام الأساسي**

```sql title=Query
DROP TABLE IF EXISTS test_data;
CREATE TABLE test_data
(
    population UInt8,
)
ENGINE = Log;

INSERT INTO test_data VALUES (3),(3),(3),(4),(4),(5),(5),(7),(11),(15);

SELECT
    stddevSampStable(population)
FROM test_data;
```

```response title=Response
┌─stddevSampStable(population)─┐
│                            4 │
└──────────────────────────────┘
```

{/*AUTOGENERATED_END*/}