---
description: 'تحسب بدقة عدة كوانتايلات عند مستويات مختلفة في الوقت نفسه، وتُرجع القيمة الوسطى الأعلى عند وجود عدد زوجي من العناصر.'
slug: /sql-reference/aggregate-functions/reference/quantilesExactHigh
title: 'quantilesExactHigh'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

<div id="quantilesExactHigh">
  ## quantilesExactHigh
</div>

أُضيف في: v20.8.0

يحسب بدقة عدة [كوانتايلات](https://en.wikipedia.org/wiki/Quantile) لتسلسل من البيانات الرقمية عند مستويات مختلفة في الوقت نفسه. وإذا كان عدد العناصر زوجيًا عند المستوى `0.5`، فإنه يعيد القيمة الأكبر من القيمتين الواقعتين في الوسط.

تكافئ هذه الدالة [`quantileExactHigh`](/ar/reference/functions/aggregate-functions/quantileExactHigh)، لكنها تتيح حساب عدة مستويات للكوانتايل في مرور واحد، ما يجعلها أكثر كفاءة من استدعاء دوال الكوانتايل الفردية.

**الصيغة**

```sql
quantilesExactHigh(level1, level2, ...)(expr)
```

**المعلمات**

* `level` — مستويات الكوانتايل. رقم ثابت واحد أو أكثر من الأعداد ذات الفاصلة العائمة من 0 إلى 1. نوصي باستخدام قيم `level` ضمن النطاق `[0.01, 0.99]`. [`Float*`](/ar/reference/data-types/float)

**الوسائط**

* `expr` — تعبير على قيم العمود ينتج أنواع بيانات رقمية أو `Date` أو `DateTime` أو `DateTime64`. [`(U)Int*`](/ar/reference/data-types/int-uint) أو [`Float*`](/ar/reference/data-types/float) أو [`Decimal*`](/ar/reference/data-types/decimal) أو [`Date`](/ar/reference/data-types/date) أو [`DateTime`](/ar/reference/data-types/datetime) أو [`DateTime64`](/ar/reference/data-types/datetime64)

**القيمة المعادة**

مصفوفة من الكوانتايلات للمستويات المحددة، وبالترتيب نفسه الذي حُدِّدت به هذه المستويات. بالنسبة إلى أنواع البيانات الرقمية، تتطابق صيغة الإخراج مع صيغة الإدخال. [`Array((U)Int*)`](/ar/reference/data-types/array) أو [`Array(Float*)`](/ar/reference/data-types/array) أو [`Array(Decimal*)`](/ar/reference/data-types/array) أو [`Array(Date)`](/ar/reference/data-types/array) أو [`Array(DateTime)`](/ar/reference/data-types/array) أو [`Array(DateTime64)`](/ar/reference/data-types/array)

**أمثلة**

**حساب عدة كوانتايلات عليا دقيقة**

```sql title=Query
SELECT quantilesExactHigh(0.25, 0.5, 0.75)(number) FROM numbers(10)
```

```response title=Response
┌─quantilesExactHigh(0.25, 0.5, 0.75)(number)─┐
│ [2,5,7]                                     │
└─────────────────────────────────────────────┘
```

{/*AUTOGENERATED_END*/}