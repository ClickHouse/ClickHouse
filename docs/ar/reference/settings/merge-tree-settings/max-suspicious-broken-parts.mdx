---
title: 'إعدادات جدول MergeTree لـ max_suspicious_broken_parts_*'
sidebarTitle: 'max_suspicious_broken_parts_*'
toc_max_heading_level: 2
description: 'إعدادات جدول MergeTree في ClickHouse ضمن المجموعة المُنشأة max_suspicious_broken_parts_*.'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/ar/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

تتوفر هذه الإعدادات في [system.merge&#95;tree&#95;settings](/ar/reference/system-tables/merge_tree_settings)، وهي مُولَّدة تلقائيًا من الشيفرة المصدرية لـ ClickHouse.

<div id="max_suspicious_broken_parts">
  ## max_suspicious_broken_parts
</div>

<SettingsInfoBlock type="UInt64" default_value="100" />

إذا تجاوز عدد الأجزاء التالفة في قسم واحد قيمة
`max_suspicious_broken_parts`، يُمنع الحذف التلقائي.

القيم المحتملة:

* أي عدد صحيح موجب.

<div id="max_suspicious_broken_parts_bytes">
  ## max_suspicious_broken_parts_bytes
</div>

<SettingsInfoBlock type="UInt64" default_value="1073741824" />

الحد الأقصى لإجمالي حجم الأجزاء التالفة؛ وإذا تجاوزه، يُمنع الحذف التلقائي.

القيم الممكنة:

* أي عدد صحيح موجب.

{/*AUTOGENERATED_END*/}