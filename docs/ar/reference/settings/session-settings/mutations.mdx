---
title: 'إعدادات الجلسة mutations_*'
sidebarTitle: 'mutations_*'
toc_max_heading_level: 2
description: 'إعدادات جلسة ClickHouse ضمن المجموعة المُنشأة mutations_*.'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/ar/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

هذه الإعدادات متاحة في [system.settings](/ar/reference/system-tables/settings)، وتُولَّد تلقائيًا من [الشفرة المصدرية](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp).

<div id="mutations_max_literal_size_to_replace">
  ## mutations_max_literal_size_to_replace
</div>

<SettingsInfoBlock type="UInt64" default_value="16384" />

الحد الأقصى لحجم القيمة الحرفية المُسلسلة، بالبايت، التي سيجري استبدالها في استعلامات `UPDATE` و`DELETE`. لا يسري هذا إلا إذا كان واحد على الأقل من الإعدادين المذكورين أعلاه مُمكّنًا. القيمة الافتراضية: 16384 ‏(16 KiB).

<div id="mutations_sync">
  ## mutations_sync
</div>

<SettingsInfoBlock type="UInt64" default_value="0" />

يسمح بتنفيذ استعلامات `ALTER TABLE ... UPDATE|DELETE|MATERIALIZE INDEX|MATERIALIZE PROJECTION|MATERIALIZE COLUMN|MATERIALIZE STATISTICS` ([mutations](/ar/reference/statements/alter/index#mutations)) بشكل متزامن.

القيم الممكنة:

| Value | Description                                                                                                                                                                     |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `0`   | تُنفَّذ عمليات `mutation` بشكل غير متزامن.                                                                                                                                      |
| `1`   | ينتظر الاستعلام حتى تكتمل جميع عمليات `mutation` على الخادم الحالي.                                                                                                             |
| `2`   | ينتظر الاستعلام حتى تكتمل جميع عمليات `mutation` على جميع النسخ المتماثلة (إن وُجدت).                                                                                           |
| `3`   | ينتظر الاستعلام اكتمال العمليات على النسخ المتماثلة النشطة فقط. وهذا مدعوم فقط في `SharedMergeTree`. أما في `ReplicatedMergeTree`، فيكون سلوكه مماثلًا لـ `mutations_sync = 2`. |

{/*AUTOGENERATED_END*/}