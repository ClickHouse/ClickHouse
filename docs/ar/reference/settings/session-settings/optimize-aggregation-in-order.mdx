---
title: 'إعدادات الجلسة optimize_aggregation_in_order_*'
sidebarTitle: 'optimize_aggregation_in_order_*'
toc_max_heading_level: 2
description: 'إعدادات جلسة ClickHouse ضمن المجموعة المُولَّدة optimize_aggregation_in_order_*.'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/ar/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import VersionHistory from "/snippets/ar/components/VersionHistory/VersionHistory.jsx";

هذه الإعدادات متاحة في [system.settings](/ar/reference/system-tables/settings)، وهي مُولَّدة تلقائيًا من [الشفرة المصدرية](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp).

<div id="optimize_aggregation_in_order">
  ## optimize_aggregation_in_order
</div>

<SettingsInfoBlock type="Bool" default_value="0" />

يُمكّن تحسين [GROUP BY](/ar/reference/statements/select/group-by) في استعلامات [SELECT](/ar/reference/statements/select/index) لتجميع البيانات وفق الترتيب المناظر في جداول [MergeTree](/ar/reference/engines/table-engines/mergetree-family/mergetree).

القيم الممكنة:

* 0 — تحسين `GROUP BY` معطّل.
* 1 — تحسين `GROUP BY` مفعّل.

**انظر أيضًا**

* [تحسين GROUP BY](/ar/reference/statements/select/group-by#group-by-optimization-depending-on-table-sorting-key)

<div id="optimize_aggregation_in_order_limit">
  ## optimize_aggregation_in_order_limit
</div>

<SettingsInfoBlock type="Bool" default_value="1" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "26.7"},{"label": "1"},{"label": "إعداد جديد لتمرير `LIMIT` إلى التجميع بالترتيب من أجل الإنهاء المبكر عندما يكون `ORDER BY` بادئة لوصف الفرز الخاص بـ `GROUP BY`."}]}]} />

عند تمكين هذا الإعداد ومع تفعيل التجميع بالترتيب، يتم تمرير LIMIT إلى خطوة التجميع لإتاحة الإنهاء المبكر بعد إنتاج عدد كافٍ من المجموعات. ويقلّل ذلك من كمية البيانات المقروءة عندما يطابق ORDER BY بادئة مفتاح GROUP BY. وقد يؤدي هذا إلى تقليل القيمة المُبلَّغ عنها بواسطة `rows_before_limit_at_least`؛ استخدم `exact_rows_before_limit` إذا كانت هناك حاجة إلى أعداد دقيقة.

{/*AUTOGENERATED_END*/}