---
title: 'إعدادات الجلسة replace_running_query_*'
sidebarTitle: 'replace_running_query_*'
toc_max_heading_level: 2
description: 'إعدادات الجلسة في ClickHouse ضمن المجموعة المُولَّدة replace_running_query_*.'
doc_type: 'مرجع'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/ar/components/SettingsInfoBlock/SettingsInfoBlock.jsx";

هذه الإعدادات متاحة في [system.settings](/ar/reference/system-tables/settings)، وهي مُولَّدة تلقائيًا من [المصدر](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp).

<div id="replace_running_query">
  ## replace_running_query
</div>

<SettingsInfoBlock type="Bool" default_value="0" />

عند استخدام واجهة HTTP، يمكن تمرير المعلَمة &#39;query&#95;id&#39;. ويمكن أن تكون هذه أي سلسلة نصية تُستخدم كمُعرّف للاستعلام.
إذا كان هناك في هذا الوقت استعلام آخر من المستخدم نفسه وبنفس &#39;query&#95;id&#39;، فإن السلوك يعتمد على المعلَمة &#39;replace&#95;running&#95;query&#39;.

`0` (الافتراضي) – طرح استثناء (عدم السماح بتشغيل الاستعلام إذا كان هناك بالفعل استعلام قيد التشغيل بنفس &#39;query&#95;id&#39;).

`1` – إلغاء الاستعلام القديم وبدء تشغيل الاستعلام الجديد.

اضبط هذه المعلَمة على 1 لتنفيذ اقتراحات شروط التقسيم. بعد إدخال الحرف التالي، إذا لم يكن الاستعلام القديم قد اكتمل بعد، فيجب إلغاؤه.

<div id="replace_running_query_max_wait_ms">
  ## replace_running_query_max_wait_ms
</div>

<SettingsInfoBlock type="مللي ثانية" default_value="5000" />

مدة الانتظار حتى ينتهي الاستعلام الجاري الذي يحمل `query_id` نفسه، وذلك عندما يكون إعداد [replace&#95;running&#95;query](/ar/reference/settings/session-settings/replace-running-query#replace_running_query) مفعّلًا.

القيم الممكنة:

* عدد صحيح موجب.
* 0 — طرح استثناء يمنع تشغيل استعلام جديد إذا كان الخادم ينفّذ بالفعل استعلامًا يحمل `query_id` نفسه.

{/*AUTOGENERATED_END*/}