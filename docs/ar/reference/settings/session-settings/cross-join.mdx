---
title: 'إعدادات الجلسة لـ cross_join_*'
sidebarTitle: 'cross_join_*'
toc_max_heading_level: 2
description: 'إعدادات جلسة ClickHouse ضمن المجموعة التي تم إنشاؤها cross_join_*.'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/}

import SettingsInfoBlock from "/snippets/ar/components/SettingsInfoBlock/SettingsInfoBlock.jsx";
import VersionHistory from "/snippets/ar/components/VersionHistory/VersionHistory.jsx";

هذه الإعدادات متاحة في [system.settings](/ar/reference/system-tables/settings)، وهي مُولَّدة تلقائيًا من [الكود المصدري](https://github.com/ClickHouse/ClickHouse/blob/master/src/Core/Settings.cpp).

<div id="cross_join_min_bytes_to_compress">
  ## cross_join_min_bytes_to_compress
</div>

<SettingsInfoBlock type="UInt64" default_value="1073741824" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "24.5"},{"label": "1073741824"},{"label": "الحد الأدنى لحجم كتلة المطلوب ضغطه في CROSS JOIN. تعني القيمة صفر تعطيل هذه العتبة. تُضغط هذه الكتلة عند بلوغ أيٍّ من العتبتين (بحسب عدد الصفوف أو عدد البايتات)."}]}]} />

الحد الأدنى لحجم كتلة المطلوب ضغطه في CROSS JOIN. تعني القيمة صفر تعطيل هذه العتبة. تُضغط هذه الكتلة عند بلوغ أيٍّ من العتبتين (بحسب عدد الصفوف أو عدد البايتات).

<div id="cross_join_min_rows_to_compress">
  ## cross_join_min_rows_to_compress
</div>

<SettingsInfoBlock type="UInt64" default_value="10000000" />

<VersionHistory rows={[{"id": "row-1","items": [{"label": "24.5"},{"label": "10000000"},{"label": "الحد الأدنى لعدد الصفوف المطلوب لضغط الكتلة في CROSS JOIN. وتعني القيمة صفر تعطيل هذه العتبة. وتُضغط هذه الكتلة عند بلوغ أيٍّ من العتبتين (بحسب عدد الصفوف أو البايتات)."}]}]} />

الحد الأدنى لعدد الصفوف المطلوب لضغط الكتلة في CROSS JOIN. وتعني القيمة صفر تعطيل هذه العتبة. وتُضغط هذه الكتلة عند بلوغ أيٍّ من العتبتين (بحسب عدد الصفوف أو البايتات).

{/*AUTOGENERATED_END*/}