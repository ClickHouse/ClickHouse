---
description: 'توثيق ClickHouse Compressor'
slug: /operations/utilities/clickhouse-compressor
title: 'clickhouse-compressor'
doc_type: 'reference'
---

برنامج بسيط لضغط البيانات وفك ضغطها.

<div id="examples">
  ### أمثلة
</div>

قم بضغط البيانات باستخدام LZ4:

```bash
$ ./clickhouse-compressor < input_file > output_file
```

فك ضغط البيانات من صيغة LZ4:

```bash
$ ./clickhouse-compressor --decompress < input_file > output_file
```

اضغط البيانات باستخدام ZSTD بالمستوى 5:

```bash
$ ./clickhouse-compressor --codec 'ZSTD(5)' < input_file > output_file
```

اضغط البيانات باستخدام Delta بأربعة بايتات وZSTD بالمستوى 10.

```bash
$ ./clickhouse-compressor --codec 'Delta(4)' --codec 'ZSTD(10)' < input_file > output_file
```