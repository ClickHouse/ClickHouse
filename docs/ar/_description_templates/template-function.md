---
title: الدالة Template
---



## functionName {#functionname-in-lower-case}

وصف مختصر.

**الصياغة** (بدون SELECT)

```sql
<function syntax>
```

الاسم المستعار: `<alias name>`. (اختياري)

نص إضافي (اختياري).

**الوسائط** (اختياري)

-   `x` — الوصف. اختياري (للوسائط الاختيارية فقط). القيم المحتملة: <values list>. القيمة الافتراضية: <value>. [Type name](relative/path/to/type/dscr.md#type).
-   `y` — الوصف. اختياري (للوسائط الاختيارية فقط). القيم المحتملة: <values list>. القيمة الافتراضية: <value>. [Type name](relative/path/to/type/dscr.md#type).

**المعاملات** (اختياري، للدوال التجميعية ذات المعاملات فقط)

-   `z` — الوصف. اختياري (للمعاملات الاختيارية فقط). القيم المحتملة: <values list>. القيمة الافتراضية: <value>. [Type name](relative/path/to/type/dscr.md#type).

**القيمة (القيم) المُعادة**

-   قائمة القيم المُعادة.

النوع: [Type name](relative/path/to/type/dscr.md#type).

**مثال**

يجب أن يوضح المثال طريقة الاستخدام و/أو حالات الاستخدام. يحتوي النص التالي على الأجزاء الموصى بها في المثال.

جدول الإدخال (اختياري):

```text
```

```sql title="الاستعلام"
```

```text title="الاستجابة"
```

**انظر أيضاً** (اختياري)

-   [رابط](#)