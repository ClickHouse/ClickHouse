--- a full-definition ATTACH states its settings, so they are checked ---
UNKNOWN_SETTING
--- a stored definition naming a non-setting still loads ---
1
7
not_a_setting_at_all
--- an unrelated ALTER on such a table still works ---
merge_with_ttl_timeout = 100
not_a_setting_at_all
--- a stored non-setting is inherited by CREATE TABLE AS, which is fresh input ---
UNKNOWN_SETTING
