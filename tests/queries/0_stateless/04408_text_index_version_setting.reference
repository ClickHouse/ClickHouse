-- default value
with_positions
-- invalid value is rejected
-- initial version: round-trip read
1024
512
0
-- initial version: merge keeps the format readable
1024
512
-- with_codec version: round-trip read
512
512
-- with_positions version: phrase search round-trip
512
512
0
-- with_positions version: merge keeps the format readable
512
512
-- initial version is incompatible with a posting list codec
-- altering into the incompatible combination is also rejected
-- phrase search index requires the with_positions version
-- adding a phrase search index on a table pinned to an older version is also rejected
-- pinning the version on an existing phrase search table keeps writes working
512
