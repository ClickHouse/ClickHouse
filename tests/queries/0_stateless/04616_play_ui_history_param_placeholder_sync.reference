param run: the clean parameterized run carries run=1
param run: the URL carries the binding
removal edit: the removed placeholder leaves tab.params
removal edit: the refreshed entry drops the stale param
removal edit: the diverged editor drops run=1
clean rerun after removal: run=1 is kept
clean rerun after removal: no stale param_x in the URL
clean rerun after removal: the entry params are empty
clear to empty: the baseline URL carries the query
clear to empty: the entry query is cleared immediately
clear to empty: the URL drops the cleared query immediately
clear to empty: no stale param_x in the URL
clear to empty: no run=1 on the cleared draft
clear to empty: tab.params is emptied immediately
clear to empty: the draft stays empty after the rebuild settles
clear to empty: the URL stays clear after the rebuild settles
reorder baseline: the clean run carries run=1
reordered clean run: run=1 is kept
reordered clean run: x keeps its value
reordered clean run: y keeps its value
reordered clean run: the tab still reflects its run
reordered clean run: an editor-only rewrite keeps run=1
immediate reordered run: run=1 is kept
immediate reordered run: the tab reads clean once the rebuild lands
mid-flight removal: tab.params follows the draft
mid-flight removal: the draft entry has no stale param_x
mid-flight removal: the draft entry has no run=1
mid-flight removal: the completed result is kept
completion before rebuild: the raced entry has no run=1
completion before rebuild: no stale param_x even before the rebuild lands
completion before rebuild: tab.params is pruned to the draft text
completion before rebuild: the landing rebuild folds tab.params
completion before rebuild: the landing rebuild repairs the entry
completion before rebuild: the repaired entry still has no run=1
completion before rebuild: the draft is intact
completion before rebuild: the completed result is kept
completion before rebuild (kept placeholder): the binding stays in the URL
completion before rebuild (kept placeholder): the diverged draft still has no run=1
completion before rebuild (kept placeholder): the landing rebuild keeps the binding
completion before rebuild (kept placeholder): the draft is intact
edit during pending restore: the fallback keeps the saved snapshot
edit during pending restore: the saved binding survives the rebuild
edit during pending restore: the rebuilt input carries the saved value
edit during pending restore: the draft is intact
edit during pending restore: the entry never persists a blanked binding
structural leave before rebuild: the capture prunes the removed binding
structural leave before rebuild: no stale param_x in the left tab's entry
structural leave before rebuild: the surviving binding stays in the entry
structural leave before rebuild: the entry carries the new draft
structural leave before rebuild: the diverged draft has no run=1
structural leave before rebuild: the superseded rebuild leaves the pruned map
structural leave before rebuild: the entry stays clean after the rebuild bails
structural leave before rebuild: the target tab is unaffected
leave during pending restore: the capture prunes the removed saved binding
leave during pending restore: no stale param_y in the left tab's entry
leave during pending restore: the surviving saved binding stays in the entry
leave during pending restore: the entry carries the new draft
leave during pending restore: the superseded tails leave the pruned map
leave during pending restore: the entry stays clean after the tails bail
leave during pending restore: the home tab is restored intact
unrebuildable restore: the fallback keeps the saved snapshot
unrebuildable restore: no live input was rebuilt
unrebuildable restore: a structural leave preserves the saved bindings
unrebuildable restore: the saved binding stays in the left tab's entry
unrebuildable restore: the recovered rebuild seeds the input from the snapshot
unrebuildable restore: a live input overrides the merged snapshot
rename baseline: the run URL carries the binding
rename before rebuild: the title is committed
rename before rebuild: the capture prunes the removed binding
rename before rebuild: no stale param_x in the entry
rename before rebuild: the entry carries the new draft
rename before rebuild: the diverged draft has no run=1
rename before rebuild: the landing rebuild leaves the pruned map
rename before rebuild: the entry stays clean after the rebuild lands
rename with a kept placeholder: the binding survives
rename with a kept placeholder: the entry keeps the binding
rename with a kept placeholder: the entry carries the new draft
OK
