-- query_plan_optimize_lazy_materialization_for_object_storage = 1
-- a defaulted column with file-only inputs is deferred on a hive-partitioned table
28	27	two_9	2
25	24	two_8	2
22	21	two_7	2
-- the lazy read step is applied
1
-- the defaulted column and the inputs of its expression are deferred; the hive column is not
Lazily read columns: a, b, s
-- a default over a hive partition column cannot be evaluated on any plan (pre-existing)
UNKNOWN_IDENTIFIER
-- query_plan_optimize_lazy_materialization_for_object_storage = 0
-- a defaulted column with file-only inputs is deferred on a hive-partitioned table
28	27	two_9	2
25	24	two_8	2
22	21	two_7	2
-- the lazy read step is applied
0
-- the defaulted column and the inputs of its expression are deferred; the hive column is not
-- a default over a hive partition column cannot be evaluated on any plan (pre-existing)
UNKNOWN_IDENTIFIER
