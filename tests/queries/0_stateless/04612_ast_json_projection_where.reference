CREATE TABLE t (`x` UInt8, PROJECTION p (SELECT x WHERE x > 0)) ENGINE = MergeTree ORDER BY x
CREATE TABLE t (`x` UInt8, `y` UInt8, PROJECTION p (SELECT x, sum(y) WHERE y != 3 GROUP BY x ORDER BY x)) ENGINE = MergeTree ORDER BY x
ALTER TABLE t (ADD PROJECTION p (SELECT x WHERE x IN (1, 2, 3)))
