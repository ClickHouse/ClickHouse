-- flag off: multipart FROM is a syntax error (master behavior)
1
-- direct children of the namespace, relative names
alpha
beta
-- LIKE applies to the relative name
alpha
-- FULL keeps the engine column
beta	Memory
-- nested namespace
gamma
-- session scope applies when FROM is omitted
alpha
beta
-- the plain database listing is unchanged
ns.alpha
ns.beta
ns.child.gamma
plain
-- a namespace with no tables fails like USE does
1
-- dictionaries have no namespaces
1
