-- Case 17: the recalculation step honours a rows-WHERE predicate
1
-- Case 18: blocked patched merge pulls part_min_ttl into the past
100
1
-- Case 19: blocked patched merge keeps its GROUP BY TTL entry
100
1	1
-- Case 20: the carried GROUP BY entry leaves the part schedulable
100
rolled up after the blocked patched merge: 1
-- Case 21: a patch moving every row into the future moves the bound too
100
1
