DPhyp plan for multi-table hyperedge predicate
Expression ((Project names + (Projection + )))
  Expression (Post Join Actions)
    Join (JOIN FillRightFirst)
      Expression (Left Pre Join Actions)
        Expression (Post Join Actions)
          Join (JOIN FillRightFirst)
            Expression (Left Pre Join Actions)
              Expression (Change column names to column identifiers)
                ReadFromMergeTree (default.he_t3)
            Expression (Right Pre Join Actions)
              Expression (Post Join Actions)
                Join (JOIN FillRightFirst)
                  Expression (Left Pre Join Actions)
                    Expression (Change column names to column identifiers)
                      ReadFromMergeTree (default.he_t2)
                  Expression (Right Pre Join Actions)
                    Expression (Change column names to column identifiers)
                      ReadFromMergeTree (default.he_t1)
      Expression (Right Pre Join Actions)
        Expression (Post Join Actions)
          Join (JOIN FillRightFirst)
            Expression (Left Pre Join Actions)
              Expression (Change column names to column identifiers)
                ReadFromMergeTree (default.he_t5)
            Expression (Right Pre Join Actions)
              Expression (Change column names to column identifiers)
                ReadFromMergeTree (default.he_t4)
13689118392925611357
13689118392925611357
