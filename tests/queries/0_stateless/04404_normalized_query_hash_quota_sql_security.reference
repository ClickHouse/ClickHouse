=== written_bytes of a SQL SECURITY DEFINER materialized view is bucketed per outer insert hash ===
--- pattern A, first insert (7 rows = 56 bytes) is within the per-pattern limit ---
allowed
--- pattern A, second insert (cumulative 160 > 80) exceeds the limit of its own bucket ---
QUOTA_EXCEEDED
--- pattern B (a structurally different insert) has its own bucket and is still allowed ---
allowed
