-- No ambiguity: the alias is not needed
1
1
1
2
1
6
1
-- Ambiguous identifier from an unaliased subquery or table function: the alias is required
-- Two structurally identical table expressions resolve the identifier by the alias, so it is ambiguous as well
0
0
-- Virtual columns of a sibling table participate in the ambiguity
1
2
-- Subcolumns are ambiguous as well
1
-- A subquery wrapped into ARRAY JOIN still needs the alias for its ambiguous column
0	2	1
-- The named side can still be qualified, and USING columns are not ambiguous
100
100
200
200
100
-- A CTE has a name to qualify with, so it is not an unaliased subquery
1	2
-- An expression alias shadows the joined column by the usual rules, this is not an ambiguity
1
-- Matcher: several columns with the same name require the alias
item_id	brand	other
1	100	1
2	200	1
item_id	brand	sub.brand
1	100	100
2	200	100
brand	item_id	item.brand
100	1	100
100	2	200
item_id
1
2
-- Equally named columns of a single table expression do not need a qualification
1	1
1	1	3
-- PASTE JOIN allows equally named columns, but only for the columns it combines itself
a	b	c
1	2	3
-- The restriction can be disabled entirely
100
100
200
200
item_id	brand	brand
1	100	100
2	200	100
-- The old analyzer keeps the strict behavior
