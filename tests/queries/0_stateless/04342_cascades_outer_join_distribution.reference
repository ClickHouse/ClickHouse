-- RIGHT JOIN then join on the null-extended (left) key
Expression ((Project names + Projection))
  MergingAggregated
    GatherExchange
      Aggregating
        Expression (Before GROUP BY)
          JoinLogical (Broadcast HashJoin )
            Expression ((Change column names to column identifiers + (Project names + Projection)))
              JoinLogical (Shuffle HashJoin )
                Expression (Change column names to column identifiers)
                  ShuffleExchange
                    ReadFromMergeTree (ParallelRead default.oj_l)
                Expression (Change column names to column identifiers)
                  ShuffleExchange
                    ReadFromMergeTree (ParallelRead default.oj_r)
            Expression (Change column names to column identifiers)
              ReadFromMergeTree (ReplicatedRead default.oj_s)
-- results match the non-distributed baseline
100000
100000
150000
150000
