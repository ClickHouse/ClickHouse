-- results match local execution
50
50
-- NOT IN
950
-- empty set
0
-- a non-key filter set ships too
50
-- a set outside storage filters is built by the initiator pipeline at query start
50
-- the index retention cap does not limit the shipped set
50
-- the set subquery itself executes as a distributed plan (deduplicated before the gather)
50
-- the transfer limits bound the shipped set
-- break overflow mode also fails closed: a built set cannot be truncated
-- a set of exactly max_rows_to_transfer rows ships
100
100
-- the byte limit measures the shipped values, not the build memory
100
-- LowCardinality IN subquery
500
500
-- a set source that cannot be serialized builds locally on the initiator
200
10
-- two sets in one query
25
-- IN over a tuple of columns
1
-- IN in HAVING (the set is consumed above the aggregation)
0	143
1	143
2	143
-- NULL in the set follows transform_null_in
49
50
-- value-producing IN in a plan that collapses to a single local stage
1
0
1
0
1
0
