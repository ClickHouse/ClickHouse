-- Q01
Expression (Project names)
  Sorting
    Expression ((Before ORDER BY + Projection))
      MergingAggregated
        GatherExchange
          Aggregating
            Expression (Before GROUP BY)
              Expression ((WHERE + Change column names to column identifiers))
                ReadFromMergeTree (ParallelRead default.lineitem)
-- Q02
Expression (Project names)
  Limit (preliminary LIMIT)
    Sorting
      Expression ((Before ORDER BY + Projection))
        Filter (WHERE)
          JoinLogical (Local HashJoin )
            JoinLogical (Local HashJoin )
              JoinLogical (Local HashJoin )
                JoinLogical (Local HashJoin  swapped)
                  Expression (Change column names to column identifiers)
                    ReadFromMergeTree (default.partsupp)
                  JoinLogical (Local HashJoin  swapped)
                    Expression ((WHERE + Change column names to column identifiers))
                      ReadFromMergeTree (default.part)
                    Expression ((Create renaming actions for scalar subquery + (Create correlated subquery result alias + (Project names + Projection))))
                      Aggregating
                        Expression ((Before GROUP BY + ))
                          JoinLogical (Local HashJoin )
                            Expression (((WHERE + Renaming correlated columns to equivalent expressions in subquery) + Change column names to column identifiers))
                              ReadFromMergeTree (default.partsupp)
                            JoinLogical (Local HashJoin )
                              Expression (Change column names to column identifiers)
                                ReadFromMergeTree (default.supplier)
                              JoinLogical (Local HashJoin )
                                Expression (Change column names to column identifiers)
                                  ReadFromMergeTree (default.nation)
                                Expression (((WHERE + Renaming correlated columns to equivalent expressions in subquery) + Change column names to column identifiers))
                                  ReadFromMergeTree (default.region)
                Expression (Change column names to column identifiers)
                  ReadFromMergeTree (default.supplier)
              Expression (Change column names to column identifiers)
                ReadFromMergeTree (default.nation)
            Expression ((WHERE + Change column names to column identifiers))
              ReadFromMergeTree (default.region)
-- Q03
Expression (Project names)
  Limit (preliminary LIMIT)
    Sorting
      Expression ((Before ORDER BY + Projection))
        Aggregating
          Expression ((Before GROUP BY + ))
            JoinLogical (Local HashJoin )
              Expression ((WHERE + Change column names to column identifiers))
                ReadFromMergeTree (default.lineitem)
              JoinLogical (Local HashJoin )
                Expression ((WHERE + Change column names to column identifiers))
                  ReadFromMergeTree (default.orders)
                Expression ((WHERE + Change column names to column identifiers))
                  ReadFromMergeTree (default.customer)
-- Q04
Expression (Project names)
  Sorting
    Expression ((Before ORDER BY + Projection))
      Aggregating
        Expression ((Before GROUP BY + WHERE))
          JoinLogical (Local HashJoin )
            Expression (Create result for always true EXISTS expression)
              Expression ((WHERE + (Renaming correlated columns to equivalent expressions in subquery + Change column names to column identifiers)))
                ReadFromMergeTree (default.lineitem)
            Expression ((WHERE + Change column names to column identifiers))
              ReadFromMergeTree (default.orders)
-- Q05
Expression (Project names)
  Sorting
    Expression ((Before ORDER BY + Projection))
      MergingAggregated
        GatherExchange
          Aggregating
            Expression ((Before GROUP BY + ))
              JoinLogical (Broadcast HashJoin )
                JoinLogical (Broadcast HashJoin )
                  Expression (Change column names to column identifiers)
                    ReadFromMergeTree (ParallelRead default.lineitem)
                  BroadcastExchange
                    JoinLogical (Local HashJoin )
                      Expression (Change column names to column identifiers)
                        ReadFromMergeTree (default.supplier)
                      JoinLogical (Local HashJoin )
                        Expression (Change column names to column identifiers)
                          ReadFromMergeTree (default.nation)
                        Expression ((WHERE + Change column names to column identifiers))
                          ReadFromMergeTree (default.region)
                BroadcastExchange
                  JoinLogical (Local HashJoin )
                    Expression ((WHERE + Change column names to column identifiers))
                      ReadFromMergeTree (default.orders)
                    Expression (Change column names to column identifiers)
                      ReadFromMergeTree (default.customer)
-- Q06
Expression ((Project names + Projection))
  MergingAggregated
    GatherExchange
      Aggregating
        Expression (Before GROUP BY)
          Expression ((WHERE + Change column names to column identifiers))
            ReadFromMergeTree (ParallelRead default.lineitem)
-- Q07
Expression (Project names)
  Sorting
    Expression ((Before ORDER BY + Projection))
      Aggregating
        Expression (Before GROUP BY)
          Filter (WHERE)
            JoinLogical (Local HashJoin )
              JoinLogical (Local HashJoin )
                Expression ((WHERE + Change column names to column identifiers))
                  ReadFromMergeTree (default.lineitem)
                JoinLogical (Local HashJoin )
                  Expression (Change column names to column identifiers)
                    ReadFromMergeTree (default.orders)
                  JoinLogical (Local HashJoin )
                    Expression (Change column names to column identifiers)
                      ReadFromMergeTree (default.customer)
                    Expression (( + Change column names to column identifiers))
                      ReadFromMergeTree (default.nation)
              JoinLogical (Local HashJoin )
                Expression (Change column names to column identifiers)
                  ReadFromMergeTree (default.supplier)
                Expression ((( + ) + Change column names to column identifiers))
                  ReadFromMergeTree (default.nation)
-- Q08
Expression ((Project names + (Before ORDER BY + Projection) [lifted up part]))
  Sorting
    Expression ((Before ORDER BY + Projection))
      Aggregating
        Expression ((Before GROUP BY + (Change column names to column identifiers + (Project names + (Projection + )))))
          JoinLogical (Local HashJoin )
            JoinLogical (Local HashJoin )
              JoinLogical (Local HashJoin )
                Expression (Change column names to column identifiers)
                  ReadFromMergeTree (default.lineitem)
                JoinLogical (Local HashJoin )
                  Expression ((WHERE + Change column names to column identifiers))
                    ReadFromMergeTree (default.orders)
                  JoinLogical (Local HashJoin )
                    Expression (Change column names to column identifiers)
                      ReadFromMergeTree (default.customer)
                    JoinLogical (Local HashJoin )
                      Expression (Change column names to column identifiers)
                        ReadFromMergeTree (default.nation)
                      Expression ((WHERE + Change column names to column identifiers))
                        ReadFromMergeTree (default.region)
              JoinLogical (Local HashJoin )
                Expression (Change column names to column identifiers)
                  ReadFromMergeTree (default.supplier)
                Expression (Change column names to column identifiers)
                  ReadFromMergeTree (default.nation)
            Expression ((WHERE + Change column names to column identifiers))
              ReadFromMergeTree (default.part)
-- Q09
Expression (Project names)
  Sorting
    Expression ((Before ORDER BY + Projection))
      Aggregating
        Expression ((Before GROUP BY + ))
          JoinLogical (Local HashJoin )
            JoinLogical (Local HashJoin )
              JoinLogical (Local HashJoin )
                JoinLogical (Local HashJoin )
                  Expression (Change column names to column identifiers)
                    ReadFromMergeTree (default.lineitem)
                  JoinLogical (Local HashJoin )
                    Expression (Change column names to column identifiers)
                      ReadFromMergeTree (default.supplier)
                    Expression (Change column names to column identifiers)
                      ReadFromMergeTree (default.nation)
                Expression ((WHERE + Change column names to column identifiers))
                  ReadFromMergeTree (default.part)
              Expression (Change column names to column identifiers)
                ReadFromMergeTree (default.orders)
            Expression (Change column names to column identifiers)
              ReadFromMergeTree (default.partsupp)
-- Q10
Expression (Project names)
  Limit (preliminary LIMIT)
    Sorting
      Expression ((Before ORDER BY + Projection))
        Aggregating
          Expression ((Before GROUP BY + ))
            JoinLogical (Local HashJoin  swapped)
              JoinLogical (Local HashJoin )
                Expression ((WHERE + Change column names to column identifiers))
                  ReadFromMergeTree (default.orders)
                JoinLogical (Local HashJoin )
                  Expression (Change column names to column identifiers)
                    ReadFromMergeTree (default.customer)
                  Expression (Change column names to column identifiers)
                    ReadFromMergeTree (default.nation)
              Expression ((WHERE + Change column names to column identifiers))
                ReadFromMergeTree (default.lineitem)
-- Q11
Expression (Project names)
  Sorting (Sorting for ORDER BY)
    Expression ((Before ORDER BY + Projection))
      Filter (HAVING)
        Aggregating
          Expression ((Before GROUP BY + ))
            Expression (Post Join Actions)
              Join (JOIN FillRightFirst)
                Expression (Left Pre Join Actions)
                  Expression (Change column names to column identifiers)
                    ReadFromMergeTree (default.partsupp)
                Expression (Right Pre Join Actions)
                  Expression (Post Join Actions)
                    Join (JOIN FillRightFirst)
                      Expression (Left Pre Join Actions)
                        Expression (Change column names to column identifiers)
                          ReadFromMergeTree (default.supplier)
                      Expression (Right Pre Join Actions)
                        Expression ((WHERE + Change column names to column identifiers))
                          ReadFromMergeTree (default.nation)
-- Q12
Expression (Project names)
  Sorting
    Expression ((Before ORDER BY + Projection))
      MergingAggregated
        GatherExchange
          Aggregating
            Expression ((Before GROUP BY + ))
              JoinLogical (Broadcast HashJoin )
                Expression ((WHERE + Change column names to column identifiers))
                  ReadFromMergeTree (ParallelRead default.lineitem)
                Expression (Change column names to column identifiers)
                  BroadcastExchange
                    ReadFromMergeTree (default.orders)
-- Q13
Expression (Project names)
  Sorting
    Expression ((Before ORDER BY + Projection))
      Aggregating
        Expression ((Before GROUP BY + (Change column names to column identifiers + (Project names + Projection))))
          Aggregating
            Expression (Before GROUP BY)
              JoinLogical (Local HashJoin )
                Expression ((Join filter + Change column names to column identifiers))
                  ReadFromMergeTree (default.orders)
                Expression (Change column names to column identifiers)
                  ReadFromMergeTree (default.customer)
-- Q14
Expression ((Project names + Projection))
  MergingAggregated
    GatherExchange
      Aggregating
        Expression ((Before GROUP BY + ))
          JoinLogical (Broadcast HashJoin )
            Expression ((WHERE + Change column names to column identifiers))
              ReadFromMergeTree (ParallelRead default.lineitem)
            Expression (Change column names to column identifiers)
              BroadcastExchange
                ReadFromMergeTree (default.part)
-- Q15
Expression (Project names)
  Sorting (Sorting for ORDER BY)
    Expression ((Before ORDER BY + (Projection + )))
      Expression (Post Join Actions)
        Join (JOIN FillRightFirst)
          Expression (Left Pre Join Actions)
            Expression (Change column names to column identifiers)
              ReadFromMergeTree (default.supplier)
          Expression (Right Pre Join Actions)
            Expression ((WHERE + (Change column names to column identifiers + (Convert VIEW subquery result to VIEW table structure + (Materialize constants after VIEW subquery + (Project names + (Projection + (Change column names to column identifiers + (Project names + Projection)))))))))
              Filter (((WHERE + (Change column names to column identifiers + (Convert VIEW subquery result to VIEW table structure + (Materialize constants after VIEW subquery + (Project names + (Projection + (Change column names to column identifiers + (Project names + Projection)))))))))[split])
                Aggregating
                  Expression (Before GROUP BY)
                    Expression ((WHERE + Change column names to column identifiers))
                      ReadFromMergeTree (default.lineitem)
-- Q16
Expression (Project names)
  Sorting
    Expression ((Before ORDER BY + Projection))
      MergingAggregated
        GatherExchange
          Aggregating
            Expression ((Before GROUP BY + WHERE))
              JoinLogical (Broadcast HashJoin )
                JoinLogical (Broadcast HashJoin  swapped)
                  Expression ((WHERE + Change column names to column identifiers))
                    ReadFromMergeTree (ParallelRead default.part)
                  Expression (Change column names to column identifiers)
                    BroadcastExchange
                      ReadFromMergeTree (default.partsupp)
                Expression (Create result for always true EXISTS expression)
                  Expression (((WHERE + (Renaming correlated columns to equivalent expressions in subquery + (Change column names to column identifiers + (Project names + (Projection + (Change column names to column identifiers + (Project names + Projection))))))) + (WHERE + Change column names to column identifiers)))
                    BroadcastExchange
                      ReadFromMergeTree (default.supplier)
-- Q17
Expression ((Project names + Projection))
  MergingAggregated
    GatherExchange
      Aggregating
        Expression (Before GROUP BY)
          Filter (WHERE)
            JoinLogical (Broadcast HashJoin )
              Expression (Change column names to column identifiers)
                ReadFromMergeTree (ParallelRead default.lineitem)
              BroadcastExchange
                JoinLogical (Local HashJoin )
                  Expression ((Create renaming actions for scalar subquery + (Create correlated subquery result alias + (Project names + Projection))))
                    MergingAggregated
                      GatherExchange
                        Aggregating
                          Expression (Before GROUP BY)
                            Expression ((WHERE + (Renaming correlated columns to equivalent expressions in subquery + Change column names to column identifiers)))
                              ReadFromMergeTree (ParallelRead default.lineitem)
                  Expression ((WHERE + Change column names to column identifiers))
                    ReadFromMergeTree (default.part)
-- Q18
Expression (Project names)
  Limit (preliminary LIMIT)
    Sorting
      Expression ((Before ORDER BY + Projection))
        Aggregating
          Expression ((Before GROUP BY + WHERE))
            JoinLogical (Local HashJoin )
              JoinLogical (Local HashJoin )
                Expression (Change column names to column identifiers)
                  ReadFromMergeTree (default.lineitem)
                JoinLogical (Local HashJoin )
                  Expression (Change column names to column identifiers)
                    ReadFromMergeTree (default.orders)
                  Expression (Change column names to column identifiers)
                    ReadFromMergeTree (default.customer)
              Expression (Create result for always true EXISTS expression)
                Filter (((WHERE + (Renaming correlated columns to equivalent expressions in subquery + (Change column names to column identifiers + (Project names + (Projection + (Change column names to column identifiers + (Project names + Projection))))))) + HAVING))
                  Aggregating
                    Expression ((((WHERE + (Renaming correlated columns to equivalent expressions in subquery + (Change column names to column identifiers + (Project names + (Projection + (Change column names to column identifiers + (Project names + Projection))))))) + HAVING) + (Before GROUP BY + Change column names to column identifiers)))
                      ReadFromMergeTree (default.lineitem)
-- Q19
Expression ((Project names + Projection))
  MergingAggregated
    GatherExchange
      Aggregating
        Expression (Before GROUP BY)
          Filter (WHERE)
            JoinLogical (Broadcast HashJoin )
              Expression (( + (WHERE + Change column names to column identifiers)))
                ReadFromMergeTree (ParallelRead default.lineitem)
              Expression (( + (WHERE + Change column names to column identifiers)))
                BroadcastExchange
                  ReadFromMergeTree (default.part)
-- Q20
Expression (Project names)
  Sorting
    GatherExchange
      Expression ((Before ORDER BY + (Projection + WHERE)))
        JoinLogical (Broadcast HashJoin  swapped)
          JoinLogical (Broadcast HashJoin )
            Expression (Change column names to column identifiers)
              ReadFromMergeTree (ParallelRead default.supplier)
            Expression ((WHERE + Change column names to column identifiers))
              BroadcastExchange
                ReadFromMergeTree (default.nation)
          Expression (Create result for always true EXISTS expression)
            Filter (((WHERE + (Renaming correlated columns to equivalent expressions in subquery + (Change column names to column identifiers + (Project names + (Projection + (Change column names to column identifiers + (Project names + Projection))))))) + WHERE))
              BroadcastExchange
                JoinLogical (Local HashJoin  swapped)
                  Expression
                    JoinLogical (Local HashJoin )
                      Expression (((((WHERE + (Renaming correlated columns to equivalent expressions in subquery + (Change column names to column identifiers + (Project names + (Projection + (Change column names to column identifiers + (Project names + Projection))))))) + WHERE) + WHERE) + Change column names to column identifiers))
                        ReadFromMergeTree (default.partsupp)
                      Expression (Create result for always true EXISTS expression)
                        Expression (((WHERE + (Renaming correlated columns to equivalent expressions in subquery + (Change column names to column identifiers + (Project names + (Projection + (Change column names to column identifiers + (Project names + Projection))))))) + (WHERE + Change column names to column identifiers)))
                          ReadFromMergeTree (default.part)
                  Expression ((((WHERE + (Renaming correlated columns to equivalent expressions in subquery + (Change column names to column identifiers + (Project names + (Projection + (Change column names to column identifiers + (Project names + Projection))))))) + WHERE) + (Create renaming actions for scalar subquery + (Create correlated subquery result alias + (Project names + Projection)))))
                    MergingAggregated
                      GatherExchange
                        Aggregating
                          Expression ((((((WHERE + (Renaming correlated columns to equivalent expressions in subquery + (Change column names to column identifiers + (Project names + (Projection + (Change column names to column identifiers + (Project names + Projection))))))) + WHERE) + (Create renaming actions for scalar subquery + (Create correlated subquery result alias + (Project names + Projection)))) + Before GROUP BY) + (WHERE + (Renaming correlated columns to equivalent expressions in subquery + Change column names to column )
                            ReadFromMergeTree (ParallelRead default.lineitem)
-- Q21
Expression (Project names)
  Limit (preliminary LIMIT)
    Sorting
      Expression ((Before ORDER BY + Projection))
        MergingAggregated
          GatherExchange
            Aggregating
              Expression ((Before GROUP BY + WHERE))
                JoinLogical (Broadcast HashJoin )
                  Expression (Create result for always true EXISTS expression)
                    Filter (WHERE)
                      JoinLogical (Broadcast HashJoin )
                        Expression ((WHERE + Change column names to column identifiers))
                          ReadFromMergeTree (ParallelRead default.lineitem)
                        Expression (Project only used columns)
                          Expression (WHERE)
                            BroadcastExchange
                              JoinLogical (Local HashJoin )
                                Expression (Create result for always true EXISTS expression)
                                  Filter (WHERE)
                                    JoinLogical (Local HashJoin  swapped)
                                      Expression (Project only used columns)
                                        Expression
                                          JoinLogical (Local HashJoin )
                                            JoinLogical (Local HashJoin )
                                              Expression ((WHERE + Change column names to column identifiers))
                                                ReadFromMergeTree (default.lineitem)
                                              JoinLogical (Local HashJoin )
                                                Expression (Change column names to column identifiers)
                                                  ReadFromMergeTree (default.supplier)
                                                Expression ((WHERE + Change column names to column identifiers))
                                                  ReadFromMergeTree (default.nation)
                                            Expression ((WHERE + Change column names to column identifiers))
                                              ReadFromMergeTree (default.orders)
                                      Expression (Change column names to column identifiers)
                                        ReadFromMergeTree (default.lineitem)
                                Expression
                                  JoinLogical (Local HashJoin )
                                    JoinLogical (Local HashJoin )
                                      Expression ((WHERE + Change column names to column identifiers))
                                        ReadFromMergeTree (default.lineitem)
                                      JoinLogical (Local HashJoin )
                                        Expression (Change column names to column identifiers)
                                          ReadFromMergeTree (default.supplier)
                                        Expression ((WHERE + Change column names to column identifiers))
                                          ReadFromMergeTree (default.nation)
                                    Expression ((WHERE + Change column names to column identifiers))
                                      ReadFromMergeTree (default.orders)
                  Expression (WHERE)
                    Expression (WHERE)
                      BroadcastExchange
                        JoinLogical (Local HashJoin )
                          Expression (Create result for always true EXISTS expression)
                            Filter (WHERE)
                              JoinLogical (Local HashJoin  swapped)
                                Expression (Project only used columns)
                                  Expression
                                    JoinLogical (Local HashJoin )
                                      JoinLogical (Local HashJoin )
                                        Expression ((WHERE + Change column names to column identifiers))
                                          ReadFromMergeTree (default.lineitem)
                                        JoinLogical (Local HashJoin )
                                          Expression (Change column names to column identifiers)
                                            ReadFromMergeTree (default.supplier)
                                          Expression ((WHERE + Change column names to column identifiers))
                                            ReadFromMergeTree (default.nation)
                                      Expression ((WHERE + Change column names to column identifiers))
                                        ReadFromMergeTree (default.orders)
                                Expression (Change column names to column identifiers)
                                  ReadFromMergeTree (default.lineitem)
                          Expression
                            JoinLogical (Local HashJoin )
                              JoinLogical (Local HashJoin )
                                Expression ((WHERE + Change column names to column identifiers))
                                  ReadFromMergeTree (default.lineitem)
                                JoinLogical (Local HashJoin )
                                  Expression (Change column names to column identifiers)
                                    ReadFromMergeTree (default.supplier)
                                  Expression ((WHERE + Change column names to column identifiers))
                                    ReadFromMergeTree (default.nation)
                              Expression ((WHERE + Change column names to column identifiers))
                                ReadFromMergeTree (default.orders)
-- Q22
Expression (Project names)
  Sorting (Sorting for ORDER BY)
    Expression ((Before ORDER BY + Projection))
      Aggregating
        Expression ((Before GROUP BY + (Change column names to column identifiers + (Project names + (Projection + WHERE)))))
          Expression (Post Join Actions)
            Join (JOIN FillRightFirst)
              Expression (Left Pre Join Actions)
                Expression (Create result for always true EXISTS expression)
                  Filter ((WHERE + (Renaming correlated columns to equivalent expressions in subquery + Change column names to column identifiers)))
                    ReadFromMergeTree (default.orders)
              Expression (Right Pre Join Actions)
                Expression ((WHERE + Change column names to column identifiers))
                  ReadFromMergeTree (default.customer)
