-- cap reduces streams for a narrow column --
1
-- virtual-only reads use the injected physical column for the cap --
1
-- cap never produces zero streams --
1
-- a narrow pipeline is never capped --
1
-- results are identical with the cap on and off --
1
-- selective read gets fewer streams than a full scan --
1
-- compact parts: cap still applies --
1
-- compact parts: results are identical with the cap on and off --
1
-- highly compressible column is sized by uncompressed bytes --
1
-- subcolumns sharing a physical column are charged once when exact sizes are disabled --
1
-- exact sizing never charges one subcolumn more than the physical column --
1
-- many parts: cap applies and results are identical --
1
1
-- unknown DEFAULT dependencies do not cap streams --
1
-- partial variable-width reads do not cap streams --
1
