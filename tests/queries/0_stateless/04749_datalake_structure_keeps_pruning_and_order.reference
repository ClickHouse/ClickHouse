-- iceberg: a retyped filter column does not prune manifest entries by the metadata type
['0','1','10','11','12','13','2','3']
[0,1,2,3]
-- iceberg: a retyped filter column does not prune row groups by the file type on an evolved file
['10','11','12','13']
-- iceberg: a structure-bearing read does not keep the metadata sorting key
0
1
-- iceberg: a declared subset that omits a key column does not keep the metadata sorting key
0
1
-- iceberg: a nested sorting key is recognized as present when the declaration keeps its parent
0
0
1
1
-- iceberg: a declaration differing only in nullability still prunes manifest entries
1
-- iceberg: a declaration differing only in low cardinality still prunes manifest entries
1
-- iceberg: an undeclared read of a promoted column still prunes row groups
1
-- iceberg: a declaration differing only in nullability still prunes row groups
1
-- iceberg: a declaration differing only in low cardinality still prunes row groups
1
-- paimon: a declaration differing only in nullability still prunes partitions
1
-- paimon: a declaration differing only in low cardinality still prunes partitions
1
-- paimon: retyping a column outside the partition keys still prunes partitions
1
-- paimon: a retyped partition key is not pruned against the metadata-typed partition key
['0','1000']
['0','1000']
['0','1000']
