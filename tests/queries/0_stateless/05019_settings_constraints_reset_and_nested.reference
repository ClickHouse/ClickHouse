-- constraint is in force on a direct SET
-- SET name = DEFAULT must not clear a CONST constraint
10
-- resetting a setting that already holds its default stays allowed
0
-- a custom setting pinned CONST must not be erasable
1
-- a nested SETTINGS clause is checked, in a subquery and in a CTE
-- a nested SETTINGS name = DEFAULT is checked too
-- a nested SETTINGS clause is checked on every member of a compound query
-- an unconstrained nested SETTINGS clause still works
3
-- a SQL SECURITY DEFINER view with inner SETTINGS still reads
3
-- a SQL SECURITY INVOKER view with inner SETTINGS is checked
-- a top-level SETTINGS clause overriding a CONST setting is checked
