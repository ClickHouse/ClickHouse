-- echo_query_id from config prints the query id in batch clickhouse-local
Query id: QUERY_ID

1
-- the CLI flag still wins over the config
1
-- echo_formatted from config formats the echoed query

SELECT 1 AS x
FORMAT `Null`

-- enable_progress_table_toggle from config is accepted
1
