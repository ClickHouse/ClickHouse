plan: filter reading the argument — pushdown applied
1
plan: filter — no pushdown when disabled
0
plan: filter not reading the argument — pushdown applied
1
plan: argument still needed above — not pushed
0
plan: nested expression (lengthUTF8(s)+1) — pushdown applied
1
plan: filter step as parent — pushdown applied
1
plan: filter step as parent — no pushdown when disabled
0
plan: group by — barrier respected
0
plan: distinct — barrier respected
0
plan: wide column removed from the sort
1
plan: wide column removed from the filter
1
plan: wide column removed from the filter that reads it
1
plan: default lift-up not regressed when push-down disabled
1
plan: mixed roots keep only volume-reducing functions below sort
1
plan: unsupported UUID/IP roots still lift up
1
eq: filter
eq: unsupported UUID/IP roots
eq: sort+limit
eq: nested expression
eq: filter step as parent
eq: group by
0	1
5	1
12	1
eq: join
0	1
5	2
12	3
eq: distinct subquery
eq: distinct union subquery
eq: alias collides with sort key
eq: stateful function in the same expression
