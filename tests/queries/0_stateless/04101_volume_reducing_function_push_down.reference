plan: filter reading the argument — cheap function pushed
1
plan: filter reading the argument — lengthUTF8 not pushed
1
plan: filter — no pushdown when disabled
0
plan: filter not reading the argument — not pushed
0
plan: merged filter not reading the argument — not pushed
0
plan: merged filter with byte-scanning function — not pushed
0
plan: scalar computed by filter — not pushed
1
plan: argument still needed above — not pushed
0
plan: sibling alias still needed above — not pushed
0
plan: sibling alias below outer sort — not pushed
0
plan: sibling alias below outer sort and limit — not pushed
0
plan: small FixedString lengthUTF8 — not pushed
0
plan: wide FixedString length — pushed
1
plan: wide FixedString lengthUTF8 — pushed
1
plan: wide FixedString lengthUTF8 — no pushdown when disabled
0
result: wide FixedString lengthUTF8
64
64
58
plan: array length — not pushed
0
plan: nested expression (lengthUTF8(s)+1) below filter — not pushed
0
plan: filter step as parent — pushdown applied
1
plan: filter step as parent — no pushdown when disabled
0
plan: group by — barrier respected
0
plan: distinct — barrier respected
0
plan: wide column removed from the sort
1
plan: wide column removed from the filter that reads it
1
plan: unused sibling alias in filter — pushed
1
plan: default lift-up not regressed when push-down disabled
1
plan: mixed roots keep only volume-reducing functions below sort
1
plan: do not push when the lifted expression needs the argument
0
plan: mixed filter functions on one argument — not pushed
1
eq: filter
eq: unsupported UUID/IP roots
eq: sort+limit
eq: nested expression
eq: filter step as parent
eq: group by
0	1
5	1
12	1
eq: join
0	1
5	2
12	3
eq: distinct subquery
eq: distinct union subquery
eq: alias collides with sort key
eq: stateful function in the same expression
converges: filter condition reading the argument
0
1
converges: argument computed below the sorting
1
6
13
