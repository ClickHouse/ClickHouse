-- a source predicate mixed with a subquery is rejected
1
-- a disjunction mixing the source and the joined local side is rejected
1
-- a negated conjunction mixing the source and the joined local side is rejected
3
-- a plain source predicate is still rejected
-- a predicate on the joined local side alone is not a filter on the source
1
2
-- no outer filter is allowed
4
