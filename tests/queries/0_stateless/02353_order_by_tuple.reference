SELECT number AS a, number % 2 AS b FROM numbers(10) ORDER BY number AS a DESC NULLS FIRST WITH FILL FROM 2 TO 1 STEP -1, number % 2 AS b DESC NULLS FIRST WITH FILL FROM 2 TO 1 STEP -1 SETTINGS enable_analyzer = 0
SELECT number AS a, number % 2 AS b FROM numbers(10) ORDER BY number AS a DESC NULLS FIRST WITH FILL FROM 2 TO 1 STEP -1, number % 2 AS b DESC NULLS FIRST WITH FILL FROM 2 TO 1 STEP -1 SETTINGS enable_analyzer = 0
OK
