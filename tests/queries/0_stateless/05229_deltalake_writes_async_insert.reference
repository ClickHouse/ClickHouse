-- async inserts, each waited for: acknowledged rows are committed and readable
3	6
versions: 3
-- async insert with the writes setting off is rejected and commits nothing
SUPPORT_IS_DISABLED
3
versions: 3
-- async insert with the writes setting off and no wait: nothing is committed
-- (the INSERT itself is still acknowledged, https://github.com/ClickHouse/ClickHouse/issues/120717)
3
versions: 3
flush errors with SUPPORT_IS_DISABLED for the no-wait insert:	1
-- two concurrent async inserts with identical settings both land (one or two flushes)
5	27
versions in [4, 5]: 1
-- a queued insert (no wait) is flushed later with the inserting session's setting, even when
-- the session that triggers the flush has writes off
6	47
one more version after the explicit flush: 1
