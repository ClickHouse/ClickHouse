-- ?limit= on INSERT ... SELECT is rejected
rejected
-- ?offset= on INSERT ... SELECT is rejected
rejected
-- ?filter= (combined-filter channel) on INSERT ... SELECT is rejected
rejected
-- nothing was inserted by any rejected statement
0
-- without construction parameters, INSERT ... SELECT inserts all rows
10
