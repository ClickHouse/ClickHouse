-- a legacy recent table degrades the pair; the samples table gets explicit zeros, not its DEFAULT 1
2	0
2
-- mirrored: a legacy samples table with a flag-carrying recent table, explicit zeros again
1
1	0
-- pre-existing rows in a mixed pair read the same through both paths
2	0
2	0
-- a range older than the recent TTL window reads only samples and honors its real flags
2	1
2	1
-- a range crossing the recent TTL boundary degrades like one wholly inside it
2	0
2	0
-- a marker older than the recent window keeps its flag while a newer one in the window degrades
2	1
1	0
