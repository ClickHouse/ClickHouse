SELECT\n    sum(plus(n, 1)),\n    sum(plus(1, n)),\n    sum(minus(n, 1)),\n    sum(minus(1, n))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    sum(multiply(n, 2)),\n    sum(multiply(2, n)),\n    sum(divide(n, 2)),\n    sum(divide(1, n))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    min(plus(n, 1)),\n    min(plus(1, n)),\n    min(minus(n, 1)),\n    min(minus(1, n))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    min(multiply(n, 2)),\n    min(multiply(2, n)),\n    min(divide(n, 2)),\n    min(divide(1, n))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    max(plus(n, 1)),\n    max(plus(1, n)),\n    max(minus(n, 1)),\n    max(minus(1, n))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    max(multiply(n, 2)),\n    max(multiply(2, n)),\n    max(divide(n, 2)),\n    max(divide(1, n))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    sum(plus(n, -1)),\n    sum(plus(-1, n)),\n    sum(minus(n, -1)),\n    sum(minus(-1, n))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    sum(multiply(n, -2)),\n    sum(multiply(-2, n)),\n    sum(divide(n, -2)),\n    sum(divide(-1, n))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    min(plus(n, -1)),\n    min(plus(-1, n)),\n    min(minus(n, -1)),\n    min(minus(-1, n))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    min(multiply(n, -2)),\n    min(multiply(-2, n)),\n    min(divide(n, -2)),\n    min(divide(-1, n))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    max(plus(n, -1)),\n    max(plus(-1, n)),\n    max(minus(n, -1)),\n    max(minus(-1, n))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    max(multiply(n, -2)),\n    max(multiply(-2, n)),\n    max(divide(n, -2)),\n    max(divide(-1, n))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    sum(plus(abs(2), 1)),\n    sum(plus(abs(2), n)),\n    sum(minus(n, abs(2))),\n    sum(minus(1, abs(2)))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    sum(multiply(abs(2), 2)),\n    sum(multiply(abs(2), n)),\n    sum(divide(n, abs(2))),\n    sum(divide(1, abs(2)))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    min(plus(abs(2), 1)),\n    min(plus(abs(2), n)),\n    min(minus(n, abs(2))),\n    min(minus(1, abs(2)))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    min(multiply(abs(2), 2)),\n    min(multiply(abs(2), n)),\n    min(divide(n, abs(2))),\n    min(divide(1, abs(2)))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    max(plus(abs(2), 1)),\n    max(plus(abs(2), n)),\n    max(minus(n, abs(2))),\n    max(minus(1, abs(2)))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    max(multiply(abs(2), 2)),\n    max(multiply(abs(2), n)),\n    max(divide(n, abs(2))),\n    max(divide(1, abs(2)))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    sum(plus(abs(n), 1)),\n    sum(plus(abs(n), n)),\n    sum(minus(n, abs(n))),\n    sum(minus(1, abs(n)))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    sum(multiply(abs(n), 2)),\n    sum(multiply(abs(n), n)),\n    sum(divide(n, abs(n))),\n    sum(divide(1, abs(n)))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    min(plus(abs(n), 1)),\n    min(plus(abs(n), n)),\n    min(minus(n, abs(n))),\n    min(minus(1, abs(n)))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    min(multiply(abs(n), 2)),\n    min(multiply(abs(n), n)),\n    min(divide(n, abs(n))),\n    min(divide(1, abs(n)))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    max(plus(abs(n), 1)),\n    max(plus(abs(n), n)),\n    max(minus(n, abs(n))),\n    max(minus(1, abs(n)))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    max(multiply(abs(n), 2)),\n    max(multiply(abs(n), n)),\n    max(divide(n, abs(n))),\n    max(divide(1, abs(n)))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    sum(plus(multiply(n, n), 1)),\n    sum(plus(1, multiply(n, n))),\n    sum(minus(multiply(n, n), 1)),\n    sum(minus(1, multiply(n, n)))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    sum(multiply(multiply(n, n), 2)),\n    sum(multiply(multiply(2, n), n)),\n    sum(divide(multiply(n, n), 2)),\n    sum(multiply(divide(1, n), n))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    min(plus(multiply(n, n), 1)),\n    min(plus(1, multiply(n, n))),\n    min(minus(multiply(n, n), 1)),\n    min(minus(1, multiply(n, n)))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    min(multiply(multiply(n, n), 2)),\n    min(multiply(multiply(2, n), n)),\n    min(divide(multiply(n, n), 2)),\n    min(multiply(divide(1, n), n))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    max(plus(multiply(n, n), 1)),\n    max(plus(1, multiply(n, n))),\n    max(minus(multiply(n, n), 1)),\n    max(minus(1, multiply(n, n)))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    max(multiply(multiply(n, n), 2)),\n    max(multiply(multiply(2, n), n)),\n    max(divide(multiply(n, n), 2)),\n    max(multiply(divide(1, n), n))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    sum(plus(plus(1, n), 1)),\n    sum(plus(plus(1, 1), n)),\n    sum(minus(plus(1, n), 1)),\n    sum(minus(plus(1, 1), n))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    sum(plus(1, multiply(n, 2))),\n    sum(plus(1, multiply(2, n))),\n    sum(plus(1, divide(n, 2))),\n    sum(plus(1, divide(1, n)))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    min(plus(plus(1, n), 1)),\n    min(plus(plus(1, 1), n)),\n    min(minus(plus(1, n), 1)),\n    min(minus(plus(1, 1), n))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    min(plus(1, multiply(n, 2))),\n    min(plus(1, multiply(2, n))),\n    min(plus(1, divide(n, 2))),\n    min(plus(1, divide(1, n)))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    max(plus(plus(1, n), 1)),\n    max(plus(plus(1, 1), n)),\n    max(minus(plus(1, n), 1)),\n    max(minus(plus(1, 1), n))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    max(plus(1, multiply(n, 2))),\n    max(plus(1, multiply(2, n))),\n    max(plus(1, divide(n, 2))),\n    max(plus(1, divide(1, n)))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    sum(plus(plus(n, -1), -1)),\n    sum(plus(plus(-1, n), -1)),\n    sum(plus(minus(n, -1), -1)),\n    sum(plus(minus(-1, n), -1))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    sum(multiply(multiply(n, -2), -1)),\n    sum(multiply(multiply(-2, n), -1)),\n    sum(divide(divide(n, -2), -1)),\n    sum(divide(divide(-1, n), -1))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    min(plus(plus(n, -1), -1)),\n    min(plus(plus(-1, n), -1)),\n    min(plus(minus(n, -1), -1)),\n    min(plus(minus(-1, n), -1))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    min(multiply(multiply(n, -2), -1)),\n    min(multiply(multiply(-2, n), -1)),\n    min(divide(divide(n, -2), -1)),\n    min(divide(divide(-1, n), -1))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    max(plus(plus(n, -1), -1)),\n    max(plus(plus(-1, n), -1)),\n    max(plus(minus(n, -1), -1)),\n    max(plus(minus(-1, n), -1))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT\n    max(multiply(multiply(n, -2), -1)),\n    max(multiply(multiply(-2, n), -1)),\n    max(divide(divide(n, -2), -1)),\n    max(divide(divide(-1, n), -1))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT plus(plus(plus(sum(plus(n, 1)), sum(plus(1, n))), sum(minus(n, 1))), sum(minus(1, n)))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT plus(plus(plus(sum(multiply(n, 2)), sum(multiply(2, n))), sum(divide(n, 2))), sum(divide(1, n)))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT plus(plus(plus(min(plus(n, 1)), min(plus(1, n))), min(minus(n, 1))), min(minus(1, n)))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT plus(plus(plus(min(multiply(n, 2)), min(multiply(2, n))), min(divide(n, 2))), min(divide(1, n)))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT plus(plus(plus(max(plus(n, 1)), max(plus(1, n))), max(minus(n, 1))), max(minus(1, n)))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
SELECT plus(plus(plus(max(multiply(n, 2)), max(multiply(2, n))), max(divide(n, 2))), max(divide(1, n)))\nFROM\n(\n    SELECT number AS n\n    FROM numbers(10)\n)
55	55	35	-35
90	90	22.5	inf
1	1	-1	-8
0	0	0	0.1111111111111111
10	10	8	1
18	18	4.5	inf
35	35	55	-55
-90	-90	-22.5	-inf
-1	-1	1	-10
-18	-18	-4.5	-inf
8	8	10	-1
0	0	-0	-0.1111111111111111
30	65	25	-10
40	90	22.5	5
3	2	-2	-1
4	0	0	0.5
3	11	7	-1
4	18	4.5	0.5
55	90	0	-35
90	285	nan	inf
1	0	0	-8
0	0	1	0.1111111111111111
10	18	0	1
18	81	1	inf
295	295	275	-275
570	570	142.5	nan
1	1	-1	-80
0	0	0	1
82	82	80	1
162	162	40.5	1
65	65	45	-25
100	100	32.5	inf
2	2	0	-7
1	1	1	1.1111111111111112
11	11	9	2
19	19	5.5	inf
25	25	45	-65
90	90	22.5	inf
-2	-2	0	-11
0	0	0	0.1111111111111111
7	7	9	-2
18	18	4.5	inf
110
inf
-7
0.1111111111111111
29
inf
-15444
68.62157087543459
243
55	55	35	-35
90	90	22.5	inf
1	1	-1	-8
0	0	0	0.1111111111111111
10	10	8	1
18	18	4.5	inf
35	35	55	-55
-90	-90	-22.5	-inf
-1	-1	1	-10
-18	-18	-4.5	-inf
8	8	10	-1
0	0	-0	-0.1111111111111111
30	65	25	-10
40	90	22.5	5
3	2	-2	-1
4	0	0	0.5
3	11	7	-1
4	18	4.5	0.5
55	90	0	-35
90	285	nan	inf
1	0	0	-8
0	0	1	0.1111111111111111
10	18	0	1
18	81	1	inf
295	295	275	-275
570	570	142.5	nan
1	1	-1	-80
0	0	0	1
82	82	80	1
162	162	40.5	1
65	65	45	-25
100	100	32.5	inf
2	2	0	-7
1	1	1	1.1111111111111112
11	11	9	2
19	19	5.5	inf
25	25	45	-65
90	90	22.5	inf
-2	-2	0	-11
0	0	0	0.1111111111111111
7	7	9	-2
18	18	4.5	inf
110
inf
-7
0.1111111111111111
29
inf
-15444
68.62157087543459
243
