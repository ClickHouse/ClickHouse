-- a column CODEC accepts any spelling and stores the canonical description
a	CODEC(ZSTD(1))
b	CODEC(ZSTD(3))
c	CODEC(Delta(8), LZ4)
d	CODEC(DoubleDelta, LZ4)
e	CODEC(NONE)
f	CODEC(Default)
g	CODEC(T64(\'bit\'), LZ4)
1000	3496500
-- the codec-valued MergeTree settings
1000	499500
-- the marks codec is parsed as written on every write path: compact and wide parts, and text index
1	42
Wide
-- a keyword argument of a codec is case-insensitive too, so a setting keeps its previously valid spelling
a	CODEC(ALP(STD))
b	CODEC(ALP(AUTO))
c	CODEC(T64(\'bit\'))
d	CODEC(Delta, default)
1000	62437.5	124875	499500	499500
1000	62437.5
ALP(STD)
-- another path that takes a codec as a string
1
-- `Multiple` is still not nameable directly, in any spelling
-- an unknown codec is still unknown
