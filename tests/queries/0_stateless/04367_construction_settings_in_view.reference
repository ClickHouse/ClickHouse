-- construction settings in a VIEW definition are rejected
-- construction settings in a MATERIALIZED VIEW definition are rejected
-- a POPULATE materialized view is rejected too (not silently accepted via the immediate-insert wrapping)
-- construction settings via ALTER TABLE ... MODIFY QUERY are rejected (cannot bypass the CREATE guard)
-- a plain view works; the reader applies construction settings to its own SELECT
0
1
2
7
8
9
