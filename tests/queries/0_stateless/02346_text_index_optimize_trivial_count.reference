Inject trivial count optimization from the text index into the query plan
-- fires: single-token hasToken / hasAnyTokens / hasAllTokens
└──ReadFromTextIndexCount (Trivial count from text index (idx, token = \'alpha\'))
└──ReadFromTextIndexCount (Trivial count from text index (idx, token = \'alpha\'))
└──ReadFromTextIndexCount (Trivial count from text index (idx, token = \'alpha\'))
-- result matches the normal path
750
750
750
750
0
0
-- disabled by query_plan_optimize_count_from_text_index = 0
0
-- disabled by the parent optimize_trivial_count_query = 0
0
-- disabled by the parent query_plan_direct_read_from_text_index = 0
0
750
-- disabled for shipped plans (ReadFromTextIndexCount is not serializable)
0
0
750
-- fires: multi-token hasAnyTokens (union)
└──ReadFromTextIndexCount (Trivial count from text index (idx, tokens = [\'alpha\', \'zeta\']))
1500
1500
-- fires: multi-token hasAllTokens (intersection)
└──ReadFromTextIndexCount (Trivial count from text index (idx, tokens = [\'alpha\', \'beta\']))
500
500
0
0
-- does not fire: residual non-text predicate
0
741
-- does not fire: not a bare count()
0
Read limits: an over-limit or concurrency-capped query falls back to the reader
-- fires without a limit
1
-- within max_partitions_to_read (4 partitions <= 100): still fires, count correct
1
500
-- exceeds max_partitions_to_read (4 partitions > 1): declines, reader enforces the limit
0
-- does not fire when the table sets max_concurrent_queries; count still correct
0
500
Multi-block postings (small block size exercises read_postings, block skipping, intersection)
-- fires with a multi-block token
└──ReadFromTextIndexCount (Trivial count from text index (idx, tokens = [\'common\', \'rare\']))
-- results match the reader (each pair: optimization on, then off)
10
10
0
0
0
0
5000
5000
2500
2500
2500
2500
Array and Map carriers reach the optimization through the shared direct-read query
-- fires: has (single token)
└──ReadFromTextIndexCount (Trivial count from text index (idx, token = \'foo\'))
500
500
-- fires: hasAny (union)
└──ReadFromTextIndexCount (Trivial count from text index (idx, tokens = [\'baz\', \'foo\']))
1000
1000
-- fires: hasAll (intersection)
└──ReadFromTextIndexCount (Trivial count from text index (idx, tokens = [\'bar\', \'foo\']))
500
500
-- fires: mapContains (key)
└──ReadFromTextIndexCount (Trivial count from text index (idx_keys, token = \'k0\'))
500
500
-- fires: mapContainsValue
└──ReadFromTextIndexCount (Trivial count from text index (idx_values, token = \'v0\'))
500
500
-- the owning index is resolved per predicate: idx_keys for keys, idx_values for values
└──ReadFromTextIndexCount (Trivial count from text index (idx_keys, token = \'k0\'))
└──ReadFromTextIndexCount (Trivial count from text index (idx_values, token = \'v0\'))
-- does not fire: m[k] = v needs a residual recheck (hint mode, not exact)
0
500
Partially materialized text index
-- one part without the index, one part with it
1
1
AggregatingProjection
└──ReadFromTextIndexCount (Trivial count from text index (idx, token = \'alpha\'))
-- results match the reader
750
750
1500
1500
500
500
0
0
-- the optimization reads fewer rows: only the unindexed part
1
-- fully materialized after ALTER: back to the plain count source
0
0
└──ReadFromTextIndexCount (Trivial count from text index (idx, token = \'alpha\'))
750
-- does not fire: no part has the index
0
100
