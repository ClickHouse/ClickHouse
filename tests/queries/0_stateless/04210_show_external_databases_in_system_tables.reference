--- system.databases always shows the database, regardless of the setting ---
PostgreSQL
PostgreSQL
--- system.tables hides external databases by default (count must be 0 without contacting the server) ---
0
--- system.columns hides external databases by default ---
0
--- the old setting name still works as an alias ---
0
--- system.settings exposes both the new name and the alias ---
show_data_lake_catalogs_in_system_tables	show_external_databases_in_system_tables
show_external_databases_in_system_tables	
