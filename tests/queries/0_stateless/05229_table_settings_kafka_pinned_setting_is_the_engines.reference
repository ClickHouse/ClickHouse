-- pinned by the engine: its own value, not the collection's; the rest is still the collection's
input_format_allow_errors_num	0	other
input_format_allow_errors_ratio	0	other
kafka_max_block_size	[HIDDEN]	named_collection
-- not pinned: all are the collection's
input_format_allow_errors_num	[HIDDEN]	named_collection
input_format_allow_errors_ratio	[HIDDEN]	named_collection
kafka_max_block_size	[HIDDEN]	named_collection
