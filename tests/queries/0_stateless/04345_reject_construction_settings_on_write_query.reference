-- INSERT ... SELECT with a construction setting in the INSERT SETTINGS clause is rejected
-- INSERT ... SELECT with a construction setting on the source SELECT is rejected
-- a construction setting in a nested subquery of the source is rejected too (would be a silent no-op)
-- nothing was inserted by any rejected statement
0
-- a non-construction setting on INSERT ... SELECT still works (all 10 rows inserted)
10
-- an explicit LIMIT clause in the source SELECT is not a construction setting and still works
3
-- CREATE ... AS SELECT with a construction setting is rejected (table not created)
0
-- CREATE ... AS SELECT without construction settings still works
10
