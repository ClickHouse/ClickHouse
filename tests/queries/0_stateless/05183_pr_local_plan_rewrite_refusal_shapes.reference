window function in the shipped select list: nothing is ordered either way
Read type: Default
1000
untuple: the rewrite takes it, so the read may order itself off the condition
Read type: InOrder
Prewhere filter column:  tenant = 5
1000
limit by: the condition reaches the read and prunes it, and orders nothing
Read type: Default
Prewhere filter column:  tenant = 5
1
a with list never reaches the rewrite, so the read is ordered as the replicas order theirs
Read type: InOrder
1000
stateful select list: the condition cannot go below it, so nothing is ordered
Read type: Default
1000
non-deterministic but not stateful: the condition does go below it
Read type: InOrder
Prewhere filter column:  tenant = 5
1000
every kind of limit keeps the condition away from the read
Read type: Default
Read type: Default
Read type: Default
a named subquery is not a with list, so the condition travels and orders the read
Read type: InOrder
Prewhere filter column:  tenant = 5
1000
final: parallel replicas do not run the query at all, so there is no mode to agree on
0
