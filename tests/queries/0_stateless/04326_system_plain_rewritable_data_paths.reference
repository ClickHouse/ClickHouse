-- at least one blob is reported for the disk
1
-- remote_path always ends with directory_remote_path/name
0
-- remote_path always starts with the common blob prefix
0
-- non-root local data paths are present
1
-- a freshly written table has no ephemeral entries
0
-- a user with SELECT but without SHOW TABLES is denied
1
