-- session SET limit then INSERT ... SELECT is rejected
-- session SET offset then INSERT ... SELECT is rejected
-- session SET filter then INSERT ... SELECT is rejected
-- session SET sort then CREATE ... AS SELECT is rejected (table not created)
0
-- nothing was inserted by any rejected statement
0
-- a non-construction session setting does not block INSERT ... SELECT
10
-- with the construction settings cleared, CREATE ... AS SELECT works
10
