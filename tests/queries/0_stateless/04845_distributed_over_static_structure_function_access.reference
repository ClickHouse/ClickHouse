-- 1. explicit columns over numbers((SELECT ... FROM secret)), no access on secret: rejected at create
1
-- 1. omitted columns over numbers((SELECT ... FROM secret)), no access on secret: rejected at create
1
-- 1. explicit columns over numbers((SELECT ... FROM secret)), with access: allowed and readable
3
-- 2. Remote engine analogue, explicit columns, no access on secret: rejected at create
1
-- 2. Remote engine analogue, with access: allowed and readable
3
