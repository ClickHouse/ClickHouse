projections: p (WITH b + 1 AS y SELECT a AS x, sum(y) GROUP BY a)	indices: ix b * c TYPE minmax GRANULARITY 1	constraints: cc CHECK a > 0
ttl: d + toIntervalYear(10) GROUP BY a, b SET c = max(c)
ttl: d + toIntervalYear(10) WHERE a > 0
