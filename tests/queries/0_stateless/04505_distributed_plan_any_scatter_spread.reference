-- 1. single-source scatter under global aggregation
Expression ((Project names + Projection))
  MergingAggregated (merge)
    GatherExchange
      Aggregating (partial)
        ScatterExchange (any)
          Expression ((Before GROUP BY + Change column names to column identifiers))
            ReadFromMergeTree (default.t_any_scatter)
31996000
-- 2. scatter into a join stage over an expression join key
1280000
single source spreads:	1
multi source spreads:	1
