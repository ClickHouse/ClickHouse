-- 1. a deterministic WASM UDF in the query: NOT cacheable
-- first run: 6
-- second run (same result, the UDF really runs): 6
-- hits after a repeat (must be 0, not cached): 0
-- 2. the same query without the UDF: cacheable
-- hits after a repeat (must be 1, cached): 1
-- 3. a deterministic executable UDF in the query: NOT cacheable
-- first run: 9
-- second run (same result, the UDF really runs): 9
-- hits after a repeat (must be 0, not cached): 0
-- 4. a WASM UDF in a row-policy filter: NOT cacheable
-- first run (rows x <= 2): 2
-- second run: 2
-- hits after a repeat (must be 0, not cached): 0
