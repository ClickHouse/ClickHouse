EXPLAIN SYNTAX
SELECT number
FROM numbers(10) AS SHUFFLE;

EXPLAIN PLAN
SELECT number
FROM numbers(10) AS SHUFFLE;

EXPLAIN PLAN
SELECT number
FROM numbers(10) AS SHUFFLE
LIMIT 3;

EXPLAIN SYNTAX
SELECT number
FROM numbers(10)
SHUFFLE;

EXPLAIN PLAN
SELECT number
FROM numbers(10)
SHUFFLE;

EXPLAIN PLAN
SELECT number
FROM numbers(10)
SHUFFLE
LIMIT 3;
