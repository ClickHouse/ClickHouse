#!/usr/bin/env bash

CURDIR=$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)
# shellcheck source=../shell_config.sh
. "$CURDIR"/../shell_config.sh

echo "1234" | $CLICKHOUSE_LOCAL --format LineAsString --query "SELECT * FROM table"

