keystroke: no new history entry is pushed
keystroke: the current entry still holds the run query
keystroke: the URL is not rewritten
keystroke: only the in-memory tab moves
close: the closed tab entry carries the latest draft
close: the draft entry does not carry run=1
close+back: the recreated tab restores the draft
close+back: the run result snapshot is kept
legacy entry: Back binds it to the live tab
legacy entry: the entry is adopted with the tab id
legacy entry + close: the fold lands the draft in the adopted entry
legacy entry + close + back: the recreated tab restores the draft
close others: only the active tab survives
close others: the surviving tab keeps its draft
close others: the current entry carries the draft
close others: the URL carries the draft
close others: the draft entry does not carry run=1
close others: the run result snapshot is kept
back: a newer unrun draft is preserved
back: the editor shows the draft
back: the entry result snapshot is adopted
forward: the draft survives the round-trip
forward: the entry result snapshot is adopted
param back: the draft query is preserved
param back: the draft params are preserved
param back: the param inputs keep the draft bindings
param forward: the draft query survives the round-trip
param forward: the draft params survive the round-trip
clean param back: the entry query is restored
clean param back: the entry params are restored verbatim
clean param forward: the entry query is restored
clean param forward: the entry params are restored verbatim
back with a clean editor: the entry query is restored
forward with a clean editor: the entry query is restored
run-selected back: the older entry query is restored
run-selected forward: the full run-selected editor is restored
run-selected+draft back: the newer draft is preserved
delayed completion: the live draft survives
delayed completion: the completed run's result is still kept
delayed completion: the entry does not carry run=1
delayed completion (params changed): the query is kept
delayed completion (params changed): the live param edit survives
delayed completion (params changed): the entry does not carry run=1
run all in flight: the live draft survives the real completion
run all in flight: the result snapshot holds the launched editor text
run all in flight: the per-statement snapshots are kept
run all in flight: the first statement result is snapshotted
run all in flight: the entry carries the draft
run all in flight: the entry does not carry run=1
run all clean: the entry holds the launched text
run all clean: the entry carries run=1
run all clean: the completed run leaves flight
legacy mid-run entry: the entry the tab was left at holds no result
legacy mid-run entry (reordered params): the tab is reactivated
legacy mid-run entry (reordered params): the finished run is preserved
legacy mid-run entry (reordered params): its bindings are preserved
legacy mid-run entry (edited params): the entry is rendered as time travel
reload after a preserved draft: the draft is restored as editor text
reload after a preserved draft: the draft is not auto-run
stale reload: the draft is restored unrun
stale reload: the draft is not auto-run
stale reload: the preserved draft entry is not stamped run=1
reload after a color toggle over a draft: the draft is restored as editor text
reload after a color toggle over a draft: the draft is not auto-run
reload after a pin toggle over a draft: the draft is restored as editor text
reload after a pin toggle over a draft: the draft is not auto-run
reload of a clean run: the run query is restored
reload of a clean run: the run query is auto-run
stale reload then rerun: the draft is restored unrun
stale reload then rerun: the preserved draft entry has no run=1
stale reload then rerun: the explicit rerun entry carries run=1
stale reload then rerun: the rerun query is restored on the next reload
stale reload then rerun: the rerun query is auto-run on the next reload
stale reload then switch: the draft is restored unrun
stale reload then switch: the preserved draft entry has no run=1
stale reload then switch: the clean run=1-backed tab keeps run=1
stale reload then switch: a tab never run under run=1 stays unstamped
plain load after a run=1 session: the run entry itself carries run=1
plain load after a run=1 session: the run-backed tab is restored
plain load after a run=1 session: the restored tab is not auto-run
plain load after a run=1 session: the synced URL is not rewritten to run=1
plain load after a run=1 session: the restored tab drops the persisted marker
plain load after a run=1 session: a subsequent reload does not auto-run
plain load after a run=1 session: a subsequent reload restores the query unrun
entry-scoped run=1: the plain session entry carries no run=1
entry-scoped run=1: the run=1 session entry carries run=1
entry-scoped run=1: Back to the older plain entry clears the policy
entry-scoped run=1: Forward to the run=1 entry restores the policy
entry-scoped run=1: rewriting the run=1 entry keeps its marker
entry-scoped run=1: a structural write does not re-stamp the older plain entry
OK
