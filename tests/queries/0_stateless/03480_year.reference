-- year() equals toYear(today()), toYear(now()) and year(today())
1	1	1
-- result type is UInt16
UInt16
-- current year is a sane value
1
-- case-insensitive
1	1	1
-- year() is a constant expression (evaluated once at analysis, like today())
1
-- year(<date>) delegates to toYear across date/time types
2023	1900	2077	2149
-- year is non-deterministic and is not an alias of toYear
year	0	
-- year(<key>) keeps toYear index/monotonicity analysis (same granule pruning)
1
1	1
