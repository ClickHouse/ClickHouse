-- miss (allowed, per-column grants):
1
2
-- hits: 0
-- hit with the same column grants must stay allowed:
1
2
-- hits: 1
-- revoking the selected column on the view denies the hit:
ACCESS_DENIED
-- a denied hit evicts the entry, so the first run after re-granting is a miss that re-stores it:
1
2
-- hits: 0
-- the source table of the view's defining query is not a plan dependency, so a schema change
-- there does not invalidate the entry (the view's own leaf is re-resolved on every hit):
1
2
-- hits: 1
