CREATE TEMPORARY TABLE t_src (id UInt32, val String) ENGINE = Memory;
INSERT INTO t_src VALUES (1, 'a'), (2, 'b');

CREATE OR REPLACE TEMPORARY VIEW tview AS
SELECT id, val FROM t_src WHERE id = 1;

SELECT * FROM tview;

EXISTS TEMPORARY VIEW tview;

CREATE OR REPLACE TEMPORARY VIEW tview AS
SELECT id, upper(val) AS u FROM t_src;

SELECT * FROM tview ORDER BY id;

SHOW TEMPORARY VIEW tview;

CREATE TEMPORARY VIEW tview AS SELECT 42; -- { serverError TABLE_ALREADY_EXISTS }

CREATE OR REPLACE TEMPORARY VIEW tview AS SELECT 42;

SELECT * FROM tview;

DROP TEMPORARY VIEW tview;

CREATE OR REPLACE TEMPORARY VIEW tview AS SELECT 42 AS x;

SELECT * FROM tview;

DROP TEMPORARY VIEW tview;
DROP TEMPORARY TABLE t_src;
