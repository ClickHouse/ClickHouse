SELECT
    a,
    b,
    c,
    d,
    heavy,
    heavy2
FROM prewhere
WHERE (a = 10) AND (b = 100) AND (c = 0) AND (d = 100)
0
SELECT
    a,
    b,
    c,
    d,
    heavy,
    heavy2
FROM prewhere
PREWHERE b = 100
WHERE (b = 100) AND ((a = 10) AND (c = 0) AND (d = 100))
SELECT
    a,
    b,
    c,
    d,
    heavy,
    heavy2
FROM prewhere
PREWHERE b < 100
WHERE (b < 100) AND ((a > 0) AND (c > 0) AND (d < 100))
SELECT
    a,
    b,
    c,
    d,
    heavy,
    heavy2
FROM prewhere
PREWHERE (d > 100) AND (b > 100)
WHERE ((d > 100) AND (b > 100)) AND ((a > 0) AND (c > 0))
SELECT
    a,
    b,
    c,
    d,
    heavy,
    heavy2
FROM prewhere
PREWHERE b < 100
WHERE (b < 100) AND ((a > 0) AND (c > 0) AND (d > 100))
SELECT
    a,
    b,
    c,
    d,
    heavy,
    heavy2
FROM prewhere
PREWHERE d < 100
WHERE (d < 100) AND ((a > 0) AND (c > 0) AND (b > 100))
SELECT
    a,
    b,
    c,
    d,
    heavy,
    heavy2
FROM prewhere
WHERE (a = 10) AND (b = 100) AND (c = 0) AND (d = 100)
0
SELECT
    a,
    b,
    c,
    d,
    heavy,
    heavy2
FROM prewhere
PREWHERE b = 100
WHERE (b = 100) AND ((a = 10) AND (c = 0) AND (d = 100))
SELECT
    a,
    b,
    c,
    d,
    heavy,
    heavy2
FROM prewhere
PREWHERE b < 100
WHERE (b < 100) AND ((a > 0) AND (c > 0) AND (d < 100))
SELECT
    a,
    b,
    c,
    d,
    heavy,
    heavy2
FROM prewhere
PREWHERE (d > 100) AND (b > 100)
WHERE ((d > 100) AND (b > 100)) AND ((a > 0) AND (c > 0))
SELECT
    a,
    b,
    c,
    d,
    heavy,
    heavy2
FROM prewhere
PREWHERE b < 100
WHERE (b < 100) AND ((a > 0) AND (c > 0) AND (d > 100))
SELECT
    a,
    b,
    c,
    d,
    heavy,
    heavy2
FROM prewhere
PREWHERE d < 100
WHERE (d < 100) AND ((a > 0) AND (c > 0) AND (b > 100))
SELECT
    b,
    d
FROM prewhere
PREWHERE d < 100
WHERE (d < 100) AND ((c > 0) AND (b > 100))
SELECT
    b,
    d
FROM prewhere
PREWHERE b < 100
WHERE (b < 100) AND ((c > 0) AND (d > 100))
