-- 1. killed mutation, String -> Nullable(UInt64)
4
-- 2. killed mutation, String -> UInt64 (not Nullable-specific)
1
-- 3. JSON type hint, no mutation is ever created
0
1
-- 4. expression index, representation-preserving column conversion
0
1
-- 5. killed mutation, Int8 -> Enum8: must not prune away a value the read rejects
-- 6. killed mutation, minmax index read without a canUseIndex guard
1
50
0
-- 7. control: a representation-preserving conversion keeps pruning
1
1
-- 8. control: extending an enum keeps pruning
32
1
-- 9. control: an unaltered table keeps pruning, including the top-k read
1
1
1
63
50
1
-- 10. known gap: index name reuse after a killed DROP INDEX
0
1
-- 11. killed mutation on a column the part carries an index for but no bytes of
1
1
-- 12. control: an absent column costs no pruning when the part has no index files
1
1
1
-- 13. expression index over a timezone-dependent expression, timezone-only MODIFY COLUMN
0
3
3
3
3
3
3
-- 14. control: a simple single-column index keeps pruning across the same timezone ALTER
1
1
1
1
-- 15. the part-side type must come from the part, not from the interned description
3
3
-- 16. timezone nested inside an AggregateFunction argument
0
3
3
1
-- 17. custom type name over the same underlying type: UInt8 -> Bool
0
32
32
1
16
-- 18. control: an UNCHANGED JSON column with a typed DateTime path keeps pruning
1
1
-- 19. the carrier of a SUBCOLUMN index is its parent, whose type the cache also erases
32
32
-- 20. over-fire control: an UNCHANGED subcolumn index must keep pruning
1
16
1
16
