--- a temporarily detached table still holds the named collection ---
1
1
--- the same in an Ordinary database, where dependencies are tracked by name ---
--- a table in a detached database still holds it, too ---
--- a stale dependency of a failed CREATE TABLE does not hold it ---
0
--- in an Ordinary database, the name is all there is to identify the table by ---
0
--- and in a detached database, where the table has no metadata to be attached from ---
0
--- a dependency on another collection is not lost when a stale one is cleaned up ---
--- a permanently detached table does not hold it: it is not loaded at startup ---
0
--- check_named_collection_dependencies = 0 still allows dropping it ---
0
