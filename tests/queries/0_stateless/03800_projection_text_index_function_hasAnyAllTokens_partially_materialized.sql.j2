{% import 'text_index_to_projection.jinja' as tx %}
-- Derived from tests/queries/0_stateless/02346_text_index_function_hasAnyAllTokens_partially_materialized.sql — do not edit the
-- body here. The rewrite (skip INDEX -> PROJECTION INDEX) lives in
-- text_index_to_projection.jinja, so upstream changes to the skip-index test
-- flow straight into this projection-index twin. Upstream's own `-- Tags:` line
-- is included verbatim and picked up from the rendered .gen.sql.
SET allow_experimental_projection_text_index = 1;
{% set upstream %}{% include '02346_text_index_function_hasAnyAllTokens_partially_materialized.sql' %}{% endset %}
{{- tx.derive(upstream) }}
