-- the cap reduces streams for a narrow column --
1
-- the cap never produces zero streams --
1
-- results are identical with the cap on and off --
1
-- unknown DEFAULT dependencies do not cap streams --
1
-- the cap still applies through an unapplied RENAME COLUMN --
1
