syntax = "proto3";

message Envelope {
    message Message {
        int32 c0 = 1;
    }
    repeated Message message = 1;
}

// Same message outside envelope for verification
message Message {
    int32 c0 = 1;
}
