-- ALL INNER JOIN, duplicated right key: each left row is emitted once per right match
1	a
1	a
2	b
1	a
1	a
2	b
-- ANY INNER JOIN, duplicated left key, unique right key: left side is deduplicated
1	a
2	b
1	a
2	b
-- SEMI LEFT JOIN is convertible: each matching left row is emitted exactly once
a
a2
b
a
a2
b
-- and it does convert
1
-- while ALL and ANY do not
0
0
-- legacy any_join_distinct_right_table_keys rewrites ANY INNER to SEMI LEFT
a
a2
b
a
a2
b
1
-- a JOIN row limit is enforced whether or not the join is converted
-- and a transfer row limit does not silently truncate the result
a
a2
b
a
a2
b
-- a query that sets either limit is not converted
0
0
-- equal numeric limits are not equal behaviour: the two regimes bound different quantities
0
0
-- a differing overflow mode alone does not block the conversion
1
-- a Join engine table keeps its declared strictness check
-- a key with dynamic structure is not converted: the IN function rejects such an argument
a
b
a
b
0
-- including a Dynamic nested inside a composite key type
a
b
a
b
0
-- a key expression consuming a left column the projection still needs is not converted
1	['a','b','c']
2	['d','e']
1	['a','b','c']
2	['d','e']
0
-- the same holds for a scalar key expression, not only for arrayJoin
1	['a','b','c']
2	['d','e']
1	['a','b','c']
2	['d','e']
0
-- while a projection of only unconsumed columns still converts
1
1
