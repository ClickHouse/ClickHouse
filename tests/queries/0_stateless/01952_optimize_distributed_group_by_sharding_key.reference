Expression (Project names)
  Distinct (DISTINCT)
    Union
      Expression
        Distinct (Preliminary DISTINCT)
          Expression ((Projection + (Change column names to column identifiers + (Convert VIEW subquery result to VIEW table structure + (Project names + (Projection + (Change column names to column identifiers + (Project names + (Projection + Change column names to column identifiers)))))))))
            ReadFromSystemNumbers
      ReadFromRemote (Read from remote replica)
Union
  Expression (( + Project names))
    Distinct (DISTINCT)
      Distinct (Preliminary DISTINCT)
        Expression ((Projection + (Change column names to column identifiers + (Convert VIEW subquery result to VIEW table structure + (Project names + (Projection + (Change column names to column identifiers + (Project names + (Projection + Change column names to column identifiers)))))))))
          ReadFromSystemNumbers
  ReadFromRemote (Read from remote replica)
Expression (Project names)
  LimitBy
    Union
      Expression ((Before LIMIT BY + ))
        LimitBy
          Expression ((Before LIMIT BY + (Projection + (Change column names to column identifiers + (Convert VIEW subquery result to VIEW table structure + (Project names + (Projection + (Change column names to column identifiers + (Project names + (Projection + Change column names to column identifiers))))))))))
            ReadFromSystemNumbers
      Expression (Before LIMIT BY)
        ReadFromRemote (Read from remote replica)
Union
  Expression (( + Project names))
    LimitBy
      Expression ((Before LIMIT BY + (Projection + (Change column names to column identifiers + (Convert VIEW subquery result to VIEW table structure + (Project names + (Projection + (Change column names to column identifiers + (Project names + (Projection + Change column names to column identifiers))))))))))
        ReadFromSystemNumbers
  ReadFromRemote (Read from remote replica)
Expression (Project names)
  Distinct (DISTINCT)
    Sorting (Merge sorted streams for ORDER BY, without aggregation)
      Union
        Expression
          Sorting (Sorting for ORDER BY)
            Expression (Before ORDER BY)
              Distinct (Preliminary DISTINCT)
                Expression ((Projection + (Change column names to column identifiers + (Convert VIEW subquery result to VIEW table structure + (Project names + (Projection + (Change column names to column identifiers + (Project names + (Projection + Change column names to column identifiers)))))))))
                  ReadFromSystemNumbers
        ReadFromRemote (Read from remote replica)
Expression (Project names)
  Sorting (Merge sorted streams after aggregation stage for ORDER BY)
    Union
      Expression
        Distinct (DISTINCT)
          Sorting (Sorting for ORDER BY)
            Expression (Before ORDER BY)
              Distinct (Preliminary DISTINCT)
                Expression ((Projection + (Change column names to column identifiers + (Convert VIEW subquery result to VIEW table structure + (Project names + (Projection + (Change column names to column identifiers + (Project names + (Projection + Change column names to column identifiers)))))))))
                  ReadFromSystemNumbers
      ReadFromRemote (Read from remote replica)
Expression (Project names)
  LimitBy
    Expression (Before LIMIT BY)
      Sorting (Merge sorted streams for ORDER BY, without aggregation)
        Union
          Expression
            LimitBy
              Expression (Before LIMIT BY)
                Sorting (Sorting for ORDER BY)
                  Expression ((Before ORDER BY + (Projection + (Change column names to column identifiers + (Convert VIEW subquery result to VIEW table structure + (Project names + (Projection + (Change column names to column identifiers + (Project names + (Projection + Change column names to column identifiers))))))))))
                    ReadFromSystemNumbers
          ReadFromRemote (Read from remote replica)
Expression (Project names)
  Sorting (Merge sorted streams after aggregation stage for ORDER BY)
    Union
      Expression
        LimitBy
          Expression (Before LIMIT BY)
            Sorting (Sorting for ORDER BY)
              Expression ((Before ORDER BY + (Projection + (Change column names to column identifiers + (Convert VIEW subquery result to VIEW table structure + (Project names + (Projection + (Change column names to column identifiers + (Project names + (Projection + Change column names to column identifiers))))))))))
                ReadFromSystemNumbers
      ReadFromRemote (Read from remote replica)
