# Note, this file is ignorelist file [1] not suppressions [2].
#
#  [1]: https://clang.llvm.org/docs/SanitizerSpecialCaseList.html
#  [2]: https://clang.llvm.org/docs/UndefinedBehaviorSanitizer.html#runtime-suppressions
#
# See also [3] for all UBSan checks.
#
#   [3]: https://github.com/llvm-mirror/compiler-rt/blob/master/lib/ubsan/ubsan_checks.inc
#
# Caveats for generic entry "fun":
# - does not work for __attribute__((__always_inline__))
# - and may not work for functions that had been inlined
# - requires asterisk at the beginning *and* end for static functions
#
[undefined]
# Some value is outside the range of representable values of type 'long' on user-provided data inside boost::geometry - ignore.
src:*/Functions/pointInPolygon.cpp
src:*/contrib/boost/boost/geometry/*

# We don't want to receive sanitizer alerts from third-party libraries during fuzzing
src:*/contrib/contrib/protobuf/*
src:*/contrib/libprotobuf-mutator/*

[unsigned-integer-overflow]
# Unsigned integer overflow is well-defined behaviour, and third-party code uses it liberally -
# in hash functions, checksums, pseudo-random number generators and bignum arithmetic. Compiled
# third-party code gets `-fno-sanitize=unsigned-integer-overflow` from `contrib/CMakeLists.txt`,
# but header-only third-party code is compiled as a part of our own translation units, so it has
# to be excluded here.
src:*/contrib/*

# Third-party code vendored under `base/`: Poco (digests, hash functions, number parsing),
# the PCG pseudo-random number generators, musl-derived string and math functions.
src:*/base/poco/*
src:*/base/pcg-random/*
src:*/base/glibc-compatibility/*
src:*/base/darwin-compatibility/*
src:*/base/widechar_width/*

# Third-party 128-bit and 256-bit integer arithmetic. Every operation on `UInt128`/`UInt256`
# is a chain of wrapping additions and multiplications of 64-bit limbs.
src:*/base/base/wide_integer_impl.h
