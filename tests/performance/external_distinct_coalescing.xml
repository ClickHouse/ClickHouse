<test>
    <!-- Small input chunks make run merging expensive. Explicit thresholds force spilling while
         leaving room to combine chunks before sorting and removing duplicates. -->
    <settings>
        <max_threads>16</max_threads>
        <max_memory_usage>4294967296</max_memory_usage>
        <max_bytes_ratio_before_external_distinct>0</max_bytes_ratio_before_external_distinct>
        <optimize_distinct_in_order>0</optimize_distinct_in_order>
        <allow_preliminary_distinct_abandoning>1</allow_preliminary_distinct_abandoning>
        <prefer_external_sort_block_bytes>1048576</prefer_external_sort_block_bytes>
    </settings>
    <query>SELECT count() FROM (SELECT DISTINCT cityHash64(number) AS k FROM numbers(4000000)) SETTINGS max_block_size = 4096, max_bytes_before_external_distinct = 33554432</query>
    <query>SELECT count(), sum(k) FROM (SELECT DISTINCT number % 131072 AS k FROM numbers(4000000)) SETTINGS max_block_size = 4096, max_bytes_before_external_distinct = 33554432</query>
    <query>SELECT count(), sum(length(k)) FROM (SELECT DISTINCT toFixedString(toString(number % 32768), 128) AS k FROM numbers(1048576)) SETTINGS max_block_size = 1024, max_bytes_before_external_distinct = 33554432</query>
    <query>SELECT count(), sum(length(k)) FROM (SELECT DISTINCT toFixedString(toString(number % 262144), 128) AS k FROM numbers(1048576)) SETTINGS max_block_size = 1024, max_bytes_before_external_distinct = 67108864</query>
    <!-- Small batches expose the cost of collecting chunks and accounting for each column. -->
    <query>SELECT count() FROM (SELECT DISTINCT cityHash64(number) AS k FROM numbers(4000000)) SETTINGS max_block_size = 128, max_bytes_before_external_distinct = 33554432</query>
    <query>SELECT count(), sum(k0) FROM (SELECT DISTINCT toUInt64(number) AS k0, cityHash64(number, 1) AS k1, cityHash64(number, 2) AS k2, cityHash64(number, 3) AS k3, cityHash64(number, 4) AS k4, cityHash64(number, 5) AS k5, cityHash64(number, 6) AS k6, cityHash64(number, 7) AS k7 FROM numbers(1048576)) SETTINGS max_block_size = 128, max_bytes_before_external_distinct = 33554432</query>
</test>
