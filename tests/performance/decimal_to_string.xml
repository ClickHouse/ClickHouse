<test>
    <!-- Formatting of the fractional part of a Decimal: a field of a known width, which is
         written in blocks of digits rather than one division per digit.
         `reinterpret` is used to get a random underlying representation, so that the fractional
         part uses its full width; `toDecimal*` of an integer would leave it zero. -->

    <query>SELECT count() FROM zeros(20000000) WHERE NOT ignore(toString(reinterpret(rand(), 'Decimal32(2)'))) SETTINGS max_threads = 1</query>
    <query>SELECT count() FROM zeros(20000000) WHERE NOT ignore(toString(reinterpret(rand(), 'Decimal32(9)'))) SETTINGS max_threads = 1</query>
    <query>SELECT count() FROM zeros(20000000) WHERE NOT ignore(toString(reinterpret(rand64(), 'Decimal64(6)'))) SETTINGS max_threads = 1</query>
    <query>SELECT count() FROM zeros(20000000) WHERE NOT ignore(toString(reinterpret(rand64(), 'Decimal64(18)'))) SETTINGS max_threads = 1</query>

    <!-- Wide scales, where the old implementation needed one big-integer division per digit.
         The row counts fall off sharply here, and deliberately so: the harness runs every query
         on the reference server as well, and allows any single run at most two seconds. The
         reference needs 0.16 us per row at `Decimal128(38)` and 7.5 to 25 us per row at
         `Decimal256`, so these counts are what keeps the reference side under the budget:
         raising them makes the job time out on the reference rather than measure anything. -->
    <query>SELECT count() FROM zeros(10000000) WHERE NOT ignore(toString(reinterpret(rand64(), 'Decimal128(20)'))) SETTINGS max_threads = 1</query>
    <query>SELECT count() FROM zeros(2000000) WHERE NOT ignore(toString(reinterpret(reinterpretAsUInt128(randomString(16)), 'Decimal128(38)'))) SETTINGS max_threads = 1</query>
    <query>SELECT count() FROM zeros(50000) WHERE NOT ignore(toString(reinterpret(reinterpretAsUInt256(randomString(32)), 'Decimal256(40)'))) SETTINGS max_threads = 1</query>
    <query>SELECT count() FROM zeros(15000) WHERE NOT ignore(toString(reinterpret(reinterpretAsUInt256(randomString(32)), 'Decimal256(76)'))) SETTINGS max_threads = 1</query>

    <!-- `toDecimalString` keeps the trailing zeros, so the whole width is always written -->
    <query>SELECT count() FROM zeros(10000000) WHERE NOT ignore(toDecimalString(reinterpret(rand64(), 'Decimal64(18)'), 18)) SETTINGS max_threads = 1</query>
    <query>SELECT count() FROM zeros(50000) WHERE NOT ignore(toDecimalString(reinterpret(reinterpretAsUInt256(randomString(32)), 'Decimal256(40)'), 40)) SETTINGS max_threads = 1</query>

    <!-- Through an output format rather than through toString -->
    <query>SELECT count() FROM zeros(10000000) WHERE NOT ignore(formatRow('TSV', reinterpret(rand64(), 'Decimal64(9)'))) SETTINGS max_threads = 1</query>
</test>
