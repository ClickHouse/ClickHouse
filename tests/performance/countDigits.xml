<test>
    <query>SELECT count() FROM hits_10m_single WHERE NOT ignore(countDigits(RegionID))</query>
    <query>SELECT count() FROM hits_10m_single WHERE NOT ignore(countDigits(AdvEngineID))</query>
    <query>SELECT count() FROM hits_10m_single WHERE NOT ignore(countDigits(ClientIP))</query>
    <query>SELECT count() FROM hits_10m_single WHERE NOT ignore(countDigits(WatchID))</query>
</test>
