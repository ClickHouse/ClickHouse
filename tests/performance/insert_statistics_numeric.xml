<test>
    <!-- The no-statistics baselines and per-statistic benchmarks use separate
         target tables so measured queries do not share state. -->
    <settings>
        <allow_statistics>1</allow_statistics>
        <allow_suspicious_low_cardinality_types>1</allow_suspicious_low_cardinality_types>
        <max_insert_threads>1</max_insert_threads>
    </settings>

    <substitutions>
        <substitution>
            <name>statistic</name>
            <values>
                <value>basic</value>
                <value>countmin</value>
                <value>minmax</value>
                <value>tdigest</value>
                <value>uniq</value>
            </values>
        </substitution>
    </substitutions>

    <create_query>
        CREATE TABLE t_statistics_low_cardinality_numeric_src(
            c1 UInt64,
            c2 UInt32,
            c3 LowCardinality(UInt32))
        ENGINE = Memory;
    </create_query>

    <create_query>
        CREATE TABLE t_statistics_numeric_regular_none(
            c1 UInt64,
            c2 UInt32)
        ENGINE = MergeTree ORDER BY c1 SETTINGS auto_statistics_types = '', materialize_statistics_on_merge = 0;
    </create_query>

    <create_query>
        CREATE TABLE t_statistics_numeric_low_cardinality_none(
            c1 UInt64,
            c2 LowCardinality(UInt32))
        ENGINE = MergeTree ORDER BY c1 SETTINGS auto_statistics_types = '', materialize_statistics_on_merge = 0;
    </create_query>

    <create_query>
        CREATE TABLE t_statistics_numeric_regular_{statistic}(
            c1 UInt64,
            c2 UInt32 STATISTICS({statistic}))
        ENGINE = MergeTree ORDER BY c1 SETTINGS auto_statistics_types = '', materialize_statistics_on_merge = 0;
    </create_query>

    <create_query>
        CREATE TABLE t_statistics_numeric_low_cardinality_{statistic}(
            c1 UInt64,
            c2 LowCardinality(UInt32) STATISTICS({statistic}))
        ENGINE = MergeTree ORDER BY c1 SETTINGS auto_statistics_types = '', materialize_statistics_on_merge = 0;
    </create_query>

    <fill_query>
        INSERT INTO t_statistics_low_cardinality_numeric_src
        SELECT
            number,
            toUInt32(number % 100000),
            toUInt32(number % 100000)
        FROM numbers(1000000)
    </fill_query>

    <fill_query>SYSTEM STOP MERGES t_statistics_numeric_regular_none</fill_query>
    <fill_query>SYSTEM STOP MERGES t_statistics_numeric_low_cardinality_none</fill_query>
    <fill_query>SYSTEM STOP MERGES t_statistics_numeric_regular_{statistic}</fill_query>
    <fill_query>SYSTEM STOP MERGES t_statistics_numeric_low_cardinality_{statistic}</fill_query>

    <query>
        INSERT INTO t_statistics_numeric_regular_none
        SELECT c1, c2 FROM t_statistics_low_cardinality_numeric_src
        SETTINGS materialize_statistics_on_insert = 1
    </query>

    <query>
        INSERT INTO t_statistics_numeric_low_cardinality_none
        SELECT c1, c3 FROM t_statistics_low_cardinality_numeric_src
        SETTINGS materialize_statistics_on_insert = 1
    </query>

    <query>
        INSERT INTO t_statistics_numeric_regular_{statistic}
        SELECT c1, c2 FROM t_statistics_low_cardinality_numeric_src
        SETTINGS materialize_statistics_on_insert = 1
    </query>

    <query>
        INSERT INTO t_statistics_numeric_low_cardinality_{statistic}
        SELECT c1, c3 FROM t_statistics_low_cardinality_numeric_src
        SETTINGS materialize_statistics_on_insert = 1
    </query>

    <drop_query>DROP TABLE IF EXISTS t_statistics_numeric_regular_none</drop_query>
    <drop_query>DROP TABLE IF EXISTS t_statistics_numeric_low_cardinality_none</drop_query>
    <drop_query>DROP TABLE IF EXISTS t_statistics_numeric_regular_{statistic}</drop_query>
    <drop_query>DROP TABLE IF EXISTS t_statistics_numeric_low_cardinality_{statistic}</drop_query>
    <drop_query>DROP TABLE IF EXISTS t_statistics_low_cardinality_numeric_src</drop_query>
</test>
