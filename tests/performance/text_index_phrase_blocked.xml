<test>
  <settings>
      <use_skip_indexes_on_data_read>1</use_skip_indexes_on_data_read>
      <query_plan_direct_read_from_text_index>1</query_plan_direct_read_from_text_index>
      <!-- Keep every phrase on the positional index so the candidate-driven path is measured. -->
      <text_index_hint_max_selectivity>1.</text_index_hint_max_selectivity>
  </settings>

  <fill_query>ALTER TABLE hits_10m_single MODIFY SETTING allow_experimental_text_index_phrase_search = 1</fill_query>
  <fill_query>ALTER TABLE hits_10m_single ADD INDEX idx_sp_blocked(SearchPhrase) TYPE text(tokenizer = 'splitByNonAlpha', support_phrase_search = 1)</fill_query>
  <!-- Building positions makes this mutation slow; wait for it so the benchmark queries measure
       the fully materialized index and the teardown DROP INDEX does not race with it. -->
  <fill_query>ALTER TABLE hits_10m_single MATERIALIZE INDEX idx_sp_blocked SETTINGS mutations_sync = 1</fill_query>

  <!-- Phrases ordered by decreasing frequency: candidate-heavy down to no candidates. -->
  <query>SELECT count() FROM hits_10m_single WHERE hasPhrase(SearchPhrase, 'video game')</query>
  <query>SELECT count() FROM hits_10m_single WHERE hasPhrase(SearchPhrase, 'world of')</query>
  <query>SELECT count() FROM hits_10m_single WHERE hasPhrase(SearchPhrase, 'the world')</query>
  <query>SELECT count() FROM hits_10m_single WHERE hasPhrase(SearchPhrase, 'google mail')</query>
  <query>SELECT count() FROM hits_10m_single WHERE hasPhrase(SearchPhrase, 'master video')</query>
  <query>SELECT count() FROM hits_10m_single WHERE hasPhrase(SearchPhrase, 'grand world')</query>
  <query>SELECT count() FROM hits_10m_single WHERE hasPhrase(SearchPhrase, 'puma oxygen')</query>
  <query>SELECT count() FROM hits_10m_single WHERE hasPhrase(SearchPhrase, 'acompletelynonexistentwordxyz market')</query>

  <drop_query>ALTER TABLE hits_10m_single DROP INDEX idx_sp_blocked</drop_query>
</test>
