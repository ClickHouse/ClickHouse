<test>
    <create_query>
        create table webpages (
            url String,
            title SimpleAggregateFunction(anyLast, Nullable(String)),
            content SimpleAggregateFunction(anyLast, Nullable(String))
        )
        engine = AggregatingMergeTree order by url;
    </create_query>

    <create_query>system stop merges webpages;</create_query>

    <fill_query>
        insert into webpages
            select toString(number % 100), randomString(100),randomString(4000) from numbers(100000)
        settings optimize_on_insert = 0;
    </fill_query>

    <query>select * from webpages final format Null settings max_threads=1;</query>

    <drop_query>drop table webpages;</drop_query>
</test>
