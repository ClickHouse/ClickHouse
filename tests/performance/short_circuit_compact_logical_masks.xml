<test>
    <settings>
        <short_circuit_function_evaluation>force_enable</short_circuit_function_evaluation>
        <max_threads>1</max_threads>
        <max_block_size>65536</max_block_size>
        <compile_expressions>0</compile_expressions>
    </settings>

    <!-- Sparse, mixed and dense masks; every result is consumed without output overhead. -->
    <query>SELECT and(number % 64 = 0, bitAnd(cityHash64(number), 1), bitAnd(cityHash64(number + 1), 1)) FROM numbers(10000000) FORMAT Null</query>
    <query>SELECT or(number % 64 != 0, bitAnd(cityHash64(number), 1), bitAnd(cityHash64(number + 1), 1)) FROM numbers(10000000) FORMAT Null</query>
    <query>SELECT and(number % 2 = 0, bitAnd(cityHash64(number), 1), bitAnd(cityHash64(number + 1), 1)) FROM numbers(10000000) FORMAT Null</query>
    <query>SELECT or(number % 2 != 0, bitAnd(cityHash64(number), 1), bitAnd(cityHash64(number + 1), 1)) FROM numbers(10000000) FORMAT Null</query>
    <query>SELECT and(number % 64 != 0, bitAnd(cityHash64(number), 1), bitAnd(cityHash64(number + 1), 1)) FROM numbers(10000000) FORMAT Null</query>
    <query>SELECT or(number % 64 = 0, bitAnd(cityHash64(number), 1), bitAnd(cityHash64(number + 1), 1)) FROM numbers(10000000) FORMAT Null</query>

    <!-- NULL must keep a row active until a decisive false (AND) or true (OR). -->
    <query>SELECT and(number % 8 = 0, nullIf(bitAnd(cityHash64(number), 3), 2), bitAnd(cityHash64(number + 1), 1)) FROM numbers(10000000) FORMAT Null</query>
    <query>SELECT or(number % 8 != 0, nullIf(bitAnd(cityHash64(number), 3), 2), bitAnd(cityHash64(number + 1), 1)) FROM numbers(10000000) FORMAT Null</query>
</test>
