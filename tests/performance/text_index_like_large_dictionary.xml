<test>
  <settings>
    <use_query_condition_cache>0</use_query_condition_cache>
    <max_threads>1</max_threads>
    <optimize_rewrite_like_perfect_affix>0</optimize_rewrite_like_perfect_affix>
  </settings>

  <!-- The dictionary of text_index_like.xml holds ~20 tokens, i.e. one dictionary block, so neither the
       block range a pattern is narrowed to nor the number of matcher calls per block is visible there.
       Here every row of a part carries its own token, giving 500008 dictionary tokens over 977 blocks, and
       the scan runs once per part, so one insert per part and 30 of them is what puts the anchored queries
       above the 0.1 s the harness asks for. Merges are pinned off to keep that part count.
       'toxx' deliberately holds no 'k': an ILIKE needle containing one is refused before the dictionary
       scan starts, because U+212A folds onto 'k'. -->
  <create_query>
    CREATE TABLE tab
    (
        id     UInt64,
        message String,
        INDEX  idx(message) TYPE text(tokenizer = 'splitByNonAlpha')
    )
    ENGINE = MergeTree
    ORDER BY id
    SETTINGS max_bytes_to_merge_at_max_space_in_pool = 1
  </create_query>

  <fill_query>INSERT INTO tab SELECT number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 1 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 2 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 3 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 4 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 5 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 6 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 7 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 8 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 9 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 10 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 11 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 12 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 13 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 14 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 15 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 16 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 17 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 18 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 19 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 20 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 21 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 22 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 23 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 24 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 25 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 26 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 27 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 28 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>
  <fill_query>INSERT INTO tab SELECT 29 * 500000 + number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>

  <query>SELECT count() FROM tab WHERE message LIKE '%toxx12%'</query>
  <query>SELECT count() FROM tab WHERE message ILIKE '%TOXX12%'</query>

  <query>SELECT count() FROM tab WHERE message LIKE 'toxx12%'</query>
  <query>SELECT count() FROM tab WHERE message LIKE '%toxx12'</query>

  <query>SELECT count() FROM tab WHERE startsWith(message, 'toxx12')</query>
  <query>SELECT count() FROM tab WHERE endsWith(message, 'toxx12')</query>

  <!-- This literal occurs in 500000 of the 500008 tokens, the other eight being the words every message
       shares, so almost no token is filtered out and the scan pays the filter on top of the matcher calls it
       still makes. One part keeps the query inside the harness's window. Each matching token has a single
       posting, hence an embedded one, which never charges the postings budget; the budget is raised anyway so
       the query keeps measuring the whole scan rather than an early bypass. -->
  <create_query>
    CREATE TABLE tab_dense
    (
        id     UInt64,
        message String,
        INDEX  idx(message) TYPE text(tokenizer = 'splitByNonAlpha')
    )
    ENGINE = MergeTree
    ORDER BY id
    SETTINGS max_bytes_to_merge_at_max_space_in_pool = 1
  </create_query>

  <fill_query>INSERT INTO tab_dense SELECT number, concat('toxx', toString(number), ' the quick brown fox jumps over the lazy dog') FROM numbers(500000) SETTINGS max_insert_block_size = 1000000</fill_query>

  <query>SELECT count() FROM tab_dense WHERE message LIKE '%toxx%' SETTINGS text_index_like_max_postings_to_read = 1000000</query>

  <drop_query>DROP TABLE IF EXISTS tab</drop_query>
  <drop_query>DROP TABLE IF EXISTS tab_dense</drop_query>
</test>
