<test>
    <query>
        WITH
            (
                SELECT bitmapBuild(groupArray(number))
                FROM numbers(8000000)
            ) AS a,
            [a, a, a] AS b
        SELECT sum(bitmapCardinality(b[(number % 3) + 1]))
        FROM numbers(27000)
    </query>
</test>
