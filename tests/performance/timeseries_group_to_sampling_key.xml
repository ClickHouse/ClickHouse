<test>
    <settings>
        <max_insert_threads>8</max_insert_threads>
    </settings>

    <!--
        Benchmarks materializing sampling keys for a repeated vector of groups.
        The query is intentionally large enough for the temporary sampling-key
        vector and the final UInt64 column to be visible in the result.
    -->
    <query>
        WITH
            (
                SELECT groupArray(group)
                FROM
                (
                    SELECT number,
                           timeSeriesTagsToGroup([('tag', toString(number))]) AS group
                    FROM numbers(500000)
                    ORDER BY number
                )
            ) AS groups
        SELECT timeSeriesGroupToSamplingKey(groups[number % 500000 + 1])
        FROM numbers(5000000)
        SETTINGS max_threads = 1, max_block_size = 500000
        FORMAT Null
    </query>
</test>
