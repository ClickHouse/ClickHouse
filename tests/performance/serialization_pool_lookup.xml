<test>
    <!--
        GROUP BY over a Dynamic key selects AggregationMethodSerialized, and
        ColumnDynamic::serializeValueIntoArena calls getDefaultSerialization for every row,
        so each row performs a SerializationObjectPool lookup from every aggregation thread.
        This measures the pool's hit path under contention.
    -->
    <settings>
        <max_threads>16</max_threads>
        <allow_suspicious_types_in_group_by>1</allow_suspicious_types_in_group_by>
    </settings>

    <!-- A single type: every lookup uses the same pool key. -->
    <query>SELECT (number % 100000)::Dynamic AS d FROM numbers_mt(5000000) GROUP BY d FORMAT Null</query>

    <!-- Mixed types: lookups are spread over several pool keys. -->
    <query>SELECT if(number % 2 = 0, (number % 100000)::Dynamic, toString(number % 100000)::Dynamic) AS d FROM numbers_mt(5000000) GROUP BY d FORMAT Null</query>

    <!--
        Multi-threaded MergeTree read of a deeply nested schema: every read task constructs
        a reader, which rebuilds the serialization of each requested subcolumn through the pool
        at every nesting level. Small granularity and frozen merges keep the number of read
        tasks high, so the reader threads contend on the pool.
    -->
    <create_query>
        CREATE TABLE nested_events
        (
            event Tuple(
                bidtime Nullable(Int64),
                sourceid Nullable(Int64),
                liveEvent Bool,
                targeting Map(String, Array(Nullable(String))),
                slots Array(Tuple(
                    bidId Nullable(String),
                    directDeals Array(Tuple(
                        activeDeal Nullable(Bool),
                        bidderIds Array(Nullable(Int64)),
                        currency Nullable(String),
                        dealCPM Nullable(Int64),
                        dealId Nullable(String),
                        wseats Array(Nullable(String)),
                        dealTerms Nullable(String))),
                    dtbMaxBid Nullable(Int64))))
        )
        ENGINE = MergeTree ORDER BY tuple()
        SETTINGS index_granularity = 28, max_bytes_to_merge_at_max_space_in_pool = 1
    </create_query>

    <fill_query>
        INSERT INTO nested_events
        SELECT tuple(
            toInt64(1783987200000 + (number % 86000) * 1000),
            toInt64(cityHash64(number) % 1000),
            true,
            map('geo', [toNullable(if(cityHash64(number, 1) % 4 = 0, 'DE', 'US'))], 'ipGeo', [toNullable('US')]),
            arrayMap(si -> tuple(
                toNullable(concat('bid_', toString(cityHash64(number, si) % 1000))),
                arrayMap(di -> tuple(
                    toNullable(true),
                    arrayMap(bi -> toNullable(toInt64(cityHash64(number, si, di, bi) % 50)), range(intDiv(cityHash64(number, si, di, 11) % 4, 3))),
                    toNullable('USD'),
                    toNullable(toInt64(cityHash64(number, si, di, 1) % 100)),
                    toNullable(if(cityHash64(number, si, di, 2) % 50000 = 0, 'target_deal', concat('deal_', toString(cityHash64(number, si, di, 2) % 300)))),
                    arrayMap(wi -> toNullable(concat('seat_', toString(cityHash64(number, si, di, wi, 6) % 20))), range(intDiv(cityHash64(number, si, di, 6) % 4, 3))),
                    toNullable(concat('terms_', toString(cityHash64(number, si, di, 7) % 12), '_', repeat('x', 200)))
                ), range(cityHash64(number, si, 8) % 3)),
                toNullable(toInt64(cityHash64(number, si, 9) % 10000))
            ), range(1 + number % 2))
        )
        FROM numbers_mt(4000000)
        SETTINGS min_insert_block_size_rows = 10000, max_insert_block_size = 10000, max_insert_threads = 4
    </fill_query>

    <query>
        SELECT count() FROM nested_events
        PREWHERE has(arrayFlatten(arrayMap(s -> s.dealId, event.slots.directDeals)), 'target_deal')
        WHERE (has(event.targeting['geo'], 'US') OR has(event.targeting['ipGeo'], 'US')) AND event.liveEvent = true
    </query>

    <drop_query>DROP TABLE IF EXISTS nested_events</drop_query>
</test>
