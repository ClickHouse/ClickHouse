<test>
    <!-- Shouldn't have been a perf test, but an EXPLAIN one. -->
    <query>SELECT sumIf(1, 0) FROM numbers(250000000)</query>
    <query>SELECT sumIf(1, 1) FROM numbers(250000000)</query>
</test>
