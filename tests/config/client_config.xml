<!-- Config set into /etc/clickhouse-client/. It's used if no other configs are found. -->
<config>
    <openSSL>
        <client>
            <loadDefaultCAFile>true</loadDefaultCAFile>
            <cacheSessions>true</cacheSessions>
            <disableProtocols>sslv2,sslv3</disableProtocols>
            <preferServerCiphers>true</preferServerCiphers>
            <invalidCertificateHandler>
                <name>AcceptCertificateHandler</name>  <!-- For tests only-->
            </invalidCertificateHandler>
            <no_max_memory_usage_for_client>1</no_max_memory_usage_for_client> <!-- May be turn it off, but need to add this flag in some distributed tests-->
        </client>
    </openSSL>
</config>
