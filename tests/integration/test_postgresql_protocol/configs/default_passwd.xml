 <clickhouse>
    <profiles>
        <default>
        </default>
    </profiles>

     <users>
         <default>
             <password>123</password>
         </default>
         <user_with_sha256>
            <!-- echo -n abacaba | openssl dgst -sha256 !-->
             <password_scram_sha256_hex>abacaba</password_scram_sha256_hex>
             <networks incl="networks" replace="replace">
                 <ip>::/0</ip>
             </networks>
             <profile>default</profile>
             <quota>default</quota>
         </user_with_sha256>
     </users>
 </clickhouse>
