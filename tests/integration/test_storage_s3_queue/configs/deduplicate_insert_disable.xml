<clickhouse>
    <profiles>
        <default>
            <!-- Overrides the `async_insert_deduplicate` the queue sets for its insert into the dependent views. -->
            <deduplicate_insert>disable</deduplicate_insert>
        </default>
    </profiles>
</clickhouse>
