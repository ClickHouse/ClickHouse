<clickhouse>
    <profiles>
        <default>
            <!-- These tests exercise user-facing S3Queue access that intentionally uses server-managed
                 credentials (environment credentials and role_arn-based STS assume-role). That is blocked
                 for user queries by default; allow it so the feature itself can be tested. -->
            <s3_allow_server_credentials_in_user_queries>1</s3_allow_server_credentials_in_user_queries>
        </default>
    </profiles>
</clickhouse>
