<clickhouse>
    <profiles>
        <default>
            <!-- Pretend to be a pre-26.6 server. The 26.6 release introduced the
                 `v1_with_codec` text index header format; under an older `compatibility`
                 the `text_index_serialization_version` MergeTree setting reverts to `v0_initial`, so a
                 freshly upgraded server keeps writing the old on-disk format that
                 pre-26.6 servers can still read during a rolling upgrade. -->
            <compatibility>26.5</compatibility>
        </default>
    </profiles>
</clickhouse>
