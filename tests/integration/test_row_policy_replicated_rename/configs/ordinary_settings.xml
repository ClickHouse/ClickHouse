<clickhouse>
    <profiles>
        <default>
            <allow_experimental_database_replicated>1</allow_experimental_database_replicated>
            <!-- The startup conversion of an Ordinary database to Atomic is a chain of renames, so
                 it exercises the preflight before the access storages are fully reloaded. -->
            <allow_deprecated_database_ordinary>1</allow_deprecated_database_ordinary>
        </default>
    </profiles>
</clickhouse>
