services:
    openbao:
        image: openbao/openbao:2.3
        restart: always
        environment:
            BAO_DEV_ROOT_TOKEN_ID: foobar
            BAO_DEV_LISTEN_ADDRESS: "0.0.0.0:8200"
        ports:
            - "8200:8200"
