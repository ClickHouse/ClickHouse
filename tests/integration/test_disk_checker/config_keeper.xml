<clickhouse>
    <!-- Small period so several probe rounds happen while the test runs. -->
    <local_disk_check_period_ms>1</local_disk_check_period_ms>
    <path>/var/lib/clickhouse/</path>

    <storage_configuration>
        <disks>
            <shared_with_keeper>
                <type>local</type>
                <path>/var/lib/clickhouse/path_shared_with_keeper/</path>
            </shared_with_keeper>
        </disks>
    </storage_configuration>

    <!-- KeeperContext::initializeDisks reads the same disks subtree, so the disk above
         gets a second DiskLocal object with its own check thread. -->
    <keeper_server>
        <tcp_port>2181</tcp_port>
        <server_id>1</server_id>
        <log_storage_path>/var/lib/clickhouse/coordination/log</log_storage_path>
        <snapshot_storage_path>/var/lib/clickhouse/coordination/snapshots</snapshot_storage_path>
        <coordination_settings>
            <session_timeout_ms>20000</session_timeout_ms>
        </coordination_settings>
        <raft_configuration>
            <server>
                <id>1</id>
                <hostname>localhost</hostname>
                <port>9444</port>
            </server>
        </raft_configuration>
    </keeper_server>
</clickhouse>
