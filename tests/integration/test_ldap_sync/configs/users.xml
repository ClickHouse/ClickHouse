<clickhouse>
    <users>
        <!-- No `default` user: every query names its user, and interserver queries must not fall back to it. -->
        <default remove="remove">
        </default>
        <admin>
            <password></password>
            <access_management>1</access_management>
            <readonly>0</readonly>
        </admin>
    </users>
</clickhouse>
