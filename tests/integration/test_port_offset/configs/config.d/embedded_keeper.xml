<clickhouse>
    <!-- Embedded Keeper with the default keeper_server.use_cluster = 1 and no <zookeeper> section:
         the server's own Keeper client synthesizes its endpoints from keeper_server.tcp_port, which
         must carry port_offset like the listener does (9181 + 100 = 9281). -->
    <keeper_server>
        <tcp_port>9181</tcp_port>
        <server_id>1</server_id>
        <log_storage_path>/var/lib/clickhouse/coordination/log</log_storage_path>
        <snapshot_storage_path>/var/lib/clickhouse/coordination/snapshots</snapshot_storage_path>
        <data_storage_path>/var/lib/clickhouse/coordination/data</data_storage_path>
        <coordination_settings>
            <operation_timeout_ms>5000</operation_timeout_ms>
            <session_timeout_ms>10000</session_timeout_ms>
            <raft_logs_level>trace</raft_logs_level>
        </coordination_settings>
        <raft_configuration>
            <server>
                <id>1</id>
                <hostname>node_embedded_keeper</hostname>
                <port>9234</port>
            </server>
        </raft_configuration>
    </keeper_server>
</clickhouse>
