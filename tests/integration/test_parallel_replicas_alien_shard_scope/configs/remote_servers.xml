<clickhouse>
    <logger>
        <level>trace</level>
    </logger>

    <remote_servers>
        <!-- The `Distributed` fan-out. Shard 2 has a single replica, and it is the initiator's own node, so
             that shard is dispatched as a local plan and carries `_shard_num = 2` as a special scalar.
             Shard 1 has 3 replicas so task-based parallel replicas stay enabled for the query. -->
        <test_alien_fanout>
            <shard>
                <internal_replication>true</internal_replication>
                <replica>
                    <host>n2</host>
                    <port>9000</port>
                </replica>
                <replica>
                    <host>n3</host>
                    <port>9000</port>
                </replica>
                <replica>
                    <host>n4</host>
                    <port>9000</port>
                </replica>
            </shard>
            <shard>
                <internal_replication>true</internal_replication>
                <replica>
                    <host>n1</host>
                    <port>9000</port>
                </replica>
            </shard>
        </test_alien_fanout>

        <!-- `cluster_for_parallel_replicas` points here: one shard, unrelated to the fan-out above. The
             shard scope of the fan-out says nothing about this cluster and must not be applied to it. -->
        <test_alien_pr_cluster>
            <shard>
                <internal_replication>true</internal_replication>
                <replica>
                    <host>n1</host>
                    <port>9000</port>
                </replica>
                <replica>
                    <host>n2</host>
                    <port>9000</port>
                </replica>
                <replica>
                    <host>n3</host>
                    <port>9000</port>
                </replica>
            </shard>
        </test_alien_pr_cluster>

        <!-- Same shard count as the fan-out, but a different layout: its shard 2 is `n3` and `n4`, not the
             initiator. A `_shard_num = 2` / `_shard_count = 2` pair from the fan-out is indistinguishable from
             this cluster's own by the numbers alone, so applying it here would make the initiator's shard read
             the wrong replica set. -->
        <test_alien_pr_cluster_same_shard_count>
            <shard>
                <internal_replication>true</internal_replication>
                <replica>
                    <host>n1</host>
                    <port>9000</port>
                </replica>
                <replica>
                    <host>n2</host>
                    <port>9000</port>
                </replica>
            </shard>
            <shard>
                <internal_replication>true</internal_replication>
                <replica>
                    <host>n3</host>
                    <port>9000</port>
                </replica>
                <replica>
                    <host>n4</host>
                    <port>9000</port>
                </replica>
            </shard>
        </test_alien_pr_cluster_same_shard_count>
    </remote_servers>
</clickhouse>
