<clickhouse>
    <profiles>
        <default>
            <!-- Several tests here exercise user-facing S3 access that intentionally uses server-managed
                 credentials: environment/IMDS credentials, role_arn STS, and auth headers/keys inherited
                 from the server `<s3>` config. Those are blocked for user queries by default; allow them. -->
            <s3_allow_server_credentials_in_user_queries>1</s3_allow_server_credentials_in_user_queries>
        </default>
    </profiles>
</clickhouse>
