<clickhouse>
    <profiles>
        <default>
            <!-- This test verifies that S3 access can use the instance metadata (IMDS) credentials, which
                 is server-managed and blocked for user queries by default. Allow it here. -->
            <s3_allow_server_credentials_in_user_queries>1</s3_allow_server_credentials_in_user_queries>
        </default>
    </profiles>
</clickhouse>
