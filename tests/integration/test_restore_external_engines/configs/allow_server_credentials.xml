<clickhouse>
    <profiles>
        <default>
            <!-- This test exercises S3 access that relies on server-managed credentials (environment, IMDS,
                 or a `from_env` value), which user queries cannot use by default. Allow it for this test. -->
            <s3_allow_server_credentials_in_user_queries>1</s3_allow_server_credentials_in_user_queries>
        </default>
    </profiles>
</clickhouse>
