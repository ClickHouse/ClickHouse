<clickhouse>
    <ldap_servers>
        <openldap>
            <host>openldap</host>
            <port>1389</port>
            <bind_dn>cn={user_name},ou=users,dc=example,dc=org</bind_dn>
            <enable_tls>no</enable_tls>
            <lookup_bind_dn>cn=admin,dc=example,dc=org</lookup_bind_dn>
            <lookup_password>clickhouse</lookup_password>
            <user_dn_detection>
                <base_dn>ou=users,dc=example,dc=org</base_dn>
                <search_filter>(&amp;(objectClass=person)(cn={user_name}))</search_filter>
            </user_dn_detection>
        </openldap>
    </ldap_servers>
    <!-- The `ldap` directory precedes `users_xml`, which defines `ghost` locally (see
         `users_with_local_ghost.xml`): a phantom `ghost` that the interserver path materialises in the
         LDAP storage shadows the local user, and `EXECUTE AS ghost` must hand the resolution over to
         `users_xml` once the directory refuses to confirm the name. -->
    <user_directories replace="replace">
        <ldap>
            <server>openldap</server>
            <role_mapping>
                <base_dn>dc=example,dc=org</base_dn>
                <scope>subtree</scope>
                <search_filter>(&amp;(objectClass=groupOfNames)(member={bind_dn}))</search_filter>
                <attribute>cn</attribute>
                <prefix>clickhouse-</prefix>
            </role_mapping>
        </ldap>
        <users_xml>
            <path>users.xml</path>
        </users_xml>
    </user_directories>
    <remote_servers>
        <ldap_cluster_first>
            <secret>foo</secret>
            <node>
                <host>shard1_node</host>
                <port>9000</port>
            </node>
            <node>
                <host>shard_ldap_first</host>
                <port>9000</port>
            </node>
        </ldap_cluster_first>
    </remote_servers>
</clickhouse>
