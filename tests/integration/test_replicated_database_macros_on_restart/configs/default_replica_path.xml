<clickhouse>
    <!-- The packaged config.xml uses {database}/{table}; the test needs {shard} in the path. -->
    <default_replica_path>/clickhouse/tables/{uuid}/{shard}</default_replica_path>
    <default_replica_name>{replica}</default_replica_name>
</clickhouse>
