<clickhouse>
    <compression>
        <case>
            <!-- ZXC is experimental but safe for untyped data. The selector must still reject it,
                 because the server-level `enable_zxc_codec` policy is not enabled here. -->
            <method>ZXC</method>
        </case>
    </compression>
</clickhouse>
