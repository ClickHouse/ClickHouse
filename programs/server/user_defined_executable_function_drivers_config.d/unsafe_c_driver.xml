<clickhouse>
    <driver>
        <!-- Source-tree example only. It is not installed by packages. -->
        <!-- Name of the driver as it appears in `CREATE FUNCTION ... ENGINE = UnsafeC(...)`. -->
        <name>UnsafeC</name>

        <!-- The driver receives the function name, return type, argument signature, and body. -->
        <!-- Relative paths are resolved against this configuration file. -->
        <create_command>../user_defined_executable_function_drivers/unsafe_c_create.sh</create_command>

        <drop_command>../user_defined_executable_function_drivers/unsafe_c_drop.sh</drop_command>

        <!--
            This driver does not declare any additional engine arguments. Arguments declared here
            would appear as e.g. ENGINE = UnsafeC(opt_level='2') and would be forwarded to
            the driver.
        -->
        <!-- <engine_arguments> -->
        <!--     <opt_level><required>false</required></opt_level> -->
        <!-- </engine_arguments> -->
    </driver>
</clickhouse>
