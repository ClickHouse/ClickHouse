#pragma once
#include "Types.h"

namespace DB::PostgreSQL {
        enum class NodeType : size_t
        {
            Stmnts = 0
        };

        // NodeType GetNodeType(const JSON::Element& node) {

        // }

        // bool IsStatementsNode(const JSON::Object& node) {
            
        // }
}
