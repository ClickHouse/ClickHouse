#pragma once

/// Future `ALTER SHARD` will live here (mirror `ASTCreateShardQuery` / `ASTDropShardQuery` and `ASTAlterNamedCollectionQuery`).
/// Parser, interpreter, and `InterpreterFactory` routing are not implemented yet.
