sudo: required
dist: trusty

os: linux

cache:
  ccache: true
  directories:
  - /var/cache/pbuilder

addons:
  apt:
    packages: [ pbuilder, fakeroot, debhelper ]

script:
  - env TEST_RUN=1 ./release --pbuilder
